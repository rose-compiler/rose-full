// MACHINE GENERATED SOURCE FILE WITH ROSE (Grammar.h)--- DO NOT MODIFY!

#include "sage3basic.h"

#ifndef ROSE_USE_INTERNAL_FRONTEND_DEVELOPMENT
   #include "AST_FILE_IO.h"
#endif 
// The header file ("rose_config.h") should only be included by source files that require it.
#include "rose_config.h"

#if _MSC_VER
#define USE_CPP_NEW_DELETE_OPERATORS 0
#endif

#define ROSE_ALLOC_TRACE 0
#undef mprintf
#define mprintf Rose::Diagnostics::mfprintf(Rose::ir_node_mlog[Rose::Diagnostics::DEBUG])

#include <Rose/BinaryAnalysis/RegisterDictionary.h>

using namespace std;

#include "Cxx_GrammarMemoryPoolSupport.h"

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNode::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNode objectArray [] = *(Memory_Block_List.begin());
     if (SgNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNode** objectArray = (SgNode**) &(SgNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNode::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNode*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNode::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNode objectArray [] = *(Memory_Block_List.begin());
     if (SgNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNode** objectArray = (SgNode**) &(SgNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNode object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNode object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNode::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNode objectArray [] = *(Memory_Block_List.begin());
     if (SgNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNode** objectArray = (SgNode**) &(SgNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNode::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNode::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNode found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNode::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNode::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNode** objectArray = (SgNode**) &(SgNode::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNode::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNode::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNode);

     return memory;
   }



/* #line 256 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSupport::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSupport objectArray [] = *(Memory_Block_List.begin());
     if (SgSupport::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSupport** objectArray = (SgSupport**) &(SgSupport::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSupport::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSupport::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSupport::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSupport*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSupport::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSupport::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSupport::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSupport objectArray [] = *(Memory_Block_List.begin());
     if (SgSupport::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSupport** objectArray = (SgSupport**) &(SgSupport::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSupport::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSupport::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSupport object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSupport object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSupport::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSupport objectArray [] = *(Memory_Block_List.begin());
     if (SgSupport::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSupport** objectArray = (SgSupport**) &(SgSupport::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSupport::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSupport::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSupport found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSupport::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSupport::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSupport** objectArray = (SgSupport**) &(SgSupport::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSupport::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSupport::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSupport::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSupport);

     return memory;
   }



/* #line 488 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModifier** objectArray = (SgModifier**) &(SgModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModifier** objectArray = (SgModifier**) &(SgModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModifier** objectArray = (SgModifier**) &(SgModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgModifier** objectArray = (SgModifier**) &(SgModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgModifier);

     return memory;
   }



/* #line 720 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgModifierNodes::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgModifierNodes objectArray [] = *(Memory_Block_List.begin());
     if (SgModifierNodes::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModifierNodes** objectArray = (SgModifierNodes**) &(SgModifierNodes::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModifierNodes::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgModifierNodes::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgModifierNodes::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgModifierNodes*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgModifierNodes::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModifierNodes::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgModifierNodes::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModifierNodes objectArray [] = *(Memory_Block_List.begin());
     if (SgModifierNodes::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModifierNodes** objectArray = (SgModifierNodes**) &(SgModifierNodes::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModifierNodes::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModifierNodes::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgModifierNodes object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgModifierNodes object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgModifierNodes::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModifierNodes objectArray [] = *(Memory_Block_List.begin());
     if (SgModifierNodes::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModifierNodes** objectArray = (SgModifierNodes**) &(SgModifierNodes::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgModifierNodes::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgModifierNodes::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgModifierNodes found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModifierNodes::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgModifierNodes::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgModifierNodes** objectArray = (SgModifierNodes**) &(SgModifierNodes::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgModifierNodes::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgModifierNodes::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModifierNodes::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgModifierNodes);

     return memory;
   }



/* #line 952 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgConstVolatileModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgConstVolatileModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgConstVolatileModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConstVolatileModifier** objectArray = (SgConstVolatileModifier**) &(SgConstVolatileModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgConstVolatileModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgConstVolatileModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgConstVolatileModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgConstVolatileModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgConstVolatileModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgConstVolatileModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgConstVolatileModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgConstVolatileModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgConstVolatileModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConstVolatileModifier** objectArray = (SgConstVolatileModifier**) &(SgConstVolatileModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgConstVolatileModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgConstVolatileModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgConstVolatileModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgConstVolatileModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgConstVolatileModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgConstVolatileModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgConstVolatileModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConstVolatileModifier** objectArray = (SgConstVolatileModifier**) &(SgConstVolatileModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgConstVolatileModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgConstVolatileModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgConstVolatileModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgConstVolatileModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgConstVolatileModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgConstVolatileModifier** objectArray = (SgConstVolatileModifier**) &(SgConstVolatileModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgConstVolatileModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgConstVolatileModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgConstVolatileModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgConstVolatileModifier);

     return memory;
   }



/* #line 1184 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgStorageModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgStorageModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgStorageModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStorageModifier** objectArray = (SgStorageModifier**) &(SgStorageModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStorageModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgStorageModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgStorageModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgStorageModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgStorageModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStorageModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgStorageModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStorageModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgStorageModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStorageModifier** objectArray = (SgStorageModifier**) &(SgStorageModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStorageModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStorageModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgStorageModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgStorageModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgStorageModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStorageModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgStorageModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStorageModifier** objectArray = (SgStorageModifier**) &(SgStorageModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgStorageModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgStorageModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgStorageModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStorageModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgStorageModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgStorageModifier** objectArray = (SgStorageModifier**) &(SgStorageModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgStorageModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgStorageModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStorageModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgStorageModifier);

     return memory;
   }



/* #line 1416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAccessModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAccessModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgAccessModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAccessModifier** objectArray = (SgAccessModifier**) &(SgAccessModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAccessModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAccessModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAccessModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAccessModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAccessModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAccessModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAccessModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAccessModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgAccessModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAccessModifier** objectArray = (SgAccessModifier**) &(SgAccessModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAccessModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAccessModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAccessModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAccessModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAccessModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAccessModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgAccessModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAccessModifier** objectArray = (SgAccessModifier**) &(SgAccessModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAccessModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAccessModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAccessModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAccessModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAccessModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAccessModifier** objectArray = (SgAccessModifier**) &(SgAccessModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAccessModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAccessModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAccessModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAccessModifier);

     return memory;
   }



/* #line 1648 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFunctionModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFunctionModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionModifier** objectArray = (SgFunctionModifier**) &(SgFunctionModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFunctionModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFunctionModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFunctionModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFunctionModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFunctionModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionModifier** objectArray = (SgFunctionModifier**) &(SgFunctionModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFunctionModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFunctionModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFunctionModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionModifier** objectArray = (SgFunctionModifier**) &(SgFunctionModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFunctionModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFunctionModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFunctionModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFunctionModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFunctionModifier** objectArray = (SgFunctionModifier**) &(SgFunctionModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFunctionModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFunctionModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFunctionModifier);

     return memory;
   }



/* #line 1880 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUPC_AccessModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUPC_AccessModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgUPC_AccessModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUPC_AccessModifier** objectArray = (SgUPC_AccessModifier**) &(SgUPC_AccessModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUPC_AccessModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUPC_AccessModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUPC_AccessModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUPC_AccessModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUPC_AccessModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUPC_AccessModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUPC_AccessModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUPC_AccessModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgUPC_AccessModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUPC_AccessModifier** objectArray = (SgUPC_AccessModifier**) &(SgUPC_AccessModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUPC_AccessModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUPC_AccessModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUPC_AccessModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUPC_AccessModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUPC_AccessModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUPC_AccessModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgUPC_AccessModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUPC_AccessModifier** objectArray = (SgUPC_AccessModifier**) &(SgUPC_AccessModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUPC_AccessModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUPC_AccessModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUPC_AccessModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUPC_AccessModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUPC_AccessModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUPC_AccessModifier** objectArray = (SgUPC_AccessModifier**) &(SgUPC_AccessModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUPC_AccessModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUPC_AccessModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUPC_AccessModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUPC_AccessModifier);

     return memory;
   }



/* #line 2112 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSpecialFunctionModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSpecialFunctionModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgSpecialFunctionModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSpecialFunctionModifier** objectArray = (SgSpecialFunctionModifier**) &(SgSpecialFunctionModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSpecialFunctionModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSpecialFunctionModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSpecialFunctionModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSpecialFunctionModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSpecialFunctionModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSpecialFunctionModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSpecialFunctionModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSpecialFunctionModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgSpecialFunctionModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSpecialFunctionModifier** objectArray = (SgSpecialFunctionModifier**) &(SgSpecialFunctionModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSpecialFunctionModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSpecialFunctionModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSpecialFunctionModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSpecialFunctionModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSpecialFunctionModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSpecialFunctionModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgSpecialFunctionModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSpecialFunctionModifier** objectArray = (SgSpecialFunctionModifier**) &(SgSpecialFunctionModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSpecialFunctionModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSpecialFunctionModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSpecialFunctionModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSpecialFunctionModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSpecialFunctionModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSpecialFunctionModifier** objectArray = (SgSpecialFunctionModifier**) &(SgSpecialFunctionModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSpecialFunctionModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSpecialFunctionModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSpecialFunctionModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSpecialFunctionModifier);

     return memory;
   }



/* #line 2344 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgElaboratedTypeModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgElaboratedTypeModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgElaboratedTypeModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElaboratedTypeModifier** objectArray = (SgElaboratedTypeModifier**) &(SgElaboratedTypeModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElaboratedTypeModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgElaboratedTypeModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgElaboratedTypeModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgElaboratedTypeModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgElaboratedTypeModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElaboratedTypeModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgElaboratedTypeModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElaboratedTypeModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgElaboratedTypeModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElaboratedTypeModifier** objectArray = (SgElaboratedTypeModifier**) &(SgElaboratedTypeModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElaboratedTypeModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElaboratedTypeModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgElaboratedTypeModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgElaboratedTypeModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgElaboratedTypeModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElaboratedTypeModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgElaboratedTypeModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElaboratedTypeModifier** objectArray = (SgElaboratedTypeModifier**) &(SgElaboratedTypeModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgElaboratedTypeModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgElaboratedTypeModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgElaboratedTypeModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElaboratedTypeModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgElaboratedTypeModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgElaboratedTypeModifier** objectArray = (SgElaboratedTypeModifier**) &(SgElaboratedTypeModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgElaboratedTypeModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgElaboratedTypeModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElaboratedTypeModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgElaboratedTypeModifier);

     return memory;
   }



/* #line 2576 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLinkageModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLinkageModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgLinkageModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLinkageModifier** objectArray = (SgLinkageModifier**) &(SgLinkageModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLinkageModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLinkageModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLinkageModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLinkageModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLinkageModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLinkageModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLinkageModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLinkageModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgLinkageModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLinkageModifier** objectArray = (SgLinkageModifier**) &(SgLinkageModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLinkageModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLinkageModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLinkageModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLinkageModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLinkageModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLinkageModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgLinkageModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLinkageModifier** objectArray = (SgLinkageModifier**) &(SgLinkageModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLinkageModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLinkageModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLinkageModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLinkageModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLinkageModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLinkageModifier** objectArray = (SgLinkageModifier**) &(SgLinkageModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLinkageModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLinkageModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLinkageModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLinkageModifier);

     return memory;
   }



/* #line 2808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBaseClassModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBaseClassModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgBaseClassModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBaseClassModifier** objectArray = (SgBaseClassModifier**) &(SgBaseClassModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBaseClassModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBaseClassModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBaseClassModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBaseClassModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBaseClassModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBaseClassModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBaseClassModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBaseClassModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgBaseClassModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBaseClassModifier** objectArray = (SgBaseClassModifier**) &(SgBaseClassModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBaseClassModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBaseClassModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBaseClassModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBaseClassModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBaseClassModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBaseClassModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgBaseClassModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBaseClassModifier** objectArray = (SgBaseClassModifier**) &(SgBaseClassModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBaseClassModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBaseClassModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBaseClassModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBaseClassModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBaseClassModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBaseClassModifier** objectArray = (SgBaseClassModifier**) &(SgBaseClassModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBaseClassModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBaseClassModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBaseClassModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBaseClassModifier);

     return memory;
   }



/* #line 3040 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgStructureModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgStructureModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgStructureModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStructureModifier** objectArray = (SgStructureModifier**) &(SgStructureModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStructureModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgStructureModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgStructureModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgStructureModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgStructureModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStructureModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgStructureModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStructureModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgStructureModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStructureModifier** objectArray = (SgStructureModifier**) &(SgStructureModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStructureModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStructureModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgStructureModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgStructureModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgStructureModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStructureModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgStructureModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStructureModifier** objectArray = (SgStructureModifier**) &(SgStructureModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgStructureModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgStructureModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgStructureModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStructureModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgStructureModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgStructureModifier** objectArray = (SgStructureModifier**) &(SgStructureModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgStructureModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgStructureModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStructureModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgStructureModifier);

     return memory;
   }



/* #line 3272 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeModifier** objectArray = (SgTypeModifier**) &(SgTypeModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeModifier** objectArray = (SgTypeModifier**) &(SgTypeModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeModifier** objectArray = (SgTypeModifier**) &(SgTypeModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeModifier** objectArray = (SgTypeModifier**) &(SgTypeModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeModifier);

     return memory;
   }



/* #line 3504 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDeclarationModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDeclarationModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgDeclarationModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeclarationModifier** objectArray = (SgDeclarationModifier**) &(SgDeclarationModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeclarationModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDeclarationModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDeclarationModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDeclarationModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDeclarationModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeclarationModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDeclarationModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeclarationModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgDeclarationModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeclarationModifier** objectArray = (SgDeclarationModifier**) &(SgDeclarationModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeclarationModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeclarationModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDeclarationModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDeclarationModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDeclarationModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeclarationModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgDeclarationModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeclarationModifier** objectArray = (SgDeclarationModifier**) &(SgDeclarationModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDeclarationModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDeclarationModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDeclarationModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeclarationModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDeclarationModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDeclarationModifier** objectArray = (SgDeclarationModifier**) &(SgDeclarationModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDeclarationModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDeclarationModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeclarationModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDeclarationModifier);

     return memory;
   }



/* #line 3736 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOpenclAccessModeModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOpenclAccessModeModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgOpenclAccessModeModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOpenclAccessModeModifier** objectArray = (SgOpenclAccessModeModifier**) &(SgOpenclAccessModeModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOpenclAccessModeModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOpenclAccessModeModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOpenclAccessModeModifier::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOpenclAccessModeModifier*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOpenclAccessModeModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOpenclAccessModeModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOpenclAccessModeModifier::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOpenclAccessModeModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgOpenclAccessModeModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOpenclAccessModeModifier** objectArray = (SgOpenclAccessModeModifier**) &(SgOpenclAccessModeModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOpenclAccessModeModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOpenclAccessModeModifier::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOpenclAccessModeModifier object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOpenclAccessModeModifier object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOpenclAccessModeModifier::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOpenclAccessModeModifier objectArray [] = *(Memory_Block_List.begin());
     if (SgOpenclAccessModeModifier::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOpenclAccessModeModifier** objectArray = (SgOpenclAccessModeModifier**) &(SgOpenclAccessModeModifier::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOpenclAccessModeModifier::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOpenclAccessModeModifier::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOpenclAccessModeModifier found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOpenclAccessModeModifier::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOpenclAccessModeModifier::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOpenclAccessModeModifier** objectArray = (SgOpenclAccessModeModifier**) &(SgOpenclAccessModeModifier::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOpenclAccessModeModifier::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOpenclAccessModeModifier::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOpenclAccessModeModifier::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOpenclAccessModeModifier);

     return memory;
   }



/* #line 3968 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgName::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgName objectArray [] = *(Memory_Block_List.begin());
     if (SgName::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgName** objectArray = (SgName**) &(SgName::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgName::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgName::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgName::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgName*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgName::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgName::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgName::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgName objectArray [] = *(Memory_Block_List.begin());
     if (SgName::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgName** objectArray = (SgName**) &(SgName::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgName::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgName::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgName object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgName object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgName::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgName objectArray [] = *(Memory_Block_List.begin());
     if (SgName::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgName** objectArray = (SgName**) &(SgName::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgName::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgName::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgName found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgName::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgName::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgName** objectArray = (SgName**) &(SgName::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgName::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgName::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgName::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgName);

     return memory;
   }



/* #line 4200 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSymbolTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSymbolTable objectArray [] = *(Memory_Block_List.begin());
     if (SgSymbolTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSymbolTable** objectArray = (SgSymbolTable**) &(SgSymbolTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSymbolTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSymbolTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSymbolTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSymbolTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSymbolTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSymbolTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSymbolTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSymbolTable objectArray [] = *(Memory_Block_List.begin());
     if (SgSymbolTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSymbolTable** objectArray = (SgSymbolTable**) &(SgSymbolTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSymbolTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSymbolTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSymbolTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSymbolTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSymbolTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSymbolTable objectArray [] = *(Memory_Block_List.begin());
     if (SgSymbolTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSymbolTable** objectArray = (SgSymbolTable**) &(SgSymbolTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSymbolTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSymbolTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSymbolTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSymbolTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSymbolTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSymbolTable** objectArray = (SgSymbolTable**) &(SgSymbolTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSymbolTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSymbolTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSymbolTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSymbolTable);

     return memory;
   }



/* #line 4432 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAttribute::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAttribute** objectArray = (SgAttribute**) &(SgAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAttribute::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAttribute*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAttribute::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAttribute** objectArray = (SgAttribute**) &(SgAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAttribute object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAttribute object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAttribute::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAttribute** objectArray = (SgAttribute**) &(SgAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAttribute::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAttribute::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAttribute found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAttribute::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAttribute** objectArray = (SgAttribute**) &(SgAttribute::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAttribute::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAttribute::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAttribute);

     return memory;
   }



/* #line 4664 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPragma::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPragma objectArray [] = *(Memory_Block_List.begin());
     if (SgPragma::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPragma** objectArray = (SgPragma**) &(SgPragma::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPragma::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPragma::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPragma::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPragma*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPragma::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPragma::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPragma::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPragma objectArray [] = *(Memory_Block_List.begin());
     if (SgPragma::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPragma** objectArray = (SgPragma**) &(SgPragma::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPragma::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPragma::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPragma object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPragma object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPragma::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPragma objectArray [] = *(Memory_Block_List.begin());
     if (SgPragma::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPragma** objectArray = (SgPragma**) &(SgPragma::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPragma::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPragma::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPragma found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPragma::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPragma::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPragma** objectArray = (SgPragma**) &(SgPragma::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPragma::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPragma::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPragma::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPragma);

     return memory;
   }



/* #line 4896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBitAttribute::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBitAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgBitAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitAttribute** objectArray = (SgBitAttribute**) &(SgBitAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBitAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBitAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBitAttribute::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBitAttribute*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBitAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBitAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBitAttribute::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBitAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgBitAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitAttribute** objectArray = (SgBitAttribute**) &(SgBitAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBitAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBitAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBitAttribute object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBitAttribute object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBitAttribute::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBitAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgBitAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitAttribute** objectArray = (SgBitAttribute**) &(SgBitAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBitAttribute::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBitAttribute::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBitAttribute found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBitAttribute::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBitAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBitAttribute** objectArray = (SgBitAttribute**) &(SgBitAttribute::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBitAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBitAttribute::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBitAttribute::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBitAttribute);

     return memory;
   }



/* #line 5128 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFuncDecl_attr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFuncDecl_attr objectArray [] = *(Memory_Block_List.begin());
     if (SgFuncDecl_attr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFuncDecl_attr** objectArray = (SgFuncDecl_attr**) &(SgFuncDecl_attr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFuncDecl_attr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFuncDecl_attr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFuncDecl_attr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFuncDecl_attr*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFuncDecl_attr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFuncDecl_attr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFuncDecl_attr::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFuncDecl_attr objectArray [] = *(Memory_Block_List.begin());
     if (SgFuncDecl_attr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFuncDecl_attr** objectArray = (SgFuncDecl_attr**) &(SgFuncDecl_attr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFuncDecl_attr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFuncDecl_attr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFuncDecl_attr object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFuncDecl_attr object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFuncDecl_attr::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFuncDecl_attr objectArray [] = *(Memory_Block_List.begin());
     if (SgFuncDecl_attr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFuncDecl_attr** objectArray = (SgFuncDecl_attr**) &(SgFuncDecl_attr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFuncDecl_attr::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFuncDecl_attr::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFuncDecl_attr found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFuncDecl_attr::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFuncDecl_attr::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFuncDecl_attr** objectArray = (SgFuncDecl_attr**) &(SgFuncDecl_attr::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFuncDecl_attr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFuncDecl_attr::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFuncDecl_attr::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFuncDecl_attr);

     return memory;
   }



/* #line 5360 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgClassDecl_attr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgClassDecl_attr objectArray [] = *(Memory_Block_List.begin());
     if (SgClassDecl_attr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassDecl_attr** objectArray = (SgClassDecl_attr**) &(SgClassDecl_attr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassDecl_attr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgClassDecl_attr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgClassDecl_attr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgClassDecl_attr*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgClassDecl_attr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassDecl_attr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgClassDecl_attr::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassDecl_attr objectArray [] = *(Memory_Block_List.begin());
     if (SgClassDecl_attr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassDecl_attr** objectArray = (SgClassDecl_attr**) &(SgClassDecl_attr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassDecl_attr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassDecl_attr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgClassDecl_attr object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgClassDecl_attr object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgClassDecl_attr::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassDecl_attr objectArray [] = *(Memory_Block_List.begin());
     if (SgClassDecl_attr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassDecl_attr** objectArray = (SgClassDecl_attr**) &(SgClassDecl_attr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgClassDecl_attr::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgClassDecl_attr::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgClassDecl_attr found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassDecl_attr::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgClassDecl_attr::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgClassDecl_attr** objectArray = (SgClassDecl_attr**) &(SgClassDecl_attr::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgClassDecl_attr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgClassDecl_attr::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassDecl_attr::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgClassDecl_attr);

     return memory;
   }



/* #line 5592 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
Sg_File_Info::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // Sg_File_Info objectArray [] = *(Memory_Block_List.begin());
     if (Sg_File_Info::pools.empty() == false)
        {
       // Generate an array of memory pools
          Sg_File_Info** objectArray = (Sg_File_Info**) &(Sg_File_Info::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < Sg_File_Info::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < Sg_File_Info::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of Sg_File_Info::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<Sg_File_Info*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < Sg_File_Info::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < Sg_File_Info::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
Sg_File_Info::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // Sg_File_Info objectArray [] = *(Memory_Block_List.begin());
     if (Sg_File_Info::pools.empty() == false)
        {
       // Generate an array of memory pools
          Sg_File_Info** objectArray = (Sg_File_Info**) &(Sg_File_Info::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < Sg_File_Info::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < Sg_File_Info::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid Sg_File_Info object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID Sg_File_Info object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
Sg_File_Info::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // Sg_File_Info objectArray [] = *(Memory_Block_List.begin());
     if (Sg_File_Info::pools.empty() == false)
        {
       // Generate an array of memory pools
          Sg_File_Info** objectArray = (Sg_File_Info**) &(Sg_File_Info::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < Sg_File_Info::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < Sg_File_Info::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for Sg_File_Info found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
Sg_File_Info::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (Sg_File_Info::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          Sg_File_Info** objectArray = (Sg_File_Info**) &(Sg_File_Info::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < Sg_File_Info::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < Sg_File_Info::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
Sg_File_Info::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(Sg_File_Info);

     return memory;
   }



/* #line 5824 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFile::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFile objectArray [] = *(Memory_Block_List.begin());
     if (SgFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFile** objectArray = (SgFile**) &(SgFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFile::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFile*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFile::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFile objectArray [] = *(Memory_Block_List.begin());
     if (SgFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFile** objectArray = (SgFile**) &(SgFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFile object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFile object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFile::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFile objectArray [] = *(Memory_Block_List.begin());
     if (SgFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFile** objectArray = (SgFile**) &(SgFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFile::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFile::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFile found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFile::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFile::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFile** objectArray = (SgFile**) &(SgFile::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFile::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFile::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFile);

     return memory;
   }



/* #line 6056 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSourceFile::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSourceFile objectArray [] = *(Memory_Block_List.begin());
     if (SgSourceFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSourceFile** objectArray = (SgSourceFile**) &(SgSourceFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSourceFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSourceFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSourceFile::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSourceFile*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSourceFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSourceFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSourceFile::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSourceFile objectArray [] = *(Memory_Block_List.begin());
     if (SgSourceFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSourceFile** objectArray = (SgSourceFile**) &(SgSourceFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSourceFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSourceFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSourceFile object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSourceFile object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSourceFile::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSourceFile objectArray [] = *(Memory_Block_List.begin());
     if (SgSourceFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSourceFile** objectArray = (SgSourceFile**) &(SgSourceFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSourceFile::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSourceFile::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSourceFile found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSourceFile::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSourceFile::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSourceFile** objectArray = (SgSourceFile**) &(SgSourceFile::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSourceFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSourceFile::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSourceFile::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSourceFile);

     return memory;
   }



/* #line 6288 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUnknownFile::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUnknownFile objectArray [] = *(Memory_Block_List.begin());
     if (SgUnknownFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnknownFile** objectArray = (SgUnknownFile**) &(SgUnknownFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnknownFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUnknownFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUnknownFile::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUnknownFile*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUnknownFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnknownFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUnknownFile::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnknownFile objectArray [] = *(Memory_Block_List.begin());
     if (SgUnknownFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnknownFile** objectArray = (SgUnknownFile**) &(SgUnknownFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnknownFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnknownFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUnknownFile object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUnknownFile object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUnknownFile::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnknownFile objectArray [] = *(Memory_Block_List.begin());
     if (SgUnknownFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnknownFile** objectArray = (SgUnknownFile**) &(SgUnknownFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUnknownFile::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUnknownFile::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUnknownFile found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnknownFile::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUnknownFile::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUnknownFile** objectArray = (SgUnknownFile**) &(SgUnknownFile::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUnknownFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUnknownFile::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnknownFile::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUnknownFile);

     return memory;
   }



/* #line 6520 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBinaryComposite::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBinaryComposite objectArray [] = *(Memory_Block_List.begin());
     if (SgBinaryComposite::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBinaryComposite** objectArray = (SgBinaryComposite**) &(SgBinaryComposite::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBinaryComposite::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBinaryComposite::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBinaryComposite::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBinaryComposite*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBinaryComposite::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBinaryComposite::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBinaryComposite::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBinaryComposite objectArray [] = *(Memory_Block_List.begin());
     if (SgBinaryComposite::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBinaryComposite** objectArray = (SgBinaryComposite**) &(SgBinaryComposite::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBinaryComposite::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBinaryComposite::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBinaryComposite object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBinaryComposite object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBinaryComposite::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBinaryComposite objectArray [] = *(Memory_Block_List.begin());
     if (SgBinaryComposite::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBinaryComposite** objectArray = (SgBinaryComposite**) &(SgBinaryComposite::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBinaryComposite::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBinaryComposite::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBinaryComposite found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBinaryComposite::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBinaryComposite::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBinaryComposite** objectArray = (SgBinaryComposite**) &(SgBinaryComposite::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBinaryComposite::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBinaryComposite::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBinaryComposite::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBinaryComposite);

     return memory;
   }



/* #line 6752 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJvmComposite::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJvmComposite objectArray [] = *(Memory_Block_List.begin());
     if (SgJvmComposite::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJvmComposite** objectArray = (SgJvmComposite**) &(SgJvmComposite::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJvmComposite::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJvmComposite::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJvmComposite::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJvmComposite*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJvmComposite::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJvmComposite::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJvmComposite::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJvmComposite objectArray [] = *(Memory_Block_List.begin());
     if (SgJvmComposite::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJvmComposite** objectArray = (SgJvmComposite**) &(SgJvmComposite::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJvmComposite::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJvmComposite::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJvmComposite object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJvmComposite object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJvmComposite::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJvmComposite objectArray [] = *(Memory_Block_List.begin());
     if (SgJvmComposite::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJvmComposite** objectArray = (SgJvmComposite**) &(SgJvmComposite::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJvmComposite::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJvmComposite::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJvmComposite found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJvmComposite::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJvmComposite::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJvmComposite** objectArray = (SgJvmComposite**) &(SgJvmComposite::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJvmComposite::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJvmComposite::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJvmComposite::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJvmComposite);

     return memory;
   }



/* #line 6984 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgProject::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgProject objectArray [] = *(Memory_Block_List.begin());
     if (SgProject::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgProject** objectArray = (SgProject**) &(SgProject::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgProject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgProject::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgProject::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgProject*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgProject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgProject::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgProject::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgProject objectArray [] = *(Memory_Block_List.begin());
     if (SgProject::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgProject** objectArray = (SgProject**) &(SgProject::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgProject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgProject::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgProject object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgProject object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgProject::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgProject objectArray [] = *(Memory_Block_List.begin());
     if (SgProject::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgProject** objectArray = (SgProject**) &(SgProject::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgProject::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgProject::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgProject found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgProject::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgProject::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgProject** objectArray = (SgProject**) &(SgProject::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgProject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgProject::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgProject::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgProject);

     return memory;
   }



/* #line 7216 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOptions::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOptions objectArray [] = *(Memory_Block_List.begin());
     if (SgOptions::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOptions** objectArray = (SgOptions**) &(SgOptions::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOptions::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOptions::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOptions::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOptions*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOptions::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOptions::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOptions::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOptions objectArray [] = *(Memory_Block_List.begin());
     if (SgOptions::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOptions** objectArray = (SgOptions**) &(SgOptions::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOptions::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOptions::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOptions object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOptions object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOptions::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOptions objectArray [] = *(Memory_Block_List.begin());
     if (SgOptions::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOptions** objectArray = (SgOptions**) &(SgOptions::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOptions::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOptions::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOptions found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOptions::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOptions::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOptions** objectArray = (SgOptions**) &(SgOptions::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOptions::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOptions::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOptions::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOptions);

     return memory;
   }



/* #line 7448 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUnparse_Info::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUnparse_Info objectArray [] = *(Memory_Block_List.begin());
     if (SgUnparse_Info::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnparse_Info** objectArray = (SgUnparse_Info**) &(SgUnparse_Info::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnparse_Info::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUnparse_Info::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUnparse_Info::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUnparse_Info*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUnparse_Info::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnparse_Info::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUnparse_Info::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnparse_Info objectArray [] = *(Memory_Block_List.begin());
     if (SgUnparse_Info::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnparse_Info** objectArray = (SgUnparse_Info**) &(SgUnparse_Info::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnparse_Info::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnparse_Info::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUnparse_Info object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUnparse_Info object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUnparse_Info::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnparse_Info objectArray [] = *(Memory_Block_List.begin());
     if (SgUnparse_Info::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnparse_Info** objectArray = (SgUnparse_Info**) &(SgUnparse_Info::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUnparse_Info::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUnparse_Info::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUnparse_Info found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnparse_Info::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUnparse_Info::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUnparse_Info** objectArray = (SgUnparse_Info**) &(SgUnparse_Info::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUnparse_Info::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUnparse_Info::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnparse_Info::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUnparse_Info);

     return memory;
   }



/* #line 7680 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBaseClass::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBaseClass objectArray [] = *(Memory_Block_List.begin());
     if (SgBaseClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBaseClass** objectArray = (SgBaseClass**) &(SgBaseClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBaseClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBaseClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBaseClass::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBaseClass*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBaseClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBaseClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBaseClass::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBaseClass objectArray [] = *(Memory_Block_List.begin());
     if (SgBaseClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBaseClass** objectArray = (SgBaseClass**) &(SgBaseClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBaseClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBaseClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBaseClass object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBaseClass object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBaseClass::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBaseClass objectArray [] = *(Memory_Block_List.begin());
     if (SgBaseClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBaseClass** objectArray = (SgBaseClass**) &(SgBaseClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBaseClass::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBaseClass::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBaseClass found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBaseClass::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBaseClass::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBaseClass** objectArray = (SgBaseClass**) &(SgBaseClass::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBaseClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBaseClass::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBaseClass::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBaseClass);

     return memory;
   }



/* #line 7912 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgExpBaseClass::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgExpBaseClass objectArray [] = *(Memory_Block_List.begin());
     if (SgExpBaseClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExpBaseClass** objectArray = (SgExpBaseClass**) &(SgExpBaseClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExpBaseClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgExpBaseClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgExpBaseClass::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgExpBaseClass*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgExpBaseClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExpBaseClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgExpBaseClass::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExpBaseClass objectArray [] = *(Memory_Block_List.begin());
     if (SgExpBaseClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExpBaseClass** objectArray = (SgExpBaseClass**) &(SgExpBaseClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExpBaseClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExpBaseClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgExpBaseClass object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgExpBaseClass object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgExpBaseClass::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExpBaseClass objectArray [] = *(Memory_Block_List.begin());
     if (SgExpBaseClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExpBaseClass** objectArray = (SgExpBaseClass**) &(SgExpBaseClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgExpBaseClass::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgExpBaseClass::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgExpBaseClass found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExpBaseClass::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgExpBaseClass::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgExpBaseClass** objectArray = (SgExpBaseClass**) &(SgExpBaseClass::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgExpBaseClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgExpBaseClass::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExpBaseClass::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgExpBaseClass);

     return memory;
   }



/* #line 8144 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNonrealBaseClass::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNonrealBaseClass objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealBaseClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealBaseClass** objectArray = (SgNonrealBaseClass**) &(SgNonrealBaseClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNonrealBaseClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNonrealBaseClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNonrealBaseClass::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNonrealBaseClass*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNonrealBaseClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNonrealBaseClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNonrealBaseClass::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNonrealBaseClass objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealBaseClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealBaseClass** objectArray = (SgNonrealBaseClass**) &(SgNonrealBaseClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNonrealBaseClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNonrealBaseClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNonrealBaseClass object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNonrealBaseClass object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNonrealBaseClass::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNonrealBaseClass objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealBaseClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealBaseClass** objectArray = (SgNonrealBaseClass**) &(SgNonrealBaseClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNonrealBaseClass::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNonrealBaseClass::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNonrealBaseClass found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNonrealBaseClass::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNonrealBaseClass::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNonrealBaseClass** objectArray = (SgNonrealBaseClass**) &(SgNonrealBaseClass::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNonrealBaseClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNonrealBaseClass::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNonrealBaseClass::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNonrealBaseClass);

     return memory;
   }



/* #line 8376 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypedefSeq::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypedefSeq objectArray [] = *(Memory_Block_List.begin());
     if (SgTypedefSeq::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypedefSeq** objectArray = (SgTypedefSeq**) &(SgTypedefSeq::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypedefSeq::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypedefSeq::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypedefSeq::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypedefSeq*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypedefSeq::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypedefSeq::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypedefSeq::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypedefSeq objectArray [] = *(Memory_Block_List.begin());
     if (SgTypedefSeq::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypedefSeq** objectArray = (SgTypedefSeq**) &(SgTypedefSeq::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypedefSeq::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypedefSeq::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypedefSeq object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypedefSeq object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypedefSeq::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypedefSeq objectArray [] = *(Memory_Block_List.begin());
     if (SgTypedefSeq::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypedefSeq** objectArray = (SgTypedefSeq**) &(SgTypedefSeq::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypedefSeq::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypedefSeq::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypedefSeq found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypedefSeq::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypedefSeq::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypedefSeq** objectArray = (SgTypedefSeq**) &(SgTypedefSeq::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypedefSeq::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypedefSeq::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypedefSeq::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypedefSeq);

     return memory;
   }



/* #line 8608 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateParameter::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateParameter objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateParameter::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateParameter** objectArray = (SgTemplateParameter**) &(SgTemplateParameter::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateParameter::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateParameter::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateParameter*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateParameter::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateParameter::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateParameter objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateParameter::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateParameter** objectArray = (SgTemplateParameter**) &(SgTemplateParameter::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateParameter::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateParameter object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateParameter object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateParameter::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateParameter objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateParameter::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateParameter** objectArray = (SgTemplateParameter**) &(SgTemplateParameter::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateParameter::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateParameter::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateParameter found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateParameter::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateParameter::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateParameter** objectArray = (SgTemplateParameter**) &(SgTemplateParameter::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateParameter::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateParameter::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateParameter);

     return memory;
   }



/* #line 8840 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateArgument::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateArgument objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateArgument::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateArgument** objectArray = (SgTemplateArgument**) &(SgTemplateArgument::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateArgument::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateArgument::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateArgument::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateArgument*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateArgument::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateArgument::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateArgument::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateArgument objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateArgument::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateArgument** objectArray = (SgTemplateArgument**) &(SgTemplateArgument::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateArgument::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateArgument::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateArgument object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateArgument object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateArgument::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateArgument objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateArgument::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateArgument** objectArray = (SgTemplateArgument**) &(SgTemplateArgument::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateArgument::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateArgument::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateArgument found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateArgument::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateArgument::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateArgument** objectArray = (SgTemplateArgument**) &(SgTemplateArgument::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateArgument::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateArgument::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateArgument::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateArgument);

     return memory;
   }



/* #line 9072 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDirectory::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDirectory objectArray [] = *(Memory_Block_List.begin());
     if (SgDirectory::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDirectory** objectArray = (SgDirectory**) &(SgDirectory::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDirectory::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDirectory::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDirectory::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDirectory*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDirectory::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDirectory::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDirectory::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDirectory objectArray [] = *(Memory_Block_List.begin());
     if (SgDirectory::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDirectory** objectArray = (SgDirectory**) &(SgDirectory::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDirectory::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDirectory::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDirectory object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDirectory object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDirectory::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDirectory objectArray [] = *(Memory_Block_List.begin());
     if (SgDirectory::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDirectory** objectArray = (SgDirectory**) &(SgDirectory::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDirectory::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDirectory::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDirectory found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDirectory::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDirectory::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDirectory** objectArray = (SgDirectory**) &(SgDirectory::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDirectory::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDirectory::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDirectory::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDirectory);

     return memory;
   }



/* #line 9304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFileList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFileList objectArray [] = *(Memory_Block_List.begin());
     if (SgFileList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFileList** objectArray = (SgFileList**) &(SgFileList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFileList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFileList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFileList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFileList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFileList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFileList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFileList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFileList objectArray [] = *(Memory_Block_List.begin());
     if (SgFileList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFileList** objectArray = (SgFileList**) &(SgFileList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFileList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFileList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFileList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFileList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFileList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFileList objectArray [] = *(Memory_Block_List.begin());
     if (SgFileList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFileList** objectArray = (SgFileList**) &(SgFileList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFileList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFileList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFileList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFileList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFileList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFileList** objectArray = (SgFileList**) &(SgFileList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFileList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFileList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFileList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFileList);

     return memory;
   }



/* #line 9536 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDirectoryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDirectoryList objectArray [] = *(Memory_Block_List.begin());
     if (SgDirectoryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDirectoryList** objectArray = (SgDirectoryList**) &(SgDirectoryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDirectoryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDirectoryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDirectoryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDirectoryList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDirectoryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDirectoryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDirectoryList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDirectoryList objectArray [] = *(Memory_Block_List.begin());
     if (SgDirectoryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDirectoryList** objectArray = (SgDirectoryList**) &(SgDirectoryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDirectoryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDirectoryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDirectoryList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDirectoryList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDirectoryList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDirectoryList objectArray [] = *(Memory_Block_List.begin());
     if (SgDirectoryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDirectoryList** objectArray = (SgDirectoryList**) &(SgDirectoryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDirectoryList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDirectoryList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDirectoryList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDirectoryList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDirectoryList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDirectoryList** objectArray = (SgDirectoryList**) &(SgDirectoryList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDirectoryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDirectoryList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDirectoryList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDirectoryList);

     return memory;
   }



/* #line 9768 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFunctionParameterTypeList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFunctionParameterTypeList objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionParameterTypeList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionParameterTypeList** objectArray = (SgFunctionParameterTypeList**) &(SgFunctionParameterTypeList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionParameterTypeList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFunctionParameterTypeList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFunctionParameterTypeList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFunctionParameterTypeList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFunctionParameterTypeList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionParameterTypeList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFunctionParameterTypeList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionParameterTypeList objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionParameterTypeList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionParameterTypeList** objectArray = (SgFunctionParameterTypeList**) &(SgFunctionParameterTypeList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionParameterTypeList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionParameterTypeList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFunctionParameterTypeList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFunctionParameterTypeList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFunctionParameterTypeList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionParameterTypeList objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionParameterTypeList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionParameterTypeList** objectArray = (SgFunctionParameterTypeList**) &(SgFunctionParameterTypeList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFunctionParameterTypeList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFunctionParameterTypeList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFunctionParameterTypeList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionParameterTypeList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFunctionParameterTypeList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFunctionParameterTypeList** objectArray = (SgFunctionParameterTypeList**) &(SgFunctionParameterTypeList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFunctionParameterTypeList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFunctionParameterTypeList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionParameterTypeList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFunctionParameterTypeList);

     return memory;
   }



/* #line 10000 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgQualifiedName::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgQualifiedName objectArray [] = *(Memory_Block_List.begin());
     if (SgQualifiedName::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgQualifiedName** objectArray = (SgQualifiedName**) &(SgQualifiedName::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgQualifiedName::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgQualifiedName::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgQualifiedName::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgQualifiedName*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgQualifiedName::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgQualifiedName::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgQualifiedName::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgQualifiedName objectArray [] = *(Memory_Block_List.begin());
     if (SgQualifiedName::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgQualifiedName** objectArray = (SgQualifiedName**) &(SgQualifiedName::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgQualifiedName::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgQualifiedName::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgQualifiedName object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgQualifiedName object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgQualifiedName::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgQualifiedName objectArray [] = *(Memory_Block_List.begin());
     if (SgQualifiedName::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgQualifiedName** objectArray = (SgQualifiedName**) &(SgQualifiedName::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgQualifiedName::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgQualifiedName::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgQualifiedName found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgQualifiedName::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgQualifiedName::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgQualifiedName** objectArray = (SgQualifiedName**) &(SgQualifiedName::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgQualifiedName::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgQualifiedName::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgQualifiedName::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgQualifiedName);

     return memory;
   }



/* #line 10232 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateArgumentList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateArgumentList objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateArgumentList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateArgumentList** objectArray = (SgTemplateArgumentList**) &(SgTemplateArgumentList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateArgumentList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateArgumentList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateArgumentList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateArgumentList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateArgumentList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateArgumentList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateArgumentList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateArgumentList objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateArgumentList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateArgumentList** objectArray = (SgTemplateArgumentList**) &(SgTemplateArgumentList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateArgumentList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateArgumentList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateArgumentList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateArgumentList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateArgumentList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateArgumentList objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateArgumentList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateArgumentList** objectArray = (SgTemplateArgumentList**) &(SgTemplateArgumentList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateArgumentList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateArgumentList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateArgumentList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateArgumentList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateArgumentList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateArgumentList** objectArray = (SgTemplateArgumentList**) &(SgTemplateArgumentList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateArgumentList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateArgumentList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateArgumentList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateArgumentList);

     return memory;
   }



/* #line 10464 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateParameterList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateParameterList objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateParameterList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateParameterList** objectArray = (SgTemplateParameterList**) &(SgTemplateParameterList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateParameterList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateParameterList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateParameterList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateParameterList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateParameterList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateParameterList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateParameterList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateParameterList objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateParameterList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateParameterList** objectArray = (SgTemplateParameterList**) &(SgTemplateParameterList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateParameterList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateParameterList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateParameterList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateParameterList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateParameterList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateParameterList objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateParameterList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateParameterList** objectArray = (SgTemplateParameterList**) &(SgTemplateParameterList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateParameterList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateParameterList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateParameterList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateParameterList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateParameterList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateParameterList** objectArray = (SgTemplateParameterList**) &(SgTemplateParameterList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateParameterList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateParameterList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateParameterList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateParameterList);

     return memory;
   }



/* #line 10696 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgGraph::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraph** objectArray = (SgGraph**) &(SgGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgGraph::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgGraph*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgGraph::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraph** objectArray = (SgGraph**) &(SgGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgGraph object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgGraph object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgGraph::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraph** objectArray = (SgGraph**) &(SgGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgGraph::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgGraph::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgGraph found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGraph::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgGraph::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgGraph** objectArray = (SgGraph**) &(SgGraph::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgGraph::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGraph::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgGraph);

     return memory;
   }



/* #line 10928 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIncidenceDirectedGraph::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIncidenceDirectedGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgIncidenceDirectedGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncidenceDirectedGraph** objectArray = (SgIncidenceDirectedGraph**) &(SgIncidenceDirectedGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIncidenceDirectedGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIncidenceDirectedGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIncidenceDirectedGraph::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIncidenceDirectedGraph*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIncidenceDirectedGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIncidenceDirectedGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIncidenceDirectedGraph::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIncidenceDirectedGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgIncidenceDirectedGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncidenceDirectedGraph** objectArray = (SgIncidenceDirectedGraph**) &(SgIncidenceDirectedGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIncidenceDirectedGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIncidenceDirectedGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIncidenceDirectedGraph object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIncidenceDirectedGraph object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIncidenceDirectedGraph::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIncidenceDirectedGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgIncidenceDirectedGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncidenceDirectedGraph** objectArray = (SgIncidenceDirectedGraph**) &(SgIncidenceDirectedGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIncidenceDirectedGraph::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIncidenceDirectedGraph::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIncidenceDirectedGraph found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIncidenceDirectedGraph::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIncidenceDirectedGraph::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIncidenceDirectedGraph** objectArray = (SgIncidenceDirectedGraph**) &(SgIncidenceDirectedGraph::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIncidenceDirectedGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIncidenceDirectedGraph::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIncidenceDirectedGraph::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIncidenceDirectedGraph);

     return memory;
   }



/* #line 11160 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBidirectionalGraph::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBidirectionalGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgBidirectionalGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBidirectionalGraph** objectArray = (SgBidirectionalGraph**) &(SgBidirectionalGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBidirectionalGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBidirectionalGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBidirectionalGraph::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBidirectionalGraph*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBidirectionalGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBidirectionalGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBidirectionalGraph::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBidirectionalGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgBidirectionalGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBidirectionalGraph** objectArray = (SgBidirectionalGraph**) &(SgBidirectionalGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBidirectionalGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBidirectionalGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBidirectionalGraph object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBidirectionalGraph object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBidirectionalGraph::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBidirectionalGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgBidirectionalGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBidirectionalGraph** objectArray = (SgBidirectionalGraph**) &(SgBidirectionalGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBidirectionalGraph::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBidirectionalGraph::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBidirectionalGraph found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBidirectionalGraph::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBidirectionalGraph::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBidirectionalGraph** objectArray = (SgBidirectionalGraph**) &(SgBidirectionalGraph::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBidirectionalGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBidirectionalGraph::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBidirectionalGraph::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBidirectionalGraph);

     return memory;
   }



/* #line 11392 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgStringKeyedBidirectionalGraph::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgStringKeyedBidirectionalGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgStringKeyedBidirectionalGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStringKeyedBidirectionalGraph** objectArray = (SgStringKeyedBidirectionalGraph**) &(SgStringKeyedBidirectionalGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStringKeyedBidirectionalGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgStringKeyedBidirectionalGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgStringKeyedBidirectionalGraph::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgStringKeyedBidirectionalGraph*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgStringKeyedBidirectionalGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStringKeyedBidirectionalGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgStringKeyedBidirectionalGraph::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStringKeyedBidirectionalGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgStringKeyedBidirectionalGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStringKeyedBidirectionalGraph** objectArray = (SgStringKeyedBidirectionalGraph**) &(SgStringKeyedBidirectionalGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStringKeyedBidirectionalGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStringKeyedBidirectionalGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgStringKeyedBidirectionalGraph object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgStringKeyedBidirectionalGraph object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgStringKeyedBidirectionalGraph::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStringKeyedBidirectionalGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgStringKeyedBidirectionalGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStringKeyedBidirectionalGraph** objectArray = (SgStringKeyedBidirectionalGraph**) &(SgStringKeyedBidirectionalGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgStringKeyedBidirectionalGraph::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgStringKeyedBidirectionalGraph::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgStringKeyedBidirectionalGraph found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStringKeyedBidirectionalGraph::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgStringKeyedBidirectionalGraph::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgStringKeyedBidirectionalGraph** objectArray = (SgStringKeyedBidirectionalGraph**) &(SgStringKeyedBidirectionalGraph::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgStringKeyedBidirectionalGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgStringKeyedBidirectionalGraph::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStringKeyedBidirectionalGraph::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgStringKeyedBidirectionalGraph);

     return memory;
   }



/* #line 11624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIntKeyedBidirectionalGraph::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIntKeyedBidirectionalGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgIntKeyedBidirectionalGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntKeyedBidirectionalGraph** objectArray = (SgIntKeyedBidirectionalGraph**) &(SgIntKeyedBidirectionalGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIntKeyedBidirectionalGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIntKeyedBidirectionalGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIntKeyedBidirectionalGraph::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIntKeyedBidirectionalGraph*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIntKeyedBidirectionalGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIntKeyedBidirectionalGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIntKeyedBidirectionalGraph::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIntKeyedBidirectionalGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgIntKeyedBidirectionalGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntKeyedBidirectionalGraph** objectArray = (SgIntKeyedBidirectionalGraph**) &(SgIntKeyedBidirectionalGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIntKeyedBidirectionalGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIntKeyedBidirectionalGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIntKeyedBidirectionalGraph object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIntKeyedBidirectionalGraph object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIntKeyedBidirectionalGraph::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIntKeyedBidirectionalGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgIntKeyedBidirectionalGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntKeyedBidirectionalGraph** objectArray = (SgIntKeyedBidirectionalGraph**) &(SgIntKeyedBidirectionalGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIntKeyedBidirectionalGraph::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIntKeyedBidirectionalGraph::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIntKeyedBidirectionalGraph found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIntKeyedBidirectionalGraph::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIntKeyedBidirectionalGraph::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIntKeyedBidirectionalGraph** objectArray = (SgIntKeyedBidirectionalGraph**) &(SgIntKeyedBidirectionalGraph::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIntKeyedBidirectionalGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIntKeyedBidirectionalGraph::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIntKeyedBidirectionalGraph::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIntKeyedBidirectionalGraph);

     return memory;
   }



/* #line 11856 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIncidenceUndirectedGraph::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIncidenceUndirectedGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgIncidenceUndirectedGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncidenceUndirectedGraph** objectArray = (SgIncidenceUndirectedGraph**) &(SgIncidenceUndirectedGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIncidenceUndirectedGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIncidenceUndirectedGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIncidenceUndirectedGraph::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIncidenceUndirectedGraph*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIncidenceUndirectedGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIncidenceUndirectedGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIncidenceUndirectedGraph::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIncidenceUndirectedGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgIncidenceUndirectedGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncidenceUndirectedGraph** objectArray = (SgIncidenceUndirectedGraph**) &(SgIncidenceUndirectedGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIncidenceUndirectedGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIncidenceUndirectedGraph::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIncidenceUndirectedGraph object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIncidenceUndirectedGraph object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIncidenceUndirectedGraph::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIncidenceUndirectedGraph objectArray [] = *(Memory_Block_List.begin());
     if (SgIncidenceUndirectedGraph::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncidenceUndirectedGraph** objectArray = (SgIncidenceUndirectedGraph**) &(SgIncidenceUndirectedGraph::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIncidenceUndirectedGraph::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIncidenceUndirectedGraph::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIncidenceUndirectedGraph found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIncidenceUndirectedGraph::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIncidenceUndirectedGraph::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIncidenceUndirectedGraph** objectArray = (SgIncidenceUndirectedGraph**) &(SgIncidenceUndirectedGraph::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIncidenceUndirectedGraph::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIncidenceUndirectedGraph::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIncidenceUndirectedGraph::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIncidenceUndirectedGraph);

     return memory;
   }



/* #line 12088 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgGraphNode::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgGraphNode objectArray [] = *(Memory_Block_List.begin());
     if (SgGraphNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraphNode** objectArray = (SgGraphNode**) &(SgGraphNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGraphNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgGraphNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgGraphNode::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgGraphNode*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgGraphNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGraphNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgGraphNode::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGraphNode objectArray [] = *(Memory_Block_List.begin());
     if (SgGraphNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraphNode** objectArray = (SgGraphNode**) &(SgGraphNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGraphNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGraphNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgGraphNode object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgGraphNode object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgGraphNode::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGraphNode objectArray [] = *(Memory_Block_List.begin());
     if (SgGraphNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraphNode** objectArray = (SgGraphNode**) &(SgGraphNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgGraphNode::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgGraphNode::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgGraphNode found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGraphNode::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgGraphNode::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgGraphNode** objectArray = (SgGraphNode**) &(SgGraphNode::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgGraphNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgGraphNode::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGraphNode::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgGraphNode);

     return memory;
   }



/* #line 12320 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgGraphEdge::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgGraphEdge objectArray [] = *(Memory_Block_List.begin());
     if (SgGraphEdge::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraphEdge** objectArray = (SgGraphEdge**) &(SgGraphEdge::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGraphEdge::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgGraphEdge::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgGraphEdge::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgGraphEdge*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgGraphEdge::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGraphEdge::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgGraphEdge::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGraphEdge objectArray [] = *(Memory_Block_List.begin());
     if (SgGraphEdge::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraphEdge** objectArray = (SgGraphEdge**) &(SgGraphEdge::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGraphEdge::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGraphEdge::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgGraphEdge object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgGraphEdge object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgGraphEdge::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGraphEdge objectArray [] = *(Memory_Block_List.begin());
     if (SgGraphEdge::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraphEdge** objectArray = (SgGraphEdge**) &(SgGraphEdge::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgGraphEdge::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgGraphEdge::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgGraphEdge found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGraphEdge::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgGraphEdge::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgGraphEdge** objectArray = (SgGraphEdge**) &(SgGraphEdge::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgGraphEdge::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgGraphEdge::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGraphEdge::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgGraphEdge);

     return memory;
   }



/* #line 12552 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDirectedGraphEdge::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDirectedGraphEdge objectArray [] = *(Memory_Block_List.begin());
     if (SgDirectedGraphEdge::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDirectedGraphEdge** objectArray = (SgDirectedGraphEdge**) &(SgDirectedGraphEdge::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDirectedGraphEdge::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDirectedGraphEdge::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDirectedGraphEdge::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDirectedGraphEdge*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDirectedGraphEdge::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDirectedGraphEdge::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDirectedGraphEdge::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDirectedGraphEdge objectArray [] = *(Memory_Block_List.begin());
     if (SgDirectedGraphEdge::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDirectedGraphEdge** objectArray = (SgDirectedGraphEdge**) &(SgDirectedGraphEdge::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDirectedGraphEdge::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDirectedGraphEdge::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDirectedGraphEdge object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDirectedGraphEdge object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDirectedGraphEdge::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDirectedGraphEdge objectArray [] = *(Memory_Block_List.begin());
     if (SgDirectedGraphEdge::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDirectedGraphEdge** objectArray = (SgDirectedGraphEdge**) &(SgDirectedGraphEdge::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDirectedGraphEdge::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDirectedGraphEdge::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDirectedGraphEdge found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDirectedGraphEdge::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDirectedGraphEdge::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDirectedGraphEdge** objectArray = (SgDirectedGraphEdge**) &(SgDirectedGraphEdge::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDirectedGraphEdge::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDirectedGraphEdge::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDirectedGraphEdge::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDirectedGraphEdge);

     return memory;
   }



/* #line 12784 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUndirectedGraphEdge::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUndirectedGraphEdge objectArray [] = *(Memory_Block_List.begin());
     if (SgUndirectedGraphEdge::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUndirectedGraphEdge** objectArray = (SgUndirectedGraphEdge**) &(SgUndirectedGraphEdge::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUndirectedGraphEdge::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUndirectedGraphEdge::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUndirectedGraphEdge::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUndirectedGraphEdge*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUndirectedGraphEdge::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUndirectedGraphEdge::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUndirectedGraphEdge::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUndirectedGraphEdge objectArray [] = *(Memory_Block_List.begin());
     if (SgUndirectedGraphEdge::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUndirectedGraphEdge** objectArray = (SgUndirectedGraphEdge**) &(SgUndirectedGraphEdge::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUndirectedGraphEdge::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUndirectedGraphEdge::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUndirectedGraphEdge object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUndirectedGraphEdge object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUndirectedGraphEdge::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUndirectedGraphEdge objectArray [] = *(Memory_Block_List.begin());
     if (SgUndirectedGraphEdge::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUndirectedGraphEdge** objectArray = (SgUndirectedGraphEdge**) &(SgUndirectedGraphEdge::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUndirectedGraphEdge::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUndirectedGraphEdge::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUndirectedGraphEdge found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUndirectedGraphEdge::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUndirectedGraphEdge::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUndirectedGraphEdge** objectArray = (SgUndirectedGraphEdge**) &(SgUndirectedGraphEdge::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUndirectedGraphEdge::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUndirectedGraphEdge::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUndirectedGraphEdge::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUndirectedGraphEdge);

     return memory;
   }



/* #line 13016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgGraphNodeList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgGraphNodeList objectArray [] = *(Memory_Block_List.begin());
     if (SgGraphNodeList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraphNodeList** objectArray = (SgGraphNodeList**) &(SgGraphNodeList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGraphNodeList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgGraphNodeList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgGraphNodeList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgGraphNodeList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgGraphNodeList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGraphNodeList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgGraphNodeList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGraphNodeList objectArray [] = *(Memory_Block_List.begin());
     if (SgGraphNodeList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraphNodeList** objectArray = (SgGraphNodeList**) &(SgGraphNodeList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGraphNodeList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGraphNodeList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgGraphNodeList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgGraphNodeList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgGraphNodeList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGraphNodeList objectArray [] = *(Memory_Block_List.begin());
     if (SgGraphNodeList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraphNodeList** objectArray = (SgGraphNodeList**) &(SgGraphNodeList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgGraphNodeList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgGraphNodeList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgGraphNodeList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGraphNodeList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgGraphNodeList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgGraphNodeList** objectArray = (SgGraphNodeList**) &(SgGraphNodeList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgGraphNodeList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgGraphNodeList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGraphNodeList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgGraphNodeList);

     return memory;
   }



/* #line 13248 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgGraphEdgeList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgGraphEdgeList objectArray [] = *(Memory_Block_List.begin());
     if (SgGraphEdgeList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraphEdgeList** objectArray = (SgGraphEdgeList**) &(SgGraphEdgeList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGraphEdgeList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgGraphEdgeList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgGraphEdgeList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgGraphEdgeList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgGraphEdgeList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGraphEdgeList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgGraphEdgeList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGraphEdgeList objectArray [] = *(Memory_Block_List.begin());
     if (SgGraphEdgeList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraphEdgeList** objectArray = (SgGraphEdgeList**) &(SgGraphEdgeList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGraphEdgeList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGraphEdgeList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgGraphEdgeList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgGraphEdgeList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgGraphEdgeList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGraphEdgeList objectArray [] = *(Memory_Block_List.begin());
     if (SgGraphEdgeList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGraphEdgeList** objectArray = (SgGraphEdgeList**) &(SgGraphEdgeList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgGraphEdgeList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgGraphEdgeList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgGraphEdgeList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGraphEdgeList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgGraphEdgeList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgGraphEdgeList** objectArray = (SgGraphEdgeList**) &(SgGraphEdgeList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgGraphEdgeList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgGraphEdgeList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGraphEdgeList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgGraphEdgeList);

     return memory;
   }



/* #line 13480 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeTable** objectArray = (SgTypeTable**) &(SgTypeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeTable** objectArray = (SgTypeTable**) &(SgTypeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeTable** objectArray = (SgTypeTable**) &(SgTypeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeTable** objectArray = (SgTypeTable**) &(SgTypeTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeTable);

     return memory;
   }



/* #line 13712 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNameGroup::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNameGroup objectArray [] = *(Memory_Block_List.begin());
     if (SgNameGroup::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNameGroup** objectArray = (SgNameGroup**) &(SgNameGroup::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNameGroup::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNameGroup::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNameGroup::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNameGroup*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNameGroup::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNameGroup::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNameGroup::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNameGroup objectArray [] = *(Memory_Block_List.begin());
     if (SgNameGroup::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNameGroup** objectArray = (SgNameGroup**) &(SgNameGroup::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNameGroup::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNameGroup::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNameGroup object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNameGroup object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNameGroup::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNameGroup objectArray [] = *(Memory_Block_List.begin());
     if (SgNameGroup::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNameGroup** objectArray = (SgNameGroup**) &(SgNameGroup::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNameGroup::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNameGroup::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNameGroup found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNameGroup::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNameGroup::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNameGroup** objectArray = (SgNameGroup**) &(SgNameGroup::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNameGroup::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNameGroup::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNameGroup::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNameGroup);

     return memory;
   }



/* #line 13944 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDimensionObject::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDimensionObject objectArray [] = *(Memory_Block_List.begin());
     if (SgDimensionObject::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDimensionObject** objectArray = (SgDimensionObject**) &(SgDimensionObject::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDimensionObject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDimensionObject::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDimensionObject::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDimensionObject*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDimensionObject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDimensionObject::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDimensionObject::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDimensionObject objectArray [] = *(Memory_Block_List.begin());
     if (SgDimensionObject::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDimensionObject** objectArray = (SgDimensionObject**) &(SgDimensionObject::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDimensionObject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDimensionObject::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDimensionObject object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDimensionObject object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDimensionObject::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDimensionObject objectArray [] = *(Memory_Block_List.begin());
     if (SgDimensionObject::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDimensionObject** objectArray = (SgDimensionObject**) &(SgDimensionObject::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDimensionObject::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDimensionObject::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDimensionObject found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDimensionObject::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDimensionObject::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDimensionObject** objectArray = (SgDimensionObject**) &(SgDimensionObject::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDimensionObject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDimensionObject::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDimensionObject::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDimensionObject);

     return memory;
   }



/* #line 14176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFormatItem::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFormatItem objectArray [] = *(Memory_Block_List.begin());
     if (SgFormatItem::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFormatItem** objectArray = (SgFormatItem**) &(SgFormatItem::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFormatItem::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFormatItem::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFormatItem::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFormatItem*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFormatItem::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFormatItem::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFormatItem::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFormatItem objectArray [] = *(Memory_Block_List.begin());
     if (SgFormatItem::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFormatItem** objectArray = (SgFormatItem**) &(SgFormatItem::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFormatItem::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFormatItem::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFormatItem object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFormatItem object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFormatItem::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFormatItem objectArray [] = *(Memory_Block_List.begin());
     if (SgFormatItem::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFormatItem** objectArray = (SgFormatItem**) &(SgFormatItem::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFormatItem::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFormatItem::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFormatItem found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFormatItem::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFormatItem::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFormatItem** objectArray = (SgFormatItem**) &(SgFormatItem::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFormatItem::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFormatItem::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFormatItem::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFormatItem);

     return memory;
   }



/* #line 14408 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFormatItemList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFormatItemList objectArray [] = *(Memory_Block_List.begin());
     if (SgFormatItemList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFormatItemList** objectArray = (SgFormatItemList**) &(SgFormatItemList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFormatItemList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFormatItemList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFormatItemList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFormatItemList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFormatItemList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFormatItemList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFormatItemList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFormatItemList objectArray [] = *(Memory_Block_List.begin());
     if (SgFormatItemList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFormatItemList** objectArray = (SgFormatItemList**) &(SgFormatItemList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFormatItemList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFormatItemList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFormatItemList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFormatItemList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFormatItemList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFormatItemList objectArray [] = *(Memory_Block_List.begin());
     if (SgFormatItemList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFormatItemList** objectArray = (SgFormatItemList**) &(SgFormatItemList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFormatItemList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFormatItemList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFormatItemList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFormatItemList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFormatItemList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFormatItemList** objectArray = (SgFormatItemList**) &(SgFormatItemList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFormatItemList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFormatItemList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFormatItemList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFormatItemList);

     return memory;
   }



/* #line 14640 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDataStatementGroup::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDataStatementGroup objectArray [] = *(Memory_Block_List.begin());
     if (SgDataStatementGroup::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDataStatementGroup** objectArray = (SgDataStatementGroup**) &(SgDataStatementGroup::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDataStatementGroup::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDataStatementGroup::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDataStatementGroup::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDataStatementGroup*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDataStatementGroup::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDataStatementGroup::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDataStatementGroup::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDataStatementGroup objectArray [] = *(Memory_Block_List.begin());
     if (SgDataStatementGroup::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDataStatementGroup** objectArray = (SgDataStatementGroup**) &(SgDataStatementGroup::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDataStatementGroup::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDataStatementGroup::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDataStatementGroup object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDataStatementGroup object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDataStatementGroup::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDataStatementGroup objectArray [] = *(Memory_Block_List.begin());
     if (SgDataStatementGroup::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDataStatementGroup** objectArray = (SgDataStatementGroup**) &(SgDataStatementGroup::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDataStatementGroup::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDataStatementGroup::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDataStatementGroup found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDataStatementGroup::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDataStatementGroup::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDataStatementGroup** objectArray = (SgDataStatementGroup**) &(SgDataStatementGroup::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDataStatementGroup::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDataStatementGroup::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDataStatementGroup::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDataStatementGroup);

     return memory;
   }



/* #line 14872 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDataStatementObject::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDataStatementObject objectArray [] = *(Memory_Block_List.begin());
     if (SgDataStatementObject::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDataStatementObject** objectArray = (SgDataStatementObject**) &(SgDataStatementObject::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDataStatementObject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDataStatementObject::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDataStatementObject::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDataStatementObject*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDataStatementObject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDataStatementObject::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDataStatementObject::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDataStatementObject objectArray [] = *(Memory_Block_List.begin());
     if (SgDataStatementObject::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDataStatementObject** objectArray = (SgDataStatementObject**) &(SgDataStatementObject::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDataStatementObject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDataStatementObject::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDataStatementObject object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDataStatementObject object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDataStatementObject::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDataStatementObject objectArray [] = *(Memory_Block_List.begin());
     if (SgDataStatementObject::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDataStatementObject** objectArray = (SgDataStatementObject**) &(SgDataStatementObject::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDataStatementObject::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDataStatementObject::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDataStatementObject found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDataStatementObject::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDataStatementObject::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDataStatementObject** objectArray = (SgDataStatementObject**) &(SgDataStatementObject::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDataStatementObject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDataStatementObject::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDataStatementObject::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDataStatementObject);

     return memory;
   }



/* #line 15104 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIncludeFile::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIncludeFile objectArray [] = *(Memory_Block_List.begin());
     if (SgIncludeFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncludeFile** objectArray = (SgIncludeFile**) &(SgIncludeFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIncludeFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIncludeFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIncludeFile::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIncludeFile*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIncludeFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIncludeFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIncludeFile::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIncludeFile objectArray [] = *(Memory_Block_List.begin());
     if (SgIncludeFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncludeFile** objectArray = (SgIncludeFile**) &(SgIncludeFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIncludeFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIncludeFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIncludeFile object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIncludeFile object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIncludeFile::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIncludeFile objectArray [] = *(Memory_Block_List.begin());
     if (SgIncludeFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncludeFile** objectArray = (SgIncludeFile**) &(SgIncludeFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIncludeFile::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIncludeFile::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIncludeFile found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIncludeFile::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIncludeFile::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIncludeFile** objectArray = (SgIncludeFile**) &(SgIncludeFile::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIncludeFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIncludeFile::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIncludeFile::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIncludeFile);

     return memory;
   }



/* #line 15336 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDataStatementValue::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDataStatementValue objectArray [] = *(Memory_Block_List.begin());
     if (SgDataStatementValue::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDataStatementValue** objectArray = (SgDataStatementValue**) &(SgDataStatementValue::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDataStatementValue::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDataStatementValue::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDataStatementValue::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDataStatementValue*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDataStatementValue::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDataStatementValue::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDataStatementValue::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDataStatementValue objectArray [] = *(Memory_Block_List.begin());
     if (SgDataStatementValue::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDataStatementValue** objectArray = (SgDataStatementValue**) &(SgDataStatementValue::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDataStatementValue::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDataStatementValue::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDataStatementValue object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDataStatementValue object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDataStatementValue::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDataStatementValue objectArray [] = *(Memory_Block_List.begin());
     if (SgDataStatementValue::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDataStatementValue** objectArray = (SgDataStatementValue**) &(SgDataStatementValue::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDataStatementValue::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDataStatementValue::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDataStatementValue found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDataStatementValue::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDataStatementValue::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDataStatementValue** objectArray = (SgDataStatementValue**) &(SgDataStatementValue::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDataStatementValue::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDataStatementValue::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDataStatementValue::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDataStatementValue);

     return memory;
   }



/* #line 15568 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaImportStatementList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaImportStatementList objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaImportStatementList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaImportStatementList** objectArray = (SgJavaImportStatementList**) &(SgJavaImportStatementList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaImportStatementList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaImportStatementList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaImportStatementList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaImportStatementList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaImportStatementList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaImportStatementList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaImportStatementList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaImportStatementList objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaImportStatementList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaImportStatementList** objectArray = (SgJavaImportStatementList**) &(SgJavaImportStatementList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaImportStatementList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaImportStatementList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaImportStatementList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaImportStatementList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaImportStatementList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaImportStatementList objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaImportStatementList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaImportStatementList** objectArray = (SgJavaImportStatementList**) &(SgJavaImportStatementList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaImportStatementList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaImportStatementList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaImportStatementList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaImportStatementList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaImportStatementList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaImportStatementList** objectArray = (SgJavaImportStatementList**) &(SgJavaImportStatementList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaImportStatementList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaImportStatementList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaImportStatementList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaImportStatementList);

     return memory;
   }



/* #line 15800 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaClassDeclarationList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaClassDeclarationList objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaClassDeclarationList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaClassDeclarationList** objectArray = (SgJavaClassDeclarationList**) &(SgJavaClassDeclarationList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaClassDeclarationList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaClassDeclarationList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaClassDeclarationList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaClassDeclarationList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaClassDeclarationList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaClassDeclarationList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaClassDeclarationList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaClassDeclarationList objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaClassDeclarationList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaClassDeclarationList** objectArray = (SgJavaClassDeclarationList**) &(SgJavaClassDeclarationList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaClassDeclarationList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaClassDeclarationList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaClassDeclarationList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaClassDeclarationList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaClassDeclarationList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaClassDeclarationList objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaClassDeclarationList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaClassDeclarationList** objectArray = (SgJavaClassDeclarationList**) &(SgJavaClassDeclarationList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaClassDeclarationList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaClassDeclarationList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaClassDeclarationList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaClassDeclarationList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaClassDeclarationList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaClassDeclarationList** objectArray = (SgJavaClassDeclarationList**) &(SgJavaClassDeclarationList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaClassDeclarationList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaClassDeclarationList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaClassDeclarationList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaClassDeclarationList);

     return memory;
   }



/* #line 16032 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgHeaderFileReport::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgHeaderFileReport objectArray [] = *(Memory_Block_List.begin());
     if (SgHeaderFileReport::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgHeaderFileReport** objectArray = (SgHeaderFileReport**) &(SgHeaderFileReport::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgHeaderFileReport::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgHeaderFileReport::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgHeaderFileReport::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgHeaderFileReport*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgHeaderFileReport::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgHeaderFileReport::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgHeaderFileReport::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgHeaderFileReport objectArray [] = *(Memory_Block_List.begin());
     if (SgHeaderFileReport::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgHeaderFileReport** objectArray = (SgHeaderFileReport**) &(SgHeaderFileReport::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgHeaderFileReport::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgHeaderFileReport::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgHeaderFileReport object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgHeaderFileReport object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgHeaderFileReport::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgHeaderFileReport objectArray [] = *(Memory_Block_List.begin());
     if (SgHeaderFileReport::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgHeaderFileReport** objectArray = (SgHeaderFileReport**) &(SgHeaderFileReport::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgHeaderFileReport::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgHeaderFileReport::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgHeaderFileReport found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgHeaderFileReport::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgHeaderFileReport::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgHeaderFileReport** objectArray = (SgHeaderFileReport**) &(SgHeaderFileReport::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgHeaderFileReport::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgHeaderFileReport::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgHeaderFileReport::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgHeaderFileReport);

     return memory;
   }



/* #line 16264 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgType objectArray [] = *(Memory_Block_List.begin());
     if (SgType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgType** objectArray = (SgType**) &(SgType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgType objectArray [] = *(Memory_Block_List.begin());
     if (SgType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgType** objectArray = (SgType**) &(SgType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgType objectArray [] = *(Memory_Block_List.begin());
     if (SgType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgType** objectArray = (SgType**) &(SgType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgType** objectArray = (SgType**) &(SgType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgType);

     return memory;
   }



/* #line 16496 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeUnknown::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeUnknown objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnknown::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnknown** objectArray = (SgTypeUnknown**) &(SgTypeUnknown::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnknown::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeUnknown::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeUnknown::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeUnknown*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeUnknown::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnknown::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeUnknown::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnknown objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnknown::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnknown** objectArray = (SgTypeUnknown**) &(SgTypeUnknown::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnknown::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnknown::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeUnknown object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeUnknown object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeUnknown::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnknown objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnknown::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnknown** objectArray = (SgTypeUnknown**) &(SgTypeUnknown::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeUnknown::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeUnknown::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeUnknown found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnknown::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeUnknown::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeUnknown** objectArray = (SgTypeUnknown**) &(SgTypeUnknown::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeUnknown::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeUnknown::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnknown::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeUnknown);

     return memory;
   }



/* #line 16728 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeVoid::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeVoid objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeVoid::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeVoid** objectArray = (SgTypeVoid**) &(SgTypeVoid::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeVoid::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeVoid::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeVoid::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeVoid*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeVoid::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeVoid::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeVoid::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeVoid objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeVoid::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeVoid** objectArray = (SgTypeVoid**) &(SgTypeVoid::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeVoid::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeVoid::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeVoid object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeVoid object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeVoid::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeVoid objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeVoid::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeVoid** objectArray = (SgTypeVoid**) &(SgTypeVoid::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeVoid::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeVoid::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeVoid found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeVoid::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeVoid::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeVoid** objectArray = (SgTypeVoid**) &(SgTypeVoid::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeVoid::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeVoid::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeVoid::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeVoid);

     return memory;
   }



/* #line 16960 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeGlobalVoid::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeGlobalVoid objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeGlobalVoid::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeGlobalVoid** objectArray = (SgTypeGlobalVoid**) &(SgTypeGlobalVoid::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeGlobalVoid::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeGlobalVoid::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeGlobalVoid::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeGlobalVoid*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeGlobalVoid::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeGlobalVoid::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeGlobalVoid::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeGlobalVoid objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeGlobalVoid::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeGlobalVoid** objectArray = (SgTypeGlobalVoid**) &(SgTypeGlobalVoid::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeGlobalVoid::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeGlobalVoid::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeGlobalVoid object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeGlobalVoid object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeGlobalVoid::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeGlobalVoid objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeGlobalVoid::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeGlobalVoid** objectArray = (SgTypeGlobalVoid**) &(SgTypeGlobalVoid::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeGlobalVoid::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeGlobalVoid::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeGlobalVoid found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeGlobalVoid::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeGlobalVoid::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeGlobalVoid** objectArray = (SgTypeGlobalVoid**) &(SgTypeGlobalVoid::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeGlobalVoid::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeGlobalVoid::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeGlobalVoid::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeGlobalVoid);

     return memory;
   }



/* #line 17192 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeTuple::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeTuple objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeTuple::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeTuple** objectArray = (SgTypeTuple**) &(SgTypeTuple::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeTuple::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeTuple::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeTuple::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeTuple*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeTuple::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeTuple::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeTuple::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeTuple objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeTuple::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeTuple** objectArray = (SgTypeTuple**) &(SgTypeTuple::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeTuple::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeTuple::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeTuple object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeTuple object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeTuple::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeTuple objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeTuple::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeTuple** objectArray = (SgTypeTuple**) &(SgTypeTuple::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeTuple::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeTuple::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeTuple found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeTuple::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeTuple::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeTuple** objectArray = (SgTypeTuple**) &(SgTypeTuple::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeTuple::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeTuple::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeTuple::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeTuple);

     return memory;
   }



/* #line 17424 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeString::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeString objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeString::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeString** objectArray = (SgTypeString**) &(SgTypeString::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeString::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeString::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeString*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeString::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeString::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeString objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeString::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeString** objectArray = (SgTypeString**) &(SgTypeString::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeString::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeString object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeString object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeString::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeString objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeString::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeString** objectArray = (SgTypeString**) &(SgTypeString::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeString::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeString::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeString found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeString::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeString::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeString** objectArray = (SgTypeString**) &(SgTypeString::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeString::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeString::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeString);

     return memory;
   }



/* #line 17656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgRangeType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgRangeType objectArray [] = *(Memory_Block_List.begin());
     if (SgRangeType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRangeType** objectArray = (SgRangeType**) &(SgRangeType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRangeType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgRangeType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgRangeType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgRangeType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgRangeType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRangeType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgRangeType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRangeType objectArray [] = *(Memory_Block_List.begin());
     if (SgRangeType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRangeType** objectArray = (SgRangeType**) &(SgRangeType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRangeType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRangeType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgRangeType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgRangeType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgRangeType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRangeType objectArray [] = *(Memory_Block_List.begin());
     if (SgRangeType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRangeType** objectArray = (SgRangeType**) &(SgRangeType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgRangeType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgRangeType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgRangeType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRangeType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgRangeType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgRangeType** objectArray = (SgRangeType**) &(SgRangeType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgRangeType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgRangeType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRangeType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgRangeType);

     return memory;
   }



/* #line 17888 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPointerType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPointerType objectArray [] = *(Memory_Block_List.begin());
     if (SgPointerType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPointerType** objectArray = (SgPointerType**) &(SgPointerType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPointerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPointerType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPointerType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPointerType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPointerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPointerType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPointerType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPointerType objectArray [] = *(Memory_Block_List.begin());
     if (SgPointerType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPointerType** objectArray = (SgPointerType**) &(SgPointerType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPointerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPointerType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPointerType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPointerType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPointerType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPointerType objectArray [] = *(Memory_Block_List.begin());
     if (SgPointerType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPointerType** objectArray = (SgPointerType**) &(SgPointerType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPointerType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPointerType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPointerType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPointerType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPointerType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPointerType** objectArray = (SgPointerType**) &(SgPointerType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPointerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPointerType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPointerType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPointerType);

     return memory;
   }



/* #line 18120 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPointerMemberType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPointerMemberType objectArray [] = *(Memory_Block_List.begin());
     if (SgPointerMemberType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPointerMemberType** objectArray = (SgPointerMemberType**) &(SgPointerMemberType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPointerMemberType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPointerMemberType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPointerMemberType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPointerMemberType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPointerMemberType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPointerMemberType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPointerMemberType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPointerMemberType objectArray [] = *(Memory_Block_List.begin());
     if (SgPointerMemberType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPointerMemberType** objectArray = (SgPointerMemberType**) &(SgPointerMemberType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPointerMemberType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPointerMemberType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPointerMemberType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPointerMemberType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPointerMemberType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPointerMemberType objectArray [] = *(Memory_Block_List.begin());
     if (SgPointerMemberType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPointerMemberType** objectArray = (SgPointerMemberType**) &(SgPointerMemberType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPointerMemberType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPointerMemberType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPointerMemberType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPointerMemberType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPointerMemberType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPointerMemberType** objectArray = (SgPointerMemberType**) &(SgPointerMemberType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPointerMemberType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPointerMemberType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPointerMemberType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPointerMemberType);

     return memory;
   }



/* #line 18352 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJovialBitType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJovialBitType objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialBitType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialBitType** objectArray = (SgJovialBitType**) &(SgJovialBitType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialBitType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJovialBitType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJovialBitType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJovialBitType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJovialBitType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialBitType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJovialBitType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialBitType objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialBitType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialBitType** objectArray = (SgJovialBitType**) &(SgJovialBitType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialBitType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialBitType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJovialBitType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJovialBitType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJovialBitType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialBitType objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialBitType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialBitType** objectArray = (SgJovialBitType**) &(SgJovialBitType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJovialBitType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJovialBitType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJovialBitType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialBitType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJovialBitType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJovialBitType** objectArray = (SgJovialBitType**) &(SgJovialBitType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJovialBitType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJovialBitType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialBitType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJovialBitType);

     return memory;
   }



/* #line 18584 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgReferenceType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgReferenceType objectArray [] = *(Memory_Block_List.begin());
     if (SgReferenceType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReferenceType** objectArray = (SgReferenceType**) &(SgReferenceType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgReferenceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgReferenceType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgReferenceType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgReferenceType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgReferenceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgReferenceType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgReferenceType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgReferenceType objectArray [] = *(Memory_Block_List.begin());
     if (SgReferenceType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReferenceType** objectArray = (SgReferenceType**) &(SgReferenceType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgReferenceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgReferenceType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgReferenceType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgReferenceType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgReferenceType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgReferenceType objectArray [] = *(Memory_Block_List.begin());
     if (SgReferenceType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReferenceType** objectArray = (SgReferenceType**) &(SgReferenceType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgReferenceType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgReferenceType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgReferenceType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgReferenceType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgReferenceType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgReferenceType** objectArray = (SgReferenceType**) &(SgReferenceType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgReferenceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgReferenceType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgReferenceType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgReferenceType);

     return memory;
   }



/* #line 18816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNamedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNamedType objectArray [] = *(Memory_Block_List.begin());
     if (SgNamedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamedType** objectArray = (SgNamedType**) &(SgNamedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNamedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNamedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNamedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNamedType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNamedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNamedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNamedType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNamedType objectArray [] = *(Memory_Block_List.begin());
     if (SgNamedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamedType** objectArray = (SgNamedType**) &(SgNamedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNamedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNamedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNamedType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNamedType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNamedType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNamedType objectArray [] = *(Memory_Block_List.begin());
     if (SgNamedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamedType** objectArray = (SgNamedType**) &(SgNamedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNamedType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNamedType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNamedType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNamedType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNamedType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNamedType** objectArray = (SgNamedType**) &(SgNamedType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNamedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNamedType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNamedType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNamedType);

     return memory;
   }



/* #line 19048 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgClassType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgClassType objectArray [] = *(Memory_Block_List.begin());
     if (SgClassType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassType** objectArray = (SgClassType**) &(SgClassType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgClassType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgClassType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgClassType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgClassType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgClassType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassType objectArray [] = *(Memory_Block_List.begin());
     if (SgClassType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassType** objectArray = (SgClassType**) &(SgClassType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgClassType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgClassType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgClassType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassType objectArray [] = *(Memory_Block_List.begin());
     if (SgClassType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassType** objectArray = (SgClassType**) &(SgClassType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgClassType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgClassType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgClassType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgClassType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgClassType** objectArray = (SgClassType**) &(SgClassType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgClassType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgClassType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgClassType);

     return memory;
   }



/* #line 19280 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaParameterType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaParameterType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaParameterType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaParameterType** objectArray = (SgJavaParameterType**) &(SgJavaParameterType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaParameterType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaParameterType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaParameterType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaParameterType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaParameterType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaParameterType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaParameterType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaParameterType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaParameterType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaParameterType** objectArray = (SgJavaParameterType**) &(SgJavaParameterType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaParameterType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaParameterType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaParameterType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaParameterType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaParameterType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaParameterType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaParameterType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaParameterType** objectArray = (SgJavaParameterType**) &(SgJavaParameterType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaParameterType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaParameterType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaParameterType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaParameterType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaParameterType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaParameterType** objectArray = (SgJavaParameterType**) &(SgJavaParameterType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaParameterType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaParameterType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaParameterType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaParameterType);

     return memory;
   }



/* #line 19512 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJovialTableType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJovialTableType objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialTableType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialTableType** objectArray = (SgJovialTableType**) &(SgJovialTableType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialTableType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJovialTableType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJovialTableType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJovialTableType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJovialTableType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialTableType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJovialTableType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialTableType objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialTableType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialTableType** objectArray = (SgJovialTableType**) &(SgJovialTableType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialTableType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialTableType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJovialTableType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJovialTableType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJovialTableType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialTableType objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialTableType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialTableType** objectArray = (SgJovialTableType**) &(SgJovialTableType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJovialTableType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJovialTableType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJovialTableType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialTableType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJovialTableType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJovialTableType** objectArray = (SgJovialTableType**) &(SgJovialTableType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJovialTableType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJovialTableType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialTableType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJovialTableType);

     return memory;
   }



/* #line 19744 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgEnumType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgEnumType objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumType** objectArray = (SgEnumType**) &(SgEnumType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEnumType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgEnumType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgEnumType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgEnumType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgEnumType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEnumType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgEnumType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEnumType objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumType** objectArray = (SgEnumType**) &(SgEnumType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEnumType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEnumType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgEnumType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgEnumType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgEnumType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEnumType objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumType** objectArray = (SgEnumType**) &(SgEnumType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgEnumType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgEnumType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgEnumType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEnumType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgEnumType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgEnumType** objectArray = (SgEnumType**) &(SgEnumType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgEnumType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgEnumType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEnumType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgEnumType);

     return memory;
   }



/* #line 19976 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypedefType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypedefType objectArray [] = *(Memory_Block_List.begin());
     if (SgTypedefType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypedefType** objectArray = (SgTypedefType**) &(SgTypedefType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypedefType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypedefType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypedefType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypedefType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypedefType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypedefType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypedefType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypedefType objectArray [] = *(Memory_Block_List.begin());
     if (SgTypedefType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypedefType** objectArray = (SgTypedefType**) &(SgTypedefType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypedefType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypedefType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypedefType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypedefType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypedefType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypedefType objectArray [] = *(Memory_Block_List.begin());
     if (SgTypedefType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypedefType** objectArray = (SgTypedefType**) &(SgTypedefType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypedefType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypedefType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypedefType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypedefType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypedefType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypedefType** objectArray = (SgTypedefType**) &(SgTypedefType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypedefType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypedefType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypedefType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypedefType);

     return memory;
   }



/* #line 20208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNonrealType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNonrealType objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealType** objectArray = (SgNonrealType**) &(SgNonrealType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNonrealType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNonrealType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNonrealType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNonrealType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNonrealType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNonrealType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNonrealType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNonrealType objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealType** objectArray = (SgNonrealType**) &(SgNonrealType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNonrealType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNonrealType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNonrealType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNonrealType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNonrealType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNonrealType objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealType** objectArray = (SgNonrealType**) &(SgNonrealType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNonrealType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNonrealType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNonrealType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNonrealType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNonrealType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNonrealType** objectArray = (SgNonrealType**) &(SgNonrealType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNonrealType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNonrealType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNonrealType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNonrealType);

     return memory;
   }



/* #line 20440 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaParameterizedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaParameterizedType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaParameterizedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaParameterizedType** objectArray = (SgJavaParameterizedType**) &(SgJavaParameterizedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaParameterizedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaParameterizedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaParameterizedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaParameterizedType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaParameterizedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaParameterizedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaParameterizedType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaParameterizedType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaParameterizedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaParameterizedType** objectArray = (SgJavaParameterizedType**) &(SgJavaParameterizedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaParameterizedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaParameterizedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaParameterizedType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaParameterizedType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaParameterizedType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaParameterizedType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaParameterizedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaParameterizedType** objectArray = (SgJavaParameterizedType**) &(SgJavaParameterizedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaParameterizedType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaParameterizedType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaParameterizedType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaParameterizedType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaParameterizedType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaParameterizedType** objectArray = (SgJavaParameterizedType**) &(SgJavaParameterizedType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaParameterizedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaParameterizedType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaParameterizedType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaParameterizedType);

     return memory;
   }



/* #line 20672 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaQualifiedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaQualifiedType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaQualifiedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaQualifiedType** objectArray = (SgJavaQualifiedType**) &(SgJavaQualifiedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaQualifiedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaQualifiedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaQualifiedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaQualifiedType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaQualifiedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaQualifiedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaQualifiedType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaQualifiedType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaQualifiedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaQualifiedType** objectArray = (SgJavaQualifiedType**) &(SgJavaQualifiedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaQualifiedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaQualifiedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaQualifiedType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaQualifiedType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaQualifiedType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaQualifiedType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaQualifiedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaQualifiedType** objectArray = (SgJavaQualifiedType**) &(SgJavaQualifiedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaQualifiedType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaQualifiedType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaQualifiedType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaQualifiedType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaQualifiedType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaQualifiedType** objectArray = (SgJavaQualifiedType**) &(SgJavaQualifiedType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaQualifiedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaQualifiedType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaQualifiedType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaQualifiedType);

     return memory;
   }



/* #line 20904 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaWildcardType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaWildcardType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaWildcardType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaWildcardType** objectArray = (SgJavaWildcardType**) &(SgJavaWildcardType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaWildcardType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaWildcardType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaWildcardType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaWildcardType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaWildcardType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaWildcardType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaWildcardType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaWildcardType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaWildcardType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaWildcardType** objectArray = (SgJavaWildcardType**) &(SgJavaWildcardType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaWildcardType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaWildcardType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaWildcardType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaWildcardType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaWildcardType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaWildcardType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaWildcardType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaWildcardType** objectArray = (SgJavaWildcardType**) &(SgJavaWildcardType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaWildcardType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaWildcardType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaWildcardType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaWildcardType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaWildcardType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaWildcardType** objectArray = (SgJavaWildcardType**) &(SgJavaWildcardType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaWildcardType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaWildcardType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaWildcardType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaWildcardType);

     return memory;
   }



/* #line 21136 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaTaskType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaTaskType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskType** objectArray = (SgAdaTaskType**) &(SgAdaTaskType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaTaskType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaTaskType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaTaskType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaTaskType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaTaskType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskType** objectArray = (SgAdaTaskType**) &(SgAdaTaskType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaTaskType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaTaskType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaTaskType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskType** objectArray = (SgAdaTaskType**) &(SgAdaTaskType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaTaskType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaTaskType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaTaskType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaTaskType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaTaskType** objectArray = (SgAdaTaskType**) &(SgAdaTaskType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaTaskType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaTaskType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaTaskType);

     return memory;
   }



/* #line 21368 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaProtectedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaProtectedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedType** objectArray = (SgAdaProtectedType**) &(SgAdaProtectedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaProtectedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaProtectedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaProtectedType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaProtectedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaProtectedType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedType** objectArray = (SgAdaProtectedType**) &(SgAdaProtectedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaProtectedType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaProtectedType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaProtectedType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedType** objectArray = (SgAdaProtectedType**) &(SgAdaProtectedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaProtectedType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaProtectedType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaProtectedType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaProtectedType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaProtectedType** objectArray = (SgAdaProtectedType**) &(SgAdaProtectedType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaProtectedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaProtectedType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaProtectedType);

     return memory;
   }



/* #line 21600 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaFormalType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaFormalType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFormalType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFormalType** objectArray = (SgAdaFormalType**) &(SgAdaFormalType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaFormalType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaFormalType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaFormalType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaFormalType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaFormalType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaFormalType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaFormalType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaFormalType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFormalType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFormalType** objectArray = (SgAdaFormalType**) &(SgAdaFormalType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaFormalType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaFormalType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaFormalType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaFormalType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaFormalType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaFormalType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFormalType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFormalType** objectArray = (SgAdaFormalType**) &(SgAdaFormalType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaFormalType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaFormalType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaFormalType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaFormalType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaFormalType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaFormalType** objectArray = (SgAdaFormalType**) &(SgAdaFormalType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaFormalType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaFormalType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaFormalType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaFormalType);

     return memory;
   }



/* #line 21832 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaDiscriminatedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaDiscriminatedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDiscriminatedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDiscriminatedType** objectArray = (SgAdaDiscriminatedType**) &(SgAdaDiscriminatedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDiscriminatedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaDiscriminatedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaDiscriminatedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaDiscriminatedType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaDiscriminatedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDiscriminatedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaDiscriminatedType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDiscriminatedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDiscriminatedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDiscriminatedType** objectArray = (SgAdaDiscriminatedType**) &(SgAdaDiscriminatedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDiscriminatedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDiscriminatedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaDiscriminatedType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaDiscriminatedType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaDiscriminatedType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDiscriminatedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDiscriminatedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDiscriminatedType** objectArray = (SgAdaDiscriminatedType**) &(SgAdaDiscriminatedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaDiscriminatedType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaDiscriminatedType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaDiscriminatedType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDiscriminatedType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaDiscriminatedType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaDiscriminatedType** objectArray = (SgAdaDiscriminatedType**) &(SgAdaDiscriminatedType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaDiscriminatedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaDiscriminatedType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDiscriminatedType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaDiscriminatedType);

     return memory;
   }



/* #line 22064 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgScopedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgScopedType objectArray [] = *(Memory_Block_List.begin());
     if (SgScopedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgScopedType** objectArray = (SgScopedType**) &(SgScopedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgScopedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgScopedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgScopedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgScopedType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgScopedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgScopedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgScopedType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgScopedType objectArray [] = *(Memory_Block_List.begin());
     if (SgScopedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgScopedType** objectArray = (SgScopedType**) &(SgScopedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgScopedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgScopedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgScopedType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgScopedType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgScopedType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgScopedType objectArray [] = *(Memory_Block_List.begin());
     if (SgScopedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgScopedType** objectArray = (SgScopedType**) &(SgScopedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgScopedType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgScopedType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgScopedType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgScopedType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgScopedType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgScopedType** objectArray = (SgScopedType**) &(SgScopedType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgScopedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgScopedType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgScopedType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgScopedType);

     return memory;
   }



/* #line 22296 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgModifierType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgModifierType objectArray [] = *(Memory_Block_List.begin());
     if (SgModifierType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModifierType** objectArray = (SgModifierType**) &(SgModifierType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModifierType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgModifierType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgModifierType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgModifierType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgModifierType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModifierType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgModifierType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModifierType objectArray [] = *(Memory_Block_List.begin());
     if (SgModifierType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModifierType** objectArray = (SgModifierType**) &(SgModifierType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModifierType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModifierType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgModifierType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgModifierType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgModifierType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModifierType objectArray [] = *(Memory_Block_List.begin());
     if (SgModifierType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModifierType** objectArray = (SgModifierType**) &(SgModifierType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgModifierType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgModifierType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgModifierType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModifierType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgModifierType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgModifierType** objectArray = (SgModifierType**) &(SgModifierType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgModifierType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgModifierType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModifierType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgModifierType);

     return memory;
   }



/* #line 22528 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFunctionType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFunctionType objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionType** objectArray = (SgFunctionType**) &(SgFunctionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFunctionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFunctionType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFunctionType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFunctionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFunctionType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionType objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionType** objectArray = (SgFunctionType**) &(SgFunctionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFunctionType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFunctionType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFunctionType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionType objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionType** objectArray = (SgFunctionType**) &(SgFunctionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFunctionType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFunctionType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFunctionType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFunctionType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFunctionType** objectArray = (SgFunctionType**) &(SgFunctionType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFunctionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFunctionType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFunctionType);

     return memory;
   }



/* #line 22760 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMemberFunctionType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMemberFunctionType objectArray [] = *(Memory_Block_List.begin());
     if (SgMemberFunctionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMemberFunctionType** objectArray = (SgMemberFunctionType**) &(SgMemberFunctionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMemberFunctionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMemberFunctionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMemberFunctionType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMemberFunctionType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMemberFunctionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMemberFunctionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMemberFunctionType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMemberFunctionType objectArray [] = *(Memory_Block_List.begin());
     if (SgMemberFunctionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMemberFunctionType** objectArray = (SgMemberFunctionType**) &(SgMemberFunctionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMemberFunctionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMemberFunctionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMemberFunctionType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMemberFunctionType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMemberFunctionType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMemberFunctionType objectArray [] = *(Memory_Block_List.begin());
     if (SgMemberFunctionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMemberFunctionType** objectArray = (SgMemberFunctionType**) &(SgMemberFunctionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMemberFunctionType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMemberFunctionType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMemberFunctionType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMemberFunctionType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMemberFunctionType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMemberFunctionType** objectArray = (SgMemberFunctionType**) &(SgMemberFunctionType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMemberFunctionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMemberFunctionType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMemberFunctionType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMemberFunctionType);

     return memory;
   }



/* #line 22992 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPartialFunctionType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPartialFunctionType objectArray [] = *(Memory_Block_List.begin());
     if (SgPartialFunctionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPartialFunctionType** objectArray = (SgPartialFunctionType**) &(SgPartialFunctionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPartialFunctionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPartialFunctionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPartialFunctionType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPartialFunctionType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPartialFunctionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPartialFunctionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPartialFunctionType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPartialFunctionType objectArray [] = *(Memory_Block_List.begin());
     if (SgPartialFunctionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPartialFunctionType** objectArray = (SgPartialFunctionType**) &(SgPartialFunctionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPartialFunctionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPartialFunctionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPartialFunctionType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPartialFunctionType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPartialFunctionType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPartialFunctionType objectArray [] = *(Memory_Block_List.begin());
     if (SgPartialFunctionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPartialFunctionType** objectArray = (SgPartialFunctionType**) &(SgPartialFunctionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPartialFunctionType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPartialFunctionType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPartialFunctionType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPartialFunctionType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPartialFunctionType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPartialFunctionType** objectArray = (SgPartialFunctionType**) &(SgPartialFunctionType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPartialFunctionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPartialFunctionType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPartialFunctionType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPartialFunctionType);

     return memory;
   }



/* #line 23224 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPartialFunctionModifierType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPartialFunctionModifierType objectArray [] = *(Memory_Block_List.begin());
     if (SgPartialFunctionModifierType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPartialFunctionModifierType** objectArray = (SgPartialFunctionModifierType**) &(SgPartialFunctionModifierType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPartialFunctionModifierType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPartialFunctionModifierType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPartialFunctionModifierType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPartialFunctionModifierType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPartialFunctionModifierType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPartialFunctionModifierType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPartialFunctionModifierType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPartialFunctionModifierType objectArray [] = *(Memory_Block_List.begin());
     if (SgPartialFunctionModifierType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPartialFunctionModifierType** objectArray = (SgPartialFunctionModifierType**) &(SgPartialFunctionModifierType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPartialFunctionModifierType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPartialFunctionModifierType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPartialFunctionModifierType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPartialFunctionModifierType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPartialFunctionModifierType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPartialFunctionModifierType objectArray [] = *(Memory_Block_List.begin());
     if (SgPartialFunctionModifierType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPartialFunctionModifierType** objectArray = (SgPartialFunctionModifierType**) &(SgPartialFunctionModifierType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPartialFunctionModifierType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPartialFunctionModifierType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPartialFunctionModifierType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPartialFunctionModifierType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPartialFunctionModifierType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPartialFunctionModifierType** objectArray = (SgPartialFunctionModifierType**) &(SgPartialFunctionModifierType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPartialFunctionModifierType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPartialFunctionModifierType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPartialFunctionModifierType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPartialFunctionModifierType);

     return memory;
   }



/* #line 23456 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgArrayType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgArrayType objectArray [] = *(Memory_Block_List.begin());
     if (SgArrayType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgArrayType** objectArray = (SgArrayType**) &(SgArrayType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgArrayType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgArrayType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgArrayType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgArrayType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgArrayType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgArrayType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgArrayType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgArrayType objectArray [] = *(Memory_Block_List.begin());
     if (SgArrayType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgArrayType** objectArray = (SgArrayType**) &(SgArrayType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgArrayType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgArrayType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgArrayType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgArrayType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgArrayType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgArrayType objectArray [] = *(Memory_Block_List.begin());
     if (SgArrayType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgArrayType** objectArray = (SgArrayType**) &(SgArrayType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgArrayType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgArrayType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgArrayType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgArrayType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgArrayType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgArrayType** objectArray = (SgArrayType**) &(SgArrayType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgArrayType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgArrayType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgArrayType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgArrayType);

     return memory;
   }



/* #line 23688 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeEllipse::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeEllipse objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeEllipse::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeEllipse** objectArray = (SgTypeEllipse**) &(SgTypeEllipse::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeEllipse::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeEllipse::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeEllipse::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeEllipse*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeEllipse::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeEllipse::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeEllipse::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeEllipse objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeEllipse::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeEllipse** objectArray = (SgTypeEllipse**) &(SgTypeEllipse::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeEllipse::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeEllipse::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeEllipse object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeEllipse object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeEllipse::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeEllipse objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeEllipse::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeEllipse** objectArray = (SgTypeEllipse**) &(SgTypeEllipse::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeEllipse::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeEllipse::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeEllipse found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeEllipse::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeEllipse::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeEllipse** objectArray = (SgTypeEllipse**) &(SgTypeEllipse::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeEllipse::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeEllipse::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeEllipse::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeEllipse);

     return memory;
   }



/* #line 23920 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateType objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateType** objectArray = (SgTemplateType**) &(SgTemplateType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateType objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateType** objectArray = (SgTemplateType**) &(SgTemplateType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateType objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateType** objectArray = (SgTemplateType**) &(SgTemplateType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateType** objectArray = (SgTemplateType**) &(SgTemplateType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateType);

     return memory;
   }



/* #line 24152 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgQualifiedNameType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgQualifiedNameType objectArray [] = *(Memory_Block_List.begin());
     if (SgQualifiedNameType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgQualifiedNameType** objectArray = (SgQualifiedNameType**) &(SgQualifiedNameType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgQualifiedNameType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgQualifiedNameType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgQualifiedNameType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgQualifiedNameType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgQualifiedNameType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgQualifiedNameType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgQualifiedNameType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgQualifiedNameType objectArray [] = *(Memory_Block_List.begin());
     if (SgQualifiedNameType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgQualifiedNameType** objectArray = (SgQualifiedNameType**) &(SgQualifiedNameType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgQualifiedNameType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgQualifiedNameType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgQualifiedNameType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgQualifiedNameType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgQualifiedNameType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgQualifiedNameType objectArray [] = *(Memory_Block_List.begin());
     if (SgQualifiedNameType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgQualifiedNameType** objectArray = (SgQualifiedNameType**) &(SgQualifiedNameType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgQualifiedNameType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgQualifiedNameType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgQualifiedNameType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgQualifiedNameType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgQualifiedNameType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgQualifiedNameType** objectArray = (SgQualifiedNameType**) &(SgQualifiedNameType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgQualifiedNameType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgQualifiedNameType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgQualifiedNameType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgQualifiedNameType);

     return memory;
   }



/* #line 24384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeComplex::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeComplex objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeComplex::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeComplex** objectArray = (SgTypeComplex**) &(SgTypeComplex::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeComplex::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeComplex::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeComplex::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeComplex*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeComplex::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeComplex::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeComplex::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeComplex objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeComplex::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeComplex** objectArray = (SgTypeComplex**) &(SgTypeComplex::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeComplex::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeComplex::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeComplex object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeComplex object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeComplex::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeComplex objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeComplex::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeComplex** objectArray = (SgTypeComplex**) &(SgTypeComplex::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeComplex::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeComplex::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeComplex found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeComplex::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeComplex::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeComplex** objectArray = (SgTypeComplex**) &(SgTypeComplex::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeComplex::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeComplex::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeComplex::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeComplex);

     return memory;
   }



/* #line 24616 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeImaginary::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeImaginary objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeImaginary::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeImaginary** objectArray = (SgTypeImaginary**) &(SgTypeImaginary::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeImaginary::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeImaginary::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeImaginary::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeImaginary*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeImaginary::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeImaginary::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeImaginary::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeImaginary objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeImaginary::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeImaginary** objectArray = (SgTypeImaginary**) &(SgTypeImaginary::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeImaginary::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeImaginary::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeImaginary object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeImaginary object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeImaginary::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeImaginary objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeImaginary::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeImaginary** objectArray = (SgTypeImaginary**) &(SgTypeImaginary::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeImaginary::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeImaginary::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeImaginary found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeImaginary::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeImaginary::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeImaginary** objectArray = (SgTypeImaginary**) &(SgTypeImaginary::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeImaginary::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeImaginary::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeImaginary::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeImaginary);

     return memory;
   }



/* #line 24848 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeDefault::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeDefault objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeDefault::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeDefault** objectArray = (SgTypeDefault**) &(SgTypeDefault::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeDefault::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeDefault::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeDefault::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeDefault*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeDefault::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeDefault::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeDefault::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeDefault objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeDefault::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeDefault** objectArray = (SgTypeDefault**) &(SgTypeDefault::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeDefault::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeDefault::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeDefault object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeDefault object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeDefault::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeDefault objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeDefault::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeDefault** objectArray = (SgTypeDefault**) &(SgTypeDefault::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeDefault::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeDefault::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeDefault found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeDefault::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeDefault::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeDefault** objectArray = (SgTypeDefault**) &(SgTypeDefault::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeDefault::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeDefault::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeDefault::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeDefault);

     return memory;
   }



/* #line 25080 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeCAFTeam::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeCAFTeam objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeCAFTeam::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeCAFTeam** objectArray = (SgTypeCAFTeam**) &(SgTypeCAFTeam::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeCAFTeam::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeCAFTeam::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeCAFTeam::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeCAFTeam*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeCAFTeam::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeCAFTeam::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeCAFTeam::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeCAFTeam objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeCAFTeam::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeCAFTeam** objectArray = (SgTypeCAFTeam**) &(SgTypeCAFTeam::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeCAFTeam::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeCAFTeam::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeCAFTeam object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeCAFTeam object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeCAFTeam::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeCAFTeam objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeCAFTeam::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeCAFTeam** objectArray = (SgTypeCAFTeam**) &(SgTypeCAFTeam::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeCAFTeam::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeCAFTeam::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeCAFTeam found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeCAFTeam::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeCAFTeam::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeCAFTeam** objectArray = (SgTypeCAFTeam**) &(SgTypeCAFTeam::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeCAFTeam::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeCAFTeam::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeCAFTeam::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeCAFTeam);

     return memory;
   }



/* #line 25312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeCrayPointer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeCrayPointer objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeCrayPointer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeCrayPointer** objectArray = (SgTypeCrayPointer**) &(SgTypeCrayPointer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeCrayPointer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeCrayPointer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeCrayPointer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeCrayPointer*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeCrayPointer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeCrayPointer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeCrayPointer::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeCrayPointer objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeCrayPointer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeCrayPointer** objectArray = (SgTypeCrayPointer**) &(SgTypeCrayPointer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeCrayPointer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeCrayPointer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeCrayPointer object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeCrayPointer object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeCrayPointer::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeCrayPointer objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeCrayPointer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeCrayPointer** objectArray = (SgTypeCrayPointer**) &(SgTypeCrayPointer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeCrayPointer::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeCrayPointer::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeCrayPointer found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeCrayPointer::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeCrayPointer::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeCrayPointer** objectArray = (SgTypeCrayPointer**) &(SgTypeCrayPointer::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeCrayPointer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeCrayPointer::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeCrayPointer::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeCrayPointer);

     return memory;
   }



/* #line 25544 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeLabel::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeLabel objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeLabel::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeLabel** objectArray = (SgTypeLabel**) &(SgTypeLabel::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeLabel::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeLabel::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeLabel::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeLabel*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeLabel::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeLabel::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeLabel::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeLabel objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeLabel::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeLabel** objectArray = (SgTypeLabel**) &(SgTypeLabel::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeLabel::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeLabel::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeLabel object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeLabel object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeLabel::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeLabel objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeLabel::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeLabel** objectArray = (SgTypeLabel**) &(SgTypeLabel::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeLabel::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeLabel::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeLabel found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeLabel::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeLabel::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeLabel** objectArray = (SgTypeLabel**) &(SgTypeLabel::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeLabel::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeLabel::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeLabel::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeLabel);

     return memory;
   }



/* #line 25776 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaUnionType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaUnionType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaUnionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaUnionType** objectArray = (SgJavaUnionType**) &(SgJavaUnionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaUnionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaUnionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaUnionType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaUnionType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaUnionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaUnionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaUnionType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaUnionType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaUnionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaUnionType** objectArray = (SgJavaUnionType**) &(SgJavaUnionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaUnionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaUnionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaUnionType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaUnionType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaUnionType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaUnionType objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaUnionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaUnionType** objectArray = (SgJavaUnionType**) &(SgJavaUnionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaUnionType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaUnionType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaUnionType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaUnionType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaUnionType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaUnionType** objectArray = (SgJavaUnionType**) &(SgJavaUnionType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaUnionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaUnionType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaUnionType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaUnionType);

     return memory;
   }



/* #line 26008 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgRvalueReferenceType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgRvalueReferenceType objectArray [] = *(Memory_Block_List.begin());
     if (SgRvalueReferenceType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRvalueReferenceType** objectArray = (SgRvalueReferenceType**) &(SgRvalueReferenceType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRvalueReferenceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgRvalueReferenceType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgRvalueReferenceType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgRvalueReferenceType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgRvalueReferenceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRvalueReferenceType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgRvalueReferenceType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRvalueReferenceType objectArray [] = *(Memory_Block_List.begin());
     if (SgRvalueReferenceType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRvalueReferenceType** objectArray = (SgRvalueReferenceType**) &(SgRvalueReferenceType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRvalueReferenceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRvalueReferenceType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgRvalueReferenceType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgRvalueReferenceType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgRvalueReferenceType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRvalueReferenceType objectArray [] = *(Memory_Block_List.begin());
     if (SgRvalueReferenceType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRvalueReferenceType** objectArray = (SgRvalueReferenceType**) &(SgRvalueReferenceType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgRvalueReferenceType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgRvalueReferenceType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgRvalueReferenceType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRvalueReferenceType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgRvalueReferenceType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgRvalueReferenceType** objectArray = (SgRvalueReferenceType**) &(SgRvalueReferenceType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgRvalueReferenceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgRvalueReferenceType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRvalueReferenceType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgRvalueReferenceType);

     return memory;
   }



/* #line 26240 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeNullptr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeNullptr objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeNullptr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeNullptr** objectArray = (SgTypeNullptr**) &(SgTypeNullptr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeNullptr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeNullptr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeNullptr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeNullptr*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeNullptr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeNullptr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeNullptr::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeNullptr objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeNullptr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeNullptr** objectArray = (SgTypeNullptr**) &(SgTypeNullptr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeNullptr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeNullptr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeNullptr object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeNullptr object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeNullptr::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeNullptr objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeNullptr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeNullptr** objectArray = (SgTypeNullptr**) &(SgTypeNullptr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeNullptr::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeNullptr::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeNullptr found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeNullptr::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeNullptr::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeNullptr** objectArray = (SgTypeNullptr**) &(SgTypeNullptr::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeNullptr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeNullptr::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeNullptr::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeNullptr);

     return memory;
   }



/* #line 26472 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDeclType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDeclType objectArray [] = *(Memory_Block_List.begin());
     if (SgDeclType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeclType** objectArray = (SgDeclType**) &(SgDeclType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeclType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDeclType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDeclType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDeclType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDeclType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeclType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDeclType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeclType objectArray [] = *(Memory_Block_List.begin());
     if (SgDeclType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeclType** objectArray = (SgDeclType**) &(SgDeclType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeclType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeclType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDeclType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDeclType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDeclType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeclType objectArray [] = *(Memory_Block_List.begin());
     if (SgDeclType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeclType** objectArray = (SgDeclType**) &(SgDeclType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDeclType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDeclType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDeclType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeclType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDeclType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDeclType** objectArray = (SgDeclType**) &(SgDeclType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDeclType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDeclType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeclType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDeclType);

     return memory;
   }



/* #line 26704 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeOfType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeOfType objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeOfType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeOfType** objectArray = (SgTypeOfType**) &(SgTypeOfType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeOfType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeOfType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeOfType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeOfType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeOfType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeOfType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeOfType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeOfType objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeOfType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeOfType** objectArray = (SgTypeOfType**) &(SgTypeOfType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeOfType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeOfType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeOfType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeOfType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeOfType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeOfType objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeOfType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeOfType** objectArray = (SgTypeOfType**) &(SgTypeOfType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeOfType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeOfType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeOfType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeOfType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeOfType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeOfType** objectArray = (SgTypeOfType**) &(SgTypeOfType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeOfType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeOfType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeOfType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeOfType);

     return memory;
   }



/* #line 26936 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeMatrix::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeMatrix objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeMatrix::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeMatrix** objectArray = (SgTypeMatrix**) &(SgTypeMatrix::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeMatrix::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeMatrix::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeMatrix::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeMatrix*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeMatrix::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeMatrix::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeMatrix::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeMatrix objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeMatrix::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeMatrix** objectArray = (SgTypeMatrix**) &(SgTypeMatrix::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeMatrix::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeMatrix::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeMatrix object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeMatrix object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeMatrix::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeMatrix objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeMatrix::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeMatrix** objectArray = (SgTypeMatrix**) &(SgTypeMatrix::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeMatrix::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeMatrix::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeMatrix found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeMatrix::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeMatrix::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeMatrix** objectArray = (SgTypeMatrix**) &(SgTypeMatrix::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeMatrix::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeMatrix::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeMatrix::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeMatrix);

     return memory;
   }



/* #line 27168 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeFixed::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeFixed objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFixed::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFixed** objectArray = (SgTypeFixed**) &(SgTypeFixed::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFixed::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeFixed::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeFixed::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeFixed*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeFixed::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFixed::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeFixed::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFixed objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFixed::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFixed** objectArray = (SgTypeFixed**) &(SgTypeFixed::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFixed::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFixed::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeFixed object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeFixed object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeFixed::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFixed objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFixed::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFixed** objectArray = (SgTypeFixed**) &(SgTypeFixed::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeFixed::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeFixed::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeFixed found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFixed::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeFixed::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeFixed** objectArray = (SgTypeFixed**) &(SgTypeFixed::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeFixed::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeFixed::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFixed::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeFixed);

     return memory;
   }



/* #line 27400 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAutoType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAutoType objectArray [] = *(Memory_Block_List.begin());
     if (SgAutoType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAutoType** objectArray = (SgAutoType**) &(SgAutoType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAutoType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAutoType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAutoType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAutoType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAutoType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAutoType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAutoType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAutoType objectArray [] = *(Memory_Block_List.begin());
     if (SgAutoType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAutoType** objectArray = (SgAutoType**) &(SgAutoType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAutoType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAutoType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAutoType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAutoType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAutoType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAutoType objectArray [] = *(Memory_Block_List.begin());
     if (SgAutoType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAutoType** objectArray = (SgAutoType**) &(SgAutoType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAutoType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAutoType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAutoType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAutoType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAutoType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAutoType** objectArray = (SgAutoType**) &(SgAutoType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAutoType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAutoType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAutoType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAutoType);

     return memory;
   }



/* #line 27632 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaAccessType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaAccessType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAccessType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAccessType** objectArray = (SgAdaAccessType**) &(SgAdaAccessType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaAccessType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaAccessType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaAccessType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaAccessType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaAccessType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaAccessType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaAccessType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaAccessType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAccessType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAccessType** objectArray = (SgAdaAccessType**) &(SgAdaAccessType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaAccessType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaAccessType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaAccessType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaAccessType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaAccessType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaAccessType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAccessType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAccessType** objectArray = (SgAdaAccessType**) &(SgAdaAccessType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaAccessType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaAccessType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaAccessType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaAccessType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaAccessType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaAccessType** objectArray = (SgAdaAccessType**) &(SgAdaAccessType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaAccessType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaAccessType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaAccessType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaAccessType);

     return memory;
   }



/* #line 27864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaSubtype::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaSubtype objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaSubtype::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaSubtype** objectArray = (SgAdaSubtype**) &(SgAdaSubtype::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaSubtype::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaSubtype::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaSubtype::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaSubtype*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaSubtype::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaSubtype::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaSubtype::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaSubtype objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaSubtype::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaSubtype** objectArray = (SgAdaSubtype**) &(SgAdaSubtype::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaSubtype::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaSubtype::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaSubtype object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaSubtype object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaSubtype::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaSubtype objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaSubtype::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaSubtype** objectArray = (SgAdaSubtype**) &(SgAdaSubtype::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaSubtype::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaSubtype::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaSubtype found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaSubtype::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaSubtype::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaSubtype** objectArray = (SgAdaSubtype**) &(SgAdaSubtype::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaSubtype::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaSubtype::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaSubtype::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaSubtype);

     return memory;
   }



/* #line 28096 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaDiscreteType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaDiscreteType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDiscreteType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDiscreteType** objectArray = (SgAdaDiscreteType**) &(SgAdaDiscreteType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDiscreteType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaDiscreteType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaDiscreteType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaDiscreteType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaDiscreteType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDiscreteType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaDiscreteType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDiscreteType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDiscreteType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDiscreteType** objectArray = (SgAdaDiscreteType**) &(SgAdaDiscreteType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDiscreteType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDiscreteType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaDiscreteType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaDiscreteType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaDiscreteType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDiscreteType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDiscreteType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDiscreteType** objectArray = (SgAdaDiscreteType**) &(SgAdaDiscreteType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaDiscreteType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaDiscreteType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaDiscreteType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDiscreteType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaDiscreteType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaDiscreteType** objectArray = (SgAdaDiscreteType**) &(SgAdaDiscreteType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaDiscreteType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaDiscreteType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDiscreteType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaDiscreteType);

     return memory;
   }



/* #line 28328 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaDerivedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaDerivedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDerivedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDerivedType** objectArray = (SgAdaDerivedType**) &(SgAdaDerivedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDerivedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaDerivedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaDerivedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaDerivedType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaDerivedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDerivedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaDerivedType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDerivedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDerivedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDerivedType** objectArray = (SgAdaDerivedType**) &(SgAdaDerivedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDerivedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDerivedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaDerivedType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaDerivedType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaDerivedType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDerivedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDerivedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDerivedType** objectArray = (SgAdaDerivedType**) &(SgAdaDerivedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaDerivedType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaDerivedType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaDerivedType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDerivedType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaDerivedType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaDerivedType** objectArray = (SgAdaDerivedType**) &(SgAdaDerivedType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaDerivedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaDerivedType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDerivedType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaDerivedType);

     return memory;
   }



/* #line 28560 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaSubroutineType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaSubroutineType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaSubroutineType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaSubroutineType** objectArray = (SgAdaSubroutineType**) &(SgAdaSubroutineType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaSubroutineType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaSubroutineType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaSubroutineType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaSubroutineType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaSubroutineType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaSubroutineType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaSubroutineType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaSubroutineType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaSubroutineType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaSubroutineType** objectArray = (SgAdaSubroutineType**) &(SgAdaSubroutineType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaSubroutineType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaSubroutineType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaSubroutineType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaSubroutineType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaSubroutineType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaSubroutineType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaSubroutineType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaSubroutineType** objectArray = (SgAdaSubroutineType**) &(SgAdaSubroutineType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaSubroutineType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaSubroutineType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaSubroutineType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaSubroutineType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaSubroutineType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaSubroutineType** objectArray = (SgAdaSubroutineType**) &(SgAdaSubroutineType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaSubroutineType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaSubroutineType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaSubroutineType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaSubroutineType);

     return memory;
   }



/* #line 28792 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIntegralType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIntegralType objectArray [] = *(Memory_Block_List.begin());
     if (SgIntegralType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntegralType** objectArray = (SgIntegralType**) &(SgIntegralType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIntegralType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIntegralType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIntegralType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIntegralType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIntegralType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIntegralType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIntegralType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIntegralType objectArray [] = *(Memory_Block_List.begin());
     if (SgIntegralType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntegralType** objectArray = (SgIntegralType**) &(SgIntegralType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIntegralType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIntegralType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIntegralType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIntegralType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIntegralType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIntegralType objectArray [] = *(Memory_Block_List.begin());
     if (SgIntegralType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntegralType** objectArray = (SgIntegralType**) &(SgIntegralType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIntegralType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIntegralType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIntegralType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIntegralType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIntegralType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIntegralType** objectArray = (SgIntegralType**) &(SgIntegralType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIntegralType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIntegralType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIntegralType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIntegralType);

     return memory;
   }



/* #line 29024 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSignedIntegralType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSignedIntegralType objectArray [] = *(Memory_Block_List.begin());
     if (SgSignedIntegralType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSignedIntegralType** objectArray = (SgSignedIntegralType**) &(SgSignedIntegralType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSignedIntegralType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSignedIntegralType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSignedIntegralType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSignedIntegralType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSignedIntegralType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSignedIntegralType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSignedIntegralType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSignedIntegralType objectArray [] = *(Memory_Block_List.begin());
     if (SgSignedIntegralType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSignedIntegralType** objectArray = (SgSignedIntegralType**) &(SgSignedIntegralType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSignedIntegralType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSignedIntegralType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSignedIntegralType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSignedIntegralType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSignedIntegralType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSignedIntegralType objectArray [] = *(Memory_Block_List.begin());
     if (SgSignedIntegralType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSignedIntegralType** objectArray = (SgSignedIntegralType**) &(SgSignedIntegralType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSignedIntegralType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSignedIntegralType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSignedIntegralType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSignedIntegralType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSignedIntegralType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSignedIntegralType** objectArray = (SgSignedIntegralType**) &(SgSignedIntegralType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSignedIntegralType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSignedIntegralType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSignedIntegralType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSignedIntegralType);

     return memory;
   }



/* #line 29256 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeInt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeInt objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeInt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeInt** objectArray = (SgTypeInt**) &(SgTypeInt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeInt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeInt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeInt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeInt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeInt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeInt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeInt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeInt objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeInt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeInt** objectArray = (SgTypeInt**) &(SgTypeInt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeInt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeInt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeInt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeInt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeInt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeInt objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeInt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeInt** objectArray = (SgTypeInt**) &(SgTypeInt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeInt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeInt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeInt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeInt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeInt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeInt** objectArray = (SgTypeInt**) &(SgTypeInt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeInt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeInt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeInt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeInt);

     return memory;
   }



/* #line 29488 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeLong::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeLong** objectArray = (SgTypeLong**) &(SgTypeLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeLong::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeLong*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeLong::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeLong** objectArray = (SgTypeLong**) &(SgTypeLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeLong object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeLong object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeLong::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeLong** objectArray = (SgTypeLong**) &(SgTypeLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeLong::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeLong::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeLong found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeLong::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeLong::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeLong** objectArray = (SgTypeLong**) &(SgTypeLong::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeLong::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeLong::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeLong);

     return memory;
   }



/* #line 29720 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeLongLong::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeLongLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeLongLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeLongLong** objectArray = (SgTypeLongLong**) &(SgTypeLongLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeLongLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeLongLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeLongLong::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeLongLong*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeLongLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeLongLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeLongLong::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeLongLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeLongLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeLongLong** objectArray = (SgTypeLongLong**) &(SgTypeLongLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeLongLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeLongLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeLongLong object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeLongLong object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeLongLong::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeLongLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeLongLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeLongLong** objectArray = (SgTypeLongLong**) &(SgTypeLongLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeLongLong::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeLongLong::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeLongLong found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeLongLong::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeLongLong::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeLongLong** objectArray = (SgTypeLongLong**) &(SgTypeLongLong::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeLongLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeLongLong::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeLongLong::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeLongLong);

     return memory;
   }



/* #line 29952 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeShort::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeShort objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeShort::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeShort** objectArray = (SgTypeShort**) &(SgTypeShort::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeShort::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeShort::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeShort::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeShort*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeShort::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeShort::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeShort::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeShort objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeShort::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeShort** objectArray = (SgTypeShort**) &(SgTypeShort::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeShort::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeShort::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeShort object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeShort object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeShort::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeShort objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeShort::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeShort** objectArray = (SgTypeShort**) &(SgTypeShort::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeShort::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeShort::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeShort found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeShort::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeShort::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeShort** objectArray = (SgTypeShort**) &(SgTypeShort::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeShort::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeShort::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeShort::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeShort);

     return memory;
   }



/* #line 30184 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeSigned128bitInteger::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeSigned128bitInteger objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSigned128bitInteger::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSigned128bitInteger** objectArray = (SgTypeSigned128bitInteger**) &(SgTypeSigned128bitInteger::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSigned128bitInteger::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeSigned128bitInteger::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeSigned128bitInteger::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeSigned128bitInteger*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeSigned128bitInteger::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSigned128bitInteger::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeSigned128bitInteger::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSigned128bitInteger objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSigned128bitInteger::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSigned128bitInteger** objectArray = (SgTypeSigned128bitInteger**) &(SgTypeSigned128bitInteger::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSigned128bitInteger::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSigned128bitInteger::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeSigned128bitInteger object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeSigned128bitInteger object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeSigned128bitInteger::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSigned128bitInteger objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSigned128bitInteger::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSigned128bitInteger** objectArray = (SgTypeSigned128bitInteger**) &(SgTypeSigned128bitInteger::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeSigned128bitInteger::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeSigned128bitInteger::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeSigned128bitInteger found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSigned128bitInteger::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeSigned128bitInteger::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeSigned128bitInteger** objectArray = (SgTypeSigned128bitInteger**) &(SgTypeSigned128bitInteger::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeSigned128bitInteger::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeSigned128bitInteger::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSigned128bitInteger::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeSigned128bitInteger);

     return memory;
   }



/* #line 30416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeSignedChar::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeSignedChar objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedChar::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedChar** objectArray = (SgTypeSignedChar**) &(SgTypeSignedChar::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSignedChar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeSignedChar::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeSignedChar::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeSignedChar*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeSignedChar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSignedChar::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeSignedChar::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSignedChar objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedChar::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedChar** objectArray = (SgTypeSignedChar**) &(SgTypeSignedChar::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSignedChar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSignedChar::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeSignedChar object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeSignedChar object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeSignedChar::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSignedChar objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedChar::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedChar** objectArray = (SgTypeSignedChar**) &(SgTypeSignedChar::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeSignedChar::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeSignedChar::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeSignedChar found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSignedChar::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeSignedChar::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeSignedChar** objectArray = (SgTypeSignedChar**) &(SgTypeSignedChar::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeSignedChar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeSignedChar::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSignedChar::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeSignedChar);

     return memory;
   }



/* #line 30648 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeSignedInt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeSignedInt objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedInt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedInt** objectArray = (SgTypeSignedInt**) &(SgTypeSignedInt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSignedInt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeSignedInt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeSignedInt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeSignedInt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeSignedInt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSignedInt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeSignedInt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSignedInt objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedInt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedInt** objectArray = (SgTypeSignedInt**) &(SgTypeSignedInt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSignedInt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSignedInt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeSignedInt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeSignedInt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeSignedInt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSignedInt objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedInt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedInt** objectArray = (SgTypeSignedInt**) &(SgTypeSignedInt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeSignedInt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeSignedInt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeSignedInt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSignedInt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeSignedInt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeSignedInt** objectArray = (SgTypeSignedInt**) &(SgTypeSignedInt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeSignedInt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeSignedInt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSignedInt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeSignedInt);

     return memory;
   }



/* #line 30880 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeSignedLong::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeSignedLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedLong** objectArray = (SgTypeSignedLong**) &(SgTypeSignedLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSignedLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeSignedLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeSignedLong::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeSignedLong*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeSignedLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSignedLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeSignedLong::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSignedLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedLong** objectArray = (SgTypeSignedLong**) &(SgTypeSignedLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSignedLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSignedLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeSignedLong object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeSignedLong object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeSignedLong::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSignedLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedLong** objectArray = (SgTypeSignedLong**) &(SgTypeSignedLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeSignedLong::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeSignedLong::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeSignedLong found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSignedLong::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeSignedLong::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeSignedLong** objectArray = (SgTypeSignedLong**) &(SgTypeSignedLong::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeSignedLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeSignedLong::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSignedLong::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeSignedLong);

     return memory;
   }



/* #line 31112 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeSignedLongLong::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeSignedLongLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedLongLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedLongLong** objectArray = (SgTypeSignedLongLong**) &(SgTypeSignedLongLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSignedLongLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeSignedLongLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeSignedLongLong::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeSignedLongLong*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeSignedLongLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSignedLongLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeSignedLongLong::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSignedLongLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedLongLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedLongLong** objectArray = (SgTypeSignedLongLong**) &(SgTypeSignedLongLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSignedLongLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSignedLongLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeSignedLongLong object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeSignedLongLong object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeSignedLongLong::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSignedLongLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedLongLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedLongLong** objectArray = (SgTypeSignedLongLong**) &(SgTypeSignedLongLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeSignedLongLong::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeSignedLongLong::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeSignedLongLong found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSignedLongLong::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeSignedLongLong::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeSignedLongLong** objectArray = (SgTypeSignedLongLong**) &(SgTypeSignedLongLong::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeSignedLongLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeSignedLongLong::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSignedLongLong::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeSignedLongLong);

     return memory;
   }



/* #line 31344 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeSignedShort::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeSignedShort objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedShort::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedShort** objectArray = (SgTypeSignedShort**) &(SgTypeSignedShort::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSignedShort::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeSignedShort::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeSignedShort::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeSignedShort*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeSignedShort::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSignedShort::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeSignedShort::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSignedShort objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedShort::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedShort** objectArray = (SgTypeSignedShort**) &(SgTypeSignedShort::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSignedShort::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSignedShort::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeSignedShort object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeSignedShort object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeSignedShort::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSignedShort objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSignedShort::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSignedShort** objectArray = (SgTypeSignedShort**) &(SgTypeSignedShort::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeSignedShort::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeSignedShort::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeSignedShort found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSignedShort::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeSignedShort::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeSignedShort** objectArray = (SgTypeSignedShort**) &(SgTypeSignedShort::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeSignedShort::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeSignedShort::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSignedShort::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeSignedShort);

     return memory;
   }



/* #line 31576 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUnsignedIntegralType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUnsignedIntegralType objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedIntegralType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedIntegralType** objectArray = (SgUnsignedIntegralType**) &(SgUnsignedIntegralType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnsignedIntegralType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUnsignedIntegralType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUnsignedIntegralType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUnsignedIntegralType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUnsignedIntegralType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnsignedIntegralType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUnsignedIntegralType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnsignedIntegralType objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedIntegralType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedIntegralType** objectArray = (SgUnsignedIntegralType**) &(SgUnsignedIntegralType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnsignedIntegralType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnsignedIntegralType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUnsignedIntegralType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUnsignedIntegralType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUnsignedIntegralType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnsignedIntegralType objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedIntegralType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedIntegralType** objectArray = (SgUnsignedIntegralType**) &(SgUnsignedIntegralType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUnsignedIntegralType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUnsignedIntegralType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUnsignedIntegralType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnsignedIntegralType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUnsignedIntegralType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUnsignedIntegralType** objectArray = (SgUnsignedIntegralType**) &(SgUnsignedIntegralType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUnsignedIntegralType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUnsignedIntegralType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnsignedIntegralType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUnsignedIntegralType);

     return memory;
   }



/* #line 31808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeUnsigned128bitInteger::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeUnsigned128bitInteger objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsigned128bitInteger::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsigned128bitInteger** objectArray = (SgTypeUnsigned128bitInteger**) &(SgTypeUnsigned128bitInteger::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnsigned128bitInteger::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeUnsigned128bitInteger::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeUnsigned128bitInteger::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeUnsigned128bitInteger*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeUnsigned128bitInteger::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnsigned128bitInteger::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeUnsigned128bitInteger::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnsigned128bitInteger objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsigned128bitInteger::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsigned128bitInteger** objectArray = (SgTypeUnsigned128bitInteger**) &(SgTypeUnsigned128bitInteger::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnsigned128bitInteger::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnsigned128bitInteger::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeUnsigned128bitInteger object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeUnsigned128bitInteger object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeUnsigned128bitInteger::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnsigned128bitInteger objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsigned128bitInteger::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsigned128bitInteger** objectArray = (SgTypeUnsigned128bitInteger**) &(SgTypeUnsigned128bitInteger::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeUnsigned128bitInteger::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeUnsigned128bitInteger::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeUnsigned128bitInteger found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnsigned128bitInteger::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeUnsigned128bitInteger::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeUnsigned128bitInteger** objectArray = (SgTypeUnsigned128bitInteger**) &(SgTypeUnsigned128bitInteger::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeUnsigned128bitInteger::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeUnsigned128bitInteger::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnsigned128bitInteger::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeUnsigned128bitInteger);

     return memory;
   }



/* #line 32040 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeUnsignedChar::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeUnsignedChar objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedChar::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedChar** objectArray = (SgTypeUnsignedChar**) &(SgTypeUnsignedChar::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnsignedChar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeUnsignedChar::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeUnsignedChar::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeUnsignedChar*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeUnsignedChar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnsignedChar::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeUnsignedChar::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnsignedChar objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedChar::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedChar** objectArray = (SgTypeUnsignedChar**) &(SgTypeUnsignedChar::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnsignedChar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnsignedChar::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeUnsignedChar object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeUnsignedChar object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeUnsignedChar::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnsignedChar objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedChar::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedChar** objectArray = (SgTypeUnsignedChar**) &(SgTypeUnsignedChar::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeUnsignedChar::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeUnsignedChar::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeUnsignedChar found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnsignedChar::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeUnsignedChar::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeUnsignedChar** objectArray = (SgTypeUnsignedChar**) &(SgTypeUnsignedChar::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeUnsignedChar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeUnsignedChar::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnsignedChar::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeUnsignedChar);

     return memory;
   }



/* #line 32272 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeUnsignedInt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeUnsignedInt objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedInt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedInt** objectArray = (SgTypeUnsignedInt**) &(SgTypeUnsignedInt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnsignedInt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeUnsignedInt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeUnsignedInt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeUnsignedInt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeUnsignedInt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnsignedInt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeUnsignedInt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnsignedInt objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedInt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedInt** objectArray = (SgTypeUnsignedInt**) &(SgTypeUnsignedInt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnsignedInt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnsignedInt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeUnsignedInt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeUnsignedInt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeUnsignedInt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnsignedInt objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedInt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedInt** objectArray = (SgTypeUnsignedInt**) &(SgTypeUnsignedInt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeUnsignedInt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeUnsignedInt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeUnsignedInt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnsignedInt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeUnsignedInt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeUnsignedInt** objectArray = (SgTypeUnsignedInt**) &(SgTypeUnsignedInt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeUnsignedInt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeUnsignedInt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnsignedInt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeUnsignedInt);

     return memory;
   }



/* #line 32504 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeUnsignedLong::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeUnsignedLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedLong** objectArray = (SgTypeUnsignedLong**) &(SgTypeUnsignedLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnsignedLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeUnsignedLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeUnsignedLong::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeUnsignedLong*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeUnsignedLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnsignedLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeUnsignedLong::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnsignedLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedLong** objectArray = (SgTypeUnsignedLong**) &(SgTypeUnsignedLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnsignedLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnsignedLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeUnsignedLong object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeUnsignedLong object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeUnsignedLong::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnsignedLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedLong** objectArray = (SgTypeUnsignedLong**) &(SgTypeUnsignedLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeUnsignedLong::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeUnsignedLong::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeUnsignedLong found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnsignedLong::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeUnsignedLong::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeUnsignedLong** objectArray = (SgTypeUnsignedLong**) &(SgTypeUnsignedLong::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeUnsignedLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeUnsignedLong::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnsignedLong::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeUnsignedLong);

     return memory;
   }



/* #line 32736 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeUnsignedLongLong::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeUnsignedLongLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedLongLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedLongLong** objectArray = (SgTypeUnsignedLongLong**) &(SgTypeUnsignedLongLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnsignedLongLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeUnsignedLongLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeUnsignedLongLong::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeUnsignedLongLong*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeUnsignedLongLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnsignedLongLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeUnsignedLongLong::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnsignedLongLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedLongLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedLongLong** objectArray = (SgTypeUnsignedLongLong**) &(SgTypeUnsignedLongLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnsignedLongLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnsignedLongLong::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeUnsignedLongLong object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeUnsignedLongLong object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeUnsignedLongLong::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnsignedLongLong objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedLongLong::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedLongLong** objectArray = (SgTypeUnsignedLongLong**) &(SgTypeUnsignedLongLong::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeUnsignedLongLong::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeUnsignedLongLong::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeUnsignedLongLong found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnsignedLongLong::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeUnsignedLongLong::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeUnsignedLongLong** objectArray = (SgTypeUnsignedLongLong**) &(SgTypeUnsignedLongLong::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeUnsignedLongLong::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeUnsignedLongLong::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnsignedLongLong::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeUnsignedLongLong);

     return memory;
   }



/* #line 32968 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeUnsignedShort::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeUnsignedShort objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedShort::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedShort** objectArray = (SgTypeUnsignedShort**) &(SgTypeUnsignedShort::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnsignedShort::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeUnsignedShort::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeUnsignedShort::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeUnsignedShort*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeUnsignedShort::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnsignedShort::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeUnsignedShort::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnsignedShort objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedShort::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedShort** objectArray = (SgTypeUnsignedShort**) &(SgTypeUnsignedShort::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeUnsignedShort::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeUnsignedShort::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeUnsignedShort object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeUnsignedShort object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeUnsignedShort::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeUnsignedShort objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeUnsignedShort::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeUnsignedShort** objectArray = (SgTypeUnsignedShort**) &(SgTypeUnsignedShort::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeUnsignedShort::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeUnsignedShort::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeUnsignedShort found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnsignedShort::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeUnsignedShort::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeUnsignedShort** objectArray = (SgTypeUnsignedShort**) &(SgTypeUnsignedShort::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeUnsignedShort::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeUnsignedShort::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeUnsignedShort::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeUnsignedShort);

     return memory;
   }



/* #line 33200 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeChar::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeChar objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeChar::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeChar** objectArray = (SgTypeChar**) &(SgTypeChar::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeChar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeChar::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeChar::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeChar*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeChar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeChar::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeChar::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeChar objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeChar::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeChar** objectArray = (SgTypeChar**) &(SgTypeChar::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeChar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeChar::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeChar object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeChar object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeChar::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeChar objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeChar::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeChar** objectArray = (SgTypeChar**) &(SgTypeChar::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeChar::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeChar::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeChar found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeChar::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeChar::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeChar** objectArray = (SgTypeChar**) &(SgTypeChar::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeChar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeChar::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeChar::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeChar);

     return memory;
   }



/* #line 33432 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeWchar::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeWchar objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeWchar::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeWchar** objectArray = (SgTypeWchar**) &(SgTypeWchar::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeWchar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeWchar::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeWchar::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeWchar*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeWchar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeWchar::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeWchar::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeWchar objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeWchar::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeWchar** objectArray = (SgTypeWchar**) &(SgTypeWchar::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeWchar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeWchar::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeWchar object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeWchar object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeWchar::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeWchar objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeWchar::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeWchar** objectArray = (SgTypeWchar**) &(SgTypeWchar::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeWchar::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeWchar::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeWchar found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeWchar::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeWchar::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeWchar** objectArray = (SgTypeWchar**) &(SgTypeWchar::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeWchar::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeWchar::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeWchar::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeWchar);

     return memory;
   }



/* #line 33664 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeChar16::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeChar16 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeChar16::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeChar16** objectArray = (SgTypeChar16**) &(SgTypeChar16::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeChar16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeChar16::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeChar16::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeChar16*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeChar16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeChar16::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeChar16::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeChar16 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeChar16::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeChar16** objectArray = (SgTypeChar16**) &(SgTypeChar16::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeChar16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeChar16::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeChar16 object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeChar16 object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeChar16::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeChar16 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeChar16::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeChar16** objectArray = (SgTypeChar16**) &(SgTypeChar16::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeChar16::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeChar16::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeChar16 found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeChar16::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeChar16::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeChar16** objectArray = (SgTypeChar16**) &(SgTypeChar16::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeChar16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeChar16::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeChar16::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeChar16);

     return memory;
   }



/* #line 33896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeChar32::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeChar32 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeChar32::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeChar32** objectArray = (SgTypeChar32**) &(SgTypeChar32::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeChar32::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeChar32::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeChar32::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeChar32*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeChar32::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeChar32::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeChar32::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeChar32 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeChar32::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeChar32** objectArray = (SgTypeChar32**) &(SgTypeChar32::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeChar32::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeChar32::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeChar32 object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeChar32 object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeChar32::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeChar32 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeChar32::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeChar32** objectArray = (SgTypeChar32**) &(SgTypeChar32::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeChar32::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeChar32::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeChar32 found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeChar32::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeChar32::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeChar32** objectArray = (SgTypeChar32**) &(SgTypeChar32::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeChar32::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeChar32::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeChar32::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeChar32);

     return memory;
   }



/* #line 34128 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaModularType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaModularType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaModularType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaModularType** objectArray = (SgAdaModularType**) &(SgAdaModularType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaModularType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaModularType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaModularType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaModularType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaModularType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaModularType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaModularType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaModularType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaModularType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaModularType** objectArray = (SgAdaModularType**) &(SgAdaModularType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaModularType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaModularType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaModularType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaModularType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaModularType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaModularType objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaModularType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaModularType** objectArray = (SgAdaModularType**) &(SgAdaModularType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaModularType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaModularType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaModularType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaModularType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaModularType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaModularType** objectArray = (SgAdaModularType**) &(SgAdaModularType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaModularType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaModularType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaModularType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaModularType);

     return memory;
   }



/* #line 34360 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeBool::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeBool objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeBool::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeBool** objectArray = (SgTypeBool**) &(SgTypeBool::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeBool::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeBool::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeBool::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeBool*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeBool::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeBool::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeBool::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeBool objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeBool::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeBool** objectArray = (SgTypeBool**) &(SgTypeBool::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeBool::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeBool::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeBool object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeBool object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeBool::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeBool objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeBool::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeBool** objectArray = (SgTypeBool**) &(SgTypeBool::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeBool::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeBool::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeBool found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeBool::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeBool::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeBool** objectArray = (SgTypeBool**) &(SgTypeBool::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeBool::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeBool::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeBool::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeBool);

     return memory;
   }



/* #line 34592 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFloatingPointType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFloatingPointType objectArray [] = *(Memory_Block_List.begin());
     if (SgFloatingPointType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloatingPointType** objectArray = (SgFloatingPointType**) &(SgFloatingPointType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloatingPointType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFloatingPointType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFloatingPointType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFloatingPointType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFloatingPointType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloatingPointType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFloatingPointType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloatingPointType objectArray [] = *(Memory_Block_List.begin());
     if (SgFloatingPointType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloatingPointType** objectArray = (SgFloatingPointType**) &(SgFloatingPointType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloatingPointType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloatingPointType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFloatingPointType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFloatingPointType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFloatingPointType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloatingPointType objectArray [] = *(Memory_Block_List.begin());
     if (SgFloatingPointType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloatingPointType** objectArray = (SgFloatingPointType**) &(SgFloatingPointType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFloatingPointType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFloatingPointType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFloatingPointType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloatingPointType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFloatingPointType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFloatingPointType** objectArray = (SgFloatingPointType**) &(SgFloatingPointType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFloatingPointType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFloatingPointType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloatingPointType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFloatingPointType);

     return memory;
   }



/* #line 34824 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeFloat::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeFloat objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat** objectArray = (SgTypeFloat**) &(SgTypeFloat::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeFloat::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeFloat::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeFloat*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeFloat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeFloat::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat** objectArray = (SgTypeFloat**) &(SgTypeFloat::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeFloat object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeFloat object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeFloat::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat** objectArray = (SgTypeFloat**) &(SgTypeFloat::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeFloat::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeFloat::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeFloat found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeFloat::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeFloat** objectArray = (SgTypeFloat**) &(SgTypeFloat::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeFloat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeFloat::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeFloat);

     return memory;
   }



/* #line 35056 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeFloat128::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeFloat128 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat128::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat128** objectArray = (SgTypeFloat128**) &(SgTypeFloat128::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat128::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeFloat128::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeFloat128::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeFloat128*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeFloat128::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat128::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeFloat128::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat128 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat128::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat128** objectArray = (SgTypeFloat128**) &(SgTypeFloat128::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat128::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat128::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeFloat128 object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeFloat128 object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeFloat128::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat128 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat128::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat128** objectArray = (SgTypeFloat128**) &(SgTypeFloat128::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeFloat128::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeFloat128::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeFloat128 found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat128::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeFloat128::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeFloat128** objectArray = (SgTypeFloat128**) &(SgTypeFloat128::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeFloat128::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeFloat128::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat128::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeFloat128);

     return memory;
   }



/* #line 35288 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeFloat80::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeFloat80 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat80::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat80** objectArray = (SgTypeFloat80**) &(SgTypeFloat80::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat80::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeFloat80::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeFloat80::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeFloat80*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeFloat80::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat80::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeFloat80::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat80 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat80::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat80** objectArray = (SgTypeFloat80**) &(SgTypeFloat80::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat80::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat80::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeFloat80 object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeFloat80 object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeFloat80::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat80 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat80::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat80** objectArray = (SgTypeFloat80**) &(SgTypeFloat80::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeFloat80::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeFloat80::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeFloat80 found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat80::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeFloat80::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeFloat80** objectArray = (SgTypeFloat80**) &(SgTypeFloat80::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeFloat80::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeFloat80::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat80::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeFloat80);

     return memory;
   }



/* #line 35520 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeDouble::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeDouble objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeDouble::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeDouble** objectArray = (SgTypeDouble**) &(SgTypeDouble::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeDouble::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeDouble::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeDouble::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeDouble*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeDouble::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeDouble::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeDouble::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeDouble objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeDouble::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeDouble** objectArray = (SgTypeDouble**) &(SgTypeDouble::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeDouble::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeDouble::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeDouble object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeDouble object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeDouble::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeDouble objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeDouble::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeDouble** objectArray = (SgTypeDouble**) &(SgTypeDouble::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeDouble::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeDouble::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeDouble found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeDouble::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeDouble::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeDouble** objectArray = (SgTypeDouble**) &(SgTypeDouble::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeDouble::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeDouble::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeDouble::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeDouble);

     return memory;
   }



/* #line 35752 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeLongDouble::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeLongDouble objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeLongDouble::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeLongDouble** objectArray = (SgTypeLongDouble**) &(SgTypeLongDouble::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeLongDouble::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeLongDouble::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeLongDouble::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeLongDouble*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeLongDouble::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeLongDouble::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeLongDouble::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeLongDouble objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeLongDouble::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeLongDouble** objectArray = (SgTypeLongDouble**) &(SgTypeLongDouble::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeLongDouble::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeLongDouble::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeLongDouble object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeLongDouble object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeLongDouble::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeLongDouble objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeLongDouble::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeLongDouble** objectArray = (SgTypeLongDouble**) &(SgTypeLongDouble::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeLongDouble::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeLongDouble::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeLongDouble found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeLongDouble::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeLongDouble::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeLongDouble** objectArray = (SgTypeLongDouble**) &(SgTypeLongDouble::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeLongDouble::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeLongDouble::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeLongDouble::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeLongDouble);

     return memory;
   }



/* #line 35984 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeFloat16::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeFloat16 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat16::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat16** objectArray = (SgTypeFloat16**) &(SgTypeFloat16::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeFloat16::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeFloat16::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeFloat16*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeFloat16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat16::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeFloat16::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat16 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat16::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat16** objectArray = (SgTypeFloat16**) &(SgTypeFloat16::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat16::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeFloat16 object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeFloat16 object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeFloat16::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat16 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat16::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat16** objectArray = (SgTypeFloat16**) &(SgTypeFloat16::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeFloat16::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeFloat16::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeFloat16 found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat16::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeFloat16::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeFloat16** objectArray = (SgTypeFloat16**) &(SgTypeFloat16::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeFloat16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeFloat16::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat16::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeFloat16);

     return memory;
   }



/* #line 36216 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeBFloat16::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeBFloat16 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeBFloat16::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeBFloat16** objectArray = (SgTypeBFloat16**) &(SgTypeBFloat16::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeBFloat16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeBFloat16::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeBFloat16::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeBFloat16*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeBFloat16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeBFloat16::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeBFloat16::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeBFloat16 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeBFloat16::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeBFloat16** objectArray = (SgTypeBFloat16**) &(SgTypeBFloat16::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeBFloat16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeBFloat16::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeBFloat16 object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeBFloat16 object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeBFloat16::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeBFloat16 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeBFloat16::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeBFloat16** objectArray = (SgTypeBFloat16**) &(SgTypeBFloat16::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeBFloat16::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeBFloat16::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeBFloat16 found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeBFloat16::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeBFloat16::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeBFloat16** objectArray = (SgTypeBFloat16**) &(SgTypeBFloat16::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeBFloat16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeBFloat16::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeBFloat16::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeBFloat16);

     return memory;
   }



/* #line 36448 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeFp16::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeFp16 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFp16::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFp16** objectArray = (SgTypeFp16**) &(SgTypeFp16::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFp16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeFp16::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeFp16::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeFp16*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeFp16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFp16::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeFp16::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFp16 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFp16::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFp16** objectArray = (SgTypeFp16**) &(SgTypeFp16::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFp16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFp16::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeFp16 object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeFp16 object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeFp16::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFp16 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFp16::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFp16** objectArray = (SgTypeFp16**) &(SgTypeFp16::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeFp16::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeFp16::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeFp16 found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFp16::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeFp16::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeFp16** objectArray = (SgTypeFp16**) &(SgTypeFp16::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeFp16::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeFp16::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFp16::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeFp16);

     return memory;
   }



/* #line 36680 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeFloat32x::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeFloat32x objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat32x::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat32x** objectArray = (SgTypeFloat32x**) &(SgTypeFloat32x::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat32x::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeFloat32x::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeFloat32x::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeFloat32x*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeFloat32x::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat32x::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeFloat32x::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat32x objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat32x::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat32x** objectArray = (SgTypeFloat32x**) &(SgTypeFloat32x::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat32x::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat32x::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeFloat32x object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeFloat32x object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeFloat32x::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat32x objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat32x::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat32x** objectArray = (SgTypeFloat32x**) &(SgTypeFloat32x::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeFloat32x::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeFloat32x::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeFloat32x found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat32x::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeFloat32x::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeFloat32x** objectArray = (SgTypeFloat32x**) &(SgTypeFloat32x::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeFloat32x::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeFloat32x::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat32x::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeFloat32x);

     return memory;
   }



/* #line 36912 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeFloat64x::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeFloat64x objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat64x::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat64x** objectArray = (SgTypeFloat64x**) &(SgTypeFloat64x::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat64x::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeFloat64x::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeFloat64x::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeFloat64x*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeFloat64x::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat64x::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeFloat64x::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat64x objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat64x::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat64x** objectArray = (SgTypeFloat64x**) &(SgTypeFloat64x::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat64x::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat64x::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeFloat64x object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeFloat64x object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeFloat64x::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat64x objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat64x::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat64x** objectArray = (SgTypeFloat64x**) &(SgTypeFloat64x::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeFloat64x::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeFloat64x::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeFloat64x found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat64x::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeFloat64x::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeFloat64x** objectArray = (SgTypeFloat64x**) &(SgTypeFloat64x::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeFloat64x::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeFloat64x::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat64x::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeFloat64x);

     return memory;
   }



/* #line 37144 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeFloat32::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeFloat32 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat32::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat32** objectArray = (SgTypeFloat32**) &(SgTypeFloat32::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat32::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeFloat32::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeFloat32::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeFloat32*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeFloat32::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat32::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeFloat32::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat32 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat32::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat32** objectArray = (SgTypeFloat32**) &(SgTypeFloat32::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat32::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat32::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeFloat32 object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeFloat32 object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeFloat32::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat32 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat32::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat32** objectArray = (SgTypeFloat32**) &(SgTypeFloat32::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeFloat32::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeFloat32::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeFloat32 found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat32::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeFloat32::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeFloat32** objectArray = (SgTypeFloat32**) &(SgTypeFloat32::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeFloat32::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeFloat32::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat32::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeFloat32);

     return memory;
   }



/* #line 37376 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeFloat64::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeFloat64 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat64::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat64** objectArray = (SgTypeFloat64**) &(SgTypeFloat64::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat64::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeFloat64::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeFloat64::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeFloat64*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeFloat64::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat64::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeFloat64::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat64 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat64::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat64** objectArray = (SgTypeFloat64**) &(SgTypeFloat64::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeFloat64::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeFloat64::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeFloat64 object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeFloat64 object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeFloat64::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeFloat64 objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeFloat64::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeFloat64** objectArray = (SgTypeFloat64**) &(SgTypeFloat64::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeFloat64::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeFloat64::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeFloat64 found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat64::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeFloat64::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeFloat64** objectArray = (SgTypeFloat64**) &(SgTypeFloat64::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeFloat64::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeFloat64::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeFloat64::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeFloat64);

     return memory;
   }



/* #line 37608 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLocatedNode::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLocatedNode objectArray [] = *(Memory_Block_List.begin());
     if (SgLocatedNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLocatedNode** objectArray = (SgLocatedNode**) &(SgLocatedNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLocatedNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLocatedNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLocatedNode::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLocatedNode*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLocatedNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLocatedNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLocatedNode::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLocatedNode objectArray [] = *(Memory_Block_List.begin());
     if (SgLocatedNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLocatedNode** objectArray = (SgLocatedNode**) &(SgLocatedNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLocatedNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLocatedNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLocatedNode object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLocatedNode object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLocatedNode::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLocatedNode objectArray [] = *(Memory_Block_List.begin());
     if (SgLocatedNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLocatedNode** objectArray = (SgLocatedNode**) &(SgLocatedNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLocatedNode::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLocatedNode::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLocatedNode found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLocatedNode::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLocatedNode::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLocatedNode** objectArray = (SgLocatedNode**) &(SgLocatedNode::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLocatedNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLocatedNode::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLocatedNode::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLocatedNode);

     return memory;
   }



/* #line 37840 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgToken::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgToken objectArray [] = *(Memory_Block_List.begin());
     if (SgToken::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgToken** objectArray = (SgToken**) &(SgToken::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgToken::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgToken::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgToken::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgToken*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgToken::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgToken::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgToken::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgToken objectArray [] = *(Memory_Block_List.begin());
     if (SgToken::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgToken** objectArray = (SgToken**) &(SgToken::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgToken::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgToken::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgToken object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgToken object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgToken::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgToken objectArray [] = *(Memory_Block_List.begin());
     if (SgToken::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgToken** objectArray = (SgToken**) &(SgToken::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgToken::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgToken::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgToken found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgToken::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgToken::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgToken** objectArray = (SgToken**) &(SgToken::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgToken::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgToken::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgToken::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgToken);

     return memory;
   }



/* #line 38072 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLocatedNodeSupport::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLocatedNodeSupport objectArray [] = *(Memory_Block_List.begin());
     if (SgLocatedNodeSupport::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLocatedNodeSupport** objectArray = (SgLocatedNodeSupport**) &(SgLocatedNodeSupport::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLocatedNodeSupport::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLocatedNodeSupport::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLocatedNodeSupport::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLocatedNodeSupport*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLocatedNodeSupport::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLocatedNodeSupport::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLocatedNodeSupport::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLocatedNodeSupport objectArray [] = *(Memory_Block_List.begin());
     if (SgLocatedNodeSupport::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLocatedNodeSupport** objectArray = (SgLocatedNodeSupport**) &(SgLocatedNodeSupport::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLocatedNodeSupport::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLocatedNodeSupport::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLocatedNodeSupport object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLocatedNodeSupport object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLocatedNodeSupport::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLocatedNodeSupport objectArray [] = *(Memory_Block_List.begin());
     if (SgLocatedNodeSupport::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLocatedNodeSupport** objectArray = (SgLocatedNodeSupport**) &(SgLocatedNodeSupport::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLocatedNodeSupport::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLocatedNodeSupport::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLocatedNodeSupport found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLocatedNodeSupport::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLocatedNodeSupport::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLocatedNodeSupport** objectArray = (SgLocatedNodeSupport**) &(SgLocatedNodeSupport::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLocatedNodeSupport::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLocatedNodeSupport::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLocatedNodeSupport::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLocatedNodeSupport);

     return memory;
   }



/* #line 38304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCommonBlockObject::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCommonBlockObject objectArray [] = *(Memory_Block_List.begin());
     if (SgCommonBlockObject::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCommonBlockObject** objectArray = (SgCommonBlockObject**) &(SgCommonBlockObject::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCommonBlockObject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCommonBlockObject::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCommonBlockObject::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCommonBlockObject*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCommonBlockObject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCommonBlockObject::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCommonBlockObject::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCommonBlockObject objectArray [] = *(Memory_Block_List.begin());
     if (SgCommonBlockObject::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCommonBlockObject** objectArray = (SgCommonBlockObject**) &(SgCommonBlockObject::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCommonBlockObject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCommonBlockObject::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCommonBlockObject object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCommonBlockObject object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCommonBlockObject::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCommonBlockObject objectArray [] = *(Memory_Block_List.begin());
     if (SgCommonBlockObject::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCommonBlockObject** objectArray = (SgCommonBlockObject**) &(SgCommonBlockObject::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCommonBlockObject::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCommonBlockObject::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCommonBlockObject found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCommonBlockObject::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCommonBlockObject::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCommonBlockObject** objectArray = (SgCommonBlockObject**) &(SgCommonBlockObject::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCommonBlockObject::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCommonBlockObject::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCommonBlockObject::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCommonBlockObject);

     return memory;
   }



/* #line 38536 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgInitializedName::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgInitializedName objectArray [] = *(Memory_Block_List.begin());
     if (SgInitializedName::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInitializedName** objectArray = (SgInitializedName**) &(SgInitializedName::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgInitializedName::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgInitializedName::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgInitializedName::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgInitializedName*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgInitializedName::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgInitializedName::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgInitializedName::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgInitializedName objectArray [] = *(Memory_Block_List.begin());
     if (SgInitializedName::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInitializedName** objectArray = (SgInitializedName**) &(SgInitializedName::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgInitializedName::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgInitializedName::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgInitializedName object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgInitializedName object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgInitializedName::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgInitializedName objectArray [] = *(Memory_Block_List.begin());
     if (SgInitializedName::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInitializedName** objectArray = (SgInitializedName**) &(SgInitializedName::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgInitializedName::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgInitializedName::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgInitializedName found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgInitializedName::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgInitializedName::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgInitializedName** objectArray = (SgInitializedName**) &(SgInitializedName::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgInitializedName::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgInitializedName::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgInitializedName::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgInitializedName);

     return memory;
   }



/* #line 38768 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgInterfaceBody::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgInterfaceBody objectArray [] = *(Memory_Block_List.begin());
     if (SgInterfaceBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInterfaceBody** objectArray = (SgInterfaceBody**) &(SgInterfaceBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgInterfaceBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgInterfaceBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgInterfaceBody::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgInterfaceBody*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgInterfaceBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgInterfaceBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgInterfaceBody::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgInterfaceBody objectArray [] = *(Memory_Block_List.begin());
     if (SgInterfaceBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInterfaceBody** objectArray = (SgInterfaceBody**) &(SgInterfaceBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgInterfaceBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgInterfaceBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgInterfaceBody object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgInterfaceBody object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgInterfaceBody::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgInterfaceBody objectArray [] = *(Memory_Block_List.begin());
     if (SgInterfaceBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInterfaceBody** objectArray = (SgInterfaceBody**) &(SgInterfaceBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgInterfaceBody::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgInterfaceBody::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgInterfaceBody found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgInterfaceBody::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgInterfaceBody::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgInterfaceBody** objectArray = (SgInterfaceBody**) &(SgInterfaceBody::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgInterfaceBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgInterfaceBody::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgInterfaceBody::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgInterfaceBody);

     return memory;
   }



/* #line 39000 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgHeaderFileBody::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgHeaderFileBody objectArray [] = *(Memory_Block_List.begin());
     if (SgHeaderFileBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgHeaderFileBody** objectArray = (SgHeaderFileBody**) &(SgHeaderFileBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgHeaderFileBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgHeaderFileBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgHeaderFileBody::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgHeaderFileBody*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgHeaderFileBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgHeaderFileBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgHeaderFileBody::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgHeaderFileBody objectArray [] = *(Memory_Block_List.begin());
     if (SgHeaderFileBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgHeaderFileBody** objectArray = (SgHeaderFileBody**) &(SgHeaderFileBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgHeaderFileBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgHeaderFileBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgHeaderFileBody object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgHeaderFileBody object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgHeaderFileBody::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgHeaderFileBody objectArray [] = *(Memory_Block_List.begin());
     if (SgHeaderFileBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgHeaderFileBody** objectArray = (SgHeaderFileBody**) &(SgHeaderFileBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgHeaderFileBody::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgHeaderFileBody::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgHeaderFileBody found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgHeaderFileBody::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgHeaderFileBody::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgHeaderFileBody** objectArray = (SgHeaderFileBody**) &(SgHeaderFileBody::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgHeaderFileBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgHeaderFileBody::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgHeaderFileBody::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgHeaderFileBody);

     return memory;
   }



/* #line 39232 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgRenamePair::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgRenamePair objectArray [] = *(Memory_Block_List.begin());
     if (SgRenamePair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRenamePair** objectArray = (SgRenamePair**) &(SgRenamePair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRenamePair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgRenamePair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgRenamePair::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgRenamePair*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgRenamePair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRenamePair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgRenamePair::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRenamePair objectArray [] = *(Memory_Block_List.begin());
     if (SgRenamePair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRenamePair** objectArray = (SgRenamePair**) &(SgRenamePair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRenamePair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRenamePair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgRenamePair object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgRenamePair object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgRenamePair::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRenamePair objectArray [] = *(Memory_Block_List.begin());
     if (SgRenamePair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRenamePair** objectArray = (SgRenamePair**) &(SgRenamePair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgRenamePair::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgRenamePair::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgRenamePair found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRenamePair::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgRenamePair::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgRenamePair** objectArray = (SgRenamePair**) &(SgRenamePair::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgRenamePair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgRenamePair::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRenamePair::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgRenamePair);

     return memory;
   }



/* #line 39464 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaMemberValuePair::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaMemberValuePair objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaMemberValuePair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaMemberValuePair** objectArray = (SgJavaMemberValuePair**) &(SgJavaMemberValuePair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaMemberValuePair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaMemberValuePair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaMemberValuePair::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaMemberValuePair*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaMemberValuePair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaMemberValuePair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaMemberValuePair::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaMemberValuePair objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaMemberValuePair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaMemberValuePair** objectArray = (SgJavaMemberValuePair**) &(SgJavaMemberValuePair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaMemberValuePair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaMemberValuePair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaMemberValuePair object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaMemberValuePair object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaMemberValuePair::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaMemberValuePair objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaMemberValuePair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaMemberValuePair** objectArray = (SgJavaMemberValuePair**) &(SgJavaMemberValuePair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaMemberValuePair::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaMemberValuePair::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaMemberValuePair found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaMemberValuePair::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaMemberValuePair::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaMemberValuePair** objectArray = (SgJavaMemberValuePair**) &(SgJavaMemberValuePair::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaMemberValuePair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaMemberValuePair::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaMemberValuePair::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaMemberValuePair);

     return memory;
   }



/* #line 39696 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpClause** objectArray = (SgOmpClause**) &(SgOmpClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpClause** objectArray = (SgOmpClause**) &(SgOmpClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpClause** objectArray = (SgOmpClause**) &(SgOmpClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpClause** objectArray = (SgOmpClause**) &(SgOmpClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpClause);

     return memory;
   }



/* #line 39928 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpNowaitClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpNowaitClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpNowaitClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpNowaitClause** objectArray = (SgOmpNowaitClause**) &(SgOmpNowaitClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpNowaitClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpNowaitClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpNowaitClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpNowaitClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpNowaitClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpNowaitClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpNowaitClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpNowaitClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpNowaitClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpNowaitClause** objectArray = (SgOmpNowaitClause**) &(SgOmpNowaitClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpNowaitClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpNowaitClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpNowaitClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpNowaitClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpNowaitClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpNowaitClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpNowaitClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpNowaitClause** objectArray = (SgOmpNowaitClause**) &(SgOmpNowaitClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpNowaitClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpNowaitClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpNowaitClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpNowaitClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpNowaitClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpNowaitClause** objectArray = (SgOmpNowaitClause**) &(SgOmpNowaitClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpNowaitClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpNowaitClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpNowaitClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpNowaitClause);

     return memory;
   }



/* #line 40160 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpBeginClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpBeginClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpBeginClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpBeginClause** objectArray = (SgOmpBeginClause**) &(SgOmpBeginClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpBeginClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpBeginClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpBeginClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpBeginClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpBeginClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpBeginClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpBeginClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpBeginClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpBeginClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpBeginClause** objectArray = (SgOmpBeginClause**) &(SgOmpBeginClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpBeginClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpBeginClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpBeginClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpBeginClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpBeginClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpBeginClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpBeginClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpBeginClause** objectArray = (SgOmpBeginClause**) &(SgOmpBeginClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpBeginClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpBeginClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpBeginClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpBeginClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpBeginClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpBeginClause** objectArray = (SgOmpBeginClause**) &(SgOmpBeginClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpBeginClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpBeginClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpBeginClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpBeginClause);

     return memory;
   }



/* #line 40392 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpEndClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpEndClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpEndClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpEndClause** objectArray = (SgOmpEndClause**) &(SgOmpEndClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpEndClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpEndClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpEndClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpEndClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpEndClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpEndClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpEndClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpEndClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpEndClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpEndClause** objectArray = (SgOmpEndClause**) &(SgOmpEndClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpEndClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpEndClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpEndClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpEndClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpEndClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpEndClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpEndClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpEndClause** objectArray = (SgOmpEndClause**) &(SgOmpEndClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpEndClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpEndClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpEndClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpEndClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpEndClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpEndClause** objectArray = (SgOmpEndClause**) &(SgOmpEndClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpEndClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpEndClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpEndClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpEndClause);

     return memory;
   }



/* #line 40624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpUntiedClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpUntiedClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpUntiedClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpUntiedClause** objectArray = (SgOmpUntiedClause**) &(SgOmpUntiedClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpUntiedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpUntiedClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpUntiedClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpUntiedClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpUntiedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpUntiedClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpUntiedClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpUntiedClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpUntiedClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpUntiedClause** objectArray = (SgOmpUntiedClause**) &(SgOmpUntiedClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpUntiedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpUntiedClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpUntiedClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpUntiedClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpUntiedClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpUntiedClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpUntiedClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpUntiedClause** objectArray = (SgOmpUntiedClause**) &(SgOmpUntiedClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpUntiedClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpUntiedClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpUntiedClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpUntiedClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpUntiedClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpUntiedClause** objectArray = (SgOmpUntiedClause**) &(SgOmpUntiedClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpUntiedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpUntiedClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpUntiedClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpUntiedClause);

     return memory;
   }



/* #line 40856 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpDefaultClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpDefaultClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDefaultClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDefaultClause** objectArray = (SgOmpDefaultClause**) &(SgOmpDefaultClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpDefaultClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpDefaultClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpDefaultClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpDefaultClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpDefaultClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpDefaultClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpDefaultClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpDefaultClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDefaultClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDefaultClause** objectArray = (SgOmpDefaultClause**) &(SgOmpDefaultClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpDefaultClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpDefaultClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpDefaultClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpDefaultClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpDefaultClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpDefaultClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDefaultClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDefaultClause** objectArray = (SgOmpDefaultClause**) &(SgOmpDefaultClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpDefaultClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpDefaultClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpDefaultClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpDefaultClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpDefaultClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpDefaultClause** objectArray = (SgOmpDefaultClause**) &(SgOmpDefaultClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpDefaultClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpDefaultClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpDefaultClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpDefaultClause);

     return memory;
   }



/* #line 41088 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpAtomicClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpAtomicClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpAtomicClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpAtomicClause** objectArray = (SgOmpAtomicClause**) &(SgOmpAtomicClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpAtomicClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpAtomicClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpAtomicClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpAtomicClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpAtomicClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpAtomicClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpAtomicClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpAtomicClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpAtomicClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpAtomicClause** objectArray = (SgOmpAtomicClause**) &(SgOmpAtomicClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpAtomicClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpAtomicClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpAtomicClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpAtomicClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpAtomicClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpAtomicClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpAtomicClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpAtomicClause** objectArray = (SgOmpAtomicClause**) &(SgOmpAtomicClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpAtomicClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpAtomicClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpAtomicClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpAtomicClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpAtomicClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpAtomicClause** objectArray = (SgOmpAtomicClause**) &(SgOmpAtomicClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpAtomicClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpAtomicClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpAtomicClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpAtomicClause);

     return memory;
   }



/* #line 41320 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpProcBindClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpProcBindClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpProcBindClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpProcBindClause** objectArray = (SgOmpProcBindClause**) &(SgOmpProcBindClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpProcBindClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpProcBindClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpProcBindClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpProcBindClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpProcBindClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpProcBindClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpProcBindClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpProcBindClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpProcBindClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpProcBindClause** objectArray = (SgOmpProcBindClause**) &(SgOmpProcBindClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpProcBindClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpProcBindClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpProcBindClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpProcBindClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpProcBindClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpProcBindClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpProcBindClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpProcBindClause** objectArray = (SgOmpProcBindClause**) &(SgOmpProcBindClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpProcBindClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpProcBindClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpProcBindClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpProcBindClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpProcBindClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpProcBindClause** objectArray = (SgOmpProcBindClause**) &(SgOmpProcBindClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpProcBindClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpProcBindClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpProcBindClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpProcBindClause);

     return memory;
   }



/* #line 41552 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpExpressionClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpExpressionClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpExpressionClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpExpressionClause** objectArray = (SgOmpExpressionClause**) &(SgOmpExpressionClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpExpressionClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpExpressionClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpExpressionClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpExpressionClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpExpressionClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpExpressionClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpExpressionClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpExpressionClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpExpressionClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpExpressionClause** objectArray = (SgOmpExpressionClause**) &(SgOmpExpressionClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpExpressionClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpExpressionClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpExpressionClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpExpressionClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpExpressionClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpExpressionClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpExpressionClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpExpressionClause** objectArray = (SgOmpExpressionClause**) &(SgOmpExpressionClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpExpressionClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpExpressionClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpExpressionClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpExpressionClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpExpressionClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpExpressionClause** objectArray = (SgOmpExpressionClause**) &(SgOmpExpressionClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpExpressionClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpExpressionClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpExpressionClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpExpressionClause);

     return memory;
   }



/* #line 41784 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpOrderedClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpOrderedClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpOrderedClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpOrderedClause** objectArray = (SgOmpOrderedClause**) &(SgOmpOrderedClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpOrderedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpOrderedClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpOrderedClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpOrderedClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpOrderedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpOrderedClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpOrderedClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpOrderedClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpOrderedClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpOrderedClause** objectArray = (SgOmpOrderedClause**) &(SgOmpOrderedClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpOrderedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpOrderedClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpOrderedClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpOrderedClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpOrderedClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpOrderedClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpOrderedClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpOrderedClause** objectArray = (SgOmpOrderedClause**) &(SgOmpOrderedClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpOrderedClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpOrderedClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpOrderedClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpOrderedClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpOrderedClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpOrderedClause** objectArray = (SgOmpOrderedClause**) &(SgOmpOrderedClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpOrderedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpOrderedClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpOrderedClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpOrderedClause);

     return memory;
   }



/* #line 42016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpCollapseClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpCollapseClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpCollapseClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpCollapseClause** objectArray = (SgOmpCollapseClause**) &(SgOmpCollapseClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpCollapseClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpCollapseClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpCollapseClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpCollapseClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpCollapseClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpCollapseClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpCollapseClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpCollapseClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpCollapseClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpCollapseClause** objectArray = (SgOmpCollapseClause**) &(SgOmpCollapseClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpCollapseClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpCollapseClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpCollapseClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpCollapseClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpCollapseClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpCollapseClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpCollapseClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpCollapseClause** objectArray = (SgOmpCollapseClause**) &(SgOmpCollapseClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpCollapseClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpCollapseClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpCollapseClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpCollapseClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpCollapseClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpCollapseClause** objectArray = (SgOmpCollapseClause**) &(SgOmpCollapseClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpCollapseClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpCollapseClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpCollapseClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpCollapseClause);

     return memory;
   }



/* #line 42248 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpIfClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpIfClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpIfClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpIfClause** objectArray = (SgOmpIfClause**) &(SgOmpIfClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpIfClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpIfClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpIfClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpIfClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpIfClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpIfClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpIfClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpIfClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpIfClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpIfClause** objectArray = (SgOmpIfClause**) &(SgOmpIfClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpIfClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpIfClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpIfClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpIfClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpIfClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpIfClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpIfClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpIfClause** objectArray = (SgOmpIfClause**) &(SgOmpIfClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpIfClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpIfClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpIfClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpIfClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpIfClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpIfClause** objectArray = (SgOmpIfClause**) &(SgOmpIfClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpIfClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpIfClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpIfClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpIfClause);

     return memory;
   }



/* #line 42480 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpNumThreadsClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpNumThreadsClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpNumThreadsClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpNumThreadsClause** objectArray = (SgOmpNumThreadsClause**) &(SgOmpNumThreadsClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpNumThreadsClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpNumThreadsClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpNumThreadsClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpNumThreadsClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpNumThreadsClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpNumThreadsClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpNumThreadsClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpNumThreadsClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpNumThreadsClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpNumThreadsClause** objectArray = (SgOmpNumThreadsClause**) &(SgOmpNumThreadsClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpNumThreadsClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpNumThreadsClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpNumThreadsClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpNumThreadsClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpNumThreadsClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpNumThreadsClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpNumThreadsClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpNumThreadsClause** objectArray = (SgOmpNumThreadsClause**) &(SgOmpNumThreadsClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpNumThreadsClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpNumThreadsClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpNumThreadsClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpNumThreadsClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpNumThreadsClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpNumThreadsClause** objectArray = (SgOmpNumThreadsClause**) &(SgOmpNumThreadsClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpNumThreadsClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpNumThreadsClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpNumThreadsClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpNumThreadsClause);

     return memory;
   }



/* #line 42712 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpDeviceClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpDeviceClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDeviceClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDeviceClause** objectArray = (SgOmpDeviceClause**) &(SgOmpDeviceClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpDeviceClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpDeviceClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpDeviceClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpDeviceClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpDeviceClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpDeviceClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpDeviceClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpDeviceClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDeviceClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDeviceClause** objectArray = (SgOmpDeviceClause**) &(SgOmpDeviceClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpDeviceClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpDeviceClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpDeviceClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpDeviceClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpDeviceClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpDeviceClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDeviceClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDeviceClause** objectArray = (SgOmpDeviceClause**) &(SgOmpDeviceClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpDeviceClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpDeviceClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpDeviceClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpDeviceClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpDeviceClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpDeviceClause** objectArray = (SgOmpDeviceClause**) &(SgOmpDeviceClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpDeviceClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpDeviceClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpDeviceClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpDeviceClause);

     return memory;
   }



/* #line 42944 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpSafelenClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpSafelenClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSafelenClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSafelenClause** objectArray = (SgOmpSafelenClause**) &(SgOmpSafelenClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSafelenClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpSafelenClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpSafelenClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpSafelenClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpSafelenClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSafelenClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpSafelenClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSafelenClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSafelenClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSafelenClause** objectArray = (SgOmpSafelenClause**) &(SgOmpSafelenClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSafelenClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSafelenClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpSafelenClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpSafelenClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpSafelenClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSafelenClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSafelenClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSafelenClause** objectArray = (SgOmpSafelenClause**) &(SgOmpSafelenClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpSafelenClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpSafelenClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpSafelenClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSafelenClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpSafelenClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpSafelenClause** objectArray = (SgOmpSafelenClause**) &(SgOmpSafelenClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpSafelenClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpSafelenClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSafelenClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpSafelenClause);

     return memory;
   }



/* #line 43176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpSimdlenClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpSimdlenClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSimdlenClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSimdlenClause** objectArray = (SgOmpSimdlenClause**) &(SgOmpSimdlenClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSimdlenClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpSimdlenClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpSimdlenClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpSimdlenClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpSimdlenClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSimdlenClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpSimdlenClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSimdlenClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSimdlenClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSimdlenClause** objectArray = (SgOmpSimdlenClause**) &(SgOmpSimdlenClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSimdlenClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSimdlenClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpSimdlenClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpSimdlenClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpSimdlenClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSimdlenClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSimdlenClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSimdlenClause** objectArray = (SgOmpSimdlenClause**) &(SgOmpSimdlenClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpSimdlenClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpSimdlenClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpSimdlenClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSimdlenClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpSimdlenClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpSimdlenClause** objectArray = (SgOmpSimdlenClause**) &(SgOmpSimdlenClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpSimdlenClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpSimdlenClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSimdlenClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpSimdlenClause);

     return memory;
   }



/* #line 43408 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpFinalClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpFinalClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpFinalClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpFinalClause** objectArray = (SgOmpFinalClause**) &(SgOmpFinalClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpFinalClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpFinalClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpFinalClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpFinalClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpFinalClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpFinalClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpFinalClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpFinalClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpFinalClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpFinalClause** objectArray = (SgOmpFinalClause**) &(SgOmpFinalClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpFinalClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpFinalClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpFinalClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpFinalClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpFinalClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpFinalClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpFinalClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpFinalClause** objectArray = (SgOmpFinalClause**) &(SgOmpFinalClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpFinalClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpFinalClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpFinalClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpFinalClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpFinalClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpFinalClause** objectArray = (SgOmpFinalClause**) &(SgOmpFinalClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpFinalClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpFinalClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpFinalClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpFinalClause);

     return memory;
   }



/* #line 43640 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpPriorityClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpPriorityClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpPriorityClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpPriorityClause** objectArray = (SgOmpPriorityClause**) &(SgOmpPriorityClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpPriorityClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpPriorityClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpPriorityClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpPriorityClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpPriorityClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpPriorityClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpPriorityClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpPriorityClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpPriorityClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpPriorityClause** objectArray = (SgOmpPriorityClause**) &(SgOmpPriorityClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpPriorityClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpPriorityClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpPriorityClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpPriorityClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpPriorityClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpPriorityClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpPriorityClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpPriorityClause** objectArray = (SgOmpPriorityClause**) &(SgOmpPriorityClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpPriorityClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpPriorityClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpPriorityClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpPriorityClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpPriorityClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpPriorityClause** objectArray = (SgOmpPriorityClause**) &(SgOmpPriorityClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpPriorityClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpPriorityClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpPriorityClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpPriorityClause);

     return memory;
   }



/* #line 43872 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpInbranchClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpInbranchClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpInbranchClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpInbranchClause** objectArray = (SgOmpInbranchClause**) &(SgOmpInbranchClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpInbranchClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpInbranchClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpInbranchClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpInbranchClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpInbranchClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpInbranchClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpInbranchClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpInbranchClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpInbranchClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpInbranchClause** objectArray = (SgOmpInbranchClause**) &(SgOmpInbranchClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpInbranchClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpInbranchClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpInbranchClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpInbranchClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpInbranchClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpInbranchClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpInbranchClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpInbranchClause** objectArray = (SgOmpInbranchClause**) &(SgOmpInbranchClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpInbranchClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpInbranchClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpInbranchClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpInbranchClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpInbranchClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpInbranchClause** objectArray = (SgOmpInbranchClause**) &(SgOmpInbranchClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpInbranchClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpInbranchClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpInbranchClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpInbranchClause);

     return memory;
   }



/* #line 44104 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpNotinbranchClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpNotinbranchClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpNotinbranchClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpNotinbranchClause** objectArray = (SgOmpNotinbranchClause**) &(SgOmpNotinbranchClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpNotinbranchClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpNotinbranchClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpNotinbranchClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpNotinbranchClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpNotinbranchClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpNotinbranchClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpNotinbranchClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpNotinbranchClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpNotinbranchClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpNotinbranchClause** objectArray = (SgOmpNotinbranchClause**) &(SgOmpNotinbranchClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpNotinbranchClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpNotinbranchClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpNotinbranchClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpNotinbranchClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpNotinbranchClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpNotinbranchClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpNotinbranchClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpNotinbranchClause** objectArray = (SgOmpNotinbranchClause**) &(SgOmpNotinbranchClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpNotinbranchClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpNotinbranchClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpNotinbranchClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpNotinbranchClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpNotinbranchClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpNotinbranchClause** objectArray = (SgOmpNotinbranchClause**) &(SgOmpNotinbranchClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpNotinbranchClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpNotinbranchClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpNotinbranchClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpNotinbranchClause);

     return memory;
   }



/* #line 44336 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpVariablesClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpVariablesClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpVariablesClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpVariablesClause** objectArray = (SgOmpVariablesClause**) &(SgOmpVariablesClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpVariablesClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpVariablesClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpVariablesClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpVariablesClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpVariablesClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpVariablesClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpVariablesClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpVariablesClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpVariablesClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpVariablesClause** objectArray = (SgOmpVariablesClause**) &(SgOmpVariablesClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpVariablesClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpVariablesClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpVariablesClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpVariablesClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpVariablesClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpVariablesClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpVariablesClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpVariablesClause** objectArray = (SgOmpVariablesClause**) &(SgOmpVariablesClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpVariablesClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpVariablesClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpVariablesClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpVariablesClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpVariablesClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpVariablesClause** objectArray = (SgOmpVariablesClause**) &(SgOmpVariablesClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpVariablesClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpVariablesClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpVariablesClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpVariablesClause);

     return memory;
   }



/* #line 44568 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpCopyprivateClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpCopyprivateClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpCopyprivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpCopyprivateClause** objectArray = (SgOmpCopyprivateClause**) &(SgOmpCopyprivateClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpCopyprivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpCopyprivateClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpCopyprivateClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpCopyprivateClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpCopyprivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpCopyprivateClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpCopyprivateClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpCopyprivateClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpCopyprivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpCopyprivateClause** objectArray = (SgOmpCopyprivateClause**) &(SgOmpCopyprivateClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpCopyprivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpCopyprivateClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpCopyprivateClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpCopyprivateClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpCopyprivateClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpCopyprivateClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpCopyprivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpCopyprivateClause** objectArray = (SgOmpCopyprivateClause**) &(SgOmpCopyprivateClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpCopyprivateClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpCopyprivateClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpCopyprivateClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpCopyprivateClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpCopyprivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpCopyprivateClause** objectArray = (SgOmpCopyprivateClause**) &(SgOmpCopyprivateClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpCopyprivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpCopyprivateClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpCopyprivateClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpCopyprivateClause);

     return memory;
   }



/* #line 44800 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpPrivateClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpPrivateClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpPrivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpPrivateClause** objectArray = (SgOmpPrivateClause**) &(SgOmpPrivateClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpPrivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpPrivateClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpPrivateClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpPrivateClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpPrivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpPrivateClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpPrivateClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpPrivateClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpPrivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpPrivateClause** objectArray = (SgOmpPrivateClause**) &(SgOmpPrivateClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpPrivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpPrivateClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpPrivateClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpPrivateClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpPrivateClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpPrivateClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpPrivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpPrivateClause** objectArray = (SgOmpPrivateClause**) &(SgOmpPrivateClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpPrivateClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpPrivateClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpPrivateClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpPrivateClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpPrivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpPrivateClause** objectArray = (SgOmpPrivateClause**) &(SgOmpPrivateClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpPrivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpPrivateClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpPrivateClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpPrivateClause);

     return memory;
   }



/* #line 45032 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpFirstprivateClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpFirstprivateClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpFirstprivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpFirstprivateClause** objectArray = (SgOmpFirstprivateClause**) &(SgOmpFirstprivateClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpFirstprivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpFirstprivateClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpFirstprivateClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpFirstprivateClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpFirstprivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpFirstprivateClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpFirstprivateClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpFirstprivateClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpFirstprivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpFirstprivateClause** objectArray = (SgOmpFirstprivateClause**) &(SgOmpFirstprivateClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpFirstprivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpFirstprivateClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpFirstprivateClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpFirstprivateClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpFirstprivateClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpFirstprivateClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpFirstprivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpFirstprivateClause** objectArray = (SgOmpFirstprivateClause**) &(SgOmpFirstprivateClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpFirstprivateClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpFirstprivateClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpFirstprivateClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpFirstprivateClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpFirstprivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpFirstprivateClause** objectArray = (SgOmpFirstprivateClause**) &(SgOmpFirstprivateClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpFirstprivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpFirstprivateClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpFirstprivateClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpFirstprivateClause);

     return memory;
   }



/* #line 45264 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpSharedClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpSharedClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSharedClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSharedClause** objectArray = (SgOmpSharedClause**) &(SgOmpSharedClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSharedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpSharedClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpSharedClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpSharedClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpSharedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSharedClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpSharedClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSharedClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSharedClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSharedClause** objectArray = (SgOmpSharedClause**) &(SgOmpSharedClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSharedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSharedClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpSharedClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpSharedClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpSharedClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSharedClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSharedClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSharedClause** objectArray = (SgOmpSharedClause**) &(SgOmpSharedClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpSharedClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpSharedClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpSharedClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSharedClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpSharedClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpSharedClause** objectArray = (SgOmpSharedClause**) &(SgOmpSharedClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpSharedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpSharedClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSharedClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpSharedClause);

     return memory;
   }



/* #line 45496 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpCopyinClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpCopyinClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpCopyinClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpCopyinClause** objectArray = (SgOmpCopyinClause**) &(SgOmpCopyinClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpCopyinClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpCopyinClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpCopyinClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpCopyinClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpCopyinClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpCopyinClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpCopyinClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpCopyinClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpCopyinClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpCopyinClause** objectArray = (SgOmpCopyinClause**) &(SgOmpCopyinClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpCopyinClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpCopyinClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpCopyinClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpCopyinClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpCopyinClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpCopyinClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpCopyinClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpCopyinClause** objectArray = (SgOmpCopyinClause**) &(SgOmpCopyinClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpCopyinClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpCopyinClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpCopyinClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpCopyinClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpCopyinClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpCopyinClause** objectArray = (SgOmpCopyinClause**) &(SgOmpCopyinClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpCopyinClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpCopyinClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpCopyinClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpCopyinClause);

     return memory;
   }



/* #line 45728 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpLastprivateClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpLastprivateClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpLastprivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpLastprivateClause** objectArray = (SgOmpLastprivateClause**) &(SgOmpLastprivateClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpLastprivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpLastprivateClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpLastprivateClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpLastprivateClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpLastprivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpLastprivateClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpLastprivateClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpLastprivateClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpLastprivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpLastprivateClause** objectArray = (SgOmpLastprivateClause**) &(SgOmpLastprivateClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpLastprivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpLastprivateClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpLastprivateClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpLastprivateClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpLastprivateClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpLastprivateClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpLastprivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpLastprivateClause** objectArray = (SgOmpLastprivateClause**) &(SgOmpLastprivateClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpLastprivateClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpLastprivateClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpLastprivateClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpLastprivateClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpLastprivateClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpLastprivateClause** objectArray = (SgOmpLastprivateClause**) &(SgOmpLastprivateClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpLastprivateClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpLastprivateClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpLastprivateClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpLastprivateClause);

     return memory;
   }



/* #line 45960 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpReductionClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpReductionClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpReductionClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpReductionClause** objectArray = (SgOmpReductionClause**) &(SgOmpReductionClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpReductionClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpReductionClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpReductionClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpReductionClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpReductionClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpReductionClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpReductionClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpReductionClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpReductionClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpReductionClause** objectArray = (SgOmpReductionClause**) &(SgOmpReductionClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpReductionClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpReductionClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpReductionClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpReductionClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpReductionClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpReductionClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpReductionClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpReductionClause** objectArray = (SgOmpReductionClause**) &(SgOmpReductionClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpReductionClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpReductionClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpReductionClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpReductionClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpReductionClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpReductionClause** objectArray = (SgOmpReductionClause**) &(SgOmpReductionClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpReductionClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpReductionClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpReductionClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpReductionClause);

     return memory;
   }



/* #line 46192 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpMapClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpMapClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpMapClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpMapClause** objectArray = (SgOmpMapClause**) &(SgOmpMapClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpMapClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpMapClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpMapClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpMapClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpMapClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpMapClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpMapClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpMapClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpMapClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpMapClause** objectArray = (SgOmpMapClause**) &(SgOmpMapClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpMapClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpMapClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpMapClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpMapClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpMapClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpMapClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpMapClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpMapClause** objectArray = (SgOmpMapClause**) &(SgOmpMapClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpMapClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpMapClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpMapClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpMapClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpMapClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpMapClause** objectArray = (SgOmpMapClause**) &(SgOmpMapClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpMapClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpMapClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpMapClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpMapClause);

     return memory;
   }



/* #line 46424 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpUniformClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpUniformClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpUniformClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpUniformClause** objectArray = (SgOmpUniformClause**) &(SgOmpUniformClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpUniformClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpUniformClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpUniformClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpUniformClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpUniformClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpUniformClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpUniformClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpUniformClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpUniformClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpUniformClause** objectArray = (SgOmpUniformClause**) &(SgOmpUniformClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpUniformClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpUniformClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpUniformClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpUniformClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpUniformClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpUniformClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpUniformClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpUniformClause** objectArray = (SgOmpUniformClause**) &(SgOmpUniformClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpUniformClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpUniformClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpUniformClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpUniformClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpUniformClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpUniformClause** objectArray = (SgOmpUniformClause**) &(SgOmpUniformClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpUniformClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpUniformClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpUniformClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpUniformClause);

     return memory;
   }



/* #line 46656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpAlignedClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpAlignedClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpAlignedClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpAlignedClause** objectArray = (SgOmpAlignedClause**) &(SgOmpAlignedClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpAlignedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpAlignedClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpAlignedClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpAlignedClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpAlignedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpAlignedClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpAlignedClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpAlignedClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpAlignedClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpAlignedClause** objectArray = (SgOmpAlignedClause**) &(SgOmpAlignedClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpAlignedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpAlignedClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpAlignedClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpAlignedClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpAlignedClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpAlignedClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpAlignedClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpAlignedClause** objectArray = (SgOmpAlignedClause**) &(SgOmpAlignedClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpAlignedClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpAlignedClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpAlignedClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpAlignedClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpAlignedClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpAlignedClause** objectArray = (SgOmpAlignedClause**) &(SgOmpAlignedClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpAlignedClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpAlignedClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpAlignedClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpAlignedClause);

     return memory;
   }



/* #line 46888 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpLinearClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpLinearClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpLinearClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpLinearClause** objectArray = (SgOmpLinearClause**) &(SgOmpLinearClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpLinearClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpLinearClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpLinearClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpLinearClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpLinearClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpLinearClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpLinearClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpLinearClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpLinearClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpLinearClause** objectArray = (SgOmpLinearClause**) &(SgOmpLinearClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpLinearClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpLinearClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpLinearClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpLinearClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpLinearClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpLinearClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpLinearClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpLinearClause** objectArray = (SgOmpLinearClause**) &(SgOmpLinearClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpLinearClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpLinearClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpLinearClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpLinearClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpLinearClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpLinearClause** objectArray = (SgOmpLinearClause**) &(SgOmpLinearClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpLinearClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpLinearClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpLinearClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpLinearClause);

     return memory;
   }



/* #line 47120 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpDependClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpDependClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDependClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDependClause** objectArray = (SgOmpDependClause**) &(SgOmpDependClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpDependClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpDependClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpDependClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpDependClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpDependClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpDependClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpDependClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpDependClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDependClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDependClause** objectArray = (SgOmpDependClause**) &(SgOmpDependClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpDependClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpDependClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpDependClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpDependClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpDependClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpDependClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDependClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDependClause** objectArray = (SgOmpDependClause**) &(SgOmpDependClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpDependClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpDependClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpDependClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpDependClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpDependClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpDependClause** objectArray = (SgOmpDependClause**) &(SgOmpDependClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpDependClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpDependClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpDependClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpDependClause);

     return memory;
   }



/* #line 47352 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpScheduleClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpScheduleClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpScheduleClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpScheduleClause** objectArray = (SgOmpScheduleClause**) &(SgOmpScheduleClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpScheduleClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpScheduleClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpScheduleClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpScheduleClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpScheduleClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpScheduleClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpScheduleClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpScheduleClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpScheduleClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpScheduleClause** objectArray = (SgOmpScheduleClause**) &(SgOmpScheduleClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpScheduleClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpScheduleClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpScheduleClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpScheduleClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpScheduleClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpScheduleClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpScheduleClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpScheduleClause** objectArray = (SgOmpScheduleClause**) &(SgOmpScheduleClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpScheduleClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpScheduleClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpScheduleClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpScheduleClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpScheduleClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpScheduleClause** objectArray = (SgOmpScheduleClause**) &(SgOmpScheduleClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpScheduleClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpScheduleClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpScheduleClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpScheduleClause);

     return memory;
   }



/* #line 47584 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpMergeableClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpMergeableClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpMergeableClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpMergeableClause** objectArray = (SgOmpMergeableClause**) &(SgOmpMergeableClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpMergeableClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpMergeableClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpMergeableClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpMergeableClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpMergeableClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpMergeableClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpMergeableClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpMergeableClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpMergeableClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpMergeableClause** objectArray = (SgOmpMergeableClause**) &(SgOmpMergeableClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpMergeableClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpMergeableClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpMergeableClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpMergeableClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpMergeableClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpMergeableClause objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpMergeableClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpMergeableClause** objectArray = (SgOmpMergeableClause**) &(SgOmpMergeableClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpMergeableClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpMergeableClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpMergeableClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpMergeableClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpMergeableClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpMergeableClause** objectArray = (SgOmpMergeableClause**) &(SgOmpMergeableClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpMergeableClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpMergeableClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpMergeableClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpMergeableClause);

     return memory;
   }



/* #line 47816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLambdaCapture::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLambdaCapture objectArray [] = *(Memory_Block_List.begin());
     if (SgLambdaCapture::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLambdaCapture** objectArray = (SgLambdaCapture**) &(SgLambdaCapture::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLambdaCapture::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLambdaCapture::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLambdaCapture::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLambdaCapture*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLambdaCapture::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLambdaCapture::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLambdaCapture::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLambdaCapture objectArray [] = *(Memory_Block_List.begin());
     if (SgLambdaCapture::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLambdaCapture** objectArray = (SgLambdaCapture**) &(SgLambdaCapture::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLambdaCapture::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLambdaCapture::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLambdaCapture object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLambdaCapture object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLambdaCapture::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLambdaCapture objectArray [] = *(Memory_Block_List.begin());
     if (SgLambdaCapture::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLambdaCapture** objectArray = (SgLambdaCapture**) &(SgLambdaCapture::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLambdaCapture::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLambdaCapture::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLambdaCapture found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLambdaCapture::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLambdaCapture::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLambdaCapture** objectArray = (SgLambdaCapture**) &(SgLambdaCapture::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLambdaCapture::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLambdaCapture::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLambdaCapture::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLambdaCapture);

     return memory;
   }



/* #line 48048 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLambdaCaptureList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLambdaCaptureList objectArray [] = *(Memory_Block_List.begin());
     if (SgLambdaCaptureList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLambdaCaptureList** objectArray = (SgLambdaCaptureList**) &(SgLambdaCaptureList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLambdaCaptureList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLambdaCaptureList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLambdaCaptureList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLambdaCaptureList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLambdaCaptureList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLambdaCaptureList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLambdaCaptureList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLambdaCaptureList objectArray [] = *(Memory_Block_List.begin());
     if (SgLambdaCaptureList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLambdaCaptureList** objectArray = (SgLambdaCaptureList**) &(SgLambdaCaptureList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLambdaCaptureList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLambdaCaptureList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLambdaCaptureList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLambdaCaptureList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLambdaCaptureList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLambdaCaptureList objectArray [] = *(Memory_Block_List.begin());
     if (SgLambdaCaptureList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLambdaCaptureList** objectArray = (SgLambdaCaptureList**) &(SgLambdaCaptureList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLambdaCaptureList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLambdaCaptureList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLambdaCaptureList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLambdaCaptureList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLambdaCaptureList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLambdaCaptureList** objectArray = (SgLambdaCaptureList**) &(SgLambdaCaptureList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLambdaCaptureList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLambdaCaptureList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLambdaCaptureList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLambdaCaptureList);

     return memory;
   }



/* #line 48280 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaTypeConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaTypeConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTypeConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTypeConstraint** objectArray = (SgAdaTypeConstraint**) &(SgAdaTypeConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTypeConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaTypeConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaTypeConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaTypeConstraint*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaTypeConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTypeConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaTypeConstraint::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTypeConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTypeConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTypeConstraint** objectArray = (SgAdaTypeConstraint**) &(SgAdaTypeConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTypeConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTypeConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaTypeConstraint object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaTypeConstraint object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaTypeConstraint::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTypeConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTypeConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTypeConstraint** objectArray = (SgAdaTypeConstraint**) &(SgAdaTypeConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaTypeConstraint::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaTypeConstraint::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaTypeConstraint found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTypeConstraint::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaTypeConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaTypeConstraint** objectArray = (SgAdaTypeConstraint**) &(SgAdaTypeConstraint::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaTypeConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaTypeConstraint::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTypeConstraint::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaTypeConstraint);

     return memory;
   }



/* #line 48512 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaRangeConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaRangeConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRangeConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRangeConstraint** objectArray = (SgAdaRangeConstraint**) &(SgAdaRangeConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaRangeConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaRangeConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaRangeConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaRangeConstraint*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaRangeConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaRangeConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaRangeConstraint::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaRangeConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRangeConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRangeConstraint** objectArray = (SgAdaRangeConstraint**) &(SgAdaRangeConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaRangeConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaRangeConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaRangeConstraint object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaRangeConstraint object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaRangeConstraint::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaRangeConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRangeConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRangeConstraint** objectArray = (SgAdaRangeConstraint**) &(SgAdaRangeConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaRangeConstraint::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaRangeConstraint::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaRangeConstraint found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaRangeConstraint::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaRangeConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaRangeConstraint** objectArray = (SgAdaRangeConstraint**) &(SgAdaRangeConstraint::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaRangeConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaRangeConstraint::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaRangeConstraint::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaRangeConstraint);

     return memory;
   }



/* #line 48744 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaIndexConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaIndexConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaIndexConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaIndexConstraint** objectArray = (SgAdaIndexConstraint**) &(SgAdaIndexConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaIndexConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaIndexConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaIndexConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaIndexConstraint*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaIndexConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaIndexConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaIndexConstraint::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaIndexConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaIndexConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaIndexConstraint** objectArray = (SgAdaIndexConstraint**) &(SgAdaIndexConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaIndexConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaIndexConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaIndexConstraint object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaIndexConstraint object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaIndexConstraint::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaIndexConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaIndexConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaIndexConstraint** objectArray = (SgAdaIndexConstraint**) &(SgAdaIndexConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaIndexConstraint::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaIndexConstraint::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaIndexConstraint found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaIndexConstraint::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaIndexConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaIndexConstraint** objectArray = (SgAdaIndexConstraint**) &(SgAdaIndexConstraint::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaIndexConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaIndexConstraint::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaIndexConstraint::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaIndexConstraint);

     return memory;
   }



/* #line 48976 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaDigitsConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaDigitsConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDigitsConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDigitsConstraint** objectArray = (SgAdaDigitsConstraint**) &(SgAdaDigitsConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDigitsConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaDigitsConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaDigitsConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaDigitsConstraint*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaDigitsConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDigitsConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaDigitsConstraint::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDigitsConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDigitsConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDigitsConstraint** objectArray = (SgAdaDigitsConstraint**) &(SgAdaDigitsConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDigitsConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDigitsConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaDigitsConstraint object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaDigitsConstraint object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaDigitsConstraint::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDigitsConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDigitsConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDigitsConstraint** objectArray = (SgAdaDigitsConstraint**) &(SgAdaDigitsConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaDigitsConstraint::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaDigitsConstraint::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaDigitsConstraint found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDigitsConstraint::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaDigitsConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaDigitsConstraint** objectArray = (SgAdaDigitsConstraint**) &(SgAdaDigitsConstraint::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaDigitsConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaDigitsConstraint::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDigitsConstraint::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaDigitsConstraint);

     return memory;
   }



/* #line 49208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaDeltaConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaDeltaConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDeltaConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDeltaConstraint** objectArray = (SgAdaDeltaConstraint**) &(SgAdaDeltaConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDeltaConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaDeltaConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaDeltaConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaDeltaConstraint*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaDeltaConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDeltaConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaDeltaConstraint::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDeltaConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDeltaConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDeltaConstraint** objectArray = (SgAdaDeltaConstraint**) &(SgAdaDeltaConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDeltaConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDeltaConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaDeltaConstraint object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaDeltaConstraint object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaDeltaConstraint::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDeltaConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDeltaConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDeltaConstraint** objectArray = (SgAdaDeltaConstraint**) &(SgAdaDeltaConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaDeltaConstraint::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaDeltaConstraint::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaDeltaConstraint found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDeltaConstraint::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaDeltaConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaDeltaConstraint** objectArray = (SgAdaDeltaConstraint**) &(SgAdaDeltaConstraint::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaDeltaConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaDeltaConstraint::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDeltaConstraint::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaDeltaConstraint);

     return memory;
   }



/* #line 49440 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaDiscriminantConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaDiscriminantConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDiscriminantConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDiscriminantConstraint** objectArray = (SgAdaDiscriminantConstraint**) &(SgAdaDiscriminantConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDiscriminantConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaDiscriminantConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaDiscriminantConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaDiscriminantConstraint*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaDiscriminantConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDiscriminantConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaDiscriminantConstraint::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDiscriminantConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDiscriminantConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDiscriminantConstraint** objectArray = (SgAdaDiscriminantConstraint**) &(SgAdaDiscriminantConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDiscriminantConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDiscriminantConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaDiscriminantConstraint object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaDiscriminantConstraint object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaDiscriminantConstraint::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDiscriminantConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDiscriminantConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDiscriminantConstraint** objectArray = (SgAdaDiscriminantConstraint**) &(SgAdaDiscriminantConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaDiscriminantConstraint::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaDiscriminantConstraint::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaDiscriminantConstraint found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDiscriminantConstraint::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaDiscriminantConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaDiscriminantConstraint** objectArray = (SgAdaDiscriminantConstraint**) &(SgAdaDiscriminantConstraint::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaDiscriminantConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaDiscriminantConstraint::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDiscriminantConstraint::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaDiscriminantConstraint);

     return memory;
   }



/* #line 49672 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaNullConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaNullConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaNullConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaNullConstraint** objectArray = (SgAdaNullConstraint**) &(SgAdaNullConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaNullConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaNullConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaNullConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaNullConstraint*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaNullConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaNullConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaNullConstraint::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaNullConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaNullConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaNullConstraint** objectArray = (SgAdaNullConstraint**) &(SgAdaNullConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaNullConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaNullConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaNullConstraint object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaNullConstraint object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaNullConstraint::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaNullConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaNullConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaNullConstraint** objectArray = (SgAdaNullConstraint**) &(SgAdaNullConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaNullConstraint::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaNullConstraint::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaNullConstraint found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaNullConstraint::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaNullConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaNullConstraint** objectArray = (SgAdaNullConstraint**) &(SgAdaNullConstraint::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaNullConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaNullConstraint::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaNullConstraint::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaNullConstraint);

     return memory;
   }



/* #line 49904 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStatement** objectArray = (SgStatement**) &(SgStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStatement** objectArray = (SgStatement**) &(SgStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStatement** objectArray = (SgStatement**) &(SgStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgStatement** objectArray = (SgStatement**) &(SgStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgStatement);

     return memory;
   }



/* #line 50136 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgScopeStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgScopeStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgScopeStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgScopeStatement** objectArray = (SgScopeStatement**) &(SgScopeStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgScopeStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgScopeStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgScopeStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgScopeStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgScopeStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgScopeStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgScopeStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgScopeStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgScopeStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgScopeStatement** objectArray = (SgScopeStatement**) &(SgScopeStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgScopeStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgScopeStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgScopeStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgScopeStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgScopeStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgScopeStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgScopeStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgScopeStatement** objectArray = (SgScopeStatement**) &(SgScopeStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgScopeStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgScopeStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgScopeStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgScopeStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgScopeStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgScopeStatement** objectArray = (SgScopeStatement**) &(SgScopeStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgScopeStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgScopeStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgScopeStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgScopeStatement);

     return memory;
   }



/* #line 50368 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgGlobal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgGlobal objectArray [] = *(Memory_Block_List.begin());
     if (SgGlobal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGlobal** objectArray = (SgGlobal**) &(SgGlobal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGlobal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgGlobal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgGlobal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgGlobal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgGlobal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGlobal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgGlobal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGlobal objectArray [] = *(Memory_Block_List.begin());
     if (SgGlobal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGlobal** objectArray = (SgGlobal**) &(SgGlobal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGlobal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGlobal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgGlobal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgGlobal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgGlobal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGlobal objectArray [] = *(Memory_Block_List.begin());
     if (SgGlobal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGlobal** objectArray = (SgGlobal**) &(SgGlobal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgGlobal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgGlobal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgGlobal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGlobal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgGlobal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgGlobal** objectArray = (SgGlobal**) &(SgGlobal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgGlobal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgGlobal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGlobal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgGlobal);

     return memory;
   }



/* #line 50600 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBasicBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBasicBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgBasicBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBasicBlock** objectArray = (SgBasicBlock**) &(SgBasicBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBasicBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBasicBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBasicBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBasicBlock*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBasicBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBasicBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBasicBlock::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBasicBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgBasicBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBasicBlock** objectArray = (SgBasicBlock**) &(SgBasicBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBasicBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBasicBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBasicBlock object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBasicBlock object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBasicBlock::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBasicBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgBasicBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBasicBlock** objectArray = (SgBasicBlock**) &(SgBasicBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBasicBlock::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBasicBlock::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBasicBlock found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBasicBlock::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBasicBlock::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBasicBlock** objectArray = (SgBasicBlock**) &(SgBasicBlock::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBasicBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBasicBlock::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBasicBlock::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBasicBlock);

     return memory;
   }



/* #line 50832 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIfStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIfStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgIfStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIfStmt** objectArray = (SgIfStmt**) &(SgIfStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIfStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIfStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIfStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIfStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIfStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIfStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIfStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIfStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgIfStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIfStmt** objectArray = (SgIfStmt**) &(SgIfStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIfStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIfStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIfStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIfStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIfStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIfStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgIfStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIfStmt** objectArray = (SgIfStmt**) &(SgIfStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIfStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIfStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIfStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIfStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIfStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIfStmt** objectArray = (SgIfStmt**) &(SgIfStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIfStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIfStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIfStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIfStmt);

     return memory;
   }



/* #line 51064 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgForStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgForStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgForStatement** objectArray = (SgForStatement**) &(SgForStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgForStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgForStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgForStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgForStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgForStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgForStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgForStatement** objectArray = (SgForStatement**) &(SgForStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgForStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgForStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgForStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgForStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgForStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgForStatement** objectArray = (SgForStatement**) &(SgForStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgForStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgForStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgForStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgForStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgForStatement** objectArray = (SgForStatement**) &(SgForStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgForStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgForStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgForStatement);

     return memory;
   }



/* #line 51296 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFunctionDefinition::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFunctionDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionDefinition** objectArray = (SgFunctionDefinition**) &(SgFunctionDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFunctionDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFunctionDefinition::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFunctionDefinition*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFunctionDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFunctionDefinition::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionDefinition** objectArray = (SgFunctionDefinition**) &(SgFunctionDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFunctionDefinition object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFunctionDefinition object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFunctionDefinition::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionDefinition** objectArray = (SgFunctionDefinition**) &(SgFunctionDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFunctionDefinition::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFunctionDefinition::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFunctionDefinition found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionDefinition::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFunctionDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFunctionDefinition** objectArray = (SgFunctionDefinition**) &(SgFunctionDefinition::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFunctionDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFunctionDefinition::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionDefinition::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFunctionDefinition);

     return memory;
   }



/* #line 51528 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateFunctionDefinition::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateFunctionDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateFunctionDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateFunctionDefinition** objectArray = (SgTemplateFunctionDefinition**) &(SgTemplateFunctionDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateFunctionDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateFunctionDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateFunctionDefinition::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateFunctionDefinition*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateFunctionDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateFunctionDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateFunctionDefinition::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateFunctionDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateFunctionDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateFunctionDefinition** objectArray = (SgTemplateFunctionDefinition**) &(SgTemplateFunctionDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateFunctionDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateFunctionDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateFunctionDefinition object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateFunctionDefinition object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateFunctionDefinition::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateFunctionDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateFunctionDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateFunctionDefinition** objectArray = (SgTemplateFunctionDefinition**) &(SgTemplateFunctionDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateFunctionDefinition::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateFunctionDefinition::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateFunctionDefinition found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateFunctionDefinition::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateFunctionDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateFunctionDefinition** objectArray = (SgTemplateFunctionDefinition**) &(SgTemplateFunctionDefinition::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateFunctionDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateFunctionDefinition::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateFunctionDefinition::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateFunctionDefinition);

     return memory;
   }



/* #line 51760 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgClassDefinition::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgClassDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgClassDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassDefinition** objectArray = (SgClassDefinition**) &(SgClassDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgClassDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgClassDefinition::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgClassDefinition*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgClassDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgClassDefinition::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgClassDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassDefinition** objectArray = (SgClassDefinition**) &(SgClassDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgClassDefinition object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgClassDefinition object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgClassDefinition::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgClassDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassDefinition** objectArray = (SgClassDefinition**) &(SgClassDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgClassDefinition::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgClassDefinition::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgClassDefinition found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassDefinition::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgClassDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgClassDefinition** objectArray = (SgClassDefinition**) &(SgClassDefinition::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgClassDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgClassDefinition::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassDefinition::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgClassDefinition);

     return memory;
   }



/* #line 51992 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateInstantiationDefn::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateInstantiationDefn objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationDefn::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationDefn** objectArray = (SgTemplateInstantiationDefn**) &(SgTemplateInstantiationDefn::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateInstantiationDefn::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateInstantiationDefn::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateInstantiationDefn::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateInstantiationDefn*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateInstantiationDefn::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateInstantiationDefn::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateInstantiationDefn::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateInstantiationDefn objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationDefn::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationDefn** objectArray = (SgTemplateInstantiationDefn**) &(SgTemplateInstantiationDefn::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateInstantiationDefn::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateInstantiationDefn::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateInstantiationDefn object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateInstantiationDefn object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateInstantiationDefn::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateInstantiationDefn objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationDefn::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationDefn** objectArray = (SgTemplateInstantiationDefn**) &(SgTemplateInstantiationDefn::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateInstantiationDefn::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateInstantiationDefn::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateInstantiationDefn found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateInstantiationDefn::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateInstantiationDefn::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateInstantiationDefn** objectArray = (SgTemplateInstantiationDefn**) &(SgTemplateInstantiationDefn::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateInstantiationDefn::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateInstantiationDefn::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateInstantiationDefn::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateInstantiationDefn);

     return memory;
   }



/* #line 52224 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateClassDefinition::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateClassDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateClassDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateClassDefinition** objectArray = (SgTemplateClassDefinition**) &(SgTemplateClassDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateClassDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateClassDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateClassDefinition::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateClassDefinition*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateClassDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateClassDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateClassDefinition::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateClassDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateClassDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateClassDefinition** objectArray = (SgTemplateClassDefinition**) &(SgTemplateClassDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateClassDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateClassDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateClassDefinition object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateClassDefinition object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateClassDefinition::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateClassDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateClassDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateClassDefinition** objectArray = (SgTemplateClassDefinition**) &(SgTemplateClassDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateClassDefinition::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateClassDefinition::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateClassDefinition found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateClassDefinition::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateClassDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateClassDefinition** objectArray = (SgTemplateClassDefinition**) &(SgTemplateClassDefinition::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateClassDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateClassDefinition::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateClassDefinition::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateClassDefinition);

     return memory;
   }



/* #line 52456 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgWhileStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgWhileStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgWhileStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWhileStmt** objectArray = (SgWhileStmt**) &(SgWhileStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWhileStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgWhileStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgWhileStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgWhileStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgWhileStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWhileStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgWhileStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWhileStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgWhileStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWhileStmt** objectArray = (SgWhileStmt**) &(SgWhileStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWhileStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWhileStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgWhileStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgWhileStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgWhileStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWhileStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgWhileStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWhileStmt** objectArray = (SgWhileStmt**) &(SgWhileStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgWhileStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgWhileStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgWhileStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWhileStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgWhileStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgWhileStmt** objectArray = (SgWhileStmt**) &(SgWhileStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgWhileStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgWhileStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWhileStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgWhileStmt);

     return memory;
   }



/* #line 52688 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDoWhileStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDoWhileStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgDoWhileStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDoWhileStmt** objectArray = (SgDoWhileStmt**) &(SgDoWhileStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDoWhileStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDoWhileStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDoWhileStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDoWhileStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDoWhileStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDoWhileStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDoWhileStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDoWhileStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgDoWhileStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDoWhileStmt** objectArray = (SgDoWhileStmt**) &(SgDoWhileStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDoWhileStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDoWhileStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDoWhileStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDoWhileStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDoWhileStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDoWhileStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgDoWhileStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDoWhileStmt** objectArray = (SgDoWhileStmt**) &(SgDoWhileStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDoWhileStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDoWhileStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDoWhileStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDoWhileStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDoWhileStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDoWhileStmt** objectArray = (SgDoWhileStmt**) &(SgDoWhileStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDoWhileStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDoWhileStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDoWhileStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDoWhileStmt);

     return memory;
   }



/* #line 52920 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSwitchStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSwitchStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSwitchStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSwitchStatement** objectArray = (SgSwitchStatement**) &(SgSwitchStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSwitchStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSwitchStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSwitchStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSwitchStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSwitchStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSwitchStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSwitchStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSwitchStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSwitchStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSwitchStatement** objectArray = (SgSwitchStatement**) &(SgSwitchStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSwitchStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSwitchStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSwitchStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSwitchStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSwitchStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSwitchStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSwitchStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSwitchStatement** objectArray = (SgSwitchStatement**) &(SgSwitchStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSwitchStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSwitchStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSwitchStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSwitchStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSwitchStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSwitchStatement** objectArray = (SgSwitchStatement**) &(SgSwitchStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSwitchStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSwitchStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSwitchStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSwitchStatement);

     return memory;
   }



/* #line 53152 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCatchOptionStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCatchOptionStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgCatchOptionStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCatchOptionStmt** objectArray = (SgCatchOptionStmt**) &(SgCatchOptionStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCatchOptionStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCatchOptionStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCatchOptionStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCatchOptionStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCatchOptionStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCatchOptionStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCatchOptionStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCatchOptionStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgCatchOptionStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCatchOptionStmt** objectArray = (SgCatchOptionStmt**) &(SgCatchOptionStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCatchOptionStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCatchOptionStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCatchOptionStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCatchOptionStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCatchOptionStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCatchOptionStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgCatchOptionStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCatchOptionStmt** objectArray = (SgCatchOptionStmt**) &(SgCatchOptionStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCatchOptionStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCatchOptionStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCatchOptionStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCatchOptionStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCatchOptionStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCatchOptionStmt** objectArray = (SgCatchOptionStmt**) &(SgCatchOptionStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCatchOptionStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCatchOptionStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCatchOptionStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCatchOptionStmt);

     return memory;
   }



/* #line 53384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNamespaceDefinitionStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNamespaceDefinitionStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNamespaceDefinitionStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamespaceDefinitionStatement** objectArray = (SgNamespaceDefinitionStatement**) &(SgNamespaceDefinitionStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNamespaceDefinitionStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNamespaceDefinitionStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNamespaceDefinitionStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNamespaceDefinitionStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNamespaceDefinitionStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNamespaceDefinitionStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNamespaceDefinitionStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNamespaceDefinitionStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNamespaceDefinitionStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamespaceDefinitionStatement** objectArray = (SgNamespaceDefinitionStatement**) &(SgNamespaceDefinitionStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNamespaceDefinitionStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNamespaceDefinitionStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNamespaceDefinitionStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNamespaceDefinitionStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNamespaceDefinitionStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNamespaceDefinitionStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNamespaceDefinitionStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamespaceDefinitionStatement** objectArray = (SgNamespaceDefinitionStatement**) &(SgNamespaceDefinitionStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNamespaceDefinitionStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNamespaceDefinitionStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNamespaceDefinitionStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNamespaceDefinitionStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNamespaceDefinitionStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNamespaceDefinitionStatement** objectArray = (SgNamespaceDefinitionStatement**) &(SgNamespaceDefinitionStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNamespaceDefinitionStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNamespaceDefinitionStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNamespaceDefinitionStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNamespaceDefinitionStatement);

     return memory;
   }



/* #line 53616 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBlockDataStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBlockDataStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgBlockDataStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBlockDataStatement** objectArray = (SgBlockDataStatement**) &(SgBlockDataStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBlockDataStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBlockDataStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBlockDataStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBlockDataStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBlockDataStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBlockDataStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBlockDataStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBlockDataStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgBlockDataStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBlockDataStatement** objectArray = (SgBlockDataStatement**) &(SgBlockDataStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBlockDataStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBlockDataStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBlockDataStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBlockDataStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBlockDataStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBlockDataStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgBlockDataStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBlockDataStatement** objectArray = (SgBlockDataStatement**) &(SgBlockDataStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBlockDataStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBlockDataStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBlockDataStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBlockDataStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBlockDataStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBlockDataStatement** objectArray = (SgBlockDataStatement**) &(SgBlockDataStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBlockDataStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBlockDataStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBlockDataStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBlockDataStatement);

     return memory;
   }



/* #line 53848 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAssociateStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAssociateStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAssociateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssociateStatement** objectArray = (SgAssociateStatement**) &(SgAssociateStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAssociateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAssociateStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAssociateStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAssociateStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAssociateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAssociateStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAssociateStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAssociateStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAssociateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssociateStatement** objectArray = (SgAssociateStatement**) &(SgAssociateStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAssociateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAssociateStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAssociateStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAssociateStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAssociateStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAssociateStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAssociateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssociateStatement** objectArray = (SgAssociateStatement**) &(SgAssociateStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAssociateStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAssociateStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAssociateStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAssociateStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAssociateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAssociateStatement** objectArray = (SgAssociateStatement**) &(SgAssociateStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAssociateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAssociateStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAssociateStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAssociateStatement);

     return memory;
   }



/* #line 54080 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFortranDo::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFortranDo objectArray [] = *(Memory_Block_List.begin());
     if (SgFortranDo::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFortranDo** objectArray = (SgFortranDo**) &(SgFortranDo::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFortranDo::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFortranDo::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFortranDo::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFortranDo*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFortranDo::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFortranDo::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFortranDo::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFortranDo objectArray [] = *(Memory_Block_List.begin());
     if (SgFortranDo::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFortranDo** objectArray = (SgFortranDo**) &(SgFortranDo::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFortranDo::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFortranDo::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFortranDo object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFortranDo object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFortranDo::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFortranDo objectArray [] = *(Memory_Block_List.begin());
     if (SgFortranDo::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFortranDo** objectArray = (SgFortranDo**) &(SgFortranDo::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFortranDo::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFortranDo::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFortranDo found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFortranDo::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFortranDo::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFortranDo** objectArray = (SgFortranDo**) &(SgFortranDo::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFortranDo::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFortranDo::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFortranDo::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFortranDo);

     return memory;
   }



/* #line 54312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFortranNonblockedDo::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFortranNonblockedDo objectArray [] = *(Memory_Block_List.begin());
     if (SgFortranNonblockedDo::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFortranNonblockedDo** objectArray = (SgFortranNonblockedDo**) &(SgFortranNonblockedDo::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFortranNonblockedDo::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFortranNonblockedDo::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFortranNonblockedDo::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFortranNonblockedDo*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFortranNonblockedDo::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFortranNonblockedDo::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFortranNonblockedDo::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFortranNonblockedDo objectArray [] = *(Memory_Block_List.begin());
     if (SgFortranNonblockedDo::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFortranNonblockedDo** objectArray = (SgFortranNonblockedDo**) &(SgFortranNonblockedDo::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFortranNonblockedDo::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFortranNonblockedDo::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFortranNonblockedDo object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFortranNonblockedDo object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFortranNonblockedDo::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFortranNonblockedDo objectArray [] = *(Memory_Block_List.begin());
     if (SgFortranNonblockedDo::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFortranNonblockedDo** objectArray = (SgFortranNonblockedDo**) &(SgFortranNonblockedDo::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFortranNonblockedDo::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFortranNonblockedDo::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFortranNonblockedDo found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFortranNonblockedDo::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFortranNonblockedDo::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFortranNonblockedDo** objectArray = (SgFortranNonblockedDo**) &(SgFortranNonblockedDo::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFortranNonblockedDo::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFortranNonblockedDo::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFortranNonblockedDo::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFortranNonblockedDo);

     return memory;
   }



/* #line 54544 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgForAllStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgForAllStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgForAllStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgForAllStatement** objectArray = (SgForAllStatement**) &(SgForAllStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgForAllStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgForAllStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgForAllStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgForAllStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgForAllStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgForAllStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgForAllStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgForAllStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgForAllStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgForAllStatement** objectArray = (SgForAllStatement**) &(SgForAllStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgForAllStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgForAllStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgForAllStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgForAllStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgForAllStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgForAllStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgForAllStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgForAllStatement** objectArray = (SgForAllStatement**) &(SgForAllStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgForAllStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgForAllStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgForAllStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgForAllStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgForAllStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgForAllStatement** objectArray = (SgForAllStatement**) &(SgForAllStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgForAllStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgForAllStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgForAllStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgForAllStatement);

     return memory;
   }



/* #line 54776 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUpcForAllStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUpcForAllStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcForAllStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcForAllStatement** objectArray = (SgUpcForAllStatement**) &(SgUpcForAllStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcForAllStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUpcForAllStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUpcForAllStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUpcForAllStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUpcForAllStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcForAllStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUpcForAllStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcForAllStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcForAllStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcForAllStatement** objectArray = (SgUpcForAllStatement**) &(SgUpcForAllStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcForAllStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcForAllStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUpcForAllStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUpcForAllStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUpcForAllStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcForAllStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcForAllStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcForAllStatement** objectArray = (SgUpcForAllStatement**) &(SgUpcForAllStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUpcForAllStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUpcForAllStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUpcForAllStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcForAllStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUpcForAllStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUpcForAllStatement** objectArray = (SgUpcForAllStatement**) &(SgUpcForAllStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUpcForAllStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUpcForAllStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcForAllStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUpcForAllStatement);

     return memory;
   }



/* #line 55008 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCAFWithTeamStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCAFWithTeamStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgCAFWithTeamStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCAFWithTeamStatement** objectArray = (SgCAFWithTeamStatement**) &(SgCAFWithTeamStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCAFWithTeamStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCAFWithTeamStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCAFWithTeamStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCAFWithTeamStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCAFWithTeamStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCAFWithTeamStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCAFWithTeamStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCAFWithTeamStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgCAFWithTeamStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCAFWithTeamStatement** objectArray = (SgCAFWithTeamStatement**) &(SgCAFWithTeamStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCAFWithTeamStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCAFWithTeamStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCAFWithTeamStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCAFWithTeamStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCAFWithTeamStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCAFWithTeamStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgCAFWithTeamStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCAFWithTeamStatement** objectArray = (SgCAFWithTeamStatement**) &(SgCAFWithTeamStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCAFWithTeamStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCAFWithTeamStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCAFWithTeamStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCAFWithTeamStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCAFWithTeamStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCAFWithTeamStatement** objectArray = (SgCAFWithTeamStatement**) &(SgCAFWithTeamStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCAFWithTeamStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCAFWithTeamStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCAFWithTeamStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCAFWithTeamStatement);

     return memory;
   }



/* #line 55240 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaForEachStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaForEachStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaForEachStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaForEachStatement** objectArray = (SgJavaForEachStatement**) &(SgJavaForEachStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaForEachStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaForEachStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaForEachStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaForEachStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaForEachStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaForEachStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaForEachStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaForEachStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaForEachStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaForEachStatement** objectArray = (SgJavaForEachStatement**) &(SgJavaForEachStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaForEachStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaForEachStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaForEachStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaForEachStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaForEachStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaForEachStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaForEachStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaForEachStatement** objectArray = (SgJavaForEachStatement**) &(SgJavaForEachStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaForEachStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaForEachStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaForEachStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaForEachStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaForEachStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaForEachStatement** objectArray = (SgJavaForEachStatement**) &(SgJavaForEachStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaForEachStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaForEachStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaForEachStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaForEachStatement);

     return memory;
   }



/* #line 55472 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaLabelStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaLabelStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaLabelStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaLabelStatement** objectArray = (SgJavaLabelStatement**) &(SgJavaLabelStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaLabelStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaLabelStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaLabelStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaLabelStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaLabelStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaLabelStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaLabelStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaLabelStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaLabelStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaLabelStatement** objectArray = (SgJavaLabelStatement**) &(SgJavaLabelStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaLabelStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaLabelStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaLabelStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaLabelStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaLabelStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaLabelStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaLabelStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaLabelStatement** objectArray = (SgJavaLabelStatement**) &(SgJavaLabelStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaLabelStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaLabelStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaLabelStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaLabelStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaLabelStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaLabelStatement** objectArray = (SgJavaLabelStatement**) &(SgJavaLabelStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaLabelStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaLabelStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaLabelStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaLabelStatement);

     return memory;
   }



/* #line 55704 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMatlabForStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMatlabForStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgMatlabForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMatlabForStatement** objectArray = (SgMatlabForStatement**) &(SgMatlabForStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMatlabForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMatlabForStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMatlabForStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMatlabForStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMatlabForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMatlabForStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMatlabForStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMatlabForStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgMatlabForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMatlabForStatement** objectArray = (SgMatlabForStatement**) &(SgMatlabForStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMatlabForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMatlabForStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMatlabForStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMatlabForStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMatlabForStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMatlabForStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgMatlabForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMatlabForStatement** objectArray = (SgMatlabForStatement**) &(SgMatlabForStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMatlabForStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMatlabForStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMatlabForStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMatlabForStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMatlabForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMatlabForStatement** objectArray = (SgMatlabForStatement**) &(SgMatlabForStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMatlabForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMatlabForStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMatlabForStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMatlabForStatement);

     return memory;
   }



/* #line 55936 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFunctionParameterScope::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFunctionParameterScope objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionParameterScope::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionParameterScope** objectArray = (SgFunctionParameterScope**) &(SgFunctionParameterScope::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionParameterScope::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFunctionParameterScope::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFunctionParameterScope::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFunctionParameterScope*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFunctionParameterScope::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionParameterScope::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFunctionParameterScope::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionParameterScope objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionParameterScope::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionParameterScope** objectArray = (SgFunctionParameterScope**) &(SgFunctionParameterScope::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionParameterScope::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionParameterScope::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFunctionParameterScope object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFunctionParameterScope object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFunctionParameterScope::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionParameterScope objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionParameterScope::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionParameterScope** objectArray = (SgFunctionParameterScope**) &(SgFunctionParameterScope::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFunctionParameterScope::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFunctionParameterScope::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFunctionParameterScope found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionParameterScope::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFunctionParameterScope::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFunctionParameterScope** objectArray = (SgFunctionParameterScope**) &(SgFunctionParameterScope::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFunctionParameterScope::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFunctionParameterScope::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionParameterScope::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFunctionParameterScope);

     return memory;
   }



/* #line 56168 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDeclarationScope::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDeclarationScope objectArray [] = *(Memory_Block_List.begin());
     if (SgDeclarationScope::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeclarationScope** objectArray = (SgDeclarationScope**) &(SgDeclarationScope::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeclarationScope::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDeclarationScope::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDeclarationScope::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDeclarationScope*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDeclarationScope::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeclarationScope::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDeclarationScope::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeclarationScope objectArray [] = *(Memory_Block_List.begin());
     if (SgDeclarationScope::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeclarationScope** objectArray = (SgDeclarationScope**) &(SgDeclarationScope::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeclarationScope::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeclarationScope::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDeclarationScope object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDeclarationScope object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDeclarationScope::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeclarationScope objectArray [] = *(Memory_Block_List.begin());
     if (SgDeclarationScope::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeclarationScope** objectArray = (SgDeclarationScope**) &(SgDeclarationScope::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDeclarationScope::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDeclarationScope::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDeclarationScope found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeclarationScope::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDeclarationScope::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDeclarationScope** objectArray = (SgDeclarationScope**) &(SgDeclarationScope::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDeclarationScope::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDeclarationScope::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeclarationScope::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDeclarationScope);

     return memory;
   }



/* #line 56400 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgRangeBasedForStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgRangeBasedForStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgRangeBasedForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRangeBasedForStatement** objectArray = (SgRangeBasedForStatement**) &(SgRangeBasedForStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRangeBasedForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgRangeBasedForStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgRangeBasedForStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgRangeBasedForStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgRangeBasedForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRangeBasedForStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgRangeBasedForStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRangeBasedForStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgRangeBasedForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRangeBasedForStatement** objectArray = (SgRangeBasedForStatement**) &(SgRangeBasedForStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRangeBasedForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRangeBasedForStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgRangeBasedForStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgRangeBasedForStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgRangeBasedForStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRangeBasedForStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgRangeBasedForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRangeBasedForStatement** objectArray = (SgRangeBasedForStatement**) &(SgRangeBasedForStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgRangeBasedForStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgRangeBasedForStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgRangeBasedForStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRangeBasedForStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgRangeBasedForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgRangeBasedForStatement** objectArray = (SgRangeBasedForStatement**) &(SgRangeBasedForStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgRangeBasedForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgRangeBasedForStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRangeBasedForStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgRangeBasedForStatement);

     return memory;
   }



/* #line 56632 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJovialForThenStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJovialForThenStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialForThenStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialForThenStatement** objectArray = (SgJovialForThenStatement**) &(SgJovialForThenStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialForThenStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJovialForThenStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJovialForThenStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJovialForThenStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJovialForThenStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialForThenStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJovialForThenStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialForThenStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialForThenStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialForThenStatement** objectArray = (SgJovialForThenStatement**) &(SgJovialForThenStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialForThenStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialForThenStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJovialForThenStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJovialForThenStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJovialForThenStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialForThenStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialForThenStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialForThenStatement** objectArray = (SgJovialForThenStatement**) &(SgJovialForThenStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJovialForThenStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJovialForThenStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJovialForThenStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialForThenStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJovialForThenStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJovialForThenStatement** objectArray = (SgJovialForThenStatement**) &(SgJovialForThenStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJovialForThenStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJovialForThenStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialForThenStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJovialForThenStatement);

     return memory;
   }



/* #line 56864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaAcceptStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaAcceptStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAcceptStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAcceptStmt** objectArray = (SgAdaAcceptStmt**) &(SgAdaAcceptStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaAcceptStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaAcceptStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaAcceptStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaAcceptStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaAcceptStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaAcceptStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaAcceptStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaAcceptStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAcceptStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAcceptStmt** objectArray = (SgAdaAcceptStmt**) &(SgAdaAcceptStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaAcceptStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaAcceptStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaAcceptStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaAcceptStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaAcceptStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaAcceptStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAcceptStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAcceptStmt** objectArray = (SgAdaAcceptStmt**) &(SgAdaAcceptStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaAcceptStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaAcceptStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaAcceptStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaAcceptStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaAcceptStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaAcceptStmt** objectArray = (SgAdaAcceptStmt**) &(SgAdaAcceptStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaAcceptStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaAcceptStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaAcceptStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaAcceptStmt);

     return memory;
   }



/* #line 57096 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaPackageSpec::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaPackageSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageSpec** objectArray = (SgAdaPackageSpec**) &(SgAdaPackageSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaPackageSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaPackageSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaPackageSpec::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaPackageSpec*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaPackageSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaPackageSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaPackageSpec::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaPackageSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageSpec** objectArray = (SgAdaPackageSpec**) &(SgAdaPackageSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaPackageSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaPackageSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaPackageSpec object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaPackageSpec object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaPackageSpec::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaPackageSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageSpec** objectArray = (SgAdaPackageSpec**) &(SgAdaPackageSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaPackageSpec::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaPackageSpec::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaPackageSpec found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaPackageSpec::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaPackageSpec::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaPackageSpec** objectArray = (SgAdaPackageSpec**) &(SgAdaPackageSpec::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaPackageSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaPackageSpec::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaPackageSpec::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaPackageSpec);

     return memory;
   }



/* #line 57328 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaPackageBody::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaPackageBody objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageBody** objectArray = (SgAdaPackageBody**) &(SgAdaPackageBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaPackageBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaPackageBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaPackageBody::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaPackageBody*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaPackageBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaPackageBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaPackageBody::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaPackageBody objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageBody** objectArray = (SgAdaPackageBody**) &(SgAdaPackageBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaPackageBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaPackageBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaPackageBody object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaPackageBody object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaPackageBody::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaPackageBody objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageBody** objectArray = (SgAdaPackageBody**) &(SgAdaPackageBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaPackageBody::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaPackageBody::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaPackageBody found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaPackageBody::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaPackageBody::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaPackageBody** objectArray = (SgAdaPackageBody**) &(SgAdaPackageBody::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaPackageBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaPackageBody::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaPackageBody::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaPackageBody);

     return memory;
   }



/* #line 57560 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaTaskSpec::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaTaskSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskSpec** objectArray = (SgAdaTaskSpec**) &(SgAdaTaskSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaTaskSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaTaskSpec::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaTaskSpec*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaTaskSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaTaskSpec::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskSpec** objectArray = (SgAdaTaskSpec**) &(SgAdaTaskSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaTaskSpec object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaTaskSpec object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaTaskSpec::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskSpec** objectArray = (SgAdaTaskSpec**) &(SgAdaTaskSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaTaskSpec::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaTaskSpec::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaTaskSpec found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskSpec::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaTaskSpec::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaTaskSpec** objectArray = (SgAdaTaskSpec**) &(SgAdaTaskSpec::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaTaskSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaTaskSpec::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskSpec::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaTaskSpec);

     return memory;
   }



/* #line 57792 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaTaskBody::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaTaskBody objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskBody** objectArray = (SgAdaTaskBody**) &(SgAdaTaskBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaTaskBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaTaskBody::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaTaskBody*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaTaskBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaTaskBody::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskBody objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskBody** objectArray = (SgAdaTaskBody**) &(SgAdaTaskBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaTaskBody object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaTaskBody object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaTaskBody::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskBody objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskBody** objectArray = (SgAdaTaskBody**) &(SgAdaTaskBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaTaskBody::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaTaskBody::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaTaskBody found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskBody::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaTaskBody::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaTaskBody** objectArray = (SgAdaTaskBody**) &(SgAdaTaskBody::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaTaskBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaTaskBody::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskBody::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaTaskBody);

     return memory;
   }



/* #line 58024 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaProtectedBody::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaProtectedBody objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedBody** objectArray = (SgAdaProtectedBody**) &(SgAdaProtectedBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaProtectedBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaProtectedBody::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaProtectedBody*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaProtectedBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaProtectedBody::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedBody objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedBody** objectArray = (SgAdaProtectedBody**) &(SgAdaProtectedBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedBody::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaProtectedBody object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaProtectedBody object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaProtectedBody::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedBody objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedBody::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedBody** objectArray = (SgAdaProtectedBody**) &(SgAdaProtectedBody::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaProtectedBody::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaProtectedBody::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaProtectedBody found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedBody::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaProtectedBody::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaProtectedBody** objectArray = (SgAdaProtectedBody**) &(SgAdaProtectedBody::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaProtectedBody::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaProtectedBody::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedBody::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaProtectedBody);

     return memory;
   }



/* #line 58256 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaProtectedSpec::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaProtectedSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedSpec** objectArray = (SgAdaProtectedSpec**) &(SgAdaProtectedSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaProtectedSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaProtectedSpec::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaProtectedSpec*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaProtectedSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaProtectedSpec::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedSpec** objectArray = (SgAdaProtectedSpec**) &(SgAdaProtectedSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaProtectedSpec object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaProtectedSpec object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaProtectedSpec::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedSpec** objectArray = (SgAdaProtectedSpec**) &(SgAdaProtectedSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaProtectedSpec::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaProtectedSpec::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaProtectedSpec found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedSpec::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaProtectedSpec::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaProtectedSpec** objectArray = (SgAdaProtectedSpec**) &(SgAdaProtectedSpec::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaProtectedSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaProtectedSpec::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedSpec::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaProtectedSpec);

     return memory;
   }



/* #line 58488 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaGenericDefn::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaGenericDefn objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericDefn::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericDefn** objectArray = (SgAdaGenericDefn**) &(SgAdaGenericDefn::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaGenericDefn::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaGenericDefn::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaGenericDefn::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaGenericDefn*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaGenericDefn::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaGenericDefn::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaGenericDefn::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaGenericDefn objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericDefn::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericDefn** objectArray = (SgAdaGenericDefn**) &(SgAdaGenericDefn::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaGenericDefn::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaGenericDefn::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaGenericDefn object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaGenericDefn object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaGenericDefn::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaGenericDefn objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericDefn::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericDefn** objectArray = (SgAdaGenericDefn**) &(SgAdaGenericDefn::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaGenericDefn::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaGenericDefn::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaGenericDefn found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaGenericDefn::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaGenericDefn::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaGenericDefn** objectArray = (SgAdaGenericDefn**) &(SgAdaGenericDefn::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaGenericDefn::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaGenericDefn::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaGenericDefn::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaGenericDefn);

     return memory;
   }



/* #line 58720 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFunctionTypeTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFunctionTypeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionTypeTable** objectArray = (SgFunctionTypeTable**) &(SgFunctionTypeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFunctionTypeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFunctionTypeTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFunctionTypeTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFunctionTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionTypeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFunctionTypeTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionTypeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionTypeTable** objectArray = (SgFunctionTypeTable**) &(SgFunctionTypeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionTypeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFunctionTypeTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFunctionTypeTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFunctionTypeTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionTypeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionTypeTable** objectArray = (SgFunctionTypeTable**) &(SgFunctionTypeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFunctionTypeTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFunctionTypeTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFunctionTypeTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionTypeTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFunctionTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFunctionTypeTable** objectArray = (SgFunctionTypeTable**) &(SgFunctionTypeTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFunctionTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFunctionTypeTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionTypeTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFunctionTypeTable);

     return memory;
   }



/* #line 58952 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDeclarationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeclarationStatement** objectArray = (SgDeclarationStatement**) &(SgDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDeclarationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDeclarationStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDeclarationStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeclarationStatement** objectArray = (SgDeclarationStatement**) &(SgDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDeclarationStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDeclarationStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDeclarationStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeclarationStatement** objectArray = (SgDeclarationStatement**) &(SgDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDeclarationStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDeclarationStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDeclarationStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeclarationStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDeclarationStatement** objectArray = (SgDeclarationStatement**) &(SgDeclarationStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDeclarationStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeclarationStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDeclarationStatement);

     return memory;
   }



/* #line 59184 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFunctionParameterList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFunctionParameterList objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionParameterList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionParameterList** objectArray = (SgFunctionParameterList**) &(SgFunctionParameterList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionParameterList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFunctionParameterList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFunctionParameterList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFunctionParameterList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFunctionParameterList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionParameterList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFunctionParameterList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionParameterList objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionParameterList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionParameterList** objectArray = (SgFunctionParameterList**) &(SgFunctionParameterList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionParameterList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionParameterList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFunctionParameterList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFunctionParameterList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFunctionParameterList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionParameterList objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionParameterList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionParameterList** objectArray = (SgFunctionParameterList**) &(SgFunctionParameterList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFunctionParameterList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFunctionParameterList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFunctionParameterList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionParameterList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFunctionParameterList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFunctionParameterList** objectArray = (SgFunctionParameterList**) &(SgFunctionParameterList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFunctionParameterList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFunctionParameterList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionParameterList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFunctionParameterList);

     return memory;
   }



/* #line 59416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgVariableDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgVariableDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgVariableDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariableDeclaration** objectArray = (SgVariableDeclaration**) &(SgVariableDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVariableDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgVariableDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgVariableDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgVariableDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgVariableDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVariableDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgVariableDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVariableDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgVariableDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariableDeclaration** objectArray = (SgVariableDeclaration**) &(SgVariableDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVariableDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVariableDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgVariableDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgVariableDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgVariableDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVariableDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgVariableDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariableDeclaration** objectArray = (SgVariableDeclaration**) &(SgVariableDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgVariableDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgVariableDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgVariableDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVariableDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgVariableDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgVariableDeclaration** objectArray = (SgVariableDeclaration**) &(SgVariableDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgVariableDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgVariableDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVariableDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgVariableDeclaration);

     return memory;
   }



/* #line 59648 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateVariableDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateVariableDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateVariableDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateVariableDeclaration** objectArray = (SgTemplateVariableDeclaration**) &(SgTemplateVariableDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateVariableDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateVariableDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateVariableDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateVariableDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateVariableDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateVariableDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateVariableDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateVariableDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateVariableDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateVariableDeclaration** objectArray = (SgTemplateVariableDeclaration**) &(SgTemplateVariableDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateVariableDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateVariableDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateVariableDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateVariableDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateVariableDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateVariableDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateVariableDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateVariableDeclaration** objectArray = (SgTemplateVariableDeclaration**) &(SgTemplateVariableDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateVariableDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateVariableDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateVariableDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateVariableDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateVariableDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateVariableDeclaration** objectArray = (SgTemplateVariableDeclaration**) &(SgTemplateVariableDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateVariableDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateVariableDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateVariableDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateVariableDeclaration);

     return memory;
   }



/* #line 59880 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateVariableInstantiation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateVariableInstantiation objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateVariableInstantiation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateVariableInstantiation** objectArray = (SgTemplateVariableInstantiation**) &(SgTemplateVariableInstantiation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateVariableInstantiation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateVariableInstantiation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateVariableInstantiation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateVariableInstantiation*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateVariableInstantiation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateVariableInstantiation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateVariableInstantiation::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateVariableInstantiation objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateVariableInstantiation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateVariableInstantiation** objectArray = (SgTemplateVariableInstantiation**) &(SgTemplateVariableInstantiation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateVariableInstantiation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateVariableInstantiation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateVariableInstantiation object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateVariableInstantiation object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateVariableInstantiation::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateVariableInstantiation objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateVariableInstantiation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateVariableInstantiation** objectArray = (SgTemplateVariableInstantiation**) &(SgTemplateVariableInstantiation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateVariableInstantiation::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateVariableInstantiation::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateVariableInstantiation found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateVariableInstantiation::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateVariableInstantiation::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateVariableInstantiation** objectArray = (SgTemplateVariableInstantiation**) &(SgTemplateVariableInstantiation::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateVariableInstantiation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateVariableInstantiation::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateVariableInstantiation::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateVariableInstantiation);

     return memory;
   }



/* #line 60112 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgVariableDefinition::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgVariableDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgVariableDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariableDefinition** objectArray = (SgVariableDefinition**) &(SgVariableDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVariableDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgVariableDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgVariableDefinition::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgVariableDefinition*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgVariableDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVariableDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgVariableDefinition::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVariableDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgVariableDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariableDefinition** objectArray = (SgVariableDefinition**) &(SgVariableDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVariableDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVariableDefinition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgVariableDefinition object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgVariableDefinition object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgVariableDefinition::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVariableDefinition objectArray [] = *(Memory_Block_List.begin());
     if (SgVariableDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariableDefinition** objectArray = (SgVariableDefinition**) &(SgVariableDefinition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgVariableDefinition::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgVariableDefinition::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgVariableDefinition found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVariableDefinition::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgVariableDefinition::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgVariableDefinition** objectArray = (SgVariableDefinition**) &(SgVariableDefinition::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgVariableDefinition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgVariableDefinition::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVariableDefinition::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgVariableDefinition);

     return memory;
   }



/* #line 60344 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgClinkageDeclarationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgClinkageDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgClinkageDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClinkageDeclarationStatement** objectArray = (SgClinkageDeclarationStatement**) &(SgClinkageDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClinkageDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgClinkageDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgClinkageDeclarationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgClinkageDeclarationStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgClinkageDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClinkageDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgClinkageDeclarationStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClinkageDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgClinkageDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClinkageDeclarationStatement** objectArray = (SgClinkageDeclarationStatement**) &(SgClinkageDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClinkageDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClinkageDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgClinkageDeclarationStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgClinkageDeclarationStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgClinkageDeclarationStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClinkageDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgClinkageDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClinkageDeclarationStatement** objectArray = (SgClinkageDeclarationStatement**) &(SgClinkageDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgClinkageDeclarationStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgClinkageDeclarationStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgClinkageDeclarationStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClinkageDeclarationStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgClinkageDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgClinkageDeclarationStatement** objectArray = (SgClinkageDeclarationStatement**) &(SgClinkageDeclarationStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgClinkageDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgClinkageDeclarationStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClinkageDeclarationStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgClinkageDeclarationStatement);

     return memory;
   }



/* #line 60576 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgClinkageStartStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgClinkageStartStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgClinkageStartStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClinkageStartStatement** objectArray = (SgClinkageStartStatement**) &(SgClinkageStartStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClinkageStartStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgClinkageStartStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgClinkageStartStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgClinkageStartStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgClinkageStartStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClinkageStartStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgClinkageStartStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClinkageStartStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgClinkageStartStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClinkageStartStatement** objectArray = (SgClinkageStartStatement**) &(SgClinkageStartStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClinkageStartStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClinkageStartStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgClinkageStartStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgClinkageStartStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgClinkageStartStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClinkageStartStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgClinkageStartStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClinkageStartStatement** objectArray = (SgClinkageStartStatement**) &(SgClinkageStartStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgClinkageStartStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgClinkageStartStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgClinkageStartStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClinkageStartStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgClinkageStartStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgClinkageStartStatement** objectArray = (SgClinkageStartStatement**) &(SgClinkageStartStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgClinkageStartStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgClinkageStartStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClinkageStartStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgClinkageStartStatement);

     return memory;
   }



/* #line 60808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgClinkageEndStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgClinkageEndStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgClinkageEndStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClinkageEndStatement** objectArray = (SgClinkageEndStatement**) &(SgClinkageEndStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClinkageEndStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgClinkageEndStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgClinkageEndStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgClinkageEndStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgClinkageEndStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClinkageEndStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgClinkageEndStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClinkageEndStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgClinkageEndStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClinkageEndStatement** objectArray = (SgClinkageEndStatement**) &(SgClinkageEndStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClinkageEndStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClinkageEndStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgClinkageEndStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgClinkageEndStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgClinkageEndStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClinkageEndStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgClinkageEndStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClinkageEndStatement** objectArray = (SgClinkageEndStatement**) &(SgClinkageEndStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgClinkageEndStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgClinkageEndStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgClinkageEndStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClinkageEndStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgClinkageEndStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgClinkageEndStatement** objectArray = (SgClinkageEndStatement**) &(SgClinkageEndStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgClinkageEndStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgClinkageEndStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClinkageEndStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgClinkageEndStatement);

     return memory;
   }



/* #line 61040 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgEnumDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgEnumDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumDeclaration** objectArray = (SgEnumDeclaration**) &(SgEnumDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEnumDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgEnumDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgEnumDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgEnumDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgEnumDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEnumDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgEnumDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEnumDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumDeclaration** objectArray = (SgEnumDeclaration**) &(SgEnumDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEnumDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEnumDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgEnumDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgEnumDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgEnumDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEnumDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumDeclaration** objectArray = (SgEnumDeclaration**) &(SgEnumDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgEnumDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgEnumDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgEnumDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEnumDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgEnumDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgEnumDeclaration** objectArray = (SgEnumDeclaration**) &(SgEnumDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgEnumDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgEnumDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEnumDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgEnumDeclaration);

     return memory;
   }



/* #line 61272 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStmt** objectArray = (SgAsmStmt**) &(SgAsmStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStmt** objectArray = (SgAsmStmt**) &(SgAsmStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStmt** objectArray = (SgAsmStmt**) &(SgAsmStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmStmt** objectArray = (SgAsmStmt**) &(SgAsmStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmStmt);

     return memory;
   }



/* #line 61504 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAttributeSpecificationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAttributeSpecificationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAttributeSpecificationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAttributeSpecificationStatement** objectArray = (SgAttributeSpecificationStatement**) &(SgAttributeSpecificationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAttributeSpecificationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAttributeSpecificationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAttributeSpecificationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAttributeSpecificationStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAttributeSpecificationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAttributeSpecificationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAttributeSpecificationStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAttributeSpecificationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAttributeSpecificationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAttributeSpecificationStatement** objectArray = (SgAttributeSpecificationStatement**) &(SgAttributeSpecificationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAttributeSpecificationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAttributeSpecificationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAttributeSpecificationStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAttributeSpecificationStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAttributeSpecificationStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAttributeSpecificationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAttributeSpecificationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAttributeSpecificationStatement** objectArray = (SgAttributeSpecificationStatement**) &(SgAttributeSpecificationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAttributeSpecificationStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAttributeSpecificationStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAttributeSpecificationStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAttributeSpecificationStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAttributeSpecificationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAttributeSpecificationStatement** objectArray = (SgAttributeSpecificationStatement**) &(SgAttributeSpecificationStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAttributeSpecificationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAttributeSpecificationStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAttributeSpecificationStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAttributeSpecificationStatement);

     return memory;
   }



/* #line 61736 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFormatStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFormatStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgFormatStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFormatStatement** objectArray = (SgFormatStatement**) &(SgFormatStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFormatStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFormatStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFormatStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFormatStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFormatStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFormatStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFormatStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFormatStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgFormatStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFormatStatement** objectArray = (SgFormatStatement**) &(SgFormatStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFormatStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFormatStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFormatStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFormatStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFormatStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFormatStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgFormatStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFormatStatement** objectArray = (SgFormatStatement**) &(SgFormatStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFormatStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFormatStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFormatStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFormatStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFormatStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFormatStatement** objectArray = (SgFormatStatement**) &(SgFormatStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFormatStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFormatStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFormatStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFormatStatement);

     return memory;
   }



/* #line 61968 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateDeclaration** objectArray = (SgTemplateDeclaration**) &(SgTemplateDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateDeclaration** objectArray = (SgTemplateDeclaration**) &(SgTemplateDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateDeclaration** objectArray = (SgTemplateDeclaration**) &(SgTemplateDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateDeclaration** objectArray = (SgTemplateDeclaration**) &(SgTemplateDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateDeclaration);

     return memory;
   }



/* #line 62200 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateInstantiationDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateInstantiationDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationDirectiveStatement** objectArray = (SgTemplateInstantiationDirectiveStatement**) &(SgTemplateInstantiationDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateInstantiationDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateInstantiationDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateInstantiationDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateInstantiationDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateInstantiationDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateInstantiationDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateInstantiationDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateInstantiationDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationDirectiveStatement** objectArray = (SgTemplateInstantiationDirectiveStatement**) &(SgTemplateInstantiationDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateInstantiationDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateInstantiationDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateInstantiationDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateInstantiationDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateInstantiationDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateInstantiationDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationDirectiveStatement** objectArray = (SgTemplateInstantiationDirectiveStatement**) &(SgTemplateInstantiationDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateInstantiationDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateInstantiationDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateInstantiationDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateInstantiationDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateInstantiationDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateInstantiationDirectiveStatement** objectArray = (SgTemplateInstantiationDirectiveStatement**) &(SgTemplateInstantiationDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateInstantiationDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateInstantiationDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateInstantiationDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateInstantiationDirectiveStatement);

     return memory;
   }



/* #line 62432 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUseStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUseStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUseStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUseStatement** objectArray = (SgUseStatement**) &(SgUseStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUseStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUseStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUseStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUseStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUseStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUseStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUseStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUseStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUseStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUseStatement** objectArray = (SgUseStatement**) &(SgUseStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUseStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUseStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUseStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUseStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUseStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUseStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUseStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUseStatement** objectArray = (SgUseStatement**) &(SgUseStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUseStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUseStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUseStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUseStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUseStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUseStatement** objectArray = (SgUseStatement**) &(SgUseStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUseStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUseStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUseStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUseStatement);

     return memory;
   }



/* #line 62664 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgParameterStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgParameterStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgParameterStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgParameterStatement** objectArray = (SgParameterStatement**) &(SgParameterStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgParameterStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgParameterStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgParameterStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgParameterStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgParameterStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgParameterStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgParameterStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgParameterStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgParameterStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgParameterStatement** objectArray = (SgParameterStatement**) &(SgParameterStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgParameterStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgParameterStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgParameterStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgParameterStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgParameterStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgParameterStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgParameterStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgParameterStatement** objectArray = (SgParameterStatement**) &(SgParameterStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgParameterStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgParameterStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgParameterStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgParameterStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgParameterStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgParameterStatement** objectArray = (SgParameterStatement**) &(SgParameterStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgParameterStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgParameterStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgParameterStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgParameterStatement);

     return memory;
   }



/* #line 62896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNamespaceDeclarationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNamespaceDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNamespaceDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamespaceDeclarationStatement** objectArray = (SgNamespaceDeclarationStatement**) &(SgNamespaceDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNamespaceDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNamespaceDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNamespaceDeclarationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNamespaceDeclarationStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNamespaceDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNamespaceDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNamespaceDeclarationStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNamespaceDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNamespaceDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamespaceDeclarationStatement** objectArray = (SgNamespaceDeclarationStatement**) &(SgNamespaceDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNamespaceDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNamespaceDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNamespaceDeclarationStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNamespaceDeclarationStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNamespaceDeclarationStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNamespaceDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNamespaceDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamespaceDeclarationStatement** objectArray = (SgNamespaceDeclarationStatement**) &(SgNamespaceDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNamespaceDeclarationStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNamespaceDeclarationStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNamespaceDeclarationStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNamespaceDeclarationStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNamespaceDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNamespaceDeclarationStatement** objectArray = (SgNamespaceDeclarationStatement**) &(SgNamespaceDeclarationStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNamespaceDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNamespaceDeclarationStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNamespaceDeclarationStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNamespaceDeclarationStatement);

     return memory;
   }



/* #line 63128 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgEquivalenceStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgEquivalenceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEquivalenceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEquivalenceStatement** objectArray = (SgEquivalenceStatement**) &(SgEquivalenceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEquivalenceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgEquivalenceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgEquivalenceStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgEquivalenceStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgEquivalenceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEquivalenceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgEquivalenceStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEquivalenceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEquivalenceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEquivalenceStatement** objectArray = (SgEquivalenceStatement**) &(SgEquivalenceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEquivalenceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEquivalenceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgEquivalenceStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgEquivalenceStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgEquivalenceStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEquivalenceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEquivalenceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEquivalenceStatement** objectArray = (SgEquivalenceStatement**) &(SgEquivalenceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgEquivalenceStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgEquivalenceStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgEquivalenceStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEquivalenceStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgEquivalenceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgEquivalenceStatement** objectArray = (SgEquivalenceStatement**) &(SgEquivalenceStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgEquivalenceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgEquivalenceStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEquivalenceStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgEquivalenceStatement);

     return memory;
   }



/* #line 63360 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgInterfaceStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgInterfaceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgInterfaceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInterfaceStatement** objectArray = (SgInterfaceStatement**) &(SgInterfaceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgInterfaceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgInterfaceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgInterfaceStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgInterfaceStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgInterfaceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgInterfaceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgInterfaceStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgInterfaceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgInterfaceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInterfaceStatement** objectArray = (SgInterfaceStatement**) &(SgInterfaceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgInterfaceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgInterfaceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgInterfaceStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgInterfaceStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgInterfaceStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgInterfaceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgInterfaceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInterfaceStatement** objectArray = (SgInterfaceStatement**) &(SgInterfaceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgInterfaceStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgInterfaceStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgInterfaceStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgInterfaceStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgInterfaceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgInterfaceStatement** objectArray = (SgInterfaceStatement**) &(SgInterfaceStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgInterfaceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgInterfaceStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgInterfaceStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgInterfaceStatement);

     return memory;
   }



/* #line 63592 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNamespaceAliasDeclarationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNamespaceAliasDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNamespaceAliasDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamespaceAliasDeclarationStatement** objectArray = (SgNamespaceAliasDeclarationStatement**) &(SgNamespaceAliasDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNamespaceAliasDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNamespaceAliasDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNamespaceAliasDeclarationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNamespaceAliasDeclarationStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNamespaceAliasDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNamespaceAliasDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNamespaceAliasDeclarationStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNamespaceAliasDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNamespaceAliasDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamespaceAliasDeclarationStatement** objectArray = (SgNamespaceAliasDeclarationStatement**) &(SgNamespaceAliasDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNamespaceAliasDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNamespaceAliasDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNamespaceAliasDeclarationStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNamespaceAliasDeclarationStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNamespaceAliasDeclarationStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNamespaceAliasDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNamespaceAliasDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamespaceAliasDeclarationStatement** objectArray = (SgNamespaceAliasDeclarationStatement**) &(SgNamespaceAliasDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNamespaceAliasDeclarationStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNamespaceAliasDeclarationStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNamespaceAliasDeclarationStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNamespaceAliasDeclarationStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNamespaceAliasDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNamespaceAliasDeclarationStatement** objectArray = (SgNamespaceAliasDeclarationStatement**) &(SgNamespaceAliasDeclarationStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNamespaceAliasDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNamespaceAliasDeclarationStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNamespaceAliasDeclarationStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNamespaceAliasDeclarationStatement);

     return memory;
   }



/* #line 63824 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCommonBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCommonBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgCommonBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCommonBlock** objectArray = (SgCommonBlock**) &(SgCommonBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCommonBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCommonBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCommonBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCommonBlock*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCommonBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCommonBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCommonBlock::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCommonBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgCommonBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCommonBlock** objectArray = (SgCommonBlock**) &(SgCommonBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCommonBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCommonBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCommonBlock object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCommonBlock object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCommonBlock::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCommonBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgCommonBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCommonBlock** objectArray = (SgCommonBlock**) &(SgCommonBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCommonBlock::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCommonBlock::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCommonBlock found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCommonBlock::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCommonBlock::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCommonBlock** objectArray = (SgCommonBlock**) &(SgCommonBlock::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCommonBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCommonBlock::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCommonBlock::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCommonBlock);

     return memory;
   }



/* #line 64056 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypedefDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypedefDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTypedefDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypedefDeclaration** objectArray = (SgTypedefDeclaration**) &(SgTypedefDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypedefDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypedefDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypedefDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypedefDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypedefDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypedefDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypedefDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypedefDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTypedefDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypedefDeclaration** objectArray = (SgTypedefDeclaration**) &(SgTypedefDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypedefDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypedefDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypedefDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypedefDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypedefDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypedefDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTypedefDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypedefDeclaration** objectArray = (SgTypedefDeclaration**) &(SgTypedefDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypedefDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypedefDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypedefDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypedefDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypedefDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypedefDeclaration** objectArray = (SgTypedefDeclaration**) &(SgTypedefDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypedefDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypedefDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypedefDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypedefDeclaration);

     return memory;
   }



/* #line 64288 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateTypedefDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateTypedefDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateTypedefDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateTypedefDeclaration** objectArray = (SgTemplateTypedefDeclaration**) &(SgTemplateTypedefDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateTypedefDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateTypedefDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateTypedefDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateTypedefDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateTypedefDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateTypedefDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateTypedefDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateTypedefDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateTypedefDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateTypedefDeclaration** objectArray = (SgTemplateTypedefDeclaration**) &(SgTemplateTypedefDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateTypedefDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateTypedefDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateTypedefDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateTypedefDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateTypedefDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateTypedefDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateTypedefDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateTypedefDeclaration** objectArray = (SgTemplateTypedefDeclaration**) &(SgTemplateTypedefDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateTypedefDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateTypedefDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateTypedefDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateTypedefDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateTypedefDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateTypedefDeclaration** objectArray = (SgTemplateTypedefDeclaration**) &(SgTemplateTypedefDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateTypedefDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateTypedefDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateTypedefDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateTypedefDeclaration);

     return memory;
   }



/* #line 64520 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateInstantiationTypedefDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateInstantiationTypedefDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationTypedefDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationTypedefDeclaration** objectArray = (SgTemplateInstantiationTypedefDeclaration**) &(SgTemplateInstantiationTypedefDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateInstantiationTypedefDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateInstantiationTypedefDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateInstantiationTypedefDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateInstantiationTypedefDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateInstantiationTypedefDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateInstantiationTypedefDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateInstantiationTypedefDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateInstantiationTypedefDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationTypedefDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationTypedefDeclaration** objectArray = (SgTemplateInstantiationTypedefDeclaration**) &(SgTemplateInstantiationTypedefDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateInstantiationTypedefDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateInstantiationTypedefDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateInstantiationTypedefDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateInstantiationTypedefDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateInstantiationTypedefDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateInstantiationTypedefDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationTypedefDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationTypedefDeclaration** objectArray = (SgTemplateInstantiationTypedefDeclaration**) &(SgTemplateInstantiationTypedefDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateInstantiationTypedefDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateInstantiationTypedefDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateInstantiationTypedefDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateInstantiationTypedefDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateInstantiationTypedefDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateInstantiationTypedefDeclaration** objectArray = (SgTemplateInstantiationTypedefDeclaration**) &(SgTemplateInstantiationTypedefDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateInstantiationTypedefDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateInstantiationTypedefDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateInstantiationTypedefDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateInstantiationTypedefDeclaration);

     return memory;
   }



/* #line 64752 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgStatementFunctionStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgStatementFunctionStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgStatementFunctionStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStatementFunctionStatement** objectArray = (SgStatementFunctionStatement**) &(SgStatementFunctionStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStatementFunctionStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgStatementFunctionStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgStatementFunctionStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgStatementFunctionStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgStatementFunctionStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStatementFunctionStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgStatementFunctionStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStatementFunctionStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgStatementFunctionStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStatementFunctionStatement** objectArray = (SgStatementFunctionStatement**) &(SgStatementFunctionStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStatementFunctionStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStatementFunctionStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgStatementFunctionStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgStatementFunctionStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgStatementFunctionStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStatementFunctionStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgStatementFunctionStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStatementFunctionStatement** objectArray = (SgStatementFunctionStatement**) &(SgStatementFunctionStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgStatementFunctionStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgStatementFunctionStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgStatementFunctionStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStatementFunctionStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgStatementFunctionStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgStatementFunctionStatement** objectArray = (SgStatementFunctionStatement**) &(SgStatementFunctionStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgStatementFunctionStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgStatementFunctionStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStatementFunctionStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgStatementFunctionStatement);

     return memory;
   }



/* #line 64984 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCtorInitializerList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCtorInitializerList objectArray [] = *(Memory_Block_List.begin());
     if (SgCtorInitializerList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCtorInitializerList** objectArray = (SgCtorInitializerList**) &(SgCtorInitializerList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCtorInitializerList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCtorInitializerList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCtorInitializerList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCtorInitializerList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCtorInitializerList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCtorInitializerList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCtorInitializerList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCtorInitializerList objectArray [] = *(Memory_Block_List.begin());
     if (SgCtorInitializerList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCtorInitializerList** objectArray = (SgCtorInitializerList**) &(SgCtorInitializerList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCtorInitializerList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCtorInitializerList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCtorInitializerList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCtorInitializerList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCtorInitializerList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCtorInitializerList objectArray [] = *(Memory_Block_List.begin());
     if (SgCtorInitializerList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCtorInitializerList** objectArray = (SgCtorInitializerList**) &(SgCtorInitializerList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCtorInitializerList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCtorInitializerList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCtorInitializerList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCtorInitializerList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCtorInitializerList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCtorInitializerList** objectArray = (SgCtorInitializerList**) &(SgCtorInitializerList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCtorInitializerList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCtorInitializerList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCtorInitializerList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCtorInitializerList);

     return memory;
   }



/* #line 65216 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPragmaDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPragmaDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgPragmaDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPragmaDeclaration** objectArray = (SgPragmaDeclaration**) &(SgPragmaDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPragmaDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPragmaDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPragmaDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPragmaDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPragmaDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPragmaDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPragmaDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPragmaDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgPragmaDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPragmaDeclaration** objectArray = (SgPragmaDeclaration**) &(SgPragmaDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPragmaDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPragmaDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPragmaDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPragmaDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPragmaDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPragmaDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgPragmaDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPragmaDeclaration** objectArray = (SgPragmaDeclaration**) &(SgPragmaDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPragmaDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPragmaDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPragmaDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPragmaDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPragmaDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPragmaDeclaration** objectArray = (SgPragmaDeclaration**) &(SgPragmaDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPragmaDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPragmaDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPragmaDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPragmaDeclaration);

     return memory;
   }



/* #line 65448 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUsingDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUsingDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUsingDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUsingDirectiveStatement** objectArray = (SgUsingDirectiveStatement**) &(SgUsingDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUsingDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUsingDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUsingDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUsingDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUsingDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUsingDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUsingDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUsingDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUsingDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUsingDirectiveStatement** objectArray = (SgUsingDirectiveStatement**) &(SgUsingDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUsingDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUsingDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUsingDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUsingDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUsingDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUsingDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUsingDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUsingDirectiveStatement** objectArray = (SgUsingDirectiveStatement**) &(SgUsingDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUsingDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUsingDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUsingDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUsingDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUsingDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUsingDirectiveStatement** objectArray = (SgUsingDirectiveStatement**) &(SgUsingDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUsingDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUsingDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUsingDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUsingDirectiveStatement);

     return memory;
   }



/* #line 65680 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgClassDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgClassDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgClassDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassDeclaration** objectArray = (SgClassDeclaration**) &(SgClassDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgClassDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgClassDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgClassDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgClassDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgClassDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgClassDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassDeclaration** objectArray = (SgClassDeclaration**) &(SgClassDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgClassDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgClassDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgClassDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgClassDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassDeclaration** objectArray = (SgClassDeclaration**) &(SgClassDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgClassDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgClassDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgClassDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgClassDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgClassDeclaration** objectArray = (SgClassDeclaration**) &(SgClassDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgClassDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgClassDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgClassDeclaration);

     return memory;
   }



/* #line 65912 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateClassDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateClassDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateClassDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateClassDeclaration** objectArray = (SgTemplateClassDeclaration**) &(SgTemplateClassDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateClassDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateClassDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateClassDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateClassDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateClassDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateClassDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateClassDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateClassDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateClassDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateClassDeclaration** objectArray = (SgTemplateClassDeclaration**) &(SgTemplateClassDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateClassDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateClassDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateClassDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateClassDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateClassDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateClassDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateClassDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateClassDeclaration** objectArray = (SgTemplateClassDeclaration**) &(SgTemplateClassDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateClassDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateClassDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateClassDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateClassDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateClassDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateClassDeclaration** objectArray = (SgTemplateClassDeclaration**) &(SgTemplateClassDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateClassDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateClassDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateClassDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateClassDeclaration);

     return memory;
   }



/* #line 66144 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateInstantiationDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateInstantiationDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationDecl** objectArray = (SgTemplateInstantiationDecl**) &(SgTemplateInstantiationDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateInstantiationDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateInstantiationDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateInstantiationDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateInstantiationDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateInstantiationDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateInstantiationDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateInstantiationDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateInstantiationDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationDecl** objectArray = (SgTemplateInstantiationDecl**) &(SgTemplateInstantiationDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateInstantiationDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateInstantiationDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateInstantiationDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateInstantiationDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateInstantiationDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateInstantiationDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationDecl** objectArray = (SgTemplateInstantiationDecl**) &(SgTemplateInstantiationDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateInstantiationDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateInstantiationDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateInstantiationDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateInstantiationDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateInstantiationDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateInstantiationDecl** objectArray = (SgTemplateInstantiationDecl**) &(SgTemplateInstantiationDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateInstantiationDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateInstantiationDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateInstantiationDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateInstantiationDecl);

     return memory;
   }



/* #line 66376 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDerivedTypeStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDerivedTypeStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDerivedTypeStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDerivedTypeStatement** objectArray = (SgDerivedTypeStatement**) &(SgDerivedTypeStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDerivedTypeStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDerivedTypeStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDerivedTypeStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDerivedTypeStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDerivedTypeStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDerivedTypeStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDerivedTypeStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDerivedTypeStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDerivedTypeStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDerivedTypeStatement** objectArray = (SgDerivedTypeStatement**) &(SgDerivedTypeStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDerivedTypeStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDerivedTypeStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDerivedTypeStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDerivedTypeStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDerivedTypeStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDerivedTypeStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDerivedTypeStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDerivedTypeStatement** objectArray = (SgDerivedTypeStatement**) &(SgDerivedTypeStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDerivedTypeStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDerivedTypeStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDerivedTypeStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDerivedTypeStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDerivedTypeStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDerivedTypeStatement** objectArray = (SgDerivedTypeStatement**) &(SgDerivedTypeStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDerivedTypeStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDerivedTypeStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDerivedTypeStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDerivedTypeStatement);

     return memory;
   }



/* #line 66608 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgModuleStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgModuleStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgModuleStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModuleStatement** objectArray = (SgModuleStatement**) &(SgModuleStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModuleStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgModuleStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgModuleStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgModuleStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgModuleStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModuleStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgModuleStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModuleStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgModuleStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModuleStatement** objectArray = (SgModuleStatement**) &(SgModuleStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModuleStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModuleStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgModuleStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgModuleStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgModuleStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModuleStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgModuleStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModuleStatement** objectArray = (SgModuleStatement**) &(SgModuleStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgModuleStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgModuleStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgModuleStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModuleStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgModuleStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgModuleStatement** objectArray = (SgModuleStatement**) &(SgModuleStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgModuleStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgModuleStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModuleStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgModuleStatement);

     return memory;
   }



/* #line 66840 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaPackageDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaPackageDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaPackageDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaPackageDeclaration** objectArray = (SgJavaPackageDeclaration**) &(SgJavaPackageDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaPackageDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaPackageDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaPackageDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaPackageDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaPackageDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaPackageDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaPackageDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaPackageDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaPackageDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaPackageDeclaration** objectArray = (SgJavaPackageDeclaration**) &(SgJavaPackageDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaPackageDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaPackageDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaPackageDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaPackageDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaPackageDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaPackageDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaPackageDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaPackageDeclaration** objectArray = (SgJavaPackageDeclaration**) &(SgJavaPackageDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaPackageDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaPackageDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaPackageDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaPackageDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaPackageDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaPackageDeclaration** objectArray = (SgJavaPackageDeclaration**) &(SgJavaPackageDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaPackageDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaPackageDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaPackageDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaPackageDeclaration);

     return memory;
   }



/* #line 67072 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJovialTableStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJovialTableStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialTableStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialTableStatement** objectArray = (SgJovialTableStatement**) &(SgJovialTableStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialTableStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJovialTableStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJovialTableStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJovialTableStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJovialTableStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialTableStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJovialTableStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialTableStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialTableStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialTableStatement** objectArray = (SgJovialTableStatement**) &(SgJovialTableStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialTableStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialTableStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJovialTableStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJovialTableStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJovialTableStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialTableStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialTableStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialTableStatement** objectArray = (SgJovialTableStatement**) &(SgJovialTableStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJovialTableStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJovialTableStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJovialTableStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialTableStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJovialTableStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJovialTableStatement** objectArray = (SgJovialTableStatement**) &(SgJovialTableStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJovialTableStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJovialTableStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialTableStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJovialTableStatement);

     return memory;
   }



/* #line 67304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgImplicitStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgImplicitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgImplicitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImplicitStatement** objectArray = (SgImplicitStatement**) &(SgImplicitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgImplicitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgImplicitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgImplicitStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgImplicitStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgImplicitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgImplicitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgImplicitStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgImplicitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgImplicitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImplicitStatement** objectArray = (SgImplicitStatement**) &(SgImplicitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgImplicitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgImplicitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgImplicitStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgImplicitStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgImplicitStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgImplicitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgImplicitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImplicitStatement** objectArray = (SgImplicitStatement**) &(SgImplicitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgImplicitStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgImplicitStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgImplicitStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgImplicitStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgImplicitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgImplicitStatement** objectArray = (SgImplicitStatement**) &(SgImplicitStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgImplicitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgImplicitStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgImplicitStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgImplicitStatement);

     return memory;
   }



/* #line 67536 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUsingDeclarationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUsingDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUsingDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUsingDeclarationStatement** objectArray = (SgUsingDeclarationStatement**) &(SgUsingDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUsingDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUsingDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUsingDeclarationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUsingDeclarationStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUsingDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUsingDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUsingDeclarationStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUsingDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUsingDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUsingDeclarationStatement** objectArray = (SgUsingDeclarationStatement**) &(SgUsingDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUsingDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUsingDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUsingDeclarationStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUsingDeclarationStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUsingDeclarationStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUsingDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUsingDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUsingDeclarationStatement** objectArray = (SgUsingDeclarationStatement**) &(SgUsingDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUsingDeclarationStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUsingDeclarationStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUsingDeclarationStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUsingDeclarationStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUsingDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUsingDeclarationStatement** objectArray = (SgUsingDeclarationStatement**) &(SgUsingDeclarationStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUsingDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUsingDeclarationStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUsingDeclarationStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUsingDeclarationStatement);

     return memory;
   }



/* #line 67768 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNamelistStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNamelistStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNamelistStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamelistStatement** objectArray = (SgNamelistStatement**) &(SgNamelistStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNamelistStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNamelistStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNamelistStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNamelistStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNamelistStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNamelistStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNamelistStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNamelistStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNamelistStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamelistStatement** objectArray = (SgNamelistStatement**) &(SgNamelistStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNamelistStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNamelistStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNamelistStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNamelistStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNamelistStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNamelistStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNamelistStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamelistStatement** objectArray = (SgNamelistStatement**) &(SgNamelistStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNamelistStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNamelistStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNamelistStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNamelistStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNamelistStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNamelistStatement** objectArray = (SgNamelistStatement**) &(SgNamelistStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNamelistStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNamelistStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNamelistStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNamelistStatement);

     return memory;
   }



/* #line 68000 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgImportStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgImportStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgImportStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImportStatement** objectArray = (SgImportStatement**) &(SgImportStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgImportStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgImportStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgImportStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgImportStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgImportStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgImportStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgImportStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgImportStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgImportStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImportStatement** objectArray = (SgImportStatement**) &(SgImportStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgImportStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgImportStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgImportStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgImportStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgImportStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgImportStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgImportStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImportStatement** objectArray = (SgImportStatement**) &(SgImportStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgImportStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgImportStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgImportStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgImportStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgImportStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgImportStatement** objectArray = (SgImportStatement**) &(SgImportStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgImportStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgImportStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgImportStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgImportStatement);

     return memory;
   }



/* #line 68232 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFunctionDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFunctionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionDeclaration** objectArray = (SgFunctionDeclaration**) &(SgFunctionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFunctionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFunctionDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFunctionDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFunctionDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionDeclaration** objectArray = (SgFunctionDeclaration**) &(SgFunctionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFunctionDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFunctionDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFunctionDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionDeclaration** objectArray = (SgFunctionDeclaration**) &(SgFunctionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFunctionDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFunctionDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFunctionDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFunctionDeclaration** objectArray = (SgFunctionDeclaration**) &(SgFunctionDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFunctionDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFunctionDeclaration);

     return memory;
   }



/* #line 68464 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateFunctionDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateFunctionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateFunctionDeclaration** objectArray = (SgTemplateFunctionDeclaration**) &(SgTemplateFunctionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateFunctionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateFunctionDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateFunctionDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateFunctionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateFunctionDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateFunctionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateFunctionDeclaration** objectArray = (SgTemplateFunctionDeclaration**) &(SgTemplateFunctionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateFunctionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateFunctionDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateFunctionDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateFunctionDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateFunctionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateFunctionDeclaration** objectArray = (SgTemplateFunctionDeclaration**) &(SgTemplateFunctionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateFunctionDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateFunctionDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateFunctionDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateFunctionDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateFunctionDeclaration** objectArray = (SgTemplateFunctionDeclaration**) &(SgTemplateFunctionDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateFunctionDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateFunctionDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateFunctionDeclaration);

     return memory;
   }



/* #line 68696 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMemberFunctionDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMemberFunctionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgMemberFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMemberFunctionDeclaration** objectArray = (SgMemberFunctionDeclaration**) &(SgMemberFunctionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMemberFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMemberFunctionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMemberFunctionDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMemberFunctionDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMemberFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMemberFunctionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMemberFunctionDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMemberFunctionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgMemberFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMemberFunctionDeclaration** objectArray = (SgMemberFunctionDeclaration**) &(SgMemberFunctionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMemberFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMemberFunctionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMemberFunctionDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMemberFunctionDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMemberFunctionDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMemberFunctionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgMemberFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMemberFunctionDeclaration** objectArray = (SgMemberFunctionDeclaration**) &(SgMemberFunctionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMemberFunctionDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMemberFunctionDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMemberFunctionDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMemberFunctionDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMemberFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMemberFunctionDeclaration** objectArray = (SgMemberFunctionDeclaration**) &(SgMemberFunctionDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMemberFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMemberFunctionDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMemberFunctionDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMemberFunctionDeclaration);

     return memory;
   }



/* #line 68928 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateMemberFunctionDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateMemberFunctionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateMemberFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateMemberFunctionDeclaration** objectArray = (SgTemplateMemberFunctionDeclaration**) &(SgTemplateMemberFunctionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateMemberFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateMemberFunctionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateMemberFunctionDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateMemberFunctionDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateMemberFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateMemberFunctionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateMemberFunctionDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateMemberFunctionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateMemberFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateMemberFunctionDeclaration** objectArray = (SgTemplateMemberFunctionDeclaration**) &(SgTemplateMemberFunctionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateMemberFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateMemberFunctionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateMemberFunctionDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateMemberFunctionDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateMemberFunctionDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateMemberFunctionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateMemberFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateMemberFunctionDeclaration** objectArray = (SgTemplateMemberFunctionDeclaration**) &(SgTemplateMemberFunctionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateMemberFunctionDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateMemberFunctionDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateMemberFunctionDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateMemberFunctionDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateMemberFunctionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateMemberFunctionDeclaration** objectArray = (SgTemplateMemberFunctionDeclaration**) &(SgTemplateMemberFunctionDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateMemberFunctionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateMemberFunctionDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateMemberFunctionDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateMemberFunctionDeclaration);

     return memory;
   }



/* #line 69160 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateInstantiationMemberFunctionDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateInstantiationMemberFunctionDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationMemberFunctionDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationMemberFunctionDecl** objectArray = (SgTemplateInstantiationMemberFunctionDecl**) &(SgTemplateInstantiationMemberFunctionDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateInstantiationMemberFunctionDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateInstantiationMemberFunctionDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateInstantiationMemberFunctionDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateInstantiationMemberFunctionDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateInstantiationMemberFunctionDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateInstantiationMemberFunctionDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateInstantiationMemberFunctionDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateInstantiationMemberFunctionDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationMemberFunctionDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationMemberFunctionDecl** objectArray = (SgTemplateInstantiationMemberFunctionDecl**) &(SgTemplateInstantiationMemberFunctionDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateInstantiationMemberFunctionDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateInstantiationMemberFunctionDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateInstantiationMemberFunctionDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateInstantiationMemberFunctionDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateInstantiationMemberFunctionDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateInstantiationMemberFunctionDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationMemberFunctionDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationMemberFunctionDecl** objectArray = (SgTemplateInstantiationMemberFunctionDecl**) &(SgTemplateInstantiationMemberFunctionDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateInstantiationMemberFunctionDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateInstantiationMemberFunctionDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateInstantiationMemberFunctionDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateInstantiationMemberFunctionDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateInstantiationMemberFunctionDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateInstantiationMemberFunctionDecl** objectArray = (SgTemplateInstantiationMemberFunctionDecl**) &(SgTemplateInstantiationMemberFunctionDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateInstantiationMemberFunctionDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateInstantiationMemberFunctionDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateInstantiationMemberFunctionDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateInstantiationMemberFunctionDecl);

     return memory;
   }



/* #line 69392 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateInstantiationFunctionDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateInstantiationFunctionDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationFunctionDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationFunctionDecl** objectArray = (SgTemplateInstantiationFunctionDecl**) &(SgTemplateInstantiationFunctionDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateInstantiationFunctionDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateInstantiationFunctionDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateInstantiationFunctionDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateInstantiationFunctionDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateInstantiationFunctionDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateInstantiationFunctionDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateInstantiationFunctionDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateInstantiationFunctionDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationFunctionDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationFunctionDecl** objectArray = (SgTemplateInstantiationFunctionDecl**) &(SgTemplateInstantiationFunctionDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateInstantiationFunctionDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateInstantiationFunctionDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateInstantiationFunctionDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateInstantiationFunctionDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateInstantiationFunctionDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateInstantiationFunctionDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateInstantiationFunctionDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateInstantiationFunctionDecl** objectArray = (SgTemplateInstantiationFunctionDecl**) &(SgTemplateInstantiationFunctionDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateInstantiationFunctionDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateInstantiationFunctionDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateInstantiationFunctionDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateInstantiationFunctionDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateInstantiationFunctionDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateInstantiationFunctionDecl** objectArray = (SgTemplateInstantiationFunctionDecl**) &(SgTemplateInstantiationFunctionDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateInstantiationFunctionDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateInstantiationFunctionDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateInstantiationFunctionDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateInstantiationFunctionDecl);

     return memory;
   }



/* #line 69624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgProgramHeaderStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgProgramHeaderStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgProgramHeaderStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgProgramHeaderStatement** objectArray = (SgProgramHeaderStatement**) &(SgProgramHeaderStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgProgramHeaderStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgProgramHeaderStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgProgramHeaderStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgProgramHeaderStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgProgramHeaderStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgProgramHeaderStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgProgramHeaderStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgProgramHeaderStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgProgramHeaderStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgProgramHeaderStatement** objectArray = (SgProgramHeaderStatement**) &(SgProgramHeaderStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgProgramHeaderStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgProgramHeaderStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgProgramHeaderStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgProgramHeaderStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgProgramHeaderStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgProgramHeaderStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgProgramHeaderStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgProgramHeaderStatement** objectArray = (SgProgramHeaderStatement**) &(SgProgramHeaderStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgProgramHeaderStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgProgramHeaderStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgProgramHeaderStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgProgramHeaderStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgProgramHeaderStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgProgramHeaderStatement** objectArray = (SgProgramHeaderStatement**) &(SgProgramHeaderStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgProgramHeaderStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgProgramHeaderStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgProgramHeaderStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgProgramHeaderStatement);

     return memory;
   }



/* #line 69856 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgProcedureHeaderStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgProcedureHeaderStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgProcedureHeaderStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgProcedureHeaderStatement** objectArray = (SgProcedureHeaderStatement**) &(SgProcedureHeaderStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgProcedureHeaderStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgProcedureHeaderStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgProcedureHeaderStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgProcedureHeaderStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgProcedureHeaderStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgProcedureHeaderStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgProcedureHeaderStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgProcedureHeaderStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgProcedureHeaderStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgProcedureHeaderStatement** objectArray = (SgProcedureHeaderStatement**) &(SgProcedureHeaderStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgProcedureHeaderStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgProcedureHeaderStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgProcedureHeaderStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgProcedureHeaderStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgProcedureHeaderStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgProcedureHeaderStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgProcedureHeaderStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgProcedureHeaderStatement** objectArray = (SgProcedureHeaderStatement**) &(SgProcedureHeaderStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgProcedureHeaderStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgProcedureHeaderStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgProcedureHeaderStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgProcedureHeaderStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgProcedureHeaderStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgProcedureHeaderStatement** objectArray = (SgProcedureHeaderStatement**) &(SgProcedureHeaderStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgProcedureHeaderStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgProcedureHeaderStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgProcedureHeaderStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgProcedureHeaderStatement);

     return memory;
   }



/* #line 70088 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgEntryStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgEntryStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEntryStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEntryStatement** objectArray = (SgEntryStatement**) &(SgEntryStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEntryStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgEntryStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgEntryStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgEntryStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgEntryStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEntryStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgEntryStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEntryStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEntryStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEntryStatement** objectArray = (SgEntryStatement**) &(SgEntryStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEntryStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEntryStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgEntryStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgEntryStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgEntryStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEntryStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEntryStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEntryStatement** objectArray = (SgEntryStatement**) &(SgEntryStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgEntryStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgEntryStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgEntryStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEntryStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgEntryStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgEntryStatement** objectArray = (SgEntryStatement**) &(SgEntryStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgEntryStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgEntryStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEntryStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgEntryStatement);

     return memory;
   }



/* #line 70320 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaEntryDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaEntryDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaEntryDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaEntryDecl** objectArray = (SgAdaEntryDecl**) &(SgAdaEntryDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaEntryDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaEntryDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaEntryDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaEntryDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaEntryDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaEntryDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaEntryDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaEntryDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaEntryDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaEntryDecl** objectArray = (SgAdaEntryDecl**) &(SgAdaEntryDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaEntryDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaEntryDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaEntryDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaEntryDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaEntryDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaEntryDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaEntryDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaEntryDecl** objectArray = (SgAdaEntryDecl**) &(SgAdaEntryDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaEntryDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaEntryDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaEntryDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaEntryDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaEntryDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaEntryDecl** objectArray = (SgAdaEntryDecl**) &(SgAdaEntryDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaEntryDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaEntryDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaEntryDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaEntryDecl);

     return memory;
   }



/* #line 70552 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaFunctionRenamingDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaFunctionRenamingDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFunctionRenamingDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFunctionRenamingDecl** objectArray = (SgAdaFunctionRenamingDecl**) &(SgAdaFunctionRenamingDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaFunctionRenamingDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaFunctionRenamingDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaFunctionRenamingDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaFunctionRenamingDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaFunctionRenamingDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaFunctionRenamingDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaFunctionRenamingDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaFunctionRenamingDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFunctionRenamingDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFunctionRenamingDecl** objectArray = (SgAdaFunctionRenamingDecl**) &(SgAdaFunctionRenamingDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaFunctionRenamingDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaFunctionRenamingDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaFunctionRenamingDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaFunctionRenamingDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaFunctionRenamingDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaFunctionRenamingDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFunctionRenamingDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFunctionRenamingDecl** objectArray = (SgAdaFunctionRenamingDecl**) &(SgAdaFunctionRenamingDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaFunctionRenamingDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaFunctionRenamingDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaFunctionRenamingDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaFunctionRenamingDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaFunctionRenamingDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaFunctionRenamingDecl** objectArray = (SgAdaFunctionRenamingDecl**) &(SgAdaFunctionRenamingDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaFunctionRenamingDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaFunctionRenamingDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaFunctionRenamingDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaFunctionRenamingDecl);

     return memory;
   }



/* #line 70784 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgContainsStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgContainsStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgContainsStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgContainsStatement** objectArray = (SgContainsStatement**) &(SgContainsStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgContainsStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgContainsStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgContainsStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgContainsStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgContainsStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgContainsStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgContainsStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgContainsStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgContainsStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgContainsStatement** objectArray = (SgContainsStatement**) &(SgContainsStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgContainsStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgContainsStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgContainsStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgContainsStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgContainsStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgContainsStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgContainsStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgContainsStatement** objectArray = (SgContainsStatement**) &(SgContainsStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgContainsStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgContainsStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgContainsStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgContainsStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgContainsStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgContainsStatement** objectArray = (SgContainsStatement**) &(SgContainsStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgContainsStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgContainsStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgContainsStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgContainsStatement);

     return memory;
   }



/* #line 71016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgC_PreprocessorDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgC_PreprocessorDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgC_PreprocessorDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgC_PreprocessorDirectiveStatement** objectArray = (SgC_PreprocessorDirectiveStatement**) &(SgC_PreprocessorDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgC_PreprocessorDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgC_PreprocessorDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgC_PreprocessorDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgC_PreprocessorDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgC_PreprocessorDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgC_PreprocessorDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgC_PreprocessorDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgC_PreprocessorDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgC_PreprocessorDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgC_PreprocessorDirectiveStatement** objectArray = (SgC_PreprocessorDirectiveStatement**) &(SgC_PreprocessorDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgC_PreprocessorDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgC_PreprocessorDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgC_PreprocessorDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgC_PreprocessorDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgC_PreprocessorDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgC_PreprocessorDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgC_PreprocessorDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgC_PreprocessorDirectiveStatement** objectArray = (SgC_PreprocessorDirectiveStatement**) &(SgC_PreprocessorDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgC_PreprocessorDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgC_PreprocessorDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgC_PreprocessorDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgC_PreprocessorDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgC_PreprocessorDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgC_PreprocessorDirectiveStatement** objectArray = (SgC_PreprocessorDirectiveStatement**) &(SgC_PreprocessorDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgC_PreprocessorDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgC_PreprocessorDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgC_PreprocessorDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgC_PreprocessorDirectiveStatement);

     return memory;
   }



/* #line 71248 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIncludeDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIncludeDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIncludeDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncludeDirectiveStatement** objectArray = (SgIncludeDirectiveStatement**) &(SgIncludeDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIncludeDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIncludeDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIncludeDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIncludeDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIncludeDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIncludeDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIncludeDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIncludeDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIncludeDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncludeDirectiveStatement** objectArray = (SgIncludeDirectiveStatement**) &(SgIncludeDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIncludeDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIncludeDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIncludeDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIncludeDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIncludeDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIncludeDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIncludeDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncludeDirectiveStatement** objectArray = (SgIncludeDirectiveStatement**) &(SgIncludeDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIncludeDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIncludeDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIncludeDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIncludeDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIncludeDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIncludeDirectiveStatement** objectArray = (SgIncludeDirectiveStatement**) &(SgIncludeDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIncludeDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIncludeDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIncludeDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIncludeDirectiveStatement);

     return memory;
   }



/* #line 71480 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDefineDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDefineDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDefineDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDefineDirectiveStatement** objectArray = (SgDefineDirectiveStatement**) &(SgDefineDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDefineDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDefineDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDefineDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDefineDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDefineDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDefineDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDefineDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDefineDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDefineDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDefineDirectiveStatement** objectArray = (SgDefineDirectiveStatement**) &(SgDefineDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDefineDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDefineDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDefineDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDefineDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDefineDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDefineDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDefineDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDefineDirectiveStatement** objectArray = (SgDefineDirectiveStatement**) &(SgDefineDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDefineDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDefineDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDefineDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDefineDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDefineDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDefineDirectiveStatement** objectArray = (SgDefineDirectiveStatement**) &(SgDefineDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDefineDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDefineDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDefineDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDefineDirectiveStatement);

     return memory;
   }



/* #line 71712 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUndefDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUndefDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUndefDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUndefDirectiveStatement** objectArray = (SgUndefDirectiveStatement**) &(SgUndefDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUndefDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUndefDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUndefDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUndefDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUndefDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUndefDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUndefDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUndefDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUndefDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUndefDirectiveStatement** objectArray = (SgUndefDirectiveStatement**) &(SgUndefDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUndefDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUndefDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUndefDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUndefDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUndefDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUndefDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUndefDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUndefDirectiveStatement** objectArray = (SgUndefDirectiveStatement**) &(SgUndefDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUndefDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUndefDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUndefDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUndefDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUndefDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUndefDirectiveStatement** objectArray = (SgUndefDirectiveStatement**) &(SgUndefDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUndefDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUndefDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUndefDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUndefDirectiveStatement);

     return memory;
   }



/* #line 71944 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIfdefDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIfdefDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIfdefDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIfdefDirectiveStatement** objectArray = (SgIfdefDirectiveStatement**) &(SgIfdefDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIfdefDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIfdefDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIfdefDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIfdefDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIfdefDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIfdefDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIfdefDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIfdefDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIfdefDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIfdefDirectiveStatement** objectArray = (SgIfdefDirectiveStatement**) &(SgIfdefDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIfdefDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIfdefDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIfdefDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIfdefDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIfdefDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIfdefDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIfdefDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIfdefDirectiveStatement** objectArray = (SgIfdefDirectiveStatement**) &(SgIfdefDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIfdefDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIfdefDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIfdefDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIfdefDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIfdefDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIfdefDirectiveStatement** objectArray = (SgIfdefDirectiveStatement**) &(SgIfdefDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIfdefDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIfdefDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIfdefDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIfdefDirectiveStatement);

     return memory;
   }



/* #line 72176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIfndefDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIfndefDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIfndefDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIfndefDirectiveStatement** objectArray = (SgIfndefDirectiveStatement**) &(SgIfndefDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIfndefDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIfndefDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIfndefDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIfndefDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIfndefDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIfndefDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIfndefDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIfndefDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIfndefDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIfndefDirectiveStatement** objectArray = (SgIfndefDirectiveStatement**) &(SgIfndefDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIfndefDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIfndefDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIfndefDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIfndefDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIfndefDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIfndefDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIfndefDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIfndefDirectiveStatement** objectArray = (SgIfndefDirectiveStatement**) &(SgIfndefDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIfndefDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIfndefDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIfndefDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIfndefDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIfndefDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIfndefDirectiveStatement** objectArray = (SgIfndefDirectiveStatement**) &(SgIfndefDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIfndefDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIfndefDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIfndefDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIfndefDirectiveStatement);

     return memory;
   }



/* #line 72408 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIfDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIfDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIfDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIfDirectiveStatement** objectArray = (SgIfDirectiveStatement**) &(SgIfDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIfDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIfDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIfDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIfDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIfDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIfDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIfDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIfDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIfDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIfDirectiveStatement** objectArray = (SgIfDirectiveStatement**) &(SgIfDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIfDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIfDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIfDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIfDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIfDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIfDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIfDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIfDirectiveStatement** objectArray = (SgIfDirectiveStatement**) &(SgIfDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIfDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIfDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIfDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIfDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIfDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIfDirectiveStatement** objectArray = (SgIfDirectiveStatement**) &(SgIfDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIfDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIfDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIfDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIfDirectiveStatement);

     return memory;
   }



/* #line 72640 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDeadIfDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDeadIfDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDeadIfDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeadIfDirectiveStatement** objectArray = (SgDeadIfDirectiveStatement**) &(SgDeadIfDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeadIfDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDeadIfDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDeadIfDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDeadIfDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDeadIfDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeadIfDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDeadIfDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeadIfDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDeadIfDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeadIfDirectiveStatement** objectArray = (SgDeadIfDirectiveStatement**) &(SgDeadIfDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeadIfDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeadIfDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDeadIfDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDeadIfDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDeadIfDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeadIfDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDeadIfDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeadIfDirectiveStatement** objectArray = (SgDeadIfDirectiveStatement**) &(SgDeadIfDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDeadIfDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDeadIfDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDeadIfDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeadIfDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDeadIfDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDeadIfDirectiveStatement** objectArray = (SgDeadIfDirectiveStatement**) &(SgDeadIfDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDeadIfDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDeadIfDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeadIfDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDeadIfDirectiveStatement);

     return memory;
   }



/* #line 72872 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgElseDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgElseDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgElseDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElseDirectiveStatement** objectArray = (SgElseDirectiveStatement**) &(SgElseDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElseDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgElseDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgElseDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgElseDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgElseDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElseDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgElseDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElseDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgElseDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElseDirectiveStatement** objectArray = (SgElseDirectiveStatement**) &(SgElseDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElseDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElseDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgElseDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgElseDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgElseDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElseDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgElseDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElseDirectiveStatement** objectArray = (SgElseDirectiveStatement**) &(SgElseDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgElseDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgElseDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgElseDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElseDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgElseDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgElseDirectiveStatement** objectArray = (SgElseDirectiveStatement**) &(SgElseDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgElseDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgElseDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElseDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgElseDirectiveStatement);

     return memory;
   }



/* #line 73104 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgElseifDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgElseifDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgElseifDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElseifDirectiveStatement** objectArray = (SgElseifDirectiveStatement**) &(SgElseifDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElseifDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgElseifDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgElseifDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgElseifDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgElseifDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElseifDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgElseifDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElseifDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgElseifDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElseifDirectiveStatement** objectArray = (SgElseifDirectiveStatement**) &(SgElseifDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElseifDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElseifDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgElseifDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgElseifDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgElseifDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElseifDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgElseifDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElseifDirectiveStatement** objectArray = (SgElseifDirectiveStatement**) &(SgElseifDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgElseifDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgElseifDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgElseifDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElseifDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgElseifDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgElseifDirectiveStatement** objectArray = (SgElseifDirectiveStatement**) &(SgElseifDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgElseifDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgElseifDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElseifDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgElseifDirectiveStatement);

     return memory;
   }



/* #line 73336 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgEndifDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgEndifDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEndifDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEndifDirectiveStatement** objectArray = (SgEndifDirectiveStatement**) &(SgEndifDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEndifDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgEndifDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgEndifDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgEndifDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgEndifDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEndifDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgEndifDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEndifDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEndifDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEndifDirectiveStatement** objectArray = (SgEndifDirectiveStatement**) &(SgEndifDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEndifDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEndifDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgEndifDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgEndifDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgEndifDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEndifDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEndifDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEndifDirectiveStatement** objectArray = (SgEndifDirectiveStatement**) &(SgEndifDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgEndifDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgEndifDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgEndifDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEndifDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgEndifDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgEndifDirectiveStatement** objectArray = (SgEndifDirectiveStatement**) &(SgEndifDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgEndifDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgEndifDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEndifDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgEndifDirectiveStatement);

     return memory;
   }



/* #line 73568 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLineDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLineDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgLineDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLineDirectiveStatement** objectArray = (SgLineDirectiveStatement**) &(SgLineDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLineDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLineDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLineDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLineDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLineDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLineDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLineDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLineDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgLineDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLineDirectiveStatement** objectArray = (SgLineDirectiveStatement**) &(SgLineDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLineDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLineDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLineDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLineDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLineDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLineDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgLineDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLineDirectiveStatement** objectArray = (SgLineDirectiveStatement**) &(SgLineDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLineDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLineDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLineDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLineDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLineDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLineDirectiveStatement** objectArray = (SgLineDirectiveStatement**) &(SgLineDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLineDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLineDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLineDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLineDirectiveStatement);

     return memory;
   }



/* #line 73800 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgWarningDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgWarningDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWarningDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWarningDirectiveStatement** objectArray = (SgWarningDirectiveStatement**) &(SgWarningDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWarningDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgWarningDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgWarningDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgWarningDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgWarningDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWarningDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgWarningDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWarningDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWarningDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWarningDirectiveStatement** objectArray = (SgWarningDirectiveStatement**) &(SgWarningDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWarningDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWarningDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgWarningDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgWarningDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgWarningDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWarningDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWarningDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWarningDirectiveStatement** objectArray = (SgWarningDirectiveStatement**) &(SgWarningDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgWarningDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgWarningDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgWarningDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWarningDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgWarningDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgWarningDirectiveStatement** objectArray = (SgWarningDirectiveStatement**) &(SgWarningDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgWarningDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgWarningDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWarningDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgWarningDirectiveStatement);

     return memory;
   }



/* #line 74032 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgErrorDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgErrorDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgErrorDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgErrorDirectiveStatement** objectArray = (SgErrorDirectiveStatement**) &(SgErrorDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgErrorDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgErrorDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgErrorDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgErrorDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgErrorDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgErrorDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgErrorDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgErrorDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgErrorDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgErrorDirectiveStatement** objectArray = (SgErrorDirectiveStatement**) &(SgErrorDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgErrorDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgErrorDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgErrorDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgErrorDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgErrorDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgErrorDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgErrorDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgErrorDirectiveStatement** objectArray = (SgErrorDirectiveStatement**) &(SgErrorDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgErrorDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgErrorDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgErrorDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgErrorDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgErrorDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgErrorDirectiveStatement** objectArray = (SgErrorDirectiveStatement**) &(SgErrorDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgErrorDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgErrorDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgErrorDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgErrorDirectiveStatement);

     return memory;
   }



/* #line 74264 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgEmptyDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgEmptyDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEmptyDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEmptyDirectiveStatement** objectArray = (SgEmptyDirectiveStatement**) &(SgEmptyDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEmptyDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgEmptyDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgEmptyDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgEmptyDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgEmptyDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEmptyDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgEmptyDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEmptyDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEmptyDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEmptyDirectiveStatement** objectArray = (SgEmptyDirectiveStatement**) &(SgEmptyDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEmptyDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEmptyDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgEmptyDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgEmptyDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgEmptyDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEmptyDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEmptyDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEmptyDirectiveStatement** objectArray = (SgEmptyDirectiveStatement**) &(SgEmptyDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgEmptyDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgEmptyDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgEmptyDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEmptyDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgEmptyDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgEmptyDirectiveStatement** objectArray = (SgEmptyDirectiveStatement**) &(SgEmptyDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgEmptyDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgEmptyDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEmptyDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgEmptyDirectiveStatement);

     return memory;
   }



/* #line 74496 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIncludeNextDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIncludeNextDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIncludeNextDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncludeNextDirectiveStatement** objectArray = (SgIncludeNextDirectiveStatement**) &(SgIncludeNextDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIncludeNextDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIncludeNextDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIncludeNextDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIncludeNextDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIncludeNextDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIncludeNextDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIncludeNextDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIncludeNextDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIncludeNextDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncludeNextDirectiveStatement** objectArray = (SgIncludeNextDirectiveStatement**) &(SgIncludeNextDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIncludeNextDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIncludeNextDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIncludeNextDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIncludeNextDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIncludeNextDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIncludeNextDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIncludeNextDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIncludeNextDirectiveStatement** objectArray = (SgIncludeNextDirectiveStatement**) &(SgIncludeNextDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIncludeNextDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIncludeNextDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIncludeNextDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIncludeNextDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIncludeNextDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIncludeNextDirectiveStatement** objectArray = (SgIncludeNextDirectiveStatement**) &(SgIncludeNextDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIncludeNextDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIncludeNextDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIncludeNextDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIncludeNextDirectiveStatement);

     return memory;
   }



/* #line 74728 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIdentDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIdentDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIdentDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIdentDirectiveStatement** objectArray = (SgIdentDirectiveStatement**) &(SgIdentDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIdentDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIdentDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIdentDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIdentDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIdentDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIdentDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIdentDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIdentDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIdentDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIdentDirectiveStatement** objectArray = (SgIdentDirectiveStatement**) &(SgIdentDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIdentDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIdentDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIdentDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIdentDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIdentDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIdentDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIdentDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIdentDirectiveStatement** objectArray = (SgIdentDirectiveStatement**) &(SgIdentDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIdentDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIdentDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIdentDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIdentDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIdentDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIdentDirectiveStatement** objectArray = (SgIdentDirectiveStatement**) &(SgIdentDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIdentDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIdentDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIdentDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIdentDirectiveStatement);

     return memory;
   }



/* #line 74960 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLinemarkerDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLinemarkerDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgLinemarkerDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLinemarkerDirectiveStatement** objectArray = (SgLinemarkerDirectiveStatement**) &(SgLinemarkerDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLinemarkerDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLinemarkerDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLinemarkerDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLinemarkerDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLinemarkerDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLinemarkerDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLinemarkerDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLinemarkerDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgLinemarkerDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLinemarkerDirectiveStatement** objectArray = (SgLinemarkerDirectiveStatement**) &(SgLinemarkerDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLinemarkerDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLinemarkerDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLinemarkerDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLinemarkerDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLinemarkerDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLinemarkerDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgLinemarkerDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLinemarkerDirectiveStatement** objectArray = (SgLinemarkerDirectiveStatement**) &(SgLinemarkerDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLinemarkerDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLinemarkerDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLinemarkerDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLinemarkerDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLinemarkerDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLinemarkerDirectiveStatement** objectArray = (SgLinemarkerDirectiveStatement**) &(SgLinemarkerDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLinemarkerDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLinemarkerDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLinemarkerDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLinemarkerDirectiveStatement);

     return memory;
   }



/* #line 75192 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpThreadprivateStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpThreadprivateStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpThreadprivateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpThreadprivateStatement** objectArray = (SgOmpThreadprivateStatement**) &(SgOmpThreadprivateStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpThreadprivateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpThreadprivateStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpThreadprivateStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpThreadprivateStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpThreadprivateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpThreadprivateStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpThreadprivateStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpThreadprivateStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpThreadprivateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpThreadprivateStatement** objectArray = (SgOmpThreadprivateStatement**) &(SgOmpThreadprivateStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpThreadprivateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpThreadprivateStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpThreadprivateStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpThreadprivateStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpThreadprivateStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpThreadprivateStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpThreadprivateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpThreadprivateStatement** objectArray = (SgOmpThreadprivateStatement**) &(SgOmpThreadprivateStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpThreadprivateStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpThreadprivateStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpThreadprivateStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpThreadprivateStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpThreadprivateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpThreadprivateStatement** objectArray = (SgOmpThreadprivateStatement**) &(SgOmpThreadprivateStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpThreadprivateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpThreadprivateStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpThreadprivateStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpThreadprivateStatement);

     return memory;
   }



/* #line 75424 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFortranIncludeLine::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFortranIncludeLine objectArray [] = *(Memory_Block_List.begin());
     if (SgFortranIncludeLine::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFortranIncludeLine** objectArray = (SgFortranIncludeLine**) &(SgFortranIncludeLine::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFortranIncludeLine::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFortranIncludeLine::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFortranIncludeLine::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFortranIncludeLine*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFortranIncludeLine::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFortranIncludeLine::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFortranIncludeLine::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFortranIncludeLine objectArray [] = *(Memory_Block_List.begin());
     if (SgFortranIncludeLine::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFortranIncludeLine** objectArray = (SgFortranIncludeLine**) &(SgFortranIncludeLine::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFortranIncludeLine::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFortranIncludeLine::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFortranIncludeLine object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFortranIncludeLine object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFortranIncludeLine::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFortranIncludeLine objectArray [] = *(Memory_Block_List.begin());
     if (SgFortranIncludeLine::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFortranIncludeLine** objectArray = (SgFortranIncludeLine**) &(SgFortranIncludeLine::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFortranIncludeLine::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFortranIncludeLine::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFortranIncludeLine found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFortranIncludeLine::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFortranIncludeLine::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFortranIncludeLine** objectArray = (SgFortranIncludeLine**) &(SgFortranIncludeLine::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFortranIncludeLine::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFortranIncludeLine::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFortranIncludeLine::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFortranIncludeLine);

     return memory;
   }



/* #line 75656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaImportStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaImportStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaImportStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaImportStatement** objectArray = (SgJavaImportStatement**) &(SgJavaImportStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaImportStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaImportStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaImportStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaImportStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaImportStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaImportStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaImportStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaImportStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaImportStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaImportStatement** objectArray = (SgJavaImportStatement**) &(SgJavaImportStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaImportStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaImportStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaImportStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaImportStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaImportStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaImportStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaImportStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaImportStatement** objectArray = (SgJavaImportStatement**) &(SgJavaImportStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaImportStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaImportStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaImportStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaImportStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaImportStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaImportStatement** objectArray = (SgJavaImportStatement**) &(SgJavaImportStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaImportStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaImportStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaImportStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaImportStatement);

     return memory;
   }



/* #line 75888 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaPackageStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaPackageStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaPackageStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaPackageStatement** objectArray = (SgJavaPackageStatement**) &(SgJavaPackageStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaPackageStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaPackageStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaPackageStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaPackageStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaPackageStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaPackageStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaPackageStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaPackageStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaPackageStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaPackageStatement** objectArray = (SgJavaPackageStatement**) &(SgJavaPackageStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaPackageStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaPackageStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaPackageStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaPackageStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaPackageStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaPackageStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaPackageStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaPackageStatement** objectArray = (SgJavaPackageStatement**) &(SgJavaPackageStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaPackageStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaPackageStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaPackageStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaPackageStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaPackageStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaPackageStatement** objectArray = (SgJavaPackageStatement**) &(SgJavaPackageStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaPackageStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaPackageStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaPackageStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaPackageStatement);

     return memory;
   }



/* #line 76120 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgStmtDeclarationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgStmtDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgStmtDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStmtDeclarationStatement** objectArray = (SgStmtDeclarationStatement**) &(SgStmtDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStmtDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgStmtDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgStmtDeclarationStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgStmtDeclarationStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgStmtDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStmtDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgStmtDeclarationStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStmtDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgStmtDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStmtDeclarationStatement** objectArray = (SgStmtDeclarationStatement**) &(SgStmtDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStmtDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStmtDeclarationStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgStmtDeclarationStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgStmtDeclarationStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgStmtDeclarationStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStmtDeclarationStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgStmtDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStmtDeclarationStatement** objectArray = (SgStmtDeclarationStatement**) &(SgStmtDeclarationStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgStmtDeclarationStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgStmtDeclarationStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgStmtDeclarationStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStmtDeclarationStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgStmtDeclarationStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgStmtDeclarationStatement** objectArray = (SgStmtDeclarationStatement**) &(SgStmtDeclarationStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgStmtDeclarationStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgStmtDeclarationStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStmtDeclarationStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgStmtDeclarationStatement);

     return memory;
   }



/* #line 76352 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgStaticAssertionDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgStaticAssertionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgStaticAssertionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStaticAssertionDeclaration** objectArray = (SgStaticAssertionDeclaration**) &(SgStaticAssertionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStaticAssertionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgStaticAssertionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgStaticAssertionDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgStaticAssertionDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgStaticAssertionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStaticAssertionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgStaticAssertionDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStaticAssertionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgStaticAssertionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStaticAssertionDeclaration** objectArray = (SgStaticAssertionDeclaration**) &(SgStaticAssertionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStaticAssertionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStaticAssertionDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgStaticAssertionDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgStaticAssertionDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgStaticAssertionDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStaticAssertionDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgStaticAssertionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStaticAssertionDeclaration** objectArray = (SgStaticAssertionDeclaration**) &(SgStaticAssertionDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgStaticAssertionDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgStaticAssertionDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgStaticAssertionDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStaticAssertionDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgStaticAssertionDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgStaticAssertionDeclaration** objectArray = (SgStaticAssertionDeclaration**) &(SgStaticAssertionDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgStaticAssertionDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgStaticAssertionDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStaticAssertionDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgStaticAssertionDeclaration);

     return memory;
   }



/* #line 76584 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpDeclareSimdStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpDeclareSimdStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDeclareSimdStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDeclareSimdStatement** objectArray = (SgOmpDeclareSimdStatement**) &(SgOmpDeclareSimdStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpDeclareSimdStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpDeclareSimdStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpDeclareSimdStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpDeclareSimdStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpDeclareSimdStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpDeclareSimdStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpDeclareSimdStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpDeclareSimdStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDeclareSimdStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDeclareSimdStatement** objectArray = (SgOmpDeclareSimdStatement**) &(SgOmpDeclareSimdStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpDeclareSimdStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpDeclareSimdStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpDeclareSimdStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpDeclareSimdStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpDeclareSimdStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpDeclareSimdStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDeclareSimdStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDeclareSimdStatement** objectArray = (SgOmpDeclareSimdStatement**) &(SgOmpDeclareSimdStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpDeclareSimdStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpDeclareSimdStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpDeclareSimdStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpDeclareSimdStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpDeclareSimdStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpDeclareSimdStatement** objectArray = (SgOmpDeclareSimdStatement**) &(SgOmpDeclareSimdStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpDeclareSimdStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpDeclareSimdStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpDeclareSimdStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpDeclareSimdStatement);

     return memory;
   }



/* #line 76816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMicrosoftAttributeDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMicrosoftAttributeDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgMicrosoftAttributeDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMicrosoftAttributeDeclaration** objectArray = (SgMicrosoftAttributeDeclaration**) &(SgMicrosoftAttributeDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMicrosoftAttributeDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMicrosoftAttributeDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMicrosoftAttributeDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMicrosoftAttributeDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMicrosoftAttributeDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMicrosoftAttributeDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMicrosoftAttributeDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMicrosoftAttributeDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgMicrosoftAttributeDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMicrosoftAttributeDeclaration** objectArray = (SgMicrosoftAttributeDeclaration**) &(SgMicrosoftAttributeDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMicrosoftAttributeDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMicrosoftAttributeDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMicrosoftAttributeDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMicrosoftAttributeDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMicrosoftAttributeDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMicrosoftAttributeDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgMicrosoftAttributeDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMicrosoftAttributeDeclaration** objectArray = (SgMicrosoftAttributeDeclaration**) &(SgMicrosoftAttributeDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMicrosoftAttributeDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMicrosoftAttributeDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMicrosoftAttributeDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMicrosoftAttributeDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMicrosoftAttributeDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMicrosoftAttributeDeclaration** objectArray = (SgMicrosoftAttributeDeclaration**) &(SgMicrosoftAttributeDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMicrosoftAttributeDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMicrosoftAttributeDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMicrosoftAttributeDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMicrosoftAttributeDeclaration);

     return memory;
   }



/* #line 77048 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJovialCompoolStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJovialCompoolStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialCompoolStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialCompoolStatement** objectArray = (SgJovialCompoolStatement**) &(SgJovialCompoolStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialCompoolStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJovialCompoolStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJovialCompoolStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJovialCompoolStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJovialCompoolStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialCompoolStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJovialCompoolStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialCompoolStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialCompoolStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialCompoolStatement** objectArray = (SgJovialCompoolStatement**) &(SgJovialCompoolStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialCompoolStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialCompoolStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJovialCompoolStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJovialCompoolStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJovialCompoolStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialCompoolStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialCompoolStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialCompoolStatement** objectArray = (SgJovialCompoolStatement**) &(SgJovialCompoolStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJovialCompoolStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJovialCompoolStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJovialCompoolStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialCompoolStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJovialCompoolStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJovialCompoolStatement** objectArray = (SgJovialCompoolStatement**) &(SgJovialCompoolStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJovialCompoolStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJovialCompoolStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialCompoolStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJovialCompoolStatement);

     return memory;
   }



/* #line 77280 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJovialDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJovialDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialDirectiveStatement** objectArray = (SgJovialDirectiveStatement**) &(SgJovialDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJovialDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJovialDirectiveStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJovialDirectiveStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJovialDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJovialDirectiveStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialDirectiveStatement** objectArray = (SgJovialDirectiveStatement**) &(SgJovialDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialDirectiveStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJovialDirectiveStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJovialDirectiveStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJovialDirectiveStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialDirectiveStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialDirectiveStatement** objectArray = (SgJovialDirectiveStatement**) &(SgJovialDirectiveStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJovialDirectiveStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJovialDirectiveStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJovialDirectiveStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialDirectiveStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJovialDirectiveStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJovialDirectiveStatement** objectArray = (SgJovialDirectiveStatement**) &(SgJovialDirectiveStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJovialDirectiveStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJovialDirectiveStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialDirectiveStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJovialDirectiveStatement);

     return memory;
   }



/* #line 77512 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJovialDefineDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJovialDefineDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialDefineDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialDefineDeclaration** objectArray = (SgJovialDefineDeclaration**) &(SgJovialDefineDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialDefineDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJovialDefineDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJovialDefineDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJovialDefineDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJovialDefineDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialDefineDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJovialDefineDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialDefineDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialDefineDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialDefineDeclaration** objectArray = (SgJovialDefineDeclaration**) &(SgJovialDefineDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialDefineDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialDefineDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJovialDefineDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJovialDefineDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJovialDefineDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialDefineDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialDefineDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialDefineDeclaration** objectArray = (SgJovialDefineDeclaration**) &(SgJovialDefineDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJovialDefineDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJovialDefineDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJovialDefineDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialDefineDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJovialDefineDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJovialDefineDeclaration** objectArray = (SgJovialDefineDeclaration**) &(SgJovialDefineDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJovialDefineDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJovialDefineDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialDefineDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJovialDefineDeclaration);

     return memory;
   }



/* #line 77744 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJovialLabelDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJovialLabelDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialLabelDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialLabelDeclaration** objectArray = (SgJovialLabelDeclaration**) &(SgJovialLabelDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialLabelDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJovialLabelDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJovialLabelDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJovialLabelDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJovialLabelDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialLabelDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJovialLabelDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialLabelDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialLabelDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialLabelDeclaration** objectArray = (SgJovialLabelDeclaration**) &(SgJovialLabelDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialLabelDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialLabelDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJovialLabelDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJovialLabelDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJovialLabelDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialLabelDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialLabelDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialLabelDeclaration** objectArray = (SgJovialLabelDeclaration**) &(SgJovialLabelDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJovialLabelDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJovialLabelDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJovialLabelDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialLabelDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJovialLabelDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJovialLabelDeclaration** objectArray = (SgJovialLabelDeclaration**) &(SgJovialLabelDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJovialLabelDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJovialLabelDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialLabelDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJovialLabelDeclaration);

     return memory;
   }



/* #line 77976 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJovialOverlayDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJovialOverlayDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialOverlayDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialOverlayDeclaration** objectArray = (SgJovialOverlayDeclaration**) &(SgJovialOverlayDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialOverlayDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJovialOverlayDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJovialOverlayDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJovialOverlayDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJovialOverlayDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialOverlayDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJovialOverlayDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialOverlayDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialOverlayDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialOverlayDeclaration** objectArray = (SgJovialOverlayDeclaration**) &(SgJovialOverlayDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialOverlayDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialOverlayDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJovialOverlayDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJovialOverlayDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJovialOverlayDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialOverlayDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialOverlayDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialOverlayDeclaration** objectArray = (SgJovialOverlayDeclaration**) &(SgJovialOverlayDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJovialOverlayDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJovialOverlayDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJovialOverlayDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialOverlayDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJovialOverlayDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJovialOverlayDeclaration** objectArray = (SgJovialOverlayDeclaration**) &(SgJovialOverlayDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJovialOverlayDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJovialOverlayDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialOverlayDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJovialOverlayDeclaration);

     return memory;
   }



/* #line 78208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNonrealDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNonrealDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealDecl** objectArray = (SgNonrealDecl**) &(SgNonrealDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNonrealDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNonrealDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNonrealDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNonrealDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNonrealDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNonrealDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNonrealDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNonrealDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealDecl** objectArray = (SgNonrealDecl**) &(SgNonrealDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNonrealDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNonrealDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNonrealDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNonrealDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNonrealDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNonrealDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealDecl** objectArray = (SgNonrealDecl**) &(SgNonrealDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNonrealDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNonrealDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNonrealDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNonrealDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNonrealDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNonrealDecl** objectArray = (SgNonrealDecl**) &(SgNonrealDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNonrealDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNonrealDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNonrealDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNonrealDecl);

     return memory;
   }



/* #line 78440 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgEmptyDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgEmptyDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgEmptyDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEmptyDeclaration** objectArray = (SgEmptyDeclaration**) &(SgEmptyDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEmptyDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgEmptyDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgEmptyDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgEmptyDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgEmptyDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEmptyDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgEmptyDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEmptyDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgEmptyDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEmptyDeclaration** objectArray = (SgEmptyDeclaration**) &(SgEmptyDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEmptyDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEmptyDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgEmptyDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgEmptyDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgEmptyDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEmptyDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgEmptyDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEmptyDeclaration** objectArray = (SgEmptyDeclaration**) &(SgEmptyDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgEmptyDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgEmptyDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgEmptyDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEmptyDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgEmptyDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgEmptyDeclaration** objectArray = (SgEmptyDeclaration**) &(SgEmptyDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgEmptyDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgEmptyDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEmptyDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgEmptyDeclaration);

     return memory;
   }



/* #line 78672 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaPackageBodyDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaPackageBodyDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageBodyDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageBodyDecl** objectArray = (SgAdaPackageBodyDecl**) &(SgAdaPackageBodyDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaPackageBodyDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaPackageBodyDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaPackageBodyDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaPackageBodyDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaPackageBodyDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaPackageBodyDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaPackageBodyDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaPackageBodyDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageBodyDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageBodyDecl** objectArray = (SgAdaPackageBodyDecl**) &(SgAdaPackageBodyDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaPackageBodyDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaPackageBodyDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaPackageBodyDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaPackageBodyDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaPackageBodyDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaPackageBodyDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageBodyDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageBodyDecl** objectArray = (SgAdaPackageBodyDecl**) &(SgAdaPackageBodyDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaPackageBodyDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaPackageBodyDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaPackageBodyDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaPackageBodyDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaPackageBodyDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaPackageBodyDecl** objectArray = (SgAdaPackageBodyDecl**) &(SgAdaPackageBodyDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaPackageBodyDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaPackageBodyDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaPackageBodyDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaPackageBodyDecl);

     return memory;
   }



/* #line 78904 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaPackageSpecDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaPackageSpecDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageSpecDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageSpecDecl** objectArray = (SgAdaPackageSpecDecl**) &(SgAdaPackageSpecDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaPackageSpecDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaPackageSpecDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaPackageSpecDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaPackageSpecDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaPackageSpecDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaPackageSpecDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaPackageSpecDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaPackageSpecDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageSpecDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageSpecDecl** objectArray = (SgAdaPackageSpecDecl**) &(SgAdaPackageSpecDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaPackageSpecDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaPackageSpecDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaPackageSpecDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaPackageSpecDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaPackageSpecDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaPackageSpecDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageSpecDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageSpecDecl** objectArray = (SgAdaPackageSpecDecl**) &(SgAdaPackageSpecDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaPackageSpecDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaPackageSpecDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaPackageSpecDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaPackageSpecDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaPackageSpecDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaPackageSpecDecl** objectArray = (SgAdaPackageSpecDecl**) &(SgAdaPackageSpecDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaPackageSpecDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaPackageSpecDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaPackageSpecDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaPackageSpecDecl);

     return memory;
   }



/* #line 79136 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaRenamingDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaRenamingDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRenamingDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRenamingDecl** objectArray = (SgAdaRenamingDecl**) &(SgAdaRenamingDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaRenamingDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaRenamingDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaRenamingDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaRenamingDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaRenamingDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaRenamingDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaRenamingDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaRenamingDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRenamingDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRenamingDecl** objectArray = (SgAdaRenamingDecl**) &(SgAdaRenamingDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaRenamingDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaRenamingDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaRenamingDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaRenamingDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaRenamingDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaRenamingDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRenamingDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRenamingDecl** objectArray = (SgAdaRenamingDecl**) &(SgAdaRenamingDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaRenamingDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaRenamingDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaRenamingDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaRenamingDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaRenamingDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaRenamingDecl** objectArray = (SgAdaRenamingDecl**) &(SgAdaRenamingDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaRenamingDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaRenamingDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaRenamingDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaRenamingDecl);

     return memory;
   }



/* #line 79368 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaTaskSpecDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaTaskSpecDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskSpecDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskSpecDecl** objectArray = (SgAdaTaskSpecDecl**) &(SgAdaTaskSpecDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskSpecDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaTaskSpecDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaTaskSpecDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaTaskSpecDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaTaskSpecDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskSpecDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaTaskSpecDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskSpecDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskSpecDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskSpecDecl** objectArray = (SgAdaTaskSpecDecl**) &(SgAdaTaskSpecDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskSpecDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskSpecDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaTaskSpecDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaTaskSpecDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaTaskSpecDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskSpecDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskSpecDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskSpecDecl** objectArray = (SgAdaTaskSpecDecl**) &(SgAdaTaskSpecDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaTaskSpecDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaTaskSpecDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaTaskSpecDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskSpecDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaTaskSpecDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaTaskSpecDecl** objectArray = (SgAdaTaskSpecDecl**) &(SgAdaTaskSpecDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaTaskSpecDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaTaskSpecDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskSpecDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaTaskSpecDecl);

     return memory;
   }



/* #line 79600 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaTaskBodyDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaTaskBodyDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskBodyDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskBodyDecl** objectArray = (SgAdaTaskBodyDecl**) &(SgAdaTaskBodyDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskBodyDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaTaskBodyDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaTaskBodyDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaTaskBodyDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaTaskBodyDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskBodyDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaTaskBodyDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskBodyDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskBodyDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskBodyDecl** objectArray = (SgAdaTaskBodyDecl**) &(SgAdaTaskBodyDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskBodyDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskBodyDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaTaskBodyDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaTaskBodyDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaTaskBodyDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskBodyDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskBodyDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskBodyDecl** objectArray = (SgAdaTaskBodyDecl**) &(SgAdaTaskBodyDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaTaskBodyDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaTaskBodyDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaTaskBodyDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskBodyDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaTaskBodyDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaTaskBodyDecl** objectArray = (SgAdaTaskBodyDecl**) &(SgAdaTaskBodyDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaTaskBodyDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaTaskBodyDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskBodyDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaTaskBodyDecl);

     return memory;
   }



/* #line 79832 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaTaskTypeDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaTaskTypeDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskTypeDecl** objectArray = (SgAdaTaskTypeDecl**) &(SgAdaTaskTypeDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaTaskTypeDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaTaskTypeDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaTaskTypeDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaTaskTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskTypeDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaTaskTypeDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskTypeDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskTypeDecl** objectArray = (SgAdaTaskTypeDecl**) &(SgAdaTaskTypeDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskTypeDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaTaskTypeDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaTaskTypeDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaTaskTypeDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskTypeDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskTypeDecl** objectArray = (SgAdaTaskTypeDecl**) &(SgAdaTaskTypeDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaTaskTypeDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaTaskTypeDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaTaskTypeDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskTypeDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaTaskTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaTaskTypeDecl** objectArray = (SgAdaTaskTypeDecl**) &(SgAdaTaskTypeDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaTaskTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaTaskTypeDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskTypeDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaTaskTypeDecl);

     return memory;
   }



/* #line 80064 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaProtectedSpecDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaProtectedSpecDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedSpecDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedSpecDecl** objectArray = (SgAdaProtectedSpecDecl**) &(SgAdaProtectedSpecDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedSpecDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaProtectedSpecDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaProtectedSpecDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaProtectedSpecDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaProtectedSpecDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedSpecDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaProtectedSpecDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedSpecDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedSpecDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedSpecDecl** objectArray = (SgAdaProtectedSpecDecl**) &(SgAdaProtectedSpecDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedSpecDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedSpecDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaProtectedSpecDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaProtectedSpecDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaProtectedSpecDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedSpecDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedSpecDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedSpecDecl** objectArray = (SgAdaProtectedSpecDecl**) &(SgAdaProtectedSpecDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaProtectedSpecDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaProtectedSpecDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaProtectedSpecDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedSpecDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaProtectedSpecDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaProtectedSpecDecl** objectArray = (SgAdaProtectedSpecDecl**) &(SgAdaProtectedSpecDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaProtectedSpecDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaProtectedSpecDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedSpecDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaProtectedSpecDecl);

     return memory;
   }



/* #line 80296 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaProtectedBodyDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaProtectedBodyDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedBodyDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedBodyDecl** objectArray = (SgAdaProtectedBodyDecl**) &(SgAdaProtectedBodyDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedBodyDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaProtectedBodyDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaProtectedBodyDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaProtectedBodyDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaProtectedBodyDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedBodyDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaProtectedBodyDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedBodyDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedBodyDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedBodyDecl** objectArray = (SgAdaProtectedBodyDecl**) &(SgAdaProtectedBodyDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedBodyDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedBodyDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaProtectedBodyDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaProtectedBodyDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaProtectedBodyDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedBodyDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedBodyDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedBodyDecl** objectArray = (SgAdaProtectedBodyDecl**) &(SgAdaProtectedBodyDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaProtectedBodyDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaProtectedBodyDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaProtectedBodyDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedBodyDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaProtectedBodyDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaProtectedBodyDecl** objectArray = (SgAdaProtectedBodyDecl**) &(SgAdaProtectedBodyDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaProtectedBodyDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaProtectedBodyDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedBodyDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaProtectedBodyDecl);

     return memory;
   }



/* #line 80528 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaProtectedTypeDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaProtectedTypeDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedTypeDecl** objectArray = (SgAdaProtectedTypeDecl**) &(SgAdaProtectedTypeDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaProtectedTypeDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaProtectedTypeDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaProtectedTypeDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaProtectedTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedTypeDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaProtectedTypeDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedTypeDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedTypeDecl** objectArray = (SgAdaProtectedTypeDecl**) &(SgAdaProtectedTypeDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedTypeDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaProtectedTypeDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaProtectedTypeDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaProtectedTypeDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedTypeDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedTypeDecl** objectArray = (SgAdaProtectedTypeDecl**) &(SgAdaProtectedTypeDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaProtectedTypeDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaProtectedTypeDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaProtectedTypeDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedTypeDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaProtectedTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaProtectedTypeDecl** objectArray = (SgAdaProtectedTypeDecl**) &(SgAdaProtectedTypeDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaProtectedTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaProtectedTypeDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedTypeDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaProtectedTypeDecl);

     return memory;
   }



/* #line 80760 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaRepresentationClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaRepresentationClause objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRepresentationClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRepresentationClause** objectArray = (SgAdaRepresentationClause**) &(SgAdaRepresentationClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaRepresentationClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaRepresentationClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaRepresentationClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaRepresentationClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaRepresentationClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaRepresentationClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaRepresentationClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaRepresentationClause objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRepresentationClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRepresentationClause** objectArray = (SgAdaRepresentationClause**) &(SgAdaRepresentationClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaRepresentationClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaRepresentationClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaRepresentationClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaRepresentationClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaRepresentationClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaRepresentationClause objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRepresentationClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRepresentationClause** objectArray = (SgAdaRepresentationClause**) &(SgAdaRepresentationClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaRepresentationClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaRepresentationClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaRepresentationClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaRepresentationClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaRepresentationClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaRepresentationClause** objectArray = (SgAdaRepresentationClause**) &(SgAdaRepresentationClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaRepresentationClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaRepresentationClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaRepresentationClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaRepresentationClause);

     return memory;
   }



/* #line 80992 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaComponentClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaComponentClause objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaComponentClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaComponentClause** objectArray = (SgAdaComponentClause**) &(SgAdaComponentClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaComponentClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaComponentClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaComponentClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaComponentClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaComponentClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaComponentClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaComponentClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaComponentClause objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaComponentClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaComponentClause** objectArray = (SgAdaComponentClause**) &(SgAdaComponentClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaComponentClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaComponentClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaComponentClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaComponentClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaComponentClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaComponentClause objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaComponentClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaComponentClause** objectArray = (SgAdaComponentClause**) &(SgAdaComponentClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaComponentClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaComponentClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaComponentClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaComponentClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaComponentClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaComponentClause** objectArray = (SgAdaComponentClause**) &(SgAdaComponentClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaComponentClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaComponentClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaComponentClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaComponentClause);

     return memory;
   }



/* #line 81224 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaAttributeClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaAttributeClause objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAttributeClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAttributeClause** objectArray = (SgAdaAttributeClause**) &(SgAdaAttributeClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaAttributeClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaAttributeClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaAttributeClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaAttributeClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaAttributeClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaAttributeClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaAttributeClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaAttributeClause objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAttributeClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAttributeClause** objectArray = (SgAdaAttributeClause**) &(SgAdaAttributeClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaAttributeClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaAttributeClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaAttributeClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaAttributeClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaAttributeClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaAttributeClause objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAttributeClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAttributeClause** objectArray = (SgAdaAttributeClause**) &(SgAdaAttributeClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaAttributeClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaAttributeClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaAttributeClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaAttributeClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaAttributeClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaAttributeClause** objectArray = (SgAdaAttributeClause**) &(SgAdaAttributeClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaAttributeClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaAttributeClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaAttributeClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaAttributeClause);

     return memory;
   }



/* #line 81456 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaEnumRepresentationClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaEnumRepresentationClause objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaEnumRepresentationClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaEnumRepresentationClause** objectArray = (SgAdaEnumRepresentationClause**) &(SgAdaEnumRepresentationClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaEnumRepresentationClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaEnumRepresentationClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaEnumRepresentationClause::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaEnumRepresentationClause*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaEnumRepresentationClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaEnumRepresentationClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaEnumRepresentationClause::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaEnumRepresentationClause objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaEnumRepresentationClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaEnumRepresentationClause** objectArray = (SgAdaEnumRepresentationClause**) &(SgAdaEnumRepresentationClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaEnumRepresentationClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaEnumRepresentationClause::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaEnumRepresentationClause object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaEnumRepresentationClause object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaEnumRepresentationClause::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaEnumRepresentationClause objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaEnumRepresentationClause::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaEnumRepresentationClause** objectArray = (SgAdaEnumRepresentationClause**) &(SgAdaEnumRepresentationClause::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaEnumRepresentationClause::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaEnumRepresentationClause::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaEnumRepresentationClause found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaEnumRepresentationClause::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaEnumRepresentationClause::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaEnumRepresentationClause** objectArray = (SgAdaEnumRepresentationClause**) &(SgAdaEnumRepresentationClause::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaEnumRepresentationClause::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaEnumRepresentationClause::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaEnumRepresentationClause::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaEnumRepresentationClause);

     return memory;
   }



/* #line 81688 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaGenericDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaGenericDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericDecl** objectArray = (SgAdaGenericDecl**) &(SgAdaGenericDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaGenericDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaGenericDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaGenericDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaGenericDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaGenericDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaGenericDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaGenericDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaGenericDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericDecl** objectArray = (SgAdaGenericDecl**) &(SgAdaGenericDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaGenericDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaGenericDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaGenericDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaGenericDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaGenericDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaGenericDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericDecl** objectArray = (SgAdaGenericDecl**) &(SgAdaGenericDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaGenericDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaGenericDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaGenericDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaGenericDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaGenericDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaGenericDecl** objectArray = (SgAdaGenericDecl**) &(SgAdaGenericDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaGenericDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaGenericDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaGenericDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaGenericDecl);

     return memory;
   }



/* #line 81920 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaFormalTypeDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaFormalTypeDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFormalTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFormalTypeDecl** objectArray = (SgAdaFormalTypeDecl**) &(SgAdaFormalTypeDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaFormalTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaFormalTypeDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaFormalTypeDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaFormalTypeDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaFormalTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaFormalTypeDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaFormalTypeDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaFormalTypeDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFormalTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFormalTypeDecl** objectArray = (SgAdaFormalTypeDecl**) &(SgAdaFormalTypeDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaFormalTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaFormalTypeDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaFormalTypeDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaFormalTypeDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaFormalTypeDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaFormalTypeDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFormalTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFormalTypeDecl** objectArray = (SgAdaFormalTypeDecl**) &(SgAdaFormalTypeDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaFormalTypeDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaFormalTypeDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaFormalTypeDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaFormalTypeDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaFormalTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaFormalTypeDecl** objectArray = (SgAdaFormalTypeDecl**) &(SgAdaFormalTypeDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaFormalTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaFormalTypeDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaFormalTypeDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaFormalTypeDecl);

     return memory;
   }



/* #line 82152 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaDiscriminatedTypeDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaDiscriminatedTypeDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDiscriminatedTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDiscriminatedTypeDecl** objectArray = (SgAdaDiscriminatedTypeDecl**) &(SgAdaDiscriminatedTypeDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDiscriminatedTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaDiscriminatedTypeDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaDiscriminatedTypeDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaDiscriminatedTypeDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaDiscriminatedTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDiscriminatedTypeDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaDiscriminatedTypeDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDiscriminatedTypeDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDiscriminatedTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDiscriminatedTypeDecl** objectArray = (SgAdaDiscriminatedTypeDecl**) &(SgAdaDiscriminatedTypeDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDiscriminatedTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDiscriminatedTypeDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaDiscriminatedTypeDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaDiscriminatedTypeDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaDiscriminatedTypeDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDiscriminatedTypeDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDiscriminatedTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDiscriminatedTypeDecl** objectArray = (SgAdaDiscriminatedTypeDecl**) &(SgAdaDiscriminatedTypeDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaDiscriminatedTypeDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaDiscriminatedTypeDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaDiscriminatedTypeDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDiscriminatedTypeDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaDiscriminatedTypeDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaDiscriminatedTypeDecl** objectArray = (SgAdaDiscriminatedTypeDecl**) &(SgAdaDiscriminatedTypeDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaDiscriminatedTypeDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaDiscriminatedTypeDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDiscriminatedTypeDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaDiscriminatedTypeDecl);

     return memory;
   }



/* #line 82384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaGenericInstanceDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaGenericInstanceDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericInstanceDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericInstanceDecl** objectArray = (SgAdaGenericInstanceDecl**) &(SgAdaGenericInstanceDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaGenericInstanceDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaGenericInstanceDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaGenericInstanceDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaGenericInstanceDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaGenericInstanceDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaGenericInstanceDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaGenericInstanceDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaGenericInstanceDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericInstanceDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericInstanceDecl** objectArray = (SgAdaGenericInstanceDecl**) &(SgAdaGenericInstanceDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaGenericInstanceDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaGenericInstanceDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaGenericInstanceDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaGenericInstanceDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaGenericInstanceDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaGenericInstanceDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericInstanceDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericInstanceDecl** objectArray = (SgAdaGenericInstanceDecl**) &(SgAdaGenericInstanceDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaGenericInstanceDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaGenericInstanceDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaGenericInstanceDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaGenericInstanceDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaGenericInstanceDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaGenericInstanceDecl** objectArray = (SgAdaGenericInstanceDecl**) &(SgAdaGenericInstanceDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaGenericInstanceDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaGenericInstanceDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaGenericInstanceDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaGenericInstanceDecl);

     return memory;
   }



/* #line 82616 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaFormalPackageDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaFormalPackageDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFormalPackageDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFormalPackageDecl** objectArray = (SgAdaFormalPackageDecl**) &(SgAdaFormalPackageDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaFormalPackageDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaFormalPackageDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaFormalPackageDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaFormalPackageDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaFormalPackageDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaFormalPackageDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaFormalPackageDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaFormalPackageDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFormalPackageDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFormalPackageDecl** objectArray = (SgAdaFormalPackageDecl**) &(SgAdaFormalPackageDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaFormalPackageDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaFormalPackageDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaFormalPackageDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaFormalPackageDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaFormalPackageDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaFormalPackageDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFormalPackageDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFormalPackageDecl** objectArray = (SgAdaFormalPackageDecl**) &(SgAdaFormalPackageDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaFormalPackageDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaFormalPackageDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaFormalPackageDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaFormalPackageDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaFormalPackageDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaFormalPackageDecl** objectArray = (SgAdaFormalPackageDecl**) &(SgAdaFormalPackageDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaFormalPackageDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaFormalPackageDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaFormalPackageDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaFormalPackageDecl);

     return memory;
   }



/* #line 82848 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaParameterList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaParameterList objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaParameterList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaParameterList** objectArray = (SgAdaParameterList**) &(SgAdaParameterList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaParameterList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaParameterList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaParameterList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaParameterList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaParameterList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaParameterList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaParameterList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaParameterList objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaParameterList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaParameterList** objectArray = (SgAdaParameterList**) &(SgAdaParameterList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaParameterList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaParameterList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaParameterList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaParameterList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaParameterList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaParameterList objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaParameterList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaParameterList** objectArray = (SgAdaParameterList**) &(SgAdaParameterList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaParameterList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaParameterList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaParameterList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaParameterList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaParameterList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaParameterList** objectArray = (SgAdaParameterList**) &(SgAdaParameterList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaParameterList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaParameterList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaParameterList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaParameterList);

     return memory;
   }



/* #line 83080 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaVariantDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaVariantDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaVariantDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaVariantDecl** objectArray = (SgAdaVariantDecl**) &(SgAdaVariantDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaVariantDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaVariantDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaVariantDecl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaVariantDecl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaVariantDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaVariantDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaVariantDecl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaVariantDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaVariantDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaVariantDecl** objectArray = (SgAdaVariantDecl**) &(SgAdaVariantDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaVariantDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaVariantDecl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaVariantDecl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaVariantDecl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaVariantDecl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaVariantDecl objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaVariantDecl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaVariantDecl** objectArray = (SgAdaVariantDecl**) &(SgAdaVariantDecl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaVariantDecl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaVariantDecl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaVariantDecl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaVariantDecl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaVariantDecl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaVariantDecl** objectArray = (SgAdaVariantDecl**) &(SgAdaVariantDecl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaVariantDecl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaVariantDecl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaVariantDecl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaVariantDecl);

     return memory;
   }



/* #line 83312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgExprStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgExprStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgExprStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExprStatement** objectArray = (SgExprStatement**) &(SgExprStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExprStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgExprStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgExprStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgExprStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgExprStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExprStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgExprStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExprStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgExprStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExprStatement** objectArray = (SgExprStatement**) &(SgExprStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExprStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExprStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgExprStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgExprStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgExprStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExprStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgExprStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExprStatement** objectArray = (SgExprStatement**) &(SgExprStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgExprStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgExprStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgExprStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExprStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgExprStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgExprStatement** objectArray = (SgExprStatement**) &(SgExprStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgExprStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgExprStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExprStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgExprStatement);

     return memory;
   }



/* #line 83544 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLabelStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLabelStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgLabelStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLabelStatement** objectArray = (SgLabelStatement**) &(SgLabelStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLabelStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLabelStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLabelStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLabelStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLabelStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLabelStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLabelStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLabelStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgLabelStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLabelStatement** objectArray = (SgLabelStatement**) &(SgLabelStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLabelStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLabelStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLabelStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLabelStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLabelStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLabelStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgLabelStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLabelStatement** objectArray = (SgLabelStatement**) &(SgLabelStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLabelStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLabelStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLabelStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLabelStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLabelStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLabelStatement** objectArray = (SgLabelStatement**) &(SgLabelStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLabelStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLabelStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLabelStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLabelStatement);

     return memory;
   }



/* #line 83776 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCaseOptionStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCaseOptionStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgCaseOptionStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCaseOptionStmt** objectArray = (SgCaseOptionStmt**) &(SgCaseOptionStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCaseOptionStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCaseOptionStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCaseOptionStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCaseOptionStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCaseOptionStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCaseOptionStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCaseOptionStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCaseOptionStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgCaseOptionStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCaseOptionStmt** objectArray = (SgCaseOptionStmt**) &(SgCaseOptionStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCaseOptionStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCaseOptionStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCaseOptionStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCaseOptionStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCaseOptionStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCaseOptionStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgCaseOptionStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCaseOptionStmt** objectArray = (SgCaseOptionStmt**) &(SgCaseOptionStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCaseOptionStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCaseOptionStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCaseOptionStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCaseOptionStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCaseOptionStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCaseOptionStmt** objectArray = (SgCaseOptionStmt**) &(SgCaseOptionStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCaseOptionStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCaseOptionStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCaseOptionStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCaseOptionStmt);

     return memory;
   }



/* #line 84008 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTryStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTryStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgTryStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTryStmt** objectArray = (SgTryStmt**) &(SgTryStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTryStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTryStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTryStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTryStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTryStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTryStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTryStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTryStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgTryStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTryStmt** objectArray = (SgTryStmt**) &(SgTryStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTryStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTryStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTryStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTryStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTryStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTryStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgTryStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTryStmt** objectArray = (SgTryStmt**) &(SgTryStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTryStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTryStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTryStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTryStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTryStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTryStmt** objectArray = (SgTryStmt**) &(SgTryStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTryStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTryStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTryStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTryStmt);

     return memory;
   }



/* #line 84240 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDefaultOptionStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDefaultOptionStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgDefaultOptionStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDefaultOptionStmt** objectArray = (SgDefaultOptionStmt**) &(SgDefaultOptionStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDefaultOptionStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDefaultOptionStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDefaultOptionStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDefaultOptionStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDefaultOptionStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDefaultOptionStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDefaultOptionStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDefaultOptionStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgDefaultOptionStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDefaultOptionStmt** objectArray = (SgDefaultOptionStmt**) &(SgDefaultOptionStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDefaultOptionStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDefaultOptionStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDefaultOptionStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDefaultOptionStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDefaultOptionStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDefaultOptionStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgDefaultOptionStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDefaultOptionStmt** objectArray = (SgDefaultOptionStmt**) &(SgDefaultOptionStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDefaultOptionStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDefaultOptionStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDefaultOptionStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDefaultOptionStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDefaultOptionStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDefaultOptionStmt** objectArray = (SgDefaultOptionStmt**) &(SgDefaultOptionStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDefaultOptionStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDefaultOptionStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDefaultOptionStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDefaultOptionStmt);

     return memory;
   }



/* #line 84472 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBreakStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBreakStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgBreakStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBreakStmt** objectArray = (SgBreakStmt**) &(SgBreakStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBreakStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBreakStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBreakStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBreakStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBreakStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBreakStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBreakStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBreakStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgBreakStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBreakStmt** objectArray = (SgBreakStmt**) &(SgBreakStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBreakStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBreakStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBreakStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBreakStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBreakStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBreakStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgBreakStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBreakStmt** objectArray = (SgBreakStmt**) &(SgBreakStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBreakStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBreakStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBreakStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBreakStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBreakStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBreakStmt** objectArray = (SgBreakStmt**) &(SgBreakStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBreakStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBreakStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBreakStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBreakStmt);

     return memory;
   }



/* #line 84704 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgContinueStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgContinueStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgContinueStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgContinueStmt** objectArray = (SgContinueStmt**) &(SgContinueStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgContinueStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgContinueStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgContinueStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgContinueStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgContinueStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgContinueStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgContinueStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgContinueStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgContinueStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgContinueStmt** objectArray = (SgContinueStmt**) &(SgContinueStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgContinueStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgContinueStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgContinueStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgContinueStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgContinueStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgContinueStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgContinueStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgContinueStmt** objectArray = (SgContinueStmt**) &(SgContinueStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgContinueStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgContinueStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgContinueStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgContinueStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgContinueStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgContinueStmt** objectArray = (SgContinueStmt**) &(SgContinueStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgContinueStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgContinueStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgContinueStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgContinueStmt);

     return memory;
   }



/* #line 84936 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgReturnStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgReturnStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgReturnStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReturnStmt** objectArray = (SgReturnStmt**) &(SgReturnStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgReturnStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgReturnStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgReturnStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgReturnStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgReturnStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgReturnStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgReturnStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgReturnStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgReturnStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReturnStmt** objectArray = (SgReturnStmt**) &(SgReturnStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgReturnStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgReturnStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgReturnStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgReturnStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgReturnStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgReturnStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgReturnStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReturnStmt** objectArray = (SgReturnStmt**) &(SgReturnStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgReturnStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgReturnStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgReturnStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgReturnStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgReturnStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgReturnStmt** objectArray = (SgReturnStmt**) &(SgReturnStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgReturnStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgReturnStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgReturnStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgReturnStmt);

     return memory;
   }



/* #line 85168 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgGotoStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgGotoStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgGotoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGotoStatement** objectArray = (SgGotoStatement**) &(SgGotoStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGotoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgGotoStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgGotoStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgGotoStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgGotoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGotoStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgGotoStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGotoStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgGotoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGotoStatement** objectArray = (SgGotoStatement**) &(SgGotoStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGotoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGotoStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgGotoStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgGotoStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgGotoStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGotoStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgGotoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGotoStatement** objectArray = (SgGotoStatement**) &(SgGotoStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgGotoStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgGotoStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgGotoStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGotoStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgGotoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgGotoStatement** objectArray = (SgGotoStatement**) &(SgGotoStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgGotoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgGotoStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGotoStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgGotoStatement);

     return memory;
   }



/* #line 85400 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSpawnStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSpawnStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgSpawnStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSpawnStmt** objectArray = (SgSpawnStmt**) &(SgSpawnStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSpawnStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSpawnStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSpawnStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSpawnStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSpawnStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSpawnStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSpawnStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSpawnStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgSpawnStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSpawnStmt** objectArray = (SgSpawnStmt**) &(SgSpawnStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSpawnStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSpawnStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSpawnStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSpawnStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSpawnStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSpawnStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgSpawnStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSpawnStmt** objectArray = (SgSpawnStmt**) &(SgSpawnStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSpawnStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSpawnStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSpawnStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSpawnStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSpawnStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSpawnStmt** objectArray = (SgSpawnStmt**) &(SgSpawnStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSpawnStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSpawnStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSpawnStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSpawnStmt);

     return memory;
   }



/* #line 85632 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNullStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNullStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNullStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNullStatement** objectArray = (SgNullStatement**) &(SgNullStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNullStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNullStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNullStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNullStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNullStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNullStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNullStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNullStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNullStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNullStatement** objectArray = (SgNullStatement**) &(SgNullStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNullStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNullStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNullStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNullStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNullStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNullStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNullStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNullStatement** objectArray = (SgNullStatement**) &(SgNullStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNullStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNullStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNullStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNullStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNullStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNullStatement** objectArray = (SgNullStatement**) &(SgNullStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNullStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNullStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNullStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNullStatement);

     return memory;
   }



/* #line 85864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgVariantStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgVariantStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgVariantStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariantStatement** objectArray = (SgVariantStatement**) &(SgVariantStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVariantStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgVariantStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgVariantStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgVariantStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgVariantStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVariantStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgVariantStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVariantStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgVariantStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariantStatement** objectArray = (SgVariantStatement**) &(SgVariantStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVariantStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVariantStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgVariantStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgVariantStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgVariantStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVariantStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgVariantStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariantStatement** objectArray = (SgVariantStatement**) &(SgVariantStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgVariantStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgVariantStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgVariantStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVariantStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgVariantStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgVariantStatement** objectArray = (SgVariantStatement**) &(SgVariantStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgVariantStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgVariantStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVariantStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgVariantStatement);

     return memory;
   }



/* #line 86096 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgForInitStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgForInitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgForInitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgForInitStatement** objectArray = (SgForInitStatement**) &(SgForInitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgForInitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgForInitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgForInitStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgForInitStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgForInitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgForInitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgForInitStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgForInitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgForInitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgForInitStatement** objectArray = (SgForInitStatement**) &(SgForInitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgForInitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgForInitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgForInitStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgForInitStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgForInitStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgForInitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgForInitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgForInitStatement** objectArray = (SgForInitStatement**) &(SgForInitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgForInitStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgForInitStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgForInitStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgForInitStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgForInitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgForInitStatement** objectArray = (SgForInitStatement**) &(SgForInitStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgForInitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgForInitStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgForInitStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgForInitStatement);

     return memory;
   }



/* #line 86328 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCatchStatementSeq::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCatchStatementSeq objectArray [] = *(Memory_Block_List.begin());
     if (SgCatchStatementSeq::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCatchStatementSeq** objectArray = (SgCatchStatementSeq**) &(SgCatchStatementSeq::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCatchStatementSeq::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCatchStatementSeq::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCatchStatementSeq::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCatchStatementSeq*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCatchStatementSeq::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCatchStatementSeq::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCatchStatementSeq::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCatchStatementSeq objectArray [] = *(Memory_Block_List.begin());
     if (SgCatchStatementSeq::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCatchStatementSeq** objectArray = (SgCatchStatementSeq**) &(SgCatchStatementSeq::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCatchStatementSeq::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCatchStatementSeq::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCatchStatementSeq object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCatchStatementSeq object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCatchStatementSeq::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCatchStatementSeq objectArray [] = *(Memory_Block_List.begin());
     if (SgCatchStatementSeq::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCatchStatementSeq** objectArray = (SgCatchStatementSeq**) &(SgCatchStatementSeq::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCatchStatementSeq::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCatchStatementSeq::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCatchStatementSeq found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCatchStatementSeq::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCatchStatementSeq::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCatchStatementSeq** objectArray = (SgCatchStatementSeq**) &(SgCatchStatementSeq::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCatchStatementSeq::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCatchStatementSeq::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCatchStatementSeq::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCatchStatementSeq);

     return memory;
   }



/* #line 86560 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgProcessControlStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgProcessControlStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgProcessControlStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgProcessControlStatement** objectArray = (SgProcessControlStatement**) &(SgProcessControlStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgProcessControlStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgProcessControlStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgProcessControlStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgProcessControlStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgProcessControlStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgProcessControlStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgProcessControlStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgProcessControlStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgProcessControlStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgProcessControlStatement** objectArray = (SgProcessControlStatement**) &(SgProcessControlStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgProcessControlStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgProcessControlStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgProcessControlStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgProcessControlStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgProcessControlStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgProcessControlStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgProcessControlStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgProcessControlStatement** objectArray = (SgProcessControlStatement**) &(SgProcessControlStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgProcessControlStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgProcessControlStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgProcessControlStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgProcessControlStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgProcessControlStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgProcessControlStatement** objectArray = (SgProcessControlStatement**) &(SgProcessControlStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgProcessControlStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgProcessControlStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgProcessControlStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgProcessControlStatement);

     return memory;
   }



/* #line 86792 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIOStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIOStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIOStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIOStatement** objectArray = (SgIOStatement**) &(SgIOStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIOStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIOStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIOStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIOStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIOStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIOStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIOStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIOStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIOStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIOStatement** objectArray = (SgIOStatement**) &(SgIOStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIOStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIOStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIOStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIOStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIOStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIOStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgIOStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIOStatement** objectArray = (SgIOStatement**) &(SgIOStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIOStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIOStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIOStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIOStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIOStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIOStatement** objectArray = (SgIOStatement**) &(SgIOStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIOStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIOStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIOStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIOStatement);

     return memory;
   }



/* #line 87024 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPrintStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPrintStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgPrintStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPrintStatement** objectArray = (SgPrintStatement**) &(SgPrintStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPrintStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPrintStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPrintStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPrintStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPrintStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPrintStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPrintStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPrintStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgPrintStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPrintStatement** objectArray = (SgPrintStatement**) &(SgPrintStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPrintStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPrintStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPrintStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPrintStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPrintStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPrintStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgPrintStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPrintStatement** objectArray = (SgPrintStatement**) &(SgPrintStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPrintStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPrintStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPrintStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPrintStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPrintStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPrintStatement** objectArray = (SgPrintStatement**) &(SgPrintStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPrintStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPrintStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPrintStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPrintStatement);

     return memory;
   }



/* #line 87256 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgReadStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgReadStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgReadStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReadStatement** objectArray = (SgReadStatement**) &(SgReadStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgReadStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgReadStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgReadStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgReadStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgReadStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgReadStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgReadStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgReadStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgReadStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReadStatement** objectArray = (SgReadStatement**) &(SgReadStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgReadStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgReadStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgReadStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgReadStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgReadStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgReadStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgReadStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReadStatement** objectArray = (SgReadStatement**) &(SgReadStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgReadStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgReadStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgReadStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgReadStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgReadStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgReadStatement** objectArray = (SgReadStatement**) &(SgReadStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgReadStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgReadStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgReadStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgReadStatement);

     return memory;
   }



/* #line 87488 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgWriteStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgWriteStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWriteStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWriteStatement** objectArray = (SgWriteStatement**) &(SgWriteStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWriteStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgWriteStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgWriteStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgWriteStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgWriteStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWriteStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgWriteStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWriteStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWriteStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWriteStatement** objectArray = (SgWriteStatement**) &(SgWriteStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWriteStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWriteStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgWriteStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgWriteStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgWriteStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWriteStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWriteStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWriteStatement** objectArray = (SgWriteStatement**) &(SgWriteStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgWriteStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgWriteStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgWriteStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWriteStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgWriteStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgWriteStatement** objectArray = (SgWriteStatement**) &(SgWriteStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgWriteStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgWriteStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWriteStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgWriteStatement);

     return memory;
   }



/* #line 87720 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOpenStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOpenStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOpenStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOpenStatement** objectArray = (SgOpenStatement**) &(SgOpenStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOpenStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOpenStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOpenStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOpenStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOpenStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOpenStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOpenStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOpenStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOpenStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOpenStatement** objectArray = (SgOpenStatement**) &(SgOpenStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOpenStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOpenStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOpenStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOpenStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOpenStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOpenStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOpenStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOpenStatement** objectArray = (SgOpenStatement**) &(SgOpenStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOpenStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOpenStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOpenStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOpenStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOpenStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOpenStatement** objectArray = (SgOpenStatement**) &(SgOpenStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOpenStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOpenStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOpenStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOpenStatement);

     return memory;
   }



/* #line 87952 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCloseStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCloseStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgCloseStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCloseStatement** objectArray = (SgCloseStatement**) &(SgCloseStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCloseStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCloseStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCloseStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCloseStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCloseStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCloseStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCloseStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCloseStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgCloseStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCloseStatement** objectArray = (SgCloseStatement**) &(SgCloseStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCloseStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCloseStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCloseStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCloseStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCloseStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCloseStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgCloseStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCloseStatement** objectArray = (SgCloseStatement**) &(SgCloseStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCloseStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCloseStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCloseStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCloseStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCloseStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCloseStatement** objectArray = (SgCloseStatement**) &(SgCloseStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCloseStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCloseStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCloseStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCloseStatement);

     return memory;
   }



/* #line 88184 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgInquireStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgInquireStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgInquireStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInquireStatement** objectArray = (SgInquireStatement**) &(SgInquireStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgInquireStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgInquireStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgInquireStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgInquireStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgInquireStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgInquireStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgInquireStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgInquireStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgInquireStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInquireStatement** objectArray = (SgInquireStatement**) &(SgInquireStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgInquireStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgInquireStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgInquireStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgInquireStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgInquireStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgInquireStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgInquireStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInquireStatement** objectArray = (SgInquireStatement**) &(SgInquireStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgInquireStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgInquireStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgInquireStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgInquireStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgInquireStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgInquireStatement** objectArray = (SgInquireStatement**) &(SgInquireStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgInquireStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgInquireStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgInquireStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgInquireStatement);

     return memory;
   }



/* #line 88416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFlushStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFlushStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgFlushStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFlushStatement** objectArray = (SgFlushStatement**) &(SgFlushStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFlushStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFlushStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFlushStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFlushStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFlushStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFlushStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFlushStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFlushStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgFlushStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFlushStatement** objectArray = (SgFlushStatement**) &(SgFlushStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFlushStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFlushStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFlushStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFlushStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFlushStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFlushStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgFlushStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFlushStatement** objectArray = (SgFlushStatement**) &(SgFlushStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFlushStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFlushStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFlushStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFlushStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFlushStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFlushStatement** objectArray = (SgFlushStatement**) &(SgFlushStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFlushStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFlushStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFlushStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFlushStatement);

     return memory;
   }



/* #line 88648 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBackspaceStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBackspaceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgBackspaceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBackspaceStatement** objectArray = (SgBackspaceStatement**) &(SgBackspaceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBackspaceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBackspaceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBackspaceStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBackspaceStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBackspaceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBackspaceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBackspaceStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBackspaceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgBackspaceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBackspaceStatement** objectArray = (SgBackspaceStatement**) &(SgBackspaceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBackspaceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBackspaceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBackspaceStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBackspaceStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBackspaceStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBackspaceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgBackspaceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBackspaceStatement** objectArray = (SgBackspaceStatement**) &(SgBackspaceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBackspaceStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBackspaceStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBackspaceStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBackspaceStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBackspaceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBackspaceStatement** objectArray = (SgBackspaceStatement**) &(SgBackspaceStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBackspaceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBackspaceStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBackspaceStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBackspaceStatement);

     return memory;
   }



/* #line 88880 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgRewindStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgRewindStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgRewindStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRewindStatement** objectArray = (SgRewindStatement**) &(SgRewindStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRewindStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgRewindStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgRewindStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgRewindStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgRewindStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRewindStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgRewindStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRewindStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgRewindStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRewindStatement** objectArray = (SgRewindStatement**) &(SgRewindStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRewindStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRewindStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgRewindStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgRewindStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgRewindStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRewindStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgRewindStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRewindStatement** objectArray = (SgRewindStatement**) &(SgRewindStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgRewindStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgRewindStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgRewindStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRewindStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgRewindStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgRewindStatement** objectArray = (SgRewindStatement**) &(SgRewindStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgRewindStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgRewindStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRewindStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgRewindStatement);

     return memory;
   }



/* #line 89112 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgEndfileStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgEndfileStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEndfileStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEndfileStatement** objectArray = (SgEndfileStatement**) &(SgEndfileStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEndfileStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgEndfileStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgEndfileStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgEndfileStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgEndfileStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEndfileStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgEndfileStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEndfileStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEndfileStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEndfileStatement** objectArray = (SgEndfileStatement**) &(SgEndfileStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEndfileStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEndfileStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgEndfileStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgEndfileStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgEndfileStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEndfileStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgEndfileStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEndfileStatement** objectArray = (SgEndfileStatement**) &(SgEndfileStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgEndfileStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgEndfileStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgEndfileStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEndfileStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgEndfileStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgEndfileStatement** objectArray = (SgEndfileStatement**) &(SgEndfileStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgEndfileStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgEndfileStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEndfileStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgEndfileStatement);

     return memory;
   }



/* #line 89344 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgWaitStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgWaitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWaitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWaitStatement** objectArray = (SgWaitStatement**) &(SgWaitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWaitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgWaitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgWaitStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgWaitStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgWaitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWaitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgWaitStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWaitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWaitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWaitStatement** objectArray = (SgWaitStatement**) &(SgWaitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWaitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWaitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgWaitStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgWaitStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgWaitStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWaitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWaitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWaitStatement** objectArray = (SgWaitStatement**) &(SgWaitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgWaitStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgWaitStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgWaitStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWaitStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgWaitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgWaitStatement** objectArray = (SgWaitStatement**) &(SgWaitStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgWaitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgWaitStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWaitStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgWaitStatement);

     return memory;
   }



/* #line 89576 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFortranContinueStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFortranContinueStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgFortranContinueStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFortranContinueStmt** objectArray = (SgFortranContinueStmt**) &(SgFortranContinueStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFortranContinueStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFortranContinueStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFortranContinueStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFortranContinueStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFortranContinueStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFortranContinueStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFortranContinueStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFortranContinueStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgFortranContinueStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFortranContinueStmt** objectArray = (SgFortranContinueStmt**) &(SgFortranContinueStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFortranContinueStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFortranContinueStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFortranContinueStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFortranContinueStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFortranContinueStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFortranContinueStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgFortranContinueStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFortranContinueStmt** objectArray = (SgFortranContinueStmt**) &(SgFortranContinueStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFortranContinueStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFortranContinueStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFortranContinueStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFortranContinueStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFortranContinueStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFortranContinueStmt** objectArray = (SgFortranContinueStmt**) &(SgFortranContinueStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFortranContinueStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFortranContinueStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFortranContinueStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFortranContinueStmt);

     return memory;
   }



/* #line 89808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgWhereStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgWhereStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWhereStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWhereStatement** objectArray = (SgWhereStatement**) &(SgWhereStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWhereStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgWhereStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgWhereStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgWhereStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgWhereStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWhereStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgWhereStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWhereStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWhereStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWhereStatement** objectArray = (SgWhereStatement**) &(SgWhereStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWhereStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWhereStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgWhereStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgWhereStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgWhereStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWhereStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWhereStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWhereStatement** objectArray = (SgWhereStatement**) &(SgWhereStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgWhereStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgWhereStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgWhereStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWhereStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgWhereStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgWhereStatement** objectArray = (SgWhereStatement**) &(SgWhereStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgWhereStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgWhereStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWhereStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgWhereStatement);

     return memory;
   }



/* #line 90040 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgElseWhereStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgElseWhereStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgElseWhereStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElseWhereStatement** objectArray = (SgElseWhereStatement**) &(SgElseWhereStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElseWhereStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgElseWhereStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgElseWhereStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgElseWhereStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgElseWhereStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElseWhereStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgElseWhereStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElseWhereStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgElseWhereStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElseWhereStatement** objectArray = (SgElseWhereStatement**) &(SgElseWhereStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElseWhereStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElseWhereStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgElseWhereStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgElseWhereStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgElseWhereStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElseWhereStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgElseWhereStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElseWhereStatement** objectArray = (SgElseWhereStatement**) &(SgElseWhereStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgElseWhereStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgElseWhereStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgElseWhereStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElseWhereStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgElseWhereStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgElseWhereStatement** objectArray = (SgElseWhereStatement**) &(SgElseWhereStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgElseWhereStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgElseWhereStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElseWhereStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgElseWhereStatement);

     return memory;
   }



/* #line 90272 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNullifyStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNullifyStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNullifyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNullifyStatement** objectArray = (SgNullifyStatement**) &(SgNullifyStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNullifyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNullifyStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNullifyStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNullifyStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNullifyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNullifyStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNullifyStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNullifyStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNullifyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNullifyStatement** objectArray = (SgNullifyStatement**) &(SgNullifyStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNullifyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNullifyStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNullifyStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNullifyStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNullifyStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNullifyStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgNullifyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNullifyStatement** objectArray = (SgNullifyStatement**) &(SgNullifyStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNullifyStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNullifyStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNullifyStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNullifyStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNullifyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNullifyStatement** objectArray = (SgNullifyStatement**) &(SgNullifyStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNullifyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNullifyStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNullifyStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNullifyStatement);

     return memory;
   }



/* #line 90504 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgArithmeticIfStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgArithmeticIfStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgArithmeticIfStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgArithmeticIfStatement** objectArray = (SgArithmeticIfStatement**) &(SgArithmeticIfStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgArithmeticIfStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgArithmeticIfStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgArithmeticIfStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgArithmeticIfStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgArithmeticIfStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgArithmeticIfStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgArithmeticIfStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgArithmeticIfStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgArithmeticIfStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgArithmeticIfStatement** objectArray = (SgArithmeticIfStatement**) &(SgArithmeticIfStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgArithmeticIfStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgArithmeticIfStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgArithmeticIfStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgArithmeticIfStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgArithmeticIfStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgArithmeticIfStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgArithmeticIfStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgArithmeticIfStatement** objectArray = (SgArithmeticIfStatement**) &(SgArithmeticIfStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgArithmeticIfStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgArithmeticIfStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgArithmeticIfStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgArithmeticIfStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgArithmeticIfStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgArithmeticIfStatement** objectArray = (SgArithmeticIfStatement**) &(SgArithmeticIfStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgArithmeticIfStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgArithmeticIfStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgArithmeticIfStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgArithmeticIfStatement);

     return memory;
   }



/* #line 90736 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAssignStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAssignStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAssignStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssignStatement** objectArray = (SgAssignStatement**) &(SgAssignStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAssignStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAssignStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAssignStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAssignStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAssignStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAssignStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAssignStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAssignStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAssignStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssignStatement** objectArray = (SgAssignStatement**) &(SgAssignStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAssignStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAssignStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAssignStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAssignStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAssignStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAssignStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAssignStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssignStatement** objectArray = (SgAssignStatement**) &(SgAssignStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAssignStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAssignStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAssignStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAssignStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAssignStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAssignStatement** objectArray = (SgAssignStatement**) &(SgAssignStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAssignStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAssignStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAssignStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAssignStatement);

     return memory;
   }



/* #line 90968 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgComputedGotoStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgComputedGotoStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgComputedGotoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgComputedGotoStatement** objectArray = (SgComputedGotoStatement**) &(SgComputedGotoStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgComputedGotoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgComputedGotoStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgComputedGotoStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgComputedGotoStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgComputedGotoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgComputedGotoStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgComputedGotoStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgComputedGotoStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgComputedGotoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgComputedGotoStatement** objectArray = (SgComputedGotoStatement**) &(SgComputedGotoStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgComputedGotoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgComputedGotoStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgComputedGotoStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgComputedGotoStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgComputedGotoStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgComputedGotoStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgComputedGotoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgComputedGotoStatement** objectArray = (SgComputedGotoStatement**) &(SgComputedGotoStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgComputedGotoStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgComputedGotoStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgComputedGotoStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgComputedGotoStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgComputedGotoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgComputedGotoStatement** objectArray = (SgComputedGotoStatement**) &(SgComputedGotoStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgComputedGotoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgComputedGotoStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgComputedGotoStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgComputedGotoStatement);

     return memory;
   }



/* #line 91200 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAssignedGotoStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAssignedGotoStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAssignedGotoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssignedGotoStatement** objectArray = (SgAssignedGotoStatement**) &(SgAssignedGotoStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAssignedGotoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAssignedGotoStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAssignedGotoStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAssignedGotoStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAssignedGotoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAssignedGotoStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAssignedGotoStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAssignedGotoStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAssignedGotoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssignedGotoStatement** objectArray = (SgAssignedGotoStatement**) &(SgAssignedGotoStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAssignedGotoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAssignedGotoStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAssignedGotoStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAssignedGotoStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAssignedGotoStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAssignedGotoStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAssignedGotoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssignedGotoStatement** objectArray = (SgAssignedGotoStatement**) &(SgAssignedGotoStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAssignedGotoStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAssignedGotoStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAssignedGotoStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAssignedGotoStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAssignedGotoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAssignedGotoStatement** objectArray = (SgAssignedGotoStatement**) &(SgAssignedGotoStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAssignedGotoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAssignedGotoStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAssignedGotoStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAssignedGotoStatement);

     return memory;
   }



/* #line 91432 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAllocateStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAllocateStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAllocateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAllocateStatement** objectArray = (SgAllocateStatement**) &(SgAllocateStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAllocateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAllocateStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAllocateStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAllocateStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAllocateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAllocateStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAllocateStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAllocateStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAllocateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAllocateStatement** objectArray = (SgAllocateStatement**) &(SgAllocateStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAllocateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAllocateStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAllocateStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAllocateStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAllocateStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAllocateStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAllocateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAllocateStatement** objectArray = (SgAllocateStatement**) &(SgAllocateStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAllocateStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAllocateStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAllocateStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAllocateStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAllocateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAllocateStatement** objectArray = (SgAllocateStatement**) &(SgAllocateStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAllocateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAllocateStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAllocateStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAllocateStatement);

     return memory;
   }



/* #line 91664 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDeallocateStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDeallocateStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDeallocateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeallocateStatement** objectArray = (SgDeallocateStatement**) &(SgDeallocateStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeallocateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDeallocateStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDeallocateStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDeallocateStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDeallocateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeallocateStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDeallocateStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeallocateStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDeallocateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeallocateStatement** objectArray = (SgDeallocateStatement**) &(SgDeallocateStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeallocateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeallocateStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDeallocateStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDeallocateStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDeallocateStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeallocateStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgDeallocateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeallocateStatement** objectArray = (SgDeallocateStatement**) &(SgDeallocateStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDeallocateStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDeallocateStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDeallocateStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeallocateStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDeallocateStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDeallocateStatement** objectArray = (SgDeallocateStatement**) &(SgDeallocateStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDeallocateStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDeallocateStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeallocateStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDeallocateStatement);

     return memory;
   }



/* #line 91896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUpcNotifyStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUpcNotifyStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcNotifyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcNotifyStatement** objectArray = (SgUpcNotifyStatement**) &(SgUpcNotifyStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcNotifyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUpcNotifyStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUpcNotifyStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUpcNotifyStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUpcNotifyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcNotifyStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUpcNotifyStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcNotifyStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcNotifyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcNotifyStatement** objectArray = (SgUpcNotifyStatement**) &(SgUpcNotifyStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcNotifyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcNotifyStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUpcNotifyStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUpcNotifyStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUpcNotifyStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcNotifyStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcNotifyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcNotifyStatement** objectArray = (SgUpcNotifyStatement**) &(SgUpcNotifyStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUpcNotifyStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUpcNotifyStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUpcNotifyStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcNotifyStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUpcNotifyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUpcNotifyStatement** objectArray = (SgUpcNotifyStatement**) &(SgUpcNotifyStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUpcNotifyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUpcNotifyStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcNotifyStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUpcNotifyStatement);

     return memory;
   }



/* #line 92128 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUpcWaitStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUpcWaitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcWaitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcWaitStatement** objectArray = (SgUpcWaitStatement**) &(SgUpcWaitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcWaitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUpcWaitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUpcWaitStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUpcWaitStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUpcWaitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcWaitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUpcWaitStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcWaitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcWaitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcWaitStatement** objectArray = (SgUpcWaitStatement**) &(SgUpcWaitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcWaitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcWaitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUpcWaitStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUpcWaitStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUpcWaitStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcWaitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcWaitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcWaitStatement** objectArray = (SgUpcWaitStatement**) &(SgUpcWaitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUpcWaitStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUpcWaitStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUpcWaitStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcWaitStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUpcWaitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUpcWaitStatement** objectArray = (SgUpcWaitStatement**) &(SgUpcWaitStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUpcWaitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUpcWaitStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcWaitStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUpcWaitStatement);

     return memory;
   }



/* #line 92360 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUpcBarrierStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUpcBarrierStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcBarrierStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcBarrierStatement** objectArray = (SgUpcBarrierStatement**) &(SgUpcBarrierStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcBarrierStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUpcBarrierStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUpcBarrierStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUpcBarrierStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUpcBarrierStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcBarrierStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUpcBarrierStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcBarrierStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcBarrierStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcBarrierStatement** objectArray = (SgUpcBarrierStatement**) &(SgUpcBarrierStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcBarrierStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcBarrierStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUpcBarrierStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUpcBarrierStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUpcBarrierStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcBarrierStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcBarrierStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcBarrierStatement** objectArray = (SgUpcBarrierStatement**) &(SgUpcBarrierStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUpcBarrierStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUpcBarrierStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUpcBarrierStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcBarrierStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUpcBarrierStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUpcBarrierStatement** objectArray = (SgUpcBarrierStatement**) &(SgUpcBarrierStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUpcBarrierStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUpcBarrierStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcBarrierStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUpcBarrierStatement);

     return memory;
   }



/* #line 92592 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUpcFenceStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUpcFenceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcFenceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcFenceStatement** objectArray = (SgUpcFenceStatement**) &(SgUpcFenceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcFenceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUpcFenceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUpcFenceStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUpcFenceStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUpcFenceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcFenceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUpcFenceStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcFenceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcFenceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcFenceStatement** objectArray = (SgUpcFenceStatement**) &(SgUpcFenceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcFenceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcFenceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUpcFenceStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUpcFenceStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUpcFenceStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcFenceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcFenceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcFenceStatement** objectArray = (SgUpcFenceStatement**) &(SgUpcFenceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUpcFenceStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUpcFenceStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUpcFenceStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcFenceStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUpcFenceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUpcFenceStatement** objectArray = (SgUpcFenceStatement**) &(SgUpcFenceStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUpcFenceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUpcFenceStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcFenceStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUpcFenceStatement);

     return memory;
   }



/* #line 92824 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpBarrierStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpBarrierStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpBarrierStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpBarrierStatement** objectArray = (SgOmpBarrierStatement**) &(SgOmpBarrierStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpBarrierStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpBarrierStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpBarrierStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpBarrierStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpBarrierStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpBarrierStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpBarrierStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpBarrierStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpBarrierStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpBarrierStatement** objectArray = (SgOmpBarrierStatement**) &(SgOmpBarrierStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpBarrierStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpBarrierStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpBarrierStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpBarrierStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpBarrierStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpBarrierStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpBarrierStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpBarrierStatement** objectArray = (SgOmpBarrierStatement**) &(SgOmpBarrierStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpBarrierStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpBarrierStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpBarrierStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpBarrierStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpBarrierStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpBarrierStatement** objectArray = (SgOmpBarrierStatement**) &(SgOmpBarrierStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpBarrierStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpBarrierStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpBarrierStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpBarrierStatement);

     return memory;
   }



/* #line 93056 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpTaskwaitStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpTaskwaitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpTaskwaitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpTaskwaitStatement** objectArray = (SgOmpTaskwaitStatement**) &(SgOmpTaskwaitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpTaskwaitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpTaskwaitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpTaskwaitStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpTaskwaitStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpTaskwaitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpTaskwaitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpTaskwaitStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpTaskwaitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpTaskwaitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpTaskwaitStatement** objectArray = (SgOmpTaskwaitStatement**) &(SgOmpTaskwaitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpTaskwaitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpTaskwaitStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpTaskwaitStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpTaskwaitStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpTaskwaitStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpTaskwaitStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpTaskwaitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpTaskwaitStatement** objectArray = (SgOmpTaskwaitStatement**) &(SgOmpTaskwaitStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpTaskwaitStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpTaskwaitStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpTaskwaitStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpTaskwaitStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpTaskwaitStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpTaskwaitStatement** objectArray = (SgOmpTaskwaitStatement**) &(SgOmpTaskwaitStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpTaskwaitStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpTaskwaitStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpTaskwaitStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpTaskwaitStatement);

     return memory;
   }



/* #line 93288 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpFlushStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpFlushStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpFlushStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpFlushStatement** objectArray = (SgOmpFlushStatement**) &(SgOmpFlushStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpFlushStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpFlushStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpFlushStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpFlushStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpFlushStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpFlushStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpFlushStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpFlushStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpFlushStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpFlushStatement** objectArray = (SgOmpFlushStatement**) &(SgOmpFlushStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpFlushStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpFlushStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpFlushStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpFlushStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpFlushStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpFlushStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpFlushStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpFlushStatement** objectArray = (SgOmpFlushStatement**) &(SgOmpFlushStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpFlushStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpFlushStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpFlushStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpFlushStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpFlushStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpFlushStatement** objectArray = (SgOmpFlushStatement**) &(SgOmpFlushStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpFlushStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpFlushStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpFlushStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpFlushStatement);

     return memory;
   }



/* #line 93520 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpBodyStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpBodyStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpBodyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpBodyStatement** objectArray = (SgOmpBodyStatement**) &(SgOmpBodyStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpBodyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpBodyStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpBodyStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpBodyStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpBodyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpBodyStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpBodyStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpBodyStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpBodyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpBodyStatement** objectArray = (SgOmpBodyStatement**) &(SgOmpBodyStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpBodyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpBodyStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpBodyStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpBodyStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpBodyStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpBodyStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpBodyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpBodyStatement** objectArray = (SgOmpBodyStatement**) &(SgOmpBodyStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpBodyStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpBodyStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpBodyStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpBodyStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpBodyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpBodyStatement** objectArray = (SgOmpBodyStatement**) &(SgOmpBodyStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpBodyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpBodyStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpBodyStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpBodyStatement);

     return memory;
   }



/* #line 93752 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpMasterStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpMasterStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpMasterStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpMasterStatement** objectArray = (SgOmpMasterStatement**) &(SgOmpMasterStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpMasterStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpMasterStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpMasterStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpMasterStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpMasterStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpMasterStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpMasterStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpMasterStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpMasterStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpMasterStatement** objectArray = (SgOmpMasterStatement**) &(SgOmpMasterStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpMasterStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpMasterStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpMasterStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpMasterStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpMasterStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpMasterStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpMasterStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpMasterStatement** objectArray = (SgOmpMasterStatement**) &(SgOmpMasterStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpMasterStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpMasterStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpMasterStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpMasterStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpMasterStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpMasterStatement** objectArray = (SgOmpMasterStatement**) &(SgOmpMasterStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpMasterStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpMasterStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpMasterStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpMasterStatement);

     return memory;
   }



/* #line 93984 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpOrderedStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpOrderedStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpOrderedStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpOrderedStatement** objectArray = (SgOmpOrderedStatement**) &(SgOmpOrderedStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpOrderedStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpOrderedStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpOrderedStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpOrderedStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpOrderedStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpOrderedStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpOrderedStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpOrderedStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpOrderedStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpOrderedStatement** objectArray = (SgOmpOrderedStatement**) &(SgOmpOrderedStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpOrderedStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpOrderedStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpOrderedStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpOrderedStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpOrderedStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpOrderedStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpOrderedStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpOrderedStatement** objectArray = (SgOmpOrderedStatement**) &(SgOmpOrderedStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpOrderedStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpOrderedStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpOrderedStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpOrderedStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpOrderedStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpOrderedStatement** objectArray = (SgOmpOrderedStatement**) &(SgOmpOrderedStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpOrderedStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpOrderedStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpOrderedStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpOrderedStatement);

     return memory;
   }



/* #line 94216 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpCriticalStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpCriticalStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpCriticalStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpCriticalStatement** objectArray = (SgOmpCriticalStatement**) &(SgOmpCriticalStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpCriticalStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpCriticalStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpCriticalStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpCriticalStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpCriticalStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpCriticalStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpCriticalStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpCriticalStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpCriticalStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpCriticalStatement** objectArray = (SgOmpCriticalStatement**) &(SgOmpCriticalStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpCriticalStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpCriticalStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpCriticalStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpCriticalStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpCriticalStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpCriticalStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpCriticalStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpCriticalStatement** objectArray = (SgOmpCriticalStatement**) &(SgOmpCriticalStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpCriticalStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpCriticalStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpCriticalStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpCriticalStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpCriticalStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpCriticalStatement** objectArray = (SgOmpCriticalStatement**) &(SgOmpCriticalStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpCriticalStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpCriticalStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpCriticalStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpCriticalStatement);

     return memory;
   }



/* #line 94448 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpSectionStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpSectionStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSectionStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSectionStatement** objectArray = (SgOmpSectionStatement**) &(SgOmpSectionStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSectionStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpSectionStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpSectionStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpSectionStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpSectionStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSectionStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpSectionStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSectionStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSectionStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSectionStatement** objectArray = (SgOmpSectionStatement**) &(SgOmpSectionStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSectionStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSectionStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpSectionStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpSectionStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpSectionStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSectionStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSectionStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSectionStatement** objectArray = (SgOmpSectionStatement**) &(SgOmpSectionStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpSectionStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpSectionStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpSectionStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSectionStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpSectionStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpSectionStatement** objectArray = (SgOmpSectionStatement**) &(SgOmpSectionStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpSectionStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpSectionStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSectionStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpSectionStatement);

     return memory;
   }



/* #line 94680 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpWorkshareStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpWorkshareStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpWorkshareStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpWorkshareStatement** objectArray = (SgOmpWorkshareStatement**) &(SgOmpWorkshareStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpWorkshareStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpWorkshareStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpWorkshareStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpWorkshareStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpWorkshareStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpWorkshareStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpWorkshareStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpWorkshareStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpWorkshareStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpWorkshareStatement** objectArray = (SgOmpWorkshareStatement**) &(SgOmpWorkshareStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpWorkshareStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpWorkshareStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpWorkshareStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpWorkshareStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpWorkshareStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpWorkshareStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpWorkshareStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpWorkshareStatement** objectArray = (SgOmpWorkshareStatement**) &(SgOmpWorkshareStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpWorkshareStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpWorkshareStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpWorkshareStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpWorkshareStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpWorkshareStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpWorkshareStatement** objectArray = (SgOmpWorkshareStatement**) &(SgOmpWorkshareStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpWorkshareStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpWorkshareStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpWorkshareStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpWorkshareStatement);

     return memory;
   }



/* #line 94912 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpClauseBodyStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpClauseBodyStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpClauseBodyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpClauseBodyStatement** objectArray = (SgOmpClauseBodyStatement**) &(SgOmpClauseBodyStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpClauseBodyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpClauseBodyStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpClauseBodyStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpClauseBodyStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpClauseBodyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpClauseBodyStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpClauseBodyStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpClauseBodyStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpClauseBodyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpClauseBodyStatement** objectArray = (SgOmpClauseBodyStatement**) &(SgOmpClauseBodyStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpClauseBodyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpClauseBodyStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpClauseBodyStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpClauseBodyStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpClauseBodyStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpClauseBodyStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpClauseBodyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpClauseBodyStatement** objectArray = (SgOmpClauseBodyStatement**) &(SgOmpClauseBodyStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpClauseBodyStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpClauseBodyStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpClauseBodyStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpClauseBodyStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpClauseBodyStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpClauseBodyStatement** objectArray = (SgOmpClauseBodyStatement**) &(SgOmpClauseBodyStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpClauseBodyStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpClauseBodyStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpClauseBodyStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpClauseBodyStatement);

     return memory;
   }



/* #line 95144 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpParallelStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpParallelStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpParallelStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpParallelStatement** objectArray = (SgOmpParallelStatement**) &(SgOmpParallelStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpParallelStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpParallelStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpParallelStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpParallelStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpParallelStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpParallelStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpParallelStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpParallelStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpParallelStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpParallelStatement** objectArray = (SgOmpParallelStatement**) &(SgOmpParallelStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpParallelStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpParallelStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpParallelStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpParallelStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpParallelStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpParallelStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpParallelStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpParallelStatement** objectArray = (SgOmpParallelStatement**) &(SgOmpParallelStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpParallelStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpParallelStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpParallelStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpParallelStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpParallelStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpParallelStatement** objectArray = (SgOmpParallelStatement**) &(SgOmpParallelStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpParallelStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpParallelStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpParallelStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpParallelStatement);

     return memory;
   }



/* #line 95376 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpSingleStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpSingleStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSingleStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSingleStatement** objectArray = (SgOmpSingleStatement**) &(SgOmpSingleStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSingleStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpSingleStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpSingleStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpSingleStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpSingleStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSingleStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpSingleStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSingleStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSingleStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSingleStatement** objectArray = (SgOmpSingleStatement**) &(SgOmpSingleStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSingleStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSingleStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpSingleStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpSingleStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpSingleStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSingleStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSingleStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSingleStatement** objectArray = (SgOmpSingleStatement**) &(SgOmpSingleStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpSingleStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpSingleStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpSingleStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSingleStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpSingleStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpSingleStatement** objectArray = (SgOmpSingleStatement**) &(SgOmpSingleStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpSingleStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpSingleStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSingleStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpSingleStatement);

     return memory;
   }



/* #line 95608 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpAtomicStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpAtomicStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpAtomicStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpAtomicStatement** objectArray = (SgOmpAtomicStatement**) &(SgOmpAtomicStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpAtomicStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpAtomicStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpAtomicStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpAtomicStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpAtomicStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpAtomicStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpAtomicStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpAtomicStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpAtomicStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpAtomicStatement** objectArray = (SgOmpAtomicStatement**) &(SgOmpAtomicStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpAtomicStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpAtomicStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpAtomicStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpAtomicStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpAtomicStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpAtomicStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpAtomicStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpAtomicStatement** objectArray = (SgOmpAtomicStatement**) &(SgOmpAtomicStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpAtomicStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpAtomicStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpAtomicStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpAtomicStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpAtomicStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpAtomicStatement** objectArray = (SgOmpAtomicStatement**) &(SgOmpAtomicStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpAtomicStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpAtomicStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpAtomicStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpAtomicStatement);

     return memory;
   }



/* #line 95840 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpTaskStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpTaskStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpTaskStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpTaskStatement** objectArray = (SgOmpTaskStatement**) &(SgOmpTaskStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpTaskStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpTaskStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpTaskStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpTaskStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpTaskStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpTaskStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpTaskStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpTaskStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpTaskStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpTaskStatement** objectArray = (SgOmpTaskStatement**) &(SgOmpTaskStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpTaskStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpTaskStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpTaskStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpTaskStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpTaskStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpTaskStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpTaskStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpTaskStatement** objectArray = (SgOmpTaskStatement**) &(SgOmpTaskStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpTaskStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpTaskStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpTaskStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpTaskStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpTaskStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpTaskStatement** objectArray = (SgOmpTaskStatement**) &(SgOmpTaskStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpTaskStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpTaskStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpTaskStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpTaskStatement);

     return memory;
   }



/* #line 96072 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpForStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpForStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpForStatement** objectArray = (SgOmpForStatement**) &(SgOmpForStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpForStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpForStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpForStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpForStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpForStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpForStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpForStatement** objectArray = (SgOmpForStatement**) &(SgOmpForStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpForStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpForStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpForStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpForStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpForStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpForStatement** objectArray = (SgOmpForStatement**) &(SgOmpForStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpForStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpForStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpForStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpForStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpForStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpForStatement** objectArray = (SgOmpForStatement**) &(SgOmpForStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpForStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpForStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpForStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpForStatement);

     return memory;
   }



/* #line 96304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpDoStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpDoStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDoStatement** objectArray = (SgOmpDoStatement**) &(SgOmpDoStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpDoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpDoStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpDoStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpDoStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpDoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpDoStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpDoStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpDoStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDoStatement** objectArray = (SgOmpDoStatement**) &(SgOmpDoStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpDoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpDoStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpDoStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpDoStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpDoStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpDoStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpDoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpDoStatement** objectArray = (SgOmpDoStatement**) &(SgOmpDoStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpDoStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpDoStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpDoStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpDoStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpDoStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpDoStatement** objectArray = (SgOmpDoStatement**) &(SgOmpDoStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpDoStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpDoStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpDoStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpDoStatement);

     return memory;
   }



/* #line 96536 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpSectionsStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpSectionsStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSectionsStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSectionsStatement** objectArray = (SgOmpSectionsStatement**) &(SgOmpSectionsStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSectionsStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpSectionsStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpSectionsStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpSectionsStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpSectionsStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSectionsStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpSectionsStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSectionsStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSectionsStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSectionsStatement** objectArray = (SgOmpSectionsStatement**) &(SgOmpSectionsStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSectionsStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSectionsStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpSectionsStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpSectionsStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpSectionsStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSectionsStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSectionsStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSectionsStatement** objectArray = (SgOmpSectionsStatement**) &(SgOmpSectionsStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpSectionsStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpSectionsStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpSectionsStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSectionsStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpSectionsStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpSectionsStatement** objectArray = (SgOmpSectionsStatement**) &(SgOmpSectionsStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpSectionsStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpSectionsStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSectionsStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpSectionsStatement);

     return memory;
   }



/* #line 96768 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpTargetStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpTargetStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpTargetStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpTargetStatement** objectArray = (SgOmpTargetStatement**) &(SgOmpTargetStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpTargetStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpTargetStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpTargetStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpTargetStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpTargetStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpTargetStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpTargetStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpTargetStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpTargetStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpTargetStatement** objectArray = (SgOmpTargetStatement**) &(SgOmpTargetStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpTargetStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpTargetStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpTargetStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpTargetStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpTargetStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpTargetStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpTargetStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpTargetStatement** objectArray = (SgOmpTargetStatement**) &(SgOmpTargetStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpTargetStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpTargetStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpTargetStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpTargetStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpTargetStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpTargetStatement** objectArray = (SgOmpTargetStatement**) &(SgOmpTargetStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpTargetStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpTargetStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpTargetStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpTargetStatement);

     return memory;
   }



/* #line 97000 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpTargetDataStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpTargetDataStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpTargetDataStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpTargetDataStatement** objectArray = (SgOmpTargetDataStatement**) &(SgOmpTargetDataStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpTargetDataStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpTargetDataStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpTargetDataStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpTargetDataStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpTargetDataStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpTargetDataStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpTargetDataStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpTargetDataStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpTargetDataStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpTargetDataStatement** objectArray = (SgOmpTargetDataStatement**) &(SgOmpTargetDataStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpTargetDataStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpTargetDataStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpTargetDataStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpTargetDataStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpTargetDataStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpTargetDataStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpTargetDataStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpTargetDataStatement** objectArray = (SgOmpTargetDataStatement**) &(SgOmpTargetDataStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpTargetDataStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpTargetDataStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpTargetDataStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpTargetDataStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpTargetDataStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpTargetDataStatement** objectArray = (SgOmpTargetDataStatement**) &(SgOmpTargetDataStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpTargetDataStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpTargetDataStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpTargetDataStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpTargetDataStatement);

     return memory;
   }



/* #line 97232 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpSimdStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpSimdStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSimdStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSimdStatement** objectArray = (SgOmpSimdStatement**) &(SgOmpSimdStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSimdStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpSimdStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpSimdStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpSimdStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpSimdStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSimdStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpSimdStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSimdStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSimdStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSimdStatement** objectArray = (SgOmpSimdStatement**) &(SgOmpSimdStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpSimdStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpSimdStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpSimdStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpSimdStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpSimdStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpSimdStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpSimdStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpSimdStatement** objectArray = (SgOmpSimdStatement**) &(SgOmpSimdStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpSimdStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpSimdStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpSimdStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSimdStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpSimdStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpSimdStatement** objectArray = (SgOmpSimdStatement**) &(SgOmpSimdStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpSimdStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpSimdStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpSimdStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpSimdStatement);

     return memory;
   }



/* #line 97464 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOmpForSimdStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOmpForSimdStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpForSimdStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpForSimdStatement** objectArray = (SgOmpForSimdStatement**) &(SgOmpForSimdStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpForSimdStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOmpForSimdStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOmpForSimdStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOmpForSimdStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOmpForSimdStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpForSimdStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOmpForSimdStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpForSimdStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpForSimdStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpForSimdStatement** objectArray = (SgOmpForSimdStatement**) &(SgOmpForSimdStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOmpForSimdStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOmpForSimdStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOmpForSimdStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOmpForSimdStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOmpForSimdStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOmpForSimdStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgOmpForSimdStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOmpForSimdStatement** objectArray = (SgOmpForSimdStatement**) &(SgOmpForSimdStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOmpForSimdStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOmpForSimdStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOmpForSimdStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpForSimdStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOmpForSimdStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOmpForSimdStatement** objectArray = (SgOmpForSimdStatement**) &(SgOmpForSimdStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOmpForSimdStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOmpForSimdStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOmpForSimdStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOmpForSimdStatement);

     return memory;
   }



/* #line 97696 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSequenceStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSequenceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSequenceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSequenceStatement** objectArray = (SgSequenceStatement**) &(SgSequenceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSequenceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSequenceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSequenceStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSequenceStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSequenceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSequenceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSequenceStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSequenceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSequenceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSequenceStatement** objectArray = (SgSequenceStatement**) &(SgSequenceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSequenceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSequenceStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSequenceStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSequenceStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSequenceStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSequenceStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSequenceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSequenceStatement** objectArray = (SgSequenceStatement**) &(SgSequenceStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSequenceStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSequenceStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSequenceStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSequenceStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSequenceStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSequenceStatement** objectArray = (SgSequenceStatement**) &(SgSequenceStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSequenceStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSequenceStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSequenceStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSequenceStatement);

     return memory;
   }



/* #line 97928 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgWithStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgWithStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWithStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWithStatement** objectArray = (SgWithStatement**) &(SgWithStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWithStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgWithStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgWithStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgWithStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgWithStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWithStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgWithStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWithStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWithStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWithStatement** objectArray = (SgWithStatement**) &(SgWithStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWithStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWithStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgWithStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgWithStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgWithStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWithStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgWithStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWithStatement** objectArray = (SgWithStatement**) &(SgWithStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgWithStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgWithStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgWithStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWithStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgWithStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgWithStatement** objectArray = (SgWithStatement**) &(SgWithStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgWithStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgWithStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWithStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgWithStatement);

     return memory;
   }



/* #line 98160 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPythonPrintStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPythonPrintStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgPythonPrintStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPythonPrintStmt** objectArray = (SgPythonPrintStmt**) &(SgPythonPrintStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPythonPrintStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPythonPrintStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPythonPrintStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPythonPrintStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPythonPrintStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPythonPrintStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPythonPrintStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPythonPrintStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgPythonPrintStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPythonPrintStmt** objectArray = (SgPythonPrintStmt**) &(SgPythonPrintStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPythonPrintStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPythonPrintStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPythonPrintStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPythonPrintStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPythonPrintStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPythonPrintStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgPythonPrintStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPythonPrintStmt** objectArray = (SgPythonPrintStmt**) &(SgPythonPrintStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPythonPrintStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPythonPrintStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPythonPrintStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPythonPrintStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPythonPrintStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPythonPrintStmt** objectArray = (SgPythonPrintStmt**) &(SgPythonPrintStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPythonPrintStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPythonPrintStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPythonPrintStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPythonPrintStmt);

     return memory;
   }



/* #line 98392 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPassStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPassStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgPassStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPassStatement** objectArray = (SgPassStatement**) &(SgPassStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPassStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPassStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPassStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPassStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPassStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPassStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPassStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPassStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgPassStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPassStatement** objectArray = (SgPassStatement**) &(SgPassStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPassStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPassStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPassStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPassStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPassStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPassStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgPassStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPassStatement** objectArray = (SgPassStatement**) &(SgPassStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPassStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPassStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPassStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPassStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPassStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPassStatement** objectArray = (SgPassStatement**) &(SgPassStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPassStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPassStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPassStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPassStatement);

     return memory;
   }



/* #line 98624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAssertStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAssertStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAssertStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssertStmt** objectArray = (SgAssertStmt**) &(SgAssertStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAssertStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAssertStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAssertStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAssertStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAssertStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAssertStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAssertStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAssertStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAssertStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssertStmt** objectArray = (SgAssertStmt**) &(SgAssertStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAssertStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAssertStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAssertStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAssertStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAssertStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAssertStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAssertStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssertStmt** objectArray = (SgAssertStmt**) &(SgAssertStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAssertStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAssertStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAssertStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAssertStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAssertStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAssertStmt** objectArray = (SgAssertStmt**) &(SgAssertStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAssertStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAssertStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAssertStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAssertStmt);

     return memory;
   }



/* #line 98856 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgExecStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgExecStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgExecStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExecStatement** objectArray = (SgExecStatement**) &(SgExecStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExecStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgExecStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgExecStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgExecStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgExecStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExecStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgExecStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExecStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgExecStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExecStatement** objectArray = (SgExecStatement**) &(SgExecStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExecStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExecStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgExecStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgExecStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgExecStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExecStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgExecStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExecStatement** objectArray = (SgExecStatement**) &(SgExecStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgExecStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgExecStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgExecStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExecStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgExecStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgExecStatement** objectArray = (SgExecStatement**) &(SgExecStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgExecStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgExecStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExecStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgExecStatement);

     return memory;
   }



/* #line 99088 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPythonGlobalStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPythonGlobalStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgPythonGlobalStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPythonGlobalStmt** objectArray = (SgPythonGlobalStmt**) &(SgPythonGlobalStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPythonGlobalStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPythonGlobalStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPythonGlobalStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPythonGlobalStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPythonGlobalStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPythonGlobalStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPythonGlobalStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPythonGlobalStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgPythonGlobalStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPythonGlobalStmt** objectArray = (SgPythonGlobalStmt**) &(SgPythonGlobalStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPythonGlobalStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPythonGlobalStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPythonGlobalStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPythonGlobalStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPythonGlobalStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPythonGlobalStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgPythonGlobalStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPythonGlobalStmt** objectArray = (SgPythonGlobalStmt**) &(SgPythonGlobalStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPythonGlobalStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPythonGlobalStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPythonGlobalStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPythonGlobalStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPythonGlobalStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPythonGlobalStmt** objectArray = (SgPythonGlobalStmt**) &(SgPythonGlobalStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPythonGlobalStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPythonGlobalStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPythonGlobalStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPythonGlobalStmt);

     return memory;
   }



/* #line 99320 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaThrowStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaThrowStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaThrowStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaThrowStatement** objectArray = (SgJavaThrowStatement**) &(SgJavaThrowStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaThrowStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaThrowStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaThrowStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaThrowStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaThrowStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaThrowStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaThrowStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaThrowStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaThrowStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaThrowStatement** objectArray = (SgJavaThrowStatement**) &(SgJavaThrowStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaThrowStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaThrowStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaThrowStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaThrowStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaThrowStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaThrowStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaThrowStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaThrowStatement** objectArray = (SgJavaThrowStatement**) &(SgJavaThrowStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaThrowStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaThrowStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaThrowStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaThrowStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaThrowStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaThrowStatement** objectArray = (SgJavaThrowStatement**) &(SgJavaThrowStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaThrowStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaThrowStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaThrowStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaThrowStatement);

     return memory;
   }



/* #line 99552 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaSynchronizedStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaSynchronizedStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaSynchronizedStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaSynchronizedStatement** objectArray = (SgJavaSynchronizedStatement**) &(SgJavaSynchronizedStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaSynchronizedStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaSynchronizedStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaSynchronizedStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaSynchronizedStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaSynchronizedStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaSynchronizedStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaSynchronizedStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaSynchronizedStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaSynchronizedStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaSynchronizedStatement** objectArray = (SgJavaSynchronizedStatement**) &(SgJavaSynchronizedStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaSynchronizedStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaSynchronizedStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaSynchronizedStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaSynchronizedStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaSynchronizedStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaSynchronizedStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaSynchronizedStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaSynchronizedStatement** objectArray = (SgJavaSynchronizedStatement**) &(SgJavaSynchronizedStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaSynchronizedStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaSynchronizedStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaSynchronizedStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaSynchronizedStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaSynchronizedStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaSynchronizedStatement** objectArray = (SgJavaSynchronizedStatement**) &(SgJavaSynchronizedStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaSynchronizedStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaSynchronizedStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaSynchronizedStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaSynchronizedStatement);

     return memory;
   }



/* #line 99784 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsyncStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsyncStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAsyncStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsyncStmt** objectArray = (SgAsyncStmt**) &(SgAsyncStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsyncStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsyncStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsyncStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsyncStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsyncStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsyncStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsyncStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsyncStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAsyncStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsyncStmt** objectArray = (SgAsyncStmt**) &(SgAsyncStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsyncStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsyncStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsyncStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsyncStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsyncStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsyncStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAsyncStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsyncStmt** objectArray = (SgAsyncStmt**) &(SgAsyncStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsyncStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsyncStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsyncStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsyncStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsyncStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsyncStmt** objectArray = (SgAsyncStmt**) &(SgAsyncStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsyncStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsyncStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsyncStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsyncStmt);

     return memory;
   }



/* #line 100016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFinishStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFinishStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgFinishStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFinishStmt** objectArray = (SgFinishStmt**) &(SgFinishStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFinishStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFinishStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFinishStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFinishStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFinishStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFinishStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFinishStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFinishStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgFinishStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFinishStmt** objectArray = (SgFinishStmt**) &(SgFinishStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFinishStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFinishStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFinishStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFinishStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFinishStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFinishStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgFinishStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFinishStmt** objectArray = (SgFinishStmt**) &(SgFinishStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFinishStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFinishStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFinishStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFinishStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFinishStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFinishStmt** objectArray = (SgFinishStmt**) &(SgFinishStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFinishStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFinishStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFinishStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFinishStmt);

     return memory;
   }



/* #line 100248 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAtStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAtStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAtStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAtStmt** objectArray = (SgAtStmt**) &(SgAtStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAtStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAtStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAtStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAtStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAtStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAtStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAtStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAtStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAtStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAtStmt** objectArray = (SgAtStmt**) &(SgAtStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAtStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAtStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAtStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAtStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAtStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAtStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAtStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAtStmt** objectArray = (SgAtStmt**) &(SgAtStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAtStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAtStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAtStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAtStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAtStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAtStmt** objectArray = (SgAtStmt**) &(SgAtStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAtStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAtStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAtStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAtStmt);

     return memory;
   }



/* #line 100480 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAtomicStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAtomicStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAtomicStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAtomicStmt** objectArray = (SgAtomicStmt**) &(SgAtomicStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAtomicStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAtomicStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAtomicStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAtomicStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAtomicStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAtomicStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAtomicStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAtomicStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAtomicStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAtomicStmt** objectArray = (SgAtomicStmt**) &(SgAtomicStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAtomicStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAtomicStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAtomicStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAtomicStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAtomicStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAtomicStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAtomicStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAtomicStmt** objectArray = (SgAtomicStmt**) &(SgAtomicStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAtomicStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAtomicStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAtomicStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAtomicStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAtomicStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAtomicStmt** objectArray = (SgAtomicStmt**) &(SgAtomicStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAtomicStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAtomicStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAtomicStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAtomicStmt);

     return memory;
   }



/* #line 100712 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgWhenStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgWhenStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgWhenStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWhenStmt** objectArray = (SgWhenStmt**) &(SgWhenStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWhenStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgWhenStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgWhenStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgWhenStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgWhenStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWhenStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgWhenStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWhenStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgWhenStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWhenStmt** objectArray = (SgWhenStmt**) &(SgWhenStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWhenStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWhenStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgWhenStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgWhenStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgWhenStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWhenStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgWhenStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWhenStmt** objectArray = (SgWhenStmt**) &(SgWhenStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgWhenStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgWhenStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgWhenStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWhenStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgWhenStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgWhenStmt** objectArray = (SgWhenStmt**) &(SgWhenStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgWhenStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgWhenStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWhenStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgWhenStmt);

     return memory;
   }



/* #line 100944 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgImageControlStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgImageControlStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgImageControlStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImageControlStatement** objectArray = (SgImageControlStatement**) &(SgImageControlStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgImageControlStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgImageControlStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgImageControlStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgImageControlStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgImageControlStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgImageControlStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgImageControlStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgImageControlStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgImageControlStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImageControlStatement** objectArray = (SgImageControlStatement**) &(SgImageControlStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgImageControlStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgImageControlStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgImageControlStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgImageControlStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgImageControlStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgImageControlStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgImageControlStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImageControlStatement** objectArray = (SgImageControlStatement**) &(SgImageControlStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgImageControlStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgImageControlStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgImageControlStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgImageControlStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgImageControlStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgImageControlStatement** objectArray = (SgImageControlStatement**) &(SgImageControlStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgImageControlStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgImageControlStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgImageControlStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgImageControlStatement);

     return memory;
   }



/* #line 101176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSyncAllStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSyncAllStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSyncAllStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSyncAllStatement** objectArray = (SgSyncAllStatement**) &(SgSyncAllStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSyncAllStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSyncAllStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSyncAllStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSyncAllStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSyncAllStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSyncAllStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSyncAllStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSyncAllStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSyncAllStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSyncAllStatement** objectArray = (SgSyncAllStatement**) &(SgSyncAllStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSyncAllStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSyncAllStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSyncAllStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSyncAllStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSyncAllStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSyncAllStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSyncAllStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSyncAllStatement** objectArray = (SgSyncAllStatement**) &(SgSyncAllStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSyncAllStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSyncAllStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSyncAllStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSyncAllStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSyncAllStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSyncAllStatement** objectArray = (SgSyncAllStatement**) &(SgSyncAllStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSyncAllStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSyncAllStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSyncAllStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSyncAllStatement);

     return memory;
   }



/* #line 101408 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSyncImagesStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSyncImagesStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSyncImagesStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSyncImagesStatement** objectArray = (SgSyncImagesStatement**) &(SgSyncImagesStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSyncImagesStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSyncImagesStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSyncImagesStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSyncImagesStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSyncImagesStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSyncImagesStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSyncImagesStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSyncImagesStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSyncImagesStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSyncImagesStatement** objectArray = (SgSyncImagesStatement**) &(SgSyncImagesStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSyncImagesStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSyncImagesStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSyncImagesStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSyncImagesStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSyncImagesStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSyncImagesStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSyncImagesStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSyncImagesStatement** objectArray = (SgSyncImagesStatement**) &(SgSyncImagesStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSyncImagesStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSyncImagesStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSyncImagesStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSyncImagesStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSyncImagesStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSyncImagesStatement** objectArray = (SgSyncImagesStatement**) &(SgSyncImagesStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSyncImagesStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSyncImagesStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSyncImagesStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSyncImagesStatement);

     return memory;
   }



/* #line 101640 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSyncMemoryStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSyncMemoryStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSyncMemoryStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSyncMemoryStatement** objectArray = (SgSyncMemoryStatement**) &(SgSyncMemoryStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSyncMemoryStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSyncMemoryStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSyncMemoryStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSyncMemoryStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSyncMemoryStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSyncMemoryStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSyncMemoryStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSyncMemoryStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSyncMemoryStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSyncMemoryStatement** objectArray = (SgSyncMemoryStatement**) &(SgSyncMemoryStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSyncMemoryStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSyncMemoryStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSyncMemoryStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSyncMemoryStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSyncMemoryStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSyncMemoryStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSyncMemoryStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSyncMemoryStatement** objectArray = (SgSyncMemoryStatement**) &(SgSyncMemoryStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSyncMemoryStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSyncMemoryStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSyncMemoryStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSyncMemoryStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSyncMemoryStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSyncMemoryStatement** objectArray = (SgSyncMemoryStatement**) &(SgSyncMemoryStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSyncMemoryStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSyncMemoryStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSyncMemoryStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSyncMemoryStatement);

     return memory;
   }



/* #line 101872 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSyncTeamStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSyncTeamStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSyncTeamStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSyncTeamStatement** objectArray = (SgSyncTeamStatement**) &(SgSyncTeamStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSyncTeamStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSyncTeamStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSyncTeamStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSyncTeamStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSyncTeamStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSyncTeamStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSyncTeamStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSyncTeamStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSyncTeamStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSyncTeamStatement** objectArray = (SgSyncTeamStatement**) &(SgSyncTeamStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSyncTeamStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSyncTeamStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSyncTeamStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSyncTeamStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSyncTeamStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSyncTeamStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgSyncTeamStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSyncTeamStatement** objectArray = (SgSyncTeamStatement**) &(SgSyncTeamStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSyncTeamStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSyncTeamStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSyncTeamStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSyncTeamStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSyncTeamStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSyncTeamStatement** objectArray = (SgSyncTeamStatement**) &(SgSyncTeamStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSyncTeamStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSyncTeamStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSyncTeamStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSyncTeamStatement);

     return memory;
   }



/* #line 102104 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLockStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLockStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgLockStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLockStatement** objectArray = (SgLockStatement**) &(SgLockStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLockStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLockStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLockStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLockStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLockStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLockStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLockStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLockStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgLockStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLockStatement** objectArray = (SgLockStatement**) &(SgLockStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLockStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLockStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLockStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLockStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLockStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLockStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgLockStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLockStatement** objectArray = (SgLockStatement**) &(SgLockStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLockStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLockStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLockStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLockStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLockStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLockStatement** objectArray = (SgLockStatement**) &(SgLockStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLockStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLockStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLockStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLockStatement);

     return memory;
   }



/* #line 102336 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUnlockStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUnlockStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUnlockStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnlockStatement** objectArray = (SgUnlockStatement**) &(SgUnlockStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnlockStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUnlockStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUnlockStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUnlockStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUnlockStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnlockStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUnlockStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnlockStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUnlockStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnlockStatement** objectArray = (SgUnlockStatement**) &(SgUnlockStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnlockStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnlockStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUnlockStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUnlockStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUnlockStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnlockStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgUnlockStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnlockStatement** objectArray = (SgUnlockStatement**) &(SgUnlockStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUnlockStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUnlockStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUnlockStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnlockStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUnlockStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUnlockStatement** objectArray = (SgUnlockStatement**) &(SgUnlockStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUnlockStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUnlockStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnlockStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUnlockStatement);

     return memory;
   }



/* #line 102568 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaExitStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaExitStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaExitStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaExitStmt** objectArray = (SgAdaExitStmt**) &(SgAdaExitStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaExitStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaExitStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaExitStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaExitStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaExitStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaExitStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaExitStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaExitStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaExitStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaExitStmt** objectArray = (SgAdaExitStmt**) &(SgAdaExitStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaExitStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaExitStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaExitStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaExitStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaExitStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaExitStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaExitStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaExitStmt** objectArray = (SgAdaExitStmt**) &(SgAdaExitStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaExitStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaExitStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaExitStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaExitStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaExitStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaExitStmt** objectArray = (SgAdaExitStmt**) &(SgAdaExitStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaExitStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaExitStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaExitStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaExitStmt);

     return memory;
   }



/* #line 102800 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaDelayStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaDelayStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDelayStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDelayStmt** objectArray = (SgAdaDelayStmt**) &(SgAdaDelayStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDelayStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaDelayStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaDelayStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaDelayStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaDelayStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDelayStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaDelayStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDelayStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDelayStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDelayStmt** objectArray = (SgAdaDelayStmt**) &(SgAdaDelayStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaDelayStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaDelayStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaDelayStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaDelayStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaDelayStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaDelayStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaDelayStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaDelayStmt** objectArray = (SgAdaDelayStmt**) &(SgAdaDelayStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaDelayStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaDelayStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaDelayStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDelayStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaDelayStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaDelayStmt** objectArray = (SgAdaDelayStmt**) &(SgAdaDelayStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaDelayStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaDelayStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaDelayStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaDelayStmt);

     return memory;
   }



/* #line 103032 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaLoopStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaLoopStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaLoopStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaLoopStmt** objectArray = (SgAdaLoopStmt**) &(SgAdaLoopStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaLoopStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaLoopStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaLoopStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaLoopStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaLoopStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaLoopStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaLoopStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaLoopStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaLoopStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaLoopStmt** objectArray = (SgAdaLoopStmt**) &(SgAdaLoopStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaLoopStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaLoopStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaLoopStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaLoopStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaLoopStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaLoopStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaLoopStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaLoopStmt** objectArray = (SgAdaLoopStmt**) &(SgAdaLoopStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaLoopStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaLoopStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaLoopStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaLoopStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaLoopStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaLoopStmt** objectArray = (SgAdaLoopStmt**) &(SgAdaLoopStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaLoopStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaLoopStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaLoopStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaLoopStmt);

     return memory;
   }



/* #line 103264 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaSelectStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaSelectStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaSelectStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaSelectStmt** objectArray = (SgAdaSelectStmt**) &(SgAdaSelectStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaSelectStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaSelectStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaSelectStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaSelectStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaSelectStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaSelectStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaSelectStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaSelectStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaSelectStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaSelectStmt** objectArray = (SgAdaSelectStmt**) &(SgAdaSelectStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaSelectStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaSelectStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaSelectStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaSelectStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaSelectStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaSelectStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaSelectStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaSelectStmt** objectArray = (SgAdaSelectStmt**) &(SgAdaSelectStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaSelectStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaSelectStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaSelectStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaSelectStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaSelectStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaSelectStmt** objectArray = (SgAdaSelectStmt**) &(SgAdaSelectStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaSelectStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaSelectStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaSelectStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaSelectStmt);

     return memory;
   }



/* #line 103496 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaSelectAlternativeStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaSelectAlternativeStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaSelectAlternativeStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaSelectAlternativeStmt** objectArray = (SgAdaSelectAlternativeStmt**) &(SgAdaSelectAlternativeStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaSelectAlternativeStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaSelectAlternativeStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaSelectAlternativeStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaSelectAlternativeStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaSelectAlternativeStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaSelectAlternativeStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaSelectAlternativeStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaSelectAlternativeStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaSelectAlternativeStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaSelectAlternativeStmt** objectArray = (SgAdaSelectAlternativeStmt**) &(SgAdaSelectAlternativeStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaSelectAlternativeStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaSelectAlternativeStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaSelectAlternativeStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaSelectAlternativeStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaSelectAlternativeStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaSelectAlternativeStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaSelectAlternativeStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaSelectAlternativeStmt** objectArray = (SgAdaSelectAlternativeStmt**) &(SgAdaSelectAlternativeStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaSelectAlternativeStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaSelectAlternativeStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaSelectAlternativeStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaSelectAlternativeStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaSelectAlternativeStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaSelectAlternativeStmt** objectArray = (SgAdaSelectAlternativeStmt**) &(SgAdaSelectAlternativeStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaSelectAlternativeStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaSelectAlternativeStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaSelectAlternativeStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaSelectAlternativeStmt);

     return memory;
   }



/* #line 103728 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaTerminateStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaTerminateStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTerminateStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTerminateStmt** objectArray = (SgAdaTerminateStmt**) &(SgAdaTerminateStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTerminateStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaTerminateStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaTerminateStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaTerminateStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaTerminateStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTerminateStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaTerminateStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTerminateStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTerminateStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTerminateStmt** objectArray = (SgAdaTerminateStmt**) &(SgAdaTerminateStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTerminateStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTerminateStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaTerminateStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaTerminateStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaTerminateStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTerminateStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTerminateStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTerminateStmt** objectArray = (SgAdaTerminateStmt**) &(SgAdaTerminateStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaTerminateStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaTerminateStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaTerminateStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTerminateStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaTerminateStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaTerminateStmt** objectArray = (SgAdaTerminateStmt**) &(SgAdaTerminateStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaTerminateStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaTerminateStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTerminateStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaTerminateStmt);

     return memory;
   }



/* #line 103960 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaUnscopedBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaUnscopedBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaUnscopedBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaUnscopedBlock** objectArray = (SgAdaUnscopedBlock**) &(SgAdaUnscopedBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaUnscopedBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaUnscopedBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaUnscopedBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaUnscopedBlock*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaUnscopedBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaUnscopedBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaUnscopedBlock::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaUnscopedBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaUnscopedBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaUnscopedBlock** objectArray = (SgAdaUnscopedBlock**) &(SgAdaUnscopedBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaUnscopedBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaUnscopedBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaUnscopedBlock object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaUnscopedBlock object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaUnscopedBlock::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaUnscopedBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaUnscopedBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaUnscopedBlock** objectArray = (SgAdaUnscopedBlock**) &(SgAdaUnscopedBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaUnscopedBlock::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaUnscopedBlock::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaUnscopedBlock found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaUnscopedBlock::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaUnscopedBlock::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaUnscopedBlock** objectArray = (SgAdaUnscopedBlock**) &(SgAdaUnscopedBlock::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaUnscopedBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaUnscopedBlock::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaUnscopedBlock::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaUnscopedBlock);

     return memory;
   }



/* #line 104192 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaVariantWhenStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaVariantWhenStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaVariantWhenStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaVariantWhenStmt** objectArray = (SgAdaVariantWhenStmt**) &(SgAdaVariantWhenStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaVariantWhenStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaVariantWhenStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaVariantWhenStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaVariantWhenStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaVariantWhenStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaVariantWhenStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaVariantWhenStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaVariantWhenStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaVariantWhenStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaVariantWhenStmt** objectArray = (SgAdaVariantWhenStmt**) &(SgAdaVariantWhenStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaVariantWhenStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaVariantWhenStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaVariantWhenStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaVariantWhenStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaVariantWhenStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaVariantWhenStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaVariantWhenStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaVariantWhenStmt** objectArray = (SgAdaVariantWhenStmt**) &(SgAdaVariantWhenStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaVariantWhenStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaVariantWhenStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaVariantWhenStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaVariantWhenStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaVariantWhenStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaVariantWhenStmt** objectArray = (SgAdaVariantWhenStmt**) &(SgAdaVariantWhenStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaVariantWhenStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaVariantWhenStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaVariantWhenStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaVariantWhenStmt);

     return memory;
   }



/* #line 104424 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExpression** objectArray = (SgExpression**) &(SgExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExpression** objectArray = (SgExpression**) &(SgExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExpression** objectArray = (SgExpression**) &(SgExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgExpression** objectArray = (SgExpression**) &(SgExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgExpression);

     return memory;
   }



/* #line 104656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUnaryOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUnaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgUnaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnaryOp** objectArray = (SgUnaryOp**) &(SgUnaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUnaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUnaryOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUnaryOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUnaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUnaryOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgUnaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnaryOp** objectArray = (SgUnaryOp**) &(SgUnaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUnaryOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUnaryOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUnaryOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgUnaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnaryOp** objectArray = (SgUnaryOp**) &(SgUnaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUnaryOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUnaryOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUnaryOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnaryOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUnaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUnaryOp** objectArray = (SgUnaryOp**) &(SgUnaryOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUnaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUnaryOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnaryOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUnaryOp);

     return memory;
   }



/* #line 104888 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgExpressionRoot::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgExpressionRoot objectArray [] = *(Memory_Block_List.begin());
     if (SgExpressionRoot::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExpressionRoot** objectArray = (SgExpressionRoot**) &(SgExpressionRoot::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExpressionRoot::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgExpressionRoot::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgExpressionRoot::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgExpressionRoot*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgExpressionRoot::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExpressionRoot::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgExpressionRoot::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExpressionRoot objectArray [] = *(Memory_Block_List.begin());
     if (SgExpressionRoot::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExpressionRoot** objectArray = (SgExpressionRoot**) &(SgExpressionRoot::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExpressionRoot::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExpressionRoot::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgExpressionRoot object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgExpressionRoot object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgExpressionRoot::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExpressionRoot objectArray [] = *(Memory_Block_List.begin());
     if (SgExpressionRoot::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExpressionRoot** objectArray = (SgExpressionRoot**) &(SgExpressionRoot::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgExpressionRoot::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgExpressionRoot::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgExpressionRoot found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExpressionRoot::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgExpressionRoot::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgExpressionRoot** objectArray = (SgExpressionRoot**) &(SgExpressionRoot::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgExpressionRoot::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgExpressionRoot::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExpressionRoot::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgExpressionRoot);

     return memory;
   }



/* #line 105120 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMinusOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMinusOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMinusOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMinusOp** objectArray = (SgMinusOp**) &(SgMinusOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMinusOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMinusOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMinusOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMinusOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMinusOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMinusOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMinusOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMinusOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMinusOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMinusOp** objectArray = (SgMinusOp**) &(SgMinusOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMinusOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMinusOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMinusOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMinusOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMinusOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMinusOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMinusOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMinusOp** objectArray = (SgMinusOp**) &(SgMinusOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMinusOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMinusOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMinusOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMinusOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMinusOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMinusOp** objectArray = (SgMinusOp**) &(SgMinusOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMinusOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMinusOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMinusOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMinusOp);

     return memory;
   }



/* #line 105352 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUnaryAddOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUnaryAddOp objectArray [] = *(Memory_Block_List.begin());
     if (SgUnaryAddOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnaryAddOp** objectArray = (SgUnaryAddOp**) &(SgUnaryAddOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnaryAddOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUnaryAddOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUnaryAddOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUnaryAddOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUnaryAddOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnaryAddOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUnaryAddOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnaryAddOp objectArray [] = *(Memory_Block_List.begin());
     if (SgUnaryAddOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnaryAddOp** objectArray = (SgUnaryAddOp**) &(SgUnaryAddOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnaryAddOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnaryAddOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUnaryAddOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUnaryAddOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUnaryAddOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnaryAddOp objectArray [] = *(Memory_Block_List.begin());
     if (SgUnaryAddOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnaryAddOp** objectArray = (SgUnaryAddOp**) &(SgUnaryAddOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUnaryAddOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUnaryAddOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUnaryAddOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnaryAddOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUnaryAddOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUnaryAddOp** objectArray = (SgUnaryAddOp**) &(SgUnaryAddOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUnaryAddOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUnaryAddOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnaryAddOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUnaryAddOp);

     return memory;
   }



/* #line 105584 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNotOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNotOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNotOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNotOp** objectArray = (SgNotOp**) &(SgNotOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNotOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNotOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNotOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNotOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNotOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNotOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNotOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNotOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNotOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNotOp** objectArray = (SgNotOp**) &(SgNotOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNotOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNotOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNotOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNotOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNotOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNotOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNotOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNotOp** objectArray = (SgNotOp**) &(SgNotOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNotOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNotOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNotOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNotOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNotOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNotOp** objectArray = (SgNotOp**) &(SgNotOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNotOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNotOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNotOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNotOp);

     return memory;
   }



/* #line 105816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPointerDerefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPointerDerefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgPointerDerefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPointerDerefExp** objectArray = (SgPointerDerefExp**) &(SgPointerDerefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPointerDerefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPointerDerefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPointerDerefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPointerDerefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPointerDerefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPointerDerefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPointerDerefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPointerDerefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgPointerDerefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPointerDerefExp** objectArray = (SgPointerDerefExp**) &(SgPointerDerefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPointerDerefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPointerDerefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPointerDerefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPointerDerefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPointerDerefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPointerDerefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgPointerDerefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPointerDerefExp** objectArray = (SgPointerDerefExp**) &(SgPointerDerefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPointerDerefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPointerDerefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPointerDerefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPointerDerefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPointerDerefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPointerDerefExp** objectArray = (SgPointerDerefExp**) &(SgPointerDerefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPointerDerefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPointerDerefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPointerDerefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPointerDerefExp);

     return memory;
   }



/* #line 106048 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAddressOfOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAddressOfOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAddressOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAddressOfOp** objectArray = (SgAddressOfOp**) &(SgAddressOfOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAddressOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAddressOfOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAddressOfOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAddressOfOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAddressOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAddressOfOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAddressOfOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAddressOfOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAddressOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAddressOfOp** objectArray = (SgAddressOfOp**) &(SgAddressOfOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAddressOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAddressOfOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAddressOfOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAddressOfOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAddressOfOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAddressOfOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAddressOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAddressOfOp** objectArray = (SgAddressOfOp**) &(SgAddressOfOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAddressOfOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAddressOfOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAddressOfOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAddressOfOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAddressOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAddressOfOp** objectArray = (SgAddressOfOp**) &(SgAddressOfOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAddressOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAddressOfOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAddressOfOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAddressOfOp);

     return memory;
   }



/* #line 106280 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMinusMinusOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMinusMinusOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMinusMinusOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMinusMinusOp** objectArray = (SgMinusMinusOp**) &(SgMinusMinusOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMinusMinusOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMinusMinusOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMinusMinusOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMinusMinusOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMinusMinusOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMinusMinusOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMinusMinusOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMinusMinusOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMinusMinusOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMinusMinusOp** objectArray = (SgMinusMinusOp**) &(SgMinusMinusOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMinusMinusOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMinusMinusOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMinusMinusOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMinusMinusOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMinusMinusOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMinusMinusOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMinusMinusOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMinusMinusOp** objectArray = (SgMinusMinusOp**) &(SgMinusMinusOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMinusMinusOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMinusMinusOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMinusMinusOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMinusMinusOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMinusMinusOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMinusMinusOp** objectArray = (SgMinusMinusOp**) &(SgMinusMinusOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMinusMinusOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMinusMinusOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMinusMinusOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMinusMinusOp);

     return memory;
   }



/* #line 106512 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPlusPlusOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPlusPlusOp objectArray [] = *(Memory_Block_List.begin());
     if (SgPlusPlusOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPlusPlusOp** objectArray = (SgPlusPlusOp**) &(SgPlusPlusOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPlusPlusOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPlusPlusOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPlusPlusOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPlusPlusOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPlusPlusOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPlusPlusOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPlusPlusOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPlusPlusOp objectArray [] = *(Memory_Block_List.begin());
     if (SgPlusPlusOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPlusPlusOp** objectArray = (SgPlusPlusOp**) &(SgPlusPlusOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPlusPlusOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPlusPlusOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPlusPlusOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPlusPlusOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPlusPlusOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPlusPlusOp objectArray [] = *(Memory_Block_List.begin());
     if (SgPlusPlusOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPlusPlusOp** objectArray = (SgPlusPlusOp**) &(SgPlusPlusOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPlusPlusOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPlusPlusOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPlusPlusOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPlusPlusOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPlusPlusOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPlusPlusOp** objectArray = (SgPlusPlusOp**) &(SgPlusPlusOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPlusPlusOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPlusPlusOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPlusPlusOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPlusPlusOp);

     return memory;
   }



/* #line 106744 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBitComplementOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBitComplementOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitComplementOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitComplementOp** objectArray = (SgBitComplementOp**) &(SgBitComplementOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBitComplementOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBitComplementOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBitComplementOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBitComplementOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBitComplementOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBitComplementOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBitComplementOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBitComplementOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitComplementOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitComplementOp** objectArray = (SgBitComplementOp**) &(SgBitComplementOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBitComplementOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBitComplementOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBitComplementOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBitComplementOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBitComplementOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBitComplementOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitComplementOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitComplementOp** objectArray = (SgBitComplementOp**) &(SgBitComplementOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBitComplementOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBitComplementOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBitComplementOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBitComplementOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBitComplementOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBitComplementOp** objectArray = (SgBitComplementOp**) &(SgBitComplementOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBitComplementOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBitComplementOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBitComplementOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBitComplementOp);

     return memory;
   }



/* #line 106976 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCastExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCastExp objectArray [] = *(Memory_Block_List.begin());
     if (SgCastExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCastExp** objectArray = (SgCastExp**) &(SgCastExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCastExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCastExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCastExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCastExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCastExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCastExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCastExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCastExp objectArray [] = *(Memory_Block_List.begin());
     if (SgCastExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCastExp** objectArray = (SgCastExp**) &(SgCastExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCastExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCastExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCastExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCastExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCastExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCastExp objectArray [] = *(Memory_Block_List.begin());
     if (SgCastExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCastExp** objectArray = (SgCastExp**) &(SgCastExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCastExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCastExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCastExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCastExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCastExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCastExp** objectArray = (SgCastExp**) &(SgCastExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCastExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCastExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCastExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCastExp);

     return memory;
   }



/* #line 107208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgThrowOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgThrowOp objectArray [] = *(Memory_Block_List.begin());
     if (SgThrowOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgThrowOp** objectArray = (SgThrowOp**) &(SgThrowOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgThrowOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgThrowOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgThrowOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgThrowOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgThrowOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgThrowOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgThrowOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgThrowOp objectArray [] = *(Memory_Block_List.begin());
     if (SgThrowOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgThrowOp** objectArray = (SgThrowOp**) &(SgThrowOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgThrowOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgThrowOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgThrowOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgThrowOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgThrowOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgThrowOp objectArray [] = *(Memory_Block_List.begin());
     if (SgThrowOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgThrowOp** objectArray = (SgThrowOp**) &(SgThrowOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgThrowOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgThrowOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgThrowOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgThrowOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgThrowOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgThrowOp** objectArray = (SgThrowOp**) &(SgThrowOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgThrowOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgThrowOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgThrowOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgThrowOp);

     return memory;
   }



/* #line 107440 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgRealPartOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgRealPartOp objectArray [] = *(Memory_Block_List.begin());
     if (SgRealPartOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRealPartOp** objectArray = (SgRealPartOp**) &(SgRealPartOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRealPartOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgRealPartOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgRealPartOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgRealPartOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgRealPartOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRealPartOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgRealPartOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRealPartOp objectArray [] = *(Memory_Block_List.begin());
     if (SgRealPartOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRealPartOp** objectArray = (SgRealPartOp**) &(SgRealPartOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRealPartOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRealPartOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgRealPartOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgRealPartOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgRealPartOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRealPartOp objectArray [] = *(Memory_Block_List.begin());
     if (SgRealPartOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRealPartOp** objectArray = (SgRealPartOp**) &(SgRealPartOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgRealPartOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgRealPartOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgRealPartOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRealPartOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgRealPartOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgRealPartOp** objectArray = (SgRealPartOp**) &(SgRealPartOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgRealPartOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgRealPartOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRealPartOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgRealPartOp);

     return memory;
   }



/* #line 107672 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgImagPartOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgImagPartOp objectArray [] = *(Memory_Block_List.begin());
     if (SgImagPartOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImagPartOp** objectArray = (SgImagPartOp**) &(SgImagPartOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgImagPartOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgImagPartOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgImagPartOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgImagPartOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgImagPartOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgImagPartOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgImagPartOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgImagPartOp objectArray [] = *(Memory_Block_List.begin());
     if (SgImagPartOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImagPartOp** objectArray = (SgImagPartOp**) &(SgImagPartOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgImagPartOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgImagPartOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgImagPartOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgImagPartOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgImagPartOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgImagPartOp objectArray [] = *(Memory_Block_List.begin());
     if (SgImagPartOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImagPartOp** objectArray = (SgImagPartOp**) &(SgImagPartOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgImagPartOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgImagPartOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgImagPartOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgImagPartOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgImagPartOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgImagPartOp** objectArray = (SgImagPartOp**) &(SgImagPartOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgImagPartOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgImagPartOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgImagPartOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgImagPartOp);

     return memory;
   }



/* #line 107904 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgConjugateOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgConjugateOp objectArray [] = *(Memory_Block_List.begin());
     if (SgConjugateOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConjugateOp** objectArray = (SgConjugateOp**) &(SgConjugateOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgConjugateOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgConjugateOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgConjugateOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgConjugateOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgConjugateOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgConjugateOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgConjugateOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgConjugateOp objectArray [] = *(Memory_Block_List.begin());
     if (SgConjugateOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConjugateOp** objectArray = (SgConjugateOp**) &(SgConjugateOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgConjugateOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgConjugateOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgConjugateOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgConjugateOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgConjugateOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgConjugateOp objectArray [] = *(Memory_Block_List.begin());
     if (SgConjugateOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConjugateOp** objectArray = (SgConjugateOp**) &(SgConjugateOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgConjugateOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgConjugateOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgConjugateOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgConjugateOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgConjugateOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgConjugateOp** objectArray = (SgConjugateOp**) &(SgConjugateOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgConjugateOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgConjugateOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgConjugateOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgConjugateOp);

     return memory;
   }



/* #line 108136 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUserDefinedUnaryOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUserDefinedUnaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgUserDefinedUnaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUserDefinedUnaryOp** objectArray = (SgUserDefinedUnaryOp**) &(SgUserDefinedUnaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUserDefinedUnaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUserDefinedUnaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUserDefinedUnaryOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUserDefinedUnaryOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUserDefinedUnaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUserDefinedUnaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUserDefinedUnaryOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUserDefinedUnaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgUserDefinedUnaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUserDefinedUnaryOp** objectArray = (SgUserDefinedUnaryOp**) &(SgUserDefinedUnaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUserDefinedUnaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUserDefinedUnaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUserDefinedUnaryOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUserDefinedUnaryOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUserDefinedUnaryOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUserDefinedUnaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgUserDefinedUnaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUserDefinedUnaryOp** objectArray = (SgUserDefinedUnaryOp**) &(SgUserDefinedUnaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUserDefinedUnaryOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUserDefinedUnaryOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUserDefinedUnaryOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUserDefinedUnaryOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUserDefinedUnaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUserDefinedUnaryOp** objectArray = (SgUserDefinedUnaryOp**) &(SgUserDefinedUnaryOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUserDefinedUnaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUserDefinedUnaryOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUserDefinedUnaryOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUserDefinedUnaryOp);

     return memory;
   }



/* #line 108368 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMatrixTransposeOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMatrixTransposeOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMatrixTransposeOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMatrixTransposeOp** objectArray = (SgMatrixTransposeOp**) &(SgMatrixTransposeOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMatrixTransposeOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMatrixTransposeOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMatrixTransposeOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMatrixTransposeOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMatrixTransposeOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMatrixTransposeOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMatrixTransposeOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMatrixTransposeOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMatrixTransposeOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMatrixTransposeOp** objectArray = (SgMatrixTransposeOp**) &(SgMatrixTransposeOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMatrixTransposeOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMatrixTransposeOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMatrixTransposeOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMatrixTransposeOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMatrixTransposeOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMatrixTransposeOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMatrixTransposeOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMatrixTransposeOp** objectArray = (SgMatrixTransposeOp**) &(SgMatrixTransposeOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMatrixTransposeOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMatrixTransposeOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMatrixTransposeOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMatrixTransposeOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMatrixTransposeOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMatrixTransposeOp** objectArray = (SgMatrixTransposeOp**) &(SgMatrixTransposeOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMatrixTransposeOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMatrixTransposeOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMatrixTransposeOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMatrixTransposeOp);

     return memory;
   }



/* #line 108600 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAbsOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAbsOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAbsOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAbsOp** objectArray = (SgAbsOp**) &(SgAbsOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAbsOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAbsOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAbsOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAbsOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAbsOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAbsOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAbsOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAbsOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAbsOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAbsOp** objectArray = (SgAbsOp**) &(SgAbsOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAbsOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAbsOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAbsOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAbsOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAbsOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAbsOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAbsOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAbsOp** objectArray = (SgAbsOp**) &(SgAbsOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAbsOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAbsOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAbsOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAbsOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAbsOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAbsOp** objectArray = (SgAbsOp**) &(SgAbsOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAbsOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAbsOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAbsOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAbsOp);

     return memory;
   }



/* #line 108832 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBinaryOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBinaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBinaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBinaryOp** objectArray = (SgBinaryOp**) &(SgBinaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBinaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBinaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBinaryOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBinaryOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBinaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBinaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBinaryOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBinaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBinaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBinaryOp** objectArray = (SgBinaryOp**) &(SgBinaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBinaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBinaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBinaryOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBinaryOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBinaryOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBinaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBinaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBinaryOp** objectArray = (SgBinaryOp**) &(SgBinaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBinaryOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBinaryOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBinaryOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBinaryOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBinaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBinaryOp** objectArray = (SgBinaryOp**) &(SgBinaryOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBinaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBinaryOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBinaryOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBinaryOp);

     return memory;
   }



/* #line 109064 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgArrowExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgArrowExp objectArray [] = *(Memory_Block_List.begin());
     if (SgArrowExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgArrowExp** objectArray = (SgArrowExp**) &(SgArrowExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgArrowExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgArrowExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgArrowExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgArrowExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgArrowExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgArrowExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgArrowExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgArrowExp objectArray [] = *(Memory_Block_List.begin());
     if (SgArrowExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgArrowExp** objectArray = (SgArrowExp**) &(SgArrowExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgArrowExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgArrowExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgArrowExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgArrowExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgArrowExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgArrowExp objectArray [] = *(Memory_Block_List.begin());
     if (SgArrowExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgArrowExp** objectArray = (SgArrowExp**) &(SgArrowExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgArrowExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgArrowExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgArrowExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgArrowExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgArrowExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgArrowExp** objectArray = (SgArrowExp**) &(SgArrowExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgArrowExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgArrowExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgArrowExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgArrowExp);

     return memory;
   }



/* #line 109296 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDotExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDotExp objectArray [] = *(Memory_Block_List.begin());
     if (SgDotExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDotExp** objectArray = (SgDotExp**) &(SgDotExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDotExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDotExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDotExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDotExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDotExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDotExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDotExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDotExp objectArray [] = *(Memory_Block_List.begin());
     if (SgDotExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDotExp** objectArray = (SgDotExp**) &(SgDotExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDotExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDotExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDotExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDotExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDotExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDotExp objectArray [] = *(Memory_Block_List.begin());
     if (SgDotExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDotExp** objectArray = (SgDotExp**) &(SgDotExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDotExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDotExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDotExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDotExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDotExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDotExp** objectArray = (SgDotExp**) &(SgDotExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDotExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDotExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDotExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDotExp);

     return memory;
   }



/* #line 109528 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDotStarOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDotStarOp objectArray [] = *(Memory_Block_List.begin());
     if (SgDotStarOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDotStarOp** objectArray = (SgDotStarOp**) &(SgDotStarOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDotStarOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDotStarOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDotStarOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDotStarOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDotStarOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDotStarOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDotStarOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDotStarOp objectArray [] = *(Memory_Block_List.begin());
     if (SgDotStarOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDotStarOp** objectArray = (SgDotStarOp**) &(SgDotStarOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDotStarOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDotStarOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDotStarOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDotStarOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDotStarOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDotStarOp objectArray [] = *(Memory_Block_List.begin());
     if (SgDotStarOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDotStarOp** objectArray = (SgDotStarOp**) &(SgDotStarOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDotStarOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDotStarOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDotStarOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDotStarOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDotStarOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDotStarOp** objectArray = (SgDotStarOp**) &(SgDotStarOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDotStarOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDotStarOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDotStarOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDotStarOp);

     return memory;
   }



/* #line 109760 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgArrowStarOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgArrowStarOp objectArray [] = *(Memory_Block_List.begin());
     if (SgArrowStarOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgArrowStarOp** objectArray = (SgArrowStarOp**) &(SgArrowStarOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgArrowStarOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgArrowStarOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgArrowStarOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgArrowStarOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgArrowStarOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgArrowStarOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgArrowStarOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgArrowStarOp objectArray [] = *(Memory_Block_List.begin());
     if (SgArrowStarOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgArrowStarOp** objectArray = (SgArrowStarOp**) &(SgArrowStarOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgArrowStarOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgArrowStarOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgArrowStarOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgArrowStarOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgArrowStarOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgArrowStarOp objectArray [] = *(Memory_Block_List.begin());
     if (SgArrowStarOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgArrowStarOp** objectArray = (SgArrowStarOp**) &(SgArrowStarOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgArrowStarOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgArrowStarOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgArrowStarOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgArrowStarOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgArrowStarOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgArrowStarOp** objectArray = (SgArrowStarOp**) &(SgArrowStarOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgArrowStarOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgArrowStarOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgArrowStarOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgArrowStarOp);

     return memory;
   }



/* #line 109992 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgEqualityOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgEqualityOp objectArray [] = *(Memory_Block_List.begin());
     if (SgEqualityOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEqualityOp** objectArray = (SgEqualityOp**) &(SgEqualityOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEqualityOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgEqualityOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgEqualityOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgEqualityOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgEqualityOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEqualityOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgEqualityOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEqualityOp objectArray [] = *(Memory_Block_List.begin());
     if (SgEqualityOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEqualityOp** objectArray = (SgEqualityOp**) &(SgEqualityOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEqualityOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEqualityOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgEqualityOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgEqualityOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgEqualityOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEqualityOp objectArray [] = *(Memory_Block_List.begin());
     if (SgEqualityOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEqualityOp** objectArray = (SgEqualityOp**) &(SgEqualityOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgEqualityOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgEqualityOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgEqualityOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEqualityOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgEqualityOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgEqualityOp** objectArray = (SgEqualityOp**) &(SgEqualityOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgEqualityOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgEqualityOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEqualityOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgEqualityOp);

     return memory;
   }



/* #line 110224 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLessThanOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLessThanOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLessThanOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLessThanOp** objectArray = (SgLessThanOp**) &(SgLessThanOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLessThanOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLessThanOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLessThanOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLessThanOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLessThanOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLessThanOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLessThanOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLessThanOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLessThanOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLessThanOp** objectArray = (SgLessThanOp**) &(SgLessThanOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLessThanOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLessThanOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLessThanOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLessThanOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLessThanOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLessThanOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLessThanOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLessThanOp** objectArray = (SgLessThanOp**) &(SgLessThanOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLessThanOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLessThanOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLessThanOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLessThanOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLessThanOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLessThanOp** objectArray = (SgLessThanOp**) &(SgLessThanOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLessThanOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLessThanOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLessThanOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLessThanOp);

     return memory;
   }



/* #line 110456 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgGreaterThanOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgGreaterThanOp objectArray [] = *(Memory_Block_List.begin());
     if (SgGreaterThanOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGreaterThanOp** objectArray = (SgGreaterThanOp**) &(SgGreaterThanOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGreaterThanOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgGreaterThanOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgGreaterThanOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgGreaterThanOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgGreaterThanOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGreaterThanOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgGreaterThanOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGreaterThanOp objectArray [] = *(Memory_Block_List.begin());
     if (SgGreaterThanOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGreaterThanOp** objectArray = (SgGreaterThanOp**) &(SgGreaterThanOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGreaterThanOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGreaterThanOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgGreaterThanOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgGreaterThanOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgGreaterThanOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGreaterThanOp objectArray [] = *(Memory_Block_List.begin());
     if (SgGreaterThanOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGreaterThanOp** objectArray = (SgGreaterThanOp**) &(SgGreaterThanOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgGreaterThanOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgGreaterThanOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgGreaterThanOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGreaterThanOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgGreaterThanOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgGreaterThanOp** objectArray = (SgGreaterThanOp**) &(SgGreaterThanOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgGreaterThanOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgGreaterThanOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGreaterThanOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgGreaterThanOp);

     return memory;
   }



/* #line 110688 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNotEqualOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNotEqualOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNotEqualOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNotEqualOp** objectArray = (SgNotEqualOp**) &(SgNotEqualOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNotEqualOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNotEqualOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNotEqualOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNotEqualOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNotEqualOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNotEqualOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNotEqualOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNotEqualOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNotEqualOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNotEqualOp** objectArray = (SgNotEqualOp**) &(SgNotEqualOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNotEqualOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNotEqualOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNotEqualOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNotEqualOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNotEqualOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNotEqualOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNotEqualOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNotEqualOp** objectArray = (SgNotEqualOp**) &(SgNotEqualOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNotEqualOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNotEqualOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNotEqualOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNotEqualOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNotEqualOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNotEqualOp** objectArray = (SgNotEqualOp**) &(SgNotEqualOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNotEqualOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNotEqualOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNotEqualOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNotEqualOp);

     return memory;
   }



/* #line 110920 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLessOrEqualOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLessOrEqualOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLessOrEqualOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLessOrEqualOp** objectArray = (SgLessOrEqualOp**) &(SgLessOrEqualOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLessOrEqualOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLessOrEqualOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLessOrEqualOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLessOrEqualOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLessOrEqualOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLessOrEqualOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLessOrEqualOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLessOrEqualOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLessOrEqualOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLessOrEqualOp** objectArray = (SgLessOrEqualOp**) &(SgLessOrEqualOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLessOrEqualOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLessOrEqualOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLessOrEqualOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLessOrEqualOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLessOrEqualOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLessOrEqualOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLessOrEqualOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLessOrEqualOp** objectArray = (SgLessOrEqualOp**) &(SgLessOrEqualOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLessOrEqualOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLessOrEqualOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLessOrEqualOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLessOrEqualOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLessOrEqualOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLessOrEqualOp** objectArray = (SgLessOrEqualOp**) &(SgLessOrEqualOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLessOrEqualOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLessOrEqualOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLessOrEqualOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLessOrEqualOp);

     return memory;
   }



/* #line 111152 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgGreaterOrEqualOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgGreaterOrEqualOp objectArray [] = *(Memory_Block_List.begin());
     if (SgGreaterOrEqualOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGreaterOrEqualOp** objectArray = (SgGreaterOrEqualOp**) &(SgGreaterOrEqualOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGreaterOrEqualOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgGreaterOrEqualOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgGreaterOrEqualOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgGreaterOrEqualOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgGreaterOrEqualOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGreaterOrEqualOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgGreaterOrEqualOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGreaterOrEqualOp objectArray [] = *(Memory_Block_List.begin());
     if (SgGreaterOrEqualOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGreaterOrEqualOp** objectArray = (SgGreaterOrEqualOp**) &(SgGreaterOrEqualOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgGreaterOrEqualOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgGreaterOrEqualOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgGreaterOrEqualOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgGreaterOrEqualOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgGreaterOrEqualOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgGreaterOrEqualOp objectArray [] = *(Memory_Block_List.begin());
     if (SgGreaterOrEqualOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgGreaterOrEqualOp** objectArray = (SgGreaterOrEqualOp**) &(SgGreaterOrEqualOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgGreaterOrEqualOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgGreaterOrEqualOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgGreaterOrEqualOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGreaterOrEqualOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgGreaterOrEqualOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgGreaterOrEqualOp** objectArray = (SgGreaterOrEqualOp**) &(SgGreaterOrEqualOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgGreaterOrEqualOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgGreaterOrEqualOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgGreaterOrEqualOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgGreaterOrEqualOp);

     return memory;
   }



/* #line 111384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAddOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAddOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAddOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAddOp** objectArray = (SgAddOp**) &(SgAddOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAddOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAddOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAddOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAddOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAddOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAddOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAddOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAddOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAddOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAddOp** objectArray = (SgAddOp**) &(SgAddOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAddOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAddOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAddOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAddOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAddOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAddOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAddOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAddOp** objectArray = (SgAddOp**) &(SgAddOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAddOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAddOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAddOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAddOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAddOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAddOp** objectArray = (SgAddOp**) &(SgAddOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAddOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAddOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAddOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAddOp);

     return memory;
   }



/* #line 111616 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSubtractOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSubtractOp objectArray [] = *(Memory_Block_List.begin());
     if (SgSubtractOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSubtractOp** objectArray = (SgSubtractOp**) &(SgSubtractOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSubtractOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSubtractOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSubtractOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSubtractOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSubtractOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSubtractOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSubtractOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSubtractOp objectArray [] = *(Memory_Block_List.begin());
     if (SgSubtractOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSubtractOp** objectArray = (SgSubtractOp**) &(SgSubtractOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSubtractOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSubtractOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSubtractOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSubtractOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSubtractOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSubtractOp objectArray [] = *(Memory_Block_List.begin());
     if (SgSubtractOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSubtractOp** objectArray = (SgSubtractOp**) &(SgSubtractOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSubtractOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSubtractOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSubtractOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSubtractOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSubtractOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSubtractOp** objectArray = (SgSubtractOp**) &(SgSubtractOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSubtractOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSubtractOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSubtractOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSubtractOp);

     return memory;
   }



/* #line 111848 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMultiplyOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMultiplyOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMultiplyOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMultiplyOp** objectArray = (SgMultiplyOp**) &(SgMultiplyOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMultiplyOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMultiplyOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMultiplyOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMultiplyOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMultiplyOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMultiplyOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMultiplyOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMultiplyOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMultiplyOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMultiplyOp** objectArray = (SgMultiplyOp**) &(SgMultiplyOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMultiplyOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMultiplyOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMultiplyOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMultiplyOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMultiplyOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMultiplyOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMultiplyOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMultiplyOp** objectArray = (SgMultiplyOp**) &(SgMultiplyOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMultiplyOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMultiplyOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMultiplyOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMultiplyOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMultiplyOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMultiplyOp** objectArray = (SgMultiplyOp**) &(SgMultiplyOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMultiplyOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMultiplyOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMultiplyOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMultiplyOp);

     return memory;
   }



/* #line 112080 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDivideOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDivideOp** objectArray = (SgDivideOp**) &(SgDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDivideOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDivideOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDivideOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDivideOp** objectArray = (SgDivideOp**) &(SgDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDivideOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDivideOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDivideOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDivideOp** objectArray = (SgDivideOp**) &(SgDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDivideOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDivideOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDivideOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDivideOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDivideOp** objectArray = (SgDivideOp**) &(SgDivideOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDivideOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDivideOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDivideOp);

     return memory;
   }



/* #line 112312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIntegerDivideOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIntegerDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIntegerDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntegerDivideOp** objectArray = (SgIntegerDivideOp**) &(SgIntegerDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIntegerDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIntegerDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIntegerDivideOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIntegerDivideOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIntegerDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIntegerDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIntegerDivideOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIntegerDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIntegerDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntegerDivideOp** objectArray = (SgIntegerDivideOp**) &(SgIntegerDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIntegerDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIntegerDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIntegerDivideOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIntegerDivideOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIntegerDivideOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIntegerDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIntegerDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntegerDivideOp** objectArray = (SgIntegerDivideOp**) &(SgIntegerDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIntegerDivideOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIntegerDivideOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIntegerDivideOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIntegerDivideOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIntegerDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIntegerDivideOp** objectArray = (SgIntegerDivideOp**) &(SgIntegerDivideOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIntegerDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIntegerDivideOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIntegerDivideOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIntegerDivideOp);

     return memory;
   }



/* #line 112544 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgModOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgModOp objectArray [] = *(Memory_Block_List.begin());
     if (SgModOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModOp** objectArray = (SgModOp**) &(SgModOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgModOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgModOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgModOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgModOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgModOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModOp objectArray [] = *(Memory_Block_List.begin());
     if (SgModOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModOp** objectArray = (SgModOp**) &(SgModOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgModOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgModOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgModOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModOp objectArray [] = *(Memory_Block_List.begin());
     if (SgModOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModOp** objectArray = (SgModOp**) &(SgModOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgModOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgModOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgModOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgModOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgModOp** objectArray = (SgModOp**) &(SgModOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgModOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgModOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgModOp);

     return memory;
   }



/* #line 112776 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAndOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAndOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAndOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAndOp** objectArray = (SgAndOp**) &(SgAndOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAndOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAndOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAndOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAndOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAndOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAndOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAndOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAndOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAndOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAndOp** objectArray = (SgAndOp**) &(SgAndOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAndOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAndOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAndOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAndOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAndOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAndOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAndOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAndOp** objectArray = (SgAndOp**) &(SgAndOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAndOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAndOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAndOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAndOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAndOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAndOp** objectArray = (SgAndOp**) &(SgAndOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAndOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAndOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAndOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAndOp);

     return memory;
   }



/* #line 113008 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgOrOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgOrOp objectArray [] = *(Memory_Block_List.begin());
     if (SgOrOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOrOp** objectArray = (SgOrOp**) &(SgOrOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOrOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgOrOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgOrOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgOrOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgOrOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOrOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgOrOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOrOp objectArray [] = *(Memory_Block_List.begin());
     if (SgOrOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOrOp** objectArray = (SgOrOp**) &(SgOrOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgOrOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgOrOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgOrOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgOrOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgOrOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgOrOp objectArray [] = *(Memory_Block_List.begin());
     if (SgOrOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgOrOp** objectArray = (SgOrOp**) &(SgOrOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgOrOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgOrOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgOrOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOrOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgOrOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgOrOp** objectArray = (SgOrOp**) &(SgOrOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgOrOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgOrOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgOrOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgOrOp);

     return memory;
   }



/* #line 113240 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBitXorOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBitXorOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitXorOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitXorOp** objectArray = (SgBitXorOp**) &(SgBitXorOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBitXorOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBitXorOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBitXorOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBitXorOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBitXorOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBitXorOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBitXorOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBitXorOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitXorOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitXorOp** objectArray = (SgBitXorOp**) &(SgBitXorOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBitXorOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBitXorOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBitXorOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBitXorOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBitXorOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBitXorOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitXorOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitXorOp** objectArray = (SgBitXorOp**) &(SgBitXorOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBitXorOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBitXorOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBitXorOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBitXorOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBitXorOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBitXorOp** objectArray = (SgBitXorOp**) &(SgBitXorOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBitXorOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBitXorOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBitXorOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBitXorOp);

     return memory;
   }



/* #line 113472 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBitAndOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBitAndOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitAndOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitAndOp** objectArray = (SgBitAndOp**) &(SgBitAndOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBitAndOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBitAndOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBitAndOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBitAndOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBitAndOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBitAndOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBitAndOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBitAndOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitAndOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitAndOp** objectArray = (SgBitAndOp**) &(SgBitAndOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBitAndOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBitAndOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBitAndOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBitAndOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBitAndOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBitAndOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitAndOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitAndOp** objectArray = (SgBitAndOp**) &(SgBitAndOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBitAndOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBitAndOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBitAndOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBitAndOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBitAndOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBitAndOp** objectArray = (SgBitAndOp**) &(SgBitAndOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBitAndOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBitAndOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBitAndOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBitAndOp);

     return memory;
   }



/* #line 113704 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBitOrOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBitOrOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitOrOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitOrOp** objectArray = (SgBitOrOp**) &(SgBitOrOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBitOrOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBitOrOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBitOrOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBitOrOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBitOrOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBitOrOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBitOrOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBitOrOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitOrOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitOrOp** objectArray = (SgBitOrOp**) &(SgBitOrOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBitOrOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBitOrOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBitOrOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBitOrOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBitOrOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBitOrOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitOrOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitOrOp** objectArray = (SgBitOrOp**) &(SgBitOrOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBitOrOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBitOrOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBitOrOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBitOrOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBitOrOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBitOrOp** objectArray = (SgBitOrOp**) &(SgBitOrOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBitOrOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBitOrOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBitOrOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBitOrOp);

     return memory;
   }



/* #line 113936 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBitEqvOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBitEqvOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitEqvOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitEqvOp** objectArray = (SgBitEqvOp**) &(SgBitEqvOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBitEqvOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBitEqvOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBitEqvOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBitEqvOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBitEqvOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBitEqvOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBitEqvOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBitEqvOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitEqvOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitEqvOp** objectArray = (SgBitEqvOp**) &(SgBitEqvOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBitEqvOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBitEqvOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBitEqvOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBitEqvOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBitEqvOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBitEqvOp objectArray [] = *(Memory_Block_List.begin());
     if (SgBitEqvOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBitEqvOp** objectArray = (SgBitEqvOp**) &(SgBitEqvOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBitEqvOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBitEqvOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBitEqvOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBitEqvOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBitEqvOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBitEqvOp** objectArray = (SgBitEqvOp**) &(SgBitEqvOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBitEqvOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBitEqvOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBitEqvOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBitEqvOp);

     return memory;
   }



/* #line 114168 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCommaOpExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCommaOpExp objectArray [] = *(Memory_Block_List.begin());
     if (SgCommaOpExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCommaOpExp** objectArray = (SgCommaOpExp**) &(SgCommaOpExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCommaOpExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCommaOpExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCommaOpExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCommaOpExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCommaOpExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCommaOpExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCommaOpExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCommaOpExp objectArray [] = *(Memory_Block_List.begin());
     if (SgCommaOpExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCommaOpExp** objectArray = (SgCommaOpExp**) &(SgCommaOpExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCommaOpExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCommaOpExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCommaOpExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCommaOpExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCommaOpExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCommaOpExp objectArray [] = *(Memory_Block_List.begin());
     if (SgCommaOpExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCommaOpExp** objectArray = (SgCommaOpExp**) &(SgCommaOpExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCommaOpExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCommaOpExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCommaOpExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCommaOpExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCommaOpExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCommaOpExp** objectArray = (SgCommaOpExp**) &(SgCommaOpExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCommaOpExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCommaOpExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCommaOpExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCommaOpExp);

     return memory;
   }



/* #line 114400 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLshiftOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLshiftOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLshiftOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLshiftOp** objectArray = (SgLshiftOp**) &(SgLshiftOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLshiftOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLshiftOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLshiftOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLshiftOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLshiftOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLshiftOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLshiftOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLshiftOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLshiftOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLshiftOp** objectArray = (SgLshiftOp**) &(SgLshiftOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLshiftOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLshiftOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLshiftOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLshiftOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLshiftOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLshiftOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLshiftOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLshiftOp** objectArray = (SgLshiftOp**) &(SgLshiftOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLshiftOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLshiftOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLshiftOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLshiftOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLshiftOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLshiftOp** objectArray = (SgLshiftOp**) &(SgLshiftOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLshiftOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLshiftOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLshiftOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLshiftOp);

     return memory;
   }



/* #line 114632 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgRshiftOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgRshiftOp objectArray [] = *(Memory_Block_List.begin());
     if (SgRshiftOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRshiftOp** objectArray = (SgRshiftOp**) &(SgRshiftOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRshiftOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgRshiftOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgRshiftOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgRshiftOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgRshiftOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRshiftOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgRshiftOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRshiftOp objectArray [] = *(Memory_Block_List.begin());
     if (SgRshiftOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRshiftOp** objectArray = (SgRshiftOp**) &(SgRshiftOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRshiftOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRshiftOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgRshiftOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgRshiftOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgRshiftOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRshiftOp objectArray [] = *(Memory_Block_List.begin());
     if (SgRshiftOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRshiftOp** objectArray = (SgRshiftOp**) &(SgRshiftOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgRshiftOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgRshiftOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgRshiftOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRshiftOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgRshiftOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgRshiftOp** objectArray = (SgRshiftOp**) &(SgRshiftOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgRshiftOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgRshiftOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRshiftOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgRshiftOp);

     return memory;
   }



/* #line 114864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPntrArrRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPntrArrRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgPntrArrRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPntrArrRefExp** objectArray = (SgPntrArrRefExp**) &(SgPntrArrRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPntrArrRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPntrArrRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPntrArrRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPntrArrRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPntrArrRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPntrArrRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPntrArrRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPntrArrRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgPntrArrRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPntrArrRefExp** objectArray = (SgPntrArrRefExp**) &(SgPntrArrRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPntrArrRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPntrArrRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPntrArrRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPntrArrRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPntrArrRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPntrArrRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgPntrArrRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPntrArrRefExp** objectArray = (SgPntrArrRefExp**) &(SgPntrArrRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPntrArrRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPntrArrRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPntrArrRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPntrArrRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPntrArrRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPntrArrRefExp** objectArray = (SgPntrArrRefExp**) &(SgPntrArrRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPntrArrRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPntrArrRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPntrArrRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPntrArrRefExp);

     return memory;
   }



/* #line 115096 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgScopeOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgScopeOp objectArray [] = *(Memory_Block_List.begin());
     if (SgScopeOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgScopeOp** objectArray = (SgScopeOp**) &(SgScopeOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgScopeOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgScopeOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgScopeOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgScopeOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgScopeOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgScopeOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgScopeOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgScopeOp objectArray [] = *(Memory_Block_List.begin());
     if (SgScopeOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgScopeOp** objectArray = (SgScopeOp**) &(SgScopeOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgScopeOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgScopeOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgScopeOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgScopeOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgScopeOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgScopeOp objectArray [] = *(Memory_Block_List.begin());
     if (SgScopeOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgScopeOp** objectArray = (SgScopeOp**) &(SgScopeOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgScopeOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgScopeOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgScopeOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgScopeOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgScopeOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgScopeOp** objectArray = (SgScopeOp**) &(SgScopeOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgScopeOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgScopeOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgScopeOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgScopeOp);

     return memory;
   }



/* #line 115328 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssignOp** objectArray = (SgAssignOp**) &(SgAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssignOp** objectArray = (SgAssignOp**) &(SgAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssignOp** objectArray = (SgAssignOp**) &(SgAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAssignOp** objectArray = (SgAssignOp**) &(SgAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAssignOp);

     return memory;
   }



/* #line 115560 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgExponentiationOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgExponentiationOp objectArray [] = *(Memory_Block_List.begin());
     if (SgExponentiationOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExponentiationOp** objectArray = (SgExponentiationOp**) &(SgExponentiationOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExponentiationOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgExponentiationOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgExponentiationOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgExponentiationOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgExponentiationOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExponentiationOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgExponentiationOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExponentiationOp objectArray [] = *(Memory_Block_List.begin());
     if (SgExponentiationOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExponentiationOp** objectArray = (SgExponentiationOp**) &(SgExponentiationOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExponentiationOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExponentiationOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgExponentiationOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgExponentiationOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgExponentiationOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExponentiationOp objectArray [] = *(Memory_Block_List.begin());
     if (SgExponentiationOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExponentiationOp** objectArray = (SgExponentiationOp**) &(SgExponentiationOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgExponentiationOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgExponentiationOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgExponentiationOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExponentiationOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgExponentiationOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgExponentiationOp** objectArray = (SgExponentiationOp**) &(SgExponentiationOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgExponentiationOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgExponentiationOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExponentiationOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgExponentiationOp);

     return memory;
   }



/* #line 115792 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaUnsignedRshiftOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaUnsignedRshiftOp objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaUnsignedRshiftOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaUnsignedRshiftOp** objectArray = (SgJavaUnsignedRshiftOp**) &(SgJavaUnsignedRshiftOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaUnsignedRshiftOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaUnsignedRshiftOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaUnsignedRshiftOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaUnsignedRshiftOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaUnsignedRshiftOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaUnsignedRshiftOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaUnsignedRshiftOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaUnsignedRshiftOp objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaUnsignedRshiftOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaUnsignedRshiftOp** objectArray = (SgJavaUnsignedRshiftOp**) &(SgJavaUnsignedRshiftOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaUnsignedRshiftOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaUnsignedRshiftOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaUnsignedRshiftOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaUnsignedRshiftOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaUnsignedRshiftOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaUnsignedRshiftOp objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaUnsignedRshiftOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaUnsignedRshiftOp** objectArray = (SgJavaUnsignedRshiftOp**) &(SgJavaUnsignedRshiftOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaUnsignedRshiftOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaUnsignedRshiftOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaUnsignedRshiftOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaUnsignedRshiftOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaUnsignedRshiftOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaUnsignedRshiftOp** objectArray = (SgJavaUnsignedRshiftOp**) &(SgJavaUnsignedRshiftOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaUnsignedRshiftOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaUnsignedRshiftOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaUnsignedRshiftOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaUnsignedRshiftOp);

     return memory;
   }



/* #line 116024 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgConcatenationOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgConcatenationOp objectArray [] = *(Memory_Block_List.begin());
     if (SgConcatenationOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConcatenationOp** objectArray = (SgConcatenationOp**) &(SgConcatenationOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgConcatenationOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgConcatenationOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgConcatenationOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgConcatenationOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgConcatenationOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgConcatenationOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgConcatenationOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgConcatenationOp objectArray [] = *(Memory_Block_List.begin());
     if (SgConcatenationOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConcatenationOp** objectArray = (SgConcatenationOp**) &(SgConcatenationOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgConcatenationOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgConcatenationOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgConcatenationOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgConcatenationOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgConcatenationOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgConcatenationOp objectArray [] = *(Memory_Block_List.begin());
     if (SgConcatenationOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConcatenationOp** objectArray = (SgConcatenationOp**) &(SgConcatenationOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgConcatenationOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgConcatenationOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgConcatenationOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgConcatenationOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgConcatenationOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgConcatenationOp** objectArray = (SgConcatenationOp**) &(SgConcatenationOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgConcatenationOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgConcatenationOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgConcatenationOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgConcatenationOp);

     return memory;
   }



/* #line 116256 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPointerAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPointerAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgPointerAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPointerAssignOp** objectArray = (SgPointerAssignOp**) &(SgPointerAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPointerAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPointerAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPointerAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPointerAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPointerAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPointerAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPointerAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPointerAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgPointerAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPointerAssignOp** objectArray = (SgPointerAssignOp**) &(SgPointerAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPointerAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPointerAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPointerAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPointerAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPointerAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPointerAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgPointerAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPointerAssignOp** objectArray = (SgPointerAssignOp**) &(SgPointerAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPointerAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPointerAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPointerAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPointerAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPointerAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPointerAssignOp** objectArray = (SgPointerAssignOp**) &(SgPointerAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPointerAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPointerAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPointerAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPointerAssignOp);

     return memory;
   }



/* #line 116488 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUserDefinedBinaryOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUserDefinedBinaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgUserDefinedBinaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUserDefinedBinaryOp** objectArray = (SgUserDefinedBinaryOp**) &(SgUserDefinedBinaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUserDefinedBinaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUserDefinedBinaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUserDefinedBinaryOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUserDefinedBinaryOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUserDefinedBinaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUserDefinedBinaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUserDefinedBinaryOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUserDefinedBinaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgUserDefinedBinaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUserDefinedBinaryOp** objectArray = (SgUserDefinedBinaryOp**) &(SgUserDefinedBinaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUserDefinedBinaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUserDefinedBinaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUserDefinedBinaryOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUserDefinedBinaryOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUserDefinedBinaryOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUserDefinedBinaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgUserDefinedBinaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUserDefinedBinaryOp** objectArray = (SgUserDefinedBinaryOp**) &(SgUserDefinedBinaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUserDefinedBinaryOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUserDefinedBinaryOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUserDefinedBinaryOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUserDefinedBinaryOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUserDefinedBinaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUserDefinedBinaryOp** objectArray = (SgUserDefinedBinaryOp**) &(SgUserDefinedBinaryOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUserDefinedBinaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUserDefinedBinaryOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUserDefinedBinaryOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUserDefinedBinaryOp);

     return memory;
   }



/* #line 116720 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCompoundAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCompoundAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgCompoundAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCompoundAssignOp** objectArray = (SgCompoundAssignOp**) &(SgCompoundAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCompoundAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCompoundAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCompoundAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCompoundAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCompoundAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCompoundAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCompoundAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCompoundAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgCompoundAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCompoundAssignOp** objectArray = (SgCompoundAssignOp**) &(SgCompoundAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCompoundAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCompoundAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCompoundAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCompoundAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCompoundAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCompoundAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgCompoundAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCompoundAssignOp** objectArray = (SgCompoundAssignOp**) &(SgCompoundAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCompoundAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCompoundAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCompoundAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCompoundAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCompoundAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCompoundAssignOp** objectArray = (SgCompoundAssignOp**) &(SgCompoundAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCompoundAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCompoundAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCompoundAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCompoundAssignOp);

     return memory;
   }



/* #line 116952 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPlusAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPlusAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgPlusAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPlusAssignOp** objectArray = (SgPlusAssignOp**) &(SgPlusAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPlusAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPlusAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPlusAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPlusAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPlusAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPlusAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPlusAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPlusAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgPlusAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPlusAssignOp** objectArray = (SgPlusAssignOp**) &(SgPlusAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPlusAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPlusAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPlusAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPlusAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPlusAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPlusAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgPlusAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPlusAssignOp** objectArray = (SgPlusAssignOp**) &(SgPlusAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPlusAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPlusAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPlusAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPlusAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPlusAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPlusAssignOp** objectArray = (SgPlusAssignOp**) &(SgPlusAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPlusAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPlusAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPlusAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPlusAssignOp);

     return memory;
   }



/* #line 117184 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMinusAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMinusAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMinusAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMinusAssignOp** objectArray = (SgMinusAssignOp**) &(SgMinusAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMinusAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMinusAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMinusAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMinusAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMinusAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMinusAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMinusAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMinusAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMinusAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMinusAssignOp** objectArray = (SgMinusAssignOp**) &(SgMinusAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMinusAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMinusAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMinusAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMinusAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMinusAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMinusAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMinusAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMinusAssignOp** objectArray = (SgMinusAssignOp**) &(SgMinusAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMinusAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMinusAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMinusAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMinusAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMinusAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMinusAssignOp** objectArray = (SgMinusAssignOp**) &(SgMinusAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMinusAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMinusAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMinusAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMinusAssignOp);

     return memory;
   }



/* #line 117416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAndAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAndAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAndAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAndAssignOp** objectArray = (SgAndAssignOp**) &(SgAndAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAndAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAndAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAndAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAndAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAndAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAndAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAndAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAndAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAndAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAndAssignOp** objectArray = (SgAndAssignOp**) &(SgAndAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAndAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAndAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAndAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAndAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAndAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAndAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAndAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAndAssignOp** objectArray = (SgAndAssignOp**) &(SgAndAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAndAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAndAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAndAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAndAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAndAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAndAssignOp** objectArray = (SgAndAssignOp**) &(SgAndAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAndAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAndAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAndAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAndAssignOp);

     return memory;
   }



/* #line 117648 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIorAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIorAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIorAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIorAssignOp** objectArray = (SgIorAssignOp**) &(SgIorAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIorAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIorAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIorAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIorAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIorAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIorAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIorAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIorAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIorAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIorAssignOp** objectArray = (SgIorAssignOp**) &(SgIorAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIorAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIorAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIorAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIorAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIorAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIorAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIorAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIorAssignOp** objectArray = (SgIorAssignOp**) &(SgIorAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIorAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIorAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIorAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIorAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIorAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIorAssignOp** objectArray = (SgIorAssignOp**) &(SgIorAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIorAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIorAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIorAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIorAssignOp);

     return memory;
   }



/* #line 117880 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMultAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMultAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMultAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMultAssignOp** objectArray = (SgMultAssignOp**) &(SgMultAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMultAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMultAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMultAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMultAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMultAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMultAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMultAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMultAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMultAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMultAssignOp** objectArray = (SgMultAssignOp**) &(SgMultAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMultAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMultAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMultAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMultAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMultAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMultAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMultAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMultAssignOp** objectArray = (SgMultAssignOp**) &(SgMultAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMultAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMultAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMultAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMultAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMultAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMultAssignOp** objectArray = (SgMultAssignOp**) &(SgMultAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMultAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMultAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMultAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMultAssignOp);

     return memory;
   }



/* #line 118112 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDivAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDivAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgDivAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDivAssignOp** objectArray = (SgDivAssignOp**) &(SgDivAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDivAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDivAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDivAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDivAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDivAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDivAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDivAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDivAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgDivAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDivAssignOp** objectArray = (SgDivAssignOp**) &(SgDivAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDivAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDivAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDivAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDivAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDivAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDivAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgDivAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDivAssignOp** objectArray = (SgDivAssignOp**) &(SgDivAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDivAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDivAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDivAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDivAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDivAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDivAssignOp** objectArray = (SgDivAssignOp**) &(SgDivAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDivAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDivAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDivAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDivAssignOp);

     return memory;
   }



/* #line 118344 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgModAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgModAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgModAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModAssignOp** objectArray = (SgModAssignOp**) &(SgModAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgModAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgModAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgModAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgModAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgModAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgModAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModAssignOp** objectArray = (SgModAssignOp**) &(SgModAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgModAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgModAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgModAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgModAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModAssignOp** objectArray = (SgModAssignOp**) &(SgModAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgModAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgModAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgModAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgModAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgModAssignOp** objectArray = (SgModAssignOp**) &(SgModAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgModAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgModAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgModAssignOp);

     return memory;
   }



/* #line 118576 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgXorAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgXorAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgXorAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgXorAssignOp** objectArray = (SgXorAssignOp**) &(SgXorAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgXorAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgXorAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgXorAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgXorAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgXorAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgXorAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgXorAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgXorAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgXorAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgXorAssignOp** objectArray = (SgXorAssignOp**) &(SgXorAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgXorAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgXorAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgXorAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgXorAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgXorAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgXorAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgXorAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgXorAssignOp** objectArray = (SgXorAssignOp**) &(SgXorAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgXorAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgXorAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgXorAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgXorAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgXorAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgXorAssignOp** objectArray = (SgXorAssignOp**) &(SgXorAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgXorAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgXorAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgXorAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgXorAssignOp);

     return memory;
   }



/* #line 118808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLshiftAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLshiftAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLshiftAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLshiftAssignOp** objectArray = (SgLshiftAssignOp**) &(SgLshiftAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLshiftAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLshiftAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLshiftAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLshiftAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLshiftAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLshiftAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLshiftAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLshiftAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLshiftAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLshiftAssignOp** objectArray = (SgLshiftAssignOp**) &(SgLshiftAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLshiftAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLshiftAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLshiftAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLshiftAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLshiftAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLshiftAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLshiftAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLshiftAssignOp** objectArray = (SgLshiftAssignOp**) &(SgLshiftAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLshiftAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLshiftAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLshiftAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLshiftAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLshiftAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLshiftAssignOp** objectArray = (SgLshiftAssignOp**) &(SgLshiftAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLshiftAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLshiftAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLshiftAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLshiftAssignOp);

     return memory;
   }



/* #line 119040 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgRshiftAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgRshiftAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgRshiftAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRshiftAssignOp** objectArray = (SgRshiftAssignOp**) &(SgRshiftAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRshiftAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgRshiftAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgRshiftAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgRshiftAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgRshiftAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRshiftAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgRshiftAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRshiftAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgRshiftAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRshiftAssignOp** objectArray = (SgRshiftAssignOp**) &(SgRshiftAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRshiftAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRshiftAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgRshiftAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgRshiftAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgRshiftAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRshiftAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgRshiftAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRshiftAssignOp** objectArray = (SgRshiftAssignOp**) &(SgRshiftAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgRshiftAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgRshiftAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgRshiftAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRshiftAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgRshiftAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgRshiftAssignOp** objectArray = (SgRshiftAssignOp**) &(SgRshiftAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgRshiftAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgRshiftAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRshiftAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgRshiftAssignOp);

     return memory;
   }



/* #line 119272 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaUnsignedRshiftAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaUnsignedRshiftAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaUnsignedRshiftAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaUnsignedRshiftAssignOp** objectArray = (SgJavaUnsignedRshiftAssignOp**) &(SgJavaUnsignedRshiftAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaUnsignedRshiftAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaUnsignedRshiftAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaUnsignedRshiftAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaUnsignedRshiftAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaUnsignedRshiftAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaUnsignedRshiftAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaUnsignedRshiftAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaUnsignedRshiftAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaUnsignedRshiftAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaUnsignedRshiftAssignOp** objectArray = (SgJavaUnsignedRshiftAssignOp**) &(SgJavaUnsignedRshiftAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaUnsignedRshiftAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaUnsignedRshiftAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaUnsignedRshiftAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaUnsignedRshiftAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaUnsignedRshiftAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaUnsignedRshiftAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaUnsignedRshiftAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaUnsignedRshiftAssignOp** objectArray = (SgJavaUnsignedRshiftAssignOp**) &(SgJavaUnsignedRshiftAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaUnsignedRshiftAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaUnsignedRshiftAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaUnsignedRshiftAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaUnsignedRshiftAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaUnsignedRshiftAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaUnsignedRshiftAssignOp** objectArray = (SgJavaUnsignedRshiftAssignOp**) &(SgJavaUnsignedRshiftAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaUnsignedRshiftAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaUnsignedRshiftAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaUnsignedRshiftAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaUnsignedRshiftAssignOp);

     return memory;
   }



/* #line 119504 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIntegerDivideAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIntegerDivideAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIntegerDivideAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntegerDivideAssignOp** objectArray = (SgIntegerDivideAssignOp**) &(SgIntegerDivideAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIntegerDivideAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIntegerDivideAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIntegerDivideAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIntegerDivideAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIntegerDivideAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIntegerDivideAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIntegerDivideAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIntegerDivideAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIntegerDivideAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntegerDivideAssignOp** objectArray = (SgIntegerDivideAssignOp**) &(SgIntegerDivideAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIntegerDivideAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIntegerDivideAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIntegerDivideAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIntegerDivideAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIntegerDivideAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIntegerDivideAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIntegerDivideAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntegerDivideAssignOp** objectArray = (SgIntegerDivideAssignOp**) &(SgIntegerDivideAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIntegerDivideAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIntegerDivideAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIntegerDivideAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIntegerDivideAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIntegerDivideAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIntegerDivideAssignOp** objectArray = (SgIntegerDivideAssignOp**) &(SgIntegerDivideAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIntegerDivideAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIntegerDivideAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIntegerDivideAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIntegerDivideAssignOp);

     return memory;
   }



/* #line 119736 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgExponentiationAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgExponentiationAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgExponentiationAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExponentiationAssignOp** objectArray = (SgExponentiationAssignOp**) &(SgExponentiationAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExponentiationAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgExponentiationAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgExponentiationAssignOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgExponentiationAssignOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgExponentiationAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExponentiationAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgExponentiationAssignOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExponentiationAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgExponentiationAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExponentiationAssignOp** objectArray = (SgExponentiationAssignOp**) &(SgExponentiationAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExponentiationAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExponentiationAssignOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgExponentiationAssignOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgExponentiationAssignOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgExponentiationAssignOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExponentiationAssignOp objectArray [] = *(Memory_Block_List.begin());
     if (SgExponentiationAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExponentiationAssignOp** objectArray = (SgExponentiationAssignOp**) &(SgExponentiationAssignOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgExponentiationAssignOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgExponentiationAssignOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgExponentiationAssignOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExponentiationAssignOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgExponentiationAssignOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgExponentiationAssignOp** objectArray = (SgExponentiationAssignOp**) &(SgExponentiationAssignOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgExponentiationAssignOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgExponentiationAssignOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExponentiationAssignOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgExponentiationAssignOp);

     return memory;
   }



/* #line 119968 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMembershipOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMembershipOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMembershipOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMembershipOp** objectArray = (SgMembershipOp**) &(SgMembershipOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMembershipOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMembershipOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMembershipOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMembershipOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMembershipOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMembershipOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMembershipOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMembershipOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMembershipOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMembershipOp** objectArray = (SgMembershipOp**) &(SgMembershipOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMembershipOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMembershipOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMembershipOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMembershipOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMembershipOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMembershipOp objectArray [] = *(Memory_Block_List.begin());
     if (SgMembershipOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMembershipOp** objectArray = (SgMembershipOp**) &(SgMembershipOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMembershipOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMembershipOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMembershipOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMembershipOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMembershipOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMembershipOp** objectArray = (SgMembershipOp**) &(SgMembershipOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMembershipOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMembershipOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMembershipOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMembershipOp);

     return memory;
   }



/* #line 120200 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSpaceshipOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSpaceshipOp objectArray [] = *(Memory_Block_List.begin());
     if (SgSpaceshipOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSpaceshipOp** objectArray = (SgSpaceshipOp**) &(SgSpaceshipOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSpaceshipOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSpaceshipOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSpaceshipOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSpaceshipOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSpaceshipOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSpaceshipOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSpaceshipOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSpaceshipOp objectArray [] = *(Memory_Block_List.begin());
     if (SgSpaceshipOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSpaceshipOp** objectArray = (SgSpaceshipOp**) &(SgSpaceshipOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSpaceshipOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSpaceshipOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSpaceshipOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSpaceshipOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSpaceshipOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSpaceshipOp objectArray [] = *(Memory_Block_List.begin());
     if (SgSpaceshipOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSpaceshipOp** objectArray = (SgSpaceshipOp**) &(SgSpaceshipOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSpaceshipOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSpaceshipOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSpaceshipOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSpaceshipOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSpaceshipOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSpaceshipOp** objectArray = (SgSpaceshipOp**) &(SgSpaceshipOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSpaceshipOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSpaceshipOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSpaceshipOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSpaceshipOp);

     return memory;
   }



/* #line 120432 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNonMembershipOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNonMembershipOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNonMembershipOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonMembershipOp** objectArray = (SgNonMembershipOp**) &(SgNonMembershipOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNonMembershipOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNonMembershipOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNonMembershipOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNonMembershipOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNonMembershipOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNonMembershipOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNonMembershipOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNonMembershipOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNonMembershipOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonMembershipOp** objectArray = (SgNonMembershipOp**) &(SgNonMembershipOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNonMembershipOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNonMembershipOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNonMembershipOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNonMembershipOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNonMembershipOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNonMembershipOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNonMembershipOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonMembershipOp** objectArray = (SgNonMembershipOp**) &(SgNonMembershipOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNonMembershipOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNonMembershipOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNonMembershipOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNonMembershipOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNonMembershipOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNonMembershipOp** objectArray = (SgNonMembershipOp**) &(SgNonMembershipOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNonMembershipOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNonMembershipOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNonMembershipOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNonMembershipOp);

     return memory;
   }



/* #line 120664 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIsOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIsOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIsOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIsOp** objectArray = (SgIsOp**) &(SgIsOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIsOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIsOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIsOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIsOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIsOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIsOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIsOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIsOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIsOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIsOp** objectArray = (SgIsOp**) &(SgIsOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIsOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIsOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIsOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIsOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIsOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIsOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIsOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIsOp** objectArray = (SgIsOp**) &(SgIsOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIsOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIsOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIsOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIsOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIsOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIsOp** objectArray = (SgIsOp**) &(SgIsOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIsOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIsOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIsOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIsOp);

     return memory;
   }



/* #line 120896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIsNotOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIsNotOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIsNotOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIsNotOp** objectArray = (SgIsNotOp**) &(SgIsNotOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIsNotOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIsNotOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIsNotOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIsNotOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIsNotOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIsNotOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIsNotOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIsNotOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIsNotOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIsNotOp** objectArray = (SgIsNotOp**) &(SgIsNotOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIsNotOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIsNotOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIsNotOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIsNotOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIsNotOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIsNotOp objectArray [] = *(Memory_Block_List.begin());
     if (SgIsNotOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIsNotOp** objectArray = (SgIsNotOp**) &(SgIsNotOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIsNotOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIsNotOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIsNotOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIsNotOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIsNotOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIsNotOp** objectArray = (SgIsNotOp**) &(SgIsNotOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIsNotOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIsNotOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIsNotOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIsNotOp);

     return memory;
   }



/* #line 121128 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDotDotExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDotDotExp objectArray [] = *(Memory_Block_List.begin());
     if (SgDotDotExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDotDotExp** objectArray = (SgDotDotExp**) &(SgDotDotExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDotDotExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDotDotExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDotDotExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDotDotExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDotDotExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDotDotExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDotDotExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDotDotExp objectArray [] = *(Memory_Block_List.begin());
     if (SgDotDotExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDotDotExp** objectArray = (SgDotDotExp**) &(SgDotDotExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDotDotExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDotDotExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDotDotExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDotDotExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDotDotExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDotDotExp objectArray [] = *(Memory_Block_List.begin());
     if (SgDotDotExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDotDotExp** objectArray = (SgDotDotExp**) &(SgDotDotExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDotDotExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDotDotExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDotDotExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDotDotExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDotDotExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDotDotExp** objectArray = (SgDotDotExp**) &(SgDotDotExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDotDotExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDotDotExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDotDotExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDotDotExp);

     return memory;
   }



/* #line 121360 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgElementwiseOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgElementwiseOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseOp** objectArray = (SgElementwiseOp**) &(SgElementwiseOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwiseOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgElementwiseOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgElementwiseOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgElementwiseOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgElementwiseOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwiseOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgElementwiseOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwiseOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseOp** objectArray = (SgElementwiseOp**) &(SgElementwiseOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwiseOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwiseOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgElementwiseOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgElementwiseOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgElementwiseOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwiseOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseOp** objectArray = (SgElementwiseOp**) &(SgElementwiseOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgElementwiseOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgElementwiseOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgElementwiseOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwiseOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgElementwiseOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgElementwiseOp** objectArray = (SgElementwiseOp**) &(SgElementwiseOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgElementwiseOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgElementwiseOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwiseOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgElementwiseOp);

     return memory;
   }



/* #line 121592 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgElementwiseMultiplyOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgElementwiseMultiplyOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseMultiplyOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseMultiplyOp** objectArray = (SgElementwiseMultiplyOp**) &(SgElementwiseMultiplyOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwiseMultiplyOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgElementwiseMultiplyOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgElementwiseMultiplyOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgElementwiseMultiplyOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgElementwiseMultiplyOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwiseMultiplyOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgElementwiseMultiplyOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwiseMultiplyOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseMultiplyOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseMultiplyOp** objectArray = (SgElementwiseMultiplyOp**) &(SgElementwiseMultiplyOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwiseMultiplyOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwiseMultiplyOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgElementwiseMultiplyOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgElementwiseMultiplyOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgElementwiseMultiplyOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwiseMultiplyOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseMultiplyOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseMultiplyOp** objectArray = (SgElementwiseMultiplyOp**) &(SgElementwiseMultiplyOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgElementwiseMultiplyOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgElementwiseMultiplyOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgElementwiseMultiplyOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwiseMultiplyOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgElementwiseMultiplyOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgElementwiseMultiplyOp** objectArray = (SgElementwiseMultiplyOp**) &(SgElementwiseMultiplyOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgElementwiseMultiplyOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgElementwiseMultiplyOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwiseMultiplyOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgElementwiseMultiplyOp);

     return memory;
   }



/* #line 121824 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgElementwisePowerOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgElementwisePowerOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwisePowerOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwisePowerOp** objectArray = (SgElementwisePowerOp**) &(SgElementwisePowerOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwisePowerOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgElementwisePowerOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgElementwisePowerOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgElementwisePowerOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgElementwisePowerOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwisePowerOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgElementwisePowerOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwisePowerOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwisePowerOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwisePowerOp** objectArray = (SgElementwisePowerOp**) &(SgElementwisePowerOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwisePowerOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwisePowerOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgElementwisePowerOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgElementwisePowerOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgElementwisePowerOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwisePowerOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwisePowerOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwisePowerOp** objectArray = (SgElementwisePowerOp**) &(SgElementwisePowerOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgElementwisePowerOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgElementwisePowerOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgElementwisePowerOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwisePowerOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgElementwisePowerOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgElementwisePowerOp** objectArray = (SgElementwisePowerOp**) &(SgElementwisePowerOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgElementwisePowerOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgElementwisePowerOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwisePowerOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgElementwisePowerOp);

     return memory;
   }



/* #line 122056 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgElementwiseLeftDivideOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgElementwiseLeftDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseLeftDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseLeftDivideOp** objectArray = (SgElementwiseLeftDivideOp**) &(SgElementwiseLeftDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwiseLeftDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgElementwiseLeftDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgElementwiseLeftDivideOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgElementwiseLeftDivideOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgElementwiseLeftDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwiseLeftDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgElementwiseLeftDivideOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwiseLeftDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseLeftDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseLeftDivideOp** objectArray = (SgElementwiseLeftDivideOp**) &(SgElementwiseLeftDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwiseLeftDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwiseLeftDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgElementwiseLeftDivideOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgElementwiseLeftDivideOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgElementwiseLeftDivideOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwiseLeftDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseLeftDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseLeftDivideOp** objectArray = (SgElementwiseLeftDivideOp**) &(SgElementwiseLeftDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgElementwiseLeftDivideOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgElementwiseLeftDivideOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgElementwiseLeftDivideOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwiseLeftDivideOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgElementwiseLeftDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgElementwiseLeftDivideOp** objectArray = (SgElementwiseLeftDivideOp**) &(SgElementwiseLeftDivideOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgElementwiseLeftDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgElementwiseLeftDivideOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwiseLeftDivideOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgElementwiseLeftDivideOp);

     return memory;
   }



/* #line 122288 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgElementwiseDivideOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgElementwiseDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseDivideOp** objectArray = (SgElementwiseDivideOp**) &(SgElementwiseDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwiseDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgElementwiseDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgElementwiseDivideOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgElementwiseDivideOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgElementwiseDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwiseDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgElementwiseDivideOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwiseDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseDivideOp** objectArray = (SgElementwiseDivideOp**) &(SgElementwiseDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwiseDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwiseDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgElementwiseDivideOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgElementwiseDivideOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgElementwiseDivideOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwiseDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseDivideOp** objectArray = (SgElementwiseDivideOp**) &(SgElementwiseDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgElementwiseDivideOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgElementwiseDivideOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgElementwiseDivideOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwiseDivideOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgElementwiseDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgElementwiseDivideOp** objectArray = (SgElementwiseDivideOp**) &(SgElementwiseDivideOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgElementwiseDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgElementwiseDivideOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwiseDivideOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgElementwiseDivideOp);

     return memory;
   }



/* #line 122520 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgElementwiseAddOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgElementwiseAddOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseAddOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseAddOp** objectArray = (SgElementwiseAddOp**) &(SgElementwiseAddOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwiseAddOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgElementwiseAddOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgElementwiseAddOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgElementwiseAddOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgElementwiseAddOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwiseAddOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgElementwiseAddOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwiseAddOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseAddOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseAddOp** objectArray = (SgElementwiseAddOp**) &(SgElementwiseAddOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwiseAddOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwiseAddOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgElementwiseAddOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgElementwiseAddOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgElementwiseAddOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwiseAddOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseAddOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseAddOp** objectArray = (SgElementwiseAddOp**) &(SgElementwiseAddOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgElementwiseAddOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgElementwiseAddOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgElementwiseAddOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwiseAddOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgElementwiseAddOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgElementwiseAddOp** objectArray = (SgElementwiseAddOp**) &(SgElementwiseAddOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgElementwiseAddOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgElementwiseAddOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwiseAddOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgElementwiseAddOp);

     return memory;
   }



/* #line 122752 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgElementwiseSubtractOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgElementwiseSubtractOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseSubtractOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseSubtractOp** objectArray = (SgElementwiseSubtractOp**) &(SgElementwiseSubtractOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwiseSubtractOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgElementwiseSubtractOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgElementwiseSubtractOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgElementwiseSubtractOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgElementwiseSubtractOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwiseSubtractOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgElementwiseSubtractOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwiseSubtractOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseSubtractOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseSubtractOp** objectArray = (SgElementwiseSubtractOp**) &(SgElementwiseSubtractOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgElementwiseSubtractOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgElementwiseSubtractOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgElementwiseSubtractOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgElementwiseSubtractOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgElementwiseSubtractOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgElementwiseSubtractOp objectArray [] = *(Memory_Block_List.begin());
     if (SgElementwiseSubtractOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgElementwiseSubtractOp** objectArray = (SgElementwiseSubtractOp**) &(SgElementwiseSubtractOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgElementwiseSubtractOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgElementwiseSubtractOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgElementwiseSubtractOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwiseSubtractOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgElementwiseSubtractOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgElementwiseSubtractOp** objectArray = (SgElementwiseSubtractOp**) &(SgElementwiseSubtractOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgElementwiseSubtractOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgElementwiseSubtractOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgElementwiseSubtractOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgElementwiseSubtractOp);

     return memory;
   }



/* #line 122984 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPowerOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPowerOp objectArray [] = *(Memory_Block_List.begin());
     if (SgPowerOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPowerOp** objectArray = (SgPowerOp**) &(SgPowerOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPowerOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPowerOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPowerOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPowerOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPowerOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPowerOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPowerOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPowerOp objectArray [] = *(Memory_Block_List.begin());
     if (SgPowerOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPowerOp** objectArray = (SgPowerOp**) &(SgPowerOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPowerOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPowerOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPowerOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPowerOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPowerOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPowerOp objectArray [] = *(Memory_Block_List.begin());
     if (SgPowerOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPowerOp** objectArray = (SgPowerOp**) &(SgPowerOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPowerOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPowerOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPowerOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPowerOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPowerOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPowerOp** objectArray = (SgPowerOp**) &(SgPowerOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPowerOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPowerOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPowerOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPowerOp);

     return memory;
   }



/* #line 123216 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLeftDivideOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLeftDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLeftDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLeftDivideOp** objectArray = (SgLeftDivideOp**) &(SgLeftDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLeftDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLeftDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLeftDivideOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLeftDivideOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLeftDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLeftDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLeftDivideOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLeftDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLeftDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLeftDivideOp** objectArray = (SgLeftDivideOp**) &(SgLeftDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLeftDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLeftDivideOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLeftDivideOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLeftDivideOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLeftDivideOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLeftDivideOp objectArray [] = *(Memory_Block_List.begin());
     if (SgLeftDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLeftDivideOp** objectArray = (SgLeftDivideOp**) &(SgLeftDivideOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLeftDivideOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLeftDivideOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLeftDivideOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLeftDivideOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLeftDivideOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLeftDivideOp** objectArray = (SgLeftDivideOp**) &(SgLeftDivideOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLeftDivideOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLeftDivideOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLeftDivideOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLeftDivideOp);

     return memory;
   }



/* #line 123448 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgRemOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgRemOp objectArray [] = *(Memory_Block_List.begin());
     if (SgRemOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRemOp** objectArray = (SgRemOp**) &(SgRemOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRemOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgRemOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgRemOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgRemOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgRemOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRemOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgRemOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRemOp objectArray [] = *(Memory_Block_List.begin());
     if (SgRemOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRemOp** objectArray = (SgRemOp**) &(SgRemOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRemOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRemOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgRemOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgRemOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgRemOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRemOp objectArray [] = *(Memory_Block_List.begin());
     if (SgRemOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRemOp** objectArray = (SgRemOp**) &(SgRemOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgRemOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgRemOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgRemOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRemOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgRemOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgRemOp** objectArray = (SgRemOp**) &(SgRemOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgRemOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgRemOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRemOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgRemOp);

     return memory;
   }



/* #line 123680 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgReplicationOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgReplicationOp objectArray [] = *(Memory_Block_List.begin());
     if (SgReplicationOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReplicationOp** objectArray = (SgReplicationOp**) &(SgReplicationOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgReplicationOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgReplicationOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgReplicationOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgReplicationOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgReplicationOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgReplicationOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgReplicationOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgReplicationOp objectArray [] = *(Memory_Block_List.begin());
     if (SgReplicationOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReplicationOp** objectArray = (SgReplicationOp**) &(SgReplicationOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgReplicationOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgReplicationOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgReplicationOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgReplicationOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgReplicationOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgReplicationOp objectArray [] = *(Memory_Block_List.begin());
     if (SgReplicationOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReplicationOp** objectArray = (SgReplicationOp**) &(SgReplicationOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgReplicationOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgReplicationOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgReplicationOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgReplicationOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgReplicationOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgReplicationOp** objectArray = (SgReplicationOp**) &(SgReplicationOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgReplicationOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgReplicationOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgReplicationOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgReplicationOp);

     return memory;
   }



/* #line 123912 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAtOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAtOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAtOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAtOp** objectArray = (SgAtOp**) &(SgAtOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAtOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAtOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAtOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAtOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAtOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAtOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAtOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAtOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAtOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAtOp** objectArray = (SgAtOp**) &(SgAtOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAtOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAtOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAtOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAtOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAtOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAtOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAtOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAtOp** objectArray = (SgAtOp**) &(SgAtOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAtOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAtOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAtOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAtOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAtOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAtOp** objectArray = (SgAtOp**) &(SgAtOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAtOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAtOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAtOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAtOp);

     return memory;
   }



/* #line 124144 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgExprListExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgExprListExp objectArray [] = *(Memory_Block_List.begin());
     if (SgExprListExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExprListExp** objectArray = (SgExprListExp**) &(SgExprListExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExprListExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgExprListExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgExprListExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgExprListExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgExprListExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExprListExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgExprListExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExprListExp objectArray [] = *(Memory_Block_List.begin());
     if (SgExprListExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExprListExp** objectArray = (SgExprListExp**) &(SgExprListExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgExprListExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgExprListExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgExprListExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgExprListExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgExprListExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgExprListExp objectArray [] = *(Memory_Block_List.begin());
     if (SgExprListExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgExprListExp** objectArray = (SgExprListExp**) &(SgExprListExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgExprListExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgExprListExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgExprListExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExprListExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgExprListExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgExprListExp** objectArray = (SgExprListExp**) &(SgExprListExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgExprListExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgExprListExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgExprListExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgExprListExp);

     return memory;
   }



/* #line 124376 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgListExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgListExp objectArray [] = *(Memory_Block_List.begin());
     if (SgListExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgListExp** objectArray = (SgListExp**) &(SgListExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgListExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgListExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgListExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgListExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgListExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgListExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgListExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgListExp objectArray [] = *(Memory_Block_List.begin());
     if (SgListExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgListExp** objectArray = (SgListExp**) &(SgListExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgListExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgListExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgListExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgListExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgListExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgListExp objectArray [] = *(Memory_Block_List.begin());
     if (SgListExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgListExp** objectArray = (SgListExp**) &(SgListExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgListExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgListExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgListExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgListExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgListExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgListExp** objectArray = (SgListExp**) &(SgListExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgListExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgListExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgListExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgListExp);

     return memory;
   }



/* #line 124608 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTupleExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTupleExp objectArray [] = *(Memory_Block_List.begin());
     if (SgTupleExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTupleExp** objectArray = (SgTupleExp**) &(SgTupleExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTupleExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTupleExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTupleExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTupleExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTupleExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTupleExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTupleExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTupleExp objectArray [] = *(Memory_Block_List.begin());
     if (SgTupleExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTupleExp** objectArray = (SgTupleExp**) &(SgTupleExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTupleExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTupleExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTupleExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTupleExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTupleExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTupleExp objectArray [] = *(Memory_Block_List.begin());
     if (SgTupleExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTupleExp** objectArray = (SgTupleExp**) &(SgTupleExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTupleExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTupleExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTupleExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTupleExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTupleExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTupleExp** objectArray = (SgTupleExp**) &(SgTupleExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTupleExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTupleExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTupleExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTupleExp);

     return memory;
   }



/* #line 124840 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMatrixExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMatrixExp objectArray [] = *(Memory_Block_List.begin());
     if (SgMatrixExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMatrixExp** objectArray = (SgMatrixExp**) &(SgMatrixExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMatrixExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMatrixExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMatrixExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMatrixExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMatrixExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMatrixExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMatrixExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMatrixExp objectArray [] = *(Memory_Block_List.begin());
     if (SgMatrixExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMatrixExp** objectArray = (SgMatrixExp**) &(SgMatrixExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMatrixExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMatrixExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMatrixExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMatrixExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMatrixExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMatrixExp objectArray [] = *(Memory_Block_List.begin());
     if (SgMatrixExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMatrixExp** objectArray = (SgMatrixExp**) &(SgMatrixExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMatrixExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMatrixExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMatrixExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMatrixExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMatrixExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMatrixExp** objectArray = (SgMatrixExp**) &(SgMatrixExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMatrixExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMatrixExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMatrixExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMatrixExp);

     return memory;
   }



/* #line 125072 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgValueExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgValueExp objectArray [] = *(Memory_Block_List.begin());
     if (SgValueExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgValueExp** objectArray = (SgValueExp**) &(SgValueExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgValueExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgValueExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgValueExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgValueExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgValueExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgValueExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgValueExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgValueExp objectArray [] = *(Memory_Block_List.begin());
     if (SgValueExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgValueExp** objectArray = (SgValueExp**) &(SgValueExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgValueExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgValueExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgValueExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgValueExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgValueExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgValueExp objectArray [] = *(Memory_Block_List.begin());
     if (SgValueExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgValueExp** objectArray = (SgValueExp**) &(SgValueExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgValueExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgValueExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgValueExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgValueExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgValueExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgValueExp** objectArray = (SgValueExp**) &(SgValueExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgValueExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgValueExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgValueExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgValueExp);

     return memory;
   }



/* #line 125304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBoolValExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBoolValExp objectArray [] = *(Memory_Block_List.begin());
     if (SgBoolValExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBoolValExp** objectArray = (SgBoolValExp**) &(SgBoolValExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBoolValExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBoolValExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBoolValExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBoolValExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBoolValExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBoolValExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBoolValExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBoolValExp objectArray [] = *(Memory_Block_List.begin());
     if (SgBoolValExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBoolValExp** objectArray = (SgBoolValExp**) &(SgBoolValExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBoolValExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBoolValExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBoolValExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBoolValExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBoolValExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBoolValExp objectArray [] = *(Memory_Block_List.begin());
     if (SgBoolValExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBoolValExp** objectArray = (SgBoolValExp**) &(SgBoolValExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBoolValExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBoolValExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBoolValExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBoolValExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBoolValExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBoolValExp** objectArray = (SgBoolValExp**) &(SgBoolValExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBoolValExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBoolValExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBoolValExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBoolValExp);

     return memory;
   }



/* #line 125536 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgStringVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgStringVal objectArray [] = *(Memory_Block_List.begin());
     if (SgStringVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStringVal** objectArray = (SgStringVal**) &(SgStringVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStringVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgStringVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgStringVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgStringVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgStringVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStringVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgStringVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStringVal objectArray [] = *(Memory_Block_List.begin());
     if (SgStringVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStringVal** objectArray = (SgStringVal**) &(SgStringVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStringVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStringVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgStringVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgStringVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgStringVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStringVal objectArray [] = *(Memory_Block_List.begin());
     if (SgStringVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStringVal** objectArray = (SgStringVal**) &(SgStringVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgStringVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgStringVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgStringVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStringVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgStringVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgStringVal** objectArray = (SgStringVal**) &(SgStringVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgStringVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgStringVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStringVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgStringVal);

     return memory;
   }



/* #line 125768 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgShortVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgShortVal objectArray [] = *(Memory_Block_List.begin());
     if (SgShortVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgShortVal** objectArray = (SgShortVal**) &(SgShortVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgShortVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgShortVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgShortVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgShortVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgShortVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgShortVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgShortVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgShortVal objectArray [] = *(Memory_Block_List.begin());
     if (SgShortVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgShortVal** objectArray = (SgShortVal**) &(SgShortVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgShortVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgShortVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgShortVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgShortVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgShortVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgShortVal objectArray [] = *(Memory_Block_List.begin());
     if (SgShortVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgShortVal** objectArray = (SgShortVal**) &(SgShortVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgShortVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgShortVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgShortVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgShortVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgShortVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgShortVal** objectArray = (SgShortVal**) &(SgShortVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgShortVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgShortVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgShortVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgShortVal);

     return memory;
   }



/* #line 126000 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCharVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCharVal objectArray [] = *(Memory_Block_List.begin());
     if (SgCharVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCharVal** objectArray = (SgCharVal**) &(SgCharVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCharVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCharVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCharVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCharVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCharVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCharVal objectArray [] = *(Memory_Block_List.begin());
     if (SgCharVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCharVal** objectArray = (SgCharVal**) &(SgCharVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCharVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCharVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCharVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCharVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCharVal objectArray [] = *(Memory_Block_List.begin());
     if (SgCharVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCharVal** objectArray = (SgCharVal**) &(SgCharVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCharVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCharVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCharVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCharVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCharVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCharVal** objectArray = (SgCharVal**) &(SgCharVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCharVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCharVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCharVal);

     return memory;
   }



/* #line 126232 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUnsignedCharVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUnsignedCharVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedCharVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedCharVal** objectArray = (SgUnsignedCharVal**) &(SgUnsignedCharVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnsignedCharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUnsignedCharVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUnsignedCharVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUnsignedCharVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUnsignedCharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnsignedCharVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUnsignedCharVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnsignedCharVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedCharVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedCharVal** objectArray = (SgUnsignedCharVal**) &(SgUnsignedCharVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnsignedCharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnsignedCharVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUnsignedCharVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUnsignedCharVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUnsignedCharVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnsignedCharVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedCharVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedCharVal** objectArray = (SgUnsignedCharVal**) &(SgUnsignedCharVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUnsignedCharVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUnsignedCharVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUnsignedCharVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnsignedCharVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUnsignedCharVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUnsignedCharVal** objectArray = (SgUnsignedCharVal**) &(SgUnsignedCharVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUnsignedCharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUnsignedCharVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnsignedCharVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUnsignedCharVal);

     return memory;
   }



/* #line 126464 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgWcharVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgWcharVal objectArray [] = *(Memory_Block_List.begin());
     if (SgWcharVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWcharVal** objectArray = (SgWcharVal**) &(SgWcharVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWcharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgWcharVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgWcharVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgWcharVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgWcharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWcharVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgWcharVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWcharVal objectArray [] = *(Memory_Block_List.begin());
     if (SgWcharVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWcharVal** objectArray = (SgWcharVal**) &(SgWcharVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgWcharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgWcharVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgWcharVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgWcharVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgWcharVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgWcharVal objectArray [] = *(Memory_Block_List.begin());
     if (SgWcharVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgWcharVal** objectArray = (SgWcharVal**) &(SgWcharVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgWcharVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgWcharVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgWcharVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWcharVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgWcharVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgWcharVal** objectArray = (SgWcharVal**) &(SgWcharVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgWcharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgWcharVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgWcharVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgWcharVal);

     return memory;
   }



/* #line 126696 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUnsignedShortVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUnsignedShortVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedShortVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedShortVal** objectArray = (SgUnsignedShortVal**) &(SgUnsignedShortVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnsignedShortVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUnsignedShortVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUnsignedShortVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUnsignedShortVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUnsignedShortVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnsignedShortVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUnsignedShortVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnsignedShortVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedShortVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedShortVal** objectArray = (SgUnsignedShortVal**) &(SgUnsignedShortVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnsignedShortVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnsignedShortVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUnsignedShortVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUnsignedShortVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUnsignedShortVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnsignedShortVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedShortVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedShortVal** objectArray = (SgUnsignedShortVal**) &(SgUnsignedShortVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUnsignedShortVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUnsignedShortVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUnsignedShortVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnsignedShortVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUnsignedShortVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUnsignedShortVal** objectArray = (SgUnsignedShortVal**) &(SgUnsignedShortVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUnsignedShortVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUnsignedShortVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnsignedShortVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUnsignedShortVal);

     return memory;
   }



/* #line 126928 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIntVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntVal** objectArray = (SgIntVal**) &(SgIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIntVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIntVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIntVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntVal** objectArray = (SgIntVal**) &(SgIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIntVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIntVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIntVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntVal** objectArray = (SgIntVal**) &(SgIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIntVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIntVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIntVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIntVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIntVal** objectArray = (SgIntVal**) &(SgIntVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIntVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIntVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIntVal);

     return memory;
   }



/* #line 127160 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgEnumVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgEnumVal objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumVal** objectArray = (SgEnumVal**) &(SgEnumVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEnumVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgEnumVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgEnumVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgEnumVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgEnumVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEnumVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgEnumVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEnumVal objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumVal** objectArray = (SgEnumVal**) &(SgEnumVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEnumVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEnumVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgEnumVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgEnumVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgEnumVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEnumVal objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumVal** objectArray = (SgEnumVal**) &(SgEnumVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgEnumVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgEnumVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgEnumVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEnumVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgEnumVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgEnumVal** objectArray = (SgEnumVal**) &(SgEnumVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgEnumVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgEnumVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEnumVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgEnumVal);

     return memory;
   }



/* #line 127392 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUnsignedIntVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUnsignedIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedIntVal** objectArray = (SgUnsignedIntVal**) &(SgUnsignedIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnsignedIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUnsignedIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUnsignedIntVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUnsignedIntVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUnsignedIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnsignedIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUnsignedIntVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnsignedIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedIntVal** objectArray = (SgUnsignedIntVal**) &(SgUnsignedIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnsignedIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnsignedIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUnsignedIntVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUnsignedIntVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUnsignedIntVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnsignedIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedIntVal** objectArray = (SgUnsignedIntVal**) &(SgUnsignedIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUnsignedIntVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUnsignedIntVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUnsignedIntVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnsignedIntVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUnsignedIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUnsignedIntVal** objectArray = (SgUnsignedIntVal**) &(SgUnsignedIntVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUnsignedIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUnsignedIntVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnsignedIntVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUnsignedIntVal);

     return memory;
   }



/* #line 127624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLongIntVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLongIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgLongIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLongIntVal** objectArray = (SgLongIntVal**) &(SgLongIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLongIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLongIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLongIntVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLongIntVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLongIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLongIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLongIntVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLongIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgLongIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLongIntVal** objectArray = (SgLongIntVal**) &(SgLongIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLongIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLongIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLongIntVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLongIntVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLongIntVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLongIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgLongIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLongIntVal** objectArray = (SgLongIntVal**) &(SgLongIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLongIntVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLongIntVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLongIntVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLongIntVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLongIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLongIntVal** objectArray = (SgLongIntVal**) &(SgLongIntVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLongIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLongIntVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLongIntVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLongIntVal);

     return memory;
   }



/* #line 127856 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLongLongIntVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLongLongIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgLongLongIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLongLongIntVal** objectArray = (SgLongLongIntVal**) &(SgLongLongIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLongLongIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLongLongIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLongLongIntVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLongLongIntVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLongLongIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLongLongIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLongLongIntVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLongLongIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgLongLongIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLongLongIntVal** objectArray = (SgLongLongIntVal**) &(SgLongLongIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLongLongIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLongLongIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLongLongIntVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLongLongIntVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLongLongIntVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLongLongIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgLongLongIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLongLongIntVal** objectArray = (SgLongLongIntVal**) &(SgLongLongIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLongLongIntVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLongLongIntVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLongLongIntVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLongLongIntVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLongLongIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLongLongIntVal** objectArray = (SgLongLongIntVal**) &(SgLongLongIntVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLongLongIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLongLongIntVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLongLongIntVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLongLongIntVal);

     return memory;
   }



/* #line 128088 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUnsignedLongLongIntVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUnsignedLongLongIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedLongLongIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedLongLongIntVal** objectArray = (SgUnsignedLongLongIntVal**) &(SgUnsignedLongLongIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnsignedLongLongIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUnsignedLongLongIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUnsignedLongLongIntVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUnsignedLongLongIntVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUnsignedLongLongIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnsignedLongLongIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUnsignedLongLongIntVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnsignedLongLongIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedLongLongIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedLongLongIntVal** objectArray = (SgUnsignedLongLongIntVal**) &(SgUnsignedLongLongIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnsignedLongLongIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnsignedLongLongIntVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUnsignedLongLongIntVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUnsignedLongLongIntVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUnsignedLongLongIntVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnsignedLongLongIntVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedLongLongIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedLongLongIntVal** objectArray = (SgUnsignedLongLongIntVal**) &(SgUnsignedLongLongIntVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUnsignedLongLongIntVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUnsignedLongLongIntVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUnsignedLongLongIntVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnsignedLongLongIntVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUnsignedLongLongIntVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUnsignedLongLongIntVal** objectArray = (SgUnsignedLongLongIntVal**) &(SgUnsignedLongLongIntVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUnsignedLongLongIntVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUnsignedLongLongIntVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnsignedLongLongIntVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUnsignedLongLongIntVal);

     return memory;
   }



/* #line 128320 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUnsignedLongVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUnsignedLongVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedLongVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedLongVal** objectArray = (SgUnsignedLongVal**) &(SgUnsignedLongVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnsignedLongVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUnsignedLongVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUnsignedLongVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUnsignedLongVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUnsignedLongVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnsignedLongVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUnsignedLongVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnsignedLongVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedLongVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedLongVal** objectArray = (SgUnsignedLongVal**) &(SgUnsignedLongVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnsignedLongVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnsignedLongVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUnsignedLongVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUnsignedLongVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUnsignedLongVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnsignedLongVal objectArray [] = *(Memory_Block_List.begin());
     if (SgUnsignedLongVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnsignedLongVal** objectArray = (SgUnsignedLongVal**) &(SgUnsignedLongVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUnsignedLongVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUnsignedLongVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUnsignedLongVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnsignedLongVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUnsignedLongVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUnsignedLongVal** objectArray = (SgUnsignedLongVal**) &(SgUnsignedLongVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUnsignedLongVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUnsignedLongVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnsignedLongVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUnsignedLongVal);

     return memory;
   }



/* #line 128552 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFloatVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFloatVal objectArray [] = *(Memory_Block_List.begin());
     if (SgFloatVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloatVal** objectArray = (SgFloatVal**) &(SgFloatVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloatVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFloatVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFloatVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFloatVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFloatVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloatVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFloatVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloatVal objectArray [] = *(Memory_Block_List.begin());
     if (SgFloatVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloatVal** objectArray = (SgFloatVal**) &(SgFloatVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloatVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloatVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFloatVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFloatVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFloatVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloatVal objectArray [] = *(Memory_Block_List.begin());
     if (SgFloatVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloatVal** objectArray = (SgFloatVal**) &(SgFloatVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFloatVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFloatVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFloatVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloatVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFloatVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFloatVal** objectArray = (SgFloatVal**) &(SgFloatVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFloatVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFloatVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloatVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFloatVal);

     return memory;
   }



/* #line 128784 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDoubleVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDoubleVal objectArray [] = *(Memory_Block_List.begin());
     if (SgDoubleVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDoubleVal** objectArray = (SgDoubleVal**) &(SgDoubleVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDoubleVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDoubleVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDoubleVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDoubleVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDoubleVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDoubleVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDoubleVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDoubleVal objectArray [] = *(Memory_Block_List.begin());
     if (SgDoubleVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDoubleVal** objectArray = (SgDoubleVal**) &(SgDoubleVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDoubleVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDoubleVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDoubleVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDoubleVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDoubleVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDoubleVal objectArray [] = *(Memory_Block_List.begin());
     if (SgDoubleVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDoubleVal** objectArray = (SgDoubleVal**) &(SgDoubleVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDoubleVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDoubleVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDoubleVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDoubleVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDoubleVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDoubleVal** objectArray = (SgDoubleVal**) &(SgDoubleVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDoubleVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDoubleVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDoubleVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDoubleVal);

     return memory;
   }



/* #line 129016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLongDoubleVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLongDoubleVal objectArray [] = *(Memory_Block_List.begin());
     if (SgLongDoubleVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLongDoubleVal** objectArray = (SgLongDoubleVal**) &(SgLongDoubleVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLongDoubleVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLongDoubleVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLongDoubleVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLongDoubleVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLongDoubleVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLongDoubleVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLongDoubleVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLongDoubleVal objectArray [] = *(Memory_Block_List.begin());
     if (SgLongDoubleVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLongDoubleVal** objectArray = (SgLongDoubleVal**) &(SgLongDoubleVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLongDoubleVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLongDoubleVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLongDoubleVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLongDoubleVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLongDoubleVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLongDoubleVal objectArray [] = *(Memory_Block_List.begin());
     if (SgLongDoubleVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLongDoubleVal** objectArray = (SgLongDoubleVal**) &(SgLongDoubleVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLongDoubleVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLongDoubleVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLongDoubleVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLongDoubleVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLongDoubleVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLongDoubleVal** objectArray = (SgLongDoubleVal**) &(SgLongDoubleVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLongDoubleVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLongDoubleVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLongDoubleVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLongDoubleVal);

     return memory;
   }



/* #line 129248 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgComplexVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgComplexVal objectArray [] = *(Memory_Block_List.begin());
     if (SgComplexVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgComplexVal** objectArray = (SgComplexVal**) &(SgComplexVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgComplexVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgComplexVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgComplexVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgComplexVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgComplexVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgComplexVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgComplexVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgComplexVal objectArray [] = *(Memory_Block_List.begin());
     if (SgComplexVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgComplexVal** objectArray = (SgComplexVal**) &(SgComplexVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgComplexVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgComplexVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgComplexVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgComplexVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgComplexVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgComplexVal objectArray [] = *(Memory_Block_List.begin());
     if (SgComplexVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgComplexVal** objectArray = (SgComplexVal**) &(SgComplexVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgComplexVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgComplexVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgComplexVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgComplexVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgComplexVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgComplexVal** objectArray = (SgComplexVal**) &(SgComplexVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgComplexVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgComplexVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgComplexVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgComplexVal);

     return memory;
   }



/* #line 129480 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUpcThreads::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUpcThreads objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcThreads::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcThreads** objectArray = (SgUpcThreads**) &(SgUpcThreads::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcThreads::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUpcThreads::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUpcThreads::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUpcThreads*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUpcThreads::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcThreads::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUpcThreads::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcThreads objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcThreads::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcThreads** objectArray = (SgUpcThreads**) &(SgUpcThreads::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcThreads::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcThreads::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUpcThreads object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUpcThreads object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUpcThreads::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcThreads objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcThreads::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcThreads** objectArray = (SgUpcThreads**) &(SgUpcThreads::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUpcThreads::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUpcThreads::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUpcThreads found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcThreads::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUpcThreads::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUpcThreads** objectArray = (SgUpcThreads**) &(SgUpcThreads::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUpcThreads::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUpcThreads::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcThreads::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUpcThreads);

     return memory;
   }



/* #line 129712 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUpcMythread::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUpcMythread objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcMythread::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcMythread** objectArray = (SgUpcMythread**) &(SgUpcMythread::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcMythread::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUpcMythread::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUpcMythread::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUpcMythread*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUpcMythread::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcMythread::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUpcMythread::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcMythread objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcMythread::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcMythread** objectArray = (SgUpcMythread**) &(SgUpcMythread::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcMythread::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcMythread::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUpcMythread object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUpcMythread object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUpcMythread::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcMythread objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcMythread::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcMythread** objectArray = (SgUpcMythread**) &(SgUpcMythread::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUpcMythread::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUpcMythread::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUpcMythread found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcMythread::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUpcMythread::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUpcMythread** objectArray = (SgUpcMythread**) &(SgUpcMythread::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUpcMythread::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUpcMythread::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcMythread::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUpcMythread);

     return memory;
   }



/* #line 129944 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateParameterVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateParameterVal objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateParameterVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateParameterVal** objectArray = (SgTemplateParameterVal**) &(SgTemplateParameterVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateParameterVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateParameterVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateParameterVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateParameterVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateParameterVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateParameterVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateParameterVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateParameterVal objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateParameterVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateParameterVal** objectArray = (SgTemplateParameterVal**) &(SgTemplateParameterVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateParameterVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateParameterVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateParameterVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateParameterVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateParameterVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateParameterVal objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateParameterVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateParameterVal** objectArray = (SgTemplateParameterVal**) &(SgTemplateParameterVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateParameterVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateParameterVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateParameterVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateParameterVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateParameterVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateParameterVal** objectArray = (SgTemplateParameterVal**) &(SgTemplateParameterVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateParameterVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateParameterVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateParameterVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateParameterVal);

     return memory;
   }



/* #line 130176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNullptrValExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNullptrValExp objectArray [] = *(Memory_Block_List.begin());
     if (SgNullptrValExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNullptrValExp** objectArray = (SgNullptrValExp**) &(SgNullptrValExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNullptrValExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNullptrValExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNullptrValExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNullptrValExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNullptrValExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNullptrValExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNullptrValExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNullptrValExp objectArray [] = *(Memory_Block_List.begin());
     if (SgNullptrValExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNullptrValExp** objectArray = (SgNullptrValExp**) &(SgNullptrValExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNullptrValExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNullptrValExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNullptrValExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNullptrValExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNullptrValExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNullptrValExp objectArray [] = *(Memory_Block_List.begin());
     if (SgNullptrValExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNullptrValExp** objectArray = (SgNullptrValExp**) &(SgNullptrValExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNullptrValExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNullptrValExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNullptrValExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNullptrValExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNullptrValExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNullptrValExp** objectArray = (SgNullptrValExp**) &(SgNullptrValExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNullptrValExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNullptrValExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNullptrValExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNullptrValExp);

     return memory;
   }



/* #line 130408 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgChar16Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgChar16Val objectArray [] = *(Memory_Block_List.begin());
     if (SgChar16Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgChar16Val** objectArray = (SgChar16Val**) &(SgChar16Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgChar16Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgChar16Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgChar16Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgChar16Val*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgChar16Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgChar16Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgChar16Val::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgChar16Val objectArray [] = *(Memory_Block_List.begin());
     if (SgChar16Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgChar16Val** objectArray = (SgChar16Val**) &(SgChar16Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgChar16Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgChar16Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgChar16Val object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgChar16Val object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgChar16Val::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgChar16Val objectArray [] = *(Memory_Block_List.begin());
     if (SgChar16Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgChar16Val** objectArray = (SgChar16Val**) &(SgChar16Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgChar16Val::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgChar16Val::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgChar16Val found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgChar16Val::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgChar16Val::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgChar16Val** objectArray = (SgChar16Val**) &(SgChar16Val::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgChar16Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgChar16Val::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgChar16Val::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgChar16Val);

     return memory;
   }



/* #line 130640 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgChar32Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgChar32Val objectArray [] = *(Memory_Block_List.begin());
     if (SgChar32Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgChar32Val** objectArray = (SgChar32Val**) &(SgChar32Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgChar32Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgChar32Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgChar32Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgChar32Val*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgChar32Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgChar32Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgChar32Val::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgChar32Val objectArray [] = *(Memory_Block_List.begin());
     if (SgChar32Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgChar32Val** objectArray = (SgChar32Val**) &(SgChar32Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgChar32Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgChar32Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgChar32Val object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgChar32Val object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgChar32Val::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgChar32Val objectArray [] = *(Memory_Block_List.begin());
     if (SgChar32Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgChar32Val** objectArray = (SgChar32Val**) &(SgChar32Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgChar32Val::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgChar32Val::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgChar32Val found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgChar32Val::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgChar32Val::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgChar32Val** objectArray = (SgChar32Val**) &(SgChar32Val::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgChar32Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgChar32Val::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgChar32Val::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgChar32Val);

     return memory;
   }



/* #line 130872 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFloat80Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFloat80Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat80Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat80Val** objectArray = (SgFloat80Val**) &(SgFloat80Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloat80Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFloat80Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFloat80Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFloat80Val*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFloat80Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloat80Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFloat80Val::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloat80Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat80Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat80Val** objectArray = (SgFloat80Val**) &(SgFloat80Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloat80Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloat80Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFloat80Val object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFloat80Val object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFloat80Val::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloat80Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat80Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat80Val** objectArray = (SgFloat80Val**) &(SgFloat80Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFloat80Val::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFloat80Val::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFloat80Val found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloat80Val::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFloat80Val::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFloat80Val** objectArray = (SgFloat80Val**) &(SgFloat80Val::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFloat80Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFloat80Val::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloat80Val::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFloat80Val);

     return memory;
   }



/* #line 131104 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFloat128Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFloat128Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat128Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat128Val** objectArray = (SgFloat128Val**) &(SgFloat128Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloat128Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFloat128Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFloat128Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFloat128Val*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFloat128Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloat128Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFloat128Val::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloat128Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat128Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat128Val** objectArray = (SgFloat128Val**) &(SgFloat128Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloat128Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloat128Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFloat128Val object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFloat128Val object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFloat128Val::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloat128Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat128Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat128Val** objectArray = (SgFloat128Val**) &(SgFloat128Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFloat128Val::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFloat128Val::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFloat128Val found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloat128Val::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFloat128Val::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFloat128Val** objectArray = (SgFloat128Val**) &(SgFloat128Val::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFloat128Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFloat128Val::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloat128Val::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFloat128Val);

     return memory;
   }



/* #line 131336 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgVoidVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgVoidVal objectArray [] = *(Memory_Block_List.begin());
     if (SgVoidVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVoidVal** objectArray = (SgVoidVal**) &(SgVoidVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVoidVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgVoidVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgVoidVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgVoidVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgVoidVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVoidVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgVoidVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVoidVal objectArray [] = *(Memory_Block_List.begin());
     if (SgVoidVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVoidVal** objectArray = (SgVoidVal**) &(SgVoidVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVoidVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVoidVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgVoidVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgVoidVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgVoidVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVoidVal objectArray [] = *(Memory_Block_List.begin());
     if (SgVoidVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVoidVal** objectArray = (SgVoidVal**) &(SgVoidVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgVoidVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgVoidVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgVoidVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVoidVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgVoidVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgVoidVal** objectArray = (SgVoidVal**) &(SgVoidVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgVoidVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgVoidVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVoidVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgVoidVal);

     return memory;
   }



/* #line 131568 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaFloatVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaFloatVal objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFloatVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFloatVal** objectArray = (SgAdaFloatVal**) &(SgAdaFloatVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaFloatVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaFloatVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaFloatVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaFloatVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaFloatVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaFloatVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaFloatVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaFloatVal objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFloatVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFloatVal** objectArray = (SgAdaFloatVal**) &(SgAdaFloatVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaFloatVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaFloatVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaFloatVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaFloatVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaFloatVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaFloatVal objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFloatVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFloatVal** objectArray = (SgAdaFloatVal**) &(SgAdaFloatVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaFloatVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaFloatVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaFloatVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaFloatVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaFloatVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaFloatVal** objectArray = (SgAdaFloatVal**) &(SgAdaFloatVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaFloatVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaFloatVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaFloatVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaFloatVal);

     return memory;
   }



/* #line 131800 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSignedCharVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSignedCharVal objectArray [] = *(Memory_Block_List.begin());
     if (SgSignedCharVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSignedCharVal** objectArray = (SgSignedCharVal**) &(SgSignedCharVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSignedCharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSignedCharVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSignedCharVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSignedCharVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSignedCharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSignedCharVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSignedCharVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSignedCharVal objectArray [] = *(Memory_Block_List.begin());
     if (SgSignedCharVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSignedCharVal** objectArray = (SgSignedCharVal**) &(SgSignedCharVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSignedCharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSignedCharVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSignedCharVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSignedCharVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSignedCharVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSignedCharVal objectArray [] = *(Memory_Block_List.begin());
     if (SgSignedCharVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSignedCharVal** objectArray = (SgSignedCharVal**) &(SgSignedCharVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSignedCharVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSignedCharVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSignedCharVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSignedCharVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSignedCharVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSignedCharVal** objectArray = (SgSignedCharVal**) &(SgSignedCharVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSignedCharVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSignedCharVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSignedCharVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSignedCharVal);

     return memory;
   }



/* #line 132032 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBFloat16Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBFloat16Val objectArray [] = *(Memory_Block_List.begin());
     if (SgBFloat16Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBFloat16Val** objectArray = (SgBFloat16Val**) &(SgBFloat16Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBFloat16Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBFloat16Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBFloat16Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBFloat16Val*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBFloat16Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBFloat16Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBFloat16Val::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBFloat16Val objectArray [] = *(Memory_Block_List.begin());
     if (SgBFloat16Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBFloat16Val** objectArray = (SgBFloat16Val**) &(SgBFloat16Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBFloat16Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBFloat16Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBFloat16Val object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBFloat16Val object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBFloat16Val::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBFloat16Val objectArray [] = *(Memory_Block_List.begin());
     if (SgBFloat16Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBFloat16Val** objectArray = (SgBFloat16Val**) &(SgBFloat16Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBFloat16Val::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBFloat16Val::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBFloat16Val found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBFloat16Val::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBFloat16Val::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBFloat16Val** objectArray = (SgBFloat16Val**) &(SgBFloat16Val::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBFloat16Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBFloat16Val::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBFloat16Val::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBFloat16Val);

     return memory;
   }



/* #line 132264 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFloat16Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFloat16Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat16Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat16Val** objectArray = (SgFloat16Val**) &(SgFloat16Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloat16Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFloat16Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFloat16Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFloat16Val*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFloat16Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloat16Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFloat16Val::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloat16Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat16Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat16Val** objectArray = (SgFloat16Val**) &(SgFloat16Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloat16Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloat16Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFloat16Val object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFloat16Val object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFloat16Val::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloat16Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat16Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat16Val** objectArray = (SgFloat16Val**) &(SgFloat16Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFloat16Val::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFloat16Val::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFloat16Val found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloat16Val::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFloat16Val::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFloat16Val** objectArray = (SgFloat16Val**) &(SgFloat16Val::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFloat16Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFloat16Val::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloat16Val::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFloat16Val);

     return memory;
   }



/* #line 132496 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFloat32Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFloat32Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat32Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat32Val** objectArray = (SgFloat32Val**) &(SgFloat32Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloat32Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFloat32Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFloat32Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFloat32Val*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFloat32Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloat32Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFloat32Val::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloat32Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat32Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat32Val** objectArray = (SgFloat32Val**) &(SgFloat32Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloat32Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloat32Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFloat32Val object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFloat32Val object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFloat32Val::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloat32Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat32Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat32Val** objectArray = (SgFloat32Val**) &(SgFloat32Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFloat32Val::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFloat32Val::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFloat32Val found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloat32Val::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFloat32Val::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFloat32Val** objectArray = (SgFloat32Val**) &(SgFloat32Val::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFloat32Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFloat32Val::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloat32Val::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFloat32Val);

     return memory;
   }



/* #line 132728 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFloat64Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFloat64Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat64Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat64Val** objectArray = (SgFloat64Val**) &(SgFloat64Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloat64Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFloat64Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFloat64Val::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFloat64Val*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFloat64Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloat64Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFloat64Val::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloat64Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat64Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat64Val** objectArray = (SgFloat64Val**) &(SgFloat64Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFloat64Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFloat64Val::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFloat64Val object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFloat64Val object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFloat64Val::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFloat64Val objectArray [] = *(Memory_Block_List.begin());
     if (SgFloat64Val::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFloat64Val** objectArray = (SgFloat64Val**) &(SgFloat64Val::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFloat64Val::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFloat64Val::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFloat64Val found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloat64Val::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFloat64Val::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFloat64Val** objectArray = (SgFloat64Val**) &(SgFloat64Val::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFloat64Val::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFloat64Val::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFloat64Val::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFloat64Val);

     return memory;
   }



/* #line 132960 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJovialBitVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJovialBitVal objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialBitVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialBitVal** objectArray = (SgJovialBitVal**) &(SgJovialBitVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialBitVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJovialBitVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJovialBitVal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJovialBitVal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJovialBitVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialBitVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJovialBitVal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialBitVal objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialBitVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialBitVal** objectArray = (SgJovialBitVal**) &(SgJovialBitVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialBitVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialBitVal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJovialBitVal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJovialBitVal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJovialBitVal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialBitVal objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialBitVal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialBitVal** objectArray = (SgJovialBitVal**) &(SgJovialBitVal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJovialBitVal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJovialBitVal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJovialBitVal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialBitVal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJovialBitVal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJovialBitVal** objectArray = (SgJovialBitVal**) &(SgJovialBitVal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJovialBitVal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJovialBitVal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialBitVal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJovialBitVal);

     return memory;
   }



/* #line 133192 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCallExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCallExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgCallExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCallExpression** objectArray = (SgCallExpression**) &(SgCallExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCallExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCallExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCallExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCallExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCallExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCallExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCallExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCallExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgCallExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCallExpression** objectArray = (SgCallExpression**) &(SgCallExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCallExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCallExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCallExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCallExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCallExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCallExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgCallExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCallExpression** objectArray = (SgCallExpression**) &(SgCallExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCallExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCallExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCallExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCallExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCallExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCallExpression** objectArray = (SgCallExpression**) &(SgCallExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCallExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCallExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCallExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCallExpression);

     return memory;
   }



/* #line 133424 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFunctionCallExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFunctionCallExp objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionCallExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionCallExp** objectArray = (SgFunctionCallExp**) &(SgFunctionCallExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionCallExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFunctionCallExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFunctionCallExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFunctionCallExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFunctionCallExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionCallExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFunctionCallExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionCallExp objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionCallExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionCallExp** objectArray = (SgFunctionCallExp**) &(SgFunctionCallExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionCallExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionCallExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFunctionCallExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFunctionCallExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFunctionCallExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionCallExp objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionCallExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionCallExp** objectArray = (SgFunctionCallExp**) &(SgFunctionCallExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFunctionCallExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFunctionCallExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFunctionCallExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionCallExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFunctionCallExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFunctionCallExp** objectArray = (SgFunctionCallExp**) &(SgFunctionCallExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFunctionCallExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFunctionCallExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionCallExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFunctionCallExp);

     return memory;
   }



/* #line 133656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCudaKernelCallExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCudaKernelCallExp objectArray [] = *(Memory_Block_List.begin());
     if (SgCudaKernelCallExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCudaKernelCallExp** objectArray = (SgCudaKernelCallExp**) &(SgCudaKernelCallExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCudaKernelCallExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCudaKernelCallExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCudaKernelCallExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCudaKernelCallExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCudaKernelCallExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCudaKernelCallExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCudaKernelCallExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCudaKernelCallExp objectArray [] = *(Memory_Block_List.begin());
     if (SgCudaKernelCallExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCudaKernelCallExp** objectArray = (SgCudaKernelCallExp**) &(SgCudaKernelCallExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCudaKernelCallExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCudaKernelCallExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCudaKernelCallExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCudaKernelCallExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCudaKernelCallExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCudaKernelCallExp objectArray [] = *(Memory_Block_List.begin());
     if (SgCudaKernelCallExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCudaKernelCallExp** objectArray = (SgCudaKernelCallExp**) &(SgCudaKernelCallExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCudaKernelCallExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCudaKernelCallExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCudaKernelCallExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCudaKernelCallExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCudaKernelCallExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCudaKernelCallExp** objectArray = (SgCudaKernelCallExp**) &(SgCudaKernelCallExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCudaKernelCallExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCudaKernelCallExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCudaKernelCallExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCudaKernelCallExp);

     return memory;
   }



/* #line 133888 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUpcLocalsizeofExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUpcLocalsizeofExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcLocalsizeofExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcLocalsizeofExpression** objectArray = (SgUpcLocalsizeofExpression**) &(SgUpcLocalsizeofExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcLocalsizeofExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUpcLocalsizeofExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUpcLocalsizeofExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUpcLocalsizeofExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUpcLocalsizeofExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcLocalsizeofExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUpcLocalsizeofExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcLocalsizeofExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcLocalsizeofExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcLocalsizeofExpression** objectArray = (SgUpcLocalsizeofExpression**) &(SgUpcLocalsizeofExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcLocalsizeofExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcLocalsizeofExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUpcLocalsizeofExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUpcLocalsizeofExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUpcLocalsizeofExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcLocalsizeofExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcLocalsizeofExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcLocalsizeofExpression** objectArray = (SgUpcLocalsizeofExpression**) &(SgUpcLocalsizeofExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUpcLocalsizeofExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUpcLocalsizeofExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUpcLocalsizeofExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcLocalsizeofExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUpcLocalsizeofExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUpcLocalsizeofExpression** objectArray = (SgUpcLocalsizeofExpression**) &(SgUpcLocalsizeofExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUpcLocalsizeofExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUpcLocalsizeofExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcLocalsizeofExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUpcLocalsizeofExpression);

     return memory;
   }



/* #line 134120 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUpcBlocksizeofExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUpcBlocksizeofExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcBlocksizeofExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcBlocksizeofExpression** objectArray = (SgUpcBlocksizeofExpression**) &(SgUpcBlocksizeofExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcBlocksizeofExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUpcBlocksizeofExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUpcBlocksizeofExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUpcBlocksizeofExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUpcBlocksizeofExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcBlocksizeofExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUpcBlocksizeofExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcBlocksizeofExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcBlocksizeofExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcBlocksizeofExpression** objectArray = (SgUpcBlocksizeofExpression**) &(SgUpcBlocksizeofExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcBlocksizeofExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcBlocksizeofExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUpcBlocksizeofExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUpcBlocksizeofExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUpcBlocksizeofExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcBlocksizeofExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcBlocksizeofExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcBlocksizeofExpression** objectArray = (SgUpcBlocksizeofExpression**) &(SgUpcBlocksizeofExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUpcBlocksizeofExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUpcBlocksizeofExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUpcBlocksizeofExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcBlocksizeofExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUpcBlocksizeofExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUpcBlocksizeofExpression** objectArray = (SgUpcBlocksizeofExpression**) &(SgUpcBlocksizeofExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUpcBlocksizeofExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUpcBlocksizeofExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcBlocksizeofExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUpcBlocksizeofExpression);

     return memory;
   }



/* #line 134352 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUpcElemsizeofExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUpcElemsizeofExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcElemsizeofExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcElemsizeofExpression** objectArray = (SgUpcElemsizeofExpression**) &(SgUpcElemsizeofExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcElemsizeofExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUpcElemsizeofExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUpcElemsizeofExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUpcElemsizeofExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUpcElemsizeofExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcElemsizeofExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUpcElemsizeofExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcElemsizeofExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcElemsizeofExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcElemsizeofExpression** objectArray = (SgUpcElemsizeofExpression**) &(SgUpcElemsizeofExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUpcElemsizeofExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUpcElemsizeofExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUpcElemsizeofExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUpcElemsizeofExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUpcElemsizeofExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUpcElemsizeofExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgUpcElemsizeofExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUpcElemsizeofExpression** objectArray = (SgUpcElemsizeofExpression**) &(SgUpcElemsizeofExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUpcElemsizeofExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUpcElemsizeofExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUpcElemsizeofExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcElemsizeofExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUpcElemsizeofExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUpcElemsizeofExpression** objectArray = (SgUpcElemsizeofExpression**) &(SgUpcElemsizeofExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUpcElemsizeofExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUpcElemsizeofExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUpcElemsizeofExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUpcElemsizeofExpression);

     return memory;
   }



/* #line 134584 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaInstanceOfOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaInstanceOfOp objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaInstanceOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaInstanceOfOp** objectArray = (SgJavaInstanceOfOp**) &(SgJavaInstanceOfOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaInstanceOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaInstanceOfOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaInstanceOfOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaInstanceOfOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaInstanceOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaInstanceOfOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaInstanceOfOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaInstanceOfOp objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaInstanceOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaInstanceOfOp** objectArray = (SgJavaInstanceOfOp**) &(SgJavaInstanceOfOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaInstanceOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaInstanceOfOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaInstanceOfOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaInstanceOfOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaInstanceOfOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaInstanceOfOp objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaInstanceOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaInstanceOfOp** objectArray = (SgJavaInstanceOfOp**) &(SgJavaInstanceOfOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaInstanceOfOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaInstanceOfOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaInstanceOfOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaInstanceOfOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaInstanceOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaInstanceOfOp** objectArray = (SgJavaInstanceOfOp**) &(SgJavaInstanceOfOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaInstanceOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaInstanceOfOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaInstanceOfOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaInstanceOfOp);

     return memory;
   }



/* #line 134816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSuperExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSuperExp objectArray [] = *(Memory_Block_List.begin());
     if (SgSuperExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSuperExp** objectArray = (SgSuperExp**) &(SgSuperExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSuperExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSuperExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSuperExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSuperExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSuperExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSuperExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSuperExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSuperExp objectArray [] = *(Memory_Block_List.begin());
     if (SgSuperExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSuperExp** objectArray = (SgSuperExp**) &(SgSuperExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSuperExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSuperExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSuperExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSuperExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSuperExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSuperExp objectArray [] = *(Memory_Block_List.begin());
     if (SgSuperExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSuperExp** objectArray = (SgSuperExp**) &(SgSuperExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSuperExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSuperExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSuperExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSuperExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSuperExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSuperExp** objectArray = (SgSuperExp**) &(SgSuperExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSuperExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSuperExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSuperExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSuperExp);

     return memory;
   }



/* #line 135048 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeIdOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeIdOp objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeIdOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeIdOp** objectArray = (SgTypeIdOp**) &(SgTypeIdOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeIdOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeIdOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeIdOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeIdOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeIdOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeIdOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeIdOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeIdOp objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeIdOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeIdOp** objectArray = (SgTypeIdOp**) &(SgTypeIdOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeIdOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeIdOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeIdOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeIdOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeIdOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeIdOp objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeIdOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeIdOp** objectArray = (SgTypeIdOp**) &(SgTypeIdOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeIdOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeIdOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeIdOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeIdOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeIdOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeIdOp** objectArray = (SgTypeIdOp**) &(SgTypeIdOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeIdOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeIdOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeIdOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeIdOp);

     return memory;
   }



/* #line 135280 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgConditionalExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgConditionalExp objectArray [] = *(Memory_Block_List.begin());
     if (SgConditionalExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConditionalExp** objectArray = (SgConditionalExp**) &(SgConditionalExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgConditionalExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgConditionalExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgConditionalExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgConditionalExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgConditionalExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgConditionalExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgConditionalExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgConditionalExp objectArray [] = *(Memory_Block_List.begin());
     if (SgConditionalExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConditionalExp** objectArray = (SgConditionalExp**) &(SgConditionalExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgConditionalExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgConditionalExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgConditionalExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgConditionalExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgConditionalExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgConditionalExp objectArray [] = *(Memory_Block_List.begin());
     if (SgConditionalExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConditionalExp** objectArray = (SgConditionalExp**) &(SgConditionalExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgConditionalExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgConditionalExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgConditionalExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgConditionalExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgConditionalExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgConditionalExp** objectArray = (SgConditionalExp**) &(SgConditionalExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgConditionalExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgConditionalExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgConditionalExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgConditionalExp);

     return memory;
   }



/* #line 135512 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNewExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNewExp objectArray [] = *(Memory_Block_List.begin());
     if (SgNewExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNewExp** objectArray = (SgNewExp**) &(SgNewExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNewExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNewExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNewExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNewExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNewExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNewExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNewExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNewExp objectArray [] = *(Memory_Block_List.begin());
     if (SgNewExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNewExp** objectArray = (SgNewExp**) &(SgNewExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNewExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNewExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNewExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNewExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNewExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNewExp objectArray [] = *(Memory_Block_List.begin());
     if (SgNewExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNewExp** objectArray = (SgNewExp**) &(SgNewExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNewExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNewExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNewExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNewExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNewExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNewExp** objectArray = (SgNewExp**) &(SgNewExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNewExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNewExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNewExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNewExp);

     return memory;
   }



/* #line 135744 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDeleteExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDeleteExp objectArray [] = *(Memory_Block_List.begin());
     if (SgDeleteExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeleteExp** objectArray = (SgDeleteExp**) &(SgDeleteExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeleteExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDeleteExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDeleteExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDeleteExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDeleteExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeleteExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDeleteExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeleteExp objectArray [] = *(Memory_Block_List.begin());
     if (SgDeleteExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeleteExp** objectArray = (SgDeleteExp**) &(SgDeleteExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDeleteExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDeleteExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDeleteExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDeleteExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDeleteExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDeleteExp objectArray [] = *(Memory_Block_List.begin());
     if (SgDeleteExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDeleteExp** objectArray = (SgDeleteExp**) &(SgDeleteExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDeleteExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDeleteExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDeleteExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeleteExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDeleteExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDeleteExp** objectArray = (SgDeleteExp**) &(SgDeleteExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDeleteExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDeleteExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDeleteExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDeleteExp);

     return memory;
   }



/* #line 135976 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgThisExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgThisExp objectArray [] = *(Memory_Block_List.begin());
     if (SgThisExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgThisExp** objectArray = (SgThisExp**) &(SgThisExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgThisExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgThisExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgThisExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgThisExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgThisExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgThisExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgThisExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgThisExp objectArray [] = *(Memory_Block_List.begin());
     if (SgThisExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgThisExp** objectArray = (SgThisExp**) &(SgThisExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgThisExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgThisExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgThisExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgThisExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgThisExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgThisExp objectArray [] = *(Memory_Block_List.begin());
     if (SgThisExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgThisExp** objectArray = (SgThisExp**) &(SgThisExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgThisExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgThisExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgThisExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgThisExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgThisExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgThisExp** objectArray = (SgThisExp**) &(SgThisExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgThisExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgThisExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgThisExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgThisExp);

     return memory;
   }



/* #line 136208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRefExp** objectArray = (SgRefExp**) &(SgRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRefExp** objectArray = (SgRefExp**) &(SgRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRefExp** objectArray = (SgRefExp**) &(SgRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgRefExp** objectArray = (SgRefExp**) &(SgRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgRefExp);

     return memory;
   }



/* #line 136440 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInitializer** objectArray = (SgInitializer**) &(SgInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgInitializer*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgInitializer::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInitializer** objectArray = (SgInitializer**) &(SgInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgInitializer object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgInitializer object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgInitializer::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInitializer** objectArray = (SgInitializer**) &(SgInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgInitializer::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgInitializer::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgInitializer found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgInitializer::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgInitializer** objectArray = (SgInitializer**) &(SgInitializer::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgInitializer::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgInitializer::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgInitializer);

     return memory;
   }



/* #line 136672 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAggregateInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAggregateInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgAggregateInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAggregateInitializer** objectArray = (SgAggregateInitializer**) &(SgAggregateInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAggregateInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAggregateInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAggregateInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAggregateInitializer*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAggregateInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAggregateInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAggregateInitializer::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAggregateInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgAggregateInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAggregateInitializer** objectArray = (SgAggregateInitializer**) &(SgAggregateInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAggregateInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAggregateInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAggregateInitializer object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAggregateInitializer object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAggregateInitializer::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAggregateInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgAggregateInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAggregateInitializer** objectArray = (SgAggregateInitializer**) &(SgAggregateInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAggregateInitializer::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAggregateInitializer::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAggregateInitializer found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAggregateInitializer::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAggregateInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAggregateInitializer** objectArray = (SgAggregateInitializer**) &(SgAggregateInitializer::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAggregateInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAggregateInitializer::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAggregateInitializer::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAggregateInitializer);

     return memory;
   }



/* #line 136904 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCompoundInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCompoundInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgCompoundInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCompoundInitializer** objectArray = (SgCompoundInitializer**) &(SgCompoundInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCompoundInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCompoundInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCompoundInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCompoundInitializer*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCompoundInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCompoundInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCompoundInitializer::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCompoundInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgCompoundInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCompoundInitializer** objectArray = (SgCompoundInitializer**) &(SgCompoundInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCompoundInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCompoundInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCompoundInitializer object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCompoundInitializer object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCompoundInitializer::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCompoundInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgCompoundInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCompoundInitializer** objectArray = (SgCompoundInitializer**) &(SgCompoundInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCompoundInitializer::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCompoundInitializer::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCompoundInitializer found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCompoundInitializer::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCompoundInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCompoundInitializer** objectArray = (SgCompoundInitializer**) &(SgCompoundInitializer::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCompoundInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCompoundInitializer::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCompoundInitializer::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCompoundInitializer);

     return memory;
   }



/* #line 137136 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgConstructorInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgConstructorInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgConstructorInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConstructorInitializer** objectArray = (SgConstructorInitializer**) &(SgConstructorInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgConstructorInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgConstructorInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgConstructorInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgConstructorInitializer*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgConstructorInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgConstructorInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgConstructorInitializer::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgConstructorInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgConstructorInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConstructorInitializer** objectArray = (SgConstructorInitializer**) &(SgConstructorInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgConstructorInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgConstructorInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgConstructorInitializer object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgConstructorInitializer object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgConstructorInitializer::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgConstructorInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgConstructorInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgConstructorInitializer** objectArray = (SgConstructorInitializer**) &(SgConstructorInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgConstructorInitializer::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgConstructorInitializer::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgConstructorInitializer found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgConstructorInitializer::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgConstructorInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgConstructorInitializer** objectArray = (SgConstructorInitializer**) &(SgConstructorInitializer::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgConstructorInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgConstructorInitializer::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgConstructorInitializer::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgConstructorInitializer);

     return memory;
   }



/* #line 137368 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAssignInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAssignInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgAssignInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssignInitializer** objectArray = (SgAssignInitializer**) &(SgAssignInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAssignInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAssignInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAssignInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAssignInitializer*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAssignInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAssignInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAssignInitializer::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAssignInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgAssignInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssignInitializer** objectArray = (SgAssignInitializer**) &(SgAssignInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAssignInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAssignInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAssignInitializer object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAssignInitializer object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAssignInitializer::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAssignInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgAssignInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAssignInitializer** objectArray = (SgAssignInitializer**) &(SgAssignInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAssignInitializer::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAssignInitializer::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAssignInitializer found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAssignInitializer::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAssignInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAssignInitializer** objectArray = (SgAssignInitializer**) &(SgAssignInitializer::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAssignInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAssignInitializer::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAssignInitializer::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAssignInitializer);

     return memory;
   }



/* #line 137600 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDesignatedInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDesignatedInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgDesignatedInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDesignatedInitializer** objectArray = (SgDesignatedInitializer**) &(SgDesignatedInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDesignatedInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDesignatedInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDesignatedInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDesignatedInitializer*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDesignatedInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDesignatedInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDesignatedInitializer::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDesignatedInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgDesignatedInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDesignatedInitializer** objectArray = (SgDesignatedInitializer**) &(SgDesignatedInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDesignatedInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDesignatedInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDesignatedInitializer object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDesignatedInitializer object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDesignatedInitializer::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDesignatedInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgDesignatedInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDesignatedInitializer** objectArray = (SgDesignatedInitializer**) &(SgDesignatedInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDesignatedInitializer::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDesignatedInitializer::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDesignatedInitializer found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDesignatedInitializer::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDesignatedInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDesignatedInitializer** objectArray = (SgDesignatedInitializer**) &(SgDesignatedInitializer::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDesignatedInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDesignatedInitializer::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDesignatedInitializer::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDesignatedInitializer);

     return memory;
   }



/* #line 137832 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgBracedInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgBracedInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgBracedInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBracedInitializer** objectArray = (SgBracedInitializer**) &(SgBracedInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBracedInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgBracedInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgBracedInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgBracedInitializer*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgBracedInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBracedInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgBracedInitializer::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBracedInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgBracedInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBracedInitializer** objectArray = (SgBracedInitializer**) &(SgBracedInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgBracedInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgBracedInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgBracedInitializer object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgBracedInitializer object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgBracedInitializer::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgBracedInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgBracedInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgBracedInitializer** objectArray = (SgBracedInitializer**) &(SgBracedInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgBracedInitializer::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgBracedInitializer::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgBracedInitializer found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBracedInitializer::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgBracedInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgBracedInitializer** objectArray = (SgBracedInitializer**) &(SgBracedInitializer::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgBracedInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgBracedInitializer::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgBracedInitializer::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgBracedInitializer);

     return memory;
   }



/* #line 138064 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaAncestorInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaAncestorInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAncestorInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAncestorInitializer** objectArray = (SgAdaAncestorInitializer**) &(SgAdaAncestorInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaAncestorInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaAncestorInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaAncestorInitializer::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaAncestorInitializer*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaAncestorInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaAncestorInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaAncestorInitializer::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaAncestorInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAncestorInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAncestorInitializer** objectArray = (SgAdaAncestorInitializer**) &(SgAdaAncestorInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaAncestorInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaAncestorInitializer::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaAncestorInitializer object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaAncestorInitializer object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaAncestorInitializer::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaAncestorInitializer objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAncestorInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAncestorInitializer** objectArray = (SgAdaAncestorInitializer**) &(SgAdaAncestorInitializer::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaAncestorInitializer::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaAncestorInitializer::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaAncestorInitializer found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaAncestorInitializer::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaAncestorInitializer::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaAncestorInitializer** objectArray = (SgAdaAncestorInitializer**) &(SgAdaAncestorInitializer::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaAncestorInitializer::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaAncestorInitializer::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaAncestorInitializer::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaAncestorInitializer);

     return memory;
   }



/* #line 138296 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgVarArgStartOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgVarArgStartOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgStartOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgStartOp** objectArray = (SgVarArgStartOp**) &(SgVarArgStartOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVarArgStartOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgVarArgStartOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgVarArgStartOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgVarArgStartOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgVarArgStartOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVarArgStartOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgVarArgStartOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVarArgStartOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgStartOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgStartOp** objectArray = (SgVarArgStartOp**) &(SgVarArgStartOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVarArgStartOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVarArgStartOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgVarArgStartOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgVarArgStartOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgVarArgStartOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVarArgStartOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgStartOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgStartOp** objectArray = (SgVarArgStartOp**) &(SgVarArgStartOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgVarArgStartOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgVarArgStartOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgVarArgStartOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVarArgStartOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgVarArgStartOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgVarArgStartOp** objectArray = (SgVarArgStartOp**) &(SgVarArgStartOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgVarArgStartOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgVarArgStartOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVarArgStartOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgVarArgStartOp);

     return memory;
   }



/* #line 138528 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgVarArgOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgVarArgOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgOp** objectArray = (SgVarArgOp**) &(SgVarArgOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVarArgOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgVarArgOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgVarArgOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgVarArgOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgVarArgOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVarArgOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgVarArgOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVarArgOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgOp** objectArray = (SgVarArgOp**) &(SgVarArgOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVarArgOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVarArgOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgVarArgOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgVarArgOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgVarArgOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVarArgOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgOp** objectArray = (SgVarArgOp**) &(SgVarArgOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgVarArgOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgVarArgOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgVarArgOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVarArgOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgVarArgOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgVarArgOp** objectArray = (SgVarArgOp**) &(SgVarArgOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgVarArgOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgVarArgOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVarArgOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgVarArgOp);

     return memory;
   }



/* #line 138760 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgVarArgEndOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgVarArgEndOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgEndOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgEndOp** objectArray = (SgVarArgEndOp**) &(SgVarArgEndOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVarArgEndOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgVarArgEndOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgVarArgEndOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgVarArgEndOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgVarArgEndOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVarArgEndOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgVarArgEndOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVarArgEndOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgEndOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgEndOp** objectArray = (SgVarArgEndOp**) &(SgVarArgEndOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVarArgEndOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVarArgEndOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgVarArgEndOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgVarArgEndOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgVarArgEndOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVarArgEndOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgEndOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgEndOp** objectArray = (SgVarArgEndOp**) &(SgVarArgEndOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgVarArgEndOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgVarArgEndOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgVarArgEndOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVarArgEndOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgVarArgEndOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgVarArgEndOp** objectArray = (SgVarArgEndOp**) &(SgVarArgEndOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgVarArgEndOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgVarArgEndOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVarArgEndOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgVarArgEndOp);

     return memory;
   }



/* #line 138992 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgVarArgCopyOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgVarArgCopyOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgCopyOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgCopyOp** objectArray = (SgVarArgCopyOp**) &(SgVarArgCopyOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVarArgCopyOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgVarArgCopyOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgVarArgCopyOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgVarArgCopyOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgVarArgCopyOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVarArgCopyOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgVarArgCopyOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVarArgCopyOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgCopyOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgCopyOp** objectArray = (SgVarArgCopyOp**) &(SgVarArgCopyOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVarArgCopyOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVarArgCopyOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgVarArgCopyOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgVarArgCopyOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgVarArgCopyOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVarArgCopyOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgCopyOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgCopyOp** objectArray = (SgVarArgCopyOp**) &(SgVarArgCopyOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgVarArgCopyOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgVarArgCopyOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgVarArgCopyOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVarArgCopyOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgVarArgCopyOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgVarArgCopyOp** objectArray = (SgVarArgCopyOp**) &(SgVarArgCopyOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgVarArgCopyOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgVarArgCopyOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVarArgCopyOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgVarArgCopyOp);

     return memory;
   }



/* #line 139224 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgVarArgStartOneOperandOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgVarArgStartOneOperandOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgStartOneOperandOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgStartOneOperandOp** objectArray = (SgVarArgStartOneOperandOp**) &(SgVarArgStartOneOperandOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVarArgStartOneOperandOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgVarArgStartOneOperandOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgVarArgStartOneOperandOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgVarArgStartOneOperandOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgVarArgStartOneOperandOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVarArgStartOneOperandOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgVarArgStartOneOperandOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVarArgStartOneOperandOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgStartOneOperandOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgStartOneOperandOp** objectArray = (SgVarArgStartOneOperandOp**) &(SgVarArgStartOneOperandOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVarArgStartOneOperandOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVarArgStartOneOperandOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgVarArgStartOneOperandOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgVarArgStartOneOperandOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgVarArgStartOneOperandOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVarArgStartOneOperandOp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarArgStartOneOperandOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarArgStartOneOperandOp** objectArray = (SgVarArgStartOneOperandOp**) &(SgVarArgStartOneOperandOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgVarArgStartOneOperandOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgVarArgStartOneOperandOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgVarArgStartOneOperandOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVarArgStartOneOperandOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgVarArgStartOneOperandOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgVarArgStartOneOperandOp** objectArray = (SgVarArgStartOneOperandOp**) &(SgVarArgStartOneOperandOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgVarArgStartOneOperandOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgVarArgStartOneOperandOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVarArgStartOneOperandOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgVarArgStartOneOperandOp);

     return memory;
   }



/* #line 139456 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNullExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNullExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgNullExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNullExpression** objectArray = (SgNullExpression**) &(SgNullExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNullExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNullExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNullExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNullExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNullExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNullExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNullExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNullExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgNullExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNullExpression** objectArray = (SgNullExpression**) &(SgNullExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNullExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNullExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNullExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNullExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNullExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNullExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgNullExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNullExpression** objectArray = (SgNullExpression**) &(SgNullExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNullExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNullExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNullExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNullExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNullExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNullExpression** objectArray = (SgNullExpression**) &(SgNullExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNullExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNullExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNullExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNullExpression);

     return memory;
   }



/* #line 139688 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgVariantExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgVariantExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgVariantExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariantExpression** objectArray = (SgVariantExpression**) &(SgVariantExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVariantExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgVariantExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgVariantExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgVariantExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgVariantExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVariantExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgVariantExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVariantExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgVariantExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariantExpression** objectArray = (SgVariantExpression**) &(SgVariantExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVariantExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVariantExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgVariantExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgVariantExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgVariantExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVariantExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgVariantExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariantExpression** objectArray = (SgVariantExpression**) &(SgVariantExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgVariantExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgVariantExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgVariantExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVariantExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgVariantExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgVariantExpression** objectArray = (SgVariantExpression**) &(SgVariantExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgVariantExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgVariantExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVariantExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgVariantExpression);

     return memory;
   }



/* #line 139920 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSubscriptExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSubscriptExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgSubscriptExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSubscriptExpression** objectArray = (SgSubscriptExpression**) &(SgSubscriptExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSubscriptExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSubscriptExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSubscriptExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSubscriptExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSubscriptExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSubscriptExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSubscriptExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSubscriptExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgSubscriptExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSubscriptExpression** objectArray = (SgSubscriptExpression**) &(SgSubscriptExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSubscriptExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSubscriptExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSubscriptExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSubscriptExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSubscriptExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSubscriptExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgSubscriptExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSubscriptExpression** objectArray = (SgSubscriptExpression**) &(SgSubscriptExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSubscriptExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSubscriptExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSubscriptExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSubscriptExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSubscriptExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSubscriptExpression** objectArray = (SgSubscriptExpression**) &(SgSubscriptExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSubscriptExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSubscriptExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSubscriptExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSubscriptExpression);

     return memory;
   }



/* #line 140152 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgColonShapeExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgColonShapeExp objectArray [] = *(Memory_Block_List.begin());
     if (SgColonShapeExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgColonShapeExp** objectArray = (SgColonShapeExp**) &(SgColonShapeExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgColonShapeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgColonShapeExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgColonShapeExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgColonShapeExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgColonShapeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgColonShapeExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgColonShapeExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgColonShapeExp objectArray [] = *(Memory_Block_List.begin());
     if (SgColonShapeExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgColonShapeExp** objectArray = (SgColonShapeExp**) &(SgColonShapeExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgColonShapeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgColonShapeExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgColonShapeExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgColonShapeExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgColonShapeExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgColonShapeExp objectArray [] = *(Memory_Block_List.begin());
     if (SgColonShapeExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgColonShapeExp** objectArray = (SgColonShapeExp**) &(SgColonShapeExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgColonShapeExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgColonShapeExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgColonShapeExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgColonShapeExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgColonShapeExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgColonShapeExp** objectArray = (SgColonShapeExp**) &(SgColonShapeExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgColonShapeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgColonShapeExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgColonShapeExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgColonShapeExp);

     return memory;
   }



/* #line 140384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsteriskShapeExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsteriskShapeExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAsteriskShapeExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsteriskShapeExp** objectArray = (SgAsteriskShapeExp**) &(SgAsteriskShapeExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsteriskShapeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsteriskShapeExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsteriskShapeExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsteriskShapeExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsteriskShapeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsteriskShapeExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsteriskShapeExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsteriskShapeExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAsteriskShapeExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsteriskShapeExp** objectArray = (SgAsteriskShapeExp**) &(SgAsteriskShapeExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsteriskShapeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsteriskShapeExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsteriskShapeExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsteriskShapeExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsteriskShapeExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsteriskShapeExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAsteriskShapeExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsteriskShapeExp** objectArray = (SgAsteriskShapeExp**) &(SgAsteriskShapeExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsteriskShapeExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsteriskShapeExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsteriskShapeExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsteriskShapeExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsteriskShapeExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsteriskShapeExp** objectArray = (SgAsteriskShapeExp**) &(SgAsteriskShapeExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsteriskShapeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsteriskShapeExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsteriskShapeExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsteriskShapeExp);

     return memory;
   }



/* #line 140616 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgImpliedDo::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgImpliedDo objectArray [] = *(Memory_Block_List.begin());
     if (SgImpliedDo::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImpliedDo** objectArray = (SgImpliedDo**) &(SgImpliedDo::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgImpliedDo::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgImpliedDo::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgImpliedDo::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgImpliedDo*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgImpliedDo::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgImpliedDo::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgImpliedDo::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgImpliedDo objectArray [] = *(Memory_Block_List.begin());
     if (SgImpliedDo::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImpliedDo** objectArray = (SgImpliedDo**) &(SgImpliedDo::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgImpliedDo::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgImpliedDo::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgImpliedDo object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgImpliedDo object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgImpliedDo::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgImpliedDo objectArray [] = *(Memory_Block_List.begin());
     if (SgImpliedDo::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgImpliedDo** objectArray = (SgImpliedDo**) &(SgImpliedDo::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgImpliedDo::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgImpliedDo::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgImpliedDo found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgImpliedDo::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgImpliedDo::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgImpliedDo** objectArray = (SgImpliedDo**) &(SgImpliedDo::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgImpliedDo::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgImpliedDo::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgImpliedDo::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgImpliedDo);

     return memory;
   }



/* #line 140848 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIOItemExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIOItemExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgIOItemExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIOItemExpression** objectArray = (SgIOItemExpression**) &(SgIOItemExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIOItemExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIOItemExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIOItemExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIOItemExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIOItemExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIOItemExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIOItemExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIOItemExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgIOItemExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIOItemExpression** objectArray = (SgIOItemExpression**) &(SgIOItemExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIOItemExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIOItemExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIOItemExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIOItemExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIOItemExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIOItemExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgIOItemExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIOItemExpression** objectArray = (SgIOItemExpression**) &(SgIOItemExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIOItemExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIOItemExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIOItemExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIOItemExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIOItemExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIOItemExpression** objectArray = (SgIOItemExpression**) &(SgIOItemExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIOItemExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIOItemExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIOItemExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIOItemExpression);

     return memory;
   }



/* #line 141080 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgActualArgumentExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgActualArgumentExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgActualArgumentExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgActualArgumentExpression** objectArray = (SgActualArgumentExpression**) &(SgActualArgumentExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgActualArgumentExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgActualArgumentExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgActualArgumentExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgActualArgumentExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgActualArgumentExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgActualArgumentExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgActualArgumentExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgActualArgumentExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgActualArgumentExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgActualArgumentExpression** objectArray = (SgActualArgumentExpression**) &(SgActualArgumentExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgActualArgumentExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgActualArgumentExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgActualArgumentExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgActualArgumentExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgActualArgumentExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgActualArgumentExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgActualArgumentExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgActualArgumentExpression** objectArray = (SgActualArgumentExpression**) &(SgActualArgumentExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgActualArgumentExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgActualArgumentExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgActualArgumentExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgActualArgumentExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgActualArgumentExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgActualArgumentExpression** objectArray = (SgActualArgumentExpression**) &(SgActualArgumentExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgActualArgumentExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgActualArgumentExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgActualArgumentExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgActualArgumentExpression);

     return memory;
   }



/* #line 141312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgStatementExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgStatementExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgStatementExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStatementExpression** objectArray = (SgStatementExpression**) &(SgStatementExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStatementExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgStatementExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgStatementExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgStatementExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgStatementExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStatementExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgStatementExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStatementExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgStatementExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStatementExpression** objectArray = (SgStatementExpression**) &(SgStatementExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStatementExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStatementExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgStatementExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgStatementExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgStatementExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStatementExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgStatementExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStatementExpression** objectArray = (SgStatementExpression**) &(SgStatementExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgStatementExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgStatementExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgStatementExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStatementExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgStatementExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgStatementExpression** objectArray = (SgStatementExpression**) &(SgStatementExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgStatementExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgStatementExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStatementExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgStatementExpression);

     return memory;
   }



/* #line 141544 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmOp** objectArray = (SgAsmOp**) &(SgAsmOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmOp** objectArray = (SgAsmOp**) &(SgAsmOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmOp** objectArray = (SgAsmOp**) &(SgAsmOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmOp** objectArray = (SgAsmOp**) &(SgAsmOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmOp);

     return memory;
   }



/* #line 141776 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCudaKernelExecConfig::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCudaKernelExecConfig objectArray [] = *(Memory_Block_List.begin());
     if (SgCudaKernelExecConfig::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCudaKernelExecConfig** objectArray = (SgCudaKernelExecConfig**) &(SgCudaKernelExecConfig::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCudaKernelExecConfig::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCudaKernelExecConfig::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCudaKernelExecConfig::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCudaKernelExecConfig*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCudaKernelExecConfig::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCudaKernelExecConfig::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCudaKernelExecConfig::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCudaKernelExecConfig objectArray [] = *(Memory_Block_List.begin());
     if (SgCudaKernelExecConfig::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCudaKernelExecConfig** objectArray = (SgCudaKernelExecConfig**) &(SgCudaKernelExecConfig::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCudaKernelExecConfig::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCudaKernelExecConfig::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCudaKernelExecConfig object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCudaKernelExecConfig object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCudaKernelExecConfig::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCudaKernelExecConfig objectArray [] = *(Memory_Block_List.begin());
     if (SgCudaKernelExecConfig::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCudaKernelExecConfig** objectArray = (SgCudaKernelExecConfig**) &(SgCudaKernelExecConfig::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCudaKernelExecConfig::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCudaKernelExecConfig::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCudaKernelExecConfig found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCudaKernelExecConfig::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCudaKernelExecConfig::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCudaKernelExecConfig** objectArray = (SgCudaKernelExecConfig**) &(SgCudaKernelExecConfig::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCudaKernelExecConfig::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCudaKernelExecConfig::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCudaKernelExecConfig::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCudaKernelExecConfig);

     return memory;
   }



/* #line 142008 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgUnknownArrayOrFunctionReference::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgUnknownArrayOrFunctionReference objectArray [] = *(Memory_Block_List.begin());
     if (SgUnknownArrayOrFunctionReference::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnknownArrayOrFunctionReference** objectArray = (SgUnknownArrayOrFunctionReference**) &(SgUnknownArrayOrFunctionReference::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnknownArrayOrFunctionReference::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgUnknownArrayOrFunctionReference::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgUnknownArrayOrFunctionReference::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgUnknownArrayOrFunctionReference*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgUnknownArrayOrFunctionReference::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnknownArrayOrFunctionReference::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgUnknownArrayOrFunctionReference::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnknownArrayOrFunctionReference objectArray [] = *(Memory_Block_List.begin());
     if (SgUnknownArrayOrFunctionReference::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnknownArrayOrFunctionReference** objectArray = (SgUnknownArrayOrFunctionReference**) &(SgUnknownArrayOrFunctionReference::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgUnknownArrayOrFunctionReference::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgUnknownArrayOrFunctionReference::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgUnknownArrayOrFunctionReference object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgUnknownArrayOrFunctionReference object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgUnknownArrayOrFunctionReference::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgUnknownArrayOrFunctionReference objectArray [] = *(Memory_Block_List.begin());
     if (SgUnknownArrayOrFunctionReference::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgUnknownArrayOrFunctionReference** objectArray = (SgUnknownArrayOrFunctionReference**) &(SgUnknownArrayOrFunctionReference::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgUnknownArrayOrFunctionReference::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgUnknownArrayOrFunctionReference::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgUnknownArrayOrFunctionReference found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnknownArrayOrFunctionReference::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgUnknownArrayOrFunctionReference::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgUnknownArrayOrFunctionReference** objectArray = (SgUnknownArrayOrFunctionReference**) &(SgUnknownArrayOrFunctionReference::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgUnknownArrayOrFunctionReference::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgUnknownArrayOrFunctionReference::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgUnknownArrayOrFunctionReference::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgUnknownArrayOrFunctionReference);

     return memory;
   }



/* #line 142240 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgPseudoDestructorRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgPseudoDestructorRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgPseudoDestructorRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPseudoDestructorRefExp** objectArray = (SgPseudoDestructorRefExp**) &(SgPseudoDestructorRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPseudoDestructorRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgPseudoDestructorRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgPseudoDestructorRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgPseudoDestructorRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgPseudoDestructorRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPseudoDestructorRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgPseudoDestructorRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPseudoDestructorRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgPseudoDestructorRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPseudoDestructorRefExp** objectArray = (SgPseudoDestructorRefExp**) &(SgPseudoDestructorRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgPseudoDestructorRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgPseudoDestructorRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgPseudoDestructorRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgPseudoDestructorRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgPseudoDestructorRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgPseudoDestructorRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgPseudoDestructorRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgPseudoDestructorRefExp** objectArray = (SgPseudoDestructorRefExp**) &(SgPseudoDestructorRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgPseudoDestructorRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgPseudoDestructorRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgPseudoDestructorRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPseudoDestructorRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgPseudoDestructorRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgPseudoDestructorRefExp** objectArray = (SgPseudoDestructorRefExp**) &(SgPseudoDestructorRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgPseudoDestructorRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgPseudoDestructorRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgPseudoDestructorRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgPseudoDestructorRefExp);

     return memory;
   }



/* #line 142472 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCAFCoExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCAFCoExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgCAFCoExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCAFCoExpression** objectArray = (SgCAFCoExpression**) &(SgCAFCoExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCAFCoExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCAFCoExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCAFCoExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCAFCoExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCAFCoExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCAFCoExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCAFCoExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCAFCoExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgCAFCoExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCAFCoExpression** objectArray = (SgCAFCoExpression**) &(SgCAFCoExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCAFCoExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCAFCoExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCAFCoExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCAFCoExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCAFCoExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCAFCoExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgCAFCoExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCAFCoExpression** objectArray = (SgCAFCoExpression**) &(SgCAFCoExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCAFCoExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCAFCoExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCAFCoExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCAFCoExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCAFCoExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCAFCoExpression** objectArray = (SgCAFCoExpression**) &(SgCAFCoExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCAFCoExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCAFCoExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCAFCoExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCAFCoExpression);

     return memory;
   }



/* #line 142704 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSizeOfOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSizeOfOp objectArray [] = *(Memory_Block_List.begin());
     if (SgSizeOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSizeOfOp** objectArray = (SgSizeOfOp**) &(SgSizeOfOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSizeOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSizeOfOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSizeOfOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSizeOfOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSizeOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSizeOfOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSizeOfOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSizeOfOp objectArray [] = *(Memory_Block_List.begin());
     if (SgSizeOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSizeOfOp** objectArray = (SgSizeOfOp**) &(SgSizeOfOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSizeOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSizeOfOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSizeOfOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSizeOfOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSizeOfOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSizeOfOp objectArray [] = *(Memory_Block_List.begin());
     if (SgSizeOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSizeOfOp** objectArray = (SgSizeOfOp**) &(SgSizeOfOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSizeOfOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSizeOfOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSizeOfOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSizeOfOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSizeOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSizeOfOp** objectArray = (SgSizeOfOp**) &(SgSizeOfOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSizeOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSizeOfOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSizeOfOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSizeOfOp);

     return memory;
   }



/* #line 142936 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLambdaRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLambdaRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgLambdaRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLambdaRefExp** objectArray = (SgLambdaRefExp**) &(SgLambdaRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLambdaRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLambdaRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLambdaRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLambdaRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLambdaRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLambdaRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLambdaRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLambdaRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgLambdaRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLambdaRefExp** objectArray = (SgLambdaRefExp**) &(SgLambdaRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLambdaRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLambdaRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLambdaRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLambdaRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLambdaRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLambdaRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgLambdaRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLambdaRefExp** objectArray = (SgLambdaRefExp**) &(SgLambdaRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLambdaRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLambdaRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLambdaRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLambdaRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLambdaRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLambdaRefExp** objectArray = (SgLambdaRefExp**) &(SgLambdaRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLambdaRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLambdaRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLambdaRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLambdaRefExp);

     return memory;
   }



/* #line 143168 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDictionaryExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDictionaryExp objectArray [] = *(Memory_Block_List.begin());
     if (SgDictionaryExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDictionaryExp** objectArray = (SgDictionaryExp**) &(SgDictionaryExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDictionaryExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDictionaryExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDictionaryExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDictionaryExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDictionaryExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDictionaryExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDictionaryExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDictionaryExp objectArray [] = *(Memory_Block_List.begin());
     if (SgDictionaryExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDictionaryExp** objectArray = (SgDictionaryExp**) &(SgDictionaryExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDictionaryExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDictionaryExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDictionaryExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDictionaryExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDictionaryExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDictionaryExp objectArray [] = *(Memory_Block_List.begin());
     if (SgDictionaryExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDictionaryExp** objectArray = (SgDictionaryExp**) &(SgDictionaryExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDictionaryExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDictionaryExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDictionaryExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDictionaryExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDictionaryExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDictionaryExp** objectArray = (SgDictionaryExp**) &(SgDictionaryExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDictionaryExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDictionaryExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDictionaryExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDictionaryExp);

     return memory;
   }



/* #line 143400 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgKeyDatumPair::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgKeyDatumPair objectArray [] = *(Memory_Block_List.begin());
     if (SgKeyDatumPair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgKeyDatumPair** objectArray = (SgKeyDatumPair**) &(SgKeyDatumPair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgKeyDatumPair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgKeyDatumPair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgKeyDatumPair::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgKeyDatumPair*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgKeyDatumPair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgKeyDatumPair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgKeyDatumPair::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgKeyDatumPair objectArray [] = *(Memory_Block_List.begin());
     if (SgKeyDatumPair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgKeyDatumPair** objectArray = (SgKeyDatumPair**) &(SgKeyDatumPair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgKeyDatumPair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgKeyDatumPair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgKeyDatumPair object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgKeyDatumPair object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgKeyDatumPair::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgKeyDatumPair objectArray [] = *(Memory_Block_List.begin());
     if (SgKeyDatumPair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgKeyDatumPair** objectArray = (SgKeyDatumPair**) &(SgKeyDatumPair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgKeyDatumPair::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgKeyDatumPair::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgKeyDatumPair found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgKeyDatumPair::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgKeyDatumPair::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgKeyDatumPair** objectArray = (SgKeyDatumPair**) &(SgKeyDatumPair::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgKeyDatumPair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgKeyDatumPair::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgKeyDatumPair::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgKeyDatumPair);

     return memory;
   }



/* #line 143632 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgReferenceExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgReferenceExp objectArray [] = *(Memory_Block_List.begin());
     if (SgReferenceExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReferenceExp** objectArray = (SgReferenceExp**) &(SgReferenceExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgReferenceExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgReferenceExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgReferenceExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgReferenceExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgReferenceExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgReferenceExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgReferenceExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgReferenceExp objectArray [] = *(Memory_Block_List.begin());
     if (SgReferenceExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReferenceExp** objectArray = (SgReferenceExp**) &(SgReferenceExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgReferenceExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgReferenceExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgReferenceExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgReferenceExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgReferenceExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgReferenceExp objectArray [] = *(Memory_Block_List.begin());
     if (SgReferenceExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgReferenceExp** objectArray = (SgReferenceExp**) &(SgReferenceExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgReferenceExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgReferenceExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgReferenceExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgReferenceExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgReferenceExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgReferenceExp** objectArray = (SgReferenceExp**) &(SgReferenceExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgReferenceExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgReferenceExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgReferenceExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgReferenceExp);

     return memory;
   }



/* #line 143864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgVarRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgVarRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarRefExp** objectArray = (SgVarRefExp**) &(SgVarRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVarRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgVarRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgVarRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgVarRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgVarRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVarRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgVarRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVarRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarRefExp** objectArray = (SgVarRefExp**) &(SgVarRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVarRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVarRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgVarRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgVarRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgVarRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVarRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgVarRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVarRefExp** objectArray = (SgVarRefExp**) &(SgVarRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgVarRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgVarRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgVarRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVarRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgVarRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgVarRefExp** objectArray = (SgVarRefExp**) &(SgVarRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgVarRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgVarRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVarRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgVarRefExp);

     return memory;
   }



/* #line 144096 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLabelRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLabelRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgLabelRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLabelRefExp** objectArray = (SgLabelRefExp**) &(SgLabelRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLabelRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLabelRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLabelRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLabelRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLabelRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLabelRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLabelRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLabelRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgLabelRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLabelRefExp** objectArray = (SgLabelRefExp**) &(SgLabelRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLabelRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLabelRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLabelRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLabelRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLabelRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLabelRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgLabelRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLabelRefExp** objectArray = (SgLabelRefExp**) &(SgLabelRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLabelRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLabelRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLabelRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLabelRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLabelRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLabelRefExp** objectArray = (SgLabelRefExp**) &(SgLabelRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLabelRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLabelRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLabelRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLabelRefExp);

     return memory;
   }



/* #line 144328 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgClassNameRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgClassNameRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgClassNameRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassNameRefExp** objectArray = (SgClassNameRefExp**) &(SgClassNameRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassNameRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgClassNameRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgClassNameRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgClassNameRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgClassNameRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassNameRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgClassNameRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassNameRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgClassNameRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassNameRefExp** objectArray = (SgClassNameRefExp**) &(SgClassNameRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassNameRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassNameRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgClassNameRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgClassNameRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgClassNameRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassNameRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgClassNameRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassNameRefExp** objectArray = (SgClassNameRefExp**) &(SgClassNameRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgClassNameRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgClassNameRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgClassNameRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassNameRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgClassNameRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgClassNameRefExp** objectArray = (SgClassNameRefExp**) &(SgClassNameRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgClassNameRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgClassNameRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassNameRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgClassNameRefExp);

     return memory;
   }



/* #line 144560 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNonrealRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNonrealRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealRefExp** objectArray = (SgNonrealRefExp**) &(SgNonrealRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNonrealRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNonrealRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNonrealRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNonrealRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNonrealRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNonrealRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNonrealRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNonrealRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealRefExp** objectArray = (SgNonrealRefExp**) &(SgNonrealRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNonrealRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNonrealRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNonrealRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNonrealRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNonrealRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNonrealRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealRefExp** objectArray = (SgNonrealRefExp**) &(SgNonrealRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNonrealRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNonrealRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNonrealRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNonrealRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNonrealRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNonrealRefExp** objectArray = (SgNonrealRefExp**) &(SgNonrealRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNonrealRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNonrealRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNonrealRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNonrealRefExp);

     return memory;
   }



/* #line 144792 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgScopedRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgScopedRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgScopedRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgScopedRefExp** objectArray = (SgScopedRefExp**) &(SgScopedRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgScopedRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgScopedRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgScopedRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgScopedRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgScopedRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgScopedRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgScopedRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgScopedRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgScopedRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgScopedRefExp** objectArray = (SgScopedRefExp**) &(SgScopedRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgScopedRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgScopedRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgScopedRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgScopedRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgScopedRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgScopedRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgScopedRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgScopedRefExp** objectArray = (SgScopedRefExp**) &(SgScopedRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgScopedRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgScopedRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgScopedRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgScopedRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgScopedRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgScopedRefExp** objectArray = (SgScopedRefExp**) &(SgScopedRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgScopedRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgScopedRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgScopedRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgScopedRefExp);

     return memory;
   }



/* #line 145024 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeRefExp** objectArray = (SgTypeRefExp**) &(SgTypeRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeRefExp** objectArray = (SgTypeRefExp**) &(SgTypeRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeRefExp** objectArray = (SgTypeRefExp**) &(SgTypeRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeRefExp** objectArray = (SgTypeRefExp**) &(SgTypeRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeRefExp);

     return memory;
   }



/* #line 145256 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFunctionRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFunctionRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionRefExp** objectArray = (SgFunctionRefExp**) &(SgFunctionRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFunctionRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFunctionRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFunctionRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFunctionRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionRefExp** objectArray = (SgFunctionRefExp**) &(SgFunctionRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFunctionRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFunctionRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFunctionRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionRefExp** objectArray = (SgFunctionRefExp**) &(SgFunctionRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFunctionRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFunctionRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFunctionRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFunctionRefExp** objectArray = (SgFunctionRefExp**) &(SgFunctionRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFunctionRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFunctionRefExp);

     return memory;
   }



/* #line 145488 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMemberFunctionRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMemberFunctionRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgMemberFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMemberFunctionRefExp** objectArray = (SgMemberFunctionRefExp**) &(SgMemberFunctionRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMemberFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMemberFunctionRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMemberFunctionRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMemberFunctionRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMemberFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMemberFunctionRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMemberFunctionRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMemberFunctionRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgMemberFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMemberFunctionRefExp** objectArray = (SgMemberFunctionRefExp**) &(SgMemberFunctionRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMemberFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMemberFunctionRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMemberFunctionRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMemberFunctionRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMemberFunctionRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMemberFunctionRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgMemberFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMemberFunctionRefExp** objectArray = (SgMemberFunctionRefExp**) &(SgMemberFunctionRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMemberFunctionRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMemberFunctionRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMemberFunctionRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMemberFunctionRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMemberFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMemberFunctionRefExp** objectArray = (SgMemberFunctionRefExp**) &(SgMemberFunctionRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMemberFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMemberFunctionRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMemberFunctionRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMemberFunctionRefExp);

     return memory;
   }



/* #line 145720 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateFunctionRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateFunctionRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateFunctionRefExp** objectArray = (SgTemplateFunctionRefExp**) &(SgTemplateFunctionRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateFunctionRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateFunctionRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateFunctionRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateFunctionRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateFunctionRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateFunctionRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateFunctionRefExp** objectArray = (SgTemplateFunctionRefExp**) &(SgTemplateFunctionRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateFunctionRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateFunctionRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateFunctionRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateFunctionRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateFunctionRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateFunctionRefExp** objectArray = (SgTemplateFunctionRefExp**) &(SgTemplateFunctionRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateFunctionRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateFunctionRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateFunctionRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateFunctionRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateFunctionRefExp** objectArray = (SgTemplateFunctionRefExp**) &(SgTemplateFunctionRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateFunctionRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateFunctionRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateFunctionRefExp);

     return memory;
   }



/* #line 145952 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateMemberFunctionRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateMemberFunctionRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateMemberFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateMemberFunctionRefExp** objectArray = (SgTemplateMemberFunctionRefExp**) &(SgTemplateMemberFunctionRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateMemberFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateMemberFunctionRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateMemberFunctionRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateMemberFunctionRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateMemberFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateMemberFunctionRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateMemberFunctionRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateMemberFunctionRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateMemberFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateMemberFunctionRefExp** objectArray = (SgTemplateMemberFunctionRefExp**) &(SgTemplateMemberFunctionRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateMemberFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateMemberFunctionRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateMemberFunctionRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateMemberFunctionRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateMemberFunctionRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateMemberFunctionRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateMemberFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateMemberFunctionRefExp** objectArray = (SgTemplateMemberFunctionRefExp**) &(SgTemplateMemberFunctionRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateMemberFunctionRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateMemberFunctionRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateMemberFunctionRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateMemberFunctionRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateMemberFunctionRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateMemberFunctionRefExp** objectArray = (SgTemplateMemberFunctionRefExp**) &(SgTemplateMemberFunctionRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateMemberFunctionRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateMemberFunctionRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateMemberFunctionRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateMemberFunctionRefExp);

     return memory;
   }



/* #line 146184 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgComprehension::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgComprehension objectArray [] = *(Memory_Block_List.begin());
     if (SgComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgComprehension** objectArray = (SgComprehension**) &(SgComprehension::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgComprehension::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgComprehension::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgComprehension*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgComprehension::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgComprehension::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgComprehension objectArray [] = *(Memory_Block_List.begin());
     if (SgComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgComprehension** objectArray = (SgComprehension**) &(SgComprehension::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgComprehension::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgComprehension object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgComprehension object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgComprehension::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgComprehension objectArray [] = *(Memory_Block_List.begin());
     if (SgComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgComprehension** objectArray = (SgComprehension**) &(SgComprehension::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgComprehension::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgComprehension::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgComprehension found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgComprehension::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgComprehension** objectArray = (SgComprehension**) &(SgComprehension::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgComprehension::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgComprehension::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgComprehension);

     return memory;
   }



/* #line 146416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgListComprehension::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgListComprehension objectArray [] = *(Memory_Block_List.begin());
     if (SgListComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgListComprehension** objectArray = (SgListComprehension**) &(SgListComprehension::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgListComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgListComprehension::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgListComprehension::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgListComprehension*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgListComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgListComprehension::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgListComprehension::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgListComprehension objectArray [] = *(Memory_Block_List.begin());
     if (SgListComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgListComprehension** objectArray = (SgListComprehension**) &(SgListComprehension::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgListComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgListComprehension::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgListComprehension object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgListComprehension object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgListComprehension::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgListComprehension objectArray [] = *(Memory_Block_List.begin());
     if (SgListComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgListComprehension** objectArray = (SgListComprehension**) &(SgListComprehension::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgListComprehension::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgListComprehension::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgListComprehension found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgListComprehension::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgListComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgListComprehension** objectArray = (SgListComprehension**) &(SgListComprehension::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgListComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgListComprehension::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgListComprehension::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgListComprehension);

     return memory;
   }



/* #line 146648 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSetComprehension::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSetComprehension objectArray [] = *(Memory_Block_List.begin());
     if (SgSetComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSetComprehension** objectArray = (SgSetComprehension**) &(SgSetComprehension::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSetComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSetComprehension::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSetComprehension::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSetComprehension*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSetComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSetComprehension::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSetComprehension::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSetComprehension objectArray [] = *(Memory_Block_List.begin());
     if (SgSetComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSetComprehension** objectArray = (SgSetComprehension**) &(SgSetComprehension::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSetComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSetComprehension::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSetComprehension object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSetComprehension object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSetComprehension::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSetComprehension objectArray [] = *(Memory_Block_List.begin());
     if (SgSetComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSetComprehension** objectArray = (SgSetComprehension**) &(SgSetComprehension::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSetComprehension::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSetComprehension::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSetComprehension found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSetComprehension::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSetComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSetComprehension** objectArray = (SgSetComprehension**) &(SgSetComprehension::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSetComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSetComprehension::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSetComprehension::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSetComprehension);

     return memory;
   }



/* #line 146880 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDictionaryComprehension::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDictionaryComprehension objectArray [] = *(Memory_Block_List.begin());
     if (SgDictionaryComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDictionaryComprehension** objectArray = (SgDictionaryComprehension**) &(SgDictionaryComprehension::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDictionaryComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDictionaryComprehension::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDictionaryComprehension::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDictionaryComprehension*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDictionaryComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDictionaryComprehension::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDictionaryComprehension::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDictionaryComprehension objectArray [] = *(Memory_Block_List.begin());
     if (SgDictionaryComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDictionaryComprehension** objectArray = (SgDictionaryComprehension**) &(SgDictionaryComprehension::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDictionaryComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDictionaryComprehension::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDictionaryComprehension object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDictionaryComprehension object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDictionaryComprehension::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDictionaryComprehension objectArray [] = *(Memory_Block_List.begin());
     if (SgDictionaryComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDictionaryComprehension** objectArray = (SgDictionaryComprehension**) &(SgDictionaryComprehension::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDictionaryComprehension::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDictionaryComprehension::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDictionaryComprehension found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDictionaryComprehension::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDictionaryComprehension::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDictionaryComprehension** objectArray = (SgDictionaryComprehension**) &(SgDictionaryComprehension::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDictionaryComprehension::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDictionaryComprehension::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDictionaryComprehension::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDictionaryComprehension);

     return memory;
   }



/* #line 147112 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNaryOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNaryOp** objectArray = (SgNaryOp**) &(SgNaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNaryOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNaryOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNaryOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNaryOp** objectArray = (SgNaryOp**) &(SgNaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNaryOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNaryOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNaryOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNaryOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNaryOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNaryOp** objectArray = (SgNaryOp**) &(SgNaryOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNaryOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNaryOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNaryOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNaryOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNaryOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNaryOp** objectArray = (SgNaryOp**) &(SgNaryOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNaryOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNaryOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNaryOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNaryOp);

     return memory;
   }



/* #line 147344 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNaryBooleanOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNaryBooleanOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNaryBooleanOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNaryBooleanOp** objectArray = (SgNaryBooleanOp**) &(SgNaryBooleanOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNaryBooleanOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNaryBooleanOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNaryBooleanOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNaryBooleanOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNaryBooleanOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNaryBooleanOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNaryBooleanOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNaryBooleanOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNaryBooleanOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNaryBooleanOp** objectArray = (SgNaryBooleanOp**) &(SgNaryBooleanOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNaryBooleanOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNaryBooleanOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNaryBooleanOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNaryBooleanOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNaryBooleanOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNaryBooleanOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNaryBooleanOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNaryBooleanOp** objectArray = (SgNaryBooleanOp**) &(SgNaryBooleanOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNaryBooleanOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNaryBooleanOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNaryBooleanOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNaryBooleanOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNaryBooleanOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNaryBooleanOp** objectArray = (SgNaryBooleanOp**) &(SgNaryBooleanOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNaryBooleanOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNaryBooleanOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNaryBooleanOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNaryBooleanOp);

     return memory;
   }



/* #line 147576 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNaryComparisonOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNaryComparisonOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNaryComparisonOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNaryComparisonOp** objectArray = (SgNaryComparisonOp**) &(SgNaryComparisonOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNaryComparisonOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNaryComparisonOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNaryComparisonOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNaryComparisonOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNaryComparisonOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNaryComparisonOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNaryComparisonOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNaryComparisonOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNaryComparisonOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNaryComparisonOp** objectArray = (SgNaryComparisonOp**) &(SgNaryComparisonOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNaryComparisonOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNaryComparisonOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNaryComparisonOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNaryComparisonOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNaryComparisonOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNaryComparisonOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNaryComparisonOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNaryComparisonOp** objectArray = (SgNaryComparisonOp**) &(SgNaryComparisonOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNaryComparisonOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNaryComparisonOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNaryComparisonOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNaryComparisonOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNaryComparisonOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNaryComparisonOp** objectArray = (SgNaryComparisonOp**) &(SgNaryComparisonOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNaryComparisonOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNaryComparisonOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNaryComparisonOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNaryComparisonOp);

     return memory;
   }



/* #line 147808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgStringConversion::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgStringConversion objectArray [] = *(Memory_Block_List.begin());
     if (SgStringConversion::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStringConversion** objectArray = (SgStringConversion**) &(SgStringConversion::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStringConversion::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgStringConversion::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgStringConversion::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgStringConversion*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgStringConversion::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStringConversion::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgStringConversion::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStringConversion objectArray [] = *(Memory_Block_List.begin());
     if (SgStringConversion::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStringConversion** objectArray = (SgStringConversion**) &(SgStringConversion::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgStringConversion::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgStringConversion::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgStringConversion object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgStringConversion object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgStringConversion::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgStringConversion objectArray [] = *(Memory_Block_List.begin());
     if (SgStringConversion::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgStringConversion** objectArray = (SgStringConversion**) &(SgStringConversion::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgStringConversion::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgStringConversion::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgStringConversion found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStringConversion::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgStringConversion::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgStringConversion** objectArray = (SgStringConversion**) &(SgStringConversion::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgStringConversion::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgStringConversion::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgStringConversion::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgStringConversion);

     return memory;
   }



/* #line 148040 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgYieldExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgYieldExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgYieldExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgYieldExpression** objectArray = (SgYieldExpression**) &(SgYieldExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgYieldExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgYieldExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgYieldExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgYieldExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgYieldExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgYieldExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgYieldExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgYieldExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgYieldExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgYieldExpression** objectArray = (SgYieldExpression**) &(SgYieldExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgYieldExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgYieldExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgYieldExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgYieldExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgYieldExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgYieldExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgYieldExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgYieldExpression** objectArray = (SgYieldExpression**) &(SgYieldExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgYieldExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgYieldExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgYieldExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgYieldExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgYieldExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgYieldExpression** objectArray = (SgYieldExpression**) &(SgYieldExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgYieldExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgYieldExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgYieldExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgYieldExpression);

     return memory;
   }



/* #line 148272 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAlignOfOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAlignOfOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAlignOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAlignOfOp** objectArray = (SgAlignOfOp**) &(SgAlignOfOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAlignOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAlignOfOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAlignOfOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAlignOfOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAlignOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAlignOfOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAlignOfOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAlignOfOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAlignOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAlignOfOp** objectArray = (SgAlignOfOp**) &(SgAlignOfOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAlignOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAlignOfOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAlignOfOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAlignOfOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAlignOfOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAlignOfOp objectArray [] = *(Memory_Block_List.begin());
     if (SgAlignOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAlignOfOp** objectArray = (SgAlignOfOp**) &(SgAlignOfOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAlignOfOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAlignOfOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAlignOfOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAlignOfOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAlignOfOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAlignOfOp** objectArray = (SgAlignOfOp**) &(SgAlignOfOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAlignOfOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAlignOfOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAlignOfOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAlignOfOp);

     return memory;
   }



/* #line 148504 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgRangeExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgRangeExp objectArray [] = *(Memory_Block_List.begin());
     if (SgRangeExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRangeExp** objectArray = (SgRangeExp**) &(SgRangeExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRangeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgRangeExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgRangeExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgRangeExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgRangeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRangeExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgRangeExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRangeExp objectArray [] = *(Memory_Block_List.begin());
     if (SgRangeExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRangeExp** objectArray = (SgRangeExp**) &(SgRangeExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRangeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRangeExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgRangeExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgRangeExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgRangeExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRangeExp objectArray [] = *(Memory_Block_List.begin());
     if (SgRangeExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRangeExp** objectArray = (SgRangeExp**) &(SgRangeExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgRangeExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgRangeExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgRangeExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRangeExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgRangeExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgRangeExp** objectArray = (SgRangeExp**) &(SgRangeExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgRangeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgRangeExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRangeExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgRangeExp);

     return memory;
   }



/* #line 148736 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMagicColonExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMagicColonExp objectArray [] = *(Memory_Block_List.begin());
     if (SgMagicColonExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMagicColonExp** objectArray = (SgMagicColonExp**) &(SgMagicColonExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMagicColonExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMagicColonExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMagicColonExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMagicColonExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMagicColonExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMagicColonExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMagicColonExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMagicColonExp objectArray [] = *(Memory_Block_List.begin());
     if (SgMagicColonExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMagicColonExp** objectArray = (SgMagicColonExp**) &(SgMagicColonExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMagicColonExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMagicColonExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMagicColonExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMagicColonExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMagicColonExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMagicColonExp objectArray [] = *(Memory_Block_List.begin());
     if (SgMagicColonExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMagicColonExp** objectArray = (SgMagicColonExp**) &(SgMagicColonExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMagicColonExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMagicColonExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMagicColonExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMagicColonExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMagicColonExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMagicColonExp** objectArray = (SgMagicColonExp**) &(SgMagicColonExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMagicColonExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMagicColonExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMagicColonExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMagicColonExp);

     return memory;
   }



/* #line 148968 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeTraitBuiltinOperator::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeTraitBuiltinOperator objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeTraitBuiltinOperator::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeTraitBuiltinOperator** objectArray = (SgTypeTraitBuiltinOperator**) &(SgTypeTraitBuiltinOperator::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeTraitBuiltinOperator::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeTraitBuiltinOperator::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeTraitBuiltinOperator::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeTraitBuiltinOperator*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeTraitBuiltinOperator::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeTraitBuiltinOperator::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeTraitBuiltinOperator::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeTraitBuiltinOperator objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeTraitBuiltinOperator::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeTraitBuiltinOperator** objectArray = (SgTypeTraitBuiltinOperator**) &(SgTypeTraitBuiltinOperator::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeTraitBuiltinOperator::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeTraitBuiltinOperator::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeTraitBuiltinOperator object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeTraitBuiltinOperator object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeTraitBuiltinOperator::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeTraitBuiltinOperator objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeTraitBuiltinOperator::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeTraitBuiltinOperator** objectArray = (SgTypeTraitBuiltinOperator**) &(SgTypeTraitBuiltinOperator::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeTraitBuiltinOperator::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeTraitBuiltinOperator::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeTraitBuiltinOperator found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeTraitBuiltinOperator::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeTraitBuiltinOperator::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeTraitBuiltinOperator** objectArray = (SgTypeTraitBuiltinOperator**) &(SgTypeTraitBuiltinOperator::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeTraitBuiltinOperator::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeTraitBuiltinOperator::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeTraitBuiltinOperator::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeTraitBuiltinOperator);

     return memory;
   }



/* #line 149200 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCompoundLiteralExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCompoundLiteralExp objectArray [] = *(Memory_Block_List.begin());
     if (SgCompoundLiteralExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCompoundLiteralExp** objectArray = (SgCompoundLiteralExp**) &(SgCompoundLiteralExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCompoundLiteralExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCompoundLiteralExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCompoundLiteralExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCompoundLiteralExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCompoundLiteralExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCompoundLiteralExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCompoundLiteralExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCompoundLiteralExp objectArray [] = *(Memory_Block_List.begin());
     if (SgCompoundLiteralExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCompoundLiteralExp** objectArray = (SgCompoundLiteralExp**) &(SgCompoundLiteralExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCompoundLiteralExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCompoundLiteralExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCompoundLiteralExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCompoundLiteralExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCompoundLiteralExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCompoundLiteralExp objectArray [] = *(Memory_Block_List.begin());
     if (SgCompoundLiteralExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCompoundLiteralExp** objectArray = (SgCompoundLiteralExp**) &(SgCompoundLiteralExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCompoundLiteralExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCompoundLiteralExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCompoundLiteralExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCompoundLiteralExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCompoundLiteralExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCompoundLiteralExp** objectArray = (SgCompoundLiteralExp**) &(SgCompoundLiteralExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCompoundLiteralExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCompoundLiteralExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCompoundLiteralExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCompoundLiteralExp);

     return memory;
   }



/* #line 149432 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaAnnotation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaAnnotation** objectArray = (SgJavaAnnotation**) &(SgJavaAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaAnnotation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaAnnotation*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaAnnotation::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaAnnotation** objectArray = (SgJavaAnnotation**) &(SgJavaAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaAnnotation object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaAnnotation object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaAnnotation::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaAnnotation** objectArray = (SgJavaAnnotation**) &(SgJavaAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaAnnotation::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaAnnotation::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaAnnotation found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaAnnotation::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaAnnotation** objectArray = (SgJavaAnnotation**) &(SgJavaAnnotation::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaAnnotation::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaAnnotation::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaAnnotation);

     return memory;
   }



/* #line 149664 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaMarkerAnnotation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaMarkerAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaMarkerAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaMarkerAnnotation** objectArray = (SgJavaMarkerAnnotation**) &(SgJavaMarkerAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaMarkerAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaMarkerAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaMarkerAnnotation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaMarkerAnnotation*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaMarkerAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaMarkerAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaMarkerAnnotation::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaMarkerAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaMarkerAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaMarkerAnnotation** objectArray = (SgJavaMarkerAnnotation**) &(SgJavaMarkerAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaMarkerAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaMarkerAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaMarkerAnnotation object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaMarkerAnnotation object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaMarkerAnnotation::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaMarkerAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaMarkerAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaMarkerAnnotation** objectArray = (SgJavaMarkerAnnotation**) &(SgJavaMarkerAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaMarkerAnnotation::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaMarkerAnnotation::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaMarkerAnnotation found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaMarkerAnnotation::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaMarkerAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaMarkerAnnotation** objectArray = (SgJavaMarkerAnnotation**) &(SgJavaMarkerAnnotation::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaMarkerAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaMarkerAnnotation::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaMarkerAnnotation::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaMarkerAnnotation);

     return memory;
   }



/* #line 149896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaSingleMemberAnnotation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaSingleMemberAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaSingleMemberAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaSingleMemberAnnotation** objectArray = (SgJavaSingleMemberAnnotation**) &(SgJavaSingleMemberAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaSingleMemberAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaSingleMemberAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaSingleMemberAnnotation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaSingleMemberAnnotation*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaSingleMemberAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaSingleMemberAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaSingleMemberAnnotation::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaSingleMemberAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaSingleMemberAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaSingleMemberAnnotation** objectArray = (SgJavaSingleMemberAnnotation**) &(SgJavaSingleMemberAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaSingleMemberAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaSingleMemberAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaSingleMemberAnnotation object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaSingleMemberAnnotation object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaSingleMemberAnnotation::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaSingleMemberAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaSingleMemberAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaSingleMemberAnnotation** objectArray = (SgJavaSingleMemberAnnotation**) &(SgJavaSingleMemberAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaSingleMemberAnnotation::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaSingleMemberAnnotation::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaSingleMemberAnnotation found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaSingleMemberAnnotation::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaSingleMemberAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaSingleMemberAnnotation** objectArray = (SgJavaSingleMemberAnnotation**) &(SgJavaSingleMemberAnnotation::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaSingleMemberAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaSingleMemberAnnotation::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaSingleMemberAnnotation::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaSingleMemberAnnotation);

     return memory;
   }



/* #line 150128 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaNormalAnnotation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaNormalAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaNormalAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaNormalAnnotation** objectArray = (SgJavaNormalAnnotation**) &(SgJavaNormalAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaNormalAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaNormalAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaNormalAnnotation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaNormalAnnotation*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaNormalAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaNormalAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaNormalAnnotation::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaNormalAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaNormalAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaNormalAnnotation** objectArray = (SgJavaNormalAnnotation**) &(SgJavaNormalAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaNormalAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaNormalAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaNormalAnnotation object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaNormalAnnotation object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaNormalAnnotation::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaNormalAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaNormalAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaNormalAnnotation** objectArray = (SgJavaNormalAnnotation**) &(SgJavaNormalAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaNormalAnnotation::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaNormalAnnotation::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaNormalAnnotation found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaNormalAnnotation::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaNormalAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaNormalAnnotation** objectArray = (SgJavaNormalAnnotation**) &(SgJavaNormalAnnotation::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaNormalAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaNormalAnnotation::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaNormalAnnotation::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaNormalAnnotation);

     return memory;
   }



/* #line 150360 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaTypeExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaTypeExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaTypeExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaTypeExpression** objectArray = (SgJavaTypeExpression**) &(SgJavaTypeExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaTypeExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaTypeExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaTypeExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaTypeExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaTypeExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaTypeExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaTypeExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaTypeExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaTypeExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaTypeExpression** objectArray = (SgJavaTypeExpression**) &(SgJavaTypeExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaTypeExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaTypeExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaTypeExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaTypeExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaTypeExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaTypeExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaTypeExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaTypeExpression** objectArray = (SgJavaTypeExpression**) &(SgJavaTypeExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaTypeExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaTypeExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaTypeExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaTypeExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaTypeExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaTypeExpression** objectArray = (SgJavaTypeExpression**) &(SgJavaTypeExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaTypeExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaTypeExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaTypeExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaTypeExpression);

     return memory;
   }



/* #line 150592 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeExpression** objectArray = (SgTypeExpression**) &(SgTypeExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeExpression** objectArray = (SgTypeExpression**) &(SgTypeExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeExpression** objectArray = (SgTypeExpression**) &(SgTypeExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeExpression** objectArray = (SgTypeExpression**) &(SgTypeExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeExpression);

     return memory;
   }



/* #line 150824 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgClassExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgClassExp objectArray [] = *(Memory_Block_List.begin());
     if (SgClassExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassExp** objectArray = (SgClassExp**) &(SgClassExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgClassExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgClassExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgClassExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgClassExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgClassExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassExp objectArray [] = *(Memory_Block_List.begin());
     if (SgClassExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassExp** objectArray = (SgClassExp**) &(SgClassExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgClassExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgClassExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgClassExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassExp objectArray [] = *(Memory_Block_List.begin());
     if (SgClassExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassExp** objectArray = (SgClassExp**) &(SgClassExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgClassExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgClassExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgClassExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgClassExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgClassExp** objectArray = (SgClassExp**) &(SgClassExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgClassExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgClassExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgClassExp);

     return memory;
   }



/* #line 151056 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFunctionParameterRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFunctionParameterRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionParameterRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionParameterRefExp** objectArray = (SgFunctionParameterRefExp**) &(SgFunctionParameterRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionParameterRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFunctionParameterRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFunctionParameterRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFunctionParameterRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFunctionParameterRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionParameterRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFunctionParameterRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionParameterRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionParameterRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionParameterRefExp** objectArray = (SgFunctionParameterRefExp**) &(SgFunctionParameterRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionParameterRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionParameterRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFunctionParameterRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFunctionParameterRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFunctionParameterRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionParameterRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionParameterRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionParameterRefExp** objectArray = (SgFunctionParameterRefExp**) &(SgFunctionParameterRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFunctionParameterRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFunctionParameterRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFunctionParameterRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionParameterRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFunctionParameterRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFunctionParameterRefExp** objectArray = (SgFunctionParameterRefExp**) &(SgFunctionParameterRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFunctionParameterRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFunctionParameterRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionParameterRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFunctionParameterRefExp);

     return memory;
   }



/* #line 151288 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLambdaExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLambdaExp objectArray [] = *(Memory_Block_List.begin());
     if (SgLambdaExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLambdaExp** objectArray = (SgLambdaExp**) &(SgLambdaExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLambdaExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLambdaExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLambdaExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLambdaExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLambdaExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLambdaExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLambdaExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLambdaExp objectArray [] = *(Memory_Block_List.begin());
     if (SgLambdaExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLambdaExp** objectArray = (SgLambdaExp**) &(SgLambdaExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLambdaExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLambdaExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLambdaExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLambdaExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLambdaExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLambdaExp objectArray [] = *(Memory_Block_List.begin());
     if (SgLambdaExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLambdaExp** objectArray = (SgLambdaExp**) &(SgLambdaExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLambdaExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLambdaExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLambdaExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLambdaExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLambdaExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLambdaExp** objectArray = (SgLambdaExp**) &(SgLambdaExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLambdaExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLambdaExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLambdaExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLambdaExp);

     return memory;
   }



/* #line 151520 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgHereExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgHereExp objectArray [] = *(Memory_Block_List.begin());
     if (SgHereExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgHereExp** objectArray = (SgHereExp**) &(SgHereExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgHereExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgHereExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgHereExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgHereExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgHereExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgHereExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgHereExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgHereExp objectArray [] = *(Memory_Block_List.begin());
     if (SgHereExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgHereExp** objectArray = (SgHereExp**) &(SgHereExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgHereExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgHereExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgHereExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgHereExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgHereExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgHereExp objectArray [] = *(Memory_Block_List.begin());
     if (SgHereExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgHereExp** objectArray = (SgHereExp**) &(SgHereExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgHereExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgHereExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgHereExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgHereExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgHereExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgHereExp** objectArray = (SgHereExp**) &(SgHereExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgHereExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgHereExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgHereExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgHereExp);

     return memory;
   }



/* #line 151752 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAtExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAtExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAtExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAtExp** objectArray = (SgAtExp**) &(SgAtExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAtExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAtExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAtExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAtExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAtExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAtExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAtExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAtExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAtExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAtExp** objectArray = (SgAtExp**) &(SgAtExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAtExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAtExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAtExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAtExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAtExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAtExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAtExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAtExp** objectArray = (SgAtExp**) &(SgAtExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAtExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAtExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAtExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAtExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAtExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAtExp** objectArray = (SgAtExp**) &(SgAtExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAtExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAtExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAtExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAtExp);

     return memory;
   }



/* #line 151984 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFinishExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFinishExp objectArray [] = *(Memory_Block_List.begin());
     if (SgFinishExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFinishExp** objectArray = (SgFinishExp**) &(SgFinishExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFinishExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFinishExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFinishExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFinishExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFinishExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFinishExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFinishExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFinishExp objectArray [] = *(Memory_Block_List.begin());
     if (SgFinishExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFinishExp** objectArray = (SgFinishExp**) &(SgFinishExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFinishExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFinishExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFinishExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFinishExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFinishExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFinishExp objectArray [] = *(Memory_Block_List.begin());
     if (SgFinishExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFinishExp** objectArray = (SgFinishExp**) &(SgFinishExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFinishExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFinishExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFinishExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFinishExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFinishExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFinishExp** objectArray = (SgFinishExp**) &(SgFinishExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFinishExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFinishExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFinishExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFinishExp);

     return memory;
   }



/* #line 152216 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNoexceptOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNoexceptOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNoexceptOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNoexceptOp** objectArray = (SgNoexceptOp**) &(SgNoexceptOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNoexceptOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNoexceptOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNoexceptOp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNoexceptOp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNoexceptOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNoexceptOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNoexceptOp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNoexceptOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNoexceptOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNoexceptOp** objectArray = (SgNoexceptOp**) &(SgNoexceptOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNoexceptOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNoexceptOp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNoexceptOp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNoexceptOp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNoexceptOp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNoexceptOp objectArray [] = *(Memory_Block_List.begin());
     if (SgNoexceptOp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNoexceptOp** objectArray = (SgNoexceptOp**) &(SgNoexceptOp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNoexceptOp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNoexceptOp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNoexceptOp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNoexceptOp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNoexceptOp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNoexceptOp** objectArray = (SgNoexceptOp**) &(SgNoexceptOp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNoexceptOp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNoexceptOp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNoexceptOp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNoexceptOp);

     return memory;
   }



/* #line 152448 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaTaskRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaTaskRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskRefExp** objectArray = (SgAdaTaskRefExp**) &(SgAdaTaskRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaTaskRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaTaskRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaTaskRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaTaskRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaTaskRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskRefExp** objectArray = (SgAdaTaskRefExp**) &(SgAdaTaskRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaTaskRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaTaskRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaTaskRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskRefExp** objectArray = (SgAdaTaskRefExp**) &(SgAdaTaskRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaTaskRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaTaskRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaTaskRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaTaskRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaTaskRefExp** objectArray = (SgAdaTaskRefExp**) &(SgAdaTaskRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaTaskRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaTaskRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaTaskRefExp);

     return memory;
   }



/* #line 152680 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaProtectedRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaProtectedRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedRefExp** objectArray = (SgAdaProtectedRefExp**) &(SgAdaProtectedRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaProtectedRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaProtectedRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaProtectedRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaProtectedRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaProtectedRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedRefExp** objectArray = (SgAdaProtectedRefExp**) &(SgAdaProtectedRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaProtectedRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaProtectedRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaProtectedRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedRefExp** objectArray = (SgAdaProtectedRefExp**) &(SgAdaProtectedRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaProtectedRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaProtectedRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaProtectedRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaProtectedRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaProtectedRefExp** objectArray = (SgAdaProtectedRefExp**) &(SgAdaProtectedRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaProtectedRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaProtectedRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaProtectedRefExp);

     return memory;
   }



/* #line 152912 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFoldExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFoldExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgFoldExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFoldExpression** objectArray = (SgFoldExpression**) &(SgFoldExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFoldExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFoldExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFoldExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFoldExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFoldExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFoldExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFoldExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFoldExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgFoldExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFoldExpression** objectArray = (SgFoldExpression**) &(SgFoldExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFoldExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFoldExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFoldExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFoldExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFoldExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFoldExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgFoldExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFoldExpression** objectArray = (SgFoldExpression**) &(SgFoldExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFoldExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFoldExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFoldExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFoldExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFoldExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFoldExpression** objectArray = (SgFoldExpression**) &(SgFoldExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFoldExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFoldExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFoldExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFoldExpression);

     return memory;
   }



/* #line 153144 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAwaitExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAwaitExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAwaitExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAwaitExpression** objectArray = (SgAwaitExpression**) &(SgAwaitExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAwaitExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAwaitExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAwaitExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAwaitExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAwaitExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAwaitExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAwaitExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAwaitExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAwaitExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAwaitExpression** objectArray = (SgAwaitExpression**) &(SgAwaitExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAwaitExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAwaitExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAwaitExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAwaitExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAwaitExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAwaitExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAwaitExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAwaitExpression** objectArray = (SgAwaitExpression**) &(SgAwaitExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAwaitExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAwaitExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAwaitExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAwaitExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAwaitExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAwaitExpression** objectArray = (SgAwaitExpression**) &(SgAwaitExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAwaitExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAwaitExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAwaitExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAwaitExpression);

     return memory;
   }



/* #line 153376 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgChooseExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgChooseExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgChooseExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgChooseExpression** objectArray = (SgChooseExpression**) &(SgChooseExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgChooseExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgChooseExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgChooseExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgChooseExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgChooseExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgChooseExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgChooseExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgChooseExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgChooseExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgChooseExpression** objectArray = (SgChooseExpression**) &(SgChooseExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgChooseExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgChooseExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgChooseExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgChooseExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgChooseExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgChooseExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgChooseExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgChooseExpression** objectArray = (SgChooseExpression**) &(SgChooseExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgChooseExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgChooseExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgChooseExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgChooseExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgChooseExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgChooseExpression** objectArray = (SgChooseExpression**) &(SgChooseExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgChooseExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgChooseExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgChooseExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgChooseExpression);

     return memory;
   }



/* #line 153608 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaAttributeExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaAttributeExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAttributeExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAttributeExp** objectArray = (SgAdaAttributeExp**) &(SgAdaAttributeExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaAttributeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaAttributeExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaAttributeExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaAttributeExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaAttributeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaAttributeExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaAttributeExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaAttributeExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAttributeExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAttributeExp** objectArray = (SgAdaAttributeExp**) &(SgAdaAttributeExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaAttributeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaAttributeExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaAttributeExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaAttributeExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaAttributeExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaAttributeExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaAttributeExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaAttributeExp** objectArray = (SgAdaAttributeExp**) &(SgAdaAttributeExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaAttributeExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaAttributeExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaAttributeExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaAttributeExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaAttributeExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaAttributeExp** objectArray = (SgAdaAttributeExp**) &(SgAdaAttributeExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaAttributeExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaAttributeExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaAttributeExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaAttributeExp);

     return memory;
   }



/* #line 153840 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJovialTablePresetExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJovialTablePresetExp objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialTablePresetExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialTablePresetExp** objectArray = (SgJovialTablePresetExp**) &(SgJovialTablePresetExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialTablePresetExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJovialTablePresetExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJovialTablePresetExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJovialTablePresetExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJovialTablePresetExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialTablePresetExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJovialTablePresetExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialTablePresetExp objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialTablePresetExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialTablePresetExp** objectArray = (SgJovialTablePresetExp**) &(SgJovialTablePresetExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialTablePresetExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialTablePresetExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJovialTablePresetExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJovialTablePresetExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJovialTablePresetExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialTablePresetExp objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialTablePresetExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialTablePresetExp** objectArray = (SgJovialTablePresetExp**) &(SgJovialTablePresetExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJovialTablePresetExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJovialTablePresetExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJovialTablePresetExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialTablePresetExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJovialTablePresetExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJovialTablePresetExp** objectArray = (SgJovialTablePresetExp**) &(SgJovialTablePresetExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJovialTablePresetExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJovialTablePresetExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialTablePresetExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJovialTablePresetExp);

     return memory;
   }



/* #line 154072 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJovialPresetPositionExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJovialPresetPositionExp objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialPresetPositionExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialPresetPositionExp** objectArray = (SgJovialPresetPositionExp**) &(SgJovialPresetPositionExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialPresetPositionExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJovialPresetPositionExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJovialPresetPositionExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJovialPresetPositionExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJovialPresetPositionExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialPresetPositionExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJovialPresetPositionExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialPresetPositionExp objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialPresetPositionExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialPresetPositionExp** objectArray = (SgJovialPresetPositionExp**) &(SgJovialPresetPositionExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJovialPresetPositionExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJovialPresetPositionExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJovialPresetPositionExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJovialPresetPositionExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJovialPresetPositionExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJovialPresetPositionExp objectArray [] = *(Memory_Block_List.begin());
     if (SgJovialPresetPositionExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJovialPresetPositionExp** objectArray = (SgJovialPresetPositionExp**) &(SgJovialPresetPositionExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJovialPresetPositionExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJovialPresetPositionExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJovialPresetPositionExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialPresetPositionExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJovialPresetPositionExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJovialPresetPositionExp** objectArray = (SgJovialPresetPositionExp**) &(SgJovialPresetPositionExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJovialPresetPositionExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJovialPresetPositionExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJovialPresetPositionExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJovialPresetPositionExp);

     return memory;
   }



/* #line 154304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaOthersExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaOthersExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaOthersExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaOthersExp** objectArray = (SgAdaOthersExp**) &(SgAdaOthersExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaOthersExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaOthersExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaOthersExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaOthersExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaOthersExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaOthersExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaOthersExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaOthersExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaOthersExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaOthersExp** objectArray = (SgAdaOthersExp**) &(SgAdaOthersExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaOthersExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaOthersExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaOthersExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaOthersExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaOthersExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaOthersExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaOthersExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaOthersExp** objectArray = (SgAdaOthersExp**) &(SgAdaOthersExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaOthersExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaOthersExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaOthersExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaOthersExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaOthersExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaOthersExp** objectArray = (SgAdaOthersExp**) &(SgAdaOthersExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaOthersExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaOthersExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaOthersExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaOthersExp);

     return memory;
   }



/* #line 154536 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaRenamingRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaRenamingRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRenamingRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRenamingRefExp** objectArray = (SgAdaRenamingRefExp**) &(SgAdaRenamingRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaRenamingRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaRenamingRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaRenamingRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaRenamingRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaRenamingRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaRenamingRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaRenamingRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaRenamingRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRenamingRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRenamingRefExp** objectArray = (SgAdaRenamingRefExp**) &(SgAdaRenamingRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaRenamingRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaRenamingRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaRenamingRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaRenamingRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaRenamingRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaRenamingRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRenamingRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRenamingRefExp** objectArray = (SgAdaRenamingRefExp**) &(SgAdaRenamingRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaRenamingRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaRenamingRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaRenamingRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaRenamingRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaRenamingRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaRenamingRefExp** objectArray = (SgAdaRenamingRefExp**) &(SgAdaRenamingRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaRenamingRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaRenamingRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaRenamingRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaRenamingRefExp);

     return memory;
   }



/* #line 154768 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaUnitRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaUnitRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaUnitRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaUnitRefExp** objectArray = (SgAdaUnitRefExp**) &(SgAdaUnitRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaUnitRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaUnitRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaUnitRefExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaUnitRefExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaUnitRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaUnitRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaUnitRefExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaUnitRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaUnitRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaUnitRefExp** objectArray = (SgAdaUnitRefExp**) &(SgAdaUnitRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaUnitRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaUnitRefExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaUnitRefExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaUnitRefExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaUnitRefExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaUnitRefExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaUnitRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaUnitRefExp** objectArray = (SgAdaUnitRefExp**) &(SgAdaUnitRefExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaUnitRefExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaUnitRefExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaUnitRefExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaUnitRefExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaUnitRefExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaUnitRefExp** objectArray = (SgAdaUnitRefExp**) &(SgAdaUnitRefExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaUnitRefExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaUnitRefExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaUnitRefExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaUnitRefExp);

     return memory;
   }



/* #line 155000 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSymbol** objectArray = (SgSymbol**) &(SgSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSymbol** objectArray = (SgSymbol**) &(SgSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgSymbol** objectArray = (SgSymbol**) &(SgSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgSymbol** objectArray = (SgSymbol**) &(SgSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgSymbol);

     return memory;
   }



/* #line 155232 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgVariableSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgVariableSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgVariableSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariableSymbol** objectArray = (SgVariableSymbol**) &(SgVariableSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVariableSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgVariableSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgVariableSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgVariableSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgVariableSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVariableSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgVariableSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVariableSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgVariableSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariableSymbol** objectArray = (SgVariableSymbol**) &(SgVariableSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgVariableSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgVariableSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgVariableSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgVariableSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgVariableSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgVariableSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgVariableSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgVariableSymbol** objectArray = (SgVariableSymbol**) &(SgVariableSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgVariableSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgVariableSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgVariableSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVariableSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgVariableSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgVariableSymbol** objectArray = (SgVariableSymbol**) &(SgVariableSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgVariableSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgVariableSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgVariableSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgVariableSymbol);

     return memory;
   }



/* #line 155464 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateVariableSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateVariableSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateVariableSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateVariableSymbol** objectArray = (SgTemplateVariableSymbol**) &(SgTemplateVariableSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateVariableSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateVariableSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateVariableSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateVariableSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateVariableSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateVariableSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateVariableSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateVariableSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateVariableSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateVariableSymbol** objectArray = (SgTemplateVariableSymbol**) &(SgTemplateVariableSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateVariableSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateVariableSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateVariableSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateVariableSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateVariableSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateVariableSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateVariableSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateVariableSymbol** objectArray = (SgTemplateVariableSymbol**) &(SgTemplateVariableSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateVariableSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateVariableSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateVariableSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateVariableSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateVariableSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateVariableSymbol** objectArray = (SgTemplateVariableSymbol**) &(SgTemplateVariableSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateVariableSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateVariableSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateVariableSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateVariableSymbol);

     return memory;
   }



/* #line 155696 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNonrealSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNonrealSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealSymbol** objectArray = (SgNonrealSymbol**) &(SgNonrealSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNonrealSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNonrealSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNonrealSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNonrealSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNonrealSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNonrealSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNonrealSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNonrealSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealSymbol** objectArray = (SgNonrealSymbol**) &(SgNonrealSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNonrealSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNonrealSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNonrealSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNonrealSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNonrealSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNonrealSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgNonrealSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNonrealSymbol** objectArray = (SgNonrealSymbol**) &(SgNonrealSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNonrealSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNonrealSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNonrealSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNonrealSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNonrealSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNonrealSymbol** objectArray = (SgNonrealSymbol**) &(SgNonrealSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNonrealSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNonrealSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNonrealSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNonrealSymbol);

     return memory;
   }



/* #line 155928 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFunctionSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionSymbol** objectArray = (SgFunctionSymbol**) &(SgFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFunctionSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFunctionSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFunctionSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionSymbol** objectArray = (SgFunctionSymbol**) &(SgFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFunctionSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFunctionSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFunctionSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionSymbol** objectArray = (SgFunctionSymbol**) &(SgFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFunctionSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFunctionSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFunctionSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFunctionSymbol** objectArray = (SgFunctionSymbol**) &(SgFunctionSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFunctionSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFunctionSymbol);

     return memory;
   }



/* #line 156160 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgMemberFunctionSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgMemberFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgMemberFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMemberFunctionSymbol** objectArray = (SgMemberFunctionSymbol**) &(SgMemberFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMemberFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgMemberFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgMemberFunctionSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgMemberFunctionSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgMemberFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMemberFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgMemberFunctionSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMemberFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgMemberFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMemberFunctionSymbol** objectArray = (SgMemberFunctionSymbol**) &(SgMemberFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgMemberFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgMemberFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgMemberFunctionSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgMemberFunctionSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgMemberFunctionSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgMemberFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgMemberFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgMemberFunctionSymbol** objectArray = (SgMemberFunctionSymbol**) &(SgMemberFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgMemberFunctionSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgMemberFunctionSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgMemberFunctionSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMemberFunctionSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgMemberFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgMemberFunctionSymbol** objectArray = (SgMemberFunctionSymbol**) &(SgMemberFunctionSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgMemberFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgMemberFunctionSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgMemberFunctionSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgMemberFunctionSymbol);

     return memory;
   }



/* #line 156392 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateMemberFunctionSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateMemberFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateMemberFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateMemberFunctionSymbol** objectArray = (SgTemplateMemberFunctionSymbol**) &(SgTemplateMemberFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateMemberFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateMemberFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateMemberFunctionSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateMemberFunctionSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateMemberFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateMemberFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateMemberFunctionSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateMemberFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateMemberFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateMemberFunctionSymbol** objectArray = (SgTemplateMemberFunctionSymbol**) &(SgTemplateMemberFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateMemberFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateMemberFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateMemberFunctionSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateMemberFunctionSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateMemberFunctionSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateMemberFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateMemberFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateMemberFunctionSymbol** objectArray = (SgTemplateMemberFunctionSymbol**) &(SgTemplateMemberFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateMemberFunctionSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateMemberFunctionSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateMemberFunctionSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateMemberFunctionSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateMemberFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateMemberFunctionSymbol** objectArray = (SgTemplateMemberFunctionSymbol**) &(SgTemplateMemberFunctionSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateMemberFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateMemberFunctionSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateMemberFunctionSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateMemberFunctionSymbol);

     return memory;
   }



/* #line 156624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateFunctionSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateFunctionSymbol** objectArray = (SgTemplateFunctionSymbol**) &(SgTemplateFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateFunctionSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateFunctionSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateFunctionSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateFunctionSymbol** objectArray = (SgTemplateFunctionSymbol**) &(SgTemplateFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateFunctionSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateFunctionSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateFunctionSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateFunctionSymbol** objectArray = (SgTemplateFunctionSymbol**) &(SgTemplateFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateFunctionSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateFunctionSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateFunctionSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateFunctionSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateFunctionSymbol** objectArray = (SgTemplateFunctionSymbol**) &(SgTemplateFunctionSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateFunctionSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateFunctionSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateFunctionSymbol);

     return memory;
   }



/* #line 156856 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgRenameSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgRenameSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgRenameSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRenameSymbol** objectArray = (SgRenameSymbol**) &(SgRenameSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRenameSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgRenameSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgRenameSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgRenameSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgRenameSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRenameSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgRenameSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRenameSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgRenameSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRenameSymbol** objectArray = (SgRenameSymbol**) &(SgRenameSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgRenameSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgRenameSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgRenameSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgRenameSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgRenameSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgRenameSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgRenameSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgRenameSymbol** objectArray = (SgRenameSymbol**) &(SgRenameSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgRenameSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgRenameSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgRenameSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRenameSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgRenameSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgRenameSymbol** objectArray = (SgRenameSymbol**) &(SgRenameSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgRenameSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgRenameSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgRenameSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgRenameSymbol);

     return memory;
   }



/* #line 157088 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaInheritedFunctionSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaInheritedFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaInheritedFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaInheritedFunctionSymbol** objectArray = (SgAdaInheritedFunctionSymbol**) &(SgAdaInheritedFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaInheritedFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaInheritedFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaInheritedFunctionSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaInheritedFunctionSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaInheritedFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaInheritedFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaInheritedFunctionSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaInheritedFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaInheritedFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaInheritedFunctionSymbol** objectArray = (SgAdaInheritedFunctionSymbol**) &(SgAdaInheritedFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaInheritedFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaInheritedFunctionSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaInheritedFunctionSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaInheritedFunctionSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaInheritedFunctionSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaInheritedFunctionSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaInheritedFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaInheritedFunctionSymbol** objectArray = (SgAdaInheritedFunctionSymbol**) &(SgAdaInheritedFunctionSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaInheritedFunctionSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaInheritedFunctionSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaInheritedFunctionSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaInheritedFunctionSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaInheritedFunctionSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaInheritedFunctionSymbol** objectArray = (SgAdaInheritedFunctionSymbol**) &(SgAdaInheritedFunctionSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaInheritedFunctionSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaInheritedFunctionSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaInheritedFunctionSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaInheritedFunctionSymbol);

     return memory;
   }



/* #line 157320 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgFunctionTypeSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgFunctionTypeSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionTypeSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionTypeSymbol** objectArray = (SgFunctionTypeSymbol**) &(SgFunctionTypeSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionTypeSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgFunctionTypeSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgFunctionTypeSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgFunctionTypeSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgFunctionTypeSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionTypeSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgFunctionTypeSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionTypeSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionTypeSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionTypeSymbol** objectArray = (SgFunctionTypeSymbol**) &(SgFunctionTypeSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgFunctionTypeSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgFunctionTypeSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgFunctionTypeSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgFunctionTypeSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgFunctionTypeSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgFunctionTypeSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgFunctionTypeSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgFunctionTypeSymbol** objectArray = (SgFunctionTypeSymbol**) &(SgFunctionTypeSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgFunctionTypeSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgFunctionTypeSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgFunctionTypeSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionTypeSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgFunctionTypeSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgFunctionTypeSymbol** objectArray = (SgFunctionTypeSymbol**) &(SgFunctionTypeSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgFunctionTypeSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgFunctionTypeSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgFunctionTypeSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgFunctionTypeSymbol);

     return memory;
   }



/* #line 157552 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateSymbol** objectArray = (SgTemplateSymbol**) &(SgTemplateSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateSymbol** objectArray = (SgTemplateSymbol**) &(SgTemplateSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateSymbol** objectArray = (SgTemplateSymbol**) &(SgTemplateSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateSymbol** objectArray = (SgTemplateSymbol**) &(SgTemplateSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateSymbol);

     return memory;
   }



/* #line 157784 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgEnumFieldSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgEnumFieldSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumFieldSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumFieldSymbol** objectArray = (SgEnumFieldSymbol**) &(SgEnumFieldSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEnumFieldSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgEnumFieldSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgEnumFieldSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgEnumFieldSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgEnumFieldSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEnumFieldSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgEnumFieldSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEnumFieldSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumFieldSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumFieldSymbol** objectArray = (SgEnumFieldSymbol**) &(SgEnumFieldSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEnumFieldSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEnumFieldSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgEnumFieldSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgEnumFieldSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgEnumFieldSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEnumFieldSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumFieldSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumFieldSymbol** objectArray = (SgEnumFieldSymbol**) &(SgEnumFieldSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgEnumFieldSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgEnumFieldSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgEnumFieldSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEnumFieldSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgEnumFieldSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgEnumFieldSymbol** objectArray = (SgEnumFieldSymbol**) &(SgEnumFieldSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgEnumFieldSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgEnumFieldSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEnumFieldSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgEnumFieldSymbol);

     return memory;
   }



/* #line 158016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypeSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypeSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSymbol** objectArray = (SgTypeSymbol**) &(SgTypeSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypeSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypeSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypeSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypeSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypeSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSymbol** objectArray = (SgTypeSymbol**) &(SgTypeSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypeSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypeSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypeSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypeSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypeSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypeSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTypeSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypeSymbol** objectArray = (SgTypeSymbol**) &(SgTypeSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypeSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypeSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypeSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypeSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypeSymbol** objectArray = (SgTypeSymbol**) &(SgTypeSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypeSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypeSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypeSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypeSymbol);

     return memory;
   }



/* #line 158248 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgClassSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgClassSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgClassSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassSymbol** objectArray = (SgClassSymbol**) &(SgClassSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgClassSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgClassSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgClassSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgClassSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgClassSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgClassSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassSymbol** objectArray = (SgClassSymbol**) &(SgClassSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgClassSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgClassSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgClassSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgClassSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgClassSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgClassSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgClassSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgClassSymbol** objectArray = (SgClassSymbol**) &(SgClassSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgClassSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgClassSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgClassSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgClassSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgClassSymbol** objectArray = (SgClassSymbol**) &(SgClassSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgClassSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgClassSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgClassSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgClassSymbol);

     return memory;
   }



/* #line 158480 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateClassSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateClassSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateClassSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateClassSymbol** objectArray = (SgTemplateClassSymbol**) &(SgTemplateClassSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateClassSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateClassSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateClassSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateClassSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateClassSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateClassSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateClassSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateClassSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateClassSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateClassSymbol** objectArray = (SgTemplateClassSymbol**) &(SgTemplateClassSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateClassSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateClassSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateClassSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateClassSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateClassSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateClassSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateClassSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateClassSymbol** objectArray = (SgTemplateClassSymbol**) &(SgTemplateClassSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateClassSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateClassSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateClassSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateClassSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateClassSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateClassSymbol** objectArray = (SgTemplateClassSymbol**) &(SgTemplateClassSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateClassSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateClassSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateClassSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateClassSymbol);

     return memory;
   }



/* #line 158712 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTypedefSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTypedefSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTypedefSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypedefSymbol** objectArray = (SgTypedefSymbol**) &(SgTypedefSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypedefSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTypedefSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTypedefSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTypedefSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTypedefSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypedefSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTypedefSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypedefSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTypedefSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypedefSymbol** objectArray = (SgTypedefSymbol**) &(SgTypedefSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTypedefSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTypedefSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTypedefSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTypedefSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTypedefSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTypedefSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTypedefSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTypedefSymbol** objectArray = (SgTypedefSymbol**) &(SgTypedefSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTypedefSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTypedefSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTypedefSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypedefSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTypedefSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTypedefSymbol** objectArray = (SgTypedefSymbol**) &(SgTypedefSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTypedefSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTypedefSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTypedefSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTypedefSymbol);

     return memory;
   }



/* #line 158944 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgTemplateTypedefSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgTemplateTypedefSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateTypedefSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateTypedefSymbol** objectArray = (SgTemplateTypedefSymbol**) &(SgTemplateTypedefSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateTypedefSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgTemplateTypedefSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgTemplateTypedefSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgTemplateTypedefSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgTemplateTypedefSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateTypedefSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgTemplateTypedefSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateTypedefSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateTypedefSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateTypedefSymbol** objectArray = (SgTemplateTypedefSymbol**) &(SgTemplateTypedefSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgTemplateTypedefSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgTemplateTypedefSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgTemplateTypedefSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgTemplateTypedefSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgTemplateTypedefSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgTemplateTypedefSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgTemplateTypedefSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgTemplateTypedefSymbol** objectArray = (SgTemplateTypedefSymbol**) &(SgTemplateTypedefSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgTemplateTypedefSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgTemplateTypedefSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgTemplateTypedefSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateTypedefSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgTemplateTypedefSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgTemplateTypedefSymbol** objectArray = (SgTemplateTypedefSymbol**) &(SgTemplateTypedefSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgTemplateTypedefSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgTemplateTypedefSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgTemplateTypedefSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgTemplateTypedefSymbol);

     return memory;
   }



/* #line 159176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgEnumSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgEnumSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumSymbol** objectArray = (SgEnumSymbol**) &(SgEnumSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEnumSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgEnumSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgEnumSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgEnumSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgEnumSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEnumSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgEnumSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEnumSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumSymbol** objectArray = (SgEnumSymbol**) &(SgEnumSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgEnumSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgEnumSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgEnumSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgEnumSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgEnumSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgEnumSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgEnumSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgEnumSymbol** objectArray = (SgEnumSymbol**) &(SgEnumSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgEnumSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgEnumSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgEnumSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEnumSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgEnumSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgEnumSymbol** objectArray = (SgEnumSymbol**) &(SgEnumSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgEnumSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgEnumSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgEnumSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgEnumSymbol);

     return memory;
   }



/* #line 159408 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgLabelSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgLabelSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgLabelSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLabelSymbol** objectArray = (SgLabelSymbol**) &(SgLabelSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLabelSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgLabelSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgLabelSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgLabelSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgLabelSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLabelSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgLabelSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLabelSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgLabelSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLabelSymbol** objectArray = (SgLabelSymbol**) &(SgLabelSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgLabelSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgLabelSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgLabelSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgLabelSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgLabelSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgLabelSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgLabelSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgLabelSymbol** objectArray = (SgLabelSymbol**) &(SgLabelSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgLabelSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgLabelSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgLabelSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLabelSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgLabelSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgLabelSymbol** objectArray = (SgLabelSymbol**) &(SgLabelSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgLabelSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgLabelSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgLabelSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgLabelSymbol);

     return memory;
   }



/* #line 159640 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgDefaultSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgDefaultSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgDefaultSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDefaultSymbol** objectArray = (SgDefaultSymbol**) &(SgDefaultSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDefaultSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgDefaultSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgDefaultSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgDefaultSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgDefaultSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDefaultSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgDefaultSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDefaultSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgDefaultSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDefaultSymbol** objectArray = (SgDefaultSymbol**) &(SgDefaultSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgDefaultSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgDefaultSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgDefaultSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgDefaultSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgDefaultSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgDefaultSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgDefaultSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgDefaultSymbol** objectArray = (SgDefaultSymbol**) &(SgDefaultSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgDefaultSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgDefaultSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgDefaultSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDefaultSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgDefaultSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgDefaultSymbol** objectArray = (SgDefaultSymbol**) &(SgDefaultSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgDefaultSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgDefaultSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgDefaultSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgDefaultSymbol);

     return memory;
   }



/* #line 159872 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgNamespaceSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgNamespaceSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgNamespaceSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamespaceSymbol** objectArray = (SgNamespaceSymbol**) &(SgNamespaceSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNamespaceSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgNamespaceSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgNamespaceSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgNamespaceSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgNamespaceSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNamespaceSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgNamespaceSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNamespaceSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgNamespaceSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamespaceSymbol** objectArray = (SgNamespaceSymbol**) &(SgNamespaceSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgNamespaceSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgNamespaceSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgNamespaceSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgNamespaceSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgNamespaceSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgNamespaceSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgNamespaceSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgNamespaceSymbol** objectArray = (SgNamespaceSymbol**) &(SgNamespaceSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgNamespaceSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgNamespaceSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgNamespaceSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNamespaceSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgNamespaceSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgNamespaceSymbol** objectArray = (SgNamespaceSymbol**) &(SgNamespaceSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgNamespaceSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgNamespaceSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgNamespaceSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgNamespaceSymbol);

     return memory;
   }



/* #line 160104 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgIntrinsicSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgIntrinsicSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgIntrinsicSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntrinsicSymbol** objectArray = (SgIntrinsicSymbol**) &(SgIntrinsicSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIntrinsicSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgIntrinsicSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgIntrinsicSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgIntrinsicSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgIntrinsicSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIntrinsicSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgIntrinsicSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIntrinsicSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgIntrinsicSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntrinsicSymbol** objectArray = (SgIntrinsicSymbol**) &(SgIntrinsicSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgIntrinsicSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgIntrinsicSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgIntrinsicSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgIntrinsicSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgIntrinsicSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgIntrinsicSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgIntrinsicSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgIntrinsicSymbol** objectArray = (SgIntrinsicSymbol**) &(SgIntrinsicSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgIntrinsicSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgIntrinsicSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgIntrinsicSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIntrinsicSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgIntrinsicSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgIntrinsicSymbol** objectArray = (SgIntrinsicSymbol**) &(SgIntrinsicSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgIntrinsicSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgIntrinsicSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgIntrinsicSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgIntrinsicSymbol);

     return memory;
   }



/* #line 160336 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgModuleSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgModuleSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgModuleSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModuleSymbol** objectArray = (SgModuleSymbol**) &(SgModuleSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModuleSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgModuleSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgModuleSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgModuleSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgModuleSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModuleSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgModuleSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModuleSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgModuleSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModuleSymbol** objectArray = (SgModuleSymbol**) &(SgModuleSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgModuleSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgModuleSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgModuleSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgModuleSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgModuleSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgModuleSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgModuleSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgModuleSymbol** objectArray = (SgModuleSymbol**) &(SgModuleSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgModuleSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgModuleSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgModuleSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModuleSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgModuleSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgModuleSymbol** objectArray = (SgModuleSymbol**) &(SgModuleSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgModuleSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgModuleSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgModuleSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgModuleSymbol);

     return memory;
   }



/* #line 160568 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgInterfaceSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgInterfaceSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgInterfaceSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInterfaceSymbol** objectArray = (SgInterfaceSymbol**) &(SgInterfaceSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgInterfaceSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgInterfaceSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgInterfaceSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgInterfaceSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgInterfaceSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgInterfaceSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgInterfaceSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgInterfaceSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgInterfaceSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInterfaceSymbol** objectArray = (SgInterfaceSymbol**) &(SgInterfaceSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgInterfaceSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgInterfaceSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgInterfaceSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgInterfaceSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgInterfaceSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgInterfaceSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgInterfaceSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgInterfaceSymbol** objectArray = (SgInterfaceSymbol**) &(SgInterfaceSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgInterfaceSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgInterfaceSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgInterfaceSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgInterfaceSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgInterfaceSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgInterfaceSymbol** objectArray = (SgInterfaceSymbol**) &(SgInterfaceSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgInterfaceSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgInterfaceSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgInterfaceSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgInterfaceSymbol);

     return memory;
   }



/* #line 160800 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgCommonSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgCommonSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgCommonSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCommonSymbol** objectArray = (SgCommonSymbol**) &(SgCommonSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCommonSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgCommonSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgCommonSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgCommonSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgCommonSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCommonSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgCommonSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCommonSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgCommonSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCommonSymbol** objectArray = (SgCommonSymbol**) &(SgCommonSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgCommonSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgCommonSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgCommonSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgCommonSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgCommonSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgCommonSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgCommonSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgCommonSymbol** objectArray = (SgCommonSymbol**) &(SgCommonSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgCommonSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgCommonSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgCommonSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCommonSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgCommonSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgCommonSymbol** objectArray = (SgCommonSymbol**) &(SgCommonSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgCommonSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgCommonSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgCommonSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgCommonSymbol);

     return memory;
   }



/* #line 161032 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAliasSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAliasSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAliasSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAliasSymbol** objectArray = (SgAliasSymbol**) &(SgAliasSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAliasSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAliasSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAliasSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAliasSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAliasSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAliasSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAliasSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAliasSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAliasSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAliasSymbol** objectArray = (SgAliasSymbol**) &(SgAliasSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAliasSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAliasSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAliasSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAliasSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAliasSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAliasSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAliasSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAliasSymbol** objectArray = (SgAliasSymbol**) &(SgAliasSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAliasSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAliasSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAliasSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAliasSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAliasSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAliasSymbol** objectArray = (SgAliasSymbol**) &(SgAliasSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAliasSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAliasSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAliasSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAliasSymbol);

     return memory;
   }



/* #line 161264 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryAddressSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryAddressSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryAddressSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryAddressSymbol** objectArray = (SgAsmBinaryAddressSymbol**) &(SgAsmBinaryAddressSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryAddressSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryAddressSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryAddressSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryAddressSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryAddressSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryAddressSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryAddressSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryAddressSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryAddressSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryAddressSymbol** objectArray = (SgAsmBinaryAddressSymbol**) &(SgAsmBinaryAddressSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryAddressSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryAddressSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryAddressSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryAddressSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryAddressSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryAddressSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryAddressSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryAddressSymbol** objectArray = (SgAsmBinaryAddressSymbol**) &(SgAsmBinaryAddressSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryAddressSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryAddressSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryAddressSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryAddressSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryAddressSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryAddressSymbol** objectArray = (SgAsmBinaryAddressSymbol**) &(SgAsmBinaryAddressSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryAddressSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryAddressSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryAddressSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryAddressSymbol);

     return memory;
   }



/* #line 161496 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryDataSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryDataSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryDataSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryDataSymbol** objectArray = (SgAsmBinaryDataSymbol**) &(SgAsmBinaryDataSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryDataSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryDataSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryDataSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryDataSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryDataSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryDataSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryDataSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryDataSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryDataSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryDataSymbol** objectArray = (SgAsmBinaryDataSymbol**) &(SgAsmBinaryDataSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryDataSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryDataSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryDataSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryDataSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryDataSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryDataSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryDataSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryDataSymbol** objectArray = (SgAsmBinaryDataSymbol**) &(SgAsmBinaryDataSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryDataSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryDataSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryDataSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryDataSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryDataSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryDataSymbol** objectArray = (SgAsmBinaryDataSymbol**) &(SgAsmBinaryDataSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryDataSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryDataSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryDataSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryDataSymbol);

     return memory;
   }



/* #line 161728 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgJavaLabelSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgJavaLabelSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaLabelSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaLabelSymbol** objectArray = (SgJavaLabelSymbol**) &(SgJavaLabelSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaLabelSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgJavaLabelSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgJavaLabelSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgJavaLabelSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgJavaLabelSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaLabelSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgJavaLabelSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaLabelSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaLabelSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaLabelSymbol** objectArray = (SgJavaLabelSymbol**) &(SgJavaLabelSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgJavaLabelSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgJavaLabelSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgJavaLabelSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgJavaLabelSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgJavaLabelSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgJavaLabelSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgJavaLabelSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgJavaLabelSymbol** objectArray = (SgJavaLabelSymbol**) &(SgJavaLabelSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgJavaLabelSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgJavaLabelSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgJavaLabelSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaLabelSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgJavaLabelSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgJavaLabelSymbol** objectArray = (SgJavaLabelSymbol**) &(SgJavaLabelSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgJavaLabelSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgJavaLabelSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgJavaLabelSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgJavaLabelSymbol);

     return memory;
   }



/* #line 161960 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaPackageSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaPackageSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageSymbol** objectArray = (SgAdaPackageSymbol**) &(SgAdaPackageSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaPackageSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaPackageSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaPackageSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaPackageSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaPackageSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaPackageSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaPackageSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaPackageSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageSymbol** objectArray = (SgAdaPackageSymbol**) &(SgAdaPackageSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaPackageSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaPackageSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaPackageSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaPackageSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaPackageSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaPackageSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaPackageSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaPackageSymbol** objectArray = (SgAdaPackageSymbol**) &(SgAdaPackageSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaPackageSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaPackageSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaPackageSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaPackageSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaPackageSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaPackageSymbol** objectArray = (SgAdaPackageSymbol**) &(SgAdaPackageSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaPackageSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaPackageSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaPackageSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaPackageSymbol);

     return memory;
   }



/* #line 162192 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaTaskSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaTaskSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskSymbol** objectArray = (SgAdaTaskSymbol**) &(SgAdaTaskSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaTaskSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaTaskSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaTaskSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaTaskSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaTaskSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskSymbol** objectArray = (SgAdaTaskSymbol**) &(SgAdaTaskSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaTaskSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaTaskSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaTaskSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaTaskSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaTaskSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaTaskSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaTaskSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaTaskSymbol** objectArray = (SgAdaTaskSymbol**) &(SgAdaTaskSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaTaskSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaTaskSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaTaskSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaTaskSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaTaskSymbol** objectArray = (SgAdaTaskSymbol**) &(SgAdaTaskSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaTaskSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaTaskSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaTaskSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaTaskSymbol);

     return memory;
   }



/* #line 162424 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaProtectedSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaProtectedSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedSymbol** objectArray = (SgAdaProtectedSymbol**) &(SgAdaProtectedSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaProtectedSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaProtectedSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaProtectedSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaProtectedSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaProtectedSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedSymbol** objectArray = (SgAdaProtectedSymbol**) &(SgAdaProtectedSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaProtectedSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaProtectedSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaProtectedSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaProtectedSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaProtectedSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaProtectedSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaProtectedSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaProtectedSymbol** objectArray = (SgAdaProtectedSymbol**) &(SgAdaProtectedSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaProtectedSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaProtectedSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaProtectedSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaProtectedSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaProtectedSymbol** objectArray = (SgAdaProtectedSymbol**) &(SgAdaProtectedSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaProtectedSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaProtectedSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaProtectedSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaProtectedSymbol);

     return memory;
   }



/* #line 162656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaRenamingSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaRenamingSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRenamingSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRenamingSymbol** objectArray = (SgAdaRenamingSymbol**) &(SgAdaRenamingSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaRenamingSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaRenamingSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaRenamingSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaRenamingSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaRenamingSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaRenamingSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaRenamingSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaRenamingSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRenamingSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRenamingSymbol** objectArray = (SgAdaRenamingSymbol**) &(SgAdaRenamingSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaRenamingSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaRenamingSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaRenamingSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaRenamingSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaRenamingSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaRenamingSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaRenamingSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaRenamingSymbol** objectArray = (SgAdaRenamingSymbol**) &(SgAdaRenamingSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaRenamingSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaRenamingSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaRenamingSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaRenamingSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaRenamingSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaRenamingSymbol** objectArray = (SgAdaRenamingSymbol**) &(SgAdaRenamingSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaRenamingSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaRenamingSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaRenamingSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaRenamingSymbol);

     return memory;
   }



/* #line 162888 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaGenericSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaGenericSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericSymbol** objectArray = (SgAdaGenericSymbol**) &(SgAdaGenericSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaGenericSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaGenericSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaGenericSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaGenericSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaGenericSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaGenericSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaGenericSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaGenericSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericSymbol** objectArray = (SgAdaGenericSymbol**) &(SgAdaGenericSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaGenericSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaGenericSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaGenericSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaGenericSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaGenericSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaGenericSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericSymbol** objectArray = (SgAdaGenericSymbol**) &(SgAdaGenericSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaGenericSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaGenericSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaGenericSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaGenericSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaGenericSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaGenericSymbol** objectArray = (SgAdaGenericSymbol**) &(SgAdaGenericSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaGenericSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaGenericSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaGenericSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaGenericSymbol);

     return memory;
   }



/* #line 163120 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaGenericInstanceSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaGenericInstanceSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericInstanceSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericInstanceSymbol** objectArray = (SgAdaGenericInstanceSymbol**) &(SgAdaGenericInstanceSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaGenericInstanceSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaGenericInstanceSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaGenericInstanceSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaGenericInstanceSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaGenericInstanceSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaGenericInstanceSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaGenericInstanceSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaGenericInstanceSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericInstanceSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericInstanceSymbol** objectArray = (SgAdaGenericInstanceSymbol**) &(SgAdaGenericInstanceSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaGenericInstanceSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaGenericInstanceSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaGenericInstanceSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaGenericInstanceSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaGenericInstanceSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaGenericInstanceSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaGenericInstanceSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaGenericInstanceSymbol** objectArray = (SgAdaGenericInstanceSymbol**) &(SgAdaGenericInstanceSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaGenericInstanceSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaGenericInstanceSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaGenericInstanceSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaGenericInstanceSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaGenericInstanceSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaGenericInstanceSymbol** objectArray = (SgAdaGenericInstanceSymbol**) &(SgAdaGenericInstanceSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaGenericInstanceSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaGenericInstanceSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaGenericInstanceSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaGenericInstanceSymbol);

     return memory;
   }



/* #line 163352 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAdaFormalPackageSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAdaFormalPackageSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFormalPackageSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFormalPackageSymbol** objectArray = (SgAdaFormalPackageSymbol**) &(SgAdaFormalPackageSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaFormalPackageSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAdaFormalPackageSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAdaFormalPackageSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAdaFormalPackageSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAdaFormalPackageSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaFormalPackageSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAdaFormalPackageSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaFormalPackageSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFormalPackageSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFormalPackageSymbol** objectArray = (SgAdaFormalPackageSymbol**) &(SgAdaFormalPackageSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAdaFormalPackageSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAdaFormalPackageSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAdaFormalPackageSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAdaFormalPackageSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAdaFormalPackageSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAdaFormalPackageSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAdaFormalPackageSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAdaFormalPackageSymbol** objectArray = (SgAdaFormalPackageSymbol**) &(SgAdaFormalPackageSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAdaFormalPackageSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAdaFormalPackageSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAdaFormalPackageSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaFormalPackageSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAdaFormalPackageSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAdaFormalPackageSymbol** objectArray = (SgAdaFormalPackageSymbol**) &(SgAdaFormalPackageSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAdaFormalPackageSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAdaFormalPackageSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAdaFormalPackageSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAdaFormalPackageSymbol);

     return memory;
   }



/* #line 163584 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmNode::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmNode objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNode** objectArray = (SgAsmNode**) &(SgAsmNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmNode::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmNode*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmNode::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNode objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNode** objectArray = (SgAsmNode**) &(SgAsmNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmNode object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmNode object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmNode::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNode objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNode** objectArray = (SgAsmNode**) &(SgAsmNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmNode::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmNode::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmNode found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNode::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmNode::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmNode** objectArray = (SgAsmNode**) &(SgAsmNode::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmNode::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNode::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmNode);

     return memory;
   }



/* #line 163816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilNode::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilNode objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilNode** objectArray = (SgAsmCilNode**) &(SgAsmCilNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilNode::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilNode*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilNode::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilNode objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilNode** objectArray = (SgAsmCilNode**) &(SgAsmCilNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilNode object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilNode object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilNode::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilNode objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilNode** objectArray = (SgAsmCilNode**) &(SgAsmCilNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilNode::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilNode::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilNode found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilNode::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilNode::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilNode** objectArray = (SgAsmCilNode**) &(SgAsmCilNode::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilNode::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilNode::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilNode);

     return memory;
   }



/* #line 164048 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilDataStream::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilDataStream objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilDataStream::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilDataStream** objectArray = (SgAsmCilDataStream**) &(SgAsmCilDataStream::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilDataStream::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilDataStream::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilDataStream::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilDataStream*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilDataStream::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilDataStream::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilDataStream::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilDataStream objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilDataStream::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilDataStream** objectArray = (SgAsmCilDataStream**) &(SgAsmCilDataStream::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilDataStream::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilDataStream::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilDataStream object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilDataStream object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilDataStream::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilDataStream objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilDataStream::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilDataStream** objectArray = (SgAsmCilDataStream**) &(SgAsmCilDataStream::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilDataStream::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilDataStream::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilDataStream found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilDataStream::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilDataStream::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilDataStream** objectArray = (SgAsmCilDataStream**) &(SgAsmCilDataStream::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilDataStream::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilDataStream::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilDataStream::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilDataStream);

     return memory;
   }



/* #line 164280 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilErrorHeap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilErrorHeap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilErrorHeap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilErrorHeap** objectArray = (SgAsmCilErrorHeap**) &(SgAsmCilErrorHeap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilErrorHeap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilErrorHeap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilErrorHeap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilErrorHeap*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilErrorHeap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilErrorHeap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilErrorHeap::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilErrorHeap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilErrorHeap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilErrorHeap** objectArray = (SgAsmCilErrorHeap**) &(SgAsmCilErrorHeap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilErrorHeap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilErrorHeap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilErrorHeap object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilErrorHeap object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilErrorHeap::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilErrorHeap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilErrorHeap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilErrorHeap** objectArray = (SgAsmCilErrorHeap**) &(SgAsmCilErrorHeap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilErrorHeap::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilErrorHeap::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilErrorHeap found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilErrorHeap::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilErrorHeap::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilErrorHeap** objectArray = (SgAsmCilErrorHeap**) &(SgAsmCilErrorHeap::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilErrorHeap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilErrorHeap::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilErrorHeap::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilErrorHeap);

     return memory;
   }



/* #line 164512 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMetadataHeap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMetadataHeap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMetadataHeap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMetadataHeap** objectArray = (SgAsmCilMetadataHeap**) &(SgAsmCilMetadataHeap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMetadataHeap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMetadataHeap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMetadataHeap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMetadataHeap*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMetadataHeap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMetadataHeap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMetadataHeap::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMetadataHeap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMetadataHeap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMetadataHeap** objectArray = (SgAsmCilMetadataHeap**) &(SgAsmCilMetadataHeap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMetadataHeap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMetadataHeap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMetadataHeap object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMetadataHeap object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMetadataHeap::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMetadataHeap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMetadataHeap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMetadataHeap** objectArray = (SgAsmCilMetadataHeap**) &(SgAsmCilMetadataHeap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMetadataHeap::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMetadataHeap::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMetadataHeap found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMetadataHeap::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMetadataHeap::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMetadataHeap** objectArray = (SgAsmCilMetadataHeap**) &(SgAsmCilMetadataHeap::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMetadataHeap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMetadataHeap::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMetadataHeap::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMetadataHeap);

     return memory;
   }



/* #line 164744 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilUint32Heap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilUint32Heap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilUint32Heap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilUint32Heap** objectArray = (SgAsmCilUint32Heap**) &(SgAsmCilUint32Heap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilUint32Heap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilUint32Heap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilUint32Heap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilUint32Heap*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilUint32Heap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilUint32Heap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilUint32Heap::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilUint32Heap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilUint32Heap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilUint32Heap** objectArray = (SgAsmCilUint32Heap**) &(SgAsmCilUint32Heap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilUint32Heap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilUint32Heap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilUint32Heap object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilUint32Heap object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilUint32Heap::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilUint32Heap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilUint32Heap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilUint32Heap** objectArray = (SgAsmCilUint32Heap**) &(SgAsmCilUint32Heap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilUint32Heap::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilUint32Heap::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilUint32Heap found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilUint32Heap::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilUint32Heap::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilUint32Heap** objectArray = (SgAsmCilUint32Heap**) &(SgAsmCilUint32Heap::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilUint32Heap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilUint32Heap::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilUint32Heap::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilUint32Heap);

     return memory;
   }



/* #line 164976 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilUint8Heap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilUint8Heap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilUint8Heap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilUint8Heap** objectArray = (SgAsmCilUint8Heap**) &(SgAsmCilUint8Heap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilUint8Heap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilUint8Heap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilUint8Heap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilUint8Heap*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilUint8Heap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilUint8Heap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilUint8Heap::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilUint8Heap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilUint8Heap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilUint8Heap** objectArray = (SgAsmCilUint8Heap**) &(SgAsmCilUint8Heap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilUint8Heap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilUint8Heap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilUint8Heap object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilUint8Heap object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilUint8Heap::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilUint8Heap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilUint8Heap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilUint8Heap** objectArray = (SgAsmCilUint8Heap**) &(SgAsmCilUint8Heap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilUint8Heap::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilUint8Heap::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilUint8Heap found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilUint8Heap::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilUint8Heap::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilUint8Heap** objectArray = (SgAsmCilUint8Heap**) &(SgAsmCilUint8Heap::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilUint8Heap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilUint8Heap::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilUint8Heap::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilUint8Heap);

     return memory;
   }



/* #line 165208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilExceptionData::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilExceptionData objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilExceptionData::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilExceptionData** objectArray = (SgAsmCilExceptionData**) &(SgAsmCilExceptionData::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilExceptionData::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilExceptionData::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilExceptionData::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilExceptionData*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilExceptionData::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilExceptionData::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilExceptionData::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilExceptionData objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilExceptionData::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilExceptionData** objectArray = (SgAsmCilExceptionData**) &(SgAsmCilExceptionData::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilExceptionData::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilExceptionData::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilExceptionData object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilExceptionData object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilExceptionData::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilExceptionData objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilExceptionData::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilExceptionData** objectArray = (SgAsmCilExceptionData**) &(SgAsmCilExceptionData::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilExceptionData::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilExceptionData::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilExceptionData found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilExceptionData::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilExceptionData::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilExceptionData** objectArray = (SgAsmCilExceptionData**) &(SgAsmCilExceptionData::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilExceptionData::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilExceptionData::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilExceptionData::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilExceptionData);

     return memory;
   }



/* #line 165440 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMetadata::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMetadata objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMetadata::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMetadata** objectArray = (SgAsmCilMetadata**) &(SgAsmCilMetadata::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMetadata::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMetadata::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMetadata::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMetadata*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMetadata::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMetadata::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMetadata::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMetadata objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMetadata::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMetadata** objectArray = (SgAsmCilMetadata**) &(SgAsmCilMetadata::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMetadata::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMetadata::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMetadata object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMetadata object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMetadata::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMetadata objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMetadata::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMetadata** objectArray = (SgAsmCilMetadata**) &(SgAsmCilMetadata::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMetadata::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMetadata::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMetadata found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMetadata::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMetadata::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMetadata** objectArray = (SgAsmCilMetadata**) &(SgAsmCilMetadata::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMetadata::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMetadata::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMetadata::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMetadata);

     return memory;
   }



/* #line 165672 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilAssembly::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilAssembly objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssembly::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssembly** objectArray = (SgAsmCilAssembly**) &(SgAsmCilAssembly::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssembly::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilAssembly::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilAssembly::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilAssembly*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilAssembly::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssembly::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilAssembly::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssembly objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssembly::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssembly** objectArray = (SgAsmCilAssembly**) &(SgAsmCilAssembly::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssembly::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssembly::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilAssembly object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilAssembly object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilAssembly::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssembly objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssembly::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssembly** objectArray = (SgAsmCilAssembly**) &(SgAsmCilAssembly::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilAssembly::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilAssembly::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilAssembly found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssembly::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilAssembly::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilAssembly** objectArray = (SgAsmCilAssembly**) &(SgAsmCilAssembly::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilAssembly::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilAssembly::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssembly::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilAssembly);

     return memory;
   }



/* #line 165904 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilAssemblyOS::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilAssemblyOS objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyOS::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyOS** objectArray = (SgAsmCilAssemblyOS**) &(SgAsmCilAssemblyOS::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyOS::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilAssemblyOS::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilAssemblyOS::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilAssemblyOS*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilAssemblyOS::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyOS::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilAssemblyOS::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyOS objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyOS::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyOS** objectArray = (SgAsmCilAssemblyOS**) &(SgAsmCilAssemblyOS::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyOS::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyOS::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilAssemblyOS object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilAssemblyOS object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilAssemblyOS::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyOS objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyOS::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyOS** objectArray = (SgAsmCilAssemblyOS**) &(SgAsmCilAssemblyOS::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilAssemblyOS::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilAssemblyOS::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilAssemblyOS found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyOS::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilAssemblyOS::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilAssemblyOS** objectArray = (SgAsmCilAssemblyOS**) &(SgAsmCilAssemblyOS::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilAssemblyOS::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilAssemblyOS::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyOS::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilAssemblyOS);

     return memory;
   }



/* #line 166136 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilAssemblyOSTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilAssemblyOSTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyOSTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyOSTable** objectArray = (SgAsmCilAssemblyOSTable**) &(SgAsmCilAssemblyOSTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyOSTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilAssemblyOSTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilAssemblyOSTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilAssemblyOSTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilAssemblyOSTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyOSTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilAssemblyOSTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyOSTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyOSTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyOSTable** objectArray = (SgAsmCilAssemblyOSTable**) &(SgAsmCilAssemblyOSTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyOSTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyOSTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilAssemblyOSTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilAssemblyOSTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilAssemblyOSTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyOSTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyOSTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyOSTable** objectArray = (SgAsmCilAssemblyOSTable**) &(SgAsmCilAssemblyOSTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilAssemblyOSTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilAssemblyOSTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilAssemblyOSTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyOSTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilAssemblyOSTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilAssemblyOSTable** objectArray = (SgAsmCilAssemblyOSTable**) &(SgAsmCilAssemblyOSTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilAssemblyOSTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilAssemblyOSTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyOSTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilAssemblyOSTable);

     return memory;
   }



/* #line 166368 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilAssemblyProcessor::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilAssemblyProcessor objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyProcessor::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyProcessor** objectArray = (SgAsmCilAssemblyProcessor**) &(SgAsmCilAssemblyProcessor::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyProcessor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilAssemblyProcessor::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilAssemblyProcessor::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilAssemblyProcessor*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilAssemblyProcessor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyProcessor::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilAssemblyProcessor::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyProcessor objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyProcessor::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyProcessor** objectArray = (SgAsmCilAssemblyProcessor**) &(SgAsmCilAssemblyProcessor::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyProcessor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyProcessor::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilAssemblyProcessor object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilAssemblyProcessor object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilAssemblyProcessor::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyProcessor objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyProcessor::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyProcessor** objectArray = (SgAsmCilAssemblyProcessor**) &(SgAsmCilAssemblyProcessor::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilAssemblyProcessor::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilAssemblyProcessor::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilAssemblyProcessor found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyProcessor::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilAssemblyProcessor::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilAssemblyProcessor** objectArray = (SgAsmCilAssemblyProcessor**) &(SgAsmCilAssemblyProcessor::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilAssemblyProcessor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilAssemblyProcessor::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyProcessor::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilAssemblyProcessor);

     return memory;
   }



/* #line 166600 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilAssemblyProcessorTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilAssemblyProcessorTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyProcessorTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyProcessorTable** objectArray = (SgAsmCilAssemblyProcessorTable**) &(SgAsmCilAssemblyProcessorTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyProcessorTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilAssemblyProcessorTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilAssemblyProcessorTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilAssemblyProcessorTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilAssemblyProcessorTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyProcessorTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilAssemblyProcessorTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyProcessorTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyProcessorTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyProcessorTable** objectArray = (SgAsmCilAssemblyProcessorTable**) &(SgAsmCilAssemblyProcessorTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyProcessorTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyProcessorTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilAssemblyProcessorTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilAssemblyProcessorTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilAssemblyProcessorTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyProcessorTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyProcessorTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyProcessorTable** objectArray = (SgAsmCilAssemblyProcessorTable**) &(SgAsmCilAssemblyProcessorTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilAssemblyProcessorTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilAssemblyProcessorTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilAssemblyProcessorTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyProcessorTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilAssemblyProcessorTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilAssemblyProcessorTable** objectArray = (SgAsmCilAssemblyProcessorTable**) &(SgAsmCilAssemblyProcessorTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilAssemblyProcessorTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilAssemblyProcessorTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyProcessorTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilAssemblyProcessorTable);

     return memory;
   }



/* #line 166832 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilAssemblyRef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilAssemblyRef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRef** objectArray = (SgAsmCilAssemblyRef**) &(SgAsmCilAssemblyRef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilAssemblyRef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilAssemblyRef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilAssemblyRef*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilAssemblyRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyRef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilAssemblyRef::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyRef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRef** objectArray = (SgAsmCilAssemblyRef**) &(SgAsmCilAssemblyRef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyRef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilAssemblyRef object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilAssemblyRef object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilAssemblyRef::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyRef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRef** objectArray = (SgAsmCilAssemblyRef**) &(SgAsmCilAssemblyRef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilAssemblyRef::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilAssemblyRef::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilAssemblyRef found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyRef::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilAssemblyRef::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilAssemblyRef** objectArray = (SgAsmCilAssemblyRef**) &(SgAsmCilAssemblyRef::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilAssemblyRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilAssemblyRef::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyRef::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilAssemblyRef);

     return memory;
   }



/* #line 167064 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilAssemblyRefOS::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilAssemblyRefOS objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefOS::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefOS** objectArray = (SgAsmCilAssemblyRefOS**) &(SgAsmCilAssemblyRefOS::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyRefOS::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilAssemblyRefOS::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilAssemblyRefOS::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilAssemblyRefOS*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilAssemblyRefOS::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyRefOS::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilAssemblyRefOS::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyRefOS objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefOS::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefOS** objectArray = (SgAsmCilAssemblyRefOS**) &(SgAsmCilAssemblyRefOS::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyRefOS::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyRefOS::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilAssemblyRefOS object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilAssemblyRefOS object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilAssemblyRefOS::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyRefOS objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefOS::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefOS** objectArray = (SgAsmCilAssemblyRefOS**) &(SgAsmCilAssemblyRefOS::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilAssemblyRefOS::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilAssemblyRefOS::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilAssemblyRefOS found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyRefOS::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilAssemblyRefOS::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilAssemblyRefOS** objectArray = (SgAsmCilAssemblyRefOS**) &(SgAsmCilAssemblyRefOS::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilAssemblyRefOS::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilAssemblyRefOS::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyRefOS::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilAssemblyRefOS);

     return memory;
   }



/* #line 167296 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilAssemblyRefOSTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilAssemblyRefOSTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefOSTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefOSTable** objectArray = (SgAsmCilAssemblyRefOSTable**) &(SgAsmCilAssemblyRefOSTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyRefOSTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilAssemblyRefOSTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilAssemblyRefOSTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilAssemblyRefOSTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilAssemblyRefOSTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyRefOSTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilAssemblyRefOSTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyRefOSTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefOSTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefOSTable** objectArray = (SgAsmCilAssemblyRefOSTable**) &(SgAsmCilAssemblyRefOSTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyRefOSTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyRefOSTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilAssemblyRefOSTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilAssemblyRefOSTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilAssemblyRefOSTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyRefOSTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefOSTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefOSTable** objectArray = (SgAsmCilAssemblyRefOSTable**) &(SgAsmCilAssemblyRefOSTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilAssemblyRefOSTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilAssemblyRefOSTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilAssemblyRefOSTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyRefOSTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilAssemblyRefOSTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilAssemblyRefOSTable** objectArray = (SgAsmCilAssemblyRefOSTable**) &(SgAsmCilAssemblyRefOSTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilAssemblyRefOSTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilAssemblyRefOSTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyRefOSTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilAssemblyRefOSTable);

     return memory;
   }



/* #line 167528 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilAssemblyRefProcessor::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilAssemblyRefProcessor objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefProcessor::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefProcessor** objectArray = (SgAsmCilAssemblyRefProcessor**) &(SgAsmCilAssemblyRefProcessor::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyRefProcessor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilAssemblyRefProcessor::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilAssemblyRefProcessor::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilAssemblyRefProcessor*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilAssemblyRefProcessor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyRefProcessor::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilAssemblyRefProcessor::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyRefProcessor objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefProcessor::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefProcessor** objectArray = (SgAsmCilAssemblyRefProcessor**) &(SgAsmCilAssemblyRefProcessor::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyRefProcessor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyRefProcessor::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilAssemblyRefProcessor object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilAssemblyRefProcessor object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilAssemblyRefProcessor::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyRefProcessor objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefProcessor::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefProcessor** objectArray = (SgAsmCilAssemblyRefProcessor**) &(SgAsmCilAssemblyRefProcessor::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilAssemblyRefProcessor::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilAssemblyRefProcessor::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilAssemblyRefProcessor found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyRefProcessor::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilAssemblyRefProcessor::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilAssemblyRefProcessor** objectArray = (SgAsmCilAssemblyRefProcessor**) &(SgAsmCilAssemblyRefProcessor::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilAssemblyRefProcessor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilAssemblyRefProcessor::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyRefProcessor::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilAssemblyRefProcessor);

     return memory;
   }



/* #line 167760 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilAssemblyRefProcessorTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilAssemblyRefProcessorTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefProcessorTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefProcessorTable** objectArray = (SgAsmCilAssemblyRefProcessorTable**) &(SgAsmCilAssemblyRefProcessorTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyRefProcessorTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilAssemblyRefProcessorTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilAssemblyRefProcessorTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilAssemblyRefProcessorTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilAssemblyRefProcessorTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyRefProcessorTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilAssemblyRefProcessorTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyRefProcessorTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefProcessorTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefProcessorTable** objectArray = (SgAsmCilAssemblyRefProcessorTable**) &(SgAsmCilAssemblyRefProcessorTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyRefProcessorTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyRefProcessorTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilAssemblyRefProcessorTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilAssemblyRefProcessorTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilAssemblyRefProcessorTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyRefProcessorTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefProcessorTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefProcessorTable** objectArray = (SgAsmCilAssemblyRefProcessorTable**) &(SgAsmCilAssemblyRefProcessorTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilAssemblyRefProcessorTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilAssemblyRefProcessorTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilAssemblyRefProcessorTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyRefProcessorTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilAssemblyRefProcessorTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilAssemblyRefProcessorTable** objectArray = (SgAsmCilAssemblyRefProcessorTable**) &(SgAsmCilAssemblyRefProcessorTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilAssemblyRefProcessorTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilAssemblyRefProcessorTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyRefProcessorTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilAssemblyRefProcessorTable);

     return memory;
   }



/* #line 167992 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilAssemblyRefTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilAssemblyRefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefTable** objectArray = (SgAsmCilAssemblyRefTable**) &(SgAsmCilAssemblyRefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilAssemblyRefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilAssemblyRefTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilAssemblyRefTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilAssemblyRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyRefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilAssemblyRefTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyRefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefTable** objectArray = (SgAsmCilAssemblyRefTable**) &(SgAsmCilAssemblyRefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyRefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilAssemblyRefTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilAssemblyRefTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilAssemblyRefTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyRefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyRefTable** objectArray = (SgAsmCilAssemblyRefTable**) &(SgAsmCilAssemblyRefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilAssemblyRefTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilAssemblyRefTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilAssemblyRefTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyRefTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilAssemblyRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilAssemblyRefTable** objectArray = (SgAsmCilAssemblyRefTable**) &(SgAsmCilAssemblyRefTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilAssemblyRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilAssemblyRefTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyRefTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilAssemblyRefTable);

     return memory;
   }



/* #line 168224 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilAssemblyTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilAssemblyTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyTable** objectArray = (SgAsmCilAssemblyTable**) &(SgAsmCilAssemblyTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilAssemblyTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilAssemblyTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilAssemblyTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilAssemblyTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilAssemblyTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyTable** objectArray = (SgAsmCilAssemblyTable**) &(SgAsmCilAssemblyTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilAssemblyTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilAssemblyTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilAssemblyTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilAssemblyTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilAssemblyTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilAssemblyTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilAssemblyTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilAssemblyTable** objectArray = (SgAsmCilAssemblyTable**) &(SgAsmCilAssemblyTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilAssemblyTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilAssemblyTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilAssemblyTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilAssemblyTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilAssemblyTable** objectArray = (SgAsmCilAssemblyTable**) &(SgAsmCilAssemblyTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilAssemblyTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilAssemblyTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilAssemblyTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilAssemblyTable);

     return memory;
   }



/* #line 168456 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilClassLayout::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilClassLayout objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilClassLayout::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilClassLayout** objectArray = (SgAsmCilClassLayout**) &(SgAsmCilClassLayout::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilClassLayout::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilClassLayout::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilClassLayout::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilClassLayout*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilClassLayout::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilClassLayout::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilClassLayout::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilClassLayout objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilClassLayout::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilClassLayout** objectArray = (SgAsmCilClassLayout**) &(SgAsmCilClassLayout::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilClassLayout::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilClassLayout::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilClassLayout object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilClassLayout object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilClassLayout::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilClassLayout objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilClassLayout::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilClassLayout** objectArray = (SgAsmCilClassLayout**) &(SgAsmCilClassLayout::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilClassLayout::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilClassLayout::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilClassLayout found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilClassLayout::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilClassLayout::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilClassLayout** objectArray = (SgAsmCilClassLayout**) &(SgAsmCilClassLayout::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilClassLayout::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilClassLayout::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilClassLayout::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilClassLayout);

     return memory;
   }



/* #line 168688 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilClassLayoutTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilClassLayoutTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilClassLayoutTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilClassLayoutTable** objectArray = (SgAsmCilClassLayoutTable**) &(SgAsmCilClassLayoutTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilClassLayoutTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilClassLayoutTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilClassLayoutTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilClassLayoutTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilClassLayoutTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilClassLayoutTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilClassLayoutTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilClassLayoutTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilClassLayoutTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilClassLayoutTable** objectArray = (SgAsmCilClassLayoutTable**) &(SgAsmCilClassLayoutTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilClassLayoutTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilClassLayoutTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilClassLayoutTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilClassLayoutTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilClassLayoutTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilClassLayoutTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilClassLayoutTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilClassLayoutTable** objectArray = (SgAsmCilClassLayoutTable**) &(SgAsmCilClassLayoutTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilClassLayoutTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilClassLayoutTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilClassLayoutTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilClassLayoutTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilClassLayoutTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilClassLayoutTable** objectArray = (SgAsmCilClassLayoutTable**) &(SgAsmCilClassLayoutTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilClassLayoutTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilClassLayoutTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilClassLayoutTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilClassLayoutTable);

     return memory;
   }



/* #line 168920 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilConstant::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilConstant objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilConstant::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilConstant** objectArray = (SgAsmCilConstant**) &(SgAsmCilConstant::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilConstant::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilConstant::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilConstant::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilConstant*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilConstant::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilConstant::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilConstant::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilConstant objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilConstant::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilConstant** objectArray = (SgAsmCilConstant**) &(SgAsmCilConstant::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilConstant::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilConstant::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilConstant object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilConstant object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilConstant::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilConstant objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilConstant::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilConstant** objectArray = (SgAsmCilConstant**) &(SgAsmCilConstant::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilConstant::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilConstant::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilConstant found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilConstant::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilConstant::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilConstant** objectArray = (SgAsmCilConstant**) &(SgAsmCilConstant::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilConstant::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilConstant::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilConstant::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilConstant);

     return memory;
   }



/* #line 169152 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilConstantTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilConstantTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilConstantTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilConstantTable** objectArray = (SgAsmCilConstantTable**) &(SgAsmCilConstantTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilConstantTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilConstantTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilConstantTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilConstantTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilConstantTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilConstantTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilConstantTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilConstantTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilConstantTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilConstantTable** objectArray = (SgAsmCilConstantTable**) &(SgAsmCilConstantTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilConstantTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilConstantTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilConstantTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilConstantTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilConstantTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilConstantTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilConstantTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilConstantTable** objectArray = (SgAsmCilConstantTable**) &(SgAsmCilConstantTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilConstantTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilConstantTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilConstantTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilConstantTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilConstantTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilConstantTable** objectArray = (SgAsmCilConstantTable**) &(SgAsmCilConstantTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilConstantTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilConstantTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilConstantTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilConstantTable);

     return memory;
   }



/* #line 169384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilCustomAttribute::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilCustomAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilCustomAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilCustomAttribute** objectArray = (SgAsmCilCustomAttribute**) &(SgAsmCilCustomAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilCustomAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilCustomAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilCustomAttribute::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilCustomAttribute*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilCustomAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilCustomAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilCustomAttribute::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilCustomAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilCustomAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilCustomAttribute** objectArray = (SgAsmCilCustomAttribute**) &(SgAsmCilCustomAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilCustomAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilCustomAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilCustomAttribute object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilCustomAttribute object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilCustomAttribute::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilCustomAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilCustomAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilCustomAttribute** objectArray = (SgAsmCilCustomAttribute**) &(SgAsmCilCustomAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilCustomAttribute::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilCustomAttribute::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilCustomAttribute found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilCustomAttribute::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilCustomAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilCustomAttribute** objectArray = (SgAsmCilCustomAttribute**) &(SgAsmCilCustomAttribute::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilCustomAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilCustomAttribute::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilCustomAttribute::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilCustomAttribute);

     return memory;
   }



/* #line 169616 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilCustomAttributeTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilCustomAttributeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilCustomAttributeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilCustomAttributeTable** objectArray = (SgAsmCilCustomAttributeTable**) &(SgAsmCilCustomAttributeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilCustomAttributeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilCustomAttributeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilCustomAttributeTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilCustomAttributeTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilCustomAttributeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilCustomAttributeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilCustomAttributeTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilCustomAttributeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilCustomAttributeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilCustomAttributeTable** objectArray = (SgAsmCilCustomAttributeTable**) &(SgAsmCilCustomAttributeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilCustomAttributeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilCustomAttributeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilCustomAttributeTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilCustomAttributeTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilCustomAttributeTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilCustomAttributeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilCustomAttributeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilCustomAttributeTable** objectArray = (SgAsmCilCustomAttributeTable**) &(SgAsmCilCustomAttributeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilCustomAttributeTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilCustomAttributeTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilCustomAttributeTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilCustomAttributeTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilCustomAttributeTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilCustomAttributeTable** objectArray = (SgAsmCilCustomAttributeTable**) &(SgAsmCilCustomAttributeTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilCustomAttributeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilCustomAttributeTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilCustomAttributeTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilCustomAttributeTable);

     return memory;
   }



/* #line 169848 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilDeclSecurity::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilDeclSecurity objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilDeclSecurity::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilDeclSecurity** objectArray = (SgAsmCilDeclSecurity**) &(SgAsmCilDeclSecurity::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilDeclSecurity::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilDeclSecurity::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilDeclSecurity::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilDeclSecurity*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilDeclSecurity::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilDeclSecurity::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilDeclSecurity::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilDeclSecurity objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilDeclSecurity::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilDeclSecurity** objectArray = (SgAsmCilDeclSecurity**) &(SgAsmCilDeclSecurity::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilDeclSecurity::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilDeclSecurity::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilDeclSecurity object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilDeclSecurity object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilDeclSecurity::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilDeclSecurity objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilDeclSecurity::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilDeclSecurity** objectArray = (SgAsmCilDeclSecurity**) &(SgAsmCilDeclSecurity::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilDeclSecurity::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilDeclSecurity::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilDeclSecurity found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilDeclSecurity::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilDeclSecurity::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilDeclSecurity** objectArray = (SgAsmCilDeclSecurity**) &(SgAsmCilDeclSecurity::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilDeclSecurity::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilDeclSecurity::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilDeclSecurity::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilDeclSecurity);

     return memory;
   }



/* #line 170080 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilDeclSecurityTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilDeclSecurityTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilDeclSecurityTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilDeclSecurityTable** objectArray = (SgAsmCilDeclSecurityTable**) &(SgAsmCilDeclSecurityTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilDeclSecurityTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilDeclSecurityTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilDeclSecurityTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilDeclSecurityTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilDeclSecurityTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilDeclSecurityTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilDeclSecurityTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilDeclSecurityTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilDeclSecurityTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilDeclSecurityTable** objectArray = (SgAsmCilDeclSecurityTable**) &(SgAsmCilDeclSecurityTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilDeclSecurityTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilDeclSecurityTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilDeclSecurityTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilDeclSecurityTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilDeclSecurityTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilDeclSecurityTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilDeclSecurityTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilDeclSecurityTable** objectArray = (SgAsmCilDeclSecurityTable**) &(SgAsmCilDeclSecurityTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilDeclSecurityTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilDeclSecurityTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilDeclSecurityTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilDeclSecurityTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilDeclSecurityTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilDeclSecurityTable** objectArray = (SgAsmCilDeclSecurityTable**) &(SgAsmCilDeclSecurityTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilDeclSecurityTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilDeclSecurityTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilDeclSecurityTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilDeclSecurityTable);

     return memory;
   }



/* #line 170312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilEvent::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilEvent objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilEvent::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilEvent** objectArray = (SgAsmCilEvent**) &(SgAsmCilEvent::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilEvent::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilEvent::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilEvent::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilEvent*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilEvent::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilEvent::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilEvent::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilEvent objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilEvent::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilEvent** objectArray = (SgAsmCilEvent**) &(SgAsmCilEvent::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilEvent::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilEvent::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilEvent object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilEvent object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilEvent::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilEvent objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilEvent::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilEvent** objectArray = (SgAsmCilEvent**) &(SgAsmCilEvent::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilEvent::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilEvent::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilEvent found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilEvent::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilEvent::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilEvent** objectArray = (SgAsmCilEvent**) &(SgAsmCilEvent::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilEvent::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilEvent::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilEvent::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilEvent);

     return memory;
   }



/* #line 170544 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilEventMap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilEventMap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilEventMap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilEventMap** objectArray = (SgAsmCilEventMap**) &(SgAsmCilEventMap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilEventMap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilEventMap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilEventMap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilEventMap*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilEventMap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilEventMap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilEventMap::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilEventMap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilEventMap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilEventMap** objectArray = (SgAsmCilEventMap**) &(SgAsmCilEventMap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilEventMap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilEventMap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilEventMap object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilEventMap object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilEventMap::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilEventMap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilEventMap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilEventMap** objectArray = (SgAsmCilEventMap**) &(SgAsmCilEventMap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilEventMap::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilEventMap::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilEventMap found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilEventMap::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilEventMap::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilEventMap** objectArray = (SgAsmCilEventMap**) &(SgAsmCilEventMap::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilEventMap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilEventMap::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilEventMap::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilEventMap);

     return memory;
   }



/* #line 170776 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilEventMapTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilEventMapTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilEventMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilEventMapTable** objectArray = (SgAsmCilEventMapTable**) &(SgAsmCilEventMapTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilEventMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilEventMapTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilEventMapTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilEventMapTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilEventMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilEventMapTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilEventMapTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilEventMapTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilEventMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilEventMapTable** objectArray = (SgAsmCilEventMapTable**) &(SgAsmCilEventMapTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilEventMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilEventMapTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilEventMapTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilEventMapTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilEventMapTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilEventMapTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilEventMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilEventMapTable** objectArray = (SgAsmCilEventMapTable**) &(SgAsmCilEventMapTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilEventMapTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilEventMapTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilEventMapTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilEventMapTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilEventMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilEventMapTable** objectArray = (SgAsmCilEventMapTable**) &(SgAsmCilEventMapTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilEventMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilEventMapTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilEventMapTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilEventMapTable);

     return memory;
   }



/* #line 171008 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilEventTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilEventTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilEventTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilEventTable** objectArray = (SgAsmCilEventTable**) &(SgAsmCilEventTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilEventTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilEventTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilEventTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilEventTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilEventTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilEventTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilEventTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilEventTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilEventTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilEventTable** objectArray = (SgAsmCilEventTable**) &(SgAsmCilEventTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilEventTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilEventTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilEventTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilEventTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilEventTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilEventTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilEventTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilEventTable** objectArray = (SgAsmCilEventTable**) &(SgAsmCilEventTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilEventTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilEventTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilEventTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilEventTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilEventTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilEventTable** objectArray = (SgAsmCilEventTable**) &(SgAsmCilEventTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilEventTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilEventTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilEventTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilEventTable);

     return memory;
   }



/* #line 171240 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilExportedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilExportedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilExportedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilExportedType** objectArray = (SgAsmCilExportedType**) &(SgAsmCilExportedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilExportedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilExportedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilExportedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilExportedType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilExportedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilExportedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilExportedType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilExportedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilExportedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilExportedType** objectArray = (SgAsmCilExportedType**) &(SgAsmCilExportedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilExportedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilExportedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilExportedType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilExportedType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilExportedType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilExportedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilExportedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilExportedType** objectArray = (SgAsmCilExportedType**) &(SgAsmCilExportedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilExportedType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilExportedType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilExportedType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilExportedType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilExportedType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilExportedType** objectArray = (SgAsmCilExportedType**) &(SgAsmCilExportedType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilExportedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilExportedType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilExportedType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilExportedType);

     return memory;
   }



/* #line 171472 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilExportedTypeTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilExportedTypeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilExportedTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilExportedTypeTable** objectArray = (SgAsmCilExportedTypeTable**) &(SgAsmCilExportedTypeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilExportedTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilExportedTypeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilExportedTypeTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilExportedTypeTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilExportedTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilExportedTypeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilExportedTypeTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilExportedTypeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilExportedTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilExportedTypeTable** objectArray = (SgAsmCilExportedTypeTable**) &(SgAsmCilExportedTypeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilExportedTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilExportedTypeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilExportedTypeTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilExportedTypeTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilExportedTypeTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilExportedTypeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilExportedTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilExportedTypeTable** objectArray = (SgAsmCilExportedTypeTable**) &(SgAsmCilExportedTypeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilExportedTypeTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilExportedTypeTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilExportedTypeTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilExportedTypeTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilExportedTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilExportedTypeTable** objectArray = (SgAsmCilExportedTypeTable**) &(SgAsmCilExportedTypeTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilExportedTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilExportedTypeTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilExportedTypeTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilExportedTypeTable);

     return memory;
   }



/* #line 171704 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilField::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilField objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilField::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilField** objectArray = (SgAsmCilField**) &(SgAsmCilField::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilField::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilField::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilField::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilField*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilField::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilField::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilField::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilField objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilField::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilField** objectArray = (SgAsmCilField**) &(SgAsmCilField::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilField::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilField::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilField object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilField object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilField::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilField objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilField::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilField** objectArray = (SgAsmCilField**) &(SgAsmCilField::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilField::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilField::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilField found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilField::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilField::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilField** objectArray = (SgAsmCilField**) &(SgAsmCilField::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilField::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilField::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilField::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilField);

     return memory;
   }



/* #line 171936 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilFieldLayout::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilFieldLayout objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldLayout::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldLayout** objectArray = (SgAsmCilFieldLayout**) &(SgAsmCilFieldLayout::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldLayout::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilFieldLayout::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilFieldLayout::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilFieldLayout*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilFieldLayout::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldLayout::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilFieldLayout::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldLayout objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldLayout::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldLayout** objectArray = (SgAsmCilFieldLayout**) &(SgAsmCilFieldLayout::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldLayout::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldLayout::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilFieldLayout object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilFieldLayout object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilFieldLayout::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldLayout objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldLayout::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldLayout** objectArray = (SgAsmCilFieldLayout**) &(SgAsmCilFieldLayout::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilFieldLayout::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilFieldLayout::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilFieldLayout found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldLayout::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilFieldLayout::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilFieldLayout** objectArray = (SgAsmCilFieldLayout**) &(SgAsmCilFieldLayout::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilFieldLayout::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilFieldLayout::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldLayout::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilFieldLayout);

     return memory;
   }



/* #line 172168 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilFieldLayoutTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilFieldLayoutTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldLayoutTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldLayoutTable** objectArray = (SgAsmCilFieldLayoutTable**) &(SgAsmCilFieldLayoutTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldLayoutTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilFieldLayoutTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilFieldLayoutTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilFieldLayoutTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilFieldLayoutTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldLayoutTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilFieldLayoutTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldLayoutTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldLayoutTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldLayoutTable** objectArray = (SgAsmCilFieldLayoutTable**) &(SgAsmCilFieldLayoutTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldLayoutTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldLayoutTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilFieldLayoutTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilFieldLayoutTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilFieldLayoutTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldLayoutTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldLayoutTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldLayoutTable** objectArray = (SgAsmCilFieldLayoutTable**) &(SgAsmCilFieldLayoutTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilFieldLayoutTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilFieldLayoutTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilFieldLayoutTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldLayoutTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilFieldLayoutTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilFieldLayoutTable** objectArray = (SgAsmCilFieldLayoutTable**) &(SgAsmCilFieldLayoutTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilFieldLayoutTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilFieldLayoutTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldLayoutTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilFieldLayoutTable);

     return memory;
   }



/* #line 172400 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilFieldMarshal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilFieldMarshal objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldMarshal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldMarshal** objectArray = (SgAsmCilFieldMarshal**) &(SgAsmCilFieldMarshal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldMarshal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilFieldMarshal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilFieldMarshal::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilFieldMarshal*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilFieldMarshal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldMarshal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilFieldMarshal::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldMarshal objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldMarshal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldMarshal** objectArray = (SgAsmCilFieldMarshal**) &(SgAsmCilFieldMarshal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldMarshal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldMarshal::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilFieldMarshal object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilFieldMarshal object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilFieldMarshal::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldMarshal objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldMarshal::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldMarshal** objectArray = (SgAsmCilFieldMarshal**) &(SgAsmCilFieldMarshal::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilFieldMarshal::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilFieldMarshal::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilFieldMarshal found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldMarshal::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilFieldMarshal::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilFieldMarshal** objectArray = (SgAsmCilFieldMarshal**) &(SgAsmCilFieldMarshal::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilFieldMarshal::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilFieldMarshal::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldMarshal::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilFieldMarshal);

     return memory;
   }



/* #line 172632 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilFieldMarshalTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilFieldMarshalTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldMarshalTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldMarshalTable** objectArray = (SgAsmCilFieldMarshalTable**) &(SgAsmCilFieldMarshalTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldMarshalTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilFieldMarshalTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilFieldMarshalTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilFieldMarshalTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilFieldMarshalTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldMarshalTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilFieldMarshalTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldMarshalTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldMarshalTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldMarshalTable** objectArray = (SgAsmCilFieldMarshalTable**) &(SgAsmCilFieldMarshalTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldMarshalTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldMarshalTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilFieldMarshalTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilFieldMarshalTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilFieldMarshalTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldMarshalTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldMarshalTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldMarshalTable** objectArray = (SgAsmCilFieldMarshalTable**) &(SgAsmCilFieldMarshalTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilFieldMarshalTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilFieldMarshalTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilFieldMarshalTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldMarshalTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilFieldMarshalTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilFieldMarshalTable** objectArray = (SgAsmCilFieldMarshalTable**) &(SgAsmCilFieldMarshalTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilFieldMarshalTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilFieldMarshalTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldMarshalTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilFieldMarshalTable);

     return memory;
   }



/* #line 172864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilFieldRVA::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilFieldRVA objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldRVA::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldRVA** objectArray = (SgAsmCilFieldRVA**) &(SgAsmCilFieldRVA::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldRVA::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilFieldRVA::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilFieldRVA::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilFieldRVA*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilFieldRVA::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldRVA::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilFieldRVA::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldRVA objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldRVA::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldRVA** objectArray = (SgAsmCilFieldRVA**) &(SgAsmCilFieldRVA::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldRVA::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldRVA::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilFieldRVA object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilFieldRVA object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilFieldRVA::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldRVA objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldRVA::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldRVA** objectArray = (SgAsmCilFieldRVA**) &(SgAsmCilFieldRVA::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilFieldRVA::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilFieldRVA::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilFieldRVA found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldRVA::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilFieldRVA::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilFieldRVA** objectArray = (SgAsmCilFieldRVA**) &(SgAsmCilFieldRVA::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilFieldRVA::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilFieldRVA::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldRVA::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilFieldRVA);

     return memory;
   }



/* #line 173096 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilFieldRVATable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilFieldRVATable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldRVATable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldRVATable** objectArray = (SgAsmCilFieldRVATable**) &(SgAsmCilFieldRVATable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldRVATable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilFieldRVATable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilFieldRVATable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilFieldRVATable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilFieldRVATable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldRVATable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilFieldRVATable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldRVATable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldRVATable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldRVATable** objectArray = (SgAsmCilFieldRVATable**) &(SgAsmCilFieldRVATable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldRVATable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldRVATable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilFieldRVATable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilFieldRVATable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilFieldRVATable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldRVATable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldRVATable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldRVATable** objectArray = (SgAsmCilFieldRVATable**) &(SgAsmCilFieldRVATable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilFieldRVATable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilFieldRVATable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilFieldRVATable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldRVATable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilFieldRVATable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilFieldRVATable** objectArray = (SgAsmCilFieldRVATable**) &(SgAsmCilFieldRVATable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilFieldRVATable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilFieldRVATable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldRVATable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilFieldRVATable);

     return memory;
   }



/* #line 173328 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilFieldTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilFieldTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldTable** objectArray = (SgAsmCilFieldTable**) &(SgAsmCilFieldTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilFieldTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilFieldTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilFieldTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilFieldTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilFieldTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldTable** objectArray = (SgAsmCilFieldTable**) &(SgAsmCilFieldTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFieldTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFieldTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilFieldTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilFieldTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilFieldTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFieldTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFieldTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFieldTable** objectArray = (SgAsmCilFieldTable**) &(SgAsmCilFieldTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilFieldTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilFieldTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilFieldTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilFieldTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilFieldTable** objectArray = (SgAsmCilFieldTable**) &(SgAsmCilFieldTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilFieldTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilFieldTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFieldTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilFieldTable);

     return memory;
   }



/* #line 173560 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilFile::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilFile objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFile** objectArray = (SgAsmCilFile**) &(SgAsmCilFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilFile::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilFile*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilFile::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFile objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFile** objectArray = (SgAsmCilFile**) &(SgAsmCilFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilFile object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilFile object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilFile::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFile objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFile** objectArray = (SgAsmCilFile**) &(SgAsmCilFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilFile::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilFile::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilFile found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFile::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilFile::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilFile** objectArray = (SgAsmCilFile**) &(SgAsmCilFile::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilFile::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFile::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilFile);

     return memory;
   }



/* #line 173792 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilFileTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilFileTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFileTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFileTable** objectArray = (SgAsmCilFileTable**) &(SgAsmCilFileTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFileTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilFileTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilFileTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilFileTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilFileTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFileTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilFileTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFileTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFileTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFileTable** objectArray = (SgAsmCilFileTable**) &(SgAsmCilFileTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilFileTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilFileTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilFileTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilFileTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilFileTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilFileTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilFileTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilFileTable** objectArray = (SgAsmCilFileTable**) &(SgAsmCilFileTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilFileTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilFileTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilFileTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFileTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilFileTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilFileTable** objectArray = (SgAsmCilFileTable**) &(SgAsmCilFileTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilFileTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilFileTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilFileTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilFileTable);

     return memory;
   }



/* #line 174024 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilGenericParamConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilGenericParamConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilGenericParamConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilGenericParamConstraint** objectArray = (SgAsmCilGenericParamConstraint**) &(SgAsmCilGenericParamConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilGenericParamConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilGenericParamConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilGenericParamConstraint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilGenericParamConstraint*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilGenericParamConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilGenericParamConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilGenericParamConstraint::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilGenericParamConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilGenericParamConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilGenericParamConstraint** objectArray = (SgAsmCilGenericParamConstraint**) &(SgAsmCilGenericParamConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilGenericParamConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilGenericParamConstraint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilGenericParamConstraint object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilGenericParamConstraint object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilGenericParamConstraint::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilGenericParamConstraint objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilGenericParamConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilGenericParamConstraint** objectArray = (SgAsmCilGenericParamConstraint**) &(SgAsmCilGenericParamConstraint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilGenericParamConstraint::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilGenericParamConstraint::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilGenericParamConstraint found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilGenericParamConstraint::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilGenericParamConstraint::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilGenericParamConstraint** objectArray = (SgAsmCilGenericParamConstraint**) &(SgAsmCilGenericParamConstraint::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilGenericParamConstraint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilGenericParamConstraint::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilGenericParamConstraint::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilGenericParamConstraint);

     return memory;
   }



/* #line 174256 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilGenericParamConstraintTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilGenericParamConstraintTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilGenericParamConstraintTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilGenericParamConstraintTable** objectArray = (SgAsmCilGenericParamConstraintTable**) &(SgAsmCilGenericParamConstraintTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilGenericParamConstraintTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilGenericParamConstraintTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilGenericParamConstraintTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilGenericParamConstraintTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilGenericParamConstraintTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilGenericParamConstraintTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilGenericParamConstraintTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilGenericParamConstraintTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilGenericParamConstraintTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilGenericParamConstraintTable** objectArray = (SgAsmCilGenericParamConstraintTable**) &(SgAsmCilGenericParamConstraintTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilGenericParamConstraintTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilGenericParamConstraintTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilGenericParamConstraintTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilGenericParamConstraintTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilGenericParamConstraintTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilGenericParamConstraintTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilGenericParamConstraintTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilGenericParamConstraintTable** objectArray = (SgAsmCilGenericParamConstraintTable**) &(SgAsmCilGenericParamConstraintTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilGenericParamConstraintTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilGenericParamConstraintTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilGenericParamConstraintTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilGenericParamConstraintTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilGenericParamConstraintTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilGenericParamConstraintTable** objectArray = (SgAsmCilGenericParamConstraintTable**) &(SgAsmCilGenericParamConstraintTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilGenericParamConstraintTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilGenericParamConstraintTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilGenericParamConstraintTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilGenericParamConstraintTable);

     return memory;
   }



/* #line 174488 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilGenericParam::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilGenericParam objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilGenericParam::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilGenericParam** objectArray = (SgAsmCilGenericParam**) &(SgAsmCilGenericParam::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilGenericParam::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilGenericParam::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilGenericParam::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilGenericParam*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilGenericParam::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilGenericParam::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilGenericParam::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilGenericParam objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilGenericParam::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilGenericParam** objectArray = (SgAsmCilGenericParam**) &(SgAsmCilGenericParam::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilGenericParam::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilGenericParam::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilGenericParam object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilGenericParam object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilGenericParam::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilGenericParam objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilGenericParam::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilGenericParam** objectArray = (SgAsmCilGenericParam**) &(SgAsmCilGenericParam::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilGenericParam::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilGenericParam::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilGenericParam found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilGenericParam::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilGenericParam::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilGenericParam** objectArray = (SgAsmCilGenericParam**) &(SgAsmCilGenericParam::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilGenericParam::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilGenericParam::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilGenericParam::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilGenericParam);

     return memory;
   }



/* #line 174720 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilGenericParamTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilGenericParamTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilGenericParamTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilGenericParamTable** objectArray = (SgAsmCilGenericParamTable**) &(SgAsmCilGenericParamTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilGenericParamTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilGenericParamTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilGenericParamTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilGenericParamTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilGenericParamTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilGenericParamTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilGenericParamTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilGenericParamTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilGenericParamTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilGenericParamTable** objectArray = (SgAsmCilGenericParamTable**) &(SgAsmCilGenericParamTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilGenericParamTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilGenericParamTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilGenericParamTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilGenericParamTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilGenericParamTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilGenericParamTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilGenericParamTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilGenericParamTable** objectArray = (SgAsmCilGenericParamTable**) &(SgAsmCilGenericParamTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilGenericParamTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilGenericParamTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilGenericParamTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilGenericParamTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilGenericParamTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilGenericParamTable** objectArray = (SgAsmCilGenericParamTable**) &(SgAsmCilGenericParamTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilGenericParamTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilGenericParamTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilGenericParamTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilGenericParamTable);

     return memory;
   }



/* #line 174952 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilImplMap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilImplMap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilImplMap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilImplMap** objectArray = (SgAsmCilImplMap**) &(SgAsmCilImplMap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilImplMap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilImplMap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilImplMap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilImplMap*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilImplMap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilImplMap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilImplMap::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilImplMap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilImplMap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilImplMap** objectArray = (SgAsmCilImplMap**) &(SgAsmCilImplMap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilImplMap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilImplMap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilImplMap object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilImplMap object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilImplMap::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilImplMap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilImplMap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilImplMap** objectArray = (SgAsmCilImplMap**) &(SgAsmCilImplMap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilImplMap::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilImplMap::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilImplMap found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilImplMap::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilImplMap::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilImplMap** objectArray = (SgAsmCilImplMap**) &(SgAsmCilImplMap::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilImplMap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilImplMap::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilImplMap::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilImplMap);

     return memory;
   }



/* #line 175184 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilImplMapTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilImplMapTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilImplMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilImplMapTable** objectArray = (SgAsmCilImplMapTable**) &(SgAsmCilImplMapTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilImplMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilImplMapTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilImplMapTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilImplMapTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilImplMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilImplMapTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilImplMapTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilImplMapTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilImplMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilImplMapTable** objectArray = (SgAsmCilImplMapTable**) &(SgAsmCilImplMapTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilImplMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilImplMapTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilImplMapTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilImplMapTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilImplMapTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilImplMapTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilImplMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilImplMapTable** objectArray = (SgAsmCilImplMapTable**) &(SgAsmCilImplMapTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilImplMapTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilImplMapTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilImplMapTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilImplMapTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilImplMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilImplMapTable** objectArray = (SgAsmCilImplMapTable**) &(SgAsmCilImplMapTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilImplMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilImplMapTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilImplMapTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilImplMapTable);

     return memory;
   }



/* #line 175416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilInterfaceImpl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilInterfaceImpl objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilInterfaceImpl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilInterfaceImpl** objectArray = (SgAsmCilInterfaceImpl**) &(SgAsmCilInterfaceImpl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilInterfaceImpl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilInterfaceImpl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilInterfaceImpl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilInterfaceImpl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilInterfaceImpl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilInterfaceImpl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilInterfaceImpl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilInterfaceImpl objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilInterfaceImpl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilInterfaceImpl** objectArray = (SgAsmCilInterfaceImpl**) &(SgAsmCilInterfaceImpl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilInterfaceImpl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilInterfaceImpl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilInterfaceImpl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilInterfaceImpl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilInterfaceImpl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilInterfaceImpl objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilInterfaceImpl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilInterfaceImpl** objectArray = (SgAsmCilInterfaceImpl**) &(SgAsmCilInterfaceImpl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilInterfaceImpl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilInterfaceImpl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilInterfaceImpl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilInterfaceImpl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilInterfaceImpl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilInterfaceImpl** objectArray = (SgAsmCilInterfaceImpl**) &(SgAsmCilInterfaceImpl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilInterfaceImpl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilInterfaceImpl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilInterfaceImpl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilInterfaceImpl);

     return memory;
   }



/* #line 175648 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilInterfaceImplTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilInterfaceImplTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilInterfaceImplTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilInterfaceImplTable** objectArray = (SgAsmCilInterfaceImplTable**) &(SgAsmCilInterfaceImplTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilInterfaceImplTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilInterfaceImplTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilInterfaceImplTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilInterfaceImplTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilInterfaceImplTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilInterfaceImplTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilInterfaceImplTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilInterfaceImplTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilInterfaceImplTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilInterfaceImplTable** objectArray = (SgAsmCilInterfaceImplTable**) &(SgAsmCilInterfaceImplTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilInterfaceImplTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilInterfaceImplTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilInterfaceImplTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilInterfaceImplTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilInterfaceImplTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilInterfaceImplTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilInterfaceImplTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilInterfaceImplTable** objectArray = (SgAsmCilInterfaceImplTable**) &(SgAsmCilInterfaceImplTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilInterfaceImplTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilInterfaceImplTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilInterfaceImplTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilInterfaceImplTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilInterfaceImplTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilInterfaceImplTable** objectArray = (SgAsmCilInterfaceImplTable**) &(SgAsmCilInterfaceImplTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilInterfaceImplTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilInterfaceImplTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilInterfaceImplTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilInterfaceImplTable);

     return memory;
   }



/* #line 175880 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilManifestResource::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilManifestResource objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilManifestResource::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilManifestResource** objectArray = (SgAsmCilManifestResource**) &(SgAsmCilManifestResource::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilManifestResource::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilManifestResource::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilManifestResource::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilManifestResource*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilManifestResource::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilManifestResource::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilManifestResource::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilManifestResource objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilManifestResource::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilManifestResource** objectArray = (SgAsmCilManifestResource**) &(SgAsmCilManifestResource::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilManifestResource::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilManifestResource::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilManifestResource object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilManifestResource object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilManifestResource::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilManifestResource objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilManifestResource::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilManifestResource** objectArray = (SgAsmCilManifestResource**) &(SgAsmCilManifestResource::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilManifestResource::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilManifestResource::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilManifestResource found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilManifestResource::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilManifestResource::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilManifestResource** objectArray = (SgAsmCilManifestResource**) &(SgAsmCilManifestResource::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilManifestResource::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilManifestResource::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilManifestResource::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilManifestResource);

     return memory;
   }



/* #line 176112 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilManifestResourceTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilManifestResourceTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilManifestResourceTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilManifestResourceTable** objectArray = (SgAsmCilManifestResourceTable**) &(SgAsmCilManifestResourceTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilManifestResourceTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilManifestResourceTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilManifestResourceTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilManifestResourceTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilManifestResourceTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilManifestResourceTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilManifestResourceTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilManifestResourceTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilManifestResourceTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilManifestResourceTable** objectArray = (SgAsmCilManifestResourceTable**) &(SgAsmCilManifestResourceTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilManifestResourceTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilManifestResourceTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilManifestResourceTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilManifestResourceTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilManifestResourceTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilManifestResourceTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilManifestResourceTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilManifestResourceTable** objectArray = (SgAsmCilManifestResourceTable**) &(SgAsmCilManifestResourceTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilManifestResourceTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilManifestResourceTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilManifestResourceTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilManifestResourceTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilManifestResourceTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilManifestResourceTable** objectArray = (SgAsmCilManifestResourceTable**) &(SgAsmCilManifestResourceTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilManifestResourceTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilManifestResourceTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilManifestResourceTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilManifestResourceTable);

     return memory;
   }



/* #line 176344 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMemberRef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMemberRef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMemberRef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMemberRef** objectArray = (SgAsmCilMemberRef**) &(SgAsmCilMemberRef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMemberRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMemberRef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMemberRef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMemberRef*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMemberRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMemberRef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMemberRef::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMemberRef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMemberRef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMemberRef** objectArray = (SgAsmCilMemberRef**) &(SgAsmCilMemberRef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMemberRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMemberRef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMemberRef object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMemberRef object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMemberRef::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMemberRef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMemberRef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMemberRef** objectArray = (SgAsmCilMemberRef**) &(SgAsmCilMemberRef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMemberRef::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMemberRef::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMemberRef found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMemberRef::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMemberRef::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMemberRef** objectArray = (SgAsmCilMemberRef**) &(SgAsmCilMemberRef::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMemberRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMemberRef::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMemberRef::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMemberRef);

     return memory;
   }



/* #line 176576 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMemberRefTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMemberRefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMemberRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMemberRefTable** objectArray = (SgAsmCilMemberRefTable**) &(SgAsmCilMemberRefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMemberRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMemberRefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMemberRefTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMemberRefTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMemberRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMemberRefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMemberRefTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMemberRefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMemberRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMemberRefTable** objectArray = (SgAsmCilMemberRefTable**) &(SgAsmCilMemberRefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMemberRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMemberRefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMemberRefTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMemberRefTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMemberRefTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMemberRefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMemberRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMemberRefTable** objectArray = (SgAsmCilMemberRefTable**) &(SgAsmCilMemberRefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMemberRefTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMemberRefTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMemberRefTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMemberRefTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMemberRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMemberRefTable** objectArray = (SgAsmCilMemberRefTable**) &(SgAsmCilMemberRefTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMemberRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMemberRefTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMemberRefTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMemberRefTable);

     return memory;
   }



/* #line 176808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMethodDef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMethodDef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodDef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodDef** objectArray = (SgAsmCilMethodDef**) &(SgAsmCilMethodDef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodDef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMethodDef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMethodDef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMethodDef*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMethodDef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodDef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMethodDef::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodDef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodDef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodDef** objectArray = (SgAsmCilMethodDef**) &(SgAsmCilMethodDef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodDef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodDef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMethodDef object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMethodDef object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMethodDef::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodDef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodDef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodDef** objectArray = (SgAsmCilMethodDef**) &(SgAsmCilMethodDef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMethodDef::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMethodDef::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMethodDef found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodDef::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMethodDef::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMethodDef** objectArray = (SgAsmCilMethodDef**) &(SgAsmCilMethodDef::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMethodDef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMethodDef::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodDef::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMethodDef);

     return memory;
   }



/* #line 177040 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMethodDefTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMethodDefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodDefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodDefTable** objectArray = (SgAsmCilMethodDefTable**) &(SgAsmCilMethodDefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodDefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMethodDefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMethodDefTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMethodDefTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMethodDefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodDefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMethodDefTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodDefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodDefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodDefTable** objectArray = (SgAsmCilMethodDefTable**) &(SgAsmCilMethodDefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodDefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodDefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMethodDefTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMethodDefTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMethodDefTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodDefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodDefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodDefTable** objectArray = (SgAsmCilMethodDefTable**) &(SgAsmCilMethodDefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMethodDefTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMethodDefTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMethodDefTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodDefTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMethodDefTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMethodDefTable** objectArray = (SgAsmCilMethodDefTable**) &(SgAsmCilMethodDefTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMethodDefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMethodDefTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodDefTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMethodDefTable);

     return memory;
   }



/* #line 177272 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMethodImpl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMethodImpl objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodImpl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodImpl** objectArray = (SgAsmCilMethodImpl**) &(SgAsmCilMethodImpl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodImpl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMethodImpl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMethodImpl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMethodImpl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMethodImpl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodImpl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMethodImpl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodImpl objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodImpl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodImpl** objectArray = (SgAsmCilMethodImpl**) &(SgAsmCilMethodImpl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodImpl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodImpl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMethodImpl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMethodImpl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMethodImpl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodImpl objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodImpl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodImpl** objectArray = (SgAsmCilMethodImpl**) &(SgAsmCilMethodImpl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMethodImpl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMethodImpl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMethodImpl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodImpl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMethodImpl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMethodImpl** objectArray = (SgAsmCilMethodImpl**) &(SgAsmCilMethodImpl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMethodImpl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMethodImpl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodImpl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMethodImpl);

     return memory;
   }



/* #line 177504 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMethodImplTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMethodImplTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodImplTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodImplTable** objectArray = (SgAsmCilMethodImplTable**) &(SgAsmCilMethodImplTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodImplTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMethodImplTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMethodImplTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMethodImplTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMethodImplTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodImplTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMethodImplTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodImplTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodImplTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodImplTable** objectArray = (SgAsmCilMethodImplTable**) &(SgAsmCilMethodImplTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodImplTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodImplTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMethodImplTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMethodImplTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMethodImplTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodImplTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodImplTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodImplTable** objectArray = (SgAsmCilMethodImplTable**) &(SgAsmCilMethodImplTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMethodImplTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMethodImplTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMethodImplTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodImplTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMethodImplTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMethodImplTable** objectArray = (SgAsmCilMethodImplTable**) &(SgAsmCilMethodImplTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMethodImplTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMethodImplTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodImplTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMethodImplTable);

     return memory;
   }



/* #line 177736 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMethodSemantics::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMethodSemantics objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodSemantics::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodSemantics** objectArray = (SgAsmCilMethodSemantics**) &(SgAsmCilMethodSemantics::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodSemantics::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMethodSemantics::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMethodSemantics::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMethodSemantics*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMethodSemantics::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodSemantics::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMethodSemantics::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodSemantics objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodSemantics::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodSemantics** objectArray = (SgAsmCilMethodSemantics**) &(SgAsmCilMethodSemantics::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodSemantics::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodSemantics::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMethodSemantics object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMethodSemantics object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMethodSemantics::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodSemantics objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodSemantics::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodSemantics** objectArray = (SgAsmCilMethodSemantics**) &(SgAsmCilMethodSemantics::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMethodSemantics::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMethodSemantics::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMethodSemantics found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodSemantics::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMethodSemantics::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMethodSemantics** objectArray = (SgAsmCilMethodSemantics**) &(SgAsmCilMethodSemantics::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMethodSemantics::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMethodSemantics::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodSemantics::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMethodSemantics);

     return memory;
   }



/* #line 177968 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMethodSemanticsTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMethodSemanticsTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodSemanticsTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodSemanticsTable** objectArray = (SgAsmCilMethodSemanticsTable**) &(SgAsmCilMethodSemanticsTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodSemanticsTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMethodSemanticsTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMethodSemanticsTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMethodSemanticsTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMethodSemanticsTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodSemanticsTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMethodSemanticsTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodSemanticsTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodSemanticsTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodSemanticsTable** objectArray = (SgAsmCilMethodSemanticsTable**) &(SgAsmCilMethodSemanticsTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodSemanticsTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodSemanticsTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMethodSemanticsTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMethodSemanticsTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMethodSemanticsTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodSemanticsTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodSemanticsTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodSemanticsTable** objectArray = (SgAsmCilMethodSemanticsTable**) &(SgAsmCilMethodSemanticsTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMethodSemanticsTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMethodSemanticsTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMethodSemanticsTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodSemanticsTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMethodSemanticsTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMethodSemanticsTable** objectArray = (SgAsmCilMethodSemanticsTable**) &(SgAsmCilMethodSemanticsTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMethodSemanticsTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMethodSemanticsTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodSemanticsTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMethodSemanticsTable);

     return memory;
   }



/* #line 178200 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMethodSpec::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMethodSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodSpec** objectArray = (SgAsmCilMethodSpec**) &(SgAsmCilMethodSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMethodSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMethodSpec::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMethodSpec*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMethodSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMethodSpec::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodSpec** objectArray = (SgAsmCilMethodSpec**) &(SgAsmCilMethodSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMethodSpec object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMethodSpec object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMethodSpec::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodSpec** objectArray = (SgAsmCilMethodSpec**) &(SgAsmCilMethodSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMethodSpec::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMethodSpec::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMethodSpec found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodSpec::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMethodSpec::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMethodSpec** objectArray = (SgAsmCilMethodSpec**) &(SgAsmCilMethodSpec::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMethodSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMethodSpec::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodSpec::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMethodSpec);

     return memory;
   }



/* #line 178432 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMethodSpecTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMethodSpecTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodSpecTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodSpecTable** objectArray = (SgAsmCilMethodSpecTable**) &(SgAsmCilMethodSpecTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodSpecTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMethodSpecTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMethodSpecTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMethodSpecTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMethodSpecTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodSpecTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMethodSpecTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodSpecTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodSpecTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodSpecTable** objectArray = (SgAsmCilMethodSpecTable**) &(SgAsmCilMethodSpecTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodSpecTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodSpecTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMethodSpecTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMethodSpecTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMethodSpecTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodSpecTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodSpecTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodSpecTable** objectArray = (SgAsmCilMethodSpecTable**) &(SgAsmCilMethodSpecTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMethodSpecTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMethodSpecTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMethodSpecTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodSpecTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMethodSpecTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMethodSpecTable** objectArray = (SgAsmCilMethodSpecTable**) &(SgAsmCilMethodSpecTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMethodSpecTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMethodSpecTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodSpecTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMethodSpecTable);

     return memory;
   }



/* #line 178664 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilModule::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilModule objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilModule::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilModule** objectArray = (SgAsmCilModule**) &(SgAsmCilModule::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilModule::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilModule::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilModule*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilModule::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilModule::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilModule objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilModule::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilModule** objectArray = (SgAsmCilModule**) &(SgAsmCilModule::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilModule::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilModule object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilModule object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilModule::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilModule objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilModule::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilModule** objectArray = (SgAsmCilModule**) &(SgAsmCilModule::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilModule::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilModule::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilModule found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilModule::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilModule::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilModule** objectArray = (SgAsmCilModule**) &(SgAsmCilModule::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilModule::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilModule::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilModule);

     return memory;
   }



/* #line 178896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilModuleRef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilModuleRef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilModuleRef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilModuleRef** objectArray = (SgAsmCilModuleRef**) &(SgAsmCilModuleRef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilModuleRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilModuleRef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilModuleRef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilModuleRef*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilModuleRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilModuleRef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilModuleRef::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilModuleRef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilModuleRef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilModuleRef** objectArray = (SgAsmCilModuleRef**) &(SgAsmCilModuleRef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilModuleRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilModuleRef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilModuleRef object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilModuleRef object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilModuleRef::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilModuleRef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilModuleRef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilModuleRef** objectArray = (SgAsmCilModuleRef**) &(SgAsmCilModuleRef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilModuleRef::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilModuleRef::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilModuleRef found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilModuleRef::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilModuleRef::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilModuleRef** objectArray = (SgAsmCilModuleRef**) &(SgAsmCilModuleRef::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilModuleRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilModuleRef::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilModuleRef::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilModuleRef);

     return memory;
   }



/* #line 179128 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilModuleRefTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilModuleRefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilModuleRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilModuleRefTable** objectArray = (SgAsmCilModuleRefTable**) &(SgAsmCilModuleRefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilModuleRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilModuleRefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilModuleRefTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilModuleRefTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilModuleRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilModuleRefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilModuleRefTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilModuleRefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilModuleRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilModuleRefTable** objectArray = (SgAsmCilModuleRefTable**) &(SgAsmCilModuleRefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilModuleRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilModuleRefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilModuleRefTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilModuleRefTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilModuleRefTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilModuleRefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilModuleRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilModuleRefTable** objectArray = (SgAsmCilModuleRefTable**) &(SgAsmCilModuleRefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilModuleRefTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilModuleRefTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilModuleRefTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilModuleRefTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilModuleRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilModuleRefTable** objectArray = (SgAsmCilModuleRefTable**) &(SgAsmCilModuleRefTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilModuleRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilModuleRefTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilModuleRefTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilModuleRefTable);

     return memory;
   }



/* #line 179360 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilModuleTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilModuleTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilModuleTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilModuleTable** objectArray = (SgAsmCilModuleTable**) &(SgAsmCilModuleTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilModuleTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilModuleTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilModuleTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilModuleTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilModuleTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilModuleTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilModuleTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilModuleTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilModuleTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilModuleTable** objectArray = (SgAsmCilModuleTable**) &(SgAsmCilModuleTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilModuleTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilModuleTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilModuleTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilModuleTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilModuleTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilModuleTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilModuleTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilModuleTable** objectArray = (SgAsmCilModuleTable**) &(SgAsmCilModuleTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilModuleTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilModuleTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilModuleTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilModuleTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilModuleTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilModuleTable** objectArray = (SgAsmCilModuleTable**) &(SgAsmCilModuleTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilModuleTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilModuleTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilModuleTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilModuleTable);

     return memory;
   }



/* #line 179592 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilNestedClass::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilNestedClass objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilNestedClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilNestedClass** objectArray = (SgAsmCilNestedClass**) &(SgAsmCilNestedClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilNestedClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilNestedClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilNestedClass::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilNestedClass*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilNestedClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilNestedClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilNestedClass::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilNestedClass objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilNestedClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilNestedClass** objectArray = (SgAsmCilNestedClass**) &(SgAsmCilNestedClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilNestedClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilNestedClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilNestedClass object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilNestedClass object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilNestedClass::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilNestedClass objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilNestedClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilNestedClass** objectArray = (SgAsmCilNestedClass**) &(SgAsmCilNestedClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilNestedClass::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilNestedClass::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilNestedClass found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilNestedClass::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilNestedClass::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilNestedClass** objectArray = (SgAsmCilNestedClass**) &(SgAsmCilNestedClass::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilNestedClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilNestedClass::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilNestedClass::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilNestedClass);

     return memory;
   }



/* #line 179824 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilNestedClassTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilNestedClassTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilNestedClassTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilNestedClassTable** objectArray = (SgAsmCilNestedClassTable**) &(SgAsmCilNestedClassTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilNestedClassTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilNestedClassTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilNestedClassTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilNestedClassTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilNestedClassTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilNestedClassTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilNestedClassTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilNestedClassTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilNestedClassTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilNestedClassTable** objectArray = (SgAsmCilNestedClassTable**) &(SgAsmCilNestedClassTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilNestedClassTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilNestedClassTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilNestedClassTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilNestedClassTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilNestedClassTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilNestedClassTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilNestedClassTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilNestedClassTable** objectArray = (SgAsmCilNestedClassTable**) &(SgAsmCilNestedClassTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilNestedClassTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilNestedClassTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilNestedClassTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilNestedClassTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilNestedClassTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilNestedClassTable** objectArray = (SgAsmCilNestedClassTable**) &(SgAsmCilNestedClassTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilNestedClassTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilNestedClassTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilNestedClassTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilNestedClassTable);

     return memory;
   }



/* #line 180056 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilParam::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilParam objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilParam::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilParam** objectArray = (SgAsmCilParam**) &(SgAsmCilParam::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilParam::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilParam::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilParam::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilParam*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilParam::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilParam::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilParam::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilParam objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilParam::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilParam** objectArray = (SgAsmCilParam**) &(SgAsmCilParam::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilParam::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilParam::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilParam object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilParam object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilParam::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilParam objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilParam::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilParam** objectArray = (SgAsmCilParam**) &(SgAsmCilParam::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilParam::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilParam::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilParam found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilParam::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilParam::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilParam** objectArray = (SgAsmCilParam**) &(SgAsmCilParam::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilParam::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilParam::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilParam::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilParam);

     return memory;
   }



/* #line 180288 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilParamTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilParamTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilParamTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilParamTable** objectArray = (SgAsmCilParamTable**) &(SgAsmCilParamTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilParamTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilParamTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilParamTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilParamTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilParamTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilParamTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilParamTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilParamTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilParamTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilParamTable** objectArray = (SgAsmCilParamTable**) &(SgAsmCilParamTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilParamTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilParamTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilParamTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilParamTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilParamTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilParamTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilParamTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilParamTable** objectArray = (SgAsmCilParamTable**) &(SgAsmCilParamTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilParamTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilParamTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilParamTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilParamTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilParamTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilParamTable** objectArray = (SgAsmCilParamTable**) &(SgAsmCilParamTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilParamTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilParamTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilParamTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilParamTable);

     return memory;
   }



/* #line 180520 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilProperty::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilProperty objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilProperty::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilProperty** objectArray = (SgAsmCilProperty**) &(SgAsmCilProperty::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilProperty::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilProperty::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilProperty::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilProperty*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilProperty::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilProperty::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilProperty::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilProperty objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilProperty::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilProperty** objectArray = (SgAsmCilProperty**) &(SgAsmCilProperty::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilProperty::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilProperty::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilProperty object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilProperty object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilProperty::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilProperty objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilProperty::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilProperty** objectArray = (SgAsmCilProperty**) &(SgAsmCilProperty::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilProperty::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilProperty::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilProperty found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilProperty::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilProperty::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilProperty** objectArray = (SgAsmCilProperty**) &(SgAsmCilProperty::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilProperty::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilProperty::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilProperty::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilProperty);

     return memory;
   }



/* #line 180752 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilPropertyMap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilPropertyMap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilPropertyMap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilPropertyMap** objectArray = (SgAsmCilPropertyMap**) &(SgAsmCilPropertyMap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilPropertyMap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilPropertyMap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilPropertyMap::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilPropertyMap*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilPropertyMap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilPropertyMap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilPropertyMap::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilPropertyMap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilPropertyMap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilPropertyMap** objectArray = (SgAsmCilPropertyMap**) &(SgAsmCilPropertyMap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilPropertyMap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilPropertyMap::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilPropertyMap object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilPropertyMap object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilPropertyMap::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilPropertyMap objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilPropertyMap::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilPropertyMap** objectArray = (SgAsmCilPropertyMap**) &(SgAsmCilPropertyMap::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilPropertyMap::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilPropertyMap::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilPropertyMap found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilPropertyMap::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilPropertyMap::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilPropertyMap** objectArray = (SgAsmCilPropertyMap**) &(SgAsmCilPropertyMap::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilPropertyMap::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilPropertyMap::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilPropertyMap::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilPropertyMap);

     return memory;
   }



/* #line 180984 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilPropertyMapTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilPropertyMapTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilPropertyMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilPropertyMapTable** objectArray = (SgAsmCilPropertyMapTable**) &(SgAsmCilPropertyMapTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilPropertyMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilPropertyMapTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilPropertyMapTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilPropertyMapTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilPropertyMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilPropertyMapTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilPropertyMapTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilPropertyMapTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilPropertyMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilPropertyMapTable** objectArray = (SgAsmCilPropertyMapTable**) &(SgAsmCilPropertyMapTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilPropertyMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilPropertyMapTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilPropertyMapTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilPropertyMapTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilPropertyMapTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilPropertyMapTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilPropertyMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilPropertyMapTable** objectArray = (SgAsmCilPropertyMapTable**) &(SgAsmCilPropertyMapTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilPropertyMapTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilPropertyMapTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilPropertyMapTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilPropertyMapTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilPropertyMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilPropertyMapTable** objectArray = (SgAsmCilPropertyMapTable**) &(SgAsmCilPropertyMapTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilPropertyMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilPropertyMapTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilPropertyMapTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilPropertyMapTable);

     return memory;
   }



/* #line 181216 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilPropertyTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilPropertyTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilPropertyTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilPropertyTable** objectArray = (SgAsmCilPropertyTable**) &(SgAsmCilPropertyTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilPropertyTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilPropertyTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilPropertyTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilPropertyTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilPropertyTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilPropertyTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilPropertyTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilPropertyTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilPropertyTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilPropertyTable** objectArray = (SgAsmCilPropertyTable**) &(SgAsmCilPropertyTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilPropertyTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilPropertyTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilPropertyTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilPropertyTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilPropertyTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilPropertyTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilPropertyTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilPropertyTable** objectArray = (SgAsmCilPropertyTable**) &(SgAsmCilPropertyTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilPropertyTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilPropertyTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilPropertyTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilPropertyTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilPropertyTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilPropertyTable** objectArray = (SgAsmCilPropertyTable**) &(SgAsmCilPropertyTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilPropertyTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilPropertyTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilPropertyTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilPropertyTable);

     return memory;
   }



/* #line 181448 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilStandAloneSig::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilStandAloneSig objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilStandAloneSig::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilStandAloneSig** objectArray = (SgAsmCilStandAloneSig**) &(SgAsmCilStandAloneSig::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilStandAloneSig::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilStandAloneSig::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilStandAloneSig::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilStandAloneSig*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilStandAloneSig::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilStandAloneSig::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilStandAloneSig::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilStandAloneSig objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilStandAloneSig::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilStandAloneSig** objectArray = (SgAsmCilStandAloneSig**) &(SgAsmCilStandAloneSig::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilStandAloneSig::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilStandAloneSig::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilStandAloneSig object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilStandAloneSig object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilStandAloneSig::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilStandAloneSig objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilStandAloneSig::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilStandAloneSig** objectArray = (SgAsmCilStandAloneSig**) &(SgAsmCilStandAloneSig::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilStandAloneSig::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilStandAloneSig::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilStandAloneSig found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilStandAloneSig::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilStandAloneSig::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilStandAloneSig** objectArray = (SgAsmCilStandAloneSig**) &(SgAsmCilStandAloneSig::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilStandAloneSig::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilStandAloneSig::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilStandAloneSig::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilStandAloneSig);

     return memory;
   }



/* #line 181680 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilStandAloneSigTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilStandAloneSigTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilStandAloneSigTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilStandAloneSigTable** objectArray = (SgAsmCilStandAloneSigTable**) &(SgAsmCilStandAloneSigTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilStandAloneSigTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilStandAloneSigTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilStandAloneSigTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilStandAloneSigTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilStandAloneSigTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilStandAloneSigTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilStandAloneSigTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilStandAloneSigTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilStandAloneSigTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilStandAloneSigTable** objectArray = (SgAsmCilStandAloneSigTable**) &(SgAsmCilStandAloneSigTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilStandAloneSigTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilStandAloneSigTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilStandAloneSigTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilStandAloneSigTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilStandAloneSigTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilStandAloneSigTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilStandAloneSigTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilStandAloneSigTable** objectArray = (SgAsmCilStandAloneSigTable**) &(SgAsmCilStandAloneSigTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilStandAloneSigTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilStandAloneSigTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilStandAloneSigTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilStandAloneSigTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilStandAloneSigTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilStandAloneSigTable** objectArray = (SgAsmCilStandAloneSigTable**) &(SgAsmCilStandAloneSigTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilStandAloneSigTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilStandAloneSigTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilStandAloneSigTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilStandAloneSigTable);

     return memory;
   }



/* #line 181912 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilTypeDef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilTypeDef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeDef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeDef** objectArray = (SgAsmCilTypeDef**) &(SgAsmCilTypeDef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilTypeDef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilTypeDef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilTypeDef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilTypeDef*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilTypeDef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilTypeDef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilTypeDef::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilTypeDef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeDef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeDef** objectArray = (SgAsmCilTypeDef**) &(SgAsmCilTypeDef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilTypeDef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilTypeDef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilTypeDef object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilTypeDef object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilTypeDef::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilTypeDef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeDef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeDef** objectArray = (SgAsmCilTypeDef**) &(SgAsmCilTypeDef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilTypeDef::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilTypeDef::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilTypeDef found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilTypeDef::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilTypeDef::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilTypeDef** objectArray = (SgAsmCilTypeDef**) &(SgAsmCilTypeDef::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilTypeDef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilTypeDef::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilTypeDef::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilTypeDef);

     return memory;
   }



/* #line 182144 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilTypeDefTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilTypeDefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeDefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeDefTable** objectArray = (SgAsmCilTypeDefTable**) &(SgAsmCilTypeDefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilTypeDefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilTypeDefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilTypeDefTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilTypeDefTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilTypeDefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilTypeDefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilTypeDefTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilTypeDefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeDefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeDefTable** objectArray = (SgAsmCilTypeDefTable**) &(SgAsmCilTypeDefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilTypeDefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilTypeDefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilTypeDefTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilTypeDefTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilTypeDefTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilTypeDefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeDefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeDefTable** objectArray = (SgAsmCilTypeDefTable**) &(SgAsmCilTypeDefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilTypeDefTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilTypeDefTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilTypeDefTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilTypeDefTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilTypeDefTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilTypeDefTable** objectArray = (SgAsmCilTypeDefTable**) &(SgAsmCilTypeDefTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilTypeDefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilTypeDefTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilTypeDefTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilTypeDefTable);

     return memory;
   }



/* #line 182376 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilTypeRef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilTypeRef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeRef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeRef** objectArray = (SgAsmCilTypeRef**) &(SgAsmCilTypeRef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilTypeRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilTypeRef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilTypeRef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilTypeRef*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilTypeRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilTypeRef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilTypeRef::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilTypeRef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeRef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeRef** objectArray = (SgAsmCilTypeRef**) &(SgAsmCilTypeRef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilTypeRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilTypeRef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilTypeRef object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilTypeRef object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilTypeRef::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilTypeRef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeRef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeRef** objectArray = (SgAsmCilTypeRef**) &(SgAsmCilTypeRef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilTypeRef::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilTypeRef::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilTypeRef found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilTypeRef::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilTypeRef::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilTypeRef** objectArray = (SgAsmCilTypeRef**) &(SgAsmCilTypeRef::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilTypeRef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilTypeRef::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilTypeRef::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilTypeRef);

     return memory;
   }



/* #line 182608 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilTypeRefTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilTypeRefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeRefTable** objectArray = (SgAsmCilTypeRefTable**) &(SgAsmCilTypeRefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilTypeRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilTypeRefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilTypeRefTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilTypeRefTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilTypeRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilTypeRefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilTypeRefTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilTypeRefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeRefTable** objectArray = (SgAsmCilTypeRefTable**) &(SgAsmCilTypeRefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilTypeRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilTypeRefTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilTypeRefTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilTypeRefTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilTypeRefTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilTypeRefTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeRefTable** objectArray = (SgAsmCilTypeRefTable**) &(SgAsmCilTypeRefTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilTypeRefTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilTypeRefTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilTypeRefTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilTypeRefTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilTypeRefTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilTypeRefTable** objectArray = (SgAsmCilTypeRefTable**) &(SgAsmCilTypeRefTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilTypeRefTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilTypeRefTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilTypeRefTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilTypeRefTable);

     return memory;
   }



/* #line 182840 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilTypeSpec::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilTypeSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeSpec** objectArray = (SgAsmCilTypeSpec**) &(SgAsmCilTypeSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilTypeSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilTypeSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilTypeSpec::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilTypeSpec*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilTypeSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilTypeSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilTypeSpec::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilTypeSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeSpec** objectArray = (SgAsmCilTypeSpec**) &(SgAsmCilTypeSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilTypeSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilTypeSpec::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilTypeSpec object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilTypeSpec object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilTypeSpec::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilTypeSpec objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeSpec::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeSpec** objectArray = (SgAsmCilTypeSpec**) &(SgAsmCilTypeSpec::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilTypeSpec::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilTypeSpec::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilTypeSpec found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilTypeSpec::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilTypeSpec::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilTypeSpec** objectArray = (SgAsmCilTypeSpec**) &(SgAsmCilTypeSpec::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilTypeSpec::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilTypeSpec::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilTypeSpec::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilTypeSpec);

     return memory;
   }



/* #line 183072 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilTypeSpecTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilTypeSpecTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeSpecTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeSpecTable** objectArray = (SgAsmCilTypeSpecTable**) &(SgAsmCilTypeSpecTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilTypeSpecTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilTypeSpecTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilTypeSpecTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilTypeSpecTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilTypeSpecTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilTypeSpecTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilTypeSpecTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilTypeSpecTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeSpecTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeSpecTable** objectArray = (SgAsmCilTypeSpecTable**) &(SgAsmCilTypeSpecTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilTypeSpecTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilTypeSpecTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilTypeSpecTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilTypeSpecTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilTypeSpecTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilTypeSpecTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilTypeSpecTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilTypeSpecTable** objectArray = (SgAsmCilTypeSpecTable**) &(SgAsmCilTypeSpecTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilTypeSpecTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilTypeSpecTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilTypeSpecTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilTypeSpecTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilTypeSpecTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilTypeSpecTable** objectArray = (SgAsmCilTypeSpecTable**) &(SgAsmCilTypeSpecTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilTypeSpecTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilTypeSpecTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilTypeSpecTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilTypeSpecTable);

     return memory;
   }



/* #line 183304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMetadataRoot::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMetadataRoot objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMetadataRoot::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMetadataRoot** objectArray = (SgAsmCilMetadataRoot**) &(SgAsmCilMetadataRoot::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMetadataRoot::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMetadataRoot::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMetadataRoot::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMetadataRoot*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMetadataRoot::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMetadataRoot::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMetadataRoot::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMetadataRoot objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMetadataRoot::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMetadataRoot** objectArray = (SgAsmCilMetadataRoot**) &(SgAsmCilMetadataRoot::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMetadataRoot::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMetadataRoot::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMetadataRoot object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMetadataRoot object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMetadataRoot::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMetadataRoot objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMetadataRoot::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMetadataRoot** objectArray = (SgAsmCilMetadataRoot**) &(SgAsmCilMetadataRoot::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMetadataRoot::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMetadataRoot::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMetadataRoot found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMetadataRoot::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMetadataRoot::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMetadataRoot** objectArray = (SgAsmCilMetadataRoot**) &(SgAsmCilMetadataRoot::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMetadataRoot::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMetadataRoot::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMetadataRoot::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMetadataRoot);

     return memory;
   }



/* #line 183536 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilMethodData::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilMethodData objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodData::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodData** objectArray = (SgAsmCilMethodData**) &(SgAsmCilMethodData::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodData::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilMethodData::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilMethodData::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilMethodData*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilMethodData::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodData::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilMethodData::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodData objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodData::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodData** objectArray = (SgAsmCilMethodData**) &(SgAsmCilMethodData::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilMethodData::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilMethodData::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilMethodData object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilMethodData object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilMethodData::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilMethodData objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilMethodData::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilMethodData** objectArray = (SgAsmCilMethodData**) &(SgAsmCilMethodData::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilMethodData::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilMethodData::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilMethodData found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodData::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilMethodData::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilMethodData** objectArray = (SgAsmCilMethodData**) &(SgAsmCilMethodData::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilMethodData::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilMethodData::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilMethodData::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilMethodData);

     return memory;
   }



/* #line 183768 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmExecutableFileFormat::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmExecutableFileFormat objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmExecutableFileFormat::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmExecutableFileFormat** objectArray = (SgAsmExecutableFileFormat**) &(SgAsmExecutableFileFormat::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmExecutableFileFormat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmExecutableFileFormat::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmExecutableFileFormat::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmExecutableFileFormat*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmExecutableFileFormat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmExecutableFileFormat::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmExecutableFileFormat::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmExecutableFileFormat objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmExecutableFileFormat::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmExecutableFileFormat** objectArray = (SgAsmExecutableFileFormat**) &(SgAsmExecutableFileFormat::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmExecutableFileFormat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmExecutableFileFormat::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmExecutableFileFormat object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmExecutableFileFormat object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmExecutableFileFormat::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmExecutableFileFormat objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmExecutableFileFormat::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmExecutableFileFormat** objectArray = (SgAsmExecutableFileFormat**) &(SgAsmExecutableFileFormat::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmExecutableFileFormat::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmExecutableFileFormat::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmExecutableFileFormat found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmExecutableFileFormat::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmExecutableFileFormat::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmExecutableFileFormat** objectArray = (SgAsmExecutableFileFormat**) &(SgAsmExecutableFileFormat::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmExecutableFileFormat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmExecutableFileFormat::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmExecutableFileFormat::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmExecutableFileFormat);

     return memory;
   }



/* #line 184000 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCoffSymbolList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCoffSymbolList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCoffSymbolList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCoffSymbolList** objectArray = (SgAsmCoffSymbolList**) &(SgAsmCoffSymbolList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCoffSymbolList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCoffSymbolList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCoffSymbolList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCoffSymbolList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCoffSymbolList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCoffSymbolList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCoffSymbolList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCoffSymbolList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCoffSymbolList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCoffSymbolList** objectArray = (SgAsmCoffSymbolList**) &(SgAsmCoffSymbolList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCoffSymbolList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCoffSymbolList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCoffSymbolList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCoffSymbolList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCoffSymbolList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCoffSymbolList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCoffSymbolList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCoffSymbolList** objectArray = (SgAsmCoffSymbolList**) &(SgAsmCoffSymbolList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCoffSymbolList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCoffSymbolList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCoffSymbolList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCoffSymbolList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCoffSymbolList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCoffSymbolList** objectArray = (SgAsmCoffSymbolList**) &(SgAsmCoffSymbolList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCoffSymbolList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCoffSymbolList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCoffSymbolList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCoffSymbolList);

     return memory;
   }



/* #line 184232 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfInformation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfInformation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfInformation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfInformation** objectArray = (SgAsmDwarfInformation**) &(SgAsmDwarfInformation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfInformation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfInformation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfInformation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfInformation*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfInformation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfInformation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfInformation::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfInformation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfInformation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfInformation** objectArray = (SgAsmDwarfInformation**) &(SgAsmDwarfInformation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfInformation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfInformation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfInformation object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfInformation object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfInformation::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfInformation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfInformation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfInformation** objectArray = (SgAsmDwarfInformation**) &(SgAsmDwarfInformation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfInformation::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfInformation::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfInformation found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfInformation::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfInformation::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfInformation** objectArray = (SgAsmDwarfInformation**) &(SgAsmDwarfInformation::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfInformation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfInformation::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfInformation::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfInformation);

     return memory;
   }



/* #line 184464 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfCompilationUnitList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfCompilationUnitList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCompilationUnitList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCompilationUnitList** objectArray = (SgAsmDwarfCompilationUnitList**) &(SgAsmDwarfCompilationUnitList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfCompilationUnitList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfCompilationUnitList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfCompilationUnitList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfCompilationUnitList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfCompilationUnitList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfCompilationUnitList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfCompilationUnitList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfCompilationUnitList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCompilationUnitList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCompilationUnitList** objectArray = (SgAsmDwarfCompilationUnitList**) &(SgAsmDwarfCompilationUnitList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfCompilationUnitList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfCompilationUnitList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfCompilationUnitList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfCompilationUnitList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfCompilationUnitList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfCompilationUnitList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCompilationUnitList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCompilationUnitList** objectArray = (SgAsmDwarfCompilationUnitList**) &(SgAsmDwarfCompilationUnitList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfCompilationUnitList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfCompilationUnitList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfCompilationUnitList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfCompilationUnitList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfCompilationUnitList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfCompilationUnitList** objectArray = (SgAsmDwarfCompilationUnitList**) &(SgAsmDwarfCompilationUnitList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfCompilationUnitList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfCompilationUnitList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfCompilationUnitList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfCompilationUnitList);

     return memory;
   }



/* #line 184696 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfConstruct::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfConstruct objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfConstruct::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfConstruct** objectArray = (SgAsmDwarfConstruct**) &(SgAsmDwarfConstruct::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfConstruct::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfConstruct::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfConstruct::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfConstruct*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfConstruct::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfConstruct::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfConstruct::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfConstruct objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfConstruct::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfConstruct** objectArray = (SgAsmDwarfConstruct**) &(SgAsmDwarfConstruct::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfConstruct::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfConstruct::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfConstruct object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfConstruct object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfConstruct::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfConstruct objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfConstruct::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfConstruct** objectArray = (SgAsmDwarfConstruct**) &(SgAsmDwarfConstruct::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfConstruct::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfConstruct::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfConstruct found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfConstruct::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfConstruct::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfConstruct** objectArray = (SgAsmDwarfConstruct**) &(SgAsmDwarfConstruct::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfConstruct::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfConstruct::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfConstruct::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfConstruct);

     return memory;
   }



/* #line 184928 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfAccessDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfAccessDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfAccessDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfAccessDeclaration** objectArray = (SgAsmDwarfAccessDeclaration**) &(SgAsmDwarfAccessDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfAccessDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfAccessDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfAccessDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfAccessDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfAccessDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfAccessDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfAccessDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfAccessDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfAccessDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfAccessDeclaration** objectArray = (SgAsmDwarfAccessDeclaration**) &(SgAsmDwarfAccessDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfAccessDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfAccessDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfAccessDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfAccessDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfAccessDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfAccessDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfAccessDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfAccessDeclaration** objectArray = (SgAsmDwarfAccessDeclaration**) &(SgAsmDwarfAccessDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfAccessDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfAccessDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfAccessDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfAccessDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfAccessDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfAccessDeclaration** objectArray = (SgAsmDwarfAccessDeclaration**) &(SgAsmDwarfAccessDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfAccessDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfAccessDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfAccessDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfAccessDeclaration);

     return memory;
   }



/* #line 185160 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfArrayType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfArrayType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfArrayType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfArrayType** objectArray = (SgAsmDwarfArrayType**) &(SgAsmDwarfArrayType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfArrayType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfArrayType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfArrayType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfArrayType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfArrayType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfArrayType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfArrayType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfArrayType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfArrayType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfArrayType** objectArray = (SgAsmDwarfArrayType**) &(SgAsmDwarfArrayType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfArrayType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfArrayType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfArrayType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfArrayType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfArrayType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfArrayType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfArrayType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfArrayType** objectArray = (SgAsmDwarfArrayType**) &(SgAsmDwarfArrayType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfArrayType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfArrayType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfArrayType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfArrayType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfArrayType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfArrayType** objectArray = (SgAsmDwarfArrayType**) &(SgAsmDwarfArrayType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfArrayType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfArrayType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfArrayType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfArrayType);

     return memory;
   }



/* #line 185392 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfBaseType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfBaseType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfBaseType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfBaseType** objectArray = (SgAsmDwarfBaseType**) &(SgAsmDwarfBaseType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfBaseType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfBaseType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfBaseType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfBaseType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfBaseType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfBaseType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfBaseType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfBaseType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfBaseType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfBaseType** objectArray = (SgAsmDwarfBaseType**) &(SgAsmDwarfBaseType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfBaseType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfBaseType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfBaseType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfBaseType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfBaseType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfBaseType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfBaseType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfBaseType** objectArray = (SgAsmDwarfBaseType**) &(SgAsmDwarfBaseType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfBaseType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfBaseType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfBaseType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfBaseType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfBaseType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfBaseType** objectArray = (SgAsmDwarfBaseType**) &(SgAsmDwarfBaseType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfBaseType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfBaseType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfBaseType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfBaseType);

     return memory;
   }



/* #line 185624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfCatchBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfCatchBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCatchBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCatchBlock** objectArray = (SgAsmDwarfCatchBlock**) &(SgAsmDwarfCatchBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfCatchBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfCatchBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfCatchBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfCatchBlock*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfCatchBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfCatchBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfCatchBlock::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfCatchBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCatchBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCatchBlock** objectArray = (SgAsmDwarfCatchBlock**) &(SgAsmDwarfCatchBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfCatchBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfCatchBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfCatchBlock object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfCatchBlock object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfCatchBlock::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfCatchBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCatchBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCatchBlock** objectArray = (SgAsmDwarfCatchBlock**) &(SgAsmDwarfCatchBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfCatchBlock::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfCatchBlock::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfCatchBlock found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfCatchBlock::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfCatchBlock::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfCatchBlock** objectArray = (SgAsmDwarfCatchBlock**) &(SgAsmDwarfCatchBlock::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfCatchBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfCatchBlock::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfCatchBlock::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfCatchBlock);

     return memory;
   }



/* #line 185856 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfClassTemplate::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfClassTemplate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfClassTemplate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfClassTemplate** objectArray = (SgAsmDwarfClassTemplate**) &(SgAsmDwarfClassTemplate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfClassTemplate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfClassTemplate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfClassTemplate::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfClassTemplate*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfClassTemplate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfClassTemplate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfClassTemplate::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfClassTemplate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfClassTemplate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfClassTemplate** objectArray = (SgAsmDwarfClassTemplate**) &(SgAsmDwarfClassTemplate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfClassTemplate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfClassTemplate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfClassTemplate object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfClassTemplate object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfClassTemplate::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfClassTemplate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfClassTemplate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfClassTemplate** objectArray = (SgAsmDwarfClassTemplate**) &(SgAsmDwarfClassTemplate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfClassTemplate::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfClassTemplate::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfClassTemplate found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfClassTemplate::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfClassTemplate::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfClassTemplate** objectArray = (SgAsmDwarfClassTemplate**) &(SgAsmDwarfClassTemplate::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfClassTemplate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfClassTemplate::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfClassTemplate::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfClassTemplate);

     return memory;
   }



/* #line 186088 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfClassType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfClassType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfClassType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfClassType** objectArray = (SgAsmDwarfClassType**) &(SgAsmDwarfClassType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfClassType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfClassType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfClassType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfClassType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfClassType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfClassType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfClassType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfClassType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfClassType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfClassType** objectArray = (SgAsmDwarfClassType**) &(SgAsmDwarfClassType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfClassType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfClassType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfClassType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfClassType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfClassType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfClassType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfClassType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfClassType** objectArray = (SgAsmDwarfClassType**) &(SgAsmDwarfClassType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfClassType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfClassType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfClassType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfClassType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfClassType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfClassType** objectArray = (SgAsmDwarfClassType**) &(SgAsmDwarfClassType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfClassType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfClassType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfClassType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfClassType);

     return memory;
   }



/* #line 186320 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfCommonBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfCommonBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCommonBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCommonBlock** objectArray = (SgAsmDwarfCommonBlock**) &(SgAsmDwarfCommonBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfCommonBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfCommonBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfCommonBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfCommonBlock*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfCommonBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfCommonBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfCommonBlock::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfCommonBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCommonBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCommonBlock** objectArray = (SgAsmDwarfCommonBlock**) &(SgAsmDwarfCommonBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfCommonBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfCommonBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfCommonBlock object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfCommonBlock object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfCommonBlock::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfCommonBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCommonBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCommonBlock** objectArray = (SgAsmDwarfCommonBlock**) &(SgAsmDwarfCommonBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfCommonBlock::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfCommonBlock::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfCommonBlock found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfCommonBlock::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfCommonBlock::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfCommonBlock** objectArray = (SgAsmDwarfCommonBlock**) &(SgAsmDwarfCommonBlock::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfCommonBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfCommonBlock::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfCommonBlock::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfCommonBlock);

     return memory;
   }



/* #line 186552 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfCommonInclusion::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfCommonInclusion objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCommonInclusion::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCommonInclusion** objectArray = (SgAsmDwarfCommonInclusion**) &(SgAsmDwarfCommonInclusion::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfCommonInclusion::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfCommonInclusion::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfCommonInclusion::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfCommonInclusion*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfCommonInclusion::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfCommonInclusion::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfCommonInclusion::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfCommonInclusion objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCommonInclusion::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCommonInclusion** objectArray = (SgAsmDwarfCommonInclusion**) &(SgAsmDwarfCommonInclusion::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfCommonInclusion::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfCommonInclusion::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfCommonInclusion object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfCommonInclusion object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfCommonInclusion::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfCommonInclusion objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCommonInclusion::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCommonInclusion** objectArray = (SgAsmDwarfCommonInclusion**) &(SgAsmDwarfCommonInclusion::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfCommonInclusion::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfCommonInclusion::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfCommonInclusion found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfCommonInclusion::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfCommonInclusion::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfCommonInclusion** objectArray = (SgAsmDwarfCommonInclusion**) &(SgAsmDwarfCommonInclusion::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfCommonInclusion::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfCommonInclusion::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfCommonInclusion::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfCommonInclusion);

     return memory;
   }



/* #line 186784 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfCompilationUnit::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfCompilationUnit objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCompilationUnit::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCompilationUnit** objectArray = (SgAsmDwarfCompilationUnit**) &(SgAsmDwarfCompilationUnit::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfCompilationUnit::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfCompilationUnit::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfCompilationUnit::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfCompilationUnit*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfCompilationUnit::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfCompilationUnit::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfCompilationUnit::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfCompilationUnit objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCompilationUnit::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCompilationUnit** objectArray = (SgAsmDwarfCompilationUnit**) &(SgAsmDwarfCompilationUnit::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfCompilationUnit::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfCompilationUnit::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfCompilationUnit object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfCompilationUnit object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfCompilationUnit::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfCompilationUnit objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCompilationUnit::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCompilationUnit** objectArray = (SgAsmDwarfCompilationUnit**) &(SgAsmDwarfCompilationUnit::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfCompilationUnit::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfCompilationUnit::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfCompilationUnit found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfCompilationUnit::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfCompilationUnit::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfCompilationUnit** objectArray = (SgAsmDwarfCompilationUnit**) &(SgAsmDwarfCompilationUnit::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfCompilationUnit::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfCompilationUnit::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfCompilationUnit::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfCompilationUnit);

     return memory;
   }



/* #line 187016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfCondition::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfCondition objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCondition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCondition** objectArray = (SgAsmDwarfCondition**) &(SgAsmDwarfCondition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfCondition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfCondition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfCondition::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfCondition*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfCondition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfCondition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfCondition::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfCondition objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCondition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCondition** objectArray = (SgAsmDwarfCondition**) &(SgAsmDwarfCondition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfCondition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfCondition::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfCondition object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfCondition object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfCondition::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfCondition objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfCondition::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfCondition** objectArray = (SgAsmDwarfCondition**) &(SgAsmDwarfCondition::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfCondition::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfCondition::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfCondition found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfCondition::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfCondition::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfCondition** objectArray = (SgAsmDwarfCondition**) &(SgAsmDwarfCondition::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfCondition::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfCondition::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfCondition::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfCondition);

     return memory;
   }



/* #line 187248 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfConstant::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfConstant objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfConstant::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfConstant** objectArray = (SgAsmDwarfConstant**) &(SgAsmDwarfConstant::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfConstant::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfConstant::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfConstant::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfConstant*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfConstant::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfConstant::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfConstant::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfConstant objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfConstant::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfConstant** objectArray = (SgAsmDwarfConstant**) &(SgAsmDwarfConstant::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfConstant::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfConstant::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfConstant object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfConstant object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfConstant::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfConstant objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfConstant::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfConstant** objectArray = (SgAsmDwarfConstant**) &(SgAsmDwarfConstant::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfConstant::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfConstant::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfConstant found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfConstant::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfConstant::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfConstant** objectArray = (SgAsmDwarfConstant**) &(SgAsmDwarfConstant::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfConstant::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfConstant::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfConstant::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfConstant);

     return memory;
   }



/* #line 187480 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfConstType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfConstType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfConstType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfConstType** objectArray = (SgAsmDwarfConstType**) &(SgAsmDwarfConstType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfConstType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfConstType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfConstType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfConstType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfConstType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfConstType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfConstType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfConstType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfConstType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfConstType** objectArray = (SgAsmDwarfConstType**) &(SgAsmDwarfConstType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfConstType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfConstType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfConstType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfConstType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfConstType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfConstType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfConstType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfConstType** objectArray = (SgAsmDwarfConstType**) &(SgAsmDwarfConstType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfConstType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfConstType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfConstType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfConstType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfConstType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfConstType** objectArray = (SgAsmDwarfConstType**) &(SgAsmDwarfConstType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfConstType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfConstType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfConstType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfConstType);

     return memory;
   }



/* #line 187712 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfDwarfProcedure::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfDwarfProcedure objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfDwarfProcedure::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfDwarfProcedure** objectArray = (SgAsmDwarfDwarfProcedure**) &(SgAsmDwarfDwarfProcedure::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfDwarfProcedure::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfDwarfProcedure::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfDwarfProcedure::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfDwarfProcedure*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfDwarfProcedure::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfDwarfProcedure::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfDwarfProcedure::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfDwarfProcedure objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfDwarfProcedure::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfDwarfProcedure** objectArray = (SgAsmDwarfDwarfProcedure**) &(SgAsmDwarfDwarfProcedure::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfDwarfProcedure::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfDwarfProcedure::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfDwarfProcedure object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfDwarfProcedure object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfDwarfProcedure::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfDwarfProcedure objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfDwarfProcedure::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfDwarfProcedure** objectArray = (SgAsmDwarfDwarfProcedure**) &(SgAsmDwarfDwarfProcedure::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfDwarfProcedure::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfDwarfProcedure::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfDwarfProcedure found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfDwarfProcedure::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfDwarfProcedure::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfDwarfProcedure** objectArray = (SgAsmDwarfDwarfProcedure**) &(SgAsmDwarfDwarfProcedure::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfDwarfProcedure::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfDwarfProcedure::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfDwarfProcedure::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfDwarfProcedure);

     return memory;
   }



/* #line 187944 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfEntryPoint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfEntryPoint objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfEntryPoint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfEntryPoint** objectArray = (SgAsmDwarfEntryPoint**) &(SgAsmDwarfEntryPoint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfEntryPoint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfEntryPoint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfEntryPoint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfEntryPoint*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfEntryPoint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfEntryPoint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfEntryPoint::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfEntryPoint objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfEntryPoint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfEntryPoint** objectArray = (SgAsmDwarfEntryPoint**) &(SgAsmDwarfEntryPoint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfEntryPoint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfEntryPoint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfEntryPoint object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfEntryPoint object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfEntryPoint::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfEntryPoint objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfEntryPoint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfEntryPoint** objectArray = (SgAsmDwarfEntryPoint**) &(SgAsmDwarfEntryPoint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfEntryPoint::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfEntryPoint::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfEntryPoint found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfEntryPoint::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfEntryPoint::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfEntryPoint** objectArray = (SgAsmDwarfEntryPoint**) &(SgAsmDwarfEntryPoint::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfEntryPoint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfEntryPoint::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfEntryPoint::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfEntryPoint);

     return memory;
   }



/* #line 188176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfEnumerationType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfEnumerationType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfEnumerationType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfEnumerationType** objectArray = (SgAsmDwarfEnumerationType**) &(SgAsmDwarfEnumerationType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfEnumerationType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfEnumerationType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfEnumerationType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfEnumerationType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfEnumerationType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfEnumerationType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfEnumerationType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfEnumerationType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfEnumerationType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfEnumerationType** objectArray = (SgAsmDwarfEnumerationType**) &(SgAsmDwarfEnumerationType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfEnumerationType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfEnumerationType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfEnumerationType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfEnumerationType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfEnumerationType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfEnumerationType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfEnumerationType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfEnumerationType** objectArray = (SgAsmDwarfEnumerationType**) &(SgAsmDwarfEnumerationType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfEnumerationType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfEnumerationType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfEnumerationType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfEnumerationType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfEnumerationType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfEnumerationType** objectArray = (SgAsmDwarfEnumerationType**) &(SgAsmDwarfEnumerationType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfEnumerationType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfEnumerationType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfEnumerationType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfEnumerationType);

     return memory;
   }



/* #line 188408 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfEnumerator::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfEnumerator objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfEnumerator::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfEnumerator** objectArray = (SgAsmDwarfEnumerator**) &(SgAsmDwarfEnumerator::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfEnumerator::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfEnumerator::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfEnumerator::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfEnumerator*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfEnumerator::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfEnumerator::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfEnumerator::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfEnumerator objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfEnumerator::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfEnumerator** objectArray = (SgAsmDwarfEnumerator**) &(SgAsmDwarfEnumerator::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfEnumerator::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfEnumerator::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfEnumerator object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfEnumerator object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfEnumerator::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfEnumerator objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfEnumerator::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfEnumerator** objectArray = (SgAsmDwarfEnumerator**) &(SgAsmDwarfEnumerator::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfEnumerator::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfEnumerator::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfEnumerator found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfEnumerator::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfEnumerator::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfEnumerator** objectArray = (SgAsmDwarfEnumerator**) &(SgAsmDwarfEnumerator::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfEnumerator::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfEnumerator::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfEnumerator::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfEnumerator);

     return memory;
   }



/* #line 188640 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfFileType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfFileType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFileType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFileType** objectArray = (SgAsmDwarfFileType**) &(SgAsmDwarfFileType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfFileType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfFileType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfFileType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfFileType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfFileType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfFileType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfFileType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfFileType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFileType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFileType** objectArray = (SgAsmDwarfFileType**) &(SgAsmDwarfFileType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfFileType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfFileType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfFileType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfFileType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfFileType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfFileType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFileType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFileType** objectArray = (SgAsmDwarfFileType**) &(SgAsmDwarfFileType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfFileType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfFileType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfFileType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfFileType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfFileType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfFileType** objectArray = (SgAsmDwarfFileType**) &(SgAsmDwarfFileType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfFileType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfFileType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfFileType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfFileType);

     return memory;
   }



/* #line 188872 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfFormalParameter::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfFormalParameter objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFormalParameter::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFormalParameter** objectArray = (SgAsmDwarfFormalParameter**) &(SgAsmDwarfFormalParameter::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfFormalParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfFormalParameter::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfFormalParameter::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfFormalParameter*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfFormalParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfFormalParameter::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfFormalParameter::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfFormalParameter objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFormalParameter::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFormalParameter** objectArray = (SgAsmDwarfFormalParameter**) &(SgAsmDwarfFormalParameter::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfFormalParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfFormalParameter::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfFormalParameter object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfFormalParameter object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfFormalParameter::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfFormalParameter objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFormalParameter::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFormalParameter** objectArray = (SgAsmDwarfFormalParameter**) &(SgAsmDwarfFormalParameter::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfFormalParameter::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfFormalParameter::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfFormalParameter found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfFormalParameter::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfFormalParameter::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfFormalParameter** objectArray = (SgAsmDwarfFormalParameter**) &(SgAsmDwarfFormalParameter::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfFormalParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfFormalParameter::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfFormalParameter::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfFormalParameter);

     return memory;
   }



/* #line 189104 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfFormatLabel::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfFormatLabel objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFormatLabel::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFormatLabel** objectArray = (SgAsmDwarfFormatLabel**) &(SgAsmDwarfFormatLabel::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfFormatLabel::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfFormatLabel::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfFormatLabel::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfFormatLabel*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfFormatLabel::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfFormatLabel::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfFormatLabel::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfFormatLabel objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFormatLabel::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFormatLabel** objectArray = (SgAsmDwarfFormatLabel**) &(SgAsmDwarfFormatLabel::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfFormatLabel::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfFormatLabel::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfFormatLabel object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfFormatLabel object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfFormatLabel::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfFormatLabel objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFormatLabel::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFormatLabel** objectArray = (SgAsmDwarfFormatLabel**) &(SgAsmDwarfFormatLabel::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfFormatLabel::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfFormatLabel::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfFormatLabel found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfFormatLabel::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfFormatLabel::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfFormatLabel** objectArray = (SgAsmDwarfFormatLabel**) &(SgAsmDwarfFormatLabel::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfFormatLabel::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfFormatLabel::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfFormatLabel::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfFormatLabel);

     return memory;
   }



/* #line 189336 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfFriend::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfFriend objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFriend::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFriend** objectArray = (SgAsmDwarfFriend**) &(SgAsmDwarfFriend::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfFriend::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfFriend::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfFriend::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfFriend*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfFriend::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfFriend::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfFriend::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfFriend objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFriend::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFriend** objectArray = (SgAsmDwarfFriend**) &(SgAsmDwarfFriend::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfFriend::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfFriend::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfFriend object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfFriend object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfFriend::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfFriend objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFriend::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFriend** objectArray = (SgAsmDwarfFriend**) &(SgAsmDwarfFriend::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfFriend::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfFriend::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfFriend found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfFriend::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfFriend::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfFriend** objectArray = (SgAsmDwarfFriend**) &(SgAsmDwarfFriend::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfFriend::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfFriend::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfFriend::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfFriend);

     return memory;
   }



/* #line 189568 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfFunctionTemplate::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfFunctionTemplate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFunctionTemplate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFunctionTemplate** objectArray = (SgAsmDwarfFunctionTemplate**) &(SgAsmDwarfFunctionTemplate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfFunctionTemplate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfFunctionTemplate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfFunctionTemplate::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfFunctionTemplate*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfFunctionTemplate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfFunctionTemplate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfFunctionTemplate::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfFunctionTemplate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFunctionTemplate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFunctionTemplate** objectArray = (SgAsmDwarfFunctionTemplate**) &(SgAsmDwarfFunctionTemplate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfFunctionTemplate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfFunctionTemplate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfFunctionTemplate object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfFunctionTemplate object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfFunctionTemplate::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfFunctionTemplate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfFunctionTemplate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfFunctionTemplate** objectArray = (SgAsmDwarfFunctionTemplate**) &(SgAsmDwarfFunctionTemplate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfFunctionTemplate::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfFunctionTemplate::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfFunctionTemplate found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfFunctionTemplate::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfFunctionTemplate::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfFunctionTemplate** objectArray = (SgAsmDwarfFunctionTemplate**) &(SgAsmDwarfFunctionTemplate::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfFunctionTemplate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfFunctionTemplate::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfFunctionTemplate::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfFunctionTemplate);

     return memory;
   }



/* #line 189800 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfImportedDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfImportedDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfImportedDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfImportedDeclaration** objectArray = (SgAsmDwarfImportedDeclaration**) &(SgAsmDwarfImportedDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfImportedDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfImportedDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfImportedDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfImportedDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfImportedDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfImportedDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfImportedDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfImportedDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfImportedDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfImportedDeclaration** objectArray = (SgAsmDwarfImportedDeclaration**) &(SgAsmDwarfImportedDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfImportedDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfImportedDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfImportedDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfImportedDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfImportedDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfImportedDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfImportedDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfImportedDeclaration** objectArray = (SgAsmDwarfImportedDeclaration**) &(SgAsmDwarfImportedDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfImportedDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfImportedDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfImportedDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfImportedDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfImportedDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfImportedDeclaration** objectArray = (SgAsmDwarfImportedDeclaration**) &(SgAsmDwarfImportedDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfImportedDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfImportedDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfImportedDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfImportedDeclaration);

     return memory;
   }



/* #line 190032 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfImportedModule::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfImportedModule objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfImportedModule::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfImportedModule** objectArray = (SgAsmDwarfImportedModule**) &(SgAsmDwarfImportedModule::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfImportedModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfImportedModule::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfImportedModule::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfImportedModule*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfImportedModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfImportedModule::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfImportedModule::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfImportedModule objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfImportedModule::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfImportedModule** objectArray = (SgAsmDwarfImportedModule**) &(SgAsmDwarfImportedModule::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfImportedModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfImportedModule::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfImportedModule object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfImportedModule object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfImportedModule::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfImportedModule objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfImportedModule::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfImportedModule** objectArray = (SgAsmDwarfImportedModule**) &(SgAsmDwarfImportedModule::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfImportedModule::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfImportedModule::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfImportedModule found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfImportedModule::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfImportedModule::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfImportedModule** objectArray = (SgAsmDwarfImportedModule**) &(SgAsmDwarfImportedModule::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfImportedModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfImportedModule::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfImportedModule::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfImportedModule);

     return memory;
   }



/* #line 190264 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfImportedUnit::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfImportedUnit objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfImportedUnit::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfImportedUnit** objectArray = (SgAsmDwarfImportedUnit**) &(SgAsmDwarfImportedUnit::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfImportedUnit::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfImportedUnit::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfImportedUnit::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfImportedUnit*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfImportedUnit::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfImportedUnit::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfImportedUnit::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfImportedUnit objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfImportedUnit::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfImportedUnit** objectArray = (SgAsmDwarfImportedUnit**) &(SgAsmDwarfImportedUnit::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfImportedUnit::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfImportedUnit::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfImportedUnit object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfImportedUnit object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfImportedUnit::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfImportedUnit objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfImportedUnit::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfImportedUnit** objectArray = (SgAsmDwarfImportedUnit**) &(SgAsmDwarfImportedUnit::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfImportedUnit::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfImportedUnit::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfImportedUnit found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfImportedUnit::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfImportedUnit::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfImportedUnit** objectArray = (SgAsmDwarfImportedUnit**) &(SgAsmDwarfImportedUnit::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfImportedUnit::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfImportedUnit::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfImportedUnit::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfImportedUnit);

     return memory;
   }



/* #line 190496 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfInheritance::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfInheritance objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfInheritance::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfInheritance** objectArray = (SgAsmDwarfInheritance**) &(SgAsmDwarfInheritance::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfInheritance::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfInheritance::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfInheritance::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfInheritance*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfInheritance::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfInheritance::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfInheritance::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfInheritance objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfInheritance::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfInheritance** objectArray = (SgAsmDwarfInheritance**) &(SgAsmDwarfInheritance::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfInheritance::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfInheritance::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfInheritance object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfInheritance object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfInheritance::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfInheritance objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfInheritance::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfInheritance** objectArray = (SgAsmDwarfInheritance**) &(SgAsmDwarfInheritance::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfInheritance::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfInheritance::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfInheritance found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfInheritance::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfInheritance::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfInheritance** objectArray = (SgAsmDwarfInheritance**) &(SgAsmDwarfInheritance::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfInheritance::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfInheritance::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfInheritance::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfInheritance);

     return memory;
   }



/* #line 190728 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfInlinedSubroutine::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfInlinedSubroutine objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfInlinedSubroutine::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfInlinedSubroutine** objectArray = (SgAsmDwarfInlinedSubroutine**) &(SgAsmDwarfInlinedSubroutine::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfInlinedSubroutine::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfInlinedSubroutine::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfInlinedSubroutine::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfInlinedSubroutine*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfInlinedSubroutine::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfInlinedSubroutine::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfInlinedSubroutine::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfInlinedSubroutine objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfInlinedSubroutine::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfInlinedSubroutine** objectArray = (SgAsmDwarfInlinedSubroutine**) &(SgAsmDwarfInlinedSubroutine::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfInlinedSubroutine::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfInlinedSubroutine::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfInlinedSubroutine object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfInlinedSubroutine object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfInlinedSubroutine::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfInlinedSubroutine objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfInlinedSubroutine::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfInlinedSubroutine** objectArray = (SgAsmDwarfInlinedSubroutine**) &(SgAsmDwarfInlinedSubroutine::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfInlinedSubroutine::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfInlinedSubroutine::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfInlinedSubroutine found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfInlinedSubroutine::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfInlinedSubroutine::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfInlinedSubroutine** objectArray = (SgAsmDwarfInlinedSubroutine**) &(SgAsmDwarfInlinedSubroutine::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfInlinedSubroutine::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfInlinedSubroutine::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfInlinedSubroutine::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfInlinedSubroutine);

     return memory;
   }



/* #line 190960 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfInterfaceType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfInterfaceType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfInterfaceType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfInterfaceType** objectArray = (SgAsmDwarfInterfaceType**) &(SgAsmDwarfInterfaceType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfInterfaceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfInterfaceType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfInterfaceType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfInterfaceType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfInterfaceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfInterfaceType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfInterfaceType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfInterfaceType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfInterfaceType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfInterfaceType** objectArray = (SgAsmDwarfInterfaceType**) &(SgAsmDwarfInterfaceType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfInterfaceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfInterfaceType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfInterfaceType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfInterfaceType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfInterfaceType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfInterfaceType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfInterfaceType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfInterfaceType** objectArray = (SgAsmDwarfInterfaceType**) &(SgAsmDwarfInterfaceType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfInterfaceType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfInterfaceType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfInterfaceType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfInterfaceType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfInterfaceType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfInterfaceType** objectArray = (SgAsmDwarfInterfaceType**) &(SgAsmDwarfInterfaceType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfInterfaceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfInterfaceType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfInterfaceType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfInterfaceType);

     return memory;
   }



/* #line 191192 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfLabel::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfLabel objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfLabel::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfLabel** objectArray = (SgAsmDwarfLabel**) &(SgAsmDwarfLabel::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfLabel::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfLabel::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfLabel::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfLabel*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfLabel::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfLabel::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfLabel::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfLabel objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfLabel::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfLabel** objectArray = (SgAsmDwarfLabel**) &(SgAsmDwarfLabel::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfLabel::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfLabel::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfLabel object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfLabel object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfLabel::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfLabel objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfLabel::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfLabel** objectArray = (SgAsmDwarfLabel**) &(SgAsmDwarfLabel::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfLabel::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfLabel::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfLabel found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfLabel::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfLabel::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfLabel** objectArray = (SgAsmDwarfLabel**) &(SgAsmDwarfLabel::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfLabel::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfLabel::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfLabel::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfLabel);

     return memory;
   }



/* #line 191424 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfLexicalBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfLexicalBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfLexicalBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfLexicalBlock** objectArray = (SgAsmDwarfLexicalBlock**) &(SgAsmDwarfLexicalBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfLexicalBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfLexicalBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfLexicalBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfLexicalBlock*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfLexicalBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfLexicalBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfLexicalBlock::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfLexicalBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfLexicalBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfLexicalBlock** objectArray = (SgAsmDwarfLexicalBlock**) &(SgAsmDwarfLexicalBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfLexicalBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfLexicalBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfLexicalBlock object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfLexicalBlock object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfLexicalBlock::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfLexicalBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfLexicalBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfLexicalBlock** objectArray = (SgAsmDwarfLexicalBlock**) &(SgAsmDwarfLexicalBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfLexicalBlock::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfLexicalBlock::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfLexicalBlock found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfLexicalBlock::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfLexicalBlock::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfLexicalBlock** objectArray = (SgAsmDwarfLexicalBlock**) &(SgAsmDwarfLexicalBlock::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfLexicalBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfLexicalBlock::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfLexicalBlock::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfLexicalBlock);

     return memory;
   }



/* #line 191656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfMember::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfMember objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfMember::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfMember** objectArray = (SgAsmDwarfMember**) &(SgAsmDwarfMember::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfMember::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfMember::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfMember::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfMember*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfMember::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfMember::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfMember::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfMember objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfMember::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfMember** objectArray = (SgAsmDwarfMember**) &(SgAsmDwarfMember::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfMember::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfMember::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfMember object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfMember object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfMember::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfMember objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfMember::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfMember** objectArray = (SgAsmDwarfMember**) &(SgAsmDwarfMember::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfMember::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfMember::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfMember found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfMember::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfMember::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfMember** objectArray = (SgAsmDwarfMember**) &(SgAsmDwarfMember::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfMember::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfMember::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfMember::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfMember);

     return memory;
   }



/* #line 191888 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfModule::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfModule objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfModule::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfModule** objectArray = (SgAsmDwarfModule**) &(SgAsmDwarfModule::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfModule::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfModule::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfModule*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfModule::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfModule::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfModule objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfModule::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfModule** objectArray = (SgAsmDwarfModule**) &(SgAsmDwarfModule::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfModule::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfModule object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfModule object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfModule::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfModule objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfModule::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfModule** objectArray = (SgAsmDwarfModule**) &(SgAsmDwarfModule::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfModule::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfModule::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfModule found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfModule::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfModule::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfModule** objectArray = (SgAsmDwarfModule**) &(SgAsmDwarfModule::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfModule::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfModule::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfModule);

     return memory;
   }



/* #line 192120 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfMutableType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfMutableType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfMutableType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfMutableType** objectArray = (SgAsmDwarfMutableType**) &(SgAsmDwarfMutableType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfMutableType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfMutableType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfMutableType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfMutableType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfMutableType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfMutableType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfMutableType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfMutableType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfMutableType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfMutableType** objectArray = (SgAsmDwarfMutableType**) &(SgAsmDwarfMutableType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfMutableType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfMutableType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfMutableType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfMutableType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfMutableType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfMutableType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfMutableType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfMutableType** objectArray = (SgAsmDwarfMutableType**) &(SgAsmDwarfMutableType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfMutableType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfMutableType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfMutableType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfMutableType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfMutableType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfMutableType** objectArray = (SgAsmDwarfMutableType**) &(SgAsmDwarfMutableType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfMutableType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfMutableType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfMutableType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfMutableType);

     return memory;
   }



/* #line 192352 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfNamelist::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfNamelist objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfNamelist::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfNamelist** objectArray = (SgAsmDwarfNamelist**) &(SgAsmDwarfNamelist::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfNamelist::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfNamelist::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfNamelist::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfNamelist*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfNamelist::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfNamelist::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfNamelist::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfNamelist objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfNamelist::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfNamelist** objectArray = (SgAsmDwarfNamelist**) &(SgAsmDwarfNamelist::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfNamelist::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfNamelist::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfNamelist object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfNamelist object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfNamelist::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfNamelist objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfNamelist::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfNamelist** objectArray = (SgAsmDwarfNamelist**) &(SgAsmDwarfNamelist::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfNamelist::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfNamelist::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfNamelist found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfNamelist::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfNamelist::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfNamelist** objectArray = (SgAsmDwarfNamelist**) &(SgAsmDwarfNamelist::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfNamelist::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfNamelist::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfNamelist::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfNamelist);

     return memory;
   }



/* #line 192584 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfNamelistItem::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfNamelistItem objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfNamelistItem::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfNamelistItem** objectArray = (SgAsmDwarfNamelistItem**) &(SgAsmDwarfNamelistItem::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfNamelistItem::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfNamelistItem::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfNamelistItem::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfNamelistItem*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfNamelistItem::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfNamelistItem::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfNamelistItem::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfNamelistItem objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfNamelistItem::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfNamelistItem** objectArray = (SgAsmDwarfNamelistItem**) &(SgAsmDwarfNamelistItem::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfNamelistItem::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfNamelistItem::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfNamelistItem object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfNamelistItem object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfNamelistItem::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfNamelistItem objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfNamelistItem::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfNamelistItem** objectArray = (SgAsmDwarfNamelistItem**) &(SgAsmDwarfNamelistItem::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfNamelistItem::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfNamelistItem::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfNamelistItem found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfNamelistItem::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfNamelistItem::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfNamelistItem** objectArray = (SgAsmDwarfNamelistItem**) &(SgAsmDwarfNamelistItem::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfNamelistItem::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfNamelistItem::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfNamelistItem::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfNamelistItem);

     return memory;
   }



/* #line 192816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfNamespace::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfNamespace objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfNamespace::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfNamespace** objectArray = (SgAsmDwarfNamespace**) &(SgAsmDwarfNamespace::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfNamespace::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfNamespace::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfNamespace::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfNamespace*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfNamespace::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfNamespace::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfNamespace::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfNamespace objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfNamespace::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfNamespace** objectArray = (SgAsmDwarfNamespace**) &(SgAsmDwarfNamespace::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfNamespace::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfNamespace::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfNamespace object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfNamespace object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfNamespace::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfNamespace objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfNamespace::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfNamespace** objectArray = (SgAsmDwarfNamespace**) &(SgAsmDwarfNamespace::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfNamespace::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfNamespace::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfNamespace found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfNamespace::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfNamespace::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfNamespace** objectArray = (SgAsmDwarfNamespace**) &(SgAsmDwarfNamespace::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfNamespace::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfNamespace::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfNamespace::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfNamespace);

     return memory;
   }



/* #line 193048 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfPackedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfPackedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfPackedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfPackedType** objectArray = (SgAsmDwarfPackedType**) &(SgAsmDwarfPackedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfPackedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfPackedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfPackedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfPackedType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfPackedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfPackedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfPackedType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfPackedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfPackedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfPackedType** objectArray = (SgAsmDwarfPackedType**) &(SgAsmDwarfPackedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfPackedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfPackedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfPackedType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfPackedType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfPackedType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfPackedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfPackedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfPackedType** objectArray = (SgAsmDwarfPackedType**) &(SgAsmDwarfPackedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfPackedType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfPackedType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfPackedType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfPackedType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfPackedType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfPackedType** objectArray = (SgAsmDwarfPackedType**) &(SgAsmDwarfPackedType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfPackedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfPackedType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfPackedType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfPackedType);

     return memory;
   }



/* #line 193280 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfPartialUnit::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfPartialUnit objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfPartialUnit::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfPartialUnit** objectArray = (SgAsmDwarfPartialUnit**) &(SgAsmDwarfPartialUnit::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfPartialUnit::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfPartialUnit::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfPartialUnit::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfPartialUnit*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfPartialUnit::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfPartialUnit::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfPartialUnit::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfPartialUnit objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfPartialUnit::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfPartialUnit** objectArray = (SgAsmDwarfPartialUnit**) &(SgAsmDwarfPartialUnit::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfPartialUnit::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfPartialUnit::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfPartialUnit object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfPartialUnit object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfPartialUnit::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfPartialUnit objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfPartialUnit::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfPartialUnit** objectArray = (SgAsmDwarfPartialUnit**) &(SgAsmDwarfPartialUnit::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfPartialUnit::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfPartialUnit::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfPartialUnit found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfPartialUnit::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfPartialUnit::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfPartialUnit** objectArray = (SgAsmDwarfPartialUnit**) &(SgAsmDwarfPartialUnit::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfPartialUnit::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfPartialUnit::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfPartialUnit::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfPartialUnit);

     return memory;
   }



/* #line 193512 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfPointerType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfPointerType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfPointerType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfPointerType** objectArray = (SgAsmDwarfPointerType**) &(SgAsmDwarfPointerType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfPointerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfPointerType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfPointerType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfPointerType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfPointerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfPointerType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfPointerType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfPointerType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfPointerType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfPointerType** objectArray = (SgAsmDwarfPointerType**) &(SgAsmDwarfPointerType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfPointerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfPointerType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfPointerType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfPointerType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfPointerType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfPointerType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfPointerType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfPointerType** objectArray = (SgAsmDwarfPointerType**) &(SgAsmDwarfPointerType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfPointerType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfPointerType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfPointerType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfPointerType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfPointerType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfPointerType** objectArray = (SgAsmDwarfPointerType**) &(SgAsmDwarfPointerType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfPointerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfPointerType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfPointerType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfPointerType);

     return memory;
   }



/* #line 193744 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfPtrToMemberType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfPtrToMemberType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfPtrToMemberType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfPtrToMemberType** objectArray = (SgAsmDwarfPtrToMemberType**) &(SgAsmDwarfPtrToMemberType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfPtrToMemberType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfPtrToMemberType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfPtrToMemberType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfPtrToMemberType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfPtrToMemberType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfPtrToMemberType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfPtrToMemberType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfPtrToMemberType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfPtrToMemberType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfPtrToMemberType** objectArray = (SgAsmDwarfPtrToMemberType**) &(SgAsmDwarfPtrToMemberType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfPtrToMemberType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfPtrToMemberType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfPtrToMemberType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfPtrToMemberType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfPtrToMemberType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfPtrToMemberType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfPtrToMemberType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfPtrToMemberType** objectArray = (SgAsmDwarfPtrToMemberType**) &(SgAsmDwarfPtrToMemberType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfPtrToMemberType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfPtrToMemberType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfPtrToMemberType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfPtrToMemberType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfPtrToMemberType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfPtrToMemberType** objectArray = (SgAsmDwarfPtrToMemberType**) &(SgAsmDwarfPtrToMemberType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfPtrToMemberType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfPtrToMemberType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfPtrToMemberType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfPtrToMemberType);

     return memory;
   }



/* #line 193976 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfReferenceType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfReferenceType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfReferenceType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfReferenceType** objectArray = (SgAsmDwarfReferenceType**) &(SgAsmDwarfReferenceType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfReferenceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfReferenceType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfReferenceType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfReferenceType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfReferenceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfReferenceType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfReferenceType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfReferenceType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfReferenceType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfReferenceType** objectArray = (SgAsmDwarfReferenceType**) &(SgAsmDwarfReferenceType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfReferenceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfReferenceType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfReferenceType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfReferenceType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfReferenceType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfReferenceType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfReferenceType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfReferenceType** objectArray = (SgAsmDwarfReferenceType**) &(SgAsmDwarfReferenceType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfReferenceType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfReferenceType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfReferenceType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfReferenceType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfReferenceType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfReferenceType** objectArray = (SgAsmDwarfReferenceType**) &(SgAsmDwarfReferenceType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfReferenceType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfReferenceType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfReferenceType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfReferenceType);

     return memory;
   }



/* #line 194208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfRestrictType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfRestrictType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfRestrictType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfRestrictType** objectArray = (SgAsmDwarfRestrictType**) &(SgAsmDwarfRestrictType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfRestrictType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfRestrictType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfRestrictType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfRestrictType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfRestrictType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfRestrictType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfRestrictType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfRestrictType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfRestrictType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfRestrictType** objectArray = (SgAsmDwarfRestrictType**) &(SgAsmDwarfRestrictType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfRestrictType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfRestrictType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfRestrictType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfRestrictType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfRestrictType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfRestrictType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfRestrictType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfRestrictType** objectArray = (SgAsmDwarfRestrictType**) &(SgAsmDwarfRestrictType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfRestrictType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfRestrictType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfRestrictType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfRestrictType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfRestrictType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfRestrictType** objectArray = (SgAsmDwarfRestrictType**) &(SgAsmDwarfRestrictType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfRestrictType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfRestrictType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfRestrictType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfRestrictType);

     return memory;
   }



/* #line 194440 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfSetType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfSetType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSetType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSetType** objectArray = (SgAsmDwarfSetType**) &(SgAsmDwarfSetType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfSetType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfSetType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfSetType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfSetType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfSetType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfSetType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfSetType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfSetType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSetType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSetType** objectArray = (SgAsmDwarfSetType**) &(SgAsmDwarfSetType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfSetType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfSetType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfSetType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfSetType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfSetType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfSetType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSetType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSetType** objectArray = (SgAsmDwarfSetType**) &(SgAsmDwarfSetType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfSetType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfSetType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfSetType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfSetType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfSetType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfSetType** objectArray = (SgAsmDwarfSetType**) &(SgAsmDwarfSetType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfSetType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfSetType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfSetType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfSetType);

     return memory;
   }



/* #line 194672 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfSharedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfSharedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSharedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSharedType** objectArray = (SgAsmDwarfSharedType**) &(SgAsmDwarfSharedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfSharedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfSharedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfSharedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfSharedType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfSharedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfSharedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfSharedType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfSharedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSharedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSharedType** objectArray = (SgAsmDwarfSharedType**) &(SgAsmDwarfSharedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfSharedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfSharedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfSharedType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfSharedType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfSharedType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfSharedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSharedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSharedType** objectArray = (SgAsmDwarfSharedType**) &(SgAsmDwarfSharedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfSharedType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfSharedType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfSharedType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfSharedType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfSharedType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfSharedType** objectArray = (SgAsmDwarfSharedType**) &(SgAsmDwarfSharedType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfSharedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfSharedType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfSharedType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfSharedType);

     return memory;
   }



/* #line 194904 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfStringType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfStringType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfStringType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfStringType** objectArray = (SgAsmDwarfStringType**) &(SgAsmDwarfStringType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfStringType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfStringType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfStringType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfStringType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfStringType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfStringType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfStringType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfStringType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfStringType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfStringType** objectArray = (SgAsmDwarfStringType**) &(SgAsmDwarfStringType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfStringType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfStringType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfStringType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfStringType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfStringType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfStringType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfStringType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfStringType** objectArray = (SgAsmDwarfStringType**) &(SgAsmDwarfStringType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfStringType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfStringType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfStringType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfStringType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfStringType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfStringType** objectArray = (SgAsmDwarfStringType**) &(SgAsmDwarfStringType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfStringType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfStringType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfStringType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfStringType);

     return memory;
   }



/* #line 195136 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfStructureType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfStructureType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfStructureType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfStructureType** objectArray = (SgAsmDwarfStructureType**) &(SgAsmDwarfStructureType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfStructureType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfStructureType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfStructureType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfStructureType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfStructureType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfStructureType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfStructureType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfStructureType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfStructureType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfStructureType** objectArray = (SgAsmDwarfStructureType**) &(SgAsmDwarfStructureType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfStructureType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfStructureType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfStructureType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfStructureType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfStructureType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfStructureType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfStructureType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfStructureType** objectArray = (SgAsmDwarfStructureType**) &(SgAsmDwarfStructureType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfStructureType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfStructureType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfStructureType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfStructureType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfStructureType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfStructureType** objectArray = (SgAsmDwarfStructureType**) &(SgAsmDwarfStructureType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfStructureType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfStructureType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfStructureType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfStructureType);

     return memory;
   }



/* #line 195368 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfSubprogram::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfSubprogram objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSubprogram::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSubprogram** objectArray = (SgAsmDwarfSubprogram**) &(SgAsmDwarfSubprogram::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfSubprogram::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfSubprogram::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfSubprogram::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfSubprogram*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfSubprogram::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfSubprogram::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfSubprogram::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfSubprogram objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSubprogram::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSubprogram** objectArray = (SgAsmDwarfSubprogram**) &(SgAsmDwarfSubprogram::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfSubprogram::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfSubprogram::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfSubprogram object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfSubprogram object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfSubprogram::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfSubprogram objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSubprogram::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSubprogram** objectArray = (SgAsmDwarfSubprogram**) &(SgAsmDwarfSubprogram::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfSubprogram::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfSubprogram::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfSubprogram found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfSubprogram::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfSubprogram::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfSubprogram** objectArray = (SgAsmDwarfSubprogram**) &(SgAsmDwarfSubprogram::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfSubprogram::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfSubprogram::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfSubprogram::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfSubprogram);

     return memory;
   }



/* #line 195600 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfSubrangeType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfSubrangeType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSubrangeType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSubrangeType** objectArray = (SgAsmDwarfSubrangeType**) &(SgAsmDwarfSubrangeType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfSubrangeType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfSubrangeType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfSubrangeType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfSubrangeType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfSubrangeType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfSubrangeType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfSubrangeType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfSubrangeType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSubrangeType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSubrangeType** objectArray = (SgAsmDwarfSubrangeType**) &(SgAsmDwarfSubrangeType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfSubrangeType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfSubrangeType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfSubrangeType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfSubrangeType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfSubrangeType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfSubrangeType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSubrangeType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSubrangeType** objectArray = (SgAsmDwarfSubrangeType**) &(SgAsmDwarfSubrangeType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfSubrangeType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfSubrangeType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfSubrangeType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfSubrangeType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfSubrangeType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfSubrangeType** objectArray = (SgAsmDwarfSubrangeType**) &(SgAsmDwarfSubrangeType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfSubrangeType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfSubrangeType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfSubrangeType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfSubrangeType);

     return memory;
   }



/* #line 195832 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfSubroutineType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfSubroutineType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSubroutineType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSubroutineType** objectArray = (SgAsmDwarfSubroutineType**) &(SgAsmDwarfSubroutineType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfSubroutineType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfSubroutineType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfSubroutineType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfSubroutineType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfSubroutineType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfSubroutineType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfSubroutineType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfSubroutineType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSubroutineType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSubroutineType** objectArray = (SgAsmDwarfSubroutineType**) &(SgAsmDwarfSubroutineType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfSubroutineType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfSubroutineType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfSubroutineType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfSubroutineType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfSubroutineType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfSubroutineType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfSubroutineType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfSubroutineType** objectArray = (SgAsmDwarfSubroutineType**) &(SgAsmDwarfSubroutineType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfSubroutineType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfSubroutineType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfSubroutineType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfSubroutineType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfSubroutineType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfSubroutineType** objectArray = (SgAsmDwarfSubroutineType**) &(SgAsmDwarfSubroutineType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfSubroutineType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfSubroutineType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfSubroutineType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfSubroutineType);

     return memory;
   }



/* #line 196064 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfTemplateTypeParameter::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfTemplateTypeParameter objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfTemplateTypeParameter::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfTemplateTypeParameter** objectArray = (SgAsmDwarfTemplateTypeParameter**) &(SgAsmDwarfTemplateTypeParameter::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfTemplateTypeParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfTemplateTypeParameter::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfTemplateTypeParameter::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfTemplateTypeParameter*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfTemplateTypeParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfTemplateTypeParameter::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfTemplateTypeParameter::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfTemplateTypeParameter objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfTemplateTypeParameter::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfTemplateTypeParameter** objectArray = (SgAsmDwarfTemplateTypeParameter**) &(SgAsmDwarfTemplateTypeParameter::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfTemplateTypeParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfTemplateTypeParameter::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfTemplateTypeParameter object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfTemplateTypeParameter object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfTemplateTypeParameter::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfTemplateTypeParameter objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfTemplateTypeParameter::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfTemplateTypeParameter** objectArray = (SgAsmDwarfTemplateTypeParameter**) &(SgAsmDwarfTemplateTypeParameter::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfTemplateTypeParameter::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfTemplateTypeParameter::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfTemplateTypeParameter found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfTemplateTypeParameter::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfTemplateTypeParameter::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfTemplateTypeParameter** objectArray = (SgAsmDwarfTemplateTypeParameter**) &(SgAsmDwarfTemplateTypeParameter::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfTemplateTypeParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfTemplateTypeParameter::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfTemplateTypeParameter::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfTemplateTypeParameter);

     return memory;
   }



/* #line 196296 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfTemplateValueParameter::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfTemplateValueParameter objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfTemplateValueParameter::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfTemplateValueParameter** objectArray = (SgAsmDwarfTemplateValueParameter**) &(SgAsmDwarfTemplateValueParameter::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfTemplateValueParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfTemplateValueParameter::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfTemplateValueParameter::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfTemplateValueParameter*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfTemplateValueParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfTemplateValueParameter::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfTemplateValueParameter::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfTemplateValueParameter objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfTemplateValueParameter::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfTemplateValueParameter** objectArray = (SgAsmDwarfTemplateValueParameter**) &(SgAsmDwarfTemplateValueParameter::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfTemplateValueParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfTemplateValueParameter::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfTemplateValueParameter object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfTemplateValueParameter object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfTemplateValueParameter::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfTemplateValueParameter objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfTemplateValueParameter::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfTemplateValueParameter** objectArray = (SgAsmDwarfTemplateValueParameter**) &(SgAsmDwarfTemplateValueParameter::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfTemplateValueParameter::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfTemplateValueParameter::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfTemplateValueParameter found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfTemplateValueParameter::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfTemplateValueParameter::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfTemplateValueParameter** objectArray = (SgAsmDwarfTemplateValueParameter**) &(SgAsmDwarfTemplateValueParameter::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfTemplateValueParameter::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfTemplateValueParameter::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfTemplateValueParameter::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfTemplateValueParameter);

     return memory;
   }



/* #line 196528 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfThrownType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfThrownType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfThrownType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfThrownType** objectArray = (SgAsmDwarfThrownType**) &(SgAsmDwarfThrownType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfThrownType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfThrownType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfThrownType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfThrownType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfThrownType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfThrownType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfThrownType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfThrownType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfThrownType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfThrownType** objectArray = (SgAsmDwarfThrownType**) &(SgAsmDwarfThrownType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfThrownType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfThrownType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfThrownType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfThrownType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfThrownType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfThrownType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfThrownType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfThrownType** objectArray = (SgAsmDwarfThrownType**) &(SgAsmDwarfThrownType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfThrownType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfThrownType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfThrownType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfThrownType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfThrownType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfThrownType** objectArray = (SgAsmDwarfThrownType**) &(SgAsmDwarfThrownType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfThrownType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfThrownType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfThrownType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfThrownType);

     return memory;
   }



/* #line 196760 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfTryBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfTryBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfTryBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfTryBlock** objectArray = (SgAsmDwarfTryBlock**) &(SgAsmDwarfTryBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfTryBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfTryBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfTryBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfTryBlock*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfTryBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfTryBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfTryBlock::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfTryBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfTryBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfTryBlock** objectArray = (SgAsmDwarfTryBlock**) &(SgAsmDwarfTryBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfTryBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfTryBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfTryBlock object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfTryBlock object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfTryBlock::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfTryBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfTryBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfTryBlock** objectArray = (SgAsmDwarfTryBlock**) &(SgAsmDwarfTryBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfTryBlock::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfTryBlock::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfTryBlock found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfTryBlock::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfTryBlock::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfTryBlock** objectArray = (SgAsmDwarfTryBlock**) &(SgAsmDwarfTryBlock::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfTryBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfTryBlock::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfTryBlock::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfTryBlock);

     return memory;
   }



/* #line 196992 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfTypedef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfTypedef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfTypedef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfTypedef** objectArray = (SgAsmDwarfTypedef**) &(SgAsmDwarfTypedef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfTypedef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfTypedef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfTypedef::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfTypedef*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfTypedef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfTypedef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfTypedef::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfTypedef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfTypedef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfTypedef** objectArray = (SgAsmDwarfTypedef**) &(SgAsmDwarfTypedef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfTypedef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfTypedef::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfTypedef object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfTypedef object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfTypedef::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfTypedef objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfTypedef::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfTypedef** objectArray = (SgAsmDwarfTypedef**) &(SgAsmDwarfTypedef::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfTypedef::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfTypedef::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfTypedef found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfTypedef::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfTypedef::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfTypedef** objectArray = (SgAsmDwarfTypedef**) &(SgAsmDwarfTypedef::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfTypedef::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfTypedef::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfTypedef::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfTypedef);

     return memory;
   }



/* #line 197224 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfUnionType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfUnionType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUnionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUnionType** objectArray = (SgAsmDwarfUnionType**) &(SgAsmDwarfUnionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUnionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfUnionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfUnionType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfUnionType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfUnionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUnionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfUnionType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUnionType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUnionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUnionType** objectArray = (SgAsmDwarfUnionType**) &(SgAsmDwarfUnionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUnionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUnionType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfUnionType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfUnionType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfUnionType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUnionType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUnionType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUnionType** objectArray = (SgAsmDwarfUnionType**) &(SgAsmDwarfUnionType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfUnionType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfUnionType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfUnionType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUnionType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfUnionType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfUnionType** objectArray = (SgAsmDwarfUnionType**) &(SgAsmDwarfUnionType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfUnionType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfUnionType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUnionType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfUnionType);

     return memory;
   }



/* #line 197456 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfUnknownConstruct::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfUnknownConstruct objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUnknownConstruct::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUnknownConstruct** objectArray = (SgAsmDwarfUnknownConstruct**) &(SgAsmDwarfUnknownConstruct::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUnknownConstruct::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfUnknownConstruct::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfUnknownConstruct::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfUnknownConstruct*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfUnknownConstruct::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUnknownConstruct::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfUnknownConstruct::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUnknownConstruct objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUnknownConstruct::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUnknownConstruct** objectArray = (SgAsmDwarfUnknownConstruct**) &(SgAsmDwarfUnknownConstruct::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUnknownConstruct::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUnknownConstruct::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfUnknownConstruct object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfUnknownConstruct object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfUnknownConstruct::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUnknownConstruct objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUnknownConstruct::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUnknownConstruct** objectArray = (SgAsmDwarfUnknownConstruct**) &(SgAsmDwarfUnknownConstruct::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfUnknownConstruct::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfUnknownConstruct::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfUnknownConstruct found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUnknownConstruct::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfUnknownConstruct::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfUnknownConstruct** objectArray = (SgAsmDwarfUnknownConstruct**) &(SgAsmDwarfUnknownConstruct::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfUnknownConstruct::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfUnknownConstruct::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUnknownConstruct::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfUnknownConstruct);

     return memory;
   }



/* #line 197688 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfUnspecifiedParameters::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfUnspecifiedParameters objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUnspecifiedParameters::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUnspecifiedParameters** objectArray = (SgAsmDwarfUnspecifiedParameters**) &(SgAsmDwarfUnspecifiedParameters::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUnspecifiedParameters::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfUnspecifiedParameters::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfUnspecifiedParameters::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfUnspecifiedParameters*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfUnspecifiedParameters::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUnspecifiedParameters::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfUnspecifiedParameters::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUnspecifiedParameters objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUnspecifiedParameters::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUnspecifiedParameters** objectArray = (SgAsmDwarfUnspecifiedParameters**) &(SgAsmDwarfUnspecifiedParameters::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUnspecifiedParameters::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUnspecifiedParameters::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfUnspecifiedParameters object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfUnspecifiedParameters object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfUnspecifiedParameters::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUnspecifiedParameters objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUnspecifiedParameters::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUnspecifiedParameters** objectArray = (SgAsmDwarfUnspecifiedParameters**) &(SgAsmDwarfUnspecifiedParameters::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfUnspecifiedParameters::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfUnspecifiedParameters::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfUnspecifiedParameters found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUnspecifiedParameters::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfUnspecifiedParameters::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfUnspecifiedParameters** objectArray = (SgAsmDwarfUnspecifiedParameters**) &(SgAsmDwarfUnspecifiedParameters::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfUnspecifiedParameters::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfUnspecifiedParameters::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUnspecifiedParameters::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfUnspecifiedParameters);

     return memory;
   }



/* #line 197920 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfUnspecifiedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfUnspecifiedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUnspecifiedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUnspecifiedType** objectArray = (SgAsmDwarfUnspecifiedType**) &(SgAsmDwarfUnspecifiedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUnspecifiedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfUnspecifiedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfUnspecifiedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfUnspecifiedType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfUnspecifiedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUnspecifiedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfUnspecifiedType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUnspecifiedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUnspecifiedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUnspecifiedType** objectArray = (SgAsmDwarfUnspecifiedType**) &(SgAsmDwarfUnspecifiedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUnspecifiedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUnspecifiedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfUnspecifiedType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfUnspecifiedType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfUnspecifiedType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUnspecifiedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUnspecifiedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUnspecifiedType** objectArray = (SgAsmDwarfUnspecifiedType**) &(SgAsmDwarfUnspecifiedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfUnspecifiedType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfUnspecifiedType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfUnspecifiedType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUnspecifiedType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfUnspecifiedType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfUnspecifiedType** objectArray = (SgAsmDwarfUnspecifiedType**) &(SgAsmDwarfUnspecifiedType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfUnspecifiedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfUnspecifiedType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUnspecifiedType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfUnspecifiedType);

     return memory;
   }



/* #line 198152 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfUpcRelaxedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfUpcRelaxedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUpcRelaxedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUpcRelaxedType** objectArray = (SgAsmDwarfUpcRelaxedType**) &(SgAsmDwarfUpcRelaxedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUpcRelaxedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfUpcRelaxedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfUpcRelaxedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfUpcRelaxedType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfUpcRelaxedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUpcRelaxedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfUpcRelaxedType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUpcRelaxedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUpcRelaxedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUpcRelaxedType** objectArray = (SgAsmDwarfUpcRelaxedType**) &(SgAsmDwarfUpcRelaxedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUpcRelaxedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUpcRelaxedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfUpcRelaxedType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfUpcRelaxedType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfUpcRelaxedType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUpcRelaxedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUpcRelaxedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUpcRelaxedType** objectArray = (SgAsmDwarfUpcRelaxedType**) &(SgAsmDwarfUpcRelaxedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfUpcRelaxedType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfUpcRelaxedType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfUpcRelaxedType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUpcRelaxedType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfUpcRelaxedType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfUpcRelaxedType** objectArray = (SgAsmDwarfUpcRelaxedType**) &(SgAsmDwarfUpcRelaxedType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfUpcRelaxedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfUpcRelaxedType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUpcRelaxedType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfUpcRelaxedType);

     return memory;
   }



/* #line 198384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfUpcSharedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfUpcSharedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUpcSharedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUpcSharedType** objectArray = (SgAsmDwarfUpcSharedType**) &(SgAsmDwarfUpcSharedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUpcSharedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfUpcSharedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfUpcSharedType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfUpcSharedType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfUpcSharedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUpcSharedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfUpcSharedType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUpcSharedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUpcSharedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUpcSharedType** objectArray = (SgAsmDwarfUpcSharedType**) &(SgAsmDwarfUpcSharedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUpcSharedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUpcSharedType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfUpcSharedType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfUpcSharedType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfUpcSharedType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUpcSharedType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUpcSharedType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUpcSharedType** objectArray = (SgAsmDwarfUpcSharedType**) &(SgAsmDwarfUpcSharedType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfUpcSharedType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfUpcSharedType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfUpcSharedType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUpcSharedType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfUpcSharedType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfUpcSharedType** objectArray = (SgAsmDwarfUpcSharedType**) &(SgAsmDwarfUpcSharedType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfUpcSharedType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfUpcSharedType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUpcSharedType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfUpcSharedType);

     return memory;
   }



/* #line 198616 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfUpcStrictType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfUpcStrictType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUpcStrictType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUpcStrictType** objectArray = (SgAsmDwarfUpcStrictType**) &(SgAsmDwarfUpcStrictType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUpcStrictType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfUpcStrictType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfUpcStrictType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfUpcStrictType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfUpcStrictType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUpcStrictType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfUpcStrictType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUpcStrictType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUpcStrictType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUpcStrictType** objectArray = (SgAsmDwarfUpcStrictType**) &(SgAsmDwarfUpcStrictType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfUpcStrictType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfUpcStrictType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfUpcStrictType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfUpcStrictType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfUpcStrictType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfUpcStrictType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfUpcStrictType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfUpcStrictType** objectArray = (SgAsmDwarfUpcStrictType**) &(SgAsmDwarfUpcStrictType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfUpcStrictType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfUpcStrictType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfUpcStrictType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUpcStrictType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfUpcStrictType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfUpcStrictType** objectArray = (SgAsmDwarfUpcStrictType**) &(SgAsmDwarfUpcStrictType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfUpcStrictType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfUpcStrictType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfUpcStrictType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfUpcStrictType);

     return memory;
   }



/* #line 198848 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfVariable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfVariable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfVariable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfVariable** objectArray = (SgAsmDwarfVariable**) &(SgAsmDwarfVariable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfVariable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfVariable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfVariable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfVariable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfVariable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfVariable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfVariable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfVariable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfVariable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfVariable** objectArray = (SgAsmDwarfVariable**) &(SgAsmDwarfVariable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfVariable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfVariable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfVariable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfVariable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfVariable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfVariable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfVariable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfVariable** objectArray = (SgAsmDwarfVariable**) &(SgAsmDwarfVariable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfVariable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfVariable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfVariable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfVariable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfVariable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfVariable** objectArray = (SgAsmDwarfVariable**) &(SgAsmDwarfVariable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfVariable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfVariable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfVariable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfVariable);

     return memory;
   }



/* #line 199080 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfVariant::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfVariant objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfVariant::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfVariant** objectArray = (SgAsmDwarfVariant**) &(SgAsmDwarfVariant::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfVariant::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfVariant::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfVariant::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfVariant*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfVariant::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfVariant::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfVariant::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfVariant objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfVariant::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfVariant** objectArray = (SgAsmDwarfVariant**) &(SgAsmDwarfVariant::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfVariant::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfVariant::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfVariant object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfVariant object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfVariant::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfVariant objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfVariant::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfVariant** objectArray = (SgAsmDwarfVariant**) &(SgAsmDwarfVariant::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfVariant::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfVariant::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfVariant found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfVariant::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfVariant::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfVariant** objectArray = (SgAsmDwarfVariant**) &(SgAsmDwarfVariant::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfVariant::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfVariant::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfVariant::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfVariant);

     return memory;
   }



/* #line 199312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfVariantPart::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfVariantPart objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfVariantPart::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfVariantPart** objectArray = (SgAsmDwarfVariantPart**) &(SgAsmDwarfVariantPart::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfVariantPart::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfVariantPart::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfVariantPart::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfVariantPart*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfVariantPart::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfVariantPart::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfVariantPart::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfVariantPart objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfVariantPart::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfVariantPart** objectArray = (SgAsmDwarfVariantPart**) &(SgAsmDwarfVariantPart::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfVariantPart::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfVariantPart::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfVariantPart object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfVariantPart object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfVariantPart::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfVariantPart objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfVariantPart::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfVariantPart** objectArray = (SgAsmDwarfVariantPart**) &(SgAsmDwarfVariantPart::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfVariantPart::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfVariantPart::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfVariantPart found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfVariantPart::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfVariantPart::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfVariantPart** objectArray = (SgAsmDwarfVariantPart**) &(SgAsmDwarfVariantPart::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfVariantPart::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfVariantPart::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfVariantPart::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfVariantPart);

     return memory;
   }



/* #line 199544 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfVolatileType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfVolatileType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfVolatileType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfVolatileType** objectArray = (SgAsmDwarfVolatileType**) &(SgAsmDwarfVolatileType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfVolatileType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfVolatileType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfVolatileType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfVolatileType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfVolatileType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfVolatileType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfVolatileType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfVolatileType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfVolatileType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfVolatileType** objectArray = (SgAsmDwarfVolatileType**) &(SgAsmDwarfVolatileType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfVolatileType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfVolatileType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfVolatileType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfVolatileType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfVolatileType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfVolatileType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfVolatileType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfVolatileType** objectArray = (SgAsmDwarfVolatileType**) &(SgAsmDwarfVolatileType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfVolatileType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfVolatileType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfVolatileType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfVolatileType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfVolatileType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfVolatileType** objectArray = (SgAsmDwarfVolatileType**) &(SgAsmDwarfVolatileType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfVolatileType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfVolatileType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfVolatileType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfVolatileType);

     return memory;
   }



/* #line 199776 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfWithStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfWithStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfWithStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfWithStmt** objectArray = (SgAsmDwarfWithStmt**) &(SgAsmDwarfWithStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfWithStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfWithStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfWithStmt::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfWithStmt*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfWithStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfWithStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfWithStmt::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfWithStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfWithStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfWithStmt** objectArray = (SgAsmDwarfWithStmt**) &(SgAsmDwarfWithStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfWithStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfWithStmt::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfWithStmt object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfWithStmt object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfWithStmt::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfWithStmt objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfWithStmt::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfWithStmt** objectArray = (SgAsmDwarfWithStmt**) &(SgAsmDwarfWithStmt::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfWithStmt::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfWithStmt::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfWithStmt found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfWithStmt::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfWithStmt::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfWithStmt** objectArray = (SgAsmDwarfWithStmt**) &(SgAsmDwarfWithStmt::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfWithStmt::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfWithStmt::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfWithStmt::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfWithStmt);

     return memory;
   }



/* #line 200008 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfConstructList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfConstructList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfConstructList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfConstructList** objectArray = (SgAsmDwarfConstructList**) &(SgAsmDwarfConstructList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfConstructList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfConstructList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfConstructList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfConstructList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfConstructList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfConstructList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfConstructList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfConstructList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfConstructList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfConstructList** objectArray = (SgAsmDwarfConstructList**) &(SgAsmDwarfConstructList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfConstructList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfConstructList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfConstructList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfConstructList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfConstructList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfConstructList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfConstructList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfConstructList** objectArray = (SgAsmDwarfConstructList**) &(SgAsmDwarfConstructList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfConstructList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfConstructList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfConstructList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfConstructList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfConstructList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfConstructList** objectArray = (SgAsmDwarfConstructList**) &(SgAsmDwarfConstructList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfConstructList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfConstructList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfConstructList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfConstructList);

     return memory;
   }



/* #line 200240 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfLine::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfLine objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfLine::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfLine** objectArray = (SgAsmDwarfLine**) &(SgAsmDwarfLine::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfLine::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfLine::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfLine::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfLine*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfLine::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfLine::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfLine::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfLine objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfLine::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfLine** objectArray = (SgAsmDwarfLine**) &(SgAsmDwarfLine::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfLine::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfLine::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfLine object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfLine object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfLine::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfLine objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfLine::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfLine** objectArray = (SgAsmDwarfLine**) &(SgAsmDwarfLine::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfLine::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfLine::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfLine found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfLine::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfLine::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfLine** objectArray = (SgAsmDwarfLine**) &(SgAsmDwarfLine::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfLine::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfLine::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfLine::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfLine);

     return memory;
   }



/* #line 200472 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfLineList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfLineList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfLineList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfLineList** objectArray = (SgAsmDwarfLineList**) &(SgAsmDwarfLineList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfLineList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfLineList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfLineList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfLineList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfLineList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfLineList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfLineList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfLineList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfLineList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfLineList** objectArray = (SgAsmDwarfLineList**) &(SgAsmDwarfLineList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfLineList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfLineList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfLineList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfLineList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfLineList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfLineList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfLineList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfLineList** objectArray = (SgAsmDwarfLineList**) &(SgAsmDwarfLineList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfLineList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfLineList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfLineList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfLineList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfLineList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfLineList** objectArray = (SgAsmDwarfLineList**) &(SgAsmDwarfLineList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfLineList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfLineList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfLineList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfLineList);

     return memory;
   }



/* #line 200704 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfMacro::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfMacro objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfMacro::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfMacro** objectArray = (SgAsmDwarfMacro**) &(SgAsmDwarfMacro::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfMacro::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfMacro::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfMacro::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfMacro*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfMacro::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfMacro::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfMacro::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfMacro objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfMacro::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfMacro** objectArray = (SgAsmDwarfMacro**) &(SgAsmDwarfMacro::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfMacro::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfMacro::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfMacro object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfMacro object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfMacro::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfMacro objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfMacro::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfMacro** objectArray = (SgAsmDwarfMacro**) &(SgAsmDwarfMacro::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfMacro::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfMacro::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfMacro found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfMacro::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfMacro::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfMacro** objectArray = (SgAsmDwarfMacro**) &(SgAsmDwarfMacro::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfMacro::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfMacro::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfMacro::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfMacro);

     return memory;
   }



/* #line 200936 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDwarfMacroList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDwarfMacroList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfMacroList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfMacroList** objectArray = (SgAsmDwarfMacroList**) &(SgAsmDwarfMacroList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfMacroList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDwarfMacroList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDwarfMacroList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDwarfMacroList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDwarfMacroList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfMacroList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDwarfMacroList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfMacroList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfMacroList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfMacroList** objectArray = (SgAsmDwarfMacroList**) &(SgAsmDwarfMacroList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDwarfMacroList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDwarfMacroList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDwarfMacroList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDwarfMacroList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDwarfMacroList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDwarfMacroList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDwarfMacroList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDwarfMacroList** objectArray = (SgAsmDwarfMacroList**) &(SgAsmDwarfMacroList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDwarfMacroList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDwarfMacroList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDwarfMacroList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfMacroList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDwarfMacroList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDwarfMacroList** objectArray = (SgAsmDwarfMacroList**) &(SgAsmDwarfMacroList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDwarfMacroList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDwarfMacroList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDwarfMacroList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDwarfMacroList);

     return memory;
   }



/* #line 201168 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfDynamicEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfDynamicEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfDynamicEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfDynamicEntry** objectArray = (SgAsmElfDynamicEntry**) &(SgAsmElfDynamicEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfDynamicEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfDynamicEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfDynamicEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfDynamicEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfDynamicEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfDynamicEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfDynamicEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfDynamicEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfDynamicEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfDynamicEntry** objectArray = (SgAsmElfDynamicEntry**) &(SgAsmElfDynamicEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfDynamicEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfDynamicEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfDynamicEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfDynamicEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfDynamicEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfDynamicEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfDynamicEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfDynamicEntry** objectArray = (SgAsmElfDynamicEntry**) &(SgAsmElfDynamicEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfDynamicEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfDynamicEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfDynamicEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfDynamicEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfDynamicEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfDynamicEntry** objectArray = (SgAsmElfDynamicEntry**) &(SgAsmElfDynamicEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfDynamicEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfDynamicEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfDynamicEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfDynamicEntry);

     return memory;
   }



/* #line 201400 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfDynamicEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfDynamicEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfDynamicEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfDynamicEntryList** objectArray = (SgAsmElfDynamicEntryList**) &(SgAsmElfDynamicEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfDynamicEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfDynamicEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfDynamicEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfDynamicEntryList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfDynamicEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfDynamicEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfDynamicEntryList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfDynamicEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfDynamicEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfDynamicEntryList** objectArray = (SgAsmElfDynamicEntryList**) &(SgAsmElfDynamicEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfDynamicEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfDynamicEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfDynamicEntryList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfDynamicEntryList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfDynamicEntryList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfDynamicEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfDynamicEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfDynamicEntryList** objectArray = (SgAsmElfDynamicEntryList**) &(SgAsmElfDynamicEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfDynamicEntryList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfDynamicEntryList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfDynamicEntryList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfDynamicEntryList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfDynamicEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfDynamicEntryList** objectArray = (SgAsmElfDynamicEntryList**) &(SgAsmElfDynamicEntryList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfDynamicEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfDynamicEntryList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfDynamicEntryList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfDynamicEntryList);

     return memory;
   }



/* #line 201632 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfEHFrameEntryCI::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfEHFrameEntryCI objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameEntryCI::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameEntryCI** objectArray = (SgAsmElfEHFrameEntryCI**) &(SgAsmElfEHFrameEntryCI::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryCI::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfEHFrameEntryCI::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfEHFrameEntryCI::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfEHFrameEntryCI*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryCI::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfEHFrameEntryCI::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfEHFrameEntryCI::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfEHFrameEntryCI objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameEntryCI::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameEntryCI** objectArray = (SgAsmElfEHFrameEntryCI**) &(SgAsmElfEHFrameEntryCI::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryCI::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfEHFrameEntryCI::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfEHFrameEntryCI object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfEHFrameEntryCI object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfEHFrameEntryCI::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfEHFrameEntryCI objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameEntryCI::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameEntryCI** objectArray = (SgAsmElfEHFrameEntryCI**) &(SgAsmElfEHFrameEntryCI::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfEHFrameEntryCI::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfEHFrameEntryCI::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfEHFrameEntryCI found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfEHFrameEntryCI::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfEHFrameEntryCI::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfEHFrameEntryCI** objectArray = (SgAsmElfEHFrameEntryCI**) &(SgAsmElfEHFrameEntryCI::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryCI::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfEHFrameEntryCI::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfEHFrameEntryCI::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfEHFrameEntryCI);

     return memory;
   }



/* #line 201864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfEHFrameEntryCIList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfEHFrameEntryCIList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameEntryCIList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameEntryCIList** objectArray = (SgAsmElfEHFrameEntryCIList**) &(SgAsmElfEHFrameEntryCIList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryCIList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfEHFrameEntryCIList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfEHFrameEntryCIList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfEHFrameEntryCIList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryCIList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfEHFrameEntryCIList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfEHFrameEntryCIList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfEHFrameEntryCIList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameEntryCIList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameEntryCIList** objectArray = (SgAsmElfEHFrameEntryCIList**) &(SgAsmElfEHFrameEntryCIList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryCIList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfEHFrameEntryCIList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfEHFrameEntryCIList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfEHFrameEntryCIList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfEHFrameEntryCIList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfEHFrameEntryCIList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameEntryCIList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameEntryCIList** objectArray = (SgAsmElfEHFrameEntryCIList**) &(SgAsmElfEHFrameEntryCIList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfEHFrameEntryCIList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfEHFrameEntryCIList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfEHFrameEntryCIList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfEHFrameEntryCIList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfEHFrameEntryCIList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfEHFrameEntryCIList** objectArray = (SgAsmElfEHFrameEntryCIList**) &(SgAsmElfEHFrameEntryCIList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryCIList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfEHFrameEntryCIList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfEHFrameEntryCIList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfEHFrameEntryCIList);

     return memory;
   }



/* #line 202096 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfEHFrameEntryFD::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfEHFrameEntryFD objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameEntryFD::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameEntryFD** objectArray = (SgAsmElfEHFrameEntryFD**) &(SgAsmElfEHFrameEntryFD::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryFD::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfEHFrameEntryFD::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfEHFrameEntryFD::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfEHFrameEntryFD*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryFD::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfEHFrameEntryFD::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfEHFrameEntryFD::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfEHFrameEntryFD objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameEntryFD::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameEntryFD** objectArray = (SgAsmElfEHFrameEntryFD**) &(SgAsmElfEHFrameEntryFD::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryFD::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfEHFrameEntryFD::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfEHFrameEntryFD object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfEHFrameEntryFD object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfEHFrameEntryFD::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfEHFrameEntryFD objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameEntryFD::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameEntryFD** objectArray = (SgAsmElfEHFrameEntryFD**) &(SgAsmElfEHFrameEntryFD::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfEHFrameEntryFD::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfEHFrameEntryFD::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfEHFrameEntryFD found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfEHFrameEntryFD::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfEHFrameEntryFD::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfEHFrameEntryFD** objectArray = (SgAsmElfEHFrameEntryFD**) &(SgAsmElfEHFrameEntryFD::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryFD::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfEHFrameEntryFD::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfEHFrameEntryFD::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfEHFrameEntryFD);

     return memory;
   }



/* #line 202328 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfEHFrameEntryFDList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfEHFrameEntryFDList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameEntryFDList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameEntryFDList** objectArray = (SgAsmElfEHFrameEntryFDList**) &(SgAsmElfEHFrameEntryFDList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryFDList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfEHFrameEntryFDList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfEHFrameEntryFDList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfEHFrameEntryFDList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryFDList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfEHFrameEntryFDList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfEHFrameEntryFDList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfEHFrameEntryFDList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameEntryFDList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameEntryFDList** objectArray = (SgAsmElfEHFrameEntryFDList**) &(SgAsmElfEHFrameEntryFDList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryFDList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfEHFrameEntryFDList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfEHFrameEntryFDList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfEHFrameEntryFDList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfEHFrameEntryFDList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfEHFrameEntryFDList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameEntryFDList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameEntryFDList** objectArray = (SgAsmElfEHFrameEntryFDList**) &(SgAsmElfEHFrameEntryFDList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfEHFrameEntryFDList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfEHFrameEntryFDList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfEHFrameEntryFDList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfEHFrameEntryFDList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfEHFrameEntryFDList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfEHFrameEntryFDList** objectArray = (SgAsmElfEHFrameEntryFDList**) &(SgAsmElfEHFrameEntryFDList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfEHFrameEntryFDList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfEHFrameEntryFDList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfEHFrameEntryFDList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfEHFrameEntryFDList);

     return memory;
   }



/* #line 202560 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfNoteEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfNoteEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfNoteEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfNoteEntry** objectArray = (SgAsmElfNoteEntry**) &(SgAsmElfNoteEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfNoteEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfNoteEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfNoteEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfNoteEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfNoteEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfNoteEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfNoteEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfNoteEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfNoteEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfNoteEntry** objectArray = (SgAsmElfNoteEntry**) &(SgAsmElfNoteEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfNoteEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfNoteEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfNoteEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfNoteEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfNoteEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfNoteEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfNoteEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfNoteEntry** objectArray = (SgAsmElfNoteEntry**) &(SgAsmElfNoteEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfNoteEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfNoteEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfNoteEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfNoteEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfNoteEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfNoteEntry** objectArray = (SgAsmElfNoteEntry**) &(SgAsmElfNoteEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfNoteEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfNoteEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfNoteEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfNoteEntry);

     return memory;
   }



/* #line 202792 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfNoteEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfNoteEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfNoteEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfNoteEntryList** objectArray = (SgAsmElfNoteEntryList**) &(SgAsmElfNoteEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfNoteEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfNoteEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfNoteEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfNoteEntryList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfNoteEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfNoteEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfNoteEntryList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfNoteEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfNoteEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfNoteEntryList** objectArray = (SgAsmElfNoteEntryList**) &(SgAsmElfNoteEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfNoteEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfNoteEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfNoteEntryList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfNoteEntryList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfNoteEntryList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfNoteEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfNoteEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfNoteEntryList** objectArray = (SgAsmElfNoteEntryList**) &(SgAsmElfNoteEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfNoteEntryList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfNoteEntryList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfNoteEntryList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfNoteEntryList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfNoteEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfNoteEntryList** objectArray = (SgAsmElfNoteEntryList**) &(SgAsmElfNoteEntryList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfNoteEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfNoteEntryList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfNoteEntryList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfNoteEntryList);

     return memory;
   }



/* #line 203024 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfRelocEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfRelocEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfRelocEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfRelocEntry** objectArray = (SgAsmElfRelocEntry**) &(SgAsmElfRelocEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfRelocEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfRelocEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfRelocEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfRelocEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfRelocEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfRelocEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfRelocEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfRelocEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfRelocEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfRelocEntry** objectArray = (SgAsmElfRelocEntry**) &(SgAsmElfRelocEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfRelocEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfRelocEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfRelocEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfRelocEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfRelocEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfRelocEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfRelocEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfRelocEntry** objectArray = (SgAsmElfRelocEntry**) &(SgAsmElfRelocEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfRelocEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfRelocEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfRelocEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfRelocEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfRelocEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfRelocEntry** objectArray = (SgAsmElfRelocEntry**) &(SgAsmElfRelocEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfRelocEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfRelocEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfRelocEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfRelocEntry);

     return memory;
   }



/* #line 203256 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfRelocEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfRelocEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfRelocEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfRelocEntryList** objectArray = (SgAsmElfRelocEntryList**) &(SgAsmElfRelocEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfRelocEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfRelocEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfRelocEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfRelocEntryList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfRelocEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfRelocEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfRelocEntryList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfRelocEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfRelocEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfRelocEntryList** objectArray = (SgAsmElfRelocEntryList**) &(SgAsmElfRelocEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfRelocEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfRelocEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfRelocEntryList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfRelocEntryList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfRelocEntryList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfRelocEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfRelocEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfRelocEntryList** objectArray = (SgAsmElfRelocEntryList**) &(SgAsmElfRelocEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfRelocEntryList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfRelocEntryList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfRelocEntryList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfRelocEntryList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfRelocEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfRelocEntryList** objectArray = (SgAsmElfRelocEntryList**) &(SgAsmElfRelocEntryList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfRelocEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfRelocEntryList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfRelocEntryList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfRelocEntryList);

     return memory;
   }



/* #line 203488 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSectionTableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSectionTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSectionTableEntry** objectArray = (SgAsmElfSectionTableEntry**) &(SgAsmElfSectionTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSectionTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSectionTableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSectionTableEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSectionTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSectionTableEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSectionTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSectionTableEntry** objectArray = (SgAsmElfSectionTableEntry**) &(SgAsmElfSectionTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSectionTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSectionTableEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSectionTableEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSectionTableEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSectionTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSectionTableEntry** objectArray = (SgAsmElfSectionTableEntry**) &(SgAsmElfSectionTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSectionTableEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSectionTableEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSectionTableEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSectionTableEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSectionTableEntry** objectArray = (SgAsmElfSectionTableEntry**) &(SgAsmElfSectionTableEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSectionTableEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSectionTableEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSectionTableEntry);

     return memory;
   }



/* #line 203720 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSegmentTableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSegmentTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSegmentTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSegmentTableEntry** objectArray = (SgAsmElfSegmentTableEntry**) &(SgAsmElfSegmentTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSegmentTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSegmentTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSegmentTableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSegmentTableEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSegmentTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSegmentTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSegmentTableEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSegmentTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSegmentTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSegmentTableEntry** objectArray = (SgAsmElfSegmentTableEntry**) &(SgAsmElfSegmentTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSegmentTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSegmentTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSegmentTableEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSegmentTableEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSegmentTableEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSegmentTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSegmentTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSegmentTableEntry** objectArray = (SgAsmElfSegmentTableEntry**) &(SgAsmElfSegmentTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSegmentTableEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSegmentTableEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSegmentTableEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSegmentTableEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSegmentTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSegmentTableEntry** objectArray = (SgAsmElfSegmentTableEntry**) &(SgAsmElfSegmentTableEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSegmentTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSegmentTableEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSegmentTableEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSegmentTableEntry);

     return memory;
   }



/* #line 203952 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSegmentTableEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSegmentTableEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSegmentTableEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSegmentTableEntryList** objectArray = (SgAsmElfSegmentTableEntryList**) &(SgAsmElfSegmentTableEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSegmentTableEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSegmentTableEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSegmentTableEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSegmentTableEntryList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSegmentTableEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSegmentTableEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSegmentTableEntryList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSegmentTableEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSegmentTableEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSegmentTableEntryList** objectArray = (SgAsmElfSegmentTableEntryList**) &(SgAsmElfSegmentTableEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSegmentTableEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSegmentTableEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSegmentTableEntryList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSegmentTableEntryList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSegmentTableEntryList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSegmentTableEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSegmentTableEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSegmentTableEntryList** objectArray = (SgAsmElfSegmentTableEntryList**) &(SgAsmElfSegmentTableEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSegmentTableEntryList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSegmentTableEntryList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSegmentTableEntryList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSegmentTableEntryList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSegmentTableEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSegmentTableEntryList** objectArray = (SgAsmElfSegmentTableEntryList**) &(SgAsmElfSegmentTableEntryList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSegmentTableEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSegmentTableEntryList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSegmentTableEntryList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSegmentTableEntryList);

     return memory;
   }



/* #line 204184 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymbolList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymbolList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymbolList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymbolList** objectArray = (SgAsmElfSymbolList**) &(SgAsmElfSymbolList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymbolList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymbolList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymbolList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymbolList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymbolList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymbolList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymbolList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymbolList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymbolList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymbolList** objectArray = (SgAsmElfSymbolList**) &(SgAsmElfSymbolList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymbolList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymbolList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymbolList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymbolList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymbolList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymbolList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymbolList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymbolList** objectArray = (SgAsmElfSymbolList**) &(SgAsmElfSymbolList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymbolList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymbolList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymbolList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymbolList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymbolList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymbolList** objectArray = (SgAsmElfSymbolList**) &(SgAsmElfSymbolList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymbolList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymbolList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymbolList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymbolList);

     return memory;
   }



/* #line 204416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymverDefinedAux::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymverDefinedAux objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedAux::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedAux** objectArray = (SgAsmElfSymverDefinedAux**) &(SgAsmElfSymverDefinedAux::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverDefinedAux::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymverDefinedAux::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymverDefinedAux::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymverDefinedAux*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymverDefinedAux::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverDefinedAux::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymverDefinedAux::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverDefinedAux objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedAux::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedAux** objectArray = (SgAsmElfSymverDefinedAux**) &(SgAsmElfSymverDefinedAux::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverDefinedAux::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverDefinedAux::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymverDefinedAux object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymverDefinedAux object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymverDefinedAux::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverDefinedAux objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedAux::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedAux** objectArray = (SgAsmElfSymverDefinedAux**) &(SgAsmElfSymverDefinedAux::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymverDefinedAux::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymverDefinedAux::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymverDefinedAux found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverDefinedAux::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymverDefinedAux::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymverDefinedAux** objectArray = (SgAsmElfSymverDefinedAux**) &(SgAsmElfSymverDefinedAux::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymverDefinedAux::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymverDefinedAux::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverDefinedAux::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymverDefinedAux);

     return memory;
   }



/* #line 204648 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymverDefinedAuxList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymverDefinedAuxList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedAuxList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedAuxList** objectArray = (SgAsmElfSymverDefinedAuxList**) &(SgAsmElfSymverDefinedAuxList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverDefinedAuxList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymverDefinedAuxList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymverDefinedAuxList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymverDefinedAuxList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymverDefinedAuxList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverDefinedAuxList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymverDefinedAuxList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverDefinedAuxList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedAuxList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedAuxList** objectArray = (SgAsmElfSymverDefinedAuxList**) &(SgAsmElfSymverDefinedAuxList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverDefinedAuxList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverDefinedAuxList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymverDefinedAuxList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymverDefinedAuxList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymverDefinedAuxList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverDefinedAuxList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedAuxList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedAuxList** objectArray = (SgAsmElfSymverDefinedAuxList**) &(SgAsmElfSymverDefinedAuxList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymverDefinedAuxList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymverDefinedAuxList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymverDefinedAuxList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverDefinedAuxList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymverDefinedAuxList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymverDefinedAuxList** objectArray = (SgAsmElfSymverDefinedAuxList**) &(SgAsmElfSymverDefinedAuxList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymverDefinedAuxList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymverDefinedAuxList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverDefinedAuxList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymverDefinedAuxList);

     return memory;
   }



/* #line 204880 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymverDefinedEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymverDefinedEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedEntry** objectArray = (SgAsmElfSymverDefinedEntry**) &(SgAsmElfSymverDefinedEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverDefinedEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymverDefinedEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymverDefinedEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymverDefinedEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymverDefinedEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverDefinedEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymverDefinedEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverDefinedEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedEntry** objectArray = (SgAsmElfSymverDefinedEntry**) &(SgAsmElfSymverDefinedEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverDefinedEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverDefinedEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymverDefinedEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymverDefinedEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymverDefinedEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverDefinedEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedEntry** objectArray = (SgAsmElfSymverDefinedEntry**) &(SgAsmElfSymverDefinedEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymverDefinedEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymverDefinedEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymverDefinedEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverDefinedEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymverDefinedEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymverDefinedEntry** objectArray = (SgAsmElfSymverDefinedEntry**) &(SgAsmElfSymverDefinedEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymverDefinedEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymverDefinedEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverDefinedEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymverDefinedEntry);

     return memory;
   }



/* #line 205112 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymverDefinedEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymverDefinedEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedEntryList** objectArray = (SgAsmElfSymverDefinedEntryList**) &(SgAsmElfSymverDefinedEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverDefinedEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymverDefinedEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymverDefinedEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymverDefinedEntryList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymverDefinedEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverDefinedEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymverDefinedEntryList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverDefinedEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedEntryList** objectArray = (SgAsmElfSymverDefinedEntryList**) &(SgAsmElfSymverDefinedEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverDefinedEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverDefinedEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymverDefinedEntryList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymverDefinedEntryList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymverDefinedEntryList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverDefinedEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedEntryList** objectArray = (SgAsmElfSymverDefinedEntryList**) &(SgAsmElfSymverDefinedEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymverDefinedEntryList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymverDefinedEntryList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymverDefinedEntryList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverDefinedEntryList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymverDefinedEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymverDefinedEntryList** objectArray = (SgAsmElfSymverDefinedEntryList**) &(SgAsmElfSymverDefinedEntryList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymverDefinedEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymverDefinedEntryList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverDefinedEntryList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymverDefinedEntryList);

     return memory;
   }



/* #line 205344 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymverEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymverEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverEntry** objectArray = (SgAsmElfSymverEntry**) &(SgAsmElfSymverEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymverEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymverEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymverEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymverEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymverEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverEntry** objectArray = (SgAsmElfSymverEntry**) &(SgAsmElfSymverEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymverEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymverEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymverEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverEntry** objectArray = (SgAsmElfSymverEntry**) &(SgAsmElfSymverEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymverEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymverEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymverEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymverEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymverEntry** objectArray = (SgAsmElfSymverEntry**) &(SgAsmElfSymverEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymverEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymverEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymverEntry);

     return memory;
   }



/* #line 205576 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymverEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymverEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverEntryList** objectArray = (SgAsmElfSymverEntryList**) &(SgAsmElfSymverEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymverEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymverEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymverEntryList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymverEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymverEntryList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverEntryList** objectArray = (SgAsmElfSymverEntryList**) &(SgAsmElfSymverEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymverEntryList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymverEntryList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymverEntryList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverEntryList** objectArray = (SgAsmElfSymverEntryList**) &(SgAsmElfSymverEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymverEntryList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymverEntryList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymverEntryList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverEntryList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymverEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymverEntryList** objectArray = (SgAsmElfSymverEntryList**) &(SgAsmElfSymverEntryList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymverEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymverEntryList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverEntryList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymverEntryList);

     return memory;
   }



/* #line 205808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymverNeededAux::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymverNeededAux objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededAux::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededAux** objectArray = (SgAsmElfSymverNeededAux**) &(SgAsmElfSymverNeededAux::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverNeededAux::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymverNeededAux::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymverNeededAux::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymverNeededAux*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymverNeededAux::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverNeededAux::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymverNeededAux::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverNeededAux objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededAux::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededAux** objectArray = (SgAsmElfSymverNeededAux**) &(SgAsmElfSymverNeededAux::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverNeededAux::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverNeededAux::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymverNeededAux object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymverNeededAux object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymverNeededAux::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverNeededAux objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededAux::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededAux** objectArray = (SgAsmElfSymverNeededAux**) &(SgAsmElfSymverNeededAux::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymverNeededAux::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymverNeededAux::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymverNeededAux found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverNeededAux::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymverNeededAux::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymverNeededAux** objectArray = (SgAsmElfSymverNeededAux**) &(SgAsmElfSymverNeededAux::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymverNeededAux::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymverNeededAux::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverNeededAux::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymverNeededAux);

     return memory;
   }



/* #line 206040 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymverNeededAuxList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymverNeededAuxList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededAuxList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededAuxList** objectArray = (SgAsmElfSymverNeededAuxList**) &(SgAsmElfSymverNeededAuxList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverNeededAuxList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymverNeededAuxList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymverNeededAuxList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymverNeededAuxList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymverNeededAuxList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverNeededAuxList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymverNeededAuxList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverNeededAuxList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededAuxList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededAuxList** objectArray = (SgAsmElfSymverNeededAuxList**) &(SgAsmElfSymverNeededAuxList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverNeededAuxList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverNeededAuxList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymverNeededAuxList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymverNeededAuxList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymverNeededAuxList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverNeededAuxList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededAuxList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededAuxList** objectArray = (SgAsmElfSymverNeededAuxList**) &(SgAsmElfSymverNeededAuxList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymverNeededAuxList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymverNeededAuxList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymverNeededAuxList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverNeededAuxList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymverNeededAuxList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymverNeededAuxList** objectArray = (SgAsmElfSymverNeededAuxList**) &(SgAsmElfSymverNeededAuxList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymverNeededAuxList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymverNeededAuxList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverNeededAuxList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymverNeededAuxList);

     return memory;
   }



/* #line 206272 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymverNeededEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymverNeededEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededEntry** objectArray = (SgAsmElfSymverNeededEntry**) &(SgAsmElfSymverNeededEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverNeededEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymverNeededEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymverNeededEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymverNeededEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymverNeededEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverNeededEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymverNeededEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverNeededEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededEntry** objectArray = (SgAsmElfSymverNeededEntry**) &(SgAsmElfSymverNeededEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverNeededEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverNeededEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymverNeededEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymverNeededEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymverNeededEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverNeededEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededEntry** objectArray = (SgAsmElfSymverNeededEntry**) &(SgAsmElfSymverNeededEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymverNeededEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymverNeededEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymverNeededEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverNeededEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymverNeededEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymverNeededEntry** objectArray = (SgAsmElfSymverNeededEntry**) &(SgAsmElfSymverNeededEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymverNeededEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymverNeededEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverNeededEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymverNeededEntry);

     return memory;
   }



/* #line 206504 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymverNeededEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymverNeededEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededEntryList** objectArray = (SgAsmElfSymverNeededEntryList**) &(SgAsmElfSymverNeededEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverNeededEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymverNeededEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymverNeededEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymverNeededEntryList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymverNeededEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverNeededEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymverNeededEntryList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverNeededEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededEntryList** objectArray = (SgAsmElfSymverNeededEntryList**) &(SgAsmElfSymverNeededEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverNeededEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverNeededEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymverNeededEntryList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymverNeededEntryList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymverNeededEntryList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverNeededEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededEntryList** objectArray = (SgAsmElfSymverNeededEntryList**) &(SgAsmElfSymverNeededEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymverNeededEntryList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymverNeededEntryList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymverNeededEntryList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverNeededEntryList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymverNeededEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymverNeededEntryList** objectArray = (SgAsmElfSymverNeededEntryList**) &(SgAsmElfSymverNeededEntryList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymverNeededEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymverNeededEntryList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverNeededEntryList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymverNeededEntryList);

     return memory;
   }



/* #line 206736 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmGenericDLL::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmGenericDLL objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericDLL::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericDLL** objectArray = (SgAsmGenericDLL**) &(SgAsmGenericDLL::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericDLL::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmGenericDLL::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmGenericDLL::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmGenericDLL*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmGenericDLL::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericDLL::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmGenericDLL::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericDLL objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericDLL::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericDLL** objectArray = (SgAsmGenericDLL**) &(SgAsmGenericDLL::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericDLL::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericDLL::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmGenericDLL object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmGenericDLL object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmGenericDLL::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericDLL objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericDLL::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericDLL** objectArray = (SgAsmGenericDLL**) &(SgAsmGenericDLL::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmGenericDLL::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmGenericDLL::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmGenericDLL found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericDLL::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmGenericDLL::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmGenericDLL** objectArray = (SgAsmGenericDLL**) &(SgAsmGenericDLL::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmGenericDLL::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmGenericDLL::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericDLL::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmGenericDLL);

     return memory;
   }



/* #line 206968 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmGenericDLLList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmGenericDLLList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericDLLList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericDLLList** objectArray = (SgAsmGenericDLLList**) &(SgAsmGenericDLLList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericDLLList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmGenericDLLList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmGenericDLLList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmGenericDLLList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmGenericDLLList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericDLLList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmGenericDLLList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericDLLList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericDLLList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericDLLList** objectArray = (SgAsmGenericDLLList**) &(SgAsmGenericDLLList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericDLLList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericDLLList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmGenericDLLList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmGenericDLLList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmGenericDLLList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericDLLList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericDLLList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericDLLList** objectArray = (SgAsmGenericDLLList**) &(SgAsmGenericDLLList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmGenericDLLList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmGenericDLLList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmGenericDLLList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericDLLList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmGenericDLLList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmGenericDLLList** objectArray = (SgAsmGenericDLLList**) &(SgAsmGenericDLLList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmGenericDLLList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmGenericDLLList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericDLLList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmGenericDLLList);

     return memory;
   }



/* #line 207200 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmGenericFile::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmGenericFile objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericFile** objectArray = (SgAsmGenericFile**) &(SgAsmGenericFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmGenericFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmGenericFile::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmGenericFile*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmGenericFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmGenericFile::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericFile objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericFile** objectArray = (SgAsmGenericFile**) &(SgAsmGenericFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericFile::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmGenericFile object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmGenericFile object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmGenericFile::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericFile objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericFile::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericFile** objectArray = (SgAsmGenericFile**) &(SgAsmGenericFile::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmGenericFile::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmGenericFile::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmGenericFile found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericFile::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmGenericFile::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmGenericFile** objectArray = (SgAsmGenericFile**) &(SgAsmGenericFile::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmGenericFile::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmGenericFile::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericFile::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmGenericFile);

     return memory;
   }



/* #line 207432 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmGenericFormat::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmGenericFormat objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericFormat::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericFormat** objectArray = (SgAsmGenericFormat**) &(SgAsmGenericFormat::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericFormat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmGenericFormat::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmGenericFormat::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmGenericFormat*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmGenericFormat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericFormat::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmGenericFormat::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericFormat objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericFormat::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericFormat** objectArray = (SgAsmGenericFormat**) &(SgAsmGenericFormat::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericFormat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericFormat::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmGenericFormat object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmGenericFormat object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmGenericFormat::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericFormat objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericFormat::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericFormat** objectArray = (SgAsmGenericFormat**) &(SgAsmGenericFormat::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmGenericFormat::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmGenericFormat::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmGenericFormat found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericFormat::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmGenericFormat::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmGenericFormat** objectArray = (SgAsmGenericFormat**) &(SgAsmGenericFormat::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmGenericFormat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmGenericFormat::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericFormat::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmGenericFormat);

     return memory;
   }



/* #line 207664 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmGenericHeaderList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmGenericHeaderList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericHeaderList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericHeaderList** objectArray = (SgAsmGenericHeaderList**) &(SgAsmGenericHeaderList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericHeaderList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmGenericHeaderList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmGenericHeaderList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmGenericHeaderList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmGenericHeaderList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericHeaderList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmGenericHeaderList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericHeaderList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericHeaderList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericHeaderList** objectArray = (SgAsmGenericHeaderList**) &(SgAsmGenericHeaderList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericHeaderList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericHeaderList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmGenericHeaderList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmGenericHeaderList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmGenericHeaderList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericHeaderList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericHeaderList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericHeaderList** objectArray = (SgAsmGenericHeaderList**) &(SgAsmGenericHeaderList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmGenericHeaderList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmGenericHeaderList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmGenericHeaderList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericHeaderList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmGenericHeaderList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmGenericHeaderList** objectArray = (SgAsmGenericHeaderList**) &(SgAsmGenericHeaderList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmGenericHeaderList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmGenericHeaderList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericHeaderList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmGenericHeaderList);

     return memory;
   }



/* #line 207896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmGenericSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmGenericSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericSection** objectArray = (SgAsmGenericSection**) &(SgAsmGenericSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmGenericSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmGenericSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmGenericSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmGenericSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmGenericSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericSection** objectArray = (SgAsmGenericSection**) &(SgAsmGenericSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmGenericSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmGenericSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmGenericSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericSection** objectArray = (SgAsmGenericSection**) &(SgAsmGenericSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmGenericSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmGenericSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmGenericSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmGenericSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmGenericSection** objectArray = (SgAsmGenericSection**) &(SgAsmGenericSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmGenericSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmGenericSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmGenericSection);

     return memory;
   }



/* #line 208128 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCliHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCliHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCliHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCliHeader** objectArray = (SgAsmCliHeader**) &(SgAsmCliHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCliHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCliHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCliHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCliHeader*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCliHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCliHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCliHeader::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCliHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCliHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCliHeader** objectArray = (SgAsmCliHeader**) &(SgAsmCliHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCliHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCliHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCliHeader object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCliHeader object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCliHeader::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCliHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCliHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCliHeader** objectArray = (SgAsmCliHeader**) &(SgAsmCliHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCliHeader::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCliHeader::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCliHeader found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCliHeader::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCliHeader::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCliHeader** objectArray = (SgAsmCliHeader**) &(SgAsmCliHeader::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCliHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCliHeader::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCliHeader::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCliHeader);

     return memory;
   }



/* #line 208360 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCoffSymbolTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCoffSymbolTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCoffSymbolTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCoffSymbolTable** objectArray = (SgAsmCoffSymbolTable**) &(SgAsmCoffSymbolTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCoffSymbolTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCoffSymbolTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCoffSymbolTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCoffSymbolTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCoffSymbolTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCoffSymbolTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCoffSymbolTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCoffSymbolTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCoffSymbolTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCoffSymbolTable** objectArray = (SgAsmCoffSymbolTable**) &(SgAsmCoffSymbolTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCoffSymbolTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCoffSymbolTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCoffSymbolTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCoffSymbolTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCoffSymbolTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCoffSymbolTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCoffSymbolTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCoffSymbolTable** objectArray = (SgAsmCoffSymbolTable**) &(SgAsmCoffSymbolTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCoffSymbolTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCoffSymbolTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCoffSymbolTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCoffSymbolTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCoffSymbolTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCoffSymbolTable** objectArray = (SgAsmCoffSymbolTable**) &(SgAsmCoffSymbolTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCoffSymbolTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCoffSymbolTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCoffSymbolTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCoffSymbolTable);

     return memory;
   }



/* #line 208592 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDOSExtendedHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDOSExtendedHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDOSExtendedHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDOSExtendedHeader** objectArray = (SgAsmDOSExtendedHeader**) &(SgAsmDOSExtendedHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDOSExtendedHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDOSExtendedHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDOSExtendedHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDOSExtendedHeader*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDOSExtendedHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDOSExtendedHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDOSExtendedHeader::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDOSExtendedHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDOSExtendedHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDOSExtendedHeader** objectArray = (SgAsmDOSExtendedHeader**) &(SgAsmDOSExtendedHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDOSExtendedHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDOSExtendedHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDOSExtendedHeader object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDOSExtendedHeader object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDOSExtendedHeader::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDOSExtendedHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDOSExtendedHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDOSExtendedHeader** objectArray = (SgAsmDOSExtendedHeader**) &(SgAsmDOSExtendedHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDOSExtendedHeader::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDOSExtendedHeader::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDOSExtendedHeader found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDOSExtendedHeader::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDOSExtendedHeader::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDOSExtendedHeader** objectArray = (SgAsmDOSExtendedHeader**) &(SgAsmDOSExtendedHeader::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDOSExtendedHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDOSExtendedHeader::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDOSExtendedHeader::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDOSExtendedHeader);

     return memory;
   }



/* #line 208824 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSection** objectArray = (SgAsmElfSection**) &(SgAsmElfSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSection** objectArray = (SgAsmElfSection**) &(SgAsmElfSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSection** objectArray = (SgAsmElfSection**) &(SgAsmElfSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSection** objectArray = (SgAsmElfSection**) &(SgAsmElfSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSection);

     return memory;
   }



/* #line 209056 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfDynamicSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfDynamicSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfDynamicSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfDynamicSection** objectArray = (SgAsmElfDynamicSection**) &(SgAsmElfDynamicSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfDynamicSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfDynamicSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfDynamicSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfDynamicSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfDynamicSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfDynamicSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfDynamicSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfDynamicSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfDynamicSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfDynamicSection** objectArray = (SgAsmElfDynamicSection**) &(SgAsmElfDynamicSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfDynamicSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfDynamicSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfDynamicSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfDynamicSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfDynamicSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfDynamicSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfDynamicSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfDynamicSection** objectArray = (SgAsmElfDynamicSection**) &(SgAsmElfDynamicSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfDynamicSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfDynamicSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfDynamicSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfDynamicSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfDynamicSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfDynamicSection** objectArray = (SgAsmElfDynamicSection**) &(SgAsmElfDynamicSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfDynamicSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfDynamicSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfDynamicSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfDynamicSection);

     return memory;
   }



/* #line 209288 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfEHFrameSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfEHFrameSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameSection** objectArray = (SgAsmElfEHFrameSection**) &(SgAsmElfEHFrameSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfEHFrameSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfEHFrameSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfEHFrameSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfEHFrameSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfEHFrameSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfEHFrameSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfEHFrameSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfEHFrameSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameSection** objectArray = (SgAsmElfEHFrameSection**) &(SgAsmElfEHFrameSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfEHFrameSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfEHFrameSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfEHFrameSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfEHFrameSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfEHFrameSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfEHFrameSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfEHFrameSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfEHFrameSection** objectArray = (SgAsmElfEHFrameSection**) &(SgAsmElfEHFrameSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfEHFrameSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfEHFrameSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfEHFrameSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfEHFrameSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfEHFrameSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfEHFrameSection** objectArray = (SgAsmElfEHFrameSection**) &(SgAsmElfEHFrameSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfEHFrameSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfEHFrameSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfEHFrameSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfEHFrameSection);

     return memory;
   }



/* #line 209520 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfNoteSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfNoteSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfNoteSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfNoteSection** objectArray = (SgAsmElfNoteSection**) &(SgAsmElfNoteSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfNoteSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfNoteSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfNoteSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfNoteSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfNoteSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfNoteSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfNoteSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfNoteSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfNoteSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfNoteSection** objectArray = (SgAsmElfNoteSection**) &(SgAsmElfNoteSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfNoteSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfNoteSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfNoteSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfNoteSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfNoteSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfNoteSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfNoteSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfNoteSection** objectArray = (SgAsmElfNoteSection**) &(SgAsmElfNoteSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfNoteSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfNoteSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfNoteSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfNoteSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfNoteSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfNoteSection** objectArray = (SgAsmElfNoteSection**) &(SgAsmElfNoteSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfNoteSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfNoteSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfNoteSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfNoteSection);

     return memory;
   }



/* #line 209752 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfRelocSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfRelocSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfRelocSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfRelocSection** objectArray = (SgAsmElfRelocSection**) &(SgAsmElfRelocSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfRelocSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfRelocSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfRelocSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfRelocSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfRelocSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfRelocSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfRelocSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfRelocSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfRelocSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfRelocSection** objectArray = (SgAsmElfRelocSection**) &(SgAsmElfRelocSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfRelocSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfRelocSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfRelocSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfRelocSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfRelocSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfRelocSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfRelocSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfRelocSection** objectArray = (SgAsmElfRelocSection**) &(SgAsmElfRelocSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfRelocSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfRelocSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfRelocSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfRelocSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfRelocSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfRelocSection** objectArray = (SgAsmElfRelocSection**) &(SgAsmElfRelocSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfRelocSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfRelocSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfRelocSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfRelocSection);

     return memory;
   }



/* #line 209984 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfStringSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfStringSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfStringSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfStringSection** objectArray = (SgAsmElfStringSection**) &(SgAsmElfStringSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfStringSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfStringSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfStringSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfStringSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfStringSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfStringSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfStringSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfStringSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfStringSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfStringSection** objectArray = (SgAsmElfStringSection**) &(SgAsmElfStringSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfStringSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfStringSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfStringSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfStringSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfStringSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfStringSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfStringSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfStringSection** objectArray = (SgAsmElfStringSection**) &(SgAsmElfStringSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfStringSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfStringSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfStringSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfStringSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfStringSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfStringSection** objectArray = (SgAsmElfStringSection**) &(SgAsmElfStringSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfStringSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfStringSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfStringSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfStringSection);

     return memory;
   }



/* #line 210216 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymbolSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymbolSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymbolSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymbolSection** objectArray = (SgAsmElfSymbolSection**) &(SgAsmElfSymbolSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymbolSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymbolSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymbolSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymbolSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymbolSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymbolSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymbolSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymbolSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymbolSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymbolSection** objectArray = (SgAsmElfSymbolSection**) &(SgAsmElfSymbolSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymbolSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymbolSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymbolSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymbolSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymbolSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymbolSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymbolSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymbolSection** objectArray = (SgAsmElfSymbolSection**) &(SgAsmElfSymbolSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymbolSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymbolSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymbolSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymbolSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymbolSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymbolSection** objectArray = (SgAsmElfSymbolSection**) &(SgAsmElfSymbolSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymbolSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymbolSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymbolSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymbolSection);

     return memory;
   }



/* #line 210448 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymverDefinedSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymverDefinedSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedSection** objectArray = (SgAsmElfSymverDefinedSection**) &(SgAsmElfSymverDefinedSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverDefinedSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymverDefinedSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymverDefinedSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymverDefinedSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymverDefinedSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverDefinedSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymverDefinedSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverDefinedSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedSection** objectArray = (SgAsmElfSymverDefinedSection**) &(SgAsmElfSymverDefinedSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverDefinedSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverDefinedSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymverDefinedSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymverDefinedSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymverDefinedSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverDefinedSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverDefinedSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverDefinedSection** objectArray = (SgAsmElfSymverDefinedSection**) &(SgAsmElfSymverDefinedSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymverDefinedSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymverDefinedSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymverDefinedSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverDefinedSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymverDefinedSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymverDefinedSection** objectArray = (SgAsmElfSymverDefinedSection**) &(SgAsmElfSymverDefinedSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymverDefinedSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymverDefinedSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverDefinedSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymverDefinedSection);

     return memory;
   }



/* #line 210680 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymverNeededSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymverNeededSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededSection** objectArray = (SgAsmElfSymverNeededSection**) &(SgAsmElfSymverNeededSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverNeededSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymverNeededSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymverNeededSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymverNeededSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymverNeededSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverNeededSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymverNeededSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverNeededSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededSection** objectArray = (SgAsmElfSymverNeededSection**) &(SgAsmElfSymverNeededSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverNeededSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverNeededSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymverNeededSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymverNeededSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymverNeededSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverNeededSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverNeededSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverNeededSection** objectArray = (SgAsmElfSymverNeededSection**) &(SgAsmElfSymverNeededSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymverNeededSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymverNeededSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymverNeededSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverNeededSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymverNeededSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymverNeededSection** objectArray = (SgAsmElfSymverNeededSection**) &(SgAsmElfSymverNeededSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymverNeededSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymverNeededSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverNeededSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymverNeededSection);

     return memory;
   }



/* #line 210912 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymverSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymverSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverSection** objectArray = (SgAsmElfSymverSection**) &(SgAsmElfSymverSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymverSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymverSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymverSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymverSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymverSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverSection** objectArray = (SgAsmElfSymverSection**) &(SgAsmElfSymverSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymverSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymverSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymverSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymverSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymverSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymverSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymverSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymverSection** objectArray = (SgAsmElfSymverSection**) &(SgAsmElfSymverSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymverSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymverSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymverSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymverSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymverSection** objectArray = (SgAsmElfSymverSection**) &(SgAsmElfSymverSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymverSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymverSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymverSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymverSection);

     return memory;
   }



/* #line 211144 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSectionTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSectionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSectionTable** objectArray = (SgAsmElfSectionTable**) &(SgAsmElfSectionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSectionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSectionTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSectionTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSectionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSectionTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSectionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSectionTable** objectArray = (SgAsmElfSectionTable**) &(SgAsmElfSectionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSectionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSectionTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSectionTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSectionTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSectionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSectionTable** objectArray = (SgAsmElfSectionTable**) &(SgAsmElfSectionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSectionTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSectionTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSectionTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSectionTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSectionTable** objectArray = (SgAsmElfSectionTable**) &(SgAsmElfSectionTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSectionTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSectionTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSectionTable);

     return memory;
   }



/* #line 211376 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSegmentTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSegmentTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSegmentTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSegmentTable** objectArray = (SgAsmElfSegmentTable**) &(SgAsmElfSegmentTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSegmentTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSegmentTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSegmentTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSegmentTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSegmentTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSegmentTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSegmentTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSegmentTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSegmentTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSegmentTable** objectArray = (SgAsmElfSegmentTable**) &(SgAsmElfSegmentTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSegmentTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSegmentTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSegmentTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSegmentTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSegmentTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSegmentTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSegmentTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSegmentTable** objectArray = (SgAsmElfSegmentTable**) &(SgAsmElfSegmentTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSegmentTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSegmentTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSegmentTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSegmentTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSegmentTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSegmentTable** objectArray = (SgAsmElfSegmentTable**) &(SgAsmElfSegmentTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSegmentTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSegmentTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSegmentTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSegmentTable);

     return memory;
   }



/* #line 211608 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmGenericHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmGenericHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericHeader** objectArray = (SgAsmGenericHeader**) &(SgAsmGenericHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmGenericHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmGenericHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmGenericHeader*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmGenericHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmGenericHeader::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericHeader** objectArray = (SgAsmGenericHeader**) &(SgAsmGenericHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmGenericHeader object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmGenericHeader object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmGenericHeader::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericHeader** objectArray = (SgAsmGenericHeader**) &(SgAsmGenericHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmGenericHeader::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmGenericHeader::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmGenericHeader found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericHeader::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmGenericHeader::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmGenericHeader** objectArray = (SgAsmGenericHeader**) &(SgAsmGenericHeader::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmGenericHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmGenericHeader::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericHeader::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmGenericHeader);

     return memory;
   }



/* #line 211840 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDOSFileHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDOSFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDOSFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDOSFileHeader** objectArray = (SgAsmDOSFileHeader**) &(SgAsmDOSFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDOSFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDOSFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDOSFileHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDOSFileHeader*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDOSFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDOSFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDOSFileHeader::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDOSFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDOSFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDOSFileHeader** objectArray = (SgAsmDOSFileHeader**) &(SgAsmDOSFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDOSFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDOSFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDOSFileHeader object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDOSFileHeader object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDOSFileHeader::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDOSFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDOSFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDOSFileHeader** objectArray = (SgAsmDOSFileHeader**) &(SgAsmDOSFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDOSFileHeader::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDOSFileHeader::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDOSFileHeader found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDOSFileHeader::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDOSFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDOSFileHeader** objectArray = (SgAsmDOSFileHeader**) &(SgAsmDOSFileHeader::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDOSFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDOSFileHeader::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDOSFileHeader::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDOSFileHeader);

     return memory;
   }



/* #line 212072 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfFileHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfFileHeader** objectArray = (SgAsmElfFileHeader**) &(SgAsmElfFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfFileHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfFileHeader*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfFileHeader::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfFileHeader** objectArray = (SgAsmElfFileHeader**) &(SgAsmElfFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfFileHeader object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfFileHeader object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfFileHeader::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfFileHeader** objectArray = (SgAsmElfFileHeader**) &(SgAsmElfFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfFileHeader::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfFileHeader::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfFileHeader found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfFileHeader::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfFileHeader** objectArray = (SgAsmElfFileHeader**) &(SgAsmElfFileHeader::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfFileHeader::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfFileHeader::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfFileHeader);

     return memory;
   }



/* #line 212304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmFileHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmFileHeader** objectArray = (SgAsmJvmFileHeader**) &(SgAsmJvmFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmFileHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmFileHeader*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmFileHeader::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmFileHeader** objectArray = (SgAsmJvmFileHeader**) &(SgAsmJvmFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmFileHeader object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmFileHeader object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmFileHeader::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmFileHeader** objectArray = (SgAsmJvmFileHeader**) &(SgAsmJvmFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmFileHeader::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmFileHeader::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmFileHeader found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmFileHeader::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmFileHeader** objectArray = (SgAsmJvmFileHeader**) &(SgAsmJvmFileHeader::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmFileHeader::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmFileHeader::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmFileHeader);

     return memory;
   }



/* #line 212536 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmLEFileHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmLEFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEFileHeader** objectArray = (SgAsmLEFileHeader**) &(SgAsmLEFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLEFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmLEFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmLEFileHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmLEFileHeader*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmLEFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLEFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmLEFileHeader::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLEFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEFileHeader** objectArray = (SgAsmLEFileHeader**) &(SgAsmLEFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLEFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLEFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmLEFileHeader object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmLEFileHeader object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmLEFileHeader::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLEFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEFileHeader** objectArray = (SgAsmLEFileHeader**) &(SgAsmLEFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmLEFileHeader::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmLEFileHeader::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmLEFileHeader found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLEFileHeader::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmLEFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmLEFileHeader** objectArray = (SgAsmLEFileHeader**) &(SgAsmLEFileHeader::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmLEFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmLEFileHeader::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLEFileHeader::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmLEFileHeader);

     return memory;
   }



/* #line 212768 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmNEFileHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmNEFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEFileHeader** objectArray = (SgAsmNEFileHeader**) &(SgAsmNEFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNEFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmNEFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmNEFileHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmNEFileHeader*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmNEFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNEFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmNEFileHeader::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNEFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEFileHeader** objectArray = (SgAsmNEFileHeader**) &(SgAsmNEFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNEFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNEFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmNEFileHeader object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmNEFileHeader object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmNEFileHeader::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNEFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEFileHeader** objectArray = (SgAsmNEFileHeader**) &(SgAsmNEFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmNEFileHeader::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmNEFileHeader::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmNEFileHeader found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNEFileHeader::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmNEFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmNEFileHeader** objectArray = (SgAsmNEFileHeader**) &(SgAsmNEFileHeader::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmNEFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmNEFileHeader::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNEFileHeader::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmNEFileHeader);

     return memory;
   }



/* #line 213000 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPEFileHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPEFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEFileHeader** objectArray = (SgAsmPEFileHeader**) &(SgAsmPEFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPEFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPEFileHeader::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPEFileHeader*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPEFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPEFileHeader::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEFileHeader** objectArray = (SgAsmPEFileHeader**) &(SgAsmPEFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEFileHeader::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPEFileHeader object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPEFileHeader object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPEFileHeader::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEFileHeader objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEFileHeader** objectArray = (SgAsmPEFileHeader**) &(SgAsmPEFileHeader::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPEFileHeader::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPEFileHeader::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPEFileHeader found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEFileHeader::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPEFileHeader::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPEFileHeader** objectArray = (SgAsmPEFileHeader**) &(SgAsmPEFileHeader::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPEFileHeader::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPEFileHeader::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEFileHeader::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPEFileHeader);

     return memory;
   }



/* #line 213232 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmAttributeTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmAttributeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmAttributeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmAttributeTable** objectArray = (SgAsmJvmAttributeTable**) &(SgAsmJvmAttributeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmAttributeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmAttributeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmAttributeTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmAttributeTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmAttributeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmAttributeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmAttributeTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmAttributeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmAttributeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmAttributeTable** objectArray = (SgAsmJvmAttributeTable**) &(SgAsmJvmAttributeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmAttributeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmAttributeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmAttributeTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmAttributeTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmAttributeTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmAttributeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmAttributeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmAttributeTable** objectArray = (SgAsmJvmAttributeTable**) &(SgAsmJvmAttributeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmAttributeTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmAttributeTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmAttributeTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmAttributeTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmAttributeTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmAttributeTable** objectArray = (SgAsmJvmAttributeTable**) &(SgAsmJvmAttributeTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmAttributeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmAttributeTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmAttributeTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmAttributeTable);

     return memory;
   }



/* #line 213464 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmConstantPool::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmConstantPool objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmConstantPool::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmConstantPool** objectArray = (SgAsmJvmConstantPool**) &(SgAsmJvmConstantPool::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmConstantPool::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmConstantPool::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmConstantPool::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmConstantPool*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmConstantPool::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmConstantPool::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmConstantPool::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmConstantPool objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmConstantPool::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmConstantPool** objectArray = (SgAsmJvmConstantPool**) &(SgAsmJvmConstantPool::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmConstantPool::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmConstantPool::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmConstantPool object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmConstantPool object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmConstantPool::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmConstantPool objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmConstantPool::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmConstantPool** objectArray = (SgAsmJvmConstantPool**) &(SgAsmJvmConstantPool::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmConstantPool::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmConstantPool::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmConstantPool found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmConstantPool::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmConstantPool::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmConstantPool** objectArray = (SgAsmJvmConstantPool**) &(SgAsmJvmConstantPool::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmConstantPool::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmConstantPool::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmConstantPool::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmConstantPool);

     return memory;
   }



/* #line 213696 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmFieldTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmFieldTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmFieldTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmFieldTable** objectArray = (SgAsmJvmFieldTable**) &(SgAsmJvmFieldTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmFieldTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmFieldTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmFieldTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmFieldTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmFieldTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmFieldTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmFieldTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmFieldTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmFieldTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmFieldTable** objectArray = (SgAsmJvmFieldTable**) &(SgAsmJvmFieldTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmFieldTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmFieldTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmFieldTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmFieldTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmFieldTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmFieldTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmFieldTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmFieldTable** objectArray = (SgAsmJvmFieldTable**) &(SgAsmJvmFieldTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmFieldTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmFieldTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmFieldTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmFieldTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmFieldTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmFieldTable** objectArray = (SgAsmJvmFieldTable**) &(SgAsmJvmFieldTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmFieldTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmFieldTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmFieldTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmFieldTable);

     return memory;
   }



/* #line 213928 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmMethodTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmMethodTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmMethodTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmMethodTable** objectArray = (SgAsmJvmMethodTable**) &(SgAsmJvmMethodTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmMethodTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmMethodTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmMethodTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmMethodTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmMethodTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmMethodTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmMethodTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmMethodTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmMethodTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmMethodTable** objectArray = (SgAsmJvmMethodTable**) &(SgAsmJvmMethodTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmMethodTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmMethodTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmMethodTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmMethodTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmMethodTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmMethodTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmMethodTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmMethodTable** objectArray = (SgAsmJvmMethodTable**) &(SgAsmJvmMethodTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmMethodTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmMethodTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmMethodTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmMethodTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmMethodTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmMethodTable** objectArray = (SgAsmJvmMethodTable**) &(SgAsmJvmMethodTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmMethodTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmMethodTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmMethodTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmMethodTable);

     return memory;
   }



/* #line 214160 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmLEEntryTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmLEEntryTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEEntryTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEEntryTable** objectArray = (SgAsmLEEntryTable**) &(SgAsmLEEntryTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLEEntryTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmLEEntryTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmLEEntryTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmLEEntryTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmLEEntryTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLEEntryTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmLEEntryTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLEEntryTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEEntryTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEEntryTable** objectArray = (SgAsmLEEntryTable**) &(SgAsmLEEntryTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLEEntryTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLEEntryTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmLEEntryTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmLEEntryTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmLEEntryTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLEEntryTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEEntryTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEEntryTable** objectArray = (SgAsmLEEntryTable**) &(SgAsmLEEntryTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmLEEntryTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmLEEntryTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmLEEntryTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLEEntryTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmLEEntryTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmLEEntryTable** objectArray = (SgAsmLEEntryTable**) &(SgAsmLEEntryTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmLEEntryTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmLEEntryTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLEEntryTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmLEEntryTable);

     return memory;
   }



/* #line 214392 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmLENameTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmLENameTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLENameTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLENameTable** objectArray = (SgAsmLENameTable**) &(SgAsmLENameTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLENameTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmLENameTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmLENameTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmLENameTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmLENameTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLENameTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmLENameTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLENameTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLENameTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLENameTable** objectArray = (SgAsmLENameTable**) &(SgAsmLENameTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLENameTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLENameTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmLENameTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmLENameTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmLENameTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLENameTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLENameTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLENameTable** objectArray = (SgAsmLENameTable**) &(SgAsmLENameTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmLENameTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmLENameTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmLENameTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLENameTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmLENameTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmLENameTable** objectArray = (SgAsmLENameTable**) &(SgAsmLENameTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmLENameTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmLENameTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLENameTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmLENameTable);

     return memory;
   }



/* #line 214624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmLEPageTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmLEPageTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEPageTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEPageTable** objectArray = (SgAsmLEPageTable**) &(SgAsmLEPageTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLEPageTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmLEPageTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmLEPageTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmLEPageTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmLEPageTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLEPageTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmLEPageTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLEPageTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEPageTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEPageTable** objectArray = (SgAsmLEPageTable**) &(SgAsmLEPageTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLEPageTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLEPageTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmLEPageTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmLEPageTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmLEPageTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLEPageTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEPageTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEPageTable** objectArray = (SgAsmLEPageTable**) &(SgAsmLEPageTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmLEPageTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmLEPageTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmLEPageTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLEPageTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmLEPageTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmLEPageTable** objectArray = (SgAsmLEPageTable**) &(SgAsmLEPageTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmLEPageTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmLEPageTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLEPageTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmLEPageTable);

     return memory;
   }



/* #line 214856 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmLERelocTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmLERelocTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLERelocTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLERelocTable** objectArray = (SgAsmLERelocTable**) &(SgAsmLERelocTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLERelocTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmLERelocTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmLERelocTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmLERelocTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmLERelocTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLERelocTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmLERelocTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLERelocTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLERelocTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLERelocTable** objectArray = (SgAsmLERelocTable**) &(SgAsmLERelocTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLERelocTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLERelocTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmLERelocTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmLERelocTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmLERelocTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLERelocTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLERelocTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLERelocTable** objectArray = (SgAsmLERelocTable**) &(SgAsmLERelocTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmLERelocTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmLERelocTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmLERelocTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLERelocTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmLERelocTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmLERelocTable** objectArray = (SgAsmLERelocTable**) &(SgAsmLERelocTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmLERelocTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmLERelocTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLERelocTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmLERelocTable);

     return memory;
   }



/* #line 215088 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmLESection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmLESection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLESection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLESection** objectArray = (SgAsmLESection**) &(SgAsmLESection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLESection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmLESection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmLESection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmLESection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmLESection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLESection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmLESection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLESection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLESection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLESection** objectArray = (SgAsmLESection**) &(SgAsmLESection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLESection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLESection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmLESection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmLESection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmLESection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLESection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLESection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLESection** objectArray = (SgAsmLESection**) &(SgAsmLESection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmLESection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmLESection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmLESection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLESection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmLESection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmLESection** objectArray = (SgAsmLESection**) &(SgAsmLESection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmLESection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmLESection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLESection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmLESection);

     return memory;
   }



/* #line 215320 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmLESectionTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmLESectionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLESectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLESectionTable** objectArray = (SgAsmLESectionTable**) &(SgAsmLESectionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLESectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmLESectionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmLESectionTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmLESectionTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmLESectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLESectionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmLESectionTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLESectionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLESectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLESectionTable** objectArray = (SgAsmLESectionTable**) &(SgAsmLESectionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLESectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLESectionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmLESectionTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmLESectionTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmLESectionTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLESectionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLESectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLESectionTable** objectArray = (SgAsmLESectionTable**) &(SgAsmLESectionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmLESectionTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmLESectionTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmLESectionTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLESectionTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmLESectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmLESectionTable** objectArray = (SgAsmLESectionTable**) &(SgAsmLESectionTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmLESectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmLESectionTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLESectionTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmLESectionTable);

     return memory;
   }



/* #line 215552 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmNEEntryTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmNEEntryTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEEntryTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEEntryTable** objectArray = (SgAsmNEEntryTable**) &(SgAsmNEEntryTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNEEntryTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmNEEntryTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmNEEntryTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmNEEntryTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmNEEntryTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNEEntryTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmNEEntryTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNEEntryTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEEntryTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEEntryTable** objectArray = (SgAsmNEEntryTable**) &(SgAsmNEEntryTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNEEntryTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNEEntryTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmNEEntryTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmNEEntryTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmNEEntryTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNEEntryTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEEntryTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEEntryTable** objectArray = (SgAsmNEEntryTable**) &(SgAsmNEEntryTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmNEEntryTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmNEEntryTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmNEEntryTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNEEntryTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmNEEntryTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmNEEntryTable** objectArray = (SgAsmNEEntryTable**) &(SgAsmNEEntryTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmNEEntryTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmNEEntryTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNEEntryTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmNEEntryTable);

     return memory;
   }



/* #line 215784 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmNEModuleTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmNEModuleTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEModuleTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEModuleTable** objectArray = (SgAsmNEModuleTable**) &(SgAsmNEModuleTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNEModuleTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmNEModuleTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmNEModuleTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmNEModuleTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmNEModuleTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNEModuleTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmNEModuleTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNEModuleTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEModuleTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEModuleTable** objectArray = (SgAsmNEModuleTable**) &(SgAsmNEModuleTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNEModuleTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNEModuleTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmNEModuleTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmNEModuleTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmNEModuleTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNEModuleTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEModuleTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEModuleTable** objectArray = (SgAsmNEModuleTable**) &(SgAsmNEModuleTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmNEModuleTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmNEModuleTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmNEModuleTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNEModuleTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmNEModuleTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmNEModuleTable** objectArray = (SgAsmNEModuleTable**) &(SgAsmNEModuleTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmNEModuleTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmNEModuleTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNEModuleTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmNEModuleTable);

     return memory;
   }



/* #line 216016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmNENameTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmNENameTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNENameTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNENameTable** objectArray = (SgAsmNENameTable**) &(SgAsmNENameTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNENameTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmNENameTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmNENameTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmNENameTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmNENameTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNENameTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmNENameTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNENameTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNENameTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNENameTable** objectArray = (SgAsmNENameTable**) &(SgAsmNENameTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNENameTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNENameTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmNENameTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmNENameTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmNENameTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNENameTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNENameTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNENameTable** objectArray = (SgAsmNENameTable**) &(SgAsmNENameTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmNENameTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmNENameTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmNENameTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNENameTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmNENameTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmNENameTable** objectArray = (SgAsmNENameTable**) &(SgAsmNENameTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmNENameTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmNENameTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNENameTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmNENameTable);

     return memory;
   }



/* #line 216248 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmNERelocTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmNERelocTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNERelocTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNERelocTable** objectArray = (SgAsmNERelocTable**) &(SgAsmNERelocTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNERelocTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmNERelocTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmNERelocTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmNERelocTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmNERelocTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNERelocTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmNERelocTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNERelocTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNERelocTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNERelocTable** objectArray = (SgAsmNERelocTable**) &(SgAsmNERelocTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNERelocTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNERelocTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmNERelocTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmNERelocTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmNERelocTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNERelocTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNERelocTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNERelocTable** objectArray = (SgAsmNERelocTable**) &(SgAsmNERelocTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmNERelocTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmNERelocTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmNERelocTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNERelocTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmNERelocTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmNERelocTable** objectArray = (SgAsmNERelocTable**) &(SgAsmNERelocTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmNERelocTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmNERelocTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNERelocTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmNERelocTable);

     return memory;
   }



/* #line 216480 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmNESection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmNESection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNESection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNESection** objectArray = (SgAsmNESection**) &(SgAsmNESection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNESection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmNESection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmNESection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmNESection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmNESection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNESection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmNESection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNESection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNESection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNESection** objectArray = (SgAsmNESection**) &(SgAsmNESection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNESection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNESection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmNESection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmNESection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmNESection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNESection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNESection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNESection** objectArray = (SgAsmNESection**) &(SgAsmNESection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmNESection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmNESection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmNESection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNESection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmNESection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmNESection** objectArray = (SgAsmNESection**) &(SgAsmNESection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmNESection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmNESection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNESection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmNESection);

     return memory;
   }



/* #line 216712 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmNESectionTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmNESectionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNESectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNESectionTable** objectArray = (SgAsmNESectionTable**) &(SgAsmNESectionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNESectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmNESectionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmNESectionTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmNESectionTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmNESectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNESectionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmNESectionTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNESectionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNESectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNESectionTable** objectArray = (SgAsmNESectionTable**) &(SgAsmNESectionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNESectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNESectionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmNESectionTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmNESectionTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmNESectionTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNESectionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNESectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNESectionTable** objectArray = (SgAsmNESectionTable**) &(SgAsmNESectionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmNESectionTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmNESectionTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmNESectionTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNESectionTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmNESectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmNESectionTable** objectArray = (SgAsmNESectionTable**) &(SgAsmNESectionTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmNESectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmNESectionTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNESectionTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmNESectionTable);

     return memory;
   }



/* #line 216944 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmNEStringTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmNEStringTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEStringTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEStringTable** objectArray = (SgAsmNEStringTable**) &(SgAsmNEStringTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNEStringTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmNEStringTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmNEStringTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmNEStringTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmNEStringTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNEStringTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmNEStringTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNEStringTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEStringTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEStringTable** objectArray = (SgAsmNEStringTable**) &(SgAsmNEStringTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNEStringTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNEStringTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmNEStringTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmNEStringTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmNEStringTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNEStringTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEStringTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEStringTable** objectArray = (SgAsmNEStringTable**) &(SgAsmNEStringTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmNEStringTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmNEStringTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmNEStringTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNEStringTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmNEStringTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmNEStringTable** objectArray = (SgAsmNEStringTable**) &(SgAsmNEStringTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmNEStringTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmNEStringTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNEStringTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmNEStringTable);

     return memory;
   }



/* #line 217176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPESection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPESection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPESection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPESection** objectArray = (SgAsmPESection**) &(SgAsmPESection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPESection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPESection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPESection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPESection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPESection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPESection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPESection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPESection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPESection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPESection** objectArray = (SgAsmPESection**) &(SgAsmPESection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPESection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPESection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPESection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPESection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPESection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPESection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPESection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPESection** objectArray = (SgAsmPESection**) &(SgAsmPESection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPESection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPESection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPESection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPESection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPESection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPESection** objectArray = (SgAsmPESection**) &(SgAsmPESection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPESection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPESection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPESection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPESection);

     return memory;
   }



/* #line 217408 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPEExportSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPEExportSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEExportSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEExportSection** objectArray = (SgAsmPEExportSection**) &(SgAsmPEExportSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEExportSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPEExportSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPEExportSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPEExportSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPEExportSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEExportSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPEExportSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEExportSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEExportSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEExportSection** objectArray = (SgAsmPEExportSection**) &(SgAsmPEExportSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEExportSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEExportSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPEExportSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPEExportSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPEExportSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEExportSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEExportSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEExportSection** objectArray = (SgAsmPEExportSection**) &(SgAsmPEExportSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPEExportSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPEExportSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPEExportSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEExportSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPEExportSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPEExportSection** objectArray = (SgAsmPEExportSection**) &(SgAsmPEExportSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPEExportSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPEExportSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEExportSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPEExportSection);

     return memory;
   }



/* #line 217640 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPEImportSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPEImportSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportSection** objectArray = (SgAsmPEImportSection**) &(SgAsmPEImportSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEImportSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPEImportSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPEImportSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPEImportSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPEImportSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEImportSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPEImportSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEImportSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportSection** objectArray = (SgAsmPEImportSection**) &(SgAsmPEImportSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEImportSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEImportSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPEImportSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPEImportSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPEImportSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEImportSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportSection** objectArray = (SgAsmPEImportSection**) &(SgAsmPEImportSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPEImportSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPEImportSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPEImportSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEImportSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPEImportSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPEImportSection** objectArray = (SgAsmPEImportSection**) &(SgAsmPEImportSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPEImportSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPEImportSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEImportSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPEImportSection);

     return memory;
   }



/* #line 217872 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPEStringSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPEStringSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEStringSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEStringSection** objectArray = (SgAsmPEStringSection**) &(SgAsmPEStringSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEStringSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPEStringSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPEStringSection::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPEStringSection*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPEStringSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEStringSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPEStringSection::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEStringSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEStringSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEStringSection** objectArray = (SgAsmPEStringSection**) &(SgAsmPEStringSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEStringSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEStringSection::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPEStringSection object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPEStringSection object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPEStringSection::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEStringSection objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEStringSection::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEStringSection** objectArray = (SgAsmPEStringSection**) &(SgAsmPEStringSection::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPEStringSection::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPEStringSection::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPEStringSection found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEStringSection::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPEStringSection::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPEStringSection** objectArray = (SgAsmPEStringSection**) &(SgAsmPEStringSection::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPEStringSection::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPEStringSection::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEStringSection::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPEStringSection);

     return memory;
   }



/* #line 218104 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPESectionTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPESectionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPESectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPESectionTable** objectArray = (SgAsmPESectionTable**) &(SgAsmPESectionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPESectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPESectionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPESectionTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPESectionTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPESectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPESectionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPESectionTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPESectionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPESectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPESectionTable** objectArray = (SgAsmPESectionTable**) &(SgAsmPESectionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPESectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPESectionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPESectionTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPESectionTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPESectionTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPESectionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPESectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPESectionTable** objectArray = (SgAsmPESectionTable**) &(SgAsmPESectionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPESectionTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPESectionTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPESectionTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPESectionTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPESectionTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPESectionTable** objectArray = (SgAsmPESectionTable**) &(SgAsmPESectionTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPESectionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPESectionTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPESectionTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPESectionTable);

     return memory;
   }



/* #line 218336 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmGenericSectionList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmGenericSectionList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericSectionList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericSectionList** objectArray = (SgAsmGenericSectionList**) &(SgAsmGenericSectionList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericSectionList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmGenericSectionList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmGenericSectionList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmGenericSectionList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmGenericSectionList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericSectionList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmGenericSectionList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericSectionList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericSectionList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericSectionList** objectArray = (SgAsmGenericSectionList**) &(SgAsmGenericSectionList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericSectionList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericSectionList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmGenericSectionList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmGenericSectionList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmGenericSectionList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericSectionList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericSectionList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericSectionList** objectArray = (SgAsmGenericSectionList**) &(SgAsmGenericSectionList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmGenericSectionList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmGenericSectionList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmGenericSectionList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericSectionList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmGenericSectionList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmGenericSectionList** objectArray = (SgAsmGenericSectionList**) &(SgAsmGenericSectionList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmGenericSectionList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmGenericSectionList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericSectionList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmGenericSectionList);

     return memory;
   }



/* #line 218568 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmGenericString::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmGenericString objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericString::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericString** objectArray = (SgAsmGenericString**) &(SgAsmGenericString::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmGenericString::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmGenericString::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmGenericString*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmGenericString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericString::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmGenericString::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericString objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericString::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericString** objectArray = (SgAsmGenericString**) &(SgAsmGenericString::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericString::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmGenericString object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmGenericString object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmGenericString::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericString objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericString::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericString** objectArray = (SgAsmGenericString**) &(SgAsmGenericString::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmGenericString::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmGenericString::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmGenericString found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericString::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmGenericString::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmGenericString** objectArray = (SgAsmGenericString**) &(SgAsmGenericString::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmGenericString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmGenericString::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericString::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmGenericString);

     return memory;
   }



/* #line 218800 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBasicString::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBasicString objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBasicString::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBasicString** objectArray = (SgAsmBasicString**) &(SgAsmBasicString::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBasicString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBasicString::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBasicString::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBasicString*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBasicString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBasicString::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBasicString::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBasicString objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBasicString::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBasicString** objectArray = (SgAsmBasicString**) &(SgAsmBasicString::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBasicString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBasicString::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBasicString object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBasicString object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBasicString::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBasicString objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBasicString::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBasicString** objectArray = (SgAsmBasicString**) &(SgAsmBasicString::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBasicString::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBasicString::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBasicString found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBasicString::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBasicString::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBasicString** objectArray = (SgAsmBasicString**) &(SgAsmBasicString::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBasicString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBasicString::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBasicString::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBasicString);

     return memory;
   }



/* #line 219032 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmStoredString::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmStoredString objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStoredString::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStoredString** objectArray = (SgAsmStoredString**) &(SgAsmStoredString::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmStoredString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmStoredString::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmStoredString::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmStoredString*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmStoredString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmStoredString::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmStoredString::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmStoredString objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStoredString::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStoredString** objectArray = (SgAsmStoredString**) &(SgAsmStoredString::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmStoredString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmStoredString::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmStoredString object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmStoredString object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmStoredString::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmStoredString objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStoredString::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStoredString** objectArray = (SgAsmStoredString**) &(SgAsmStoredString::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmStoredString::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmStoredString::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmStoredString found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmStoredString::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmStoredString::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmStoredString** objectArray = (SgAsmStoredString**) &(SgAsmStoredString::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmStoredString::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmStoredString::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmStoredString::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmStoredString);

     return memory;
   }



/* #line 219264 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmGenericStrtab::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmGenericStrtab objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericStrtab::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericStrtab** objectArray = (SgAsmGenericStrtab**) &(SgAsmGenericStrtab::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericStrtab::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmGenericStrtab::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmGenericStrtab::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmGenericStrtab*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmGenericStrtab::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericStrtab::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmGenericStrtab::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericStrtab objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericStrtab::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericStrtab** objectArray = (SgAsmGenericStrtab**) &(SgAsmGenericStrtab::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericStrtab::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericStrtab::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmGenericStrtab object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmGenericStrtab object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmGenericStrtab::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericStrtab objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericStrtab::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericStrtab** objectArray = (SgAsmGenericStrtab**) &(SgAsmGenericStrtab::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmGenericStrtab::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmGenericStrtab::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmGenericStrtab found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericStrtab::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmGenericStrtab::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmGenericStrtab** objectArray = (SgAsmGenericStrtab**) &(SgAsmGenericStrtab::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmGenericStrtab::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmGenericStrtab::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericStrtab::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmGenericStrtab);

     return memory;
   }



/* #line 219496 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCoffStrtab::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCoffStrtab objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCoffStrtab::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCoffStrtab** objectArray = (SgAsmCoffStrtab**) &(SgAsmCoffStrtab::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCoffStrtab::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCoffStrtab::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCoffStrtab::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCoffStrtab*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCoffStrtab::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCoffStrtab::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCoffStrtab::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCoffStrtab objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCoffStrtab::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCoffStrtab** objectArray = (SgAsmCoffStrtab**) &(SgAsmCoffStrtab::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCoffStrtab::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCoffStrtab::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCoffStrtab object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCoffStrtab object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCoffStrtab::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCoffStrtab objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCoffStrtab::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCoffStrtab** objectArray = (SgAsmCoffStrtab**) &(SgAsmCoffStrtab::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCoffStrtab::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCoffStrtab::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCoffStrtab found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCoffStrtab::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCoffStrtab::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCoffStrtab** objectArray = (SgAsmCoffStrtab**) &(SgAsmCoffStrtab::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCoffStrtab::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCoffStrtab::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCoffStrtab::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCoffStrtab);

     return memory;
   }



/* #line 219728 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfStrtab::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfStrtab objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfStrtab::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfStrtab** objectArray = (SgAsmElfStrtab**) &(SgAsmElfStrtab::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfStrtab::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfStrtab::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfStrtab::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfStrtab*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfStrtab::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfStrtab::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfStrtab::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfStrtab objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfStrtab::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfStrtab** objectArray = (SgAsmElfStrtab**) &(SgAsmElfStrtab::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfStrtab::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfStrtab::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfStrtab object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfStrtab object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfStrtab::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfStrtab objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfStrtab::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfStrtab** objectArray = (SgAsmElfStrtab**) &(SgAsmElfStrtab::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfStrtab::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfStrtab::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfStrtab found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfStrtab::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfStrtab::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfStrtab** objectArray = (SgAsmElfStrtab**) &(SgAsmElfStrtab::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfStrtab::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfStrtab::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfStrtab::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfStrtab);

     return memory;
   }



/* #line 219960 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmGenericSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmGenericSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericSymbol** objectArray = (SgAsmGenericSymbol**) &(SgAsmGenericSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmGenericSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmGenericSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmGenericSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmGenericSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmGenericSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericSymbol** objectArray = (SgAsmGenericSymbol**) &(SgAsmGenericSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmGenericSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmGenericSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmGenericSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericSymbol** objectArray = (SgAsmGenericSymbol**) &(SgAsmGenericSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmGenericSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmGenericSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmGenericSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmGenericSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmGenericSymbol** objectArray = (SgAsmGenericSymbol**) &(SgAsmGenericSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmGenericSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmGenericSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmGenericSymbol);

     return memory;
   }



/* #line 220192 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCoffSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCoffSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCoffSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCoffSymbol** objectArray = (SgAsmCoffSymbol**) &(SgAsmCoffSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCoffSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCoffSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCoffSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCoffSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCoffSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCoffSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCoffSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCoffSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCoffSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCoffSymbol** objectArray = (SgAsmCoffSymbol**) &(SgAsmCoffSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCoffSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCoffSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCoffSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCoffSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCoffSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCoffSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCoffSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCoffSymbol** objectArray = (SgAsmCoffSymbol**) &(SgAsmCoffSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCoffSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCoffSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCoffSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCoffSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCoffSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCoffSymbol** objectArray = (SgAsmCoffSymbol**) &(SgAsmCoffSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCoffSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCoffSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCoffSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCoffSymbol);

     return memory;
   }



/* #line 220424 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmElfSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmElfSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymbol** objectArray = (SgAsmElfSymbol**) &(SgAsmElfSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmElfSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmElfSymbol::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmElfSymbol*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmElfSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmElfSymbol::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymbol** objectArray = (SgAsmElfSymbol**) &(SgAsmElfSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmElfSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmElfSymbol::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmElfSymbol object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmElfSymbol object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmElfSymbol::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmElfSymbol objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmElfSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmElfSymbol** objectArray = (SgAsmElfSymbol**) &(SgAsmElfSymbol::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmElfSymbol::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmElfSymbol::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmElfSymbol found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymbol::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmElfSymbol::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmElfSymbol** objectArray = (SgAsmElfSymbol**) &(SgAsmElfSymbol::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmElfSymbol::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmElfSymbol::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmElfSymbol::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmElfSymbol);

     return memory;
   }



/* #line 220656 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmGenericSymbolList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmGenericSymbolList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericSymbolList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericSymbolList** objectArray = (SgAsmGenericSymbolList**) &(SgAsmGenericSymbolList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericSymbolList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmGenericSymbolList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmGenericSymbolList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmGenericSymbolList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmGenericSymbolList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericSymbolList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmGenericSymbolList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericSymbolList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericSymbolList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericSymbolList** objectArray = (SgAsmGenericSymbolList**) &(SgAsmGenericSymbolList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericSymbolList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericSymbolList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmGenericSymbolList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmGenericSymbolList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmGenericSymbolList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericSymbolList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericSymbolList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericSymbolList** objectArray = (SgAsmGenericSymbolList**) &(SgAsmGenericSymbolList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmGenericSymbolList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmGenericSymbolList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmGenericSymbolList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericSymbolList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmGenericSymbolList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmGenericSymbolList** objectArray = (SgAsmGenericSymbolList**) &(SgAsmGenericSymbolList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmGenericSymbolList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmGenericSymbolList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericSymbolList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmGenericSymbolList);

     return memory;
   }



/* #line 220888 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmLEEntryPoint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmLEEntryPoint objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEEntryPoint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEEntryPoint** objectArray = (SgAsmLEEntryPoint**) &(SgAsmLEEntryPoint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLEEntryPoint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmLEEntryPoint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmLEEntryPoint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmLEEntryPoint*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmLEEntryPoint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLEEntryPoint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmLEEntryPoint::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLEEntryPoint objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEEntryPoint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEEntryPoint** objectArray = (SgAsmLEEntryPoint**) &(SgAsmLEEntryPoint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLEEntryPoint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLEEntryPoint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmLEEntryPoint object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmLEEntryPoint object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmLEEntryPoint::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLEEntryPoint objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEEntryPoint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEEntryPoint** objectArray = (SgAsmLEEntryPoint**) &(SgAsmLEEntryPoint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmLEEntryPoint::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmLEEntryPoint::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmLEEntryPoint found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLEEntryPoint::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmLEEntryPoint::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmLEEntryPoint** objectArray = (SgAsmLEEntryPoint**) &(SgAsmLEEntryPoint::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmLEEntryPoint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmLEEntryPoint::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLEEntryPoint::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmLEEntryPoint);

     return memory;
   }



/* #line 221120 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmLEPageTableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmLEPageTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEPageTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEPageTableEntry** objectArray = (SgAsmLEPageTableEntry**) &(SgAsmLEPageTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLEPageTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmLEPageTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmLEPageTableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmLEPageTableEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmLEPageTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLEPageTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmLEPageTableEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLEPageTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEPageTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEPageTableEntry** objectArray = (SgAsmLEPageTableEntry**) &(SgAsmLEPageTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLEPageTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLEPageTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmLEPageTableEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmLEPageTableEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmLEPageTableEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLEPageTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLEPageTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLEPageTableEntry** objectArray = (SgAsmLEPageTableEntry**) &(SgAsmLEPageTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmLEPageTableEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmLEPageTableEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmLEPageTableEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLEPageTableEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmLEPageTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmLEPageTableEntry** objectArray = (SgAsmLEPageTableEntry**) &(SgAsmLEPageTableEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmLEPageTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmLEPageTableEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLEPageTableEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmLEPageTableEntry);

     return memory;
   }



/* #line 221352 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmLESectionTableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmLESectionTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLESectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLESectionTableEntry** objectArray = (SgAsmLESectionTableEntry**) &(SgAsmLESectionTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLESectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmLESectionTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmLESectionTableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmLESectionTableEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmLESectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLESectionTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmLESectionTableEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLESectionTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLESectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLESectionTableEntry** objectArray = (SgAsmLESectionTableEntry**) &(SgAsmLESectionTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmLESectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmLESectionTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmLESectionTableEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmLESectionTableEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmLESectionTableEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmLESectionTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmLESectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmLESectionTableEntry** objectArray = (SgAsmLESectionTableEntry**) &(SgAsmLESectionTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmLESectionTableEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmLESectionTableEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmLESectionTableEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLESectionTableEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmLESectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmLESectionTableEntry** objectArray = (SgAsmLESectionTableEntry**) &(SgAsmLESectionTableEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmLESectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmLESectionTableEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmLESectionTableEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmLESectionTableEntry);

     return memory;
   }



/* #line 221584 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmNEEntryPoint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmNEEntryPoint objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEEntryPoint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEEntryPoint** objectArray = (SgAsmNEEntryPoint**) &(SgAsmNEEntryPoint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNEEntryPoint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmNEEntryPoint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmNEEntryPoint::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmNEEntryPoint*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmNEEntryPoint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNEEntryPoint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmNEEntryPoint::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNEEntryPoint objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEEntryPoint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEEntryPoint** objectArray = (SgAsmNEEntryPoint**) &(SgAsmNEEntryPoint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNEEntryPoint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNEEntryPoint::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmNEEntryPoint object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmNEEntryPoint object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmNEEntryPoint::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNEEntryPoint objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNEEntryPoint::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNEEntryPoint** objectArray = (SgAsmNEEntryPoint**) &(SgAsmNEEntryPoint::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmNEEntryPoint::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmNEEntryPoint::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmNEEntryPoint found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNEEntryPoint::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmNEEntryPoint::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmNEEntryPoint** objectArray = (SgAsmNEEntryPoint**) &(SgAsmNEEntryPoint::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmNEEntryPoint::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmNEEntryPoint::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNEEntryPoint::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmNEEntryPoint);

     return memory;
   }



/* #line 221816 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmNERelocEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmNERelocEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNERelocEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNERelocEntry** objectArray = (SgAsmNERelocEntry**) &(SgAsmNERelocEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNERelocEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmNERelocEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmNERelocEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmNERelocEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmNERelocEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNERelocEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmNERelocEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNERelocEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNERelocEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNERelocEntry** objectArray = (SgAsmNERelocEntry**) &(SgAsmNERelocEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNERelocEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNERelocEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmNERelocEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmNERelocEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmNERelocEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNERelocEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNERelocEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNERelocEntry** objectArray = (SgAsmNERelocEntry**) &(SgAsmNERelocEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmNERelocEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmNERelocEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmNERelocEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNERelocEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmNERelocEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmNERelocEntry** objectArray = (SgAsmNERelocEntry**) &(SgAsmNERelocEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmNERelocEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmNERelocEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNERelocEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmNERelocEntry);

     return memory;
   }



/* #line 222048 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmNESectionTableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmNESectionTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNESectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNESectionTableEntry** objectArray = (SgAsmNESectionTableEntry**) &(SgAsmNESectionTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNESectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmNESectionTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmNESectionTableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmNESectionTableEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmNESectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNESectionTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmNESectionTableEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNESectionTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNESectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNESectionTableEntry** objectArray = (SgAsmNESectionTableEntry**) &(SgAsmNESectionTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNESectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNESectionTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmNESectionTableEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmNESectionTableEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmNESectionTableEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNESectionTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNESectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNESectionTableEntry** objectArray = (SgAsmNESectionTableEntry**) &(SgAsmNESectionTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmNESectionTableEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmNESectionTableEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmNESectionTableEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNESectionTableEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmNESectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmNESectionTableEntry** objectArray = (SgAsmNESectionTableEntry**) &(SgAsmNESectionTableEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmNESectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmNESectionTableEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNESectionTableEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmNESectionTableEntry);

     return memory;
   }



/* #line 222280 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPEExportDirectory::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPEExportDirectory objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEExportDirectory::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEExportDirectory** objectArray = (SgAsmPEExportDirectory**) &(SgAsmPEExportDirectory::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEExportDirectory::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPEExportDirectory::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPEExportDirectory::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPEExportDirectory*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPEExportDirectory::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEExportDirectory::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPEExportDirectory::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEExportDirectory objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEExportDirectory::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEExportDirectory** objectArray = (SgAsmPEExportDirectory**) &(SgAsmPEExportDirectory::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEExportDirectory::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEExportDirectory::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPEExportDirectory object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPEExportDirectory object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPEExportDirectory::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEExportDirectory objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEExportDirectory::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEExportDirectory** objectArray = (SgAsmPEExportDirectory**) &(SgAsmPEExportDirectory::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPEExportDirectory::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPEExportDirectory::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPEExportDirectory found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEExportDirectory::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPEExportDirectory::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPEExportDirectory** objectArray = (SgAsmPEExportDirectory**) &(SgAsmPEExportDirectory::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPEExportDirectory::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPEExportDirectory::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEExportDirectory::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPEExportDirectory);

     return memory;
   }



/* #line 222512 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPEExportEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPEExportEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEExportEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEExportEntry** objectArray = (SgAsmPEExportEntry**) &(SgAsmPEExportEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEExportEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPEExportEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPEExportEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPEExportEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPEExportEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEExportEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPEExportEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEExportEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEExportEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEExportEntry** objectArray = (SgAsmPEExportEntry**) &(SgAsmPEExportEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEExportEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEExportEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPEExportEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPEExportEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPEExportEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEExportEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEExportEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEExportEntry** objectArray = (SgAsmPEExportEntry**) &(SgAsmPEExportEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPEExportEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPEExportEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPEExportEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEExportEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPEExportEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPEExportEntry** objectArray = (SgAsmPEExportEntry**) &(SgAsmPEExportEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPEExportEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPEExportEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEExportEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPEExportEntry);

     return memory;
   }



/* #line 222744 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPEExportEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPEExportEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEExportEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEExportEntryList** objectArray = (SgAsmPEExportEntryList**) &(SgAsmPEExportEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEExportEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPEExportEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPEExportEntryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPEExportEntryList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPEExportEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEExportEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPEExportEntryList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEExportEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEExportEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEExportEntryList** objectArray = (SgAsmPEExportEntryList**) &(SgAsmPEExportEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEExportEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEExportEntryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPEExportEntryList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPEExportEntryList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPEExportEntryList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEExportEntryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEExportEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEExportEntryList** objectArray = (SgAsmPEExportEntryList**) &(SgAsmPEExportEntryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPEExportEntryList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPEExportEntryList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPEExportEntryList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEExportEntryList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPEExportEntryList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPEExportEntryList** objectArray = (SgAsmPEExportEntryList**) &(SgAsmPEExportEntryList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPEExportEntryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPEExportEntryList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEExportEntryList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPEExportEntryList);

     return memory;
   }



/* #line 222976 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPEImportDirectory::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPEImportDirectory objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportDirectory::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportDirectory** objectArray = (SgAsmPEImportDirectory**) &(SgAsmPEImportDirectory::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEImportDirectory::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPEImportDirectory::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPEImportDirectory::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPEImportDirectory*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPEImportDirectory::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEImportDirectory::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPEImportDirectory::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEImportDirectory objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportDirectory::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportDirectory** objectArray = (SgAsmPEImportDirectory**) &(SgAsmPEImportDirectory::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEImportDirectory::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEImportDirectory::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPEImportDirectory object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPEImportDirectory object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPEImportDirectory::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEImportDirectory objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportDirectory::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportDirectory** objectArray = (SgAsmPEImportDirectory**) &(SgAsmPEImportDirectory::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPEImportDirectory::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPEImportDirectory::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPEImportDirectory found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEImportDirectory::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPEImportDirectory::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPEImportDirectory** objectArray = (SgAsmPEImportDirectory**) &(SgAsmPEImportDirectory::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPEImportDirectory::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPEImportDirectory::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEImportDirectory::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPEImportDirectory);

     return memory;
   }



/* #line 223208 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPEImportDirectoryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPEImportDirectoryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportDirectoryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportDirectoryList** objectArray = (SgAsmPEImportDirectoryList**) &(SgAsmPEImportDirectoryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEImportDirectoryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPEImportDirectoryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPEImportDirectoryList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPEImportDirectoryList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPEImportDirectoryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEImportDirectoryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPEImportDirectoryList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEImportDirectoryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportDirectoryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportDirectoryList** objectArray = (SgAsmPEImportDirectoryList**) &(SgAsmPEImportDirectoryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEImportDirectoryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEImportDirectoryList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPEImportDirectoryList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPEImportDirectoryList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPEImportDirectoryList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEImportDirectoryList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportDirectoryList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportDirectoryList** objectArray = (SgAsmPEImportDirectoryList**) &(SgAsmPEImportDirectoryList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPEImportDirectoryList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPEImportDirectoryList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPEImportDirectoryList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEImportDirectoryList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPEImportDirectoryList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPEImportDirectoryList** objectArray = (SgAsmPEImportDirectoryList**) &(SgAsmPEImportDirectoryList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPEImportDirectoryList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPEImportDirectoryList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEImportDirectoryList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPEImportDirectoryList);

     return memory;
   }



/* #line 223440 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPEImportItem::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPEImportItem objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportItem::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportItem** objectArray = (SgAsmPEImportItem**) &(SgAsmPEImportItem::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEImportItem::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPEImportItem::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPEImportItem::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPEImportItem*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPEImportItem::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEImportItem::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPEImportItem::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEImportItem objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportItem::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportItem** objectArray = (SgAsmPEImportItem**) &(SgAsmPEImportItem::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEImportItem::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEImportItem::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPEImportItem object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPEImportItem object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPEImportItem::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEImportItem objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportItem::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportItem** objectArray = (SgAsmPEImportItem**) &(SgAsmPEImportItem::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPEImportItem::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPEImportItem::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPEImportItem found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEImportItem::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPEImportItem::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPEImportItem** objectArray = (SgAsmPEImportItem**) &(SgAsmPEImportItem::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPEImportItem::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPEImportItem::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEImportItem::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPEImportItem);

     return memory;
   }



/* #line 223672 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPEImportItemList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPEImportItemList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportItemList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportItemList** objectArray = (SgAsmPEImportItemList**) &(SgAsmPEImportItemList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEImportItemList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPEImportItemList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPEImportItemList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPEImportItemList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPEImportItemList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEImportItemList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPEImportItemList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEImportItemList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportItemList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportItemList** objectArray = (SgAsmPEImportItemList**) &(SgAsmPEImportItemList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPEImportItemList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPEImportItemList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPEImportItemList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPEImportItemList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPEImportItemList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPEImportItemList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPEImportItemList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPEImportItemList** objectArray = (SgAsmPEImportItemList**) &(SgAsmPEImportItemList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPEImportItemList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPEImportItemList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPEImportItemList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEImportItemList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPEImportItemList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPEImportItemList** objectArray = (SgAsmPEImportItemList**) &(SgAsmPEImportItemList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPEImportItemList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPEImportItemList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPEImportItemList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPEImportItemList);

     return memory;
   }



/* #line 223904 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPERVASizePair::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPERVASizePair objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPERVASizePair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPERVASizePair** objectArray = (SgAsmPERVASizePair**) &(SgAsmPERVASizePair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPERVASizePair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPERVASizePair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPERVASizePair::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPERVASizePair*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPERVASizePair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPERVASizePair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPERVASizePair::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPERVASizePair objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPERVASizePair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPERVASizePair** objectArray = (SgAsmPERVASizePair**) &(SgAsmPERVASizePair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPERVASizePair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPERVASizePair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPERVASizePair object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPERVASizePair object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPERVASizePair::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPERVASizePair objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPERVASizePair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPERVASizePair** objectArray = (SgAsmPERVASizePair**) &(SgAsmPERVASizePair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPERVASizePair::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPERVASizePair::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPERVASizePair found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPERVASizePair::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPERVASizePair::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPERVASizePair** objectArray = (SgAsmPERVASizePair**) &(SgAsmPERVASizePair::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPERVASizePair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPERVASizePair::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPERVASizePair::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPERVASizePair);

     return memory;
   }



/* #line 224136 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPERVASizePairList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPERVASizePairList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPERVASizePairList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPERVASizePairList** objectArray = (SgAsmPERVASizePairList**) &(SgAsmPERVASizePairList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPERVASizePairList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPERVASizePairList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPERVASizePairList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPERVASizePairList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPERVASizePairList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPERVASizePairList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPERVASizePairList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPERVASizePairList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPERVASizePairList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPERVASizePairList** objectArray = (SgAsmPERVASizePairList**) &(SgAsmPERVASizePairList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPERVASizePairList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPERVASizePairList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPERVASizePairList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPERVASizePairList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPERVASizePairList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPERVASizePairList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPERVASizePairList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPERVASizePairList** objectArray = (SgAsmPERVASizePairList**) &(SgAsmPERVASizePairList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPERVASizePairList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPERVASizePairList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPERVASizePairList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPERVASizePairList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPERVASizePairList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPERVASizePairList** objectArray = (SgAsmPERVASizePairList**) &(SgAsmPERVASizePairList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPERVASizePairList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPERVASizePairList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPERVASizePairList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPERVASizePairList);

     return memory;
   }



/* #line 224368 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPESectionTableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPESectionTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPESectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPESectionTableEntry** objectArray = (SgAsmPESectionTableEntry**) &(SgAsmPESectionTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPESectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPESectionTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPESectionTableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPESectionTableEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPESectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPESectionTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPESectionTableEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPESectionTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPESectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPESectionTableEntry** objectArray = (SgAsmPESectionTableEntry**) &(SgAsmPESectionTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPESectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPESectionTableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPESectionTableEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPESectionTableEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPESectionTableEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPESectionTableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPESectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPESectionTableEntry** objectArray = (SgAsmPESectionTableEntry**) &(SgAsmPESectionTableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPESectionTableEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPESectionTableEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPESectionTableEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPESectionTableEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPESectionTableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPESectionTableEntry** objectArray = (SgAsmPESectionTableEntry**) &(SgAsmPESectionTableEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPESectionTableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPESectionTableEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPESectionTableEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPESectionTableEntry);

     return memory;
   }



/* #line 224600 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmStringStorage::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmStringStorage objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStringStorage::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStringStorage** objectArray = (SgAsmStringStorage**) &(SgAsmStringStorage::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmStringStorage::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmStringStorage::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmStringStorage::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmStringStorage*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmStringStorage::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmStringStorage::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmStringStorage::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmStringStorage objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStringStorage::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStringStorage** objectArray = (SgAsmStringStorage**) &(SgAsmStringStorage::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmStringStorage::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmStringStorage::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmStringStorage object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmStringStorage object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmStringStorage::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmStringStorage objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStringStorage::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStringStorage** objectArray = (SgAsmStringStorage**) &(SgAsmStringStorage::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmStringStorage::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmStringStorage::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmStringStorage found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmStringStorage::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmStringStorage::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmStringStorage** objectArray = (SgAsmStringStorage**) &(SgAsmStringStorage::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmStringStorage::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmStringStorage::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmStringStorage::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmStringStorage);

     return memory;
   }



/* #line 224832 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmExpression** objectArray = (SgAsmExpression**) &(SgAsmExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmExpression** objectArray = (SgAsmExpression**) &(SgAsmExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmExpression** objectArray = (SgAsmExpression**) &(SgAsmExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmExpression** objectArray = (SgAsmExpression**) &(SgAsmExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmExpression);

     return memory;
   }



/* #line 225064 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmAarch32Coprocessor::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmAarch32Coprocessor objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmAarch32Coprocessor::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmAarch32Coprocessor** objectArray = (SgAsmAarch32Coprocessor**) &(SgAsmAarch32Coprocessor::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmAarch32Coprocessor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmAarch32Coprocessor::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmAarch32Coprocessor::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmAarch32Coprocessor*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmAarch32Coprocessor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmAarch32Coprocessor::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmAarch32Coprocessor::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmAarch32Coprocessor objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmAarch32Coprocessor::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmAarch32Coprocessor** objectArray = (SgAsmAarch32Coprocessor**) &(SgAsmAarch32Coprocessor::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmAarch32Coprocessor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmAarch32Coprocessor::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmAarch32Coprocessor object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmAarch32Coprocessor object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmAarch32Coprocessor::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmAarch32Coprocessor objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmAarch32Coprocessor::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmAarch32Coprocessor** objectArray = (SgAsmAarch32Coprocessor**) &(SgAsmAarch32Coprocessor::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmAarch32Coprocessor::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmAarch32Coprocessor::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmAarch32Coprocessor found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmAarch32Coprocessor::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmAarch32Coprocessor::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmAarch32Coprocessor** objectArray = (SgAsmAarch32Coprocessor**) &(SgAsmAarch32Coprocessor::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmAarch32Coprocessor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmAarch32Coprocessor::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmAarch32Coprocessor::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmAarch32Coprocessor);

     return memory;
   }



/* #line 225296 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryExpression** objectArray = (SgAsmBinaryExpression**) &(SgAsmBinaryExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryExpression** objectArray = (SgAsmBinaryExpression**) &(SgAsmBinaryExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryExpression** objectArray = (SgAsmBinaryExpression**) &(SgAsmBinaryExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryExpression** objectArray = (SgAsmBinaryExpression**) &(SgAsmBinaryExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryExpression);

     return memory;
   }



/* #line 225528 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryAdd::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryAdd objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryAdd::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryAdd** objectArray = (SgAsmBinaryAdd**) &(SgAsmBinaryAdd::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryAdd::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryAdd::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryAdd::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryAdd*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryAdd::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryAdd::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryAdd::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryAdd objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryAdd::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryAdd** objectArray = (SgAsmBinaryAdd**) &(SgAsmBinaryAdd::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryAdd::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryAdd::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryAdd object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryAdd object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryAdd::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryAdd objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryAdd::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryAdd** objectArray = (SgAsmBinaryAdd**) &(SgAsmBinaryAdd::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryAdd::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryAdd::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryAdd found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryAdd::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryAdd::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryAdd** objectArray = (SgAsmBinaryAdd**) &(SgAsmBinaryAdd::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryAdd::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryAdd::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryAdd::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryAdd);

     return memory;
   }



/* #line 225760 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryAsr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryAsr objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryAsr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryAsr** objectArray = (SgAsmBinaryAsr**) &(SgAsmBinaryAsr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryAsr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryAsr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryAsr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryAsr*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryAsr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryAsr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryAsr::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryAsr objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryAsr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryAsr** objectArray = (SgAsmBinaryAsr**) &(SgAsmBinaryAsr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryAsr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryAsr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryAsr object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryAsr object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryAsr::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryAsr objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryAsr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryAsr** objectArray = (SgAsmBinaryAsr**) &(SgAsmBinaryAsr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryAsr::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryAsr::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryAsr found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryAsr::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryAsr::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryAsr** objectArray = (SgAsmBinaryAsr**) &(SgAsmBinaryAsr::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryAsr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryAsr::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryAsr::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryAsr);

     return memory;
   }



/* #line 225992 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryConcat::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryConcat objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryConcat::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryConcat** objectArray = (SgAsmBinaryConcat**) &(SgAsmBinaryConcat::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryConcat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryConcat::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryConcat::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryConcat*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryConcat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryConcat::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryConcat::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryConcat objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryConcat::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryConcat** objectArray = (SgAsmBinaryConcat**) &(SgAsmBinaryConcat::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryConcat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryConcat::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryConcat object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryConcat object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryConcat::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryConcat objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryConcat::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryConcat** objectArray = (SgAsmBinaryConcat**) &(SgAsmBinaryConcat::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryConcat::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryConcat::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryConcat found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryConcat::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryConcat::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryConcat** objectArray = (SgAsmBinaryConcat**) &(SgAsmBinaryConcat::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryConcat::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryConcat::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryConcat::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryConcat);

     return memory;
   }



/* #line 226224 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryDivide::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryDivide objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryDivide::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryDivide** objectArray = (SgAsmBinaryDivide**) &(SgAsmBinaryDivide::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryDivide::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryDivide::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryDivide::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryDivide*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryDivide::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryDivide::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryDivide::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryDivide objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryDivide::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryDivide** objectArray = (SgAsmBinaryDivide**) &(SgAsmBinaryDivide::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryDivide::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryDivide::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryDivide object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryDivide object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryDivide::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryDivide objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryDivide::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryDivide** objectArray = (SgAsmBinaryDivide**) &(SgAsmBinaryDivide::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryDivide::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryDivide::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryDivide found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryDivide::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryDivide::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryDivide** objectArray = (SgAsmBinaryDivide**) &(SgAsmBinaryDivide::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryDivide::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryDivide::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryDivide::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryDivide);

     return memory;
   }



/* #line 226456 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryLsl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryLsl objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryLsl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryLsl** objectArray = (SgAsmBinaryLsl**) &(SgAsmBinaryLsl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryLsl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryLsl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryLsl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryLsl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryLsl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryLsl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryLsl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryLsl objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryLsl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryLsl** objectArray = (SgAsmBinaryLsl**) &(SgAsmBinaryLsl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryLsl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryLsl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryLsl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryLsl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryLsl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryLsl objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryLsl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryLsl** objectArray = (SgAsmBinaryLsl**) &(SgAsmBinaryLsl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryLsl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryLsl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryLsl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryLsl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryLsl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryLsl** objectArray = (SgAsmBinaryLsl**) &(SgAsmBinaryLsl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryLsl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryLsl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryLsl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryLsl);

     return memory;
   }



/* #line 226688 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryLsr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryLsr objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryLsr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryLsr** objectArray = (SgAsmBinaryLsr**) &(SgAsmBinaryLsr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryLsr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryLsr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryLsr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryLsr*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryLsr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryLsr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryLsr::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryLsr objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryLsr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryLsr** objectArray = (SgAsmBinaryLsr**) &(SgAsmBinaryLsr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryLsr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryLsr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryLsr object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryLsr object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryLsr::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryLsr objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryLsr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryLsr** objectArray = (SgAsmBinaryLsr**) &(SgAsmBinaryLsr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryLsr::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryLsr::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryLsr found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryLsr::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryLsr::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryLsr** objectArray = (SgAsmBinaryLsr**) &(SgAsmBinaryLsr::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryLsr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryLsr::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryLsr::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryLsr);

     return memory;
   }



/* #line 226920 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryMod::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryMod objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryMod::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryMod** objectArray = (SgAsmBinaryMod**) &(SgAsmBinaryMod::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryMod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryMod::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryMod::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryMod*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryMod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryMod::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryMod::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryMod objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryMod::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryMod** objectArray = (SgAsmBinaryMod**) &(SgAsmBinaryMod::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryMod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryMod::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryMod object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryMod object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryMod::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryMod objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryMod::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryMod** objectArray = (SgAsmBinaryMod**) &(SgAsmBinaryMod::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryMod::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryMod::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryMod found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryMod::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryMod::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryMod** objectArray = (SgAsmBinaryMod**) &(SgAsmBinaryMod::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryMod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryMod::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryMod::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryMod);

     return memory;
   }



/* #line 227152 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryMsl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryMsl objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryMsl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryMsl** objectArray = (SgAsmBinaryMsl**) &(SgAsmBinaryMsl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryMsl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryMsl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryMsl::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryMsl*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryMsl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryMsl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryMsl::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryMsl objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryMsl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryMsl** objectArray = (SgAsmBinaryMsl**) &(SgAsmBinaryMsl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryMsl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryMsl::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryMsl object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryMsl object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryMsl::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryMsl objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryMsl::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryMsl** objectArray = (SgAsmBinaryMsl**) &(SgAsmBinaryMsl::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryMsl::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryMsl::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryMsl found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryMsl::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryMsl::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryMsl** objectArray = (SgAsmBinaryMsl**) &(SgAsmBinaryMsl::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryMsl::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryMsl::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryMsl::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryMsl);

     return memory;
   }



/* #line 227384 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryMultiply::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryMultiply objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryMultiply::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryMultiply** objectArray = (SgAsmBinaryMultiply**) &(SgAsmBinaryMultiply::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryMultiply::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryMultiply::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryMultiply::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryMultiply*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryMultiply::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryMultiply::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryMultiply::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryMultiply objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryMultiply::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryMultiply** objectArray = (SgAsmBinaryMultiply**) &(SgAsmBinaryMultiply::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryMultiply::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryMultiply::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryMultiply object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryMultiply object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryMultiply::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryMultiply objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryMultiply::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryMultiply** objectArray = (SgAsmBinaryMultiply**) &(SgAsmBinaryMultiply::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryMultiply::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryMultiply::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryMultiply found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryMultiply::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryMultiply::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryMultiply** objectArray = (SgAsmBinaryMultiply**) &(SgAsmBinaryMultiply::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryMultiply::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryMultiply::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryMultiply::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryMultiply);

     return memory;
   }



/* #line 227616 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryPostupdate::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryPostupdate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryPostupdate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryPostupdate** objectArray = (SgAsmBinaryPostupdate**) &(SgAsmBinaryPostupdate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryPostupdate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryPostupdate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryPostupdate::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryPostupdate*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryPostupdate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryPostupdate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryPostupdate::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryPostupdate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryPostupdate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryPostupdate** objectArray = (SgAsmBinaryPostupdate**) &(SgAsmBinaryPostupdate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryPostupdate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryPostupdate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryPostupdate object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryPostupdate object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryPostupdate::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryPostupdate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryPostupdate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryPostupdate** objectArray = (SgAsmBinaryPostupdate**) &(SgAsmBinaryPostupdate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryPostupdate::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryPostupdate::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryPostupdate found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryPostupdate::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryPostupdate::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryPostupdate** objectArray = (SgAsmBinaryPostupdate**) &(SgAsmBinaryPostupdate::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryPostupdate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryPostupdate::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryPostupdate::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryPostupdate);

     return memory;
   }



/* #line 227848 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryPreupdate::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryPreupdate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryPreupdate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryPreupdate** objectArray = (SgAsmBinaryPreupdate**) &(SgAsmBinaryPreupdate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryPreupdate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryPreupdate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryPreupdate::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryPreupdate*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryPreupdate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryPreupdate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryPreupdate::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryPreupdate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryPreupdate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryPreupdate** objectArray = (SgAsmBinaryPreupdate**) &(SgAsmBinaryPreupdate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryPreupdate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryPreupdate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryPreupdate object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryPreupdate object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryPreupdate::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryPreupdate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryPreupdate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryPreupdate** objectArray = (SgAsmBinaryPreupdate**) &(SgAsmBinaryPreupdate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryPreupdate::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryPreupdate::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryPreupdate found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryPreupdate::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryPreupdate::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryPreupdate** objectArray = (SgAsmBinaryPreupdate**) &(SgAsmBinaryPreupdate::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryPreupdate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryPreupdate::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryPreupdate::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryPreupdate);

     return memory;
   }



/* #line 228080 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinaryRor::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinaryRor objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryRor::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryRor** objectArray = (SgAsmBinaryRor**) &(SgAsmBinaryRor::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryRor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinaryRor::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinaryRor::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinaryRor*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinaryRor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryRor::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinaryRor::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryRor objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryRor::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryRor** objectArray = (SgAsmBinaryRor**) &(SgAsmBinaryRor::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinaryRor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinaryRor::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinaryRor object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinaryRor object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinaryRor::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinaryRor objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinaryRor::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinaryRor** objectArray = (SgAsmBinaryRor**) &(SgAsmBinaryRor::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinaryRor::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinaryRor::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinaryRor found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryRor::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinaryRor::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinaryRor** objectArray = (SgAsmBinaryRor**) &(SgAsmBinaryRor::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinaryRor::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinaryRor::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinaryRor::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinaryRor);

     return memory;
   }



/* #line 228312 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBinarySubtract::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBinarySubtract objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinarySubtract::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinarySubtract** objectArray = (SgAsmBinarySubtract**) &(SgAsmBinarySubtract::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinarySubtract::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBinarySubtract::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBinarySubtract::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBinarySubtract*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBinarySubtract::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinarySubtract::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBinarySubtract::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinarySubtract objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinarySubtract::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinarySubtract** objectArray = (SgAsmBinarySubtract**) &(SgAsmBinarySubtract::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBinarySubtract::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBinarySubtract::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBinarySubtract object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBinarySubtract object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBinarySubtract::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBinarySubtract objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBinarySubtract::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBinarySubtract** objectArray = (SgAsmBinarySubtract**) &(SgAsmBinarySubtract::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBinarySubtract::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBinarySubtract::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBinarySubtract found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinarySubtract::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBinarySubtract::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBinarySubtract** objectArray = (SgAsmBinarySubtract**) &(SgAsmBinarySubtract::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBinarySubtract::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBinarySubtract::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBinarySubtract::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBinarySubtract);

     return memory;
   }



/* #line 228544 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmByteOrder::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmByteOrder objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmByteOrder::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmByteOrder** objectArray = (SgAsmByteOrder**) &(SgAsmByteOrder::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmByteOrder::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmByteOrder::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmByteOrder::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmByteOrder*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmByteOrder::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmByteOrder::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmByteOrder::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmByteOrder objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmByteOrder::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmByteOrder** objectArray = (SgAsmByteOrder**) &(SgAsmByteOrder::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmByteOrder::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmByteOrder::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmByteOrder object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmByteOrder object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmByteOrder::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmByteOrder objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmByteOrder::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmByteOrder** objectArray = (SgAsmByteOrder**) &(SgAsmByteOrder::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmByteOrder::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmByteOrder::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmByteOrder found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmByteOrder::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmByteOrder::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmByteOrder** objectArray = (SgAsmByteOrder**) &(SgAsmByteOrder::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmByteOrder::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmByteOrder::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmByteOrder::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmByteOrder);

     return memory;
   }



/* #line 228776 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCommonSubExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCommonSubExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCommonSubExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCommonSubExpression** objectArray = (SgAsmCommonSubExpression**) &(SgAsmCommonSubExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCommonSubExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCommonSubExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCommonSubExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCommonSubExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCommonSubExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCommonSubExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCommonSubExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCommonSubExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCommonSubExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCommonSubExpression** objectArray = (SgAsmCommonSubExpression**) &(SgAsmCommonSubExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCommonSubExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCommonSubExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCommonSubExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCommonSubExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCommonSubExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCommonSubExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCommonSubExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCommonSubExpression** objectArray = (SgAsmCommonSubExpression**) &(SgAsmCommonSubExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCommonSubExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCommonSubExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCommonSubExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCommonSubExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCommonSubExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCommonSubExpression** objectArray = (SgAsmCommonSubExpression**) &(SgAsmCommonSubExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCommonSubExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCommonSubExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCommonSubExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCommonSubExpression);

     return memory;
   }



/* #line 229008 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmControlFlagsExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmControlFlagsExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmControlFlagsExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmControlFlagsExpression** objectArray = (SgAsmControlFlagsExpression**) &(SgAsmControlFlagsExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmControlFlagsExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmControlFlagsExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmControlFlagsExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmControlFlagsExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmControlFlagsExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmControlFlagsExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmControlFlagsExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmControlFlagsExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmControlFlagsExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmControlFlagsExpression** objectArray = (SgAsmControlFlagsExpression**) &(SgAsmControlFlagsExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmControlFlagsExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmControlFlagsExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmControlFlagsExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmControlFlagsExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmControlFlagsExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmControlFlagsExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmControlFlagsExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmControlFlagsExpression** objectArray = (SgAsmControlFlagsExpression**) &(SgAsmControlFlagsExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmControlFlagsExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmControlFlagsExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmControlFlagsExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmControlFlagsExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmControlFlagsExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmControlFlagsExpression** objectArray = (SgAsmControlFlagsExpression**) &(SgAsmControlFlagsExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmControlFlagsExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmControlFlagsExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmControlFlagsExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmControlFlagsExpression);

     return memory;
   }



/* #line 229240 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmExprListExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmExprListExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmExprListExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmExprListExp** objectArray = (SgAsmExprListExp**) &(SgAsmExprListExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmExprListExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmExprListExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmExprListExp::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmExprListExp*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmExprListExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmExprListExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmExprListExp::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmExprListExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmExprListExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmExprListExp** objectArray = (SgAsmExprListExp**) &(SgAsmExprListExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmExprListExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmExprListExp::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmExprListExp object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmExprListExp object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmExprListExp::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmExprListExp objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmExprListExp::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmExprListExp** objectArray = (SgAsmExprListExp**) &(SgAsmExprListExp::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmExprListExp::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmExprListExp::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmExprListExp found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmExprListExp::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmExprListExp::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmExprListExp** objectArray = (SgAsmExprListExp**) &(SgAsmExprListExp::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmExprListExp::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmExprListExp::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmExprListExp::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmExprListExp);

     return memory;
   }



/* #line 229472 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmMemoryReferenceExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmMemoryReferenceExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmMemoryReferenceExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmMemoryReferenceExpression** objectArray = (SgAsmMemoryReferenceExpression**) &(SgAsmMemoryReferenceExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmMemoryReferenceExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmMemoryReferenceExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmMemoryReferenceExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmMemoryReferenceExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmMemoryReferenceExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmMemoryReferenceExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmMemoryReferenceExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmMemoryReferenceExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmMemoryReferenceExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmMemoryReferenceExpression** objectArray = (SgAsmMemoryReferenceExpression**) &(SgAsmMemoryReferenceExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmMemoryReferenceExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmMemoryReferenceExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmMemoryReferenceExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmMemoryReferenceExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmMemoryReferenceExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmMemoryReferenceExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmMemoryReferenceExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmMemoryReferenceExpression** objectArray = (SgAsmMemoryReferenceExpression**) &(SgAsmMemoryReferenceExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmMemoryReferenceExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmMemoryReferenceExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmMemoryReferenceExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmMemoryReferenceExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmMemoryReferenceExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmMemoryReferenceExpression** objectArray = (SgAsmMemoryReferenceExpression**) &(SgAsmMemoryReferenceExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmMemoryReferenceExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmMemoryReferenceExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmMemoryReferenceExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmMemoryReferenceExpression);

     return memory;
   }



/* #line 229704 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmRegisterNames::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmRegisterNames objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmRegisterNames::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmRegisterNames** objectArray = (SgAsmRegisterNames**) &(SgAsmRegisterNames::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmRegisterNames::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmRegisterNames::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmRegisterNames::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmRegisterNames*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmRegisterNames::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmRegisterNames::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmRegisterNames::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmRegisterNames objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmRegisterNames::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmRegisterNames** objectArray = (SgAsmRegisterNames**) &(SgAsmRegisterNames::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmRegisterNames::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmRegisterNames::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmRegisterNames object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmRegisterNames object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmRegisterNames::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmRegisterNames objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmRegisterNames::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmRegisterNames** objectArray = (SgAsmRegisterNames**) &(SgAsmRegisterNames::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmRegisterNames::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmRegisterNames::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmRegisterNames found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmRegisterNames::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmRegisterNames::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmRegisterNames** objectArray = (SgAsmRegisterNames**) &(SgAsmRegisterNames::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmRegisterNames::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmRegisterNames::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmRegisterNames::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmRegisterNames);

     return memory;
   }



/* #line 229936 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmRegisterReferenceExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmRegisterReferenceExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmRegisterReferenceExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmRegisterReferenceExpression** objectArray = (SgAsmRegisterReferenceExpression**) &(SgAsmRegisterReferenceExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmRegisterReferenceExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmRegisterReferenceExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmRegisterReferenceExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmRegisterReferenceExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmRegisterReferenceExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmRegisterReferenceExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmRegisterReferenceExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmRegisterReferenceExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmRegisterReferenceExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmRegisterReferenceExpression** objectArray = (SgAsmRegisterReferenceExpression**) &(SgAsmRegisterReferenceExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmRegisterReferenceExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmRegisterReferenceExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmRegisterReferenceExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmRegisterReferenceExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmRegisterReferenceExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmRegisterReferenceExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmRegisterReferenceExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmRegisterReferenceExpression** objectArray = (SgAsmRegisterReferenceExpression**) &(SgAsmRegisterReferenceExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmRegisterReferenceExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmRegisterReferenceExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmRegisterReferenceExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmRegisterReferenceExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmRegisterReferenceExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmRegisterReferenceExpression** objectArray = (SgAsmRegisterReferenceExpression**) &(SgAsmRegisterReferenceExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmRegisterReferenceExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmRegisterReferenceExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmRegisterReferenceExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmRegisterReferenceExpression);

     return memory;
   }



/* #line 230168 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmDirectRegisterExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmDirectRegisterExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDirectRegisterExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDirectRegisterExpression** objectArray = (SgAsmDirectRegisterExpression**) &(SgAsmDirectRegisterExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDirectRegisterExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmDirectRegisterExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmDirectRegisterExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmDirectRegisterExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmDirectRegisterExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDirectRegisterExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmDirectRegisterExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDirectRegisterExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDirectRegisterExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDirectRegisterExpression** objectArray = (SgAsmDirectRegisterExpression**) &(SgAsmDirectRegisterExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmDirectRegisterExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmDirectRegisterExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmDirectRegisterExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmDirectRegisterExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmDirectRegisterExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmDirectRegisterExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmDirectRegisterExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmDirectRegisterExpression** objectArray = (SgAsmDirectRegisterExpression**) &(SgAsmDirectRegisterExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmDirectRegisterExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmDirectRegisterExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmDirectRegisterExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDirectRegisterExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmDirectRegisterExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmDirectRegisterExpression** objectArray = (SgAsmDirectRegisterExpression**) &(SgAsmDirectRegisterExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmDirectRegisterExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmDirectRegisterExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmDirectRegisterExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmDirectRegisterExpression);

     return memory;
   }



/* #line 230400 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmIndirectRegisterExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmIndirectRegisterExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmIndirectRegisterExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmIndirectRegisterExpression** objectArray = (SgAsmIndirectRegisterExpression**) &(SgAsmIndirectRegisterExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmIndirectRegisterExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmIndirectRegisterExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmIndirectRegisterExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmIndirectRegisterExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmIndirectRegisterExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmIndirectRegisterExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmIndirectRegisterExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmIndirectRegisterExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmIndirectRegisterExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmIndirectRegisterExpression** objectArray = (SgAsmIndirectRegisterExpression**) &(SgAsmIndirectRegisterExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmIndirectRegisterExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmIndirectRegisterExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmIndirectRegisterExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmIndirectRegisterExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmIndirectRegisterExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmIndirectRegisterExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmIndirectRegisterExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmIndirectRegisterExpression** objectArray = (SgAsmIndirectRegisterExpression**) &(SgAsmIndirectRegisterExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmIndirectRegisterExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmIndirectRegisterExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmIndirectRegisterExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmIndirectRegisterExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmIndirectRegisterExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmIndirectRegisterExpression** objectArray = (SgAsmIndirectRegisterExpression**) &(SgAsmIndirectRegisterExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmIndirectRegisterExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmIndirectRegisterExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmIndirectRegisterExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmIndirectRegisterExpression);

     return memory;
   }



/* #line 230632 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmRiscOperation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmRiscOperation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmRiscOperation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmRiscOperation** objectArray = (SgAsmRiscOperation**) &(SgAsmRiscOperation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmRiscOperation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmRiscOperation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmRiscOperation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmRiscOperation*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmRiscOperation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmRiscOperation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmRiscOperation::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmRiscOperation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmRiscOperation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmRiscOperation** objectArray = (SgAsmRiscOperation**) &(SgAsmRiscOperation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmRiscOperation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmRiscOperation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmRiscOperation object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmRiscOperation object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmRiscOperation::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmRiscOperation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmRiscOperation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmRiscOperation** objectArray = (SgAsmRiscOperation**) &(SgAsmRiscOperation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmRiscOperation::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmRiscOperation::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmRiscOperation found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmRiscOperation::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmRiscOperation::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmRiscOperation** objectArray = (SgAsmRiscOperation**) &(SgAsmRiscOperation::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmRiscOperation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmRiscOperation::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmRiscOperation::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmRiscOperation);

     return memory;
   }



/* #line 230864 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmStackExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmStackExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStackExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStackExpression** objectArray = (SgAsmStackExpression**) &(SgAsmStackExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmStackExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmStackExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmStackExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmStackExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmStackExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmStackExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmStackExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmStackExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStackExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStackExpression** objectArray = (SgAsmStackExpression**) &(SgAsmStackExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmStackExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmStackExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmStackExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmStackExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmStackExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmStackExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStackExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStackExpression** objectArray = (SgAsmStackExpression**) &(SgAsmStackExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmStackExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmStackExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmStackExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmStackExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmStackExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmStackExpression** objectArray = (SgAsmStackExpression**) &(SgAsmStackExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmStackExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmStackExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmStackExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmStackExpression);

     return memory;
   }



/* #line 231096 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmUnaryExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmUnaryExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryExpression** objectArray = (SgAsmUnaryExpression**) &(SgAsmUnaryExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnaryExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmUnaryExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmUnaryExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmUnaryExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmUnaryExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnaryExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmUnaryExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnaryExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryExpression** objectArray = (SgAsmUnaryExpression**) &(SgAsmUnaryExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnaryExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnaryExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmUnaryExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmUnaryExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmUnaryExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnaryExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryExpression** objectArray = (SgAsmUnaryExpression**) &(SgAsmUnaryExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmUnaryExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmUnaryExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmUnaryExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnaryExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmUnaryExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmUnaryExpression** objectArray = (SgAsmUnaryExpression**) &(SgAsmUnaryExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmUnaryExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmUnaryExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnaryExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmUnaryExpression);

     return memory;
   }



/* #line 231328 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmUnaryMinus::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmUnaryMinus objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryMinus::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryMinus** objectArray = (SgAsmUnaryMinus**) &(SgAsmUnaryMinus::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnaryMinus::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmUnaryMinus::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmUnaryMinus::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmUnaryMinus*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmUnaryMinus::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnaryMinus::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmUnaryMinus::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnaryMinus objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryMinus::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryMinus** objectArray = (SgAsmUnaryMinus**) &(SgAsmUnaryMinus::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnaryMinus::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnaryMinus::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmUnaryMinus object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmUnaryMinus object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmUnaryMinus::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnaryMinus objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryMinus::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryMinus** objectArray = (SgAsmUnaryMinus**) &(SgAsmUnaryMinus::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmUnaryMinus::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmUnaryMinus::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmUnaryMinus found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnaryMinus::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmUnaryMinus::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmUnaryMinus** objectArray = (SgAsmUnaryMinus**) &(SgAsmUnaryMinus::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmUnaryMinus::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmUnaryMinus::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnaryMinus::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmUnaryMinus);

     return memory;
   }



/* #line 231560 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmUnaryPlus::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmUnaryPlus objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryPlus::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryPlus** objectArray = (SgAsmUnaryPlus**) &(SgAsmUnaryPlus::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnaryPlus::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmUnaryPlus::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmUnaryPlus::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmUnaryPlus*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmUnaryPlus::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnaryPlus::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmUnaryPlus::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnaryPlus objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryPlus::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryPlus** objectArray = (SgAsmUnaryPlus**) &(SgAsmUnaryPlus::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnaryPlus::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnaryPlus::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmUnaryPlus object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmUnaryPlus object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmUnaryPlus::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnaryPlus objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryPlus::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryPlus** objectArray = (SgAsmUnaryPlus**) &(SgAsmUnaryPlus::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmUnaryPlus::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmUnaryPlus::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmUnaryPlus found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnaryPlus::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmUnaryPlus::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmUnaryPlus** objectArray = (SgAsmUnaryPlus**) &(SgAsmUnaryPlus::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmUnaryPlus::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmUnaryPlus::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnaryPlus::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmUnaryPlus);

     return memory;
   }



/* #line 231792 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmUnaryRrx::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmUnaryRrx objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryRrx::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryRrx** objectArray = (SgAsmUnaryRrx**) &(SgAsmUnaryRrx::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnaryRrx::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmUnaryRrx::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmUnaryRrx::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmUnaryRrx*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmUnaryRrx::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnaryRrx::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmUnaryRrx::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnaryRrx objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryRrx::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryRrx** objectArray = (SgAsmUnaryRrx**) &(SgAsmUnaryRrx::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnaryRrx::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnaryRrx::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmUnaryRrx object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmUnaryRrx object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmUnaryRrx::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnaryRrx objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryRrx::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryRrx** objectArray = (SgAsmUnaryRrx**) &(SgAsmUnaryRrx::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmUnaryRrx::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmUnaryRrx::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmUnaryRrx found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnaryRrx::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmUnaryRrx::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmUnaryRrx** objectArray = (SgAsmUnaryRrx**) &(SgAsmUnaryRrx::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmUnaryRrx::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmUnaryRrx::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnaryRrx::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmUnaryRrx);

     return memory;
   }



/* #line 232024 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmUnarySignedExtend::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmUnarySignedExtend objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnarySignedExtend::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnarySignedExtend** objectArray = (SgAsmUnarySignedExtend**) &(SgAsmUnarySignedExtend::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnarySignedExtend::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmUnarySignedExtend::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmUnarySignedExtend::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmUnarySignedExtend*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmUnarySignedExtend::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnarySignedExtend::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmUnarySignedExtend::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnarySignedExtend objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnarySignedExtend::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnarySignedExtend** objectArray = (SgAsmUnarySignedExtend**) &(SgAsmUnarySignedExtend::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnarySignedExtend::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnarySignedExtend::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmUnarySignedExtend object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmUnarySignedExtend object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmUnarySignedExtend::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnarySignedExtend objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnarySignedExtend::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnarySignedExtend** objectArray = (SgAsmUnarySignedExtend**) &(SgAsmUnarySignedExtend::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmUnarySignedExtend::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmUnarySignedExtend::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmUnarySignedExtend found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnarySignedExtend::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmUnarySignedExtend::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmUnarySignedExtend** objectArray = (SgAsmUnarySignedExtend**) &(SgAsmUnarySignedExtend::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmUnarySignedExtend::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmUnarySignedExtend::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnarySignedExtend::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmUnarySignedExtend);

     return memory;
   }



/* #line 232256 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmUnaryTruncate::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmUnaryTruncate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryTruncate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryTruncate** objectArray = (SgAsmUnaryTruncate**) &(SgAsmUnaryTruncate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnaryTruncate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmUnaryTruncate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmUnaryTruncate::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmUnaryTruncate*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmUnaryTruncate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnaryTruncate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmUnaryTruncate::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnaryTruncate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryTruncate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryTruncate** objectArray = (SgAsmUnaryTruncate**) &(SgAsmUnaryTruncate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnaryTruncate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnaryTruncate::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmUnaryTruncate object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmUnaryTruncate object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmUnaryTruncate::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnaryTruncate objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryTruncate::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryTruncate** objectArray = (SgAsmUnaryTruncate**) &(SgAsmUnaryTruncate::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmUnaryTruncate::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmUnaryTruncate::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmUnaryTruncate found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnaryTruncate::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmUnaryTruncate::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmUnaryTruncate** objectArray = (SgAsmUnaryTruncate**) &(SgAsmUnaryTruncate::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmUnaryTruncate::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmUnaryTruncate::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnaryTruncate::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmUnaryTruncate);

     return memory;
   }



/* #line 232488 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmUnaryUnsignedExtend::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmUnaryUnsignedExtend objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryUnsignedExtend::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryUnsignedExtend** objectArray = (SgAsmUnaryUnsignedExtend**) &(SgAsmUnaryUnsignedExtend::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnaryUnsignedExtend::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmUnaryUnsignedExtend::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmUnaryUnsignedExtend::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmUnaryUnsignedExtend*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmUnaryUnsignedExtend::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnaryUnsignedExtend::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmUnaryUnsignedExtend::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnaryUnsignedExtend objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryUnsignedExtend::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryUnsignedExtend** objectArray = (SgAsmUnaryUnsignedExtend**) &(SgAsmUnaryUnsignedExtend::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUnaryUnsignedExtend::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUnaryUnsignedExtend::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmUnaryUnsignedExtend object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmUnaryUnsignedExtend object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmUnaryUnsignedExtend::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUnaryUnsignedExtend objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUnaryUnsignedExtend::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUnaryUnsignedExtend** objectArray = (SgAsmUnaryUnsignedExtend**) &(SgAsmUnaryUnsignedExtend::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmUnaryUnsignedExtend::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmUnaryUnsignedExtend::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmUnaryUnsignedExtend found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnaryUnsignedExtend::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmUnaryUnsignedExtend::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmUnaryUnsignedExtend** objectArray = (SgAsmUnaryUnsignedExtend**) &(SgAsmUnaryUnsignedExtend::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmUnaryUnsignedExtend::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmUnaryUnsignedExtend::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUnaryUnsignedExtend::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmUnaryUnsignedExtend);

     return memory;
   }



/* #line 232720 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmValueExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmValueExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmValueExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmValueExpression** objectArray = (SgAsmValueExpression**) &(SgAsmValueExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmValueExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmValueExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmValueExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmValueExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmValueExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmValueExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmValueExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmValueExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmValueExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmValueExpression** objectArray = (SgAsmValueExpression**) &(SgAsmValueExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmValueExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmValueExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmValueExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmValueExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmValueExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmValueExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmValueExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmValueExpression** objectArray = (SgAsmValueExpression**) &(SgAsmValueExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmValueExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmValueExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmValueExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmValueExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmValueExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmValueExpression** objectArray = (SgAsmValueExpression**) &(SgAsmValueExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmValueExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmValueExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmValueExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmValueExpression);

     return memory;
   }



/* #line 232952 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmConstantExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmConstantExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmConstantExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmConstantExpression** objectArray = (SgAsmConstantExpression**) &(SgAsmConstantExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmConstantExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmConstantExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmConstantExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmConstantExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmConstantExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmConstantExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmConstantExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmConstantExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmConstantExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmConstantExpression** objectArray = (SgAsmConstantExpression**) &(SgAsmConstantExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmConstantExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmConstantExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmConstantExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmConstantExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmConstantExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmConstantExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmConstantExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmConstantExpression** objectArray = (SgAsmConstantExpression**) &(SgAsmConstantExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmConstantExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmConstantExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmConstantExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmConstantExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmConstantExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmConstantExpression** objectArray = (SgAsmConstantExpression**) &(SgAsmConstantExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmConstantExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmConstantExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmConstantExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmConstantExpression);

     return memory;
   }



/* #line 233184 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmFloatValueExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmFloatValueExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmFloatValueExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmFloatValueExpression** objectArray = (SgAsmFloatValueExpression**) &(SgAsmFloatValueExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmFloatValueExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmFloatValueExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmFloatValueExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmFloatValueExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmFloatValueExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmFloatValueExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmFloatValueExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmFloatValueExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmFloatValueExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmFloatValueExpression** objectArray = (SgAsmFloatValueExpression**) &(SgAsmFloatValueExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmFloatValueExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmFloatValueExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmFloatValueExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmFloatValueExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmFloatValueExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmFloatValueExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmFloatValueExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmFloatValueExpression** objectArray = (SgAsmFloatValueExpression**) &(SgAsmFloatValueExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmFloatValueExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmFloatValueExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmFloatValueExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmFloatValueExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmFloatValueExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmFloatValueExpression** objectArray = (SgAsmFloatValueExpression**) &(SgAsmFloatValueExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmFloatValueExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmFloatValueExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmFloatValueExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmFloatValueExpression);

     return memory;
   }



/* #line 233416 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmIntegerValueExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmIntegerValueExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmIntegerValueExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmIntegerValueExpression** objectArray = (SgAsmIntegerValueExpression**) &(SgAsmIntegerValueExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmIntegerValueExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmIntegerValueExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmIntegerValueExpression::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmIntegerValueExpression*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmIntegerValueExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmIntegerValueExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmIntegerValueExpression::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmIntegerValueExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmIntegerValueExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmIntegerValueExpression** objectArray = (SgAsmIntegerValueExpression**) &(SgAsmIntegerValueExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmIntegerValueExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmIntegerValueExpression::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmIntegerValueExpression object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmIntegerValueExpression object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmIntegerValueExpression::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmIntegerValueExpression objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmIntegerValueExpression::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmIntegerValueExpression** objectArray = (SgAsmIntegerValueExpression**) &(SgAsmIntegerValueExpression::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmIntegerValueExpression::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmIntegerValueExpression::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmIntegerValueExpression found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmIntegerValueExpression::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmIntegerValueExpression::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmIntegerValueExpression** objectArray = (SgAsmIntegerValueExpression**) &(SgAsmIntegerValueExpression::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmIntegerValueExpression::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmIntegerValueExpression::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmIntegerValueExpression::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmIntegerValueExpression);

     return memory;
   }



/* #line 233648 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmGenericFileList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmGenericFileList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericFileList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericFileList** objectArray = (SgAsmGenericFileList**) &(SgAsmGenericFileList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericFileList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmGenericFileList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmGenericFileList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmGenericFileList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmGenericFileList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericFileList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmGenericFileList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericFileList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericFileList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericFileList** objectArray = (SgAsmGenericFileList**) &(SgAsmGenericFileList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmGenericFileList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmGenericFileList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmGenericFileList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmGenericFileList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmGenericFileList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmGenericFileList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmGenericFileList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmGenericFileList** objectArray = (SgAsmGenericFileList**) &(SgAsmGenericFileList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmGenericFileList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmGenericFileList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmGenericFileList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericFileList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmGenericFileList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmGenericFileList** objectArray = (SgAsmGenericFileList**) &(SgAsmGenericFileList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmGenericFileList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmGenericFileList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmGenericFileList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmGenericFileList);

     return memory;
   }



/* #line 233880 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmInstructionList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmInstructionList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmInstructionList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmInstructionList** objectArray = (SgAsmInstructionList**) &(SgAsmInstructionList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmInstructionList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmInstructionList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmInstructionList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmInstructionList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmInstructionList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmInstructionList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmInstructionList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmInstructionList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmInstructionList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmInstructionList** objectArray = (SgAsmInstructionList**) &(SgAsmInstructionList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmInstructionList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmInstructionList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmInstructionList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmInstructionList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmInstructionList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmInstructionList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmInstructionList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmInstructionList** objectArray = (SgAsmInstructionList**) &(SgAsmInstructionList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmInstructionList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmInstructionList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmInstructionList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmInstructionList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmInstructionList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmInstructionList** objectArray = (SgAsmInstructionList**) &(SgAsmInstructionList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmInstructionList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmInstructionList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmInstructionList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmInstructionList);

     return memory;
   }



/* #line 234112 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmInterpretation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmInterpretation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmInterpretation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmInterpretation** objectArray = (SgAsmInterpretation**) &(SgAsmInterpretation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmInterpretation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmInterpretation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmInterpretation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmInterpretation*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmInterpretation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmInterpretation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmInterpretation::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmInterpretation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmInterpretation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmInterpretation** objectArray = (SgAsmInterpretation**) &(SgAsmInterpretation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmInterpretation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmInterpretation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmInterpretation object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmInterpretation object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmInterpretation::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmInterpretation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmInterpretation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmInterpretation** objectArray = (SgAsmInterpretation**) &(SgAsmInterpretation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmInterpretation::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmInterpretation::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmInterpretation found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmInterpretation::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmInterpretation::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmInterpretation** objectArray = (SgAsmInterpretation**) &(SgAsmInterpretation::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmInterpretation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmInterpretation::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmInterpretation::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmInterpretation);

     return memory;
   }



/* #line 234344 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmInterpretationList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmInterpretationList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmInterpretationList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmInterpretationList** objectArray = (SgAsmInterpretationList**) &(SgAsmInterpretationList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmInterpretationList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmInterpretationList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmInterpretationList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmInterpretationList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmInterpretationList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmInterpretationList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmInterpretationList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmInterpretationList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmInterpretationList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmInterpretationList** objectArray = (SgAsmInterpretationList**) &(SgAsmInterpretationList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmInterpretationList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmInterpretationList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmInterpretationList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmInterpretationList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmInterpretationList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmInterpretationList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmInterpretationList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmInterpretationList** objectArray = (SgAsmInterpretationList**) &(SgAsmInterpretationList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmInterpretationList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmInterpretationList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmInterpretationList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmInterpretationList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmInterpretationList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmInterpretationList** objectArray = (SgAsmInterpretationList**) &(SgAsmInterpretationList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmInterpretationList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmInterpretationList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmInterpretationList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmInterpretationList);

     return memory;
   }



/* #line 234576 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmNode::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmNode objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmNode** objectArray = (SgAsmJvmNode**) &(SgAsmJvmNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmNode::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmNode*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmNode::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmNode objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmNode** objectArray = (SgAsmJvmNode**) &(SgAsmJvmNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmNode::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmNode object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmNode object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmNode::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmNode objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmNode::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmNode** objectArray = (SgAsmJvmNode**) &(SgAsmJvmNode::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmNode::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmNode::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmNode found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmNode::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmNode::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmNode** objectArray = (SgAsmJvmNode**) &(SgAsmJvmNode::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmNode::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmNode::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmNode::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmNode);

     return memory;
   }



/* #line 234808 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmAttribute::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmAttribute** objectArray = (SgAsmJvmAttribute**) &(SgAsmJvmAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmAttribute::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmAttribute*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmAttribute::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmAttribute** objectArray = (SgAsmJvmAttribute**) &(SgAsmJvmAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmAttribute object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmAttribute object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmAttribute::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmAttribute** objectArray = (SgAsmJvmAttribute**) &(SgAsmJvmAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmAttribute::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmAttribute::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmAttribute found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmAttribute::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmAttribute** objectArray = (SgAsmJvmAttribute**) &(SgAsmJvmAttribute::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmAttribute::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmAttribute::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmAttribute);

     return memory;
   }



/* #line 235040 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmAnnotationDefault::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmAnnotationDefault objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmAnnotationDefault::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmAnnotationDefault** objectArray = (SgAsmJvmAnnotationDefault**) &(SgAsmJvmAnnotationDefault::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmAnnotationDefault::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmAnnotationDefault::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmAnnotationDefault::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmAnnotationDefault*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmAnnotationDefault::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmAnnotationDefault::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmAnnotationDefault::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmAnnotationDefault objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmAnnotationDefault::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmAnnotationDefault** objectArray = (SgAsmJvmAnnotationDefault**) &(SgAsmJvmAnnotationDefault::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmAnnotationDefault::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmAnnotationDefault::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmAnnotationDefault object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmAnnotationDefault object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmAnnotationDefault::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmAnnotationDefault objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmAnnotationDefault::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmAnnotationDefault** objectArray = (SgAsmJvmAnnotationDefault**) &(SgAsmJvmAnnotationDefault::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmAnnotationDefault::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmAnnotationDefault::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmAnnotationDefault found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmAnnotationDefault::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmAnnotationDefault::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmAnnotationDefault** objectArray = (SgAsmJvmAnnotationDefault**) &(SgAsmJvmAnnotationDefault::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmAnnotationDefault::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmAnnotationDefault::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmAnnotationDefault::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmAnnotationDefault);

     return memory;
   }



/* #line 235272 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmBootstrapMethods::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmBootstrapMethods objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmBootstrapMethods::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmBootstrapMethods** objectArray = (SgAsmJvmBootstrapMethods**) &(SgAsmJvmBootstrapMethods::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmBootstrapMethods::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmBootstrapMethods::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmBootstrapMethods::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmBootstrapMethods*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmBootstrapMethods::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmBootstrapMethods::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmBootstrapMethods::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmBootstrapMethods objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmBootstrapMethods::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmBootstrapMethods** objectArray = (SgAsmJvmBootstrapMethods**) &(SgAsmJvmBootstrapMethods::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmBootstrapMethods::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmBootstrapMethods::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmBootstrapMethods object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmBootstrapMethods object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmBootstrapMethods::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmBootstrapMethods objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmBootstrapMethods::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmBootstrapMethods** objectArray = (SgAsmJvmBootstrapMethods**) &(SgAsmJvmBootstrapMethods::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmBootstrapMethods::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmBootstrapMethods::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmBootstrapMethods found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmBootstrapMethods::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmBootstrapMethods::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmBootstrapMethods** objectArray = (SgAsmJvmBootstrapMethods**) &(SgAsmJvmBootstrapMethods::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmBootstrapMethods::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmBootstrapMethods::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmBootstrapMethods::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmBootstrapMethods);

     return memory;
   }



/* #line 235504 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmCodeAttribute::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmCodeAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmCodeAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmCodeAttribute** objectArray = (SgAsmJvmCodeAttribute**) &(SgAsmJvmCodeAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmCodeAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmCodeAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmCodeAttribute::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmCodeAttribute*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmCodeAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmCodeAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmCodeAttribute::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmCodeAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmCodeAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmCodeAttribute** objectArray = (SgAsmJvmCodeAttribute**) &(SgAsmJvmCodeAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmCodeAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmCodeAttribute::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmCodeAttribute object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmCodeAttribute object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmCodeAttribute::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmCodeAttribute objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmCodeAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmCodeAttribute** objectArray = (SgAsmJvmCodeAttribute**) &(SgAsmJvmCodeAttribute::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmCodeAttribute::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmCodeAttribute::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmCodeAttribute found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmCodeAttribute::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmCodeAttribute::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmCodeAttribute** objectArray = (SgAsmJvmCodeAttribute**) &(SgAsmJvmCodeAttribute::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmCodeAttribute::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmCodeAttribute::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmCodeAttribute::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmCodeAttribute);

     return memory;
   }



/* #line 235736 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmEnclosingMethod::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmEnclosingMethod objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmEnclosingMethod::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmEnclosingMethod** objectArray = (SgAsmJvmEnclosingMethod**) &(SgAsmJvmEnclosingMethod::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmEnclosingMethod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmEnclosingMethod::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmEnclosingMethod::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmEnclosingMethod*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmEnclosingMethod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmEnclosingMethod::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmEnclosingMethod::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmEnclosingMethod objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmEnclosingMethod::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmEnclosingMethod** objectArray = (SgAsmJvmEnclosingMethod**) &(SgAsmJvmEnclosingMethod::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmEnclosingMethod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmEnclosingMethod::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmEnclosingMethod object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmEnclosingMethod object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmEnclosingMethod::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmEnclosingMethod objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmEnclosingMethod::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmEnclosingMethod** objectArray = (SgAsmJvmEnclosingMethod**) &(SgAsmJvmEnclosingMethod::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmEnclosingMethod::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmEnclosingMethod::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmEnclosingMethod found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmEnclosingMethod::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmEnclosingMethod::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmEnclosingMethod** objectArray = (SgAsmJvmEnclosingMethod**) &(SgAsmJvmEnclosingMethod::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmEnclosingMethod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmEnclosingMethod::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmEnclosingMethod::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmEnclosingMethod);

     return memory;
   }



/* #line 235968 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmIndexedAttr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmIndexedAttr objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmIndexedAttr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmIndexedAttr** objectArray = (SgAsmJvmIndexedAttr**) &(SgAsmJvmIndexedAttr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmIndexedAttr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmIndexedAttr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmIndexedAttr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmIndexedAttr*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmIndexedAttr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmIndexedAttr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmIndexedAttr::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmIndexedAttr objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmIndexedAttr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmIndexedAttr** objectArray = (SgAsmJvmIndexedAttr**) &(SgAsmJvmIndexedAttr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmIndexedAttr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmIndexedAttr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmIndexedAttr object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmIndexedAttr object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmIndexedAttr::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmIndexedAttr objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmIndexedAttr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmIndexedAttr** objectArray = (SgAsmJvmIndexedAttr**) &(SgAsmJvmIndexedAttr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmIndexedAttr::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmIndexedAttr::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmIndexedAttr found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmIndexedAttr::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmIndexedAttr::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmIndexedAttr** objectArray = (SgAsmJvmIndexedAttr**) &(SgAsmJvmIndexedAttr::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmIndexedAttr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmIndexedAttr::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmIndexedAttr::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmIndexedAttr);

     return memory;
   }



/* #line 236200 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmIndexTableAttr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmIndexTableAttr objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmIndexTableAttr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmIndexTableAttr** objectArray = (SgAsmJvmIndexTableAttr**) &(SgAsmJvmIndexTableAttr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmIndexTableAttr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmIndexTableAttr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmIndexTableAttr::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmIndexTableAttr*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmIndexTableAttr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmIndexTableAttr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmIndexTableAttr::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmIndexTableAttr objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmIndexTableAttr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmIndexTableAttr** objectArray = (SgAsmJvmIndexTableAttr**) &(SgAsmJvmIndexTableAttr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmIndexTableAttr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmIndexTableAttr::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmIndexTableAttr object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmIndexTableAttr object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmIndexTableAttr::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmIndexTableAttr objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmIndexTableAttr::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmIndexTableAttr** objectArray = (SgAsmJvmIndexTableAttr**) &(SgAsmJvmIndexTableAttr::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmIndexTableAttr::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmIndexTableAttr::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmIndexTableAttr found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmIndexTableAttr::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmIndexTableAttr::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmIndexTableAttr** objectArray = (SgAsmJvmIndexTableAttr**) &(SgAsmJvmIndexTableAttr::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmIndexTableAttr::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmIndexTableAttr::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmIndexTableAttr::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmIndexTableAttr);

     return memory;
   }



/* #line 236432 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmInnerClasses::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmInnerClasses objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmInnerClasses::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmInnerClasses** objectArray = (SgAsmJvmInnerClasses**) &(SgAsmJvmInnerClasses::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmInnerClasses::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmInnerClasses::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmInnerClasses::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmInnerClasses*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmInnerClasses::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmInnerClasses::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmInnerClasses::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmInnerClasses objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmInnerClasses::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmInnerClasses** objectArray = (SgAsmJvmInnerClasses**) &(SgAsmJvmInnerClasses::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmInnerClasses::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmInnerClasses::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmInnerClasses object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmInnerClasses object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmInnerClasses::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmInnerClasses objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmInnerClasses::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmInnerClasses** objectArray = (SgAsmJvmInnerClasses**) &(SgAsmJvmInnerClasses::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmInnerClasses::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmInnerClasses::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmInnerClasses found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmInnerClasses::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmInnerClasses::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmInnerClasses** objectArray = (SgAsmJvmInnerClasses**) &(SgAsmJvmInnerClasses::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmInnerClasses::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmInnerClasses::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmInnerClasses::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmInnerClasses);

     return memory;
   }



/* #line 236664 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmLineNumberTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmLineNumberTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLineNumberTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLineNumberTable** objectArray = (SgAsmJvmLineNumberTable**) &(SgAsmJvmLineNumberTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmLineNumberTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmLineNumberTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmLineNumberTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmLineNumberTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmLineNumberTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmLineNumberTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmLineNumberTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmLineNumberTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLineNumberTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLineNumberTable** objectArray = (SgAsmJvmLineNumberTable**) &(SgAsmJvmLineNumberTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmLineNumberTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmLineNumberTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmLineNumberTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmLineNumberTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmLineNumberTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmLineNumberTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLineNumberTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLineNumberTable** objectArray = (SgAsmJvmLineNumberTable**) &(SgAsmJvmLineNumberTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmLineNumberTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmLineNumberTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmLineNumberTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmLineNumberTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmLineNumberTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmLineNumberTable** objectArray = (SgAsmJvmLineNumberTable**) &(SgAsmJvmLineNumberTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmLineNumberTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmLineNumberTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmLineNumberTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmLineNumberTable);

     return memory;
   }



/* #line 236896 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmLocalVariableTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmLocalVariableTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLocalVariableTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLocalVariableTable** objectArray = (SgAsmJvmLocalVariableTable**) &(SgAsmJvmLocalVariableTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmLocalVariableTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmLocalVariableTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmLocalVariableTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmLocalVariableTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmLocalVariableTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmLocalVariableTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmLocalVariableTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmLocalVariableTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLocalVariableTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLocalVariableTable** objectArray = (SgAsmJvmLocalVariableTable**) &(SgAsmJvmLocalVariableTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmLocalVariableTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmLocalVariableTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmLocalVariableTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmLocalVariableTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmLocalVariableTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmLocalVariableTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLocalVariableTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLocalVariableTable** objectArray = (SgAsmJvmLocalVariableTable**) &(SgAsmJvmLocalVariableTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmLocalVariableTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmLocalVariableTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmLocalVariableTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmLocalVariableTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmLocalVariableTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmLocalVariableTable** objectArray = (SgAsmJvmLocalVariableTable**) &(SgAsmJvmLocalVariableTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmLocalVariableTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmLocalVariableTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmLocalVariableTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmLocalVariableTable);

     return memory;
   }



/* #line 237128 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmLocalVariableTypeTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmLocalVariableTypeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLocalVariableTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLocalVariableTypeTable** objectArray = (SgAsmJvmLocalVariableTypeTable**) &(SgAsmJvmLocalVariableTypeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmLocalVariableTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmLocalVariableTypeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmLocalVariableTypeTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmLocalVariableTypeTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmLocalVariableTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmLocalVariableTypeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmLocalVariableTypeTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmLocalVariableTypeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLocalVariableTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLocalVariableTypeTable** objectArray = (SgAsmJvmLocalVariableTypeTable**) &(SgAsmJvmLocalVariableTypeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmLocalVariableTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmLocalVariableTypeTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmLocalVariableTypeTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmLocalVariableTypeTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmLocalVariableTypeTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmLocalVariableTypeTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLocalVariableTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLocalVariableTypeTable** objectArray = (SgAsmJvmLocalVariableTypeTable**) &(SgAsmJvmLocalVariableTypeTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmLocalVariableTypeTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmLocalVariableTypeTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmLocalVariableTypeTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmLocalVariableTypeTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmLocalVariableTypeTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmLocalVariableTypeTable** objectArray = (SgAsmJvmLocalVariableTypeTable**) &(SgAsmJvmLocalVariableTypeTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmLocalVariableTypeTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmLocalVariableTypeTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmLocalVariableTypeTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmLocalVariableTypeTable);

     return memory;
   }



/* #line 237360 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmMethodParameters::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmMethodParameters objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmMethodParameters::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmMethodParameters** objectArray = (SgAsmJvmMethodParameters**) &(SgAsmJvmMethodParameters::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmMethodParameters::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmMethodParameters::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmMethodParameters::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmMethodParameters*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmMethodParameters::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmMethodParameters::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmMethodParameters::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmMethodParameters objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmMethodParameters::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmMethodParameters** objectArray = (SgAsmJvmMethodParameters**) &(SgAsmJvmMethodParameters::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmMethodParameters::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmMethodParameters::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmMethodParameters object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmMethodParameters object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmMethodParameters::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmMethodParameters objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmMethodParameters::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmMethodParameters** objectArray = (SgAsmJvmMethodParameters**) &(SgAsmJvmMethodParameters::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmMethodParameters::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmMethodParameters::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmMethodParameters found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmMethodParameters::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmMethodParameters::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmMethodParameters** objectArray = (SgAsmJvmMethodParameters**) &(SgAsmJvmMethodParameters::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmMethodParameters::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmMethodParameters::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmMethodParameters::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmMethodParameters);

     return memory;
   }



/* #line 237592 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmModule::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmModule objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmModule::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmModule** objectArray = (SgAsmJvmModule**) &(SgAsmJvmModule::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmModule::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmModule::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmModule*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmModule::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmModule::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmModule objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmModule::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmModule** objectArray = (SgAsmJvmModule**) &(SgAsmJvmModule::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmModule::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmModule object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmModule object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmModule::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmModule objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmModule::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmModule** objectArray = (SgAsmJvmModule**) &(SgAsmJvmModule::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmModule::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmModule::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmModule found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmModule::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmModule::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmModule** objectArray = (SgAsmJvmModule**) &(SgAsmJvmModule::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmModule::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmModule::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmModule::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmModule);

     return memory;
   }



/* #line 237824 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmRuntimeInvisibleAnnotations::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmRuntimeInvisibleAnnotations objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeInvisibleAnnotations::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeInvisibleAnnotations** objectArray = (SgAsmJvmRuntimeInvisibleAnnotations**) &(SgAsmJvmRuntimeInvisibleAnnotations::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeInvisibleAnnotations::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmRuntimeInvisibleAnnotations::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmRuntimeInvisibleAnnotations::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmRuntimeInvisibleAnnotations*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmRuntimeInvisibleAnnotations::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeInvisibleAnnotations::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmRuntimeInvisibleAnnotations::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeInvisibleAnnotations objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeInvisibleAnnotations::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeInvisibleAnnotations** objectArray = (SgAsmJvmRuntimeInvisibleAnnotations**) &(SgAsmJvmRuntimeInvisibleAnnotations::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeInvisibleAnnotations::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeInvisibleAnnotations::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmRuntimeInvisibleAnnotations object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmRuntimeInvisibleAnnotations object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmRuntimeInvisibleAnnotations::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeInvisibleAnnotations objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeInvisibleAnnotations::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeInvisibleAnnotations** objectArray = (SgAsmJvmRuntimeInvisibleAnnotations**) &(SgAsmJvmRuntimeInvisibleAnnotations::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmRuntimeInvisibleAnnotations::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmRuntimeInvisibleAnnotations::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmRuntimeInvisibleAnnotations found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeInvisibleAnnotations::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmRuntimeInvisibleAnnotations::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmRuntimeInvisibleAnnotations** objectArray = (SgAsmJvmRuntimeInvisibleAnnotations**) &(SgAsmJvmRuntimeInvisibleAnnotations::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmRuntimeInvisibleAnnotations::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmRuntimeInvisibleAnnotations::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeInvisibleAnnotations::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmRuntimeInvisibleAnnotations);

     return memory;
   }



/* #line 238056 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmRuntimeVisibilityParamAnnotations::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmRuntimeVisibilityParamAnnotations objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeVisibilityParamAnnotations::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeVisibilityParamAnnotations** objectArray = (SgAsmJvmRuntimeVisibilityParamAnnotations**) &(SgAsmJvmRuntimeVisibilityParamAnnotations::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeVisibilityParamAnnotations::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmRuntimeVisibilityParamAnnotations::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmRuntimeVisibilityParamAnnotations*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmRuntimeVisibilityParamAnnotations::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmRuntimeVisibilityParamAnnotations::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeVisibilityParamAnnotations objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeVisibilityParamAnnotations::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeVisibilityParamAnnotations** objectArray = (SgAsmJvmRuntimeVisibilityParamAnnotations**) &(SgAsmJvmRuntimeVisibilityParamAnnotations::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeVisibilityParamAnnotations::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmRuntimeVisibilityParamAnnotations object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmRuntimeVisibilityParamAnnotations object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmRuntimeVisibilityParamAnnotations::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeVisibilityParamAnnotations objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeVisibilityParamAnnotations::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeVisibilityParamAnnotations** objectArray = (SgAsmJvmRuntimeVisibilityParamAnnotations**) &(SgAsmJvmRuntimeVisibilityParamAnnotations::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmRuntimeVisibilityParamAnnotations::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmRuntimeVisibilityParamAnnotations found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeVisibilityParamAnnotations::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmRuntimeVisibilityParamAnnotations::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmRuntimeVisibilityParamAnnotations** objectArray = (SgAsmJvmRuntimeVisibilityParamAnnotations**) &(SgAsmJvmRuntimeVisibilityParamAnnotations::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmRuntimeVisibilityParamAnnotations::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmRuntimeVisibilityParamAnnotations::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeVisibilityParamAnnotations::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmRuntimeVisibilityParamAnnotations);

     return memory;
   }



/* #line 238288 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmRuntimeVisibleAnnotations::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmRuntimeVisibleAnnotations objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeVisibleAnnotations::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeVisibleAnnotations** objectArray = (SgAsmJvmRuntimeVisibleAnnotations**) &(SgAsmJvmRuntimeVisibleAnnotations::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeVisibleAnnotations::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmRuntimeVisibleAnnotations::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmRuntimeVisibleAnnotations::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmRuntimeVisibleAnnotations*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmRuntimeVisibleAnnotations::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeVisibleAnnotations::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmRuntimeVisibleAnnotations::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeVisibleAnnotations objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeVisibleAnnotations::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeVisibleAnnotations** objectArray = (SgAsmJvmRuntimeVisibleAnnotations**) &(SgAsmJvmRuntimeVisibleAnnotations::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeVisibleAnnotations::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeVisibleAnnotations::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmRuntimeVisibleAnnotations object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmRuntimeVisibleAnnotations object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmRuntimeVisibleAnnotations::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeVisibleAnnotations objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeVisibleAnnotations::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeVisibleAnnotations** objectArray = (SgAsmJvmRuntimeVisibleAnnotations**) &(SgAsmJvmRuntimeVisibleAnnotations::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmRuntimeVisibleAnnotations::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmRuntimeVisibleAnnotations::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmRuntimeVisibleAnnotations found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeVisibleAnnotations::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmRuntimeVisibleAnnotations::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmRuntimeVisibleAnnotations** objectArray = (SgAsmJvmRuntimeVisibleAnnotations**) &(SgAsmJvmRuntimeVisibleAnnotations::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmRuntimeVisibleAnnotations::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmRuntimeVisibleAnnotations::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeVisibleAnnotations::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmRuntimeVisibleAnnotations);

     return memory;
   }



/* #line 238520 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmStackMapTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmStackMapTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmStackMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmStackMapTable** objectArray = (SgAsmJvmStackMapTable**) &(SgAsmJvmStackMapTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmStackMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmStackMapTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmStackMapTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmStackMapTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmStackMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmStackMapTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmStackMapTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmStackMapTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmStackMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmStackMapTable** objectArray = (SgAsmJvmStackMapTable**) &(SgAsmJvmStackMapTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmStackMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmStackMapTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmStackMapTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmStackMapTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmStackMapTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmStackMapTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmStackMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmStackMapTable** objectArray = (SgAsmJvmStackMapTable**) &(SgAsmJvmStackMapTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmStackMapTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmStackMapTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmStackMapTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmStackMapTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmStackMapTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmStackMapTable** objectArray = (SgAsmJvmStackMapTable**) &(SgAsmJvmStackMapTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmStackMapTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmStackMapTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmStackMapTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmStackMapTable);

     return memory;
   }



/* #line 238752 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmBootstrapMethod::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmBootstrapMethod objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmBootstrapMethod::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmBootstrapMethod** objectArray = (SgAsmJvmBootstrapMethod**) &(SgAsmJvmBootstrapMethod::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmBootstrapMethod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmBootstrapMethod::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmBootstrapMethod::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmBootstrapMethod*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmBootstrapMethod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmBootstrapMethod::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmBootstrapMethod::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmBootstrapMethod objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmBootstrapMethod::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmBootstrapMethod** objectArray = (SgAsmJvmBootstrapMethod**) &(SgAsmJvmBootstrapMethod::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmBootstrapMethod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmBootstrapMethod::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmBootstrapMethod object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmBootstrapMethod object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmBootstrapMethod::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmBootstrapMethod objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmBootstrapMethod::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmBootstrapMethod** objectArray = (SgAsmJvmBootstrapMethod**) &(SgAsmJvmBootstrapMethod::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmBootstrapMethod::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmBootstrapMethod::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmBootstrapMethod found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmBootstrapMethod::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmBootstrapMethod::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmBootstrapMethod** objectArray = (SgAsmJvmBootstrapMethod**) &(SgAsmJvmBootstrapMethod::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmBootstrapMethod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmBootstrapMethod::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmBootstrapMethod::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmBootstrapMethod);

     return memory;
   }



/* #line 238984 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmClass::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmClass objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmClass** objectArray = (SgAsmJvmClass**) &(SgAsmJvmClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmClass::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmClass*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmClass::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmClass objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmClass** objectArray = (SgAsmJvmClass**) &(SgAsmJvmClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmClass::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmClass object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmClass object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmClass::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmClass objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmClass::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmClass** objectArray = (SgAsmJvmClass**) &(SgAsmJvmClass::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmClass::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmClass::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmClass found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmClass::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmClass::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmClass** objectArray = (SgAsmJvmClass**) &(SgAsmJvmClass::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmClass::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmClass::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmClass::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmClass);

     return memory;
   }



/* #line 239216 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmConstantPoolEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmConstantPoolEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmConstantPoolEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmConstantPoolEntry** objectArray = (SgAsmJvmConstantPoolEntry**) &(SgAsmJvmConstantPoolEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmConstantPoolEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmConstantPoolEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmConstantPoolEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmConstantPoolEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmConstantPoolEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmConstantPoolEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmConstantPoolEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmConstantPoolEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmConstantPoolEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmConstantPoolEntry** objectArray = (SgAsmJvmConstantPoolEntry**) &(SgAsmJvmConstantPoolEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmConstantPoolEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmConstantPoolEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmConstantPoolEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmConstantPoolEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmConstantPoolEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmConstantPoolEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmConstantPoolEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmConstantPoolEntry** objectArray = (SgAsmJvmConstantPoolEntry**) &(SgAsmJvmConstantPoolEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmConstantPoolEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmConstantPoolEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmConstantPoolEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmConstantPoolEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmConstantPoolEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmConstantPoolEntry** objectArray = (SgAsmJvmConstantPoolEntry**) &(SgAsmJvmConstantPoolEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmConstantPoolEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmConstantPoolEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmConstantPoolEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmConstantPoolEntry);

     return memory;
   }



/* #line 239448 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmExceptionHandler::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmExceptionHandler objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmExceptionHandler::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmExceptionHandler** objectArray = (SgAsmJvmExceptionHandler**) &(SgAsmJvmExceptionHandler::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmExceptionHandler::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmExceptionHandler::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmExceptionHandler::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmExceptionHandler*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmExceptionHandler::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmExceptionHandler::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmExceptionHandler::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmExceptionHandler objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmExceptionHandler::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmExceptionHandler** objectArray = (SgAsmJvmExceptionHandler**) &(SgAsmJvmExceptionHandler::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmExceptionHandler::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmExceptionHandler::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmExceptionHandler object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmExceptionHandler object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmExceptionHandler::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmExceptionHandler objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmExceptionHandler::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmExceptionHandler** objectArray = (SgAsmJvmExceptionHandler**) &(SgAsmJvmExceptionHandler::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmExceptionHandler::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmExceptionHandler::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmExceptionHandler found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmExceptionHandler::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmExceptionHandler::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmExceptionHandler** objectArray = (SgAsmJvmExceptionHandler**) &(SgAsmJvmExceptionHandler::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmExceptionHandler::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmExceptionHandler::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmExceptionHandler::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmExceptionHandler);

     return memory;
   }



/* #line 239680 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmExceptionTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmExceptionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmExceptionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmExceptionTable** objectArray = (SgAsmJvmExceptionTable**) &(SgAsmJvmExceptionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmExceptionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmExceptionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmExceptionTable::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmExceptionTable*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmExceptionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmExceptionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmExceptionTable::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmExceptionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmExceptionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmExceptionTable** objectArray = (SgAsmJvmExceptionTable**) &(SgAsmJvmExceptionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmExceptionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmExceptionTable::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmExceptionTable object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmExceptionTable object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmExceptionTable::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmExceptionTable objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmExceptionTable::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmExceptionTable** objectArray = (SgAsmJvmExceptionTable**) &(SgAsmJvmExceptionTable::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmExceptionTable::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmExceptionTable::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmExceptionTable found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmExceptionTable::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmExceptionTable::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmExceptionTable** objectArray = (SgAsmJvmExceptionTable**) &(SgAsmJvmExceptionTable::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmExceptionTable::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmExceptionTable::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmExceptionTable::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmExceptionTable);

     return memory;
   }



/* #line 239912 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmField::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmField objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmField::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmField** objectArray = (SgAsmJvmField**) &(SgAsmJvmField::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmField::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmField::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmField::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmField*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmField::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmField::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmField::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmField objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmField::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmField** objectArray = (SgAsmJvmField**) &(SgAsmJvmField::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmField::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmField::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmField object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmField object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmField::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmField objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmField::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmField** objectArray = (SgAsmJvmField**) &(SgAsmJvmField::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmField::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmField::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmField found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmField::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmField::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmField** objectArray = (SgAsmJvmField**) &(SgAsmJvmField::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmField::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmField::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmField::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmField);

     return memory;
   }



/* #line 240144 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmLocalVariableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmLocalVariableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLocalVariableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLocalVariableEntry** objectArray = (SgAsmJvmLocalVariableEntry**) &(SgAsmJvmLocalVariableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmLocalVariableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmLocalVariableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmLocalVariableEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmLocalVariableEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmLocalVariableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmLocalVariableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmLocalVariableEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmLocalVariableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLocalVariableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLocalVariableEntry** objectArray = (SgAsmJvmLocalVariableEntry**) &(SgAsmJvmLocalVariableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmLocalVariableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmLocalVariableEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmLocalVariableEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmLocalVariableEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmLocalVariableEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmLocalVariableEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLocalVariableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLocalVariableEntry** objectArray = (SgAsmJvmLocalVariableEntry**) &(SgAsmJvmLocalVariableEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmLocalVariableEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmLocalVariableEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmLocalVariableEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmLocalVariableEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmLocalVariableEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmLocalVariableEntry** objectArray = (SgAsmJvmLocalVariableEntry**) &(SgAsmJvmLocalVariableEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmLocalVariableEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmLocalVariableEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmLocalVariableEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmLocalVariableEntry);

     return memory;
   }



/* #line 240376 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmLocalVariableTypeEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmLocalVariableTypeEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLocalVariableTypeEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLocalVariableTypeEntry** objectArray = (SgAsmJvmLocalVariableTypeEntry**) &(SgAsmJvmLocalVariableTypeEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmLocalVariableTypeEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmLocalVariableTypeEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmLocalVariableTypeEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmLocalVariableTypeEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmLocalVariableTypeEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmLocalVariableTypeEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmLocalVariableTypeEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmLocalVariableTypeEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLocalVariableTypeEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLocalVariableTypeEntry** objectArray = (SgAsmJvmLocalVariableTypeEntry**) &(SgAsmJvmLocalVariableTypeEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmLocalVariableTypeEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmLocalVariableTypeEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmLocalVariableTypeEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmLocalVariableTypeEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmLocalVariableTypeEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmLocalVariableTypeEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmLocalVariableTypeEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmLocalVariableTypeEntry** objectArray = (SgAsmJvmLocalVariableTypeEntry**) &(SgAsmJvmLocalVariableTypeEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmLocalVariableTypeEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmLocalVariableTypeEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmLocalVariableTypeEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmLocalVariableTypeEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmLocalVariableTypeEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmLocalVariableTypeEntry** objectArray = (SgAsmJvmLocalVariableTypeEntry**) &(SgAsmJvmLocalVariableTypeEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmLocalVariableTypeEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmLocalVariableTypeEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmLocalVariableTypeEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmLocalVariableTypeEntry);

     return memory;
   }



/* #line 240608 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmMethod::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmMethod objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmMethod::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmMethod** objectArray = (SgAsmJvmMethod**) &(SgAsmJvmMethod::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmMethod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmMethod::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmMethod::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmMethod*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmMethod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmMethod::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmMethod::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmMethod objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmMethod::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmMethod** objectArray = (SgAsmJvmMethod**) &(SgAsmJvmMethod::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmMethod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmMethod::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmMethod object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmMethod object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmMethod::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmMethod objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmMethod::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmMethod** objectArray = (SgAsmJvmMethod**) &(SgAsmJvmMethod::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmMethod::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmMethod::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmMethod found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmMethod::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmMethod::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmMethod** objectArray = (SgAsmJvmMethod**) &(SgAsmJvmMethod::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmMethod::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmMethod::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmMethod::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmMethod);

     return memory;
   }



/* #line 240840 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmMethodParametersEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmMethodParametersEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmMethodParametersEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmMethodParametersEntry** objectArray = (SgAsmJvmMethodParametersEntry**) &(SgAsmJvmMethodParametersEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmMethodParametersEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmMethodParametersEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmMethodParametersEntry::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmMethodParametersEntry*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmMethodParametersEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmMethodParametersEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmMethodParametersEntry::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmMethodParametersEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmMethodParametersEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmMethodParametersEntry** objectArray = (SgAsmJvmMethodParametersEntry**) &(SgAsmJvmMethodParametersEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmMethodParametersEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmMethodParametersEntry::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmMethodParametersEntry object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmMethodParametersEntry object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmMethodParametersEntry::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmMethodParametersEntry objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmMethodParametersEntry::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmMethodParametersEntry** objectArray = (SgAsmJvmMethodParametersEntry**) &(SgAsmJvmMethodParametersEntry::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmMethodParametersEntry::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmMethodParametersEntry::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmMethodParametersEntry found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmMethodParametersEntry::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmMethodParametersEntry::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmMethodParametersEntry** objectArray = (SgAsmJvmMethodParametersEntry**) &(SgAsmJvmMethodParametersEntry::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmMethodParametersEntry::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmMethodParametersEntry::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmMethodParametersEntry::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmMethodParametersEntry);

     return memory;
   }



/* #line 241072 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmRuntimeAnnotation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmRuntimeAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeAnnotation** objectArray = (SgAsmJvmRuntimeAnnotation**) &(SgAsmJvmRuntimeAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmRuntimeAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmRuntimeAnnotation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmRuntimeAnnotation*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmRuntimeAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmRuntimeAnnotation::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeAnnotation** objectArray = (SgAsmJvmRuntimeAnnotation**) &(SgAsmJvmRuntimeAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmRuntimeAnnotation object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmRuntimeAnnotation object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmRuntimeAnnotation::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeAnnotation** objectArray = (SgAsmJvmRuntimeAnnotation**) &(SgAsmJvmRuntimeAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmRuntimeAnnotation::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmRuntimeAnnotation::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmRuntimeAnnotation found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeAnnotation::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmRuntimeAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmRuntimeAnnotation** objectArray = (SgAsmJvmRuntimeAnnotation**) &(SgAsmJvmRuntimeAnnotation::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmRuntimeAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmRuntimeAnnotation::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeAnnotation::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmRuntimeAnnotation);

     return memory;
   }



/* #line 241304 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmRuntimeAnnotationPair::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmRuntimeAnnotationPair objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeAnnotationPair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeAnnotationPair** objectArray = (SgAsmJvmRuntimeAnnotationPair**) &(SgAsmJvmRuntimeAnnotationPair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeAnnotationPair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmRuntimeAnnotationPair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmRuntimeAnnotationPair::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmRuntimeAnnotationPair*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmRuntimeAnnotationPair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeAnnotationPair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmRuntimeAnnotationPair::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeAnnotationPair objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeAnnotationPair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeAnnotationPair** objectArray = (SgAsmJvmRuntimeAnnotationPair**) &(SgAsmJvmRuntimeAnnotationPair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeAnnotationPair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeAnnotationPair::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmRuntimeAnnotationPair object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmRuntimeAnnotationPair object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmRuntimeAnnotationPair::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeAnnotationPair objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeAnnotationPair::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeAnnotationPair** objectArray = (SgAsmJvmRuntimeAnnotationPair**) &(SgAsmJvmRuntimeAnnotationPair::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmRuntimeAnnotationPair::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmRuntimeAnnotationPair::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmRuntimeAnnotationPair found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeAnnotationPair::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmRuntimeAnnotationPair::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmRuntimeAnnotationPair** objectArray = (SgAsmJvmRuntimeAnnotationPair**) &(SgAsmJvmRuntimeAnnotationPair::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmRuntimeAnnotationPair::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmRuntimeAnnotationPair::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeAnnotationPair::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmRuntimeAnnotationPair);

     return memory;
   }



/* #line 241536 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmRuntimeAnnotationValue::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmRuntimeAnnotationValue objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeAnnotationValue::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeAnnotationValue** objectArray = (SgAsmJvmRuntimeAnnotationValue**) &(SgAsmJvmRuntimeAnnotationValue::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeAnnotationValue::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmRuntimeAnnotationValue::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmRuntimeAnnotationValue::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmRuntimeAnnotationValue*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmRuntimeAnnotationValue::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeAnnotationValue::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmRuntimeAnnotationValue::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeAnnotationValue objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeAnnotationValue::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeAnnotationValue** objectArray = (SgAsmJvmRuntimeAnnotationValue**) &(SgAsmJvmRuntimeAnnotationValue::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeAnnotationValue::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeAnnotationValue::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmRuntimeAnnotationValue object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmRuntimeAnnotationValue object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmRuntimeAnnotationValue::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeAnnotationValue objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeAnnotationValue::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeAnnotationValue** objectArray = (SgAsmJvmRuntimeAnnotationValue**) &(SgAsmJvmRuntimeAnnotationValue::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmRuntimeAnnotationValue::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmRuntimeAnnotationValue::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmRuntimeAnnotationValue found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeAnnotationValue::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmRuntimeAnnotationValue::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmRuntimeAnnotationValue** objectArray = (SgAsmJvmRuntimeAnnotationValue**) &(SgAsmJvmRuntimeAnnotationValue::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmRuntimeAnnotationValue::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmRuntimeAnnotationValue::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeAnnotationValue::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmRuntimeAnnotationValue);

     return memory;
   }



/* #line 241768 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmRuntimeParameterAnnotation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmRuntimeParameterAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeParameterAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeParameterAnnotation** objectArray = (SgAsmJvmRuntimeParameterAnnotation**) &(SgAsmJvmRuntimeParameterAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeParameterAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmRuntimeParameterAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmRuntimeParameterAnnotation::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmRuntimeParameterAnnotation*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmRuntimeParameterAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeParameterAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmRuntimeParameterAnnotation::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeParameterAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeParameterAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeParameterAnnotation** objectArray = (SgAsmJvmRuntimeParameterAnnotation**) &(SgAsmJvmRuntimeParameterAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmRuntimeParameterAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmRuntimeParameterAnnotation::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmRuntimeParameterAnnotation object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmRuntimeParameterAnnotation object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmRuntimeParameterAnnotation::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmRuntimeParameterAnnotation objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmRuntimeParameterAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmRuntimeParameterAnnotation** objectArray = (SgAsmJvmRuntimeParameterAnnotation**) &(SgAsmJvmRuntimeParameterAnnotation::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmRuntimeParameterAnnotation::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmRuntimeParameterAnnotation::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmRuntimeParameterAnnotation found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeParameterAnnotation::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmRuntimeParameterAnnotation::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmRuntimeParameterAnnotation** objectArray = (SgAsmJvmRuntimeParameterAnnotation**) &(SgAsmJvmRuntimeParameterAnnotation::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmRuntimeParameterAnnotation::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmRuntimeParameterAnnotation::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmRuntimeParameterAnnotation::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmRuntimeParameterAnnotation);

     return memory;
   }



/* #line 242000 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmStackMapFrame::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmStackMapFrame objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmStackMapFrame::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmStackMapFrame** objectArray = (SgAsmJvmStackMapFrame**) &(SgAsmJvmStackMapFrame::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmStackMapFrame::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmStackMapFrame::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmStackMapFrame::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmStackMapFrame*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmStackMapFrame::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmStackMapFrame::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmStackMapFrame::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmStackMapFrame objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmStackMapFrame::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmStackMapFrame** objectArray = (SgAsmJvmStackMapFrame**) &(SgAsmJvmStackMapFrame::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmStackMapFrame::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmStackMapFrame::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmStackMapFrame object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmStackMapFrame object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmStackMapFrame::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmStackMapFrame objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmStackMapFrame::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmStackMapFrame** objectArray = (SgAsmJvmStackMapFrame**) &(SgAsmJvmStackMapFrame::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmStackMapFrame::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmStackMapFrame::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmStackMapFrame found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmStackMapFrame::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmStackMapFrame::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmStackMapFrame** objectArray = (SgAsmJvmStackMapFrame**) &(SgAsmJvmStackMapFrame::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmStackMapFrame::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmStackMapFrame::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmStackMapFrame::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmStackMapFrame);

     return memory;
   }



/* #line 242232 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmStackMapVerificationType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmStackMapVerificationType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmStackMapVerificationType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmStackMapVerificationType** objectArray = (SgAsmJvmStackMapVerificationType**) &(SgAsmJvmStackMapVerificationType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmStackMapVerificationType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmStackMapVerificationType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmStackMapVerificationType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmStackMapVerificationType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmStackMapVerificationType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmStackMapVerificationType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmStackMapVerificationType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmStackMapVerificationType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmStackMapVerificationType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmStackMapVerificationType** objectArray = (SgAsmJvmStackMapVerificationType**) &(SgAsmJvmStackMapVerificationType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmStackMapVerificationType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmStackMapVerificationType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmStackMapVerificationType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmStackMapVerificationType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmStackMapVerificationType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmStackMapVerificationType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmStackMapVerificationType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmStackMapVerificationType** objectArray = (SgAsmJvmStackMapVerificationType**) &(SgAsmJvmStackMapVerificationType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmStackMapVerificationType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmStackMapVerificationType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmStackMapVerificationType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmStackMapVerificationType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmStackMapVerificationType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmStackMapVerificationType** objectArray = (SgAsmJvmStackMapVerificationType**) &(SgAsmJvmStackMapVerificationType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmStackMapVerificationType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmStackMapVerificationType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmStackMapVerificationType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmStackMapVerificationType);

     return memory;
   }



/* #line 242464 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmOperandList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmOperandList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmOperandList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmOperandList** objectArray = (SgAsmOperandList**) &(SgAsmOperandList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmOperandList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmOperandList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmOperandList::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmOperandList*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmOperandList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmOperandList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmOperandList::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmOperandList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmOperandList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmOperandList** objectArray = (SgAsmOperandList**) &(SgAsmOperandList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmOperandList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmOperandList::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmOperandList object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmOperandList object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmOperandList::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmOperandList objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmOperandList::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmOperandList** objectArray = (SgAsmOperandList**) &(SgAsmOperandList::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmOperandList::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmOperandList::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmOperandList found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmOperandList::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmOperandList::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmOperandList** objectArray = (SgAsmOperandList**) &(SgAsmOperandList::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmOperandList::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmOperandList::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmOperandList::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmOperandList);

     return memory;
   }



/* #line 242696 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStatement** objectArray = (SgAsmStatement**) &(SgAsmStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmStatement::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmStatement*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmStatement::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStatement** objectArray = (SgAsmStatement**) &(SgAsmStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmStatement::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmStatement object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmStatement object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmStatement::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmStatement objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStatement::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStatement** objectArray = (SgAsmStatement**) &(SgAsmStatement::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmStatement::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmStatement::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmStatement found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmStatement::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmStatement::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmStatement** objectArray = (SgAsmStatement**) &(SgAsmStatement::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmStatement::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmStatement::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmStatement::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmStatement);

     return memory;
   }



/* #line 242928 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBlock** objectArray = (SgAsmBlock**) &(SgAsmBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmBlock::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmBlock*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmBlock::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBlock** objectArray = (SgAsmBlock**) &(SgAsmBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmBlock::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmBlock object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmBlock object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmBlock::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmBlock objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmBlock::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmBlock** objectArray = (SgAsmBlock**) &(SgAsmBlock::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmBlock::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmBlock::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmBlock found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBlock::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmBlock::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmBlock** objectArray = (SgAsmBlock**) &(SgAsmBlock::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmBlock::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmBlock::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmBlock::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmBlock);

     return memory;
   }



/* #line 243160 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmInstruction** objectArray = (SgAsmInstruction**) &(SgAsmInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmInstruction*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmInstruction::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmInstruction** objectArray = (SgAsmInstruction**) &(SgAsmInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmInstruction object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmInstruction object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmInstruction::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmInstruction** objectArray = (SgAsmInstruction**) &(SgAsmInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmInstruction::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmInstruction::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmInstruction found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmInstruction::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmInstruction** objectArray = (SgAsmInstruction**) &(SgAsmInstruction::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmInstruction::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmInstruction::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmInstruction);

     return memory;
   }



/* #line 243392 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmCilInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmCilInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilInstruction** objectArray = (SgAsmCilInstruction**) &(SgAsmCilInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmCilInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmCilInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmCilInstruction*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmCilInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmCilInstruction::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilInstruction** objectArray = (SgAsmCilInstruction**) &(SgAsmCilInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmCilInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmCilInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmCilInstruction object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmCilInstruction object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmCilInstruction::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmCilInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmCilInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmCilInstruction** objectArray = (SgAsmCilInstruction**) &(SgAsmCilInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmCilInstruction::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmCilInstruction::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmCilInstruction found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilInstruction::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmCilInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmCilInstruction** objectArray = (SgAsmCilInstruction**) &(SgAsmCilInstruction::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmCilInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmCilInstruction::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmCilInstruction::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmCilInstruction);

     return memory;
   }



/* #line 243624 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmJvmInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmJvmInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmInstruction** objectArray = (SgAsmJvmInstruction**) &(SgAsmJvmInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmJvmInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmJvmInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmJvmInstruction*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmJvmInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmJvmInstruction::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmInstruction** objectArray = (SgAsmJvmInstruction**) &(SgAsmJvmInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmJvmInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmJvmInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmJvmInstruction object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmJvmInstruction object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmJvmInstruction::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmJvmInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmJvmInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmJvmInstruction** objectArray = (SgAsmJvmInstruction**) &(SgAsmJvmInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmJvmInstruction::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmJvmInstruction::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmJvmInstruction found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmInstruction::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmJvmInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmJvmInstruction** objectArray = (SgAsmJvmInstruction**) &(SgAsmJvmInstruction::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmJvmInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmJvmInstruction::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmJvmInstruction::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmJvmInstruction);

     return memory;
   }



/* #line 243856 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmM68kInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmM68kInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmM68kInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmM68kInstruction** objectArray = (SgAsmM68kInstruction**) &(SgAsmM68kInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmM68kInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmM68kInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmM68kInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmM68kInstruction*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmM68kInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmM68kInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmM68kInstruction::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmM68kInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmM68kInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmM68kInstruction** objectArray = (SgAsmM68kInstruction**) &(SgAsmM68kInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmM68kInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmM68kInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmM68kInstruction object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmM68kInstruction object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmM68kInstruction::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmM68kInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmM68kInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmM68kInstruction** objectArray = (SgAsmM68kInstruction**) &(SgAsmM68kInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmM68kInstruction::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmM68kInstruction::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmM68kInstruction found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmM68kInstruction::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmM68kInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmM68kInstruction** objectArray = (SgAsmM68kInstruction**) &(SgAsmM68kInstruction::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmM68kInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmM68kInstruction::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmM68kInstruction::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmM68kInstruction);

     return memory;
   }



/* #line 244088 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmMipsInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmMipsInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmMipsInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmMipsInstruction** objectArray = (SgAsmMipsInstruction**) &(SgAsmMipsInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmMipsInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmMipsInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmMipsInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmMipsInstruction*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmMipsInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmMipsInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmMipsInstruction::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmMipsInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmMipsInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmMipsInstruction** objectArray = (SgAsmMipsInstruction**) &(SgAsmMipsInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmMipsInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmMipsInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmMipsInstruction object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmMipsInstruction object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmMipsInstruction::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmMipsInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmMipsInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmMipsInstruction** objectArray = (SgAsmMipsInstruction**) &(SgAsmMipsInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmMipsInstruction::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmMipsInstruction::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmMipsInstruction found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmMipsInstruction::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmMipsInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmMipsInstruction** objectArray = (SgAsmMipsInstruction**) &(SgAsmMipsInstruction::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmMipsInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmMipsInstruction::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmMipsInstruction::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmMipsInstruction);

     return memory;
   }



/* #line 244320 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmNullInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmNullInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNullInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNullInstruction** objectArray = (SgAsmNullInstruction**) &(SgAsmNullInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNullInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmNullInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmNullInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmNullInstruction*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmNullInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNullInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmNullInstruction::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNullInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNullInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNullInstruction** objectArray = (SgAsmNullInstruction**) &(SgAsmNullInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmNullInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmNullInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmNullInstruction object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmNullInstruction object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmNullInstruction::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmNullInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmNullInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmNullInstruction** objectArray = (SgAsmNullInstruction**) &(SgAsmNullInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmNullInstruction::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmNullInstruction::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmNullInstruction found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNullInstruction::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmNullInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmNullInstruction** objectArray = (SgAsmNullInstruction**) &(SgAsmNullInstruction::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmNullInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmNullInstruction::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmNullInstruction::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmNullInstruction);

     return memory;
   }



/* #line 244552 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPowerpcInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPowerpcInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPowerpcInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPowerpcInstruction** objectArray = (SgAsmPowerpcInstruction**) &(SgAsmPowerpcInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPowerpcInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPowerpcInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPowerpcInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPowerpcInstruction*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPowerpcInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPowerpcInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPowerpcInstruction::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPowerpcInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPowerpcInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPowerpcInstruction** objectArray = (SgAsmPowerpcInstruction**) &(SgAsmPowerpcInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPowerpcInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPowerpcInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPowerpcInstruction object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPowerpcInstruction object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPowerpcInstruction::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPowerpcInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPowerpcInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPowerpcInstruction** objectArray = (SgAsmPowerpcInstruction**) &(SgAsmPowerpcInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPowerpcInstruction::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPowerpcInstruction::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPowerpcInstruction found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPowerpcInstruction::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPowerpcInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPowerpcInstruction** objectArray = (SgAsmPowerpcInstruction**) &(SgAsmPowerpcInstruction::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPowerpcInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPowerpcInstruction::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPowerpcInstruction::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPowerpcInstruction);

     return memory;
   }



/* #line 244784 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmUserInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmUserInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUserInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUserInstruction** objectArray = (SgAsmUserInstruction**) &(SgAsmUserInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUserInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmUserInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmUserInstruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmUserInstruction*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmUserInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUserInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmUserInstruction::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUserInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUserInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUserInstruction** objectArray = (SgAsmUserInstruction**) &(SgAsmUserInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmUserInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmUserInstruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmUserInstruction object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmUserInstruction object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmUserInstruction::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmUserInstruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmUserInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmUserInstruction** objectArray = (SgAsmUserInstruction**) &(SgAsmUserInstruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmUserInstruction::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmUserInstruction::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmUserInstruction found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUserInstruction::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmUserInstruction::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmUserInstruction** objectArray = (SgAsmUserInstruction**) &(SgAsmUserInstruction::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmUserInstruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmUserInstruction::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmUserInstruction::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmUserInstruction);

     return memory;
   }



/* #line 245016 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmX86Instruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmX86Instruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmX86Instruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmX86Instruction** objectArray = (SgAsmX86Instruction**) &(SgAsmX86Instruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmX86Instruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmX86Instruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmX86Instruction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmX86Instruction*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmX86Instruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmX86Instruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmX86Instruction::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmX86Instruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmX86Instruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmX86Instruction** objectArray = (SgAsmX86Instruction**) &(SgAsmX86Instruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmX86Instruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmX86Instruction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmX86Instruction object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmX86Instruction object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmX86Instruction::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmX86Instruction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmX86Instruction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmX86Instruction** objectArray = (SgAsmX86Instruction**) &(SgAsmX86Instruction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmX86Instruction::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmX86Instruction::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmX86Instruction found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmX86Instruction::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmX86Instruction::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmX86Instruction** objectArray = (SgAsmX86Instruction**) &(SgAsmX86Instruction::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmX86Instruction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmX86Instruction::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmX86Instruction::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmX86Instruction);

     return memory;
   }



/* #line 245248 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmStaticData::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmStaticData objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStaticData::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStaticData** objectArray = (SgAsmStaticData**) &(SgAsmStaticData::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmStaticData::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmStaticData::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmStaticData::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmStaticData*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmStaticData::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmStaticData::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmStaticData::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmStaticData objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStaticData::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStaticData** objectArray = (SgAsmStaticData**) &(SgAsmStaticData::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmStaticData::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmStaticData::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmStaticData object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmStaticData object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmStaticData::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmStaticData objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmStaticData::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmStaticData** objectArray = (SgAsmStaticData**) &(SgAsmStaticData::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmStaticData::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmStaticData::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmStaticData found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmStaticData::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmStaticData::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmStaticData** objectArray = (SgAsmStaticData**) &(SgAsmStaticData::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmStaticData::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmStaticData::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmStaticData::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmStaticData);

     return memory;
   }



/* #line 245480 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmSynthesizedDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmSynthesizedDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmSynthesizedDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmSynthesizedDeclaration** objectArray = (SgAsmSynthesizedDeclaration**) &(SgAsmSynthesizedDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmSynthesizedDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmSynthesizedDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmSynthesizedDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmSynthesizedDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmSynthesizedDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmSynthesizedDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmSynthesizedDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmSynthesizedDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmSynthesizedDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmSynthesizedDeclaration** objectArray = (SgAsmSynthesizedDeclaration**) &(SgAsmSynthesizedDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmSynthesizedDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmSynthesizedDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmSynthesizedDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmSynthesizedDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmSynthesizedDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmSynthesizedDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmSynthesizedDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmSynthesizedDeclaration** objectArray = (SgAsmSynthesizedDeclaration**) &(SgAsmSynthesizedDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmSynthesizedDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmSynthesizedDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmSynthesizedDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmSynthesizedDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmSynthesizedDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmSynthesizedDeclaration** objectArray = (SgAsmSynthesizedDeclaration**) &(SgAsmSynthesizedDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmSynthesizedDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmSynthesizedDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmSynthesizedDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmSynthesizedDeclaration);

     return memory;
   }



/* #line 245712 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmFunction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmFunction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmFunction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmFunction** objectArray = (SgAsmFunction**) &(SgAsmFunction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmFunction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmFunction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmFunction::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmFunction*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmFunction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmFunction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmFunction::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmFunction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmFunction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmFunction** objectArray = (SgAsmFunction**) &(SgAsmFunction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmFunction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmFunction::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmFunction object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmFunction object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmFunction::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmFunction objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmFunction::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmFunction** objectArray = (SgAsmFunction**) &(SgAsmFunction::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmFunction::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmFunction::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmFunction found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmFunction::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmFunction::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmFunction** objectArray = (SgAsmFunction**) &(SgAsmFunction::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmFunction::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmFunction::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmFunction::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmFunction);

     return memory;
   }



/* #line 245944 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmSynthesizedDataStructureDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmSynthesizedDataStructureDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmSynthesizedDataStructureDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmSynthesizedDataStructureDeclaration** objectArray = (SgAsmSynthesizedDataStructureDeclaration**) &(SgAsmSynthesizedDataStructureDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmSynthesizedDataStructureDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmSynthesizedDataStructureDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmSynthesizedDataStructureDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmSynthesizedDataStructureDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmSynthesizedDataStructureDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmSynthesizedDataStructureDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmSynthesizedDataStructureDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmSynthesizedDataStructureDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmSynthesizedDataStructureDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmSynthesizedDataStructureDeclaration** objectArray = (SgAsmSynthesizedDataStructureDeclaration**) &(SgAsmSynthesizedDataStructureDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmSynthesizedDataStructureDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmSynthesizedDataStructureDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmSynthesizedDataStructureDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmSynthesizedDataStructureDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmSynthesizedDataStructureDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmSynthesizedDataStructureDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmSynthesizedDataStructureDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmSynthesizedDataStructureDeclaration** objectArray = (SgAsmSynthesizedDataStructureDeclaration**) &(SgAsmSynthesizedDataStructureDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmSynthesizedDataStructureDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmSynthesizedDataStructureDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmSynthesizedDataStructureDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmSynthesizedDataStructureDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmSynthesizedDataStructureDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmSynthesizedDataStructureDeclaration** objectArray = (SgAsmSynthesizedDataStructureDeclaration**) &(SgAsmSynthesizedDataStructureDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmSynthesizedDataStructureDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmSynthesizedDataStructureDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmSynthesizedDataStructureDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmSynthesizedDataStructureDeclaration);

     return memory;
   }



/* #line 246176 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmSynthesizedFieldDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmSynthesizedFieldDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmSynthesizedFieldDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmSynthesizedFieldDeclaration** objectArray = (SgAsmSynthesizedFieldDeclaration**) &(SgAsmSynthesizedFieldDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmSynthesizedFieldDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmSynthesizedFieldDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmSynthesizedFieldDeclaration::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmSynthesizedFieldDeclaration*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmSynthesizedFieldDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmSynthesizedFieldDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmSynthesizedFieldDeclaration::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmSynthesizedFieldDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmSynthesizedFieldDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmSynthesizedFieldDeclaration** objectArray = (SgAsmSynthesizedFieldDeclaration**) &(SgAsmSynthesizedFieldDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmSynthesizedFieldDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmSynthesizedFieldDeclaration::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmSynthesizedFieldDeclaration object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmSynthesizedFieldDeclaration object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmSynthesizedFieldDeclaration::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmSynthesizedFieldDeclaration objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmSynthesizedFieldDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmSynthesizedFieldDeclaration** objectArray = (SgAsmSynthesizedFieldDeclaration**) &(SgAsmSynthesizedFieldDeclaration::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmSynthesizedFieldDeclaration::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmSynthesizedFieldDeclaration::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmSynthesizedFieldDeclaration found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmSynthesizedFieldDeclaration::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmSynthesizedFieldDeclaration::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmSynthesizedFieldDeclaration** objectArray = (SgAsmSynthesizedFieldDeclaration**) &(SgAsmSynthesizedFieldDeclaration::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmSynthesizedFieldDeclaration::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmSynthesizedFieldDeclaration::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmSynthesizedFieldDeclaration::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmSynthesizedFieldDeclaration);

     return memory;
   }



/* #line 246408 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmType** objectArray = (SgAsmType**) &(SgAsmType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmType** objectArray = (SgAsmType**) &(SgAsmType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmType** objectArray = (SgAsmType**) &(SgAsmType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmType** objectArray = (SgAsmType**) &(SgAsmType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmType);

     return memory;
   }



/* #line 246640 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmScalarType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmScalarType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmScalarType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmScalarType** objectArray = (SgAsmScalarType**) &(SgAsmScalarType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmScalarType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmScalarType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmScalarType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmScalarType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmScalarType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmScalarType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmScalarType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmScalarType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmScalarType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmScalarType** objectArray = (SgAsmScalarType**) &(SgAsmScalarType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmScalarType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmScalarType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmScalarType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmScalarType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmScalarType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmScalarType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmScalarType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmScalarType** objectArray = (SgAsmScalarType**) &(SgAsmScalarType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmScalarType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmScalarType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmScalarType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmScalarType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmScalarType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmScalarType** objectArray = (SgAsmScalarType**) &(SgAsmScalarType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmScalarType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmScalarType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmScalarType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmScalarType);

     return memory;
   }



/* #line 246872 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmFloatType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmFloatType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmFloatType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmFloatType** objectArray = (SgAsmFloatType**) &(SgAsmFloatType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmFloatType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmFloatType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmFloatType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmFloatType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmFloatType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmFloatType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmFloatType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmFloatType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmFloatType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmFloatType** objectArray = (SgAsmFloatType**) &(SgAsmFloatType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmFloatType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmFloatType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmFloatType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmFloatType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmFloatType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmFloatType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmFloatType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmFloatType** objectArray = (SgAsmFloatType**) &(SgAsmFloatType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmFloatType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmFloatType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmFloatType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmFloatType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmFloatType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmFloatType** objectArray = (SgAsmFloatType**) &(SgAsmFloatType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmFloatType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmFloatType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmFloatType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmFloatType);

     return memory;
   }



/* #line 247104 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmIntegerType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmIntegerType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmIntegerType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmIntegerType** objectArray = (SgAsmIntegerType**) &(SgAsmIntegerType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmIntegerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmIntegerType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmIntegerType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmIntegerType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmIntegerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmIntegerType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmIntegerType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmIntegerType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmIntegerType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmIntegerType** objectArray = (SgAsmIntegerType**) &(SgAsmIntegerType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmIntegerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmIntegerType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmIntegerType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmIntegerType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmIntegerType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmIntegerType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmIntegerType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmIntegerType** objectArray = (SgAsmIntegerType**) &(SgAsmIntegerType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmIntegerType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmIntegerType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmIntegerType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmIntegerType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmIntegerType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmIntegerType** objectArray = (SgAsmIntegerType**) &(SgAsmIntegerType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmIntegerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmIntegerType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmIntegerType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmIntegerType);

     return memory;
   }



/* #line 247336 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmPointerType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmPointerType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPointerType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPointerType** objectArray = (SgAsmPointerType**) &(SgAsmPointerType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPointerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmPointerType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmPointerType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmPointerType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmPointerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPointerType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmPointerType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPointerType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPointerType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPointerType** objectArray = (SgAsmPointerType**) &(SgAsmPointerType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmPointerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmPointerType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmPointerType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmPointerType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmPointerType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmPointerType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmPointerType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmPointerType** objectArray = (SgAsmPointerType**) &(SgAsmPointerType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmPointerType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmPointerType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmPointerType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPointerType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmPointerType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmPointerType** objectArray = (SgAsmPointerType**) &(SgAsmPointerType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmPointerType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmPointerType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmPointerType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmPointerType);

     return memory;
   }



/* #line 247568 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmVectorType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmVectorType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmVectorType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmVectorType** objectArray = (SgAsmVectorType**) &(SgAsmVectorType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmVectorType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmVectorType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmVectorType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmVectorType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmVectorType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmVectorType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmVectorType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmVectorType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmVectorType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmVectorType** objectArray = (SgAsmVectorType**) &(SgAsmVectorType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmVectorType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmVectorType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmVectorType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmVectorType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmVectorType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmVectorType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmVectorType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmVectorType** objectArray = (SgAsmVectorType**) &(SgAsmVectorType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmVectorType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmVectorType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmVectorType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmVectorType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmVectorType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmVectorType** objectArray = (SgAsmVectorType**) &(SgAsmVectorType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmVectorType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmVectorType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmVectorType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmVectorType);

     return memory;
   }



/* #line 247800 "/workspace/generated/src/frontend/SageIII//Cxx_GrammarTraverseMemoryPool.C" */

/* #line 1 "/workspace/generated/src/ROSETTA/Grammar/grammarTraverseMemoryPool.macro" */
void
SgAsmVoidType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal)
   {
  // This function traverses the memory pool for only a specific IR node 
  // and calls the visit function of the input class execute a traversal 
  // similar to the style of the attribute based traversals within ROSE.
  // This traversal will visit ALL nodes of the AST where as the other 
  // attribute based traversals visit only the embedded tree within the AST.

  // Initialize array to the address of the first element of the STL vector
  // (which is guaranteed to be contiguous storage).
  // SgAsmVoidType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmVoidType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmVoidType** objectArray = (SgAsmVoidType**) &(SgAsmVoidType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();
#if 0
       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmVoidType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (int j=0; j < SgAsmVoidType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                       }
                  }
             }
#else
       // DQ (10/26/2016): To avoid operations in the visit functions that might change the list, 
       // compute the list first and then call the visit function on each list element.

       // printf ("Inside of SgAsmVoidType::traverseMemoryPoolNodes(ROSE_VisitTraversal & traversal) \n");

          std::vector<SgAsmVoidType*> nodeList;

       // Iterate over the memory pools to build the saved list of IR nodes for this type.
          for (unsigned int i=0; i < SgAsmVoidType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmVoidType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         nodeList.push_back(&(objectArray[i][j]));
                       }
                  }
             }

       // Iterate over the saved list
          size_t nodeListSize = nodeList.size();
          for (size_t i=0; i < nodeListSize; i++)
             {
               ROSE_ASSERT(nodeList[i] != NULL);
#if 0
               traversal.visit(nodeList[i]);
#else
               if (nodeList[i]->p_freepointer == IS_VALID_POINTER)
                  {
                    traversal.visit(nodeList[i]);
                  }
#endif
             }
#endif
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }


void
SgAsmVoidType::traverseMemoryPoolVisitorPattern ( ROSE_VisitorPattern & visitor )
   {
  // This function traverses the memory pool for an IR node and
  // calls the function to execute the visitor object.

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmVoidType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmVoidType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmVoidType** objectArray = (SgAsmVoidType**) &(SgAsmVoidType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          for (unsigned int i=0; i < SgAsmVoidType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool
               for (unsigned j=0; j < SgAsmVoidType::pool_size; j++)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                      // printf ("Found a valid SgAsmVoidType object in the memory pool %d at position %d \n",i,j);
                      // printf ("Calling executeVisitorMemberFunction(visitor) \n");
                         objectArray[i][j].executeVisitorMemberFunction(visitor);
                      // printf ("DONE: executeVisitorMemberFunction(visitor) \n");
                       }
                      else
                       {
                      // printf ("Found a INVALID SgAsmVoidType object in the memory pool \n");
                       }
                  }
             }
        }

  // This should not be required since all previously static data members are 
  // now static pointers and ALL IR nodes are allocated from the IR memory pools.
     
   }

void
SgAsmVoidType::visitRepresentativeNode(ROSE_VisitTraversal & traversal)
   {
  // This function visits only a single IR node of the memory pool.
  // it is typically called once for each type of IR node within 
  // the automatically generated function: traverseRepresentativeNodes().

  // Initialize array to the address of the first element of the STL vector
  // (which is guarenteed to be contiguous storage).
  // SgAsmVoidType objectArray [] = *(Memory_Block_List.begin());
     if (SgAsmVoidType::pools.empty() == false)
        {
       // Generate an array of memory pools
          SgAsmVoidType** objectArray = (SgAsmVoidType**) &(SgAsmVoidType::pools[0]);

       // Build a local variable for better performance
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over the memory pools
          bool done = false;
          unsigned i=0;

       // find the first valid IR node, call visit function, and then leave
          while ( done == false && i < SgAsmVoidType::pools.size() )
             {
            // objectArray[i] is a single memory pool
               unsigned j=0;
               while (done == false && j < SgAsmVoidType::pool_size)
                  {
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         traversal.visit(&(objectArray[i][j]));
                         done = true;
                       }
                    j++;
                  }
               i++;
             }

#if 0
       // DQ (10/28/2020): Commented out this output spew that happens in the output of IR node statistics.
          if (done == false)
             {
               printf ("No representative for SgAsmVoidType found in memory pools \n");
             }
#endif
        }
   }


// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmVoidType::numberOfNodes()
   {
  // This function traverses the memory pool for an IR node and
  // counts the number of IR nodes of a particular Sage III IR 
  // nodes type.

     size_t count = 0;
     if (SgAsmVoidType::pools.empty() == false)
        {
       // Generate an array of memory pools (this is actually a STL vector, 
       // but it is contiguious, so OK to treat this way).
          SgAsmVoidType** objectArray = (SgAsmVoidType**) &(SgAsmVoidType::pools[0]);

       // Build a local variable for better performance (make it a loop invariant variable).
          const SgNode* IS_VALID_POINTER = AST_FileIO::IS_VALID_POINTER();

       // Iterate over all of the memory pools for this IR node.
          for (unsigned int i=0; i < SgAsmVoidType::pools.size(); i++)
             {
            // objectArray[i] is a single memory pool, iterate over all the 
            // IR nodes and only count those that are valid IR nodes used in 
            // the AST (i.e. allocated IR nodes).
               for (unsigned j=0; j < SgAsmVoidType::pool_size; j++)
                  {
                 // This is indexing the STL vector of C/C++ style arrays as a doubly 
                 // indexed array access. It is OK since we have leveraged the semantics 
                 // of STL vector memory as contigous and cast the memory as an array
                 // of arrays to use the 2D array indexing.  Hope this is not confusing,
                 // but it s very fast as an implementation.
                    if (objectArray[i][j].p_freepointer == IS_VALID_POINTER)
                       {
                         count++;
                       }
                  }
             }
        }

     

     return count;
   }

// DQ (6/7/2010): Change the return type to size_t to support larger number of IR nodes
// using values that overflow signed values of int.
size_t
SgAsmVoidType::memoryUsage()
   {
  // This function is required because we need the class name as a type when we call sizeof
  // There might be another way to implement this if we have a traversal that only called a 
  // representative object (one call for each type of Sage IIIIR node).
     size_t memory = numberOfNodes() * sizeof(SgAsmVoidType);

     return memory;
   }




